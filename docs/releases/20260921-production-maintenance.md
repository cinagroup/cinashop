# 2026-09-21 生产维护预检结果

本次在用户授权后建立短期只读预检通道，未执行数据库结构、角色、权限或业务记录写入，未切换正式 API/Pages 流量。此记录仅含去标识化计数、结构状态及合成 CI 诊断，不含订单明细、个人信息、数据库凭据或备份。

## 发布结论

**不能合并部署，保留任务分支。** 候选提交 `4f5ea6b003930bf84f8d23e132f3f180e6ecc7e5` 的 [Linux CI 35522518306](https://github.com/cinagroup/cinashop/actions/runs/35522518306) 已以 failure 结束；生产另有未安装协议、结构漂移和最小权限未供应。当前预检工具增量不属于该不可变 CI 提交，不能借用它的局部通过结果作为增量完整验收。

## 生产只读证据

2026-09-21 00:30、00:37、00:47（Asia/Singapore）通过现有 Hyperdrive 检查。数据库为 PostgreSQL 16.14，public 有 263 张普通/分区表；连接身份仍具有 superuser、CREATEDB、CREATEROLE、BYPASSRLS，尚非受限业务身份。

| 协议 | 本次目录观察 | 含义 |
| --- | --- | --- |
| 0150 分佣付款订单保护 | 函数与三项触发器缺失 | 12 条符合保护范围的已付款订单找不到用户，安装器要求显式处理 |
| 0155 后台退款操作凭据 | 表不存在 | 尚未安装，不是已就绪 |
| 0156 后台退款创建凭据 | 表不存在 | 尚未安装，不是已就绪 |
| 0157 发票证据 | fresh | 匹配可安装前态，不是安装完成 |
| 0158 退款拆单凭据 | fresh | 尚未安装；还依赖发票 v2 |
| 0159 线下收银 | drift | 四项共享表指纹不匹配，必须拒绝直接安装 |
| 0160 普通结算计价锁 | 依赖表安全、函数缺失 | 尚未供应函数专用所有者及独立业务授权 |

线下收银的十项共享依赖均存在、由维护身份拥有且通过该目录的表属性检查。`system_config`、`user`、`user_bill`、`user_money` 指纹不匹配；其余六项匹配固定、独立生成的已审核前态指纹。没有把线上指纹重新登记为可接受基线。

进一步读取这四张表的有界索引名称/列名/定义哈希，确认至少缺少以下 19 个前态普通索引，不只是同一索引使用旧名。**这不是其它列、约束和索引差异已全部排除的证明。**

- `system_config`：`system_config_is_store_idx`、`config_tab_id`、`system_config_menu_name_idx`。
- `user`：`user_account_idx`、`spreaduid`、`level`、`user_status_idx`、`work_uid`、`is_promoter`、`user_phone_idx`、`user_delete_time_idx`、`add_time_delete_sex`。
- `user_bill`：`ub_uid_idx`、`ub_status`、`ub_add_time`、`ub_pm`、`ub_cat_type_link_idx`。
- `user_money`：`um_uid_idx`、`um_type_link`。

旧前置检查的九张表共计 65 行，两次观察的服务端聚合指纹一致，孤儿付款用户计数均为 12。指纹仅为这九张表、这两个观察时刻的证据，不代表全库持续无写入，也不是行级备份。新协议检查与旧前置检查采用各自的有界快照，不声称全局原子快照。

## 数据处理的最新用户选择

用户先确认这 12 条均为测试订单，允许“备份并核对关联记录后，仅清理这 12 条及确认属于它们的测试记录”。备份执行在进程启动前被安全审查拦截，原因是需要明确确认含潜在个人信息的本机保存位置。

用户随后明确选择：**“暂不导出，仅保留去标识化审计汇总”。** 按最新选择，不导出记录、不删除订单，不以跳过备份或改用其它目的地绕过限制。尚未部署的私有备份实现、入口和测试已撤回；没有生成订单备份目录或文件。以后若重新处理数据，需要在用户认可的保护方式下重新核对目标及关联范围，不能复用计数作为精确删除清单。

## CI 完整终态

| 分片 | 文件 | 通过断言 | 失败断言 | pending |
| --- | ---: | ---: | ---: | ---: |
| 1/2 | 267 | 3,886 | 1,343 | 70 |
| 2/2 | 266 | 2,528 | 1,299 | 177 |
| 合计 | 533 | 6,414 | 2,642 | 247 |

原生 JSON 报告覆盖 533 个文件，但失败及 pending 均不满足零跳过验收；132 个文件状态非 passed。不能将此表理解为每个文件/断言均成功执行。两片单元及汇总门禁失败；workerd 运行时、独立 PG16 目录审计、Admin/PC/Supplier/Kefu/UniApp 构建、密钥扫描通过。

已定位的集中失败：102 个文件的首项失败消息为测试计价锁供应要求“精确自有回环 PG16 夹具”；专门夹具测试观察到容器内 `inet_server_addr()` 为 `172.18.0.2/32`，而代码要求 `127.0.0.1/32`。客户端端口映射到回环不意味着服务器端地址也是回环。另有前端/路由清单、源码位置、外键数量和完整目录计数断言漂移；它们需要逐项核对，不可直接改快照或放宽保护求通过。此轮未修复这些 CI 失败，未重跑同一已知失败门禁冒充新证据。

## 本次预检增量及清理

复用短期审计 Worker，新增固定 `/release-protocols` GET 入口和 `-ReleaseProtocols` 参数；不接受调用者 SQL/schema/role，不安装任何协议。使用受限超时、只读事务、固定查询、索引输出数量上限和错误脱敏，报告始终明确 `ready=false`。Cloudflare/Wrangler 与 PostgreSQL 最小权限技能用于限定临时入口、短事务及权限隔离边界；本次未供应或变更正式角色。

最终三文件定向测试 113 项通过（2.33 秒），覆盖 HTTP 边界、预检状态/预算/快照失败及迁移注册；不等于全量 CI 通过。新增测试文件后库存为 534 文件、原生两片 267/267，清单 SHA-256 `781a232c3b938c6872b3c7f780958e3898ea62a72b58ac2fb0b4fab51d43af9d`。没有执行该增量的全量 534 文件回归。

撤回私有备份代码后的两套 Worker TypeScript 检查均退出 0。最终 Wrangler 4.122.0 临时审计配置 `deploy --dry-run` 成功，917.80 KiB / gzip 160.63 KiB，仅编译，不上传或改变流量。

三个临时 Worker 均验证匿名 403、错误方法 405、查询参数 404，随后删除并通过 Cloudflare 控制面 404 复核：

- `cinashop-paid-runtime-audit-119da8affa88`
- `cinashop-paid-runtime-audit-570204510139`
- `cinashop-paid-runtime-audit-014dbbe4ee24`

未删除任务分支、正式服务、旧线上部署、数据库对象或业务数据。正式生产源版本与前一份[候选预检](20260921-migration-candidate.md)相同；本轮没有应用发布操作。main 仍停留在 `9fb7d27`，未把未发布分支当作可清理对象。

# 2026-09-21 迁移候选发布预检

用户要求提交、推送、部署和清理。本次整理此前尚未提交的迁移实现、配套前端、测试与审计记录，不把候选保存等同于生产上线。

后续已完成授权后的生产只读预检及候选 CI 终态核对，见[生产维护预检结果](20260921-production-maintenance.md)。下文保留首次候选保存时的观察，不将后续失败改写为通过。

## 验证及当前线上基线

- 本次重新执行两套 Worker TypeScript 检查，均退出 0。
- Wrangler 4.122.0 `deploy --dry-run` 编译成功（8013.25 KiB，gzip 1500.72 KiB），未上传、修改绑定或切换流量。
- 提交前重新执行迁移注册、报告传输、逐文件日志及本地运行器五文件 225 项，全部通过（7.07 秒）。仅修正生成 SQL 与对应 TypeScript 模板的一处尾随空格、另一文件 EOF 空行；注册测试确认生成 SQL 仍逐字一致。
- 校验下载包 SHA-256 后使用 CI 同版 Gitleaks 8.29.0 扫描暂存差异，结果无密钥泄露。唯一初次命中为 Out 注册测试固定幂等 UUID，核对调用用途后只允许该精确值/文件/规则，不关闭扫描规则。
- 最近一轮六文件 171 项定向测试通过；修正后的 533 文件完整回归尚未执行。此前完整回归超时与失败记录继续保留。
- 只读 Cloudflare 控制面确认 API 仍为 `d502bfb7-473c-4591-830c-32ab20579864`（100% 流量），对应源提交 `9fb7d27f99eac6399b0eea7febc97d5035dcec57`。
- Admin/H5/Supplier/Kefu 当前 production 部署源提交仍为上述 `9fb7d27`；PC 为 `58ec07b18bade136cead91e3c080c2181ea5f788`，部署 `c224e558-7b50-4070-a706-cd7cc0fce35f`。商城仍使用 `shop.cinaseek.ai`，后台仍使用 `cinashop-admin.pages.dev`。

## 暂不能切换应用的依赖

新增 0155–0160 外部迁移涉及后台退款协议、发票证据、商品退款拆单、线下收银和普通结算计价锁。普通下单已依赖严格验证的固定锁函数，不能只覆盖 Worker 而跳过匹配协议及权限供应。

本机 Process/User 环境均未配置 `OFFLINE_ORDER_MAINTENANCE_DATABASE_URL` 或 `CHECKOUT_PRICING_MAINTENANCE_DATABASE_URL`，亦无 `DATABASE_URL` / `PRODUCTION_DATABASE_URL`。Cloudflare 部署凭据存在，不等于已供应审核过的数据库维护身份、独立 NOLOGIN 函数所有者及业务权限。此处是凭据存在性和本地发布依赖检查，**不是本次生产数据库目录已检查或协议缺失已证实**。

继续发布前需要显式确认生产数据库维护目标、独立所有者及运行角色，完成只读安装预检与受控迁移/权限验证，再按同一提交协调发布六应用。不得自动调用历史 runAll、清库、删除孤儿订单、扩大业务写权限、放宽协议校验或更换 Hyperdrive 凭据来绕过依赖。

线下 `other_order.type=3` 继续保持 PHP 范围，不新增退款，应付至少 0.01 元。商品订单原有退款不属于线下退款新增。

## 保存及清理边界

候选提交推送到当前任务分支后运行现有 Linux CI；未满足发布依赖时保留该分支，不把未合并/未发布分支当作垃圾删除。本次仅可清理已确认 `.git` 指针文件不存在的 Git worktree 登记；其中旧发布目录仍存在，不删除其目录内容、数据库、缓存诊断或旧线上部署。9 月 15 日遗留的空索引锁已在确认无 Git 进程后移至本任务缓存保留。最终提交号、CI 地址及实际清理结果在任务交付中记录。

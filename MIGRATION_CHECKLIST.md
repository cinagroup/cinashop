# CinaShop PHP → Cloudflare 迁移完成 Checklist

审计起点：`main@55f2652`（2026-08-28）；本文件保留各阶段审计历史，最新状态以上方日期增量为准，旧阶段的“未部署/尚未验证”不自动代表当前状态。PHP 源码仍是功能合同和201表结构参考，Cloudflare 目标为本仓库 `workers-ts` 与五个 TypeScript 前端。2026-09-04 项目所有者确认本部署是全新系统，不承接旧 PHP 站真实历史数据，因此源 MySQL复制、逐行对账和旧对象迁移按“不适用”关闭。2026-09-21 已发布基线为 `main@7a5508f2b0935d7be13f40b4ea0fdb7fd37ed38b`；生产业务/后台已切至独立受限 Hyperdrive，旧 `9748c294e21c49a99579c9cef70102e0` 仅保留维护用途，未绑定回主站。实际客户、provider、完整业务覆盖及正式生产验收仍按开放项推进；当前工作区新增代码仅为未发布的本地候选。

## 审计结论

FE-003C 五旧客户路由代码候选（2026-09-25，本分支未合入／未发布）：按复选框独立核算为 **246 勾选／158 开放／404 总项**，仅 FE-003C 从开放改勾选。旧申请、状态、双域当前记录及员工页按原路径直达，代理商与分销员分别接真实 Worker 合同；独立 PostgreSQL 16.15 五项路由/并发场景、UniApp 工具链 752/752、三端构建和 390×844 H5 合成只读路由烟测通过，详见 `workers-ts/audit/fe003c-h5-local-smoke.md`。本候选合入前仍须最终精确头 GitHub Actions 全绿；FE-003J/K 的真实设备、账号、历史数据、旧客户端原位升级和发布门禁维持开放。远端 `main` 在本 PR 合入前仍为 245／159／404。

主线合入复核（2026-09-24 13:08 UTC）：经批准，[PR #1](https://github.com/cinagroup/cinashop/pull/1) 已以 merge commit `787bee5a13ca2ef19e61b46273f87923b68ae8b7` 合入远端 `main`；从远端 checklist 原文独立核算为 **245 勾选／159 开放／404 总项**。较合入前 `main@7a5508f` 的 240／164／404，新增勾选 FE-001D 全部后台路由逐屏代码审计、FE-001D4 系统设置76屏审计、FE-002B 商品详情字段合同线上只读复验、FE-003H 社区深链本地范围和 FE-003L-A3e 砍价只读规格目录。其他159项保留真实角色、业务链路、渠道或发布门禁，不因新增候选页自动勾选。

完整 Linux 门禁夹具修复增量（2026-09-24，精确代码提交 `72d0bbbdb17f6cb123bac59b7d0c0a7ac3c1d1ed`／完整 Linux CI 11/11 成功、未发布）：`6b1e8aa` 的 [Actions 35987160528](https://github.com/cinagroup/cinashop/actions/runs/35987160528) 已失败，五端构建、workerd、密钥扫描及隔离 PG16 容量审计均成功；Worker 分片1完整执行 6112 项，6108通过／4失败／0跳过。订单六状态失败由合成样本重复使用默认空 `unique` 键触发 `so_unique_uid_uq`，供应商商品退役的3项失败由夹具缺少 `store_product_relation` 表触发；未据此更改业务约束或业务 SQL。分片2在60分钟上限取消，取消前日志未出现失败标记，仍不能算通过。新候选给样本各自独立键、补齐夹具表，并改用 Vitest verbose 及时输出失败断言，保留 JSON、完整分片与零跳过审计；定向 CI 断言5/5、Worker单元类型检查通过。本轮 [完整 Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35993281606) 已绑定精确提交，隔离PG16目录及第二分片303文件4708/4708零跳过均已通过，第一分片303文件6133/6133、零失败／零跳过，完整两分片合计10841项；11项作业全部成功。复选框 **245／159／404** 不变。

领取记录与完整分片预算增量（2026-09-24，精确提交 `64b7c8e`／首轮 Linux CI 失败）：旧 `/admin/marketing/store_coupon_user/index` 的只读领取记录页已接入独立 `coupon_record.view`、禁止缓存的 Worker 列表和 Admin `/marketing/coupon-records`。旧三项筛选、每页15条、ID倒序及十列在本地合同中覆盖，状态与 `is_fail` 分别显示，目标表额外状态3明确标出；折扣百分比按旧显示截整。真实历史记录、受限角色及发布仍待验。逐屏全集为 **candidate 41／partial 121／missing 105／retired 7**；定向测试34/34、Worker双类型及Admin类型检查通过，本机磁盘不足以执行生产构建／原生PG。前一提交 `48bc8c1` 的 [Actions 35983849804](https://github.com/cinagroup/cinashop/actions/runs/35983849804) 第一分片在60分钟作业上限取消，取消前已观察到 `admin-order-generations.test.ts` 20项中1项失败；工作流分片预算增至90分钟，门禁断言5/5通过，后续确认该项为合成数据唯一键冲突，修正与复验见上方。新[完整 Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35990341249) 已绑定 `64b7c8e86244b24a4a7f3f55eb6322fe06e75903`，隔离PG16目录和第二分片303文件4708/4708零跳过已通过；第一分片303文件6129通过／4失败／0跳过，均为上方已修复的两处夹具缺口；复选框仍为 **245／159／404**。

上一轮 Linux 复验（2026-09-24，未发布）：已再次用 `gh api user` 在线确认 CLI 账号为 `cinagroup`。精确提交 `48bc8c1336d174a3165cf6f271a29ecf43563f54` 的 [Actions 35983849804](https://github.com/cinagroup/cinashop/actions/runs/35983849804) 已以失败结束：隔离 PG16 容量审计在合成装载后对 `store_product` 的 NOWAIT 表锁竞争失败；Worker 分片 2 为 4712 通过／5 失败，其中 4 项是砍价自提旧错误提示合同，1 项是查询证据片段因新增关联而过期；分片 1 达到60分钟作业上限被取消，取消前观察到 `admin-order-generations.test.ts` 20项中1项失败。精确修正提交 `6b1e8aa` 保留自提提示、更新实际查询证据，并在临时容量库装载前关闭六表自动清理且继续显式 ANALYZE；另含下方积分日志候选。[完整 Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35987160528) 绑定 `6b1e8aa14c59adeba02902468fec60193af9e693` 并已失败，具体夹具原因和下一轮见上方。当前复选框 **245／159／404**。

后台积分日志增量（2026-09-24，代码/Linux CI 完成／未发布）：旧 `/admin/marketing/user_point/index` 从缺失推进为部分覆盖。新 Admin `/marketing/user-point` 使用独立 `integral_log.view` 只读权限和禁止缓存的 Worker 积分流水／四卡统计接口；固定 `category=integral`，有界分页、会员关键词、时间与类型筛选，四卡按旧 PHP 口径排除指定退款、计算签到与使用积分。新增精确类型筛选及随筛选刷新统计与旧页交互存在差异，旧 Excel 导出、真实历史数据及受限角色验收仍缺，不能称整屏完成。逐屏全集变为 **candidate 40／partial 121／missing 106／retired 7**；定向测试 35/35、Worker 双类型及 Admin 类型检查通过。本机磁盘不足以安全执行 Admin 生产构建，合集完整 Linux CI 结果见最上方；复选框仍为 **245／159／404**。

最新 Linux 门禁复核（2026-09-24，未发布）：GitHub CLI 已恢复 `cinagroup` 登录。订单六状态页精确提交 b61e140 的 [Actions 35976997054](https://github.com/cinagroup/cinashop/actions/runs/35976997054) 中 9 个具体作业成功，Worker 分片 1 因新增测试错误断言单供应商订单 `pid>0` 失败：原生夹具实际合法返回 `pid=0`，失败 JSON 为 6100 项中 1 项失败；506a4eb 已将断言修为 `pid>=0`。用户分组 f992bf7 的 [Actions 35982006698](https://github.com/cinagroup/cinashop/actions/runs/35982006698) 因该确定性测试问题取消；506a4eb 的 [Actions 35982561707](https://github.com/cinagroup/cinashop/actions/runs/35982561707) 又在发现砍价最终结算商品退役缺口后主动取消。前一精确提交 48bc8c1 的 [完整 Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35983849804) 已失败；失败原因及后续修正见上方。该提交包括商城用户分组、订单断言修正及砍价源商品退役边界：平台/供应商软删除与发起/帮砍按同商品事务锁串行，已删除/下架商品的历史参与不再显示可购买，砍价结算最终库存写入重查商品资格，供应商回收购物车先于商品写入；新增 7 条原生 PostgreSQL 行为/竞争用例，本机无可用原生 PG，原生 Linux 结果以当前复验为准。当前复选框仍为 **245／159／404**；真实角色、完整交易和发布验收仍开放。

平台资金流水与砍价并发增量（2026-09-24，代码/Linux CI 完成／未发布）：新增独立 `/finance/capital-flow` 管理页，使用 `capital_flow` 而非用户余额账本；补齐旧页的交易类型、订单/昵称/电话/UID、上海时间范围、20 条分页、流水列及 200 字备注。备注写入在事务中与不含备注内容的管理员日志一并提交，列表参数有界且通配符按字面量搜索。本地 Worker 四文件 15/15、双 TypeScript 检查、Admin 构建及前端日期转换 3/3 通过。精确代码提交 25181d6 的[首轮 Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35968560823)因两份 Admin 审计快照未同步新页面/API 而失败，另有一例独立 PG 运费协议 NOWAIT 锁竞争；已在 dcbddca 同步快照，本地四文件审计 13/13 通过，[完整复验](https://github.com/cinagroup/cinashop/actions/runs/35972181778) 11/11 成功。跨模块逐屏台账中 `/admin/statistic/capital` 从 missing 调为 candidate，该提交快照的全集分布为 **candidate 39／partial 120／missing 108／retired 7**，仍须真实角色浏览器与代表性数据核对。砍价帮砍/取消双顺序新增两条原生 PG16 竞态用例，同文件 11/11 通过；A3f 其他全局锁序、真实渠道与发布验收仍开放。两项均不改变复选框，当前 **245／159／404**。

后台订单六状态增量（2026-09-24，合集 Linux CI 已通过／未发布）：旧订单统计页虽显示八个标签，旧 PHP 仅实际赋值全部、未支付、未发货、待收货、待评价、已完成六数，退款中与已退款两项被注释。新 Admin /order 同屏展示六数，Worker /order/chart 在只读快照中一次聚合，并执行 order.view、无缓存及无筛选参数门禁；图表与分页请求分离，失败可独立重试。新口径是跨平台／门店／供应商的未删除当前履约单，包含拆单子单但不含支付父单，与旧默认仅平台、pid 0/-1 且可能含用户删除行不同，仍须代表性历史数据对照，逐屏分类保持 partial。精确提交 b61e140 的 Worker 双类型、Admin 生产构建、前端 50/50、相关审计与权限定向项、桌面及 390px 隔离浏览器交互通过；[首轮精确提交 Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35976997054)因一条夹具断言失败；修正和复验见最上方，复选框仍为 245／159／404。

后台商城用户分组增量（2026-09-24，合集 Linux CI 已通过／未发布）：旧 `/admin/user/group` 此前整屏缺失，新 Admin `/user/groups` 已按旧页每页10条接回目录、添加、编辑、删除，并提供名称查询、占用分组删除提示和受限角色只读视图；沿用 Worker 已有商城 `user_group` 服务与 `user.view`／`user.manage` 门禁，三项分组响应另加 `private, no-store`。旧表单限20字，新页及Worker限64字，已在台账标为待运营确认的差异。精确提交 f992bf7 的 Admin生产构建、Worker双类型、定向7文件27/27、Admin API 357/357注册及本地mock桌面／390px浏览器交互（14请求、零console错误／横向溢出）通过。逐屏台账把该路由从missing调为candidate，当前全集为 **candidate 40／partial 120／missing 107／retired 7**；生产真实分组、受限角色及发布未验，复选框仍为 **245／159／404**。

当前本地候选进度（2026-09-24，未发布）：**245 勾选／159 开放／404 总项**，分母未变化。旧 UniApp 151 条路由唯一归档，现有目标 88 页、原路径直达 23、兼容 100、显式缺口 28；FE-003D 新人／排行／直播及 FE-003B 协议目录／正文旧入口均有本地候选，排行活动身份和独立会员协议现已补齐本地候选。活动交易、账号注销、会员码扫码和真实渠道仍未验收。代客支付的回调先到并发守卫已通过四组原生 PG 去重 121/121；真实 Provider 和旧客户端原位升级证据仍缺。砍价 A3e 本机 PostgreSQL 30/30 已过；GitHub CLI 已恢复登录，[Linux CI 首跑](https://github.com/cinagroup/cinashop/actions/runs/35941614593) 的原生 PG16 目录 30/30 且零跳过，但两分片另有旧夹具/断言失败，密钥扫描误报，完整门禁失败。修正候选 a6922089 的 [Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35946334384) 11/11 门禁成功；砍价目录 30/30、零跳过，A3e 已勾选。系统设置 FE-001D4 的 76/76 与 FE-001D 的 274/274 逐屏代码审计均经自身 Linux CI 收口，功能缺口留在 FE-001 等功能父项。局部代码、路由和测试进展不会自动使跨渠道整项勾选。

后台逐屏代码审计历史快照（截至 b1dc6ac，2026-09-24，代码/Linux完成／未发布）：11份语义台账按权威清单覆盖274/274条旧Admin业务页，candidate 38、partial 120、missing 109、retired 7；全集去重测试缺失/重复/越界均为0。本机13文件62项、语义修订后定向5文件22项及Worker双类型检查通过；精确提交`b1dc6ac6f9f54dd2554b4b7b2ebc8c145c10f6c6`的[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35962566501) 11/11成功，两片合计599文件10,796项、零跳过，包含274路由全集断言与原生PG砍价规则并发。此前营销48屏与首批砍价状态修复亦在`648e025b`的[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35956362112) 11/11通过。FE-001D仅按逐屏代码审计范围勾选；109屏缺失、120屏部分覆盖、真实角色/生产数据与发布验收仍归FE-001等功能项及FE-001G/H。

旧客户端原位升级证据复核（2026-09-24，只读）：新版UniApp manifest 的渠道AppID为空；旧源码 manifest 的AppID与3.1.1版本只是源码字面量，不能证明线上发布身份。仓库未发现APK/AAB/IPA或签名文件，GitHub Releases为空，140条Actions artifact元数据中未发现明确的客户端包候选。线上AppID/包名/签名指纹、各渠道最高已发布版本及旧包实际API域名仍无法核验，强制升级门禁继续开放。

商品详情 FE-002B 部署后只读验收（2026-09-24）：当前 PC Production 固定部署 b4d86172 对应 7a5508f，包含已通过 Linux CI 的详情修复 9308448。公开别名、固定部署和实时商品 API 对照后，无头 Edge 实页显示真实标题、价格、无有效规格的禁购提示及两个禁用按钮，零控制台/网络错误；证据见[线上复核](workers-ts/audit/pc-product-detail-live-recheck-20260924.json)。本项由开放改为勾选，计数为 **244／160／404**；商品素材、真实SKU交易和完整发布验收仍开放。

排行／会员协议／注销边界增量（2026-09-24，本地候选／未发布）：排行 API 为当前页商品补权威活动 ID、类型和有效期只读投影，UniApp 仅对与商品/优先级匹配、未过期的目标跳转；原生 PG 排行 4/4、前端运行时 7/7、既有推荐 16/16 通过。公开 `/agreement/:type` 已解除误指向用户协议的别名，独立返回会员/推广协议；与旧 PHP 一致，公开接口即使 `status=0` 仍按类型返回记录，`payVip` 严格校验类型和结构后显示其正文，会员主页另行隐藏停用协议。旧 PHP 注销用 `delete_time` 软删除，本地已使登录、鉴权和社交手机号认领同时检查 `is_del` 与 `delete_time`，软删除 SQL 专项 3/3、相关登录/微信回归 20/20 通过；旧会员码/扫码链另见[恢复合同](workers-ts/docs/member-code-recovery.md)，注销完整写入与资金处置另见[注销边界](workers-ts/docs/account-cancellation-boundary.md)。这些属于 FE-003B/D 的局部收口，真实数据、订单结算、渠道身份、真机、Linux CI 和发布未验，复选框当前为 **243／161／404**。

新人／排行／微信直播路由收口（2026-09-24，前一阶段本地候选／未发布）：旧活动商品深链按 type=7／1／3／6 分别精确进入新人、秒杀、拼团、预售详情；无效、重复及不支持参数失败关闭。新人券未知类型／范围不伪称折扣或指定范围。商品排行三榜使用公开权威排序接口，每页 10 件；当时仅引导查看普通详情，活动身份后来由上方排行增量补齐。微信直播旧列表每页 10 间，仅微信小程序请求并可进入直播插件，H5／App 展示平台限制；真实插件未验收。排行专项 6/6、新人深链专项 12/12、合并 UniApp 工具链 733/733、路由台账 15/15、类型检查、H5／微信小程序／App 三端构建及产物检查 3/3 通过。真实运营内容、登录账号、直播插件、真机、活动结算、自身 CI 与发布仍待验，FE-003D 不勾选；未连接生产、提交、推送或部署。

代客支付回调先到并发栅栏收口（2026-09-24，本地候选／未发布）：回调登记、订单变更、渠道声明与查单结算共用订单粒度 PostgreSQL 事务 advisory lock；查单成功先在既有对账案件登记交易 T2，再结算，若已存在回调 T1 或竞争中出现冲突则不入账、不覆盖事件。商城、充值、会员付款事务在订单行锁后持有边界与案件锁至提交；人工 `ACCEPT_LOCAL` 对三类订单使用 `NOWAIT` 避免反向等待。精确 `xx` 离线单号维持独立锁协议。四座独立 PG16.15 集群最终为完整代客流程 49/49、并发及正向查单 4/4、对账 35/35、离线回调 33/33，共 **121 个去重原生用例**；另有对账文本单测 16/16、Worker unit/runtime 类型和空白检查通过。每座最终集群夹具余留 0、`pg_ctl` 停机、无 PID 或监听；阶段性失败及未进入测试的建库超时已在[支付回调审计](workers-ts/audit/assisted-payment-callback-boundary-20260924.json)单列，不计为通过。真实微信／支付宝签名回调、Provider 未知结果、历史订单号域、线上旧小程序／App 的包身份签名版本与实际 API 源仍未验收，FE-003G 不勾选；无生产连接、提交、推送或部署。

用户协议与隐私页增量（2026-09-24，前一阶段本地只读候选／未发布）：旧 `/pages/users/privacy/index` 映射到新的协议正文页，保留 `type=user/privacy/cancel/supplier` 精确白名单；旧三项协议目录映射到新列表页。公开 Worker `/user_agreement/:type` 按原内容键返回，客户端核验响应类型并在 `rich-text` 前清理危险标记；当时缺失、`payVip` 和未知类型零请求报错。旧 `payVip` 应走 PHP 独立 `/agreement/:type` 的会员说明，当时 Worker 同路径误设用户协议别名，后来由上方会员协议增量修正；账号注销的 `cancel/user` 写合同与会员码 type=90 二维码／扫码承接仍缺，保持显式缺口。该阶段协议专项 5/5、Worker 既有内容合同 6/6、合并 UniApp 工具链 738/738、路由台账 16/16、类型、H5／微信小程序／App 三端构建与产物 3/3 通过。真实发布内容、身份、真机和发布未验，FE-003B 不勾选；未连接生产、提交、推送或部署。

社区深链整项收口（2026-09-23，本地候选／未发布）：FE-003H 的三条旧查询深链与 App 条件分支已实现，详情见下方勾选项和[前端路由审计](workers-ts/audit/uniapp-frontend-parity.json)。本地测试、三端构建及 H5 页面交互通过；真实视频播放、真机和生产数据仍归 FE-003J/K。该阶段复选框计数为 **241 项勾选／163 项开放／404 项总计**；A3e 后续收口后的当前计数见本文开头。此前 240／164／404 长期不变，是因增量集中于尚未满足完整验收条件的 FE-003G；本次仅在 FE-003H 原范围完成后增加一个勾选项。未连接生产、提交、推送或部署。

反馈与企业 Work 六页增量（2026-09-24，本地候选／未发布）：反馈旧页和五条企业 Work 旧路由已注册，分别接入已鉴权反馈 API、独立于商城登录的企业微信上下文授权与只读客户／订单／群／商品记录。无提示目标由 SDK 发现；OAuth 回跳交换前／激活前、五页私有读取提交前、订单／足迹／群分页请求及响应、群成员点击和商品推送均复核当前会话，变化时清除内存凭证与私有资料；SDK 签名配置按无 hash URL 缓存，目标本身不缓存。带 userid 的群成员跳转由后端员工跟进关系验权，显式目标沿客户→订单→订单详情→足迹保留；持续可见且没有任何页面事件的聊天切换仍不能保证即时清屏。此六页增量使目标页 76→82、旧路由直达 14→20、缺口 39→33；随后新人页增量另列。真实 SDK 目标切换专项 13/13、合并后的 UniApp 工具链 725/725、类型、H5／微信小程序／App 构建及产物 3/3、浏览器匿名反馈和 Work 无 SDK 安全错误态已验证；真实企业微信 SDK／员工权限、登录反馈提交、真机与生产数据仍待验。FE-003G 整项仍开放，本段当时的复选框计数 **241／163／404** 不变；线上小程序／App 原位升级身份与签名证据当前未提供，发布门禁保持。未连接生产、提交、推送或部署。

新人专享旧入口增量（2026-09-24，本地只读候选／未发布）：`/pages/activity/new_customer/index` 已原路径注册，另增活动 ID 的新人详情页；首页 newVip 与聚合活动页可进入，目录每页 9 件、授权返回、错误同页重试和迟到响应隔离，详情仅显示活动规格与价格，不把 type=7 导向普通商品购买。Worker `/marketing/newcomer/info` 原先直接返回 camelCase 用户券行，与 PHP 整理后的旧字段合同不一致；现复用用户券钱包投影补 `coupon_price/use_min_price/coupon_type/applicable_type`，缺失模板范围标记未知。新人页面专项 10/10、Worker 相关 12/12 与类型检查、合并 UniApp 工具链 725/725、三端构建／产物 3/3、路由审计与 14/14 台账回归通过。本地 H5 桌面仅验证请求失败／重试和无效详情 ID 的渲染；移动视口被浏览器安全检查阻断，未绕过。type=7 加购结算、真实账号／运营数据、真机、自身 CI 和发布仍开放，FE-003D 不勾选；该阶段路由台账为 84 页／21 直达／32 缺口；后续增量以上方当前计数为准。无生产连接、提交、推送或部署。

代客扫码一次性发起门禁（2026-09-23，本地候选／未发布）：微信／支付宝在本地确定性校验后、外部请求或签名 URL 交付前，于短 PostgreSQL 事务中持久声明本订单的一次支付意图；订单行锁下重核代客归属、未付状态和整数分金额，提交后才出事务接触 Provider。普通会员支付入口也对代客订单执行同一声明，微信使用实际 `wechat/routine/app` profile；旧支付宝 `pay_key` 复用首次签名 URL，不再次声明。已发起意图或已记录的回调交易证据会阻止后续改价、本地收款、取消及超时自动取消，定时扫描跳过待对账代客订单。最终原生 PG16.15 **43/43**、夹具余留 0 且隔离集群停止，相关 Vitest 六文件 **34/34**、另有 Kefu 迁移文件 **10/10**、Worker unit/runtime 类型及空白检查通过；Kefu 初跑 9/10 揭示内嵌 0103 SQL 的 CRLF 与外部 LF 不同，已仅规范该段内嵌文本换行并复跑通过。Provider 在测试中为合成，真实渠道与回调仍未验收；历史／旁路“回调先到”事务若尚未提交，当前 `READ COMMITTED` 的跨渠道声明或本地订单变更仍可能看不到其证据；回调注册与订单变更的订单粒度协调、未知结果人工对账／重发协议继续作为发布门禁。FE-003G 与 240／164／404 不变；无生产连接、提交推送部署。

代客收银浏览器复验与强制升级预检（2026-09-23，本地隔离候选／未发布）：本会话的应用内浏览器控制恢复；在 `127.0.0.1` 本机最终 H5 构建和合成 Admin/API 上，完成管理员登录→本人记录→收银的实际导航。扫码页仅在显式点击后出现本机二维码，付款状态可再次读取；零元订单取消二次确认时付款 POST 为 0，确认后仅 1 次 POST（`expected_pay_price=0.00`），页面显示已付且没有二维码。桌面及 390px 移动视口无水平溢出，控制台 0 error、1 条既有 Vue Router 弃用警告；预览和测试标签已关闭。合成身份、金额和支付响应不代表真实 Admin/JWT/Provider、回调或真机验收。新增显式离线发布元数据预检及 **8/8** 测试，核对渠道记录与源码／构建可见的身份、版本和 API 地址字面量，并要求记录签名指纹与平台升级资格证据引用；它不验签、不证明编译资源的实际请求去向或渠道升级资格。当前空 AppID、微信 `touristappid` 和缺少已发布证据会阻断预检，真实签名包与旧新客户端验收仍须另做。FE-003G 和 240／164／404 不变；无生产连接、提交推送部署。

旧客户端原位升级发布门禁（2026-09-23，所有者确认线上小程序与 App 均已有用户）：新版 manifest 的 DCloud／微信标识当前为空，版本 `0.1.0(100)` 低于旧源码 `3.1.1(311)`；旧源码的 DCloud ID `__UNI__70A74E1`、小程序 AppID `wxde839b88fb8fce3f` 和默认 API `https://demo.crmeb.com` 仅是代码线索。2026-09-24 对两个工作区的只读查找未发现已签名安装包或渠道发布记录，现有发布记录也明确未包含 App／小程序；所有者暂无线上身份、签名和版本证据。公开代客列表的 HTTP 400 提示只能限制抵达新 Worker 的旧记录请求，不能把已安装小程序／App 整体强制升级。发布前必须核验线上真实 AppID、包名／Bundle ID、签名连续性、最高已发布版本及实际 API 目的地，设置可原位升级的新包并经相应渠道发布，再用旧包／新包实物验证阻断与通行；未知标识或签名不能凭源码猜填。此门禁未满足，不能全量部署或关闭 FE-003G。

代客收银页面增量（2026-09-23，本地候选／未部署）：旧 `/pages/behalf/cashier/index` 已注册，目标页 71→72、直接注册旧路由 10→11、未映射／跨界面 43→42，旧 151 路由仍完整记账。独立 Admin 身份下先查本人订单付款状态与最小详情，再由管理员显式发起现金、零元或扫码；零元代客按旧 PHP 可结算，但须确认且不生成二维码，不能套用仅针对 type=3 线下消费的 0.01 元政策。所有付款请求带 `expected_pay_price`；现金／零元在真实订单行锁后按整数分精确比较，不匹配时不支付、不写 outbox。原生 PG16 flow 首轮 37/39（两例新增并发测试在 HTTP 入锁前轮询超时），修正测试同步钩子后独立重跑 39/39、夹具余留0且集群停机；Worker 双类型通过。前端收银定点 15/15、工具链 693/693、类型、三端构建与产物 3/3 通过，本机二维码采用整数像素无重叠绘制。Provider 重复发起／金额竞态、真实回调、真实 Admin／设备浏览器仍是发布门禁。根会话浏览器／电脑操作入口重置后仍报 `kernel assets … os error 3`；独立子会话 IAB 在本地最终 H5 构建上完成桌面和 390px 移动匿名收银守卫→管理员登录导航，控制台 0 error、1 条既有 Vue Router 弃用警告，无水平溢出；浏览器安全检查拒绝网络请求审计，不能声称无外发，也没有已登录收银／真机验收。FE-003G 与 240／164／404 不变，无生产连接、提交推送部署。

代客旧客户端门禁修正（2026-09-23，本地候选）：所有者选择强制升级后，公开 `GET /api/admin/order/place/list` 已改为在管理员鉴权之后、查询之前只接受唯一的 `paging=cursor`；旧 `page/limit`、缺失或重复分页参数返回 HTTP 400 与明确升级提示，并禁止缓存。下段“旧数组合同暂保留”仅指内部服务代码，不代表旧客户端还能通过公开路由读取列表。此门禁并不等于 H5／小程序／App 整包已强制更新；分发版本与旧包阻断仍需发布前验收。

代客订单列表规模增量（2026-09-23，本地候选／未部署）：本人代客订单新增显式 `paging=cursor`，以 `add_time DESC, id DESC` 的完整位置分页、每页前瞻一条，并在每页重施创建管理员、代客渠道与删除状态条件；旧 `page/limit` 数组合同暂保留，但最多跳过 10,000 行，不能作为深页方案。新增与等值作用域和排序一致的 `so_assisted_actor_list` 外部 0165／内嵌 0171 索引；普通事务内建索引仅允许目标表不超过 100,000 行，超过时拒绝并须另行规划在线维护。原先 OR 游标在 3 万同管理员行中虽用索引却过滤 10,001 条前缀；已改为 `(add_time,id)` 行值比较，原生 PG16 generic/custom 计划均下推完整 `Index Cond`，前缀过滤降至 0；两文件 29/29、0 跳过，私有集群余留0且确认停机。20 万无关管理员行的首屏计划也通过，但关键词和部分状态过滤仍可能扫描／排序，不把局部耗时冒充生产容量验收。正式库表规模、超过阈值的在线安装、真实 Admin／浏览器及发布仍开放，FE-003G 与 240／164／404 不变。所有者已选旧 PHP 版 UniApp **强制升级**，不恢复 `refund/cartInfo` 敏感整行；实际版本拦截／分发验收仍是发布门禁。本增量未连接生产、提交推送部署。

代客只读详情与会话到期增量（2026-09-23，本地最终测试通过／未部署）：新增 `order.assisted` 管理员归属的付款根单最小详情与 71 页中的新只读页面；列表改为字段白名单，不再返回收货／购物车／退款整行。付款和表单读取只接受精确订单号，旧三位前缀仅在只读轮询中兼容；精确号属于他人或已软删时不回落到别名，最长 36 字符旧轮询号及 37 字符拒绝已用原生 PG 验证。Admin 会话自然到期自动清屏，旧定时器不能清新登录；最终 UniApp 工具链 **674/674**、三端构建／产物3项、Worker静态21项及双类型通过，两座独立 PG16.15 分别 **33/33、37/37** 通过、0 跳过、夹具余留0且独立确认停机。严格原号过渡版曾 32/33 出现一次未复现的上传前“订单已过期”，诊断重跑通过但不把红测计绿。计时器变更前 IAB 验证了匿名守卫及导航，最终版 IAB 不可用，真实 Admin／平台浏览器验收未完成；旧 PHP 客户端记录页与新私有列表不兼容，升级或安全版本化摘要待决，列表规模索引／深分页、图片生命周期、收银与真实支付仍开放。新详情非旧 PHP 直达路由，直接映射仍10条；FE-003G 与 240完成／164开放／404总项不变，无生产连接、提交推送部署或分支清理。见[代客运行审计](workers-ts/docs/assisted-purchase-runtime.md)和[前端路由证据](workers-ts/audit/uniapp-frontend-parity.json)。

预售积分封顶整链补验（2026-09-23，本地通过／未部署）：按所有者“允许积分抵扣，报价、实际扣减及未付取消返还一致”的规则，增加百分比封顶与非整数兑换率的原生 PG 回归；报价抵 ¥0.54／36 分，锁后扣分和订单／商品账本一致，事后关闭积分配置仍按原扣 36 分返还。最终 `presale-checkout-postgres` **61/61** 通过、0 跳过；独立测试实例夹具余留0、停机核验通过。既有业务实现不变，不把早前 73 项跨文件复验或前端历史结果重复计入；累计限购、已付款退款与真实 Provider 门禁仍开放，240／164／404不变。见[预售交易审计](workers-ts/docs/presale-transaction-audit.md)。

代客表单跨请求一致性增量（2026-09-23，本地验证／未部署）：报价确认指纹现绑定权威启用模板的 ID、名称与解析后的内容，建单在事务内先锁模板再核对凭证；纯名称／标题编辑而原答案仍合法时，旧凭证也要求重报价。普通代客商品库存更新新增 `system_form_id` 条件守卫，封住模板锁后商品改绑窗口；预售路径原有守卫不变。三座全新独立 PG16.15 集群依次 30／31／32 项通过，最终 32 项含两种纯文字编辑、快检与行锁间编辑、跨连接商品改绑、回滚及同 key 新凭证成功，夹具余留 0 且独立核验停机。扩展普通确认测试初跑 146 失败／54 通过／111 跳过，最小复现为 PGlite 无正式原购凭据／计价锁协议；现已将真实建单组限定在独立 PG16，轻量凭证 21 项通过／268 项明确跳过、独立原生 289 项全部通过，不能把轻量跳过当绿测。原 77 项代客历史套件单文件运行触及既有十分钟上限 `ETIMEDOUT`，一个合成夹具库保留在已停机诊断目录；随后不改断言或时限，将其精确拆为 40＋37 两入口，分别在全新 PG16 集群 **40/40、37/37** 通过，夹具余留 0、停机独立核验。Worker 双类型、相关单元 22 项、runner 边界 111 项及空白检查通过；本轮去重业务／单元 420 项、另 111 项 runner 通过。只读审计还确认现有 R2 key-only 清理队列不能用于未使用／结果未知图片，且代客详情须独立管理员归属投影、收银仍缺前端；ABA 内容恢复、图片生命周期、详情／收银及真实平台验收继续开放。FE-003G 和 240／164／404不变；无生产连接、DDL／权限／绑定、提交推送部署或分支清理。见[代客运行审计](workers-ts/docs/assisted-purchase-runtime.md)。

代客表单页面与恢复增量（2026-09-23，本地通过／浏览器未验收／未部署）：确认页已接十类权威表单、独立 Admin 图片上传及限当前结算的只读预览续签。持久恢复仅保存最小答案和 canonical 图片引用；未知建单结果按原内容重试，模板变动清空答案，原生选图返回保留答案但重新报价。修复共享时间范围丢失第一次选择及日期范围只填一端的问题。最终去重 **1,056 项通过（新增47、无失败／跳过）**，含33项原生、649项移动工具链、294项PC、68项后端及12项产物／依赖检查；两端类型、Worker双类型和构建通过，两座私有集群独立确认停止且夹具余留0。浏览器仍因 `kernel assets … os error 3` 无法初始化，六项渲染检查未验收，未静默使用替代工具。前端／Workers／PostgreSQL技能约束了生命周期、身份与短事务。修正 FE-003G 的旧描述，当前70注册页；跨请求模板版本、图片生命周期、完整详情／收银及真实平台验收仍开放，240／164／404不变。无生产、DDL／授权计划／绑定／依赖、提交推送部署变更。见[本轮完整审计与源码摘要](workers-ts/docs/assisted-purchase-runtime.md)。

预售积分规则复验（2026-09-23 15:29，本地通过／未部署）：再次确认允许预售积分抵扣，现有报价、锁后扣减和未付取消原额返还实现不变。两个原生 PostgreSQL 文件 73 项全部通过、无失败／跳过，Worker 双类型及空白检查通过；真实跨连接验证防重复扣返和付款先提交拒绝取消。独立临时集群夹具余留 0，停机后再次确认无 PID、引导口令或监听。本轮仅更新审计记录，未新增用例，不累计历史前端／浏览器结果；不涉及生产连接、提交推送部署或累计限购政策，240／164／404 不变。见[本次精确结果及源码摘要](workers-ts/docs/presale-transaction-audit.md)。

代客表单图片消费／读取增量（2026-09-23，本地通过／页面未接通／未部署）：建单只接受管理员、买家／游客、本次结算及表单共同绑定的独立图片，拒绝借用会员库和 URL 绕过；服务端生成归属快照并在原事务锁定附件。新增原管理员授权的订单表单读取，会员及实际拆单详情追溯精确付款根单，异常归属不签图。真实游客拆单测试发现通知 UID=0 拒绝导致回滚，已修复为订单来源双重校验、仅保留配置短信，不读取共享会员／微信身份或写 UID=0 站内消息。最终原生114、单元／权限194，共 **308 项通过（新增39、无跳过）**，双Worker类型、空白检查及19文件摘要复核通过；四座本机测试集群余留0且独立确认停止，失败过程留档。Workers／PostgreSQL技能用于事务内归属和独立运行身份；云传输／支付转换为合成，不等于实际平台或浏览器验收。页面控件、答案恢复、草稿续签／图片生命周期及跨请求模板版本仍开放，FE-003G和240／164／404不变。无DDL／权限计划／绑定／页面或生产变更，未提交推送部署。见[最新审计](workers-ts/docs/assisted-purchase-runtime.md)与[机器证据](workers-ts/audit/assisted-form-consumption-20260923.json)。

代客表单图片上传前置增量（2026-09-23，本地通过／端到端未完成／未部署）：新增 `order.assisted` 管理员鉴权的专用上传路由，仍使用应用 LOGIN；图片独立于后台素材／会员／游客聊天库，持久摘要绑定管理员、会员或游客、本次结算及商品表单。先验结算后读取有界 multipart，R2 写完再在短事务内重验并锁定归属；明确拒绝补偿对象，清理失败入队，未知提交结果（含 40003）不误删。最终原生77、单元／权限70，共 **147 项通过（新增52、无跳过）**，双Worker类型及空白检查通过；首轮77仅作中间证据，两座本机集群余留0且独立确认停止。Workers／PostgreSQL技能用于外部I/O隔离和未知结果处理；R2／KV／Queue为合成，不等于平台或浏览器验收。新附件的建单消费、不可变订单／拆单读取、草稿续签与生命周期、页面及答案恢复仍未接通，页面保持表单禁用保护；FE-003G与240／164／404不变。没有DDL／授权计划／绑定／页面或生产变更，未提交推送部署。见[最新审计](workers-ts/docs/assisted-purchase-runtime.md)及[机器证据](workers-ts/audit/assisted-form-uploads-20260923.json)。

代客商品表单服务增量（2026-09-23，本地通过／端到端未完成／未部署）：确认／重算在原应用 LOGIN 只读快照内返回商品绑定的启用模板；建单转交答案，十类组件及旧时间戳键对象统一校验，快照与收集同事务。创建用既有行锁权限 `FOR SHARE NOWAIT` 固定模板到提交；私有图片限定会员本人及正确命名空间，游客 UID=0 不借用或签署零归属图片。仅明确提交前表单／报价拒绝携带可纠正标记，SQL／锁／提交后回读错误保持原请求恢复。最终目标原生65、本地合同65、客户端76，共 **206 项通过（新增26、无跳过）**，双Worker／双客户端类型及空白检查通过；换行断言、错误夹具身份保护及修正重跑均留档，三座本机集群余留0且独立确认停止。PostgreSQL／Workers技能用于有界读取与短事务锁边界；没有DDL／权限／绑定／页面或生产变更，未提交推送部署。代客上传隔离、表单控件和答案持久恢复、跨请求模板版本、浏览器／provider仍开放；页面保留必填表单禁用保护，FE-003G及240／164／404不变。详见[最新审计](workers-ts/docs/assisted-purchase-runtime.md)和[机器证据](workers-ts/audit/assisted-system-forms-20260923.json)。

代客确认页面／持久恢复增量（2026-09-23，本地通过／浏览器阻塞／未部署）：恢复旧确认订单路由，接通会员保存地址、city_area 手填级联、自提、候选券／积分、完整报价和显式原生确认建单，不自动收款。按管理员持久保存原 key／报价凭据／提交内容，页面披露本机收货信息留存；写入／回读失败不发单，未知结果只重试原内容，服务器核验后可清除。选客／购物车写入在边界检查恢复锁，换买家立即废弃旧报价；系统表单未接通时禁止建单。最终工具链612、三端真实构建依赖审计9、产物3、导航17，共 **641 个不同用例通过（新增47、无跳过）**，类型／空白检查通过；首轮半成品语法错误及修正留档。浏览器初始化仍报 kernel assets／os error 3，0渲染验收，未静默回退。目标页69→70，旧直达9→10，未映射44→43，仅为路由与本地客户端合同进展；FE-003G、240／164／404保持不变。收银／表单、完整券和大目录、跨标签协调、异常记录人工处置及真实账号／provider／浏览器仍开放。无后端业务／DDL／权限／绑定变更、生产连接或提交推送部署。详见[最新审计](workers-ts/docs/assisted-purchase-runtime.md)和[最终证据](workers-ts/audit/uniapp-frontend-parity.json)。

代客确认报价前置增量（2026-09-23，本地通过／未部署）：修复确认展示与报价分开读取、重算缺完整信息、手填地址未返回和积分开关固定关闭。商品／地址／SQL计价在应用LOGIN只读可重复读事务中读取，KV首单配置及支付可用性在事务外加载，凭证在结束后写入；首单KV不冒充SQL同快照。重算保留旧平铺金额并返回完整确认与版本化选择元数据，报价／建单支付方式一致、自提门店提前校验。最终原生代客37／共同凭证27／地址58、预售回归60及合同4，共 **186 个不同用例通过（新增9、无跳过）**，双Worker类型通过。首轮95通过／2项测试断言失败及修正、测试替身类型修正均留档；两个私有集群独立确认停止且余留0。Workers／PostgreSQL技能推动短只读事务和真实跨连接并发验证；无DDL／权限计划／绑定／前端页面变更，无生产连接、提交推送部署。确认／收银UI、表单、完整券展示、冻结建单恢复及浏览器等仍开放；修正文中代客缺页4→2的陈旧描述，目标页69及240／164／404不变。详见[本轮审计](workers-ts/docs/assisted-purchase-runtime.md)与[最终源码／运行证据](workers-ts/audit/assisted-checkout-preview-20260923.json)。

代客选客／选品／购物车增量（2026-09-23，本地通过／浏览器未验收／未部署）：恢复旧选客和商品列表两个路由及个人中心入口；独立 Admin 登录、权限分离、会员搜索分页与游客安全随机标识、真实 SKU、会员／游客范围购物车加改删均接通。价格明确为参考价，预售不混入普通代客购物车；URL 买家不能独立授权，隐藏清会员资料，旧响应不能跨身份。累加加购不自动重试，在途锁跨隐藏保留，未确认结果要求重新读取及人工核对；不宣称持久幂等或超时取消。最终前端工具链565、三端依赖审计9、产物3、导航17，共 **594 个不同用例通过（新增53、无跳过）**，类型与三端构建通过。记录了旧产物两项失败及 H5 首页别名断言修正，最终精确校验实际69页构建；保留MP既有空chunk警告。目标页67→69，直达7→9，未映射46→44，仅路由账本进展；确认订单／收银、分类及旧筛选、真实登录／provider／浏览器仍开放。前端测试技能要求渲染证据；前序插件初始化故障未解决，0页面／截图／交互验收，未静默使用Playwright。游客和恢复锁仅内存、App游客随机能力、大购物车分页及多标签边界明确保留；预售积分规则不变。无后端／DDL／权限计划／绑定变更，无生产连接或提交推送部署；FE-003G及240／164／404不变。详见[代客最新审计](workers-ts/docs/assisted-purchase-runtime.md)和[源码／运行证据](workers-ts/audit/uniapp-frontend-parity.json)。

代客选客前置读取增量（2026-09-23，本地通过／未部署）：修复共用用户列表直接返回整行（含密码摘要等非列表字段）、零值分页导致无限制读取及搜索被忽略的问题。现在 SQL 仅取 12 个必要字段，提供 4 个模板别名；分页／过滤有界、UID 降序、两种软删除标记均排除，重复／未知参数拒绝，响应禁止缓存。昵称／手机号按字面子串、UID 精确搜索，不照搬 PHP 的 UID 模糊及未迁移高级筛选。保留 `user.view` 与 `order.assisted` 权限分离，真实 JWT／独立 LOGIN 验证双路径访问及列权限最小投影；只读事务实际拒写、5 秒上限及更严格超时恢复通过。最终 154 项本地与 110 项原生去除重复 22 项，共 **242 个不同用例通过（新增 25、无跳过）**，含 60 项预售报价／积分扣返回归；最终版目录另以非超级用户复验相同 22 项。双类型、语法／空白检查通过，两个私有集群独立核验停止且夹具余留 0。PostgreSQL／Workers 技能促使列白名单、权限负向验证和连接释放；PGlite 测试代理重复包装失败及修正有记录。没有 UI、DDL／正式权限计划／绑定变更，没有生产连接或提交推送部署；选客／选品／结算／收银及浏览器等门禁继续开放，FE-003G、67 目标页及 240／164／404 不变。详见[代客读取审计](workers-ts/docs/assisted-purchase-runtime.md)及[源码与运行证据](workers-ts/audit/uniapp-frontend-parity.json)。

代客前端记录入口增量（2026-09-23，本地代码／测试／三端构建通过，浏览器未验收）：恢复旧 `/pages/behalf/record/index` 直达页及个人中心入口，使用独立、仅内存的 Admin 登录／权限，查询本人代客订单、筛选分页／原页重试及只读付款状态；不使用商城会员 token，不提供付款／退款写操作。修复共享请求层把 Admin／Work 失效响应误用于清除商城登录，以及迟到旧登录解除新登录忙锁的问题；隐藏／退出清空订单数据，旧响应不能覆盖新身份。最终前端工具链 513、产物 3、运行时依赖／真实构建 9、导航 16，共 541 个不同用例通过（新增 25、无跳过），UniApp 类型与 H5／MP／App 构建通过。目标注册页 66→67，直达旧路由 6→7，未映射 47→46；注册不代表完整旧页面行为。前端测试技能要求实际渲染验证；统一浏览器在初始化连续两次报 `kernel assets ... os error 3`，重置无效，0 页面／截图／交互验证，未静默切换工具，替代测试已询问。FE-003G 保持开放：其余四个代客页、详情／支付跳转、反馈／Work、真实登录／provider／端到端仍待完成。240／164／404 不变，无生产连接、提交推送部署。证据记录在[前端路由审计](workers-ts/audit/uniapp-frontend-parity.json)。

代客全路由与游客付款修复（2026-09-23，本地通过／未部署）：同一套真实 JWT／SQL 权限、独立受限 LOGIN 验收扩展至全部 11 条代客接口，新增购物车查询／改量／删除、优惠券、订单列表、现金付款与状态。首轮实际复现游客现金已付款、outbox 后置任务因无 UID=0 用户失败；现仅明确的代客游客跳过会员账户赠券／抽奖／购买次数，供应商入账和任务完成照常，缺来源标记仍拒绝。会员权益一次、游客无匿名权益、现金／队列重试幂等、缺审计或 outbox INSERT 整笔回滚、优惠券取消释放／付款核销通过；Queue 发送失败后持久事件可由真实消费服务恢复。原生 55、单元 24，共 79 个不同用例（新增 11、无跳过），双类型通过，三个私有集群独立确认停止。PostgreSQL／Workers 技能推动最小权限、事务回滚及请求连接释放；仅修复后置业务分支和既有测试换行比较，不改 DDL／角色计划／绑定。无生产连接、提交推送部署，11 路由本地覆盖不等于 provider／Queue 传输／前端／当前候选 Linux/workerd/CI 已验收，FE-003G 等仍开放；累计限购关闭，240／164／404 不变。详见[最新代客审计](workers-ts/docs/assisted-purchase-runtime.md)及[本轮证据](workers-ts/audit/assisted-lifecycle-runtime-20260923.json)。

代客会员／游客运行身份补验（2026-09-23，本地通过／未部署）：完整 279 表上的独立应用／后台 LOGIN、真实逐路由 JWT／实时 SQL 权限校验，覆盖加购→报价→重算→建单→定时取消。会员保存地址／手填／自提／虚拟及游客手填／虚拟六场景验证原购／取消凭据、会员 100 积分扣返／游客零积分、库存恢复及幂等；跨主体、报价后账号／密码／角色变化及缺凭据 INSERT 均拒绝，不回退后台连接，业务写入回滚，请求会话余留 0。原生 73、单元／边界 128，共 201 个不同用例（新增 16、无跳过），双类型通过，三个私有集群均独立确认停机。首轮夹具通配路由导致误判，按正式逐路由挂载修正，未放宽权限。PostgreSQL／Workers 技能推动独立身份、最小权限及请求连接释放；不修改业务代码、DDL、权限计划或依赖。本地签发 JWT、合成 KV／序列／Hyperdrive 传输、关闭 Redis 撤销不代表生产验收；其余七条代客路由、其他活动、前端 FE-003G、provider 与当前候选 Linux/workerd/CI 仍开放。累计限购关闭；240 完成／164 开放／404 总项不变，没有生产连接、提交推送部署。见[代客身份审计](workers-ts/docs/assisted-purchase-runtime.md)及[机器证据](workers-ts/audit/assisted-purchase-runtime-20260923.json)。以下保留此前阶段记录。

原购／取消凭据运行身份增量（2026-09-23，本地通过／未部署）：强化原测试后复现整店受限账号公开下单 42501，修正应用权限计划遗漏，仅增加两张凭据表 SELECT/INSERT，管理账号仍无访问权。维护授权前后在共同栅栏及六表 NOWAIT 锁内验证准确协议／源表，拒绝缺失、漂移或未保护 ORM 表；只读审计不切换身份、不修复。初始化操作升为显式 v2，既有非空角色拒绝整店重放，只能按独立协议升级后复核；历史生产脚本不作为本次发布工具。真实独立 LOGIN 下普通／预售公开报价、建单、取消／未付删除／定时关闭验证通过，HTTP 身份／KV／序列为合成。原生 31、单元 22，共 53 项不同用例（新增 28、无跳过），双类型及空白检查通过；五个私有集群夹具余留 0 且独立确认停止，失败过程保留。Windows workerd 启动崩溃、0 用例，不计通过。PostgreSQL／Workers 技能保留最小权限和维护／运行分离。全活动／代客／访客、生产权限启用、provider 和当前候选 Linux/workerd/CI 仍开放，累计限购关闭；240 完成／164 开放／404 总项不变，没有生产连接或提交推送部署。详见[运行权限审计](workers-ts/docs/purchase-cancellation-evidence.md)及[机器证据](workers-ts/audit/purchase-evidence-runtime-20260923.json)。以下保留此前阶段记录。

取消凭据正式登记增量（2026-09-23，本地通过／未部署）：登记外部 0164、内嵌根事务 0170 及正式模型出口，当前 279 表／171 内嵌／166 外部步骤。九路径真实 PG16.15 的 279 表／3,885 列／668 约束／1,075 索引／227 序列一致；原购／取消准确初态、显式 ORM 补全、对象身份／权限保留、重复安装和无回填通过，既有 278 表前向升级保留旧行且拒绝 runAll 重放，不自动授权。单元／边界 337、原生 807，共 1,144 个不同用例、31 文件、无跳过（新增 5）；含八个迁移文件、八个完整 ORM 工厂及真实取消／删除／预售权限回归，双 Worker 类型、语法和空白检查通过，审计输入摘要复核一致。首轮约束触发器计数遗漏修正后完整重跑通过；前日未收最终结果不计通过，保留诊断。七个有结果的集群均夹具余留 0 且独立核验停止，另一个未收结果集群已停止但夹具数未知。PostgreSQL/Workers 技能保留独立维护事务、锁内证明与权限分离；全活动／代客／访客运行身份、生产启用及 provider 验收仍开放，累计额度政策未定且功能关闭。240 完成／164 开放／404 总项不变，无生产连接、提交推送部署。详见[登记审计](workers-ts/docs/purchase-cancellation-evidence.md)及[机器证据](workers-ts/audit/purchase-cancellation-registration-20260923.json)。以下保留历史阶段状态。

取消凭据 ORM 保护补全增量（2026-09-22，本地未登记／未部署）：新增独立候选模型与显式空表补全；原购 v1 就绪后，在共同栅栏／六表锁和凭据排他锁内重验准确结构、无外部授权与空表，原地安装保护，不授运行权限、不删除或回填历史。普通安装／CLI 拒绝隐式接管；完整 278 模型加候选实际生成 279 表并保留原关系身份，不等于正式登记或九路径认证。原生安装 88、真实取消 34、单元／边界 214，共 336 个不同用例通过（新增 28，无跳过），最终双 Worker 类型通过；指纹校准、测试默认幂等键碰撞及类型修正过程均保留，三个私有集群已独立核验停止且夹具余留 0。正式登记／全建库／前向升级、全入口与运行权限和生产验收继续开放；正式 278 表／170 内嵌／165 外部步骤及 240 完成／164 开放／404 总项不变，累计限购关闭，无生产连接或提交推送部署。PostgreSQL/Workers 技能促使锁内空表证明和维护／运行身份分离。详见[ORM 补全审计](workers-ts/docs/purchase-cancellation-evidence.md)及[机器证据](workers-ts/audit/purchase-cancellation-orm-20260922.json)。以下保留此前阶段状态。

取消凭据受控安装增量（2026-09-22，本地未登记／未部署）：补齐六组件目录指纹、原购 v1／源表索引依赖、固定 NOWAIT 锁及独立维护 CLI；拒绝漂移、部分对象、危险可达角色、默认权限和源权限缺失，不修复或回填，只对明确已有 LOGIN 增授凭据 SELECT/INSERT。安装原生 60、切换安装器后的真实取消 34、单元／边界 214，共 308 个不同用例通过（新增 61，无跳过），最终双 Worker 类型通过；指纹校准及测试自身授权导致的首轮 ACL 比较差异均保留，完整重跑安装文件后通过。三个私有集群夹具余留 0、独立核验已停止。ORM 空表保护补全、正式注册／全建库／前向升级、完整业务身份与生产验收仍开放；局部 `runtimeReady` 不等于全部业务权限。正式 278 表／170 内嵌／165 外部步骤及 240 完成／164 开放／404 总项不变，累计限购关闭，无生产连接、提交推送部署。详见[本轮安装审计](workers-ts/docs/purchase-cancellation-evidence.md)及[机器证据](workers-ts/audit/purchase-cancellation-installation-20260922.json)。以下保留此前阶段状态。

取消购买凭据候选增量（2026-09-22，本地未登记／未部署）：候选数据库协议捕获真实未付款→已取消转换，在提交前核对原购商品行、唯一取消日志与原扣／返还积分账单；与手动取消、未付删除、定时关闭的资源补偿同事务回滚。凭据只保留有界原购投影，拒绝直接补写和修改／删除／清空；无原购及启用前已取消订单不追认历史，不自动释放累计额度。原生 34、单元／边界 213，共 247 个不同用例通过（新增 58，无跳过），双 Worker 类型通过；日志／积分表各 20 万合成行验证索引查询，两个测试集群均清理夹具并独立核验停止。受控安装、正式 ORM／外部／内嵌注册、全建库路径及其余活动／代客／访客身份验收仍待完成；正式 278 表／170 内嵌／165 外部步骤与 240 完成／164 开放／404 总项均不变。累计限购关闭，额度恢复政策仍独立待定，无生产连接、提交推送部署。详见[取消凭据合同与验收](workers-ts/docs/purchase-cancellation-evidence.md)及[机器证据](workers-ts/audit/purchase-cancellation-candidate-20260922.json)。

原购凭据真实建单接线增量（2026-09-22，本地未部署）：公开建单服务强制在同一事务保存新订单原购凭据，与订单、库存、积分及券预约共同提交，并在最终资格与数据库时钟检查前完成；捕获或后续检查失败整单回滚。报价、事务前/幂等锁后的旧单重放不写入或回填，客户端不能关闭。原生 PG16 为 1,177 项／32 文件，单元/边界/运行器 138 项／3 文件，共 1,315 个不同用例通过、无跳过，新增 24；最终双 Worker 类型通过。八个完整 ORM 夹具工厂显式补全空表保护，运行器只增补 16 个确切文件白名单。中途初始化 SQL 超时、600 秒批次超时及两轮夹具遗漏均保留，不声称首轮全绿或超时根因已解决；九个本机私有集群均已停止，其中一个保留 3 个合成数据库供诊断。实际生产协议与所有建单 LOGIN（含后台代客）权限验收仍是发布前置；取消收据、跨根并发额度及独立的额度恢复时点决策仍开放，累计限购关闭。240 完成／164 开放／404 总项不变，无生产连接、提交推送部署或本轮浏览器验收。详见[本轮交易审计](workers-ts/docs/presale-transaction-audit.md)及[机器可读证据](workers-ts/audit/purchase-origin-checkout-20260922.json)。以下保留各历史阶段状态。

原购凭据正式登记增量（2026-09-22，本地已登记/未接业务/未部署）：已登记外部0163、内嵌0169及正式schema模型出口，当前为165个外部步骤、170个内嵌步骤、278表。完整九路径真实PG16审计通过：278表／3876列／659约束／1073索引／227序列一致；额外核验凭据保护状态、对象身份和无回填，ORM只通过显式空表补全。两种既有277表前向升级入口均保留旧行及目录、拒绝重放初始化，不自动授权。原生115、普通238、受影响迁移原生320，共673个不同用例通过，新增5项；双套类型及diff检查通过，三个本机集群均清理夹具并独立核验停止。审计期间只更正过一条SQL头注释，已用输入摘要反向复核可执行SQL未变。真实建单业务接线、启用来源边界、取消收据和累计额度协议仍开放，累计限购仍关闭；240完成／164开放／404总项不变。PostgreSQL/Workers技能约束独立维护事务和运行权限分离。详见[登记审计](workers-ts/docs/presale-transaction-audit.md)及[机器可读证据](workers-ts/audit/purchase-origin-registration-20260922.json)。无生产连接或提交推送部署，以下保留历史阶段结论。

原购凭据ORM保护补全增量（2026-09-22，本地未登记/未接业务/未部署）：新增独立候选模型与显式空表补全协议；只接受精确结构、无触发器/函数、无外部授权的表，并持排他锁确认无数据后安装保护。普通升级、运行角色授权和CLI不隐式补全；已有数据、漂移、提前授权、并发写入或异常事务均拒绝，不修复/删除/回填。补全后目录指纹与直接安装一致；完整277模型加候选共278表实际生成成功，原关系身份不变。安装70、真实建单凭据18、单元/运行器118，共206个不同用例通过（新增27）；正式169迁移/277表仍未变，九路径登记与业务接线须继续。PostgreSQL/Workers技能推动锁内空表证明和维护/运行边界。累计限购继续关闭，240完成／164开放／404总项不变。详见[ORM补全证据与登记门禁](workers-ts/docs/presale-transaction-audit.md)。无生产连接、提交推送部署或前端修改。

原购凭据受控安装增量（2026-09-22，本地未接业务/未部署）：补齐PG16目录指纹、独立维护事务、精确运行角色核验及显式目标CLI；只识别空白、准确v1和漂移三态，漂移拒绝，不接管/重写/回填。固定锁序、NOWAIT安装栅栏及依赖锁后重验；只增授凭据表SELECT/INSERT，不创建角色或修复源表权限。修复根cart_id为NULL时SQL三值逻辑可能漏拒的问题。新安装43项、经安装器运行的完整建单凭据18项原生测试及单元/运行器118项，共179个不同用例通过（新增44），双Worker类型通过；四个本机私有集群独立确认停止。正式迁移登记、ORM完整建库及业务接线仍未完成，正式169迁移/277表不变；不将截取一致性当原始建单来源证明。PostgreSQL/Workers技能推动目录/权限/锁验证及独立维护连接边界。累计模式继续关闭，240完成／164开放／404总项不变。详见[受控安装与剩余门禁](workers-ts/docs/presale-transaction-audit.md)。无生产连接、提交推送部署或本轮前端修改。

原始购买数量存储候选（2026-09-22，本地未接业务/未部署）：新增独立原购凭据候选，数据库锁后从活动未付根及现代冻结商品行生成最小数量/积分投影，调用方不能提交自造快照；唯一订单去重，写入后UPDATE/DELETE/TRUNCATE均拒绝。真实建单事务回调内验证原子提交/回滚，普通混合及预售、真实退款后锚点不变、权限故障与两种跨连接锁等待均通过。原生49及单元/运行器117，共166个不同用例通过（新增47），双Worker类型通过，两个私有集群独立确认停止。SQL仍在pending，正式169迁移/277表和商城建单不变；尚缺维护目录/角色/迁移登记及业务接入。SQL本身不证明截取发生于原始建单时，禁止据此无来源回填存量订单；付款、取消/退款凭据和并发累计额度仍须继续落实。PostgreSQL/Workers技能推动有界存储、真实锁/回滚与无外部I/O验证。累计模式关闭，240完成／164开放／404总项不变。详见[原购候选及接入门禁](workers-ts/docs/presale-transaction-audit.md)。无生产连接、提交推送部署或前端修改。

未付款取消行证据核验（2026-09-22，本地未部署）：原生红测复现数量、用户、购物车编号和冻结商品编号不一致时取消仍成功，数量漂移可能错误恢复库存。共同取消入口现锁住有界原始商品行，核对订单/行归属、数量总和、退款/拆分状态及现代冻结快照积分总和；单行64KiB、最多200行及总8MiB限制在读取/解析处执行。专用连接真实行锁等待后重查新提交数量，异常拒绝且无库存/积分/日志补偿；修复合成漂移后可重试。54项新校验、5项既有迁移及105项原生测试，共164个不同用例通过（新增62），双Worker类型通过。PostgreSQL/Workers技能促使有界锁后核验和无外部I/O事务验证；三个临时集群独立确认停止。此为取消前结构保护，不是不可变数量凭据、持久取消收据或额度释放协议，旧快照兼容不提升为现代额度证明。累计限购继续关闭，240完成／164开放／404总项不变。详见[取消行证据与验证边界](workers-ts/docs/presale-transaction-audit.md)。无生产连接、提交推送部署或本轮前端修改。

未付款混合主体订单取消修复（2026-09-22，本地未部署）：真实普通结算会把平台+供应商混合主单标为待分配，但分配发生在付款之后。原取消条件把此标志误认为已拆单，导致取消、删除和超时关闭均失败。现允许未付且未拆的原始主单取消，锁后重查付款、父子归属及实际子单；全部积分订单先以NOWAIT取得用户锁再恢复库存，避免与新积分建单反向等待。PC/H5共享删除准入同步，PC未付详情不再误报“支付成功”。原生PG110、Worker单元101、PC294、H5订单58，共563个不同用例通过（新增18）；双Worker/双前端类型检查及PC/H5构建通过。浏览器插件在本机合成API验证PC 1280×900、H5 390×844的保留无写入、确认删除单次请求及刷新后已付订单仍受保护；无应用错误，H5仅既有路由弃用警告。数据库与浏览器为分层验收，不等于生产联调。未付数量凭据、跨根汇总与并发限购协议仍未实现，额度恢复政策仍单独待确认，累计限购保持关闭；240完成／164开放／404总项不变。详见[本轮取消修复与复验边界](workers-ts/docs/presale-transaction-audit.md)。无生产连接、提交推送部署。

预售积分取消竞争复验（2026-09-22，本地未部署）：沿用所有者“允许抵扣，报价/实际扣减/取消返还一致”的决定，既有服务端与双端实现不变。新增6项原生并发测试，以实际订单行锁等待覆盖取消/未付删除补偿的四种组合及付款状态先提交的两种竞争；确认只返还一次原扣积分，已付款拒绝取消且无资源返还。预售建单60、受限LOGIN预售13、客户订单删除20，共93项全部通过、无跳过，双Worker类型检查通过。本轮仅补测试/审计记录，无生产连接、提交推送部署；不将积分返还选择当作累计限购额度恢复政策。累计限购保持关闭，240完成／164开放／404总项不变。详见[积分并发复验及边界](workers-ts/docs/presale-transaction-audit.md)。

已付款订单数量整链增量（2026-09-22，本地未部署）：新增 `PurchaseQuotaPaymentLedger`，将保留付款根的原始商品数量、供应商/首次履约拆单、逐代完成退款及退款后再次履约分支合并核验。按明确前驱而非时间或编号顺序遍历，要求每次发货拆分两分支齐全、历史指纹匹配、源行到目的行数量守恒，当前全部子单与行必须被覆盖；保留隐藏订单与售后记录，不把主单/子单重复计入购买。返回单个付款根下的原购/已退/申请中/未完成退款数量，只读且不代表可用额度。退款凭据新增来源购物车锚点及有界批量读取；调用方必须提供可重复读或可串行化事务，避免多次查询混读。最终单元/运行器213、原生数量/退款权限24，共237个不同用例通过（新增64）；真实测试覆盖取消申请、连续预售退款、两次发货拆分后各分支退款、多主体订单、隐藏历史、故障回滚、权限拒绝及跨连接快照一致性。两个本机私有集群已独立确认停止，无生产操作。跨付款根汇总、未付款取消、并发额度占用/恢复和购买入口仍未实现，恢复时点仍待单独确认，累计模式保持关闭；FE-003B/FE-003D及240完成／164开放／404总项不变。详见[数量整链验证与下一步](workers-ts/docs/presale-transaction-audit.md)。

累计限购退款凭据基础增量（2026-09-22，本地未部署）：再次收到并沿用所有者“允许预售积分抵扣，报价/扣减/取消返还一致”的决定，相关服务与双端实现不回退。新增 `PurchaseQuotaRefundReceipt`，从受保护的已完成退款实体凭据中逐行核验用户、付款根、商品、源代次、预占指纹及整单/部分拆分数量；拒绝把申请预占或清零后的实时 `refund_num` 当作退款完成数量。只按精确主键和用户/付款根读取有界投影，不返回联系信息或整份快照，不写业务数据、不新增权限。该组件尚未接入额度消费/恢复/购买入口，不等于累计限购已实现；累计限购继续关闭，退款后额度恢复时点仍须单独确认，不能从本次积分选择推断。原始购买锚点、完整前驱/履约分支核验、退款去重与并发额度事务仍待完成。详见[退款凭据基础与积分复验](workers-ts/docs/presale-transaction-audit.md)。FE-003B/FE-003D及240完成／164开放／404总项不变，无生产访问、提交推送部署。

预售专区与推荐入口增量（2026-09-22，本地未部署）：PC `/presale`（兼容旧 `/goods_presell`）及 UniApp `/pages/activity/presale` 接通旧 PHP 的未开始/进行中/已结束三态、品牌标签、分页与基础商品详情；顶部导航/活动页和旧 `presell/index` 链接可进入。共享显示合同限制金额、身份、时间、页长与安全图片/样式，分页失败保留原页并显式重试，重复/总量漂移要求刷新；隐藏/卸载/身份切换同步清除私有列表并丢弃旧响应。足迹预售推荐只接受匹配预售标志、版本和基础商品 ID 的服务端导航合同，不回退普通购买或推算活动 ID；未来/结束可浏览但购买由详情重验。前端不新增购物车/报价/订单/付款写入。

本轮实际 PC 运行时284+图片9、Uni完整工具链487+产物3+i18n9、原生PG目录/推荐37、路由导航15，共844个不同用例通过（新增21）；Worker双类型、双前端类型、PC/H5/微信/App构建与66页路由审计通过。浏览器插件在本机合成API验证双端专区→第二页失败→同页重试补齐9件→未来商品详情不可预订、H5足迹推荐直达、PC旧链接与导航，390px/1280px布局无横向溢出；无应用错误，仅H5既有Vue Router警告。数据库/API与浏览器分别验证，不宣称真实JWT/Hyperdrive/provider整链。前端技能推动真实页面与生命周期验收，PostgreSQL/Workers技能保持既有只读快照与连接边界。旧151路由账为6直接/60候选/38部分/47缺口，`presell`仅部分覆盖；累计限购账本、完整受限身份权益链、真实用户/真机/provider和生产部署验收仍开放，FE-003B/FE-003D与240完成／164开放／404总项不变。没有生产连接、依赖/权限修改、提交推送部署。详见[本轮预售审计](workers-ts/docs/presale-transaction-audit.md)。以下为历史记录，先前“专区列表/推荐尚未接通”由本增量覆盖。

移动预售入口与会员展示一致性增量（2026-09-22，本地未部署）：新增 UniApp 预售规格页与普通详情入口，保留真实基础 SKU/每单限购/完整结束秒，以 `type=6/new=1` 加购并进入现有积分结算；普通详情不再绕过预售规则提交普通或套餐购物车。登录往返只保留选择，重读后不静默缩量；路由/身份/隐藏/卸载隔离迟到响应，已知cart导航失败只重试跳转。预售列表与规格共用只读一致快照，按终身或未到期付费会员过滤，停用/删除主体与未知VIP商品标志拒绝；列表count与可见性一致、稳定分页、private no-store，最终购买仍重新检查数据库时间与资格。原生PG规格/列表/加购49、积分报价/扣减/取消返还54，移动完整运行时474、产物3、i18n9及目录/导航19，共608个不同用例通过（新增34）；双Worker/Uni类型、三端构建、65页路由审计通过。统一浏览器插件在本机H5 390×844和1280×900验证普通详情→预售→结算、限购和结束/累计关闭，50积分抵¥0.50使应付¥160.50→¥160.00；无真实建单付款，合成身份/API不代表JWT/Hyperdrive整链。无应用错误，1条既有Vue Router警告；技能推动真实渲染及原生只读拒写核验。专区列表/推荐、累计账本、真实provider/真机及完整发布仍开放，FE-003B/FE-003D和240完成／164开放／404总项不变。详见[本轮预售审计与验证边界](workers-ts/docs/presale-transaction-audit.md)。以下为此前阶段记录。

预售限购合同与 PC 详情入口增量（2026-09-22，本地，未部署）：规格只读快照新增经校验的 `purchase_limits`，数量上限同时受基础商品/SKU库存、每单限购和32767约束，库存与选择上限分开；累计限购仍明确关闭。新增共享限购校验，立即购买记录和数量修改在商品锁内执行，与报价/建单保持一致；无效配置拒绝，不以库存替代限购。PC 新增 `/presale/:id`，普通商品详情提供明确入口，全款/北京时间/发货约定、真实基础SKU、限购、登录返回及已知购买记录的继续结算均接通。共享前端解析器验证版本、商品与基础SKU归属、完整结束秒、限购和有界图片，不回退到旧详情或普通 type=0。

本轮原生PostgreSQL规格/购物车39项、结算54项，PC运行时278项及图片9项，共380个不同用例通过，新增25项；Worker双类型、PC类型和生产构建通过。浏览器插件以回环合成API验证商品详情→预售→type=6结算→50积分抵扣，测试报价¥160.50→¥160.00；超量、累计限购及结束状态均不能结算，1280/390px布局、空白页/覆盖层/控制台/交互已核验，窄屏无横向溢出。仅1次合成加购，无建单/支付/券请求。临时集群和测试主机已独立确认停止，未使用生产。数据库技能约束一致快照、短事务与原生锁后限购重验，前端技能推动真实渲染验证。仍须完成 UniApp 预售入口、专区列表/推荐直达、会员可见性一致性、累计额度账本和真实部署联调；FE-003B/FE-003D、240完成／164开放／404总项不变。详见[预售入口验收与剩余边界](workers-ts/docs/presale-transaction-audit.md)。以下“列表/详情入口待接入”不再包含本轮已实现的PC详情路径。

预售积分双端接入增量（2026-09-22，本地，未部署）：PC 与 UniApp 已使用同一结算资格函数，仅同类普通/预售订单开放积分抵扣；开关同时控制显示和实际请求，预售普通优惠券仍排除。补验异步旧报价不可覆盖新选择、报价期间禁止提交、明确拒绝后换凭据且保留选择、连接中断后刷新/重试复用原键和完整请求。PC 回归266项、Uni工具链450项，共716个不同用例通过（新增18项）；两端类型、PC构建及Uni H5/微信/App三端编译通过。浏览器插件实际访问本机两端构建产物，以合成API验证50积分/¥20.50、余额变化后的20积分/¥20.80及待确认恢复；移动390px视口无横向溢出，请求日志确认无支付/券请求。前端测试技能推动实际页面、控制台与恢复交互核验；不将合成API测试视作生产联调。累计限购、列表/详情入口、真实支付/Hyperdrive和完整发布门禁仍开放，FE-003B/FE-003D及240完成／164开放／404总项不变。无生产操作、提交推送部署。详见[双端积分接入与验证边界](workers-ts/docs/presale-transaction-audit.md)。以下为历史阶段，先前“PC/Uni尚未接通”的描述由本增量覆盖。

预售积分与会员参考价增量（2026-09-22，本地，未部署）：所有者已明确允许预售积分抵扣，报价、扣减和取消返还必须一致，不再等待规则选择。服务端 type=6 接入既有 SQL 积分政策、可用余额及锁后重算，抵扣/库存/订单/积分账单原子提交；确认后额度变化要求重新确认，取消按原扣分返还，并在恢复库存前 NOWAIT 取用户锁，避免与新建单反向等待。现代购物车已补预售会员参考价，与确认共用计算器且保留原价。原生下单54、购物车15、会员政策26、受限LOGIN预售13、会员代次3及普通五文件138，共249项不同用例通过，双类型通过；三个测试集群均独立确认清理停机，失败首轮与修复记录保留。PC/Uni 仍有 type=0 积分显示/参数限制，下一步须接通并做真实页面与恢复验收；累计限购、真实支付/Hyperdrive及完整发布门禁未关闭。没有生产/角色/迁移/依赖/前端修改、提交推送部署，暂存0，FE-003B/FE-003D和240完成／164开放／404总项不变。详见[最新积分交易审计](workers-ts/docs/presale-transaction-audit.md)。以下为此前阶段记录，积分“待选择”已由本决定覆盖。

预售受限身份原子退款组合增量（2026-09-22，本地，未部署）：新增完整169步迁移/277表上的独立真实 LOGIN 测试，串联正式 atomic-v2 退款申请/最终执行、已退款付款恢复、正常付款后连续退款及原到期意图交付。精确核验表/列/序列权限，无所有权或维护身份；欠缺凭据/日志/付款完成列/卡归属权限时，相关事务全部回滚，恢复预先声明权限后只执行一次。新增原生10项、既有退款权限10项、预售恢复38项和运行器84项，共142项不同用例通过、零跳过；双类型检查通过，两个私有集群清理停机并独立核验无残留。付款标记仍为合成，正向会员/赠券/抽奖/打印的独立身份权限、真实 provider/Hyperdrive/浏览器和完整预售门禁仍开放。没有修改生产授权、角色或业务实现，也未访问生产、提交推送部署；FE-003B/FE-003D及240完成／164开放／404总项不变。详见[受限身份组合与验证边界](workers-ts/docs/presale-transaction-audit.md)。以下为此前阶段记录，不将此前“atomic-v2 未验证”或本轮局部通过等同于完整运行身份验收。

预售付款退款恢复增量（2026-09-22，本地，未部署）：按所有者确认继续统一限制发货、自提和自动交付必须等到预售结束。新增数量/子单恢复证明，逐代绑定冻结根单、真实退款申请、分区及末端身份，并与已有供应商财务证明在同一 `order.paid` 事务核验相同覆盖范围；已退部分不再交付，派生收入不重复记账，付款计数/日志及既有权益仍原子处理。保留单商品预售建单边界，不放宽规则构造多供应商成功样本。完整受限运行身份、atomic-v2/provider/浏览器及其他预售门禁仍开放，不宣称整个预售或迁移完成。具体测试、失败首轮与修正见[最新付款恢复审计](workers-ts/docs/presale-transaction-audit.md)。无生产连接、提交推送部署；FE-003B/FE-003D与240完成／164开放／404总项不变。以下为此前阶段记录。

预售财务索引登记与完整目录增量（2026-09-21，本地，未部署）：已将候选移入外部0162、内嵌0168并同步 ORM，完整外部164步/内嵌169步；不改历史 SQL、权限或付款运行链。原生九路径每条277表/3868列/651约束/1071索引/227序列零差异，新增索引先验证已登记再重复运行，防止后置修复掩盖缺失。完整旧目录上的合成财务记录前向升级只加两个索引，保留原对象/数据，升级前后拒绝全量初始化重放。索引25、登记67、预售迁移9、八套既有建库/独立LOGIN退款权限308、CHECK/目录工具14，共423个不同用例通过；双套类型与空白检查通过，五个私有集群均独立核验停机。PostgreSQL/Workers技能推动真实目录/锁验证并维持独立维护事务。**完整预售付款恢复仍未完成**，数量/退款子单身份与财务证明尚未共同接入，不宣称整个预售或迁移完成。无生产连接、提交推送部署；FE-003B/FE-003D和240完成／164开放／404总项不变。详见[最新索引登记与验证边界](workers-ts/docs/presale-transaction-audit.md)。以下为此前阶段记录。

预售财务检索索引/容量增量（2026-09-21，本地未登记候选，未部署）：补充两张供应商财务表的普通 `(link_id,id)` 索引安装组件，精确拒绝索引/列/表漂移；固定锁序、NOWAIT、有界维护行数、调用方事务回滚，不修复账本或权限。真实证明查询保留退役/删除证据及超限哨兵；每表20万条无关记录、2及203个订单号的原生执行计划使用准确索引，无全表扫描。索引文件24项、真实退款8项、普通回归25项共57个不同用例通过，另1项旧原生专用未复跑；双套类型及空白检查通过，三个私有集群已独立核验停机。候选 SQL 保存在 pending 目录，**尚未写入正式外部/内嵌迁移及 ORM**，当前登记合同不变；完整建库目录与付款恢复仍是下一步门禁。详见[最新索引候选与验证边界](workers-ts/docs/presale-transaction-audit.md)。无生产连接、提交推送部署，FE-003B/FE-003D和240完成／164开放／404总项不变。以下为此前阶段记录。

预售供应商财务核验增量（2026-09-21，本地候选，未接付款恢复/未部署）：新增只读组件，从真实退款凭据提取有界供应商账本子对象，逐一核对原 P/R 交易、退役流水与 S/D 派生身份、金额守恒、结算时间及当前子单/商品行金额；缺失凭据或额外收入拒绝，不补账。真实服务产生的一次/多次部分退款、直接全退、部分后全退均通过，35次账务漂移/真实重复付款记账注入拒绝且回滚。原生联合文件8项、普通四文件129项通过（另6项旧原生专用本轮跳过），本轮137个不同用例，不累加此前201项；独立连接持有订单/用户/流水/供应商锁时可重试拒绝，释放后通过。4个私有临时集群均停止且独立核验无PID/口令/监听/进程。**仍未完成整个付款后置恢复**：当前组件仅测试引用，须与数量及退款子单证明共同接入；两张财务表还缺 link_id 检索索引，索引前向注册与容量验证也是接入门禁。Workers/PostgreSQL技能约束事务边界、锁序并推动索引检查。无生产连接、DDL/角色/依赖/配置修改、提交推送部署；FE-003B/FE-003D与240完成／164开放／404总项不变。详见[最新财务核验及边界](workers-ts/docs/presale-transaction-audit.md)。以下为此前阶段记录。

预售已退款基线增量（2026-09-21，本地候选，未提交/未部署）：新增严格 v2 预约基线，沿明确退款前驱逐代核对真实退款指纹、经营归属、申请商品行与剩余分区；冻结历史摘要，不用 ID/时间截止忽略历史，也不把剥除标记的退款子单当初始订单。到期可从基线继续处理后续部分/整单退款，原结束秒不变。追加反例发现11种后续退款行证据错误曾被接受，现逐行绑定申请与分区，整单退款同样检查，错误全部拒绝且无交付副作用。

本轮最终原生预约/自动交付/手工核销185项、真实预售建单→退款执行→实体拆单→到期交付3项及普通交付/付款队列13项，共201个不同用例通过；双Worker类型、运行器语法与空白检查通过。新联合测试等待数据库时钟，不改截止合同；付款标记为合成、退款资金/实体操作分开，不冒充 atomic-v2、provider/回调、生产LOGIN或浏览器验收。6个私有测试集群均停止并独立核验无PID/口令/监听/所属进程。**首次付款后置的退款恢复仍开放**：当前全子单循环遇已退款子单仍原子拒绝；不能只跳过该子单，因为后续 P 记账可能重复已有 S/D 派生供应商流水，须将财务身份一并接入恢复。已完成的是基线组件而非整个付款恢复。详见[最新预售审计及发布阻断](workers-ts/docs/presale-transaction-audit.md)。无生产连接或DDL/角色/依赖/配置修改，暂存0，FE-003B/FE-003D和240完成／164开放／404总项不变。以下为此前阶段记录。

预售到期自动交付接线（2026-09-21，本地候选，未提交/未部署）：付款后置事务已原子预约独立事件，付款次数与日志不随等待重复；新Queue动作、扫描/重放/DLQ、数据库到期判断及退款等待均接通。到期按有界退款实体账本核对剩余代次，卡密认领、订单状态、通知与事件完成同事务；全部退款不发卡、进行中退款不耗失败预算。外部0161/内嵌0167/ORM与独立升级器已登记，只扩已知事件检查、不更改角色或业务记录；已升级库拒绝全量初始化重放旧检查。

当前自动交付29、迁移9、预约66、手工/核销63共167项取得原生PG16验证；相关通用、建库、既有LOGIN和目录工具测试按用例去重共301项。另原生九路径完整建库目录审计通过，277表/3868列/651约束/1069索引/227序列零差异；保留0144历史清单，单独验证0161前向升级和旧初始化降级拒绝。补充回归复现并修正实体预售付款误被履约时间门禁阻挡，修正后原生四文件105项通过，付款事实不延期。仍不能发布为完整预售闭环：首次付款后置处理之前已发生退款实体拆分时，当前原子拒绝并留FAILED，须补已退款基线恢复；完整真实报价/回调/实体退款/到期交付及运行身份、积分/累计限购与前端门禁未完成。见[预售交易合同审计](workers-ts/docs/presale-transaction-audit.md)。本轮无生产连接、推送或部署；FE-003B/FE-003D及240完成／164开放／404总项不变。以下为较早阶段留档。

预售自动交付延期意图增量（2026-09-21，本地未提交/未部署）：新增独立 `order.presale.fulfillment` 候选协议组件，锁后核验付款/子单/买家与经营归属，冻结原始数量、结束秒及交付内容摘要；相同键冲突拒绝，意图跟随调用方事务回滚。认领前置使用数据库时间，提前重复消息恢复PENDING且不消耗失败次数，不覆盖有效处理租约；原始退款预占不当作完成退款扣减。仅测试引用，付款/Queue/实际发卡与ORM/迁移白名单均尚未接通，当前自动交付仍不能宣称符合统一规则。现有ORM白名单拒绝新事件的原子回滚已有实测，不提前启用会失败的付款写路径。

新增最终66项（62通用、4原生专用）；最终原生两文件129项全部通过，普通outbox/付款队列/虚拟交付三文件17项通过，去重共146项，Worker双类型通过。两个新建非超级用户回环PG16.15集群独立确认停止，无所属进程/端口/PID/临时口令文件，保留私有诊断目录。PostgreSQL技能促使原生锁和独立时钟验证，Workers技能约束等待与失败重试分离、无事务内外部I/O。下一步为迁移/权限/队列注册及退款代次解析后接入真正到期发卡，详见 [预售交易合同审计](workers-ts/docs/presale-transaction-audit.md)。积分、累计限购、完整交易/身份和前端验收仍开放；FE-003B/FE-003D及240已完成／164开放／404总项不变。以下保留较早阶段记录。

预售履约增量（2026-09-21，本地未提交/未部署）：后台发货、供应商整单/拆单、面单创建与消费统一使用订单中的新旧预售证据；缺失/损坏/跨商品/未知版本拒绝，禁止从当前商品补造合同。200行/64KiB上限在SQL读取处执行，SHARE NOWAIT保护快照到提交，数据库时间按PHP结束秒开头准许手工发货，不误加1秒或发货天数。面单申请前重查，签发后证据变化保留UNKNOWN及已有单号，重放不重复申请。实际连续两次拆单与剩余单发货保留合同。所有者进一步明确统一限制所有履约，已接入自提/送达核销写入；自动卡密仍需独立持久到期任务，当前不可作为统一规则完成版本发布，不能反复执行付款计数/账务或耗尽失败重试。

手工阶段7文件93项通用和3项原生专用通过；加入核销后最终本地8文件104项通过、4项原生专用跳过，原生4文件84项全部通过，本轮去重108项、Worker双类型及空白检查通过。四个临时集群独立确认已停机且无进程/端口/PID/口令文件余留，私有诊断目录保留，详见 [预售交易合同审计](workers-ts/docs/presale-transaction-audit.md)。PGlite共享JS时钟、CRLF差异、测试登录缓存隔离和核销夹具遗漏会员表均有记录，不修改迁移SQL或放宽业务断言。按Workers/PostgreSQL技能验证事务外provider调用及原生锁冲突。积分、自动交付延期实现、累计限购、完整建单履约/退款链、身份权限和前端验收仍开放；FE-003B/FE-003D及240已完成／164开放／404总项不变。以下为较早阶段记录。

预售报价/建单增量（2026-09-21，本地未提交/未部署）：新增 `PresalePurchaseSnapshot`，共用报价和建单校验当前类型/时段/直接购买范围/账号/基础SKU/库存/单次限购；type=6采用PHP会员价合同，继续排除优惠券与首单促销，未选择或修改积分政策。确认收据绑定版本化预售合同，订单行冻结结束时间与发货天数；购物车认领（含自提）及库存UPDATE比较相同权威事实，普通商品报价后转预售也拒绝。用户资格用NOWAIT保护到提交，所有SQL等待之后以数据库实时时钟复核预售和时效会员截止。取消未付订单与幂等重放不依赖后来编辑的预售商品。保持既有READ COMMITTED短事务、超时上限及现有连接/绑定，无外部I/O或生产操作。

累计限购仍关闭并明确拒绝建单：退款申请即预占`refundNum`，实体退款又会重置计数/保留审计根，不能直接求和释放额度。新版快照已生成但发货读取端尚未对接：供应商只读旧字段、后台为独立无预售门禁入口；还需自提、虚拟、面单、拆单/退款代次验证。本阶段不开放前端购买或宣称完整预售完成；积分的“允许并实际扣减 / 全部禁止”仍需具体选择。最新验证和资源清理见 [预售交易合同审计](workers-ts/docs/presale-transaction-audit.md)。FE-003B/FE-003D以及240已完成／164开放／404总项不变。以下为较早阶段记录，以上方最新增量为准。

预售购物车与交易规则审计增量（2026-09-21，本地未提交/未部署）：新增实际预售结算选择入口，旧 type=0/new=1 按锁后商品转换为6，显式type=6不能伪造普通商品；仅接受当前基础SKU和独立立即购买，不合并、不预留库存。数量修改按购物车→SKU→商品锁序，重查归属/未认领/商品/规格/库存，写后以数据库实时时钟核验开始与完整结束秒，失败整事务回滚；失效、降级或配置变更在读取中明确无效。5秒语句/空闲及2秒锁超时保留更严格设置。普通旧版变更入口拒绝预售type=0继续使用，未开启页面/推荐购买。

本轮进一步发现PHP预售会员价可用，但目标报价仅对type=0使用会员价；PHP报价允许type=6积分抵扣而建单又关闭实际扣积分，已请求所有者在“允许并原子扣减”或“报价/建单都禁止”之间选择，未自行决定。最终建单时间/类型权威、累计限购与退款代次计数、确认收据和发货快照仍未完成，不能把购物车校验当作下单闭环。最终本地10文件105项通过，3项原生测试按条件跳过；原生PG16.15非超级用户预售32项、独立维护协议夹具砍价17项全部通过，共125个不同用例（重复的29项不累加），双类型检查通过。扩展回归曾有1项换行差异和3项PGlite缺计价能力失败，分别统一测试读入换行和切至真实协议复验，不放宽业务判断；仅扩展运行器精确测试文件白名单。三个临时集群独立确认停机，无PID/口令文件/监听/所属进程，保留私有诊断目录。PostgreSQL技能促使真实锁等待、跨截止与整事务回滚复验；Workers技能约束现有连接和事务无外部I/O。无生产操作或本轮浏览器证据，FE-003B/FE-003D及240／164／404不变。详见 [预售交易合同审计](workers-ts/docs/presale-transaction-audit.md)；以下保留上轮只读阶段记录。

预售合同核对与只读规格增量（2026-09-21，本地未提交/未部署）：旧 `pages/activity/presell/index` 三态列表进入 `goods_details?type=6`，后者仍调用普通商品详情并提交基础商品/基础SKU的立即购买；PHP `StoreCartServices::setCart` 在商品为预售时把立即购买记录由0转换为6。该版本是全款预售、结束后按 `presale_day` 天数发货，不是定金/尾款模型。新增可复用 `PresaleScheduleService`，保留Unix秒首尾均包含、最后一秒毫秒请求仍有效、午夜结束不自动延长一天；全0默认时间按已结束处理，异常配置明确失败。

既有 `GET /product/detail/:id` 增加显式 `?view=presale` 只读合同，不改变未选择视图时的旧详情路径；响应 `private, no-store`，包含版本、`type=6/payment_mode=full/selection_only`、基础商品、真实销量、标题/简介/安全有界图片、开始/结束和配置发货天数、最多500个当前 `type=0` 基础SKU及原始十进制目录价格。不读取伪造 `type=6` SKU、其它商品SKU或退役SKU，不把参考库存/金额作为购买资格或预留。商品审核/上下架/删除/预售标记和当前认证主体可见性在一次 `REPEATABLE READ, READ ONLY` 快照内重读；匿名2次、登录3次业务SELECT，参数/重复视图/冲突类型在SQL前拒绝，保留更严格调用方超时。未增加数据库结构、权限、平台绑定、依赖、前端路由或生产写入。

预售验证：时段6项、真实Hono/服务/DAO/PGlite读取11项，共17项通过；与旧详情缓存隔离、公共目录、会员展示和推荐回归合计7文件106项通过。相同17项在独立原生PostgreSQL16.15/非超级用户下复验（其中11项操作SQL，6项纯时段测试，不重复累计），明确观察隔离级别/只读开关、5秒上限及750毫秒较严限制、实际UPDATE被SQLSTATE 25006拒绝、事务结束后只读状态恢复及业务数据不变。新增事务测试曾因PGlite插桩双重包装、结果数组元数据及postgres.js手写保存点未隔离驱动错误跟踪失败；已分别改为外层代理、验证实际结果行、驱动保存点API，保留拒写断言后双引擎通过，未修改公共夹具或放宽业务逻辑。最终集群 `finance-postgres-WrhyqG` 及前两次 `3sEPoW/1dsTph` 均夹具余留0；独立 `pg_ctl` 退出3、PID/口令文件/监听/所属进程均无残留，保留停止后的诊断目录。两套Worker类型检查通过。本轮仅后端，未新增浏览器/真机证据，不沿用上轮截图冒充本合同验收。

预售仍未完成：源端证据揭示目标 `StoreCartService::addResolved` 尚无基于商品的0→6转换及完整开始/结束门禁，`StoreOrderCreateService` 仍比较调用方type与购物车type，其新快照未冻结 `productInfo.presale_end_time`，而履约预售门禁读取该字段。下一步须先完成加购/数量/报价/建单的权威预售类型与时段重验、价格/优惠/积分及限购PHP合同、发货快照和跨截止并发回归，再接预售列表/详情/推荐与结算恢复；不能以本只读视图替代购买闭环或照普通type=0开放购买。FE-003B、FE-003D保持开放，目标64页与240／164／404不变。PostgreSQL技能促使有界索引读取、只读一致快照和原生拒写复验；Workers技能核对[官方Hyperdrive实践](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/)，本轮保持既有连接与配置，最新类型获取失败时使用已安装5.20260828.1作核验。

推荐活动直达增量（2026-09-21，本地未提交/未部署）：`/product/hot` 在保留原 `activity` 优先级CSV及商品字段的同时，新增带版本和商品归属的 `recommendation_target`，返回真实秒杀/砍价/拼团ID及有效期；足迹推荐按类型白名单进入对应详情，不把基础商品ID或结束时间当成活动/时段ID。沿用PHP“首位0优先普通、后续0不终止活动查找”的顺序；同类型多活动改为明确的 `sort DESC,id DESC`，不复制PHP未指定排序的物理行顺序。秒杀复用现有北京时间父子活动/每日时段判定，砍价和拼团沿用现有详情的时间边界。整页最多100商品、每类最多500候选、最多4次附加批量读取；超过候选上限明确失败，不静默截断或退成普通商品。无数据库结构、权限、绑定及业务写入变更。

客户端严格校验版本、商品归属、活动类型/ID与标准UTC有效期；已过期或异常元数据停止导航并提示刷新，不自动降级普通商品或发起业务操作。仅旧服务端未提供新字段时保留活动专区兼容路径；预售仍明确不可直达，详情自身继续负责规格、资格和购买时段校验。本增量完成三类活动导航，不等于预售、促销边框或整条下单链等价；FE-003B与 `partial_replacement` 保持开放，240／164／404不变。

本增量验证：足迹45项包含在完整前端工具链441项内；后端7文件共89项通过（先6文件84项，再补跑公共目录5项，未将未匹配的过滤文件算作通过）。其中推荐导航9项及推荐分页7项经过真实Hono/服务/DAO/PGlite，相同16项在独立原生PostgreSQL16.15、非超级用户身份下复验，不重复累计；覆盖优先级、父子时段/边界、停用/隐藏/删除、同类稳定选择、100商品4次读取、501候选拒绝、业务数据不变，以及hot返回的秒杀ID进入实际详情并映射同一基础SKU。初次详情夹具的短 `char(8)` SKU被补空格后正确拒绝，已改为合法8字符测试值复验，未放宽业务验证。原生临时集群 `finance-postgres-6sEMo4` 余留夹具0，独立核验已停止、无PID/口令文件/监听/所属进程，保留诊断目录。UniApp及两套Worker类型、H5/MP-Weixin/App构建、产物3项、i18n9项、路由审计通过；保留既有小程序空chunk提示。

浏览器为Chrome153/Playwright1.62.1本机H5，390×844与1280×900：页面身份、非空、无错误覆盖层、首屏与详情截图、三类卡片进入精确活动ID并选择规格均通过；预售/过期/未知链接提示且不跳转，普通商品仅验证目标URL。0控制台错误/外发/业务写入，保留1条既有Vue Router导入弃用警告；未点击购买、发起砍价或加购。首次详情截图残留上一操作提示，测试已改为等待实际提示文本自然消失后取图并目视复验。API为合成响应，与独立SQL测试不能合并宣称真实JWT/Hyperdrive浏览器整链；真实账号、真机、完整CI及候选部署仍开放。前端技能要求实际渲染取证，PostgreSQL技能促使有界批量读取并保留既有分页合同；Workers技能按[官方最佳实践](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/)核对原Hyperdrive访问路径，最新类型获取失败时核对已安装5.20260828.1，无平台API/配置变更。截图和临时脚本在仓库外。本段覆盖下方“真实活动ID/有效时段尚待接入”的历史描述，不关闭其余迁移或发布门禁。

推荐卡片展示与导航保护增量（2026-09-21，本地未提交/未部署）：足迹空态推荐补齐现有接口的品牌、商品标签和参考会员价。标签纯文本渲染，最多展示8项，颜色仅接受三/六位十六进制，图标沿用安全图片校验；装饰字段异常单独忽略，不使整页失效。依据 `price_type` 区分“等级参考价”和“SVIP参考价”，保留基础展示价，不把 `vip_price` 当成当前访客的购买资格或结算承诺。真实SQL已确认 `/product/hot` 的 `activity` 仍是优先级CSV而非活动ID：仅明确普通优先的商品进入普通详情；其它合法优先级临时引导活动专区，预售及未知合同明确提示不可直达。此为保守导航保护，不是PHP活动直达等价实现；真实活动ID/有效时段解析、预售详情、促销边框仍需补齐，FE-003B和 `partial_replacement` 保持开放，240／164／404不变。

本增量验证：足迹41项纳入前端工具链437项全通过；后端四文件26项全通过，其中推荐7项经实际Hono→服务→DAO→PGlite，并在独立原生PostgreSQL16.15/非超级用户下复验相同7项，不重复累加。新增SQL用例验证品牌/标签可见性、SVIP参考价对普通/匿名访客的展示合同、权益关闭后清空优惠，以及读取前后业务数据不变。临时集群 `finance-postgres-hYLXbf` 夹具余留0，独立核验 `pg_ctl` 退出3、无PID/口令文件/监听/所属进程。UniApp与两套Worker类型、三端生产构建、产物3项、i18n9项通过；保留已有小程序空chunk提示。Chrome153在本机H5的390×844和1280×900通过实际渲染、长标签换行/HTML字面量、安全图标、会员价区分、原页重试/滚动分页、普通/活动专区/预售导航及匿名不读；0控制台错误/外发/业务写入，1条既有Vue Router弃用警告。浏览器使用合成响应，不冒充SQL联合或真实用户/真机验收；截图/临时脚本在仓库外。前端技能要求实际渲染验证；PostgreSQL技能促使保持限量稳定分页及独立测试身份，无新增业务SQL、权限或生产资源修改。本段覆盖下方“会员/标签未恢复”的旧阶段描述，不关闭剩余活动合同或发布门禁。

足迹空态推荐读链增量（2026-09-21，本地未提交/未部署）：核对PHP `StoreProduct::product_hot` → `getProducts/getPageValue` 及旧足迹 `hotPage/hotLimit`，确认其为数组分页接口；修复目标 `/product/hot` 丢弃页码、固定返回前100件的问题，使用既有推荐服务/DAO的 `sort DESC,id DESC` 分页，默认10/最多100，超出可表示范围的offset在SQL前拒绝。无数据库结构、索引、权限、绑定或业务写入变更。页面仅在足迹成功读空后显示“为你推荐”，支持滚动翻页、失败手动重试原页、末页/全空、商品展示价/库存状态和详情跳转；足迹读取失败不伪装为空，隐藏/卸载/身份变化/刷新均丢弃迟到推荐，推荐不进入足迹批量删除/收藏选择集。旧 `recommend` → `WaterfallsFlow` → `goShopDetail` 另有活动专属导航及会员/促销展示，本轮只补基础推荐读链，仍作为明确剩余差异，不把普通商品跳转当作该活动合同已经迁移，`partial_replacement`、FE-003B及240／164／404不变。

推荐验证增量：足迹运行测试35项含在完整前端工具链431项内，零失败/跳过；后端四文件25项通过，其中新的6项通过实际Hono控制器→服务→DAO→PGlite；同6项另经原生PostgreSQL16.15独立集群/非超级用户复验，覆盖分页不重复、111件跨页/短末页/空页、上限、可见性和VIP过滤、极端参数及读取前后业务状态不变。测试仅使用合成身份，非真实JWT/Hyperdrive验收；临时集群 `finance-postgres-mg2FQL` 余留夹具0，独立 `pg_ctl` 退出3、PID/口令文件/监听/所属进程均无残留，保留停止后的诊断目录。Chrome153本地H5在390×844和1280×900验证首屏失败→重试10件→真实滚动下一页失败→手动重试共11件→末页→商品目标URL，以及全空、足迹失败不加载推荐、匿名不读；0错误/外发/业务写入，1条既有Vue Router弃用警告，截图/临时脚本在仓库外。前端技能推动实际渲染验收；PostgreSQL技能促使沿用稳定排序、限制结果量且不扩大数据库权限，因PHP合同保留offset而未擅自换cursor；Workers技能要求核对官方文档并保留原Hyperdrive访问路径，无新增平台API或配置，最新类型获取失败时核对已安装5.20260828.1。本段覆盖下段首轮“空态推荐尚未恢复”的阶段描述；活动卡片差异、真实账号/真机、当前候选完整CI及单独发布批准仍开放。

推荐终验补充：最终源代码的UniApp及两套Worker类型检查、H5/MP-Weixin/App生产构建、产物3项、i18n审计9项和 `audit:uniapp` 均退出0。原生PG6项是前述25项中相同用例的跨引擎复验，不累加成31项不同后端用例；浏览器使用合成响应，不能与独立SQL测试拼接宣称为浏览器/数据库整链。按[Cloudflare官方最佳实践](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/)核对本次Workers改动，无部署、平台绑定或外部访问路径修改。先前单项测试曾误以为共享运行器记录HTTP method导致计数断言失败，已用实际发送回调计数复验，未删掉写入隔离断言。

足迹页首轮增量（2026-09-21，本地未提交/未部署，以下保留推荐接入前的阶段记录）：FE-003B 新增 `pages/user/visitHistory`、个人中心入口及旧 `pages/users/visit_list/index` 映射。接入既有鉴权 `GET /user/visit_list`、`DELETE /user/visit` 和 `POST /collect/add`，按服务端日期分组、同页分页重试、商品状态/安全图片展示；删除提交商品ID而非日志ID，一次最多200件、须确认，成功后重读第一页；收藏最多100件且保留足迹。结果不明禁止自动重试，用户刷新核对；隐藏/卸载、退出/切换/更新会话清除私有状态，迟到响应不能恢复。旧空态推荐尚未恢复，故映射为 **partial_replacement**，不关闭FE-003B。目标注册64页，旧151路由=6直接注册+60候选覆盖+37部分替代+48未映射/跨端缺口，不将注册视为功能等价。

本轮验证：真实Vue/Pinia/请求适配器的足迹24项包含在工具链420项内，全部通过；UniApp类型检查、H5/MP-Weixin/App构建、产物3项、业务图i18n审计9项通过；Worker导航/路由台账两文件14项和 `audit:uniapp` 通过。Windows初次导航SQL比较13/14通过，确认仅CRLF/LF差异后只统一测试输入换行，复验14/14；不修改SQL协议。Chrome 153 / Playwright 1.62.1 的本地H5在1280×900、390×844实际渲染通过：页身份、非空、无错误覆盖层、分页失败重试、收藏保留记录、取消/确认删除、未知结果禁止重发及刷新核对、空态、匿名不读取私有足迹。6次合成列表读取、2次合成删除、1次合成收藏，外发0、控制台错误0；保留1条已有Vue Router导入弃用警告，小程序构建另有已有空chunk提示。当前会话未列出专用Browser技能，按前端测试技能使用现有Playwright；截图和临时脚本在仓库外。合成API不是Worker/SQL联合测试，也不等于真实用户、真机或生产验收；本轮未访问生产数据库、提交、推送或发布，清单父项不新增勾选。

已发布基线与维护终态（2026-09-21，与本轮候选区分）：`7a5508f` 的 [CI 35562210220](https://github.com/cinagroup/cinashop/actions/runs/35562210220) 已重新只读核对 `completed/success`。此前约07:00 UTC生产只读复核确认主API `e468c0f0-aa75-43b5-9c6a-81a2ad4a9e82` 100%及五个Pages正式部署均对应该提交；业务/后台绑定分别为 `ba7faa6680cd48d4b3a1d36a7a5fc8f7` / `446e94a4de0143f58c8e5178ec55db8b`。已授权的4条未付款测试订单完成私有备份、定向删除（4订单+4购物明细+1失败拼团）和独立归属/保留数据指纹复核；维护入口已删除，380秒观察内新未付款/拼团扫描成功、无新取消失败，1条未分类warning保留。这里只记录去标识化汇总，不把私有备份或原始业务数据纳入Git；不重复清理，不据此关闭其它孤儿裁决或全局发布/真实业务验收项。下文2026-09-20及更早的权限尚未切换、主站尚未发布、CI未完成等是对应历史阶段，不覆盖本段。

报告传输与中断诊断增量（2026-09-21）：外键审计改用独立同步管道回传，严格拒绝缺报/截断/重复/子进程失败；本机全量入口增加逐模块哈希链进度日志，仍要求完整原生JSON、精确覆盖和零跳过，不以部分日志代替通过。真实Vitest两分片合同、运行器边界、外键及旧文件报告兼容共六文件171项定向测试通过，双Worker类型通过。当前533文件（267/266）全量尚未执行，历史临时目录删除者未定，本机容量和不可变CI仍开放。详见[最新验证与历史失败边界](workers-ts/docs/local-full-unit-gate.md)。本增量不改变业务政策、生产数据或240／164／404清单状态；后续提交/发布状态以发布记录为准。

本机完整单元门禁审计（2026-09-20，本地未部署）：新增仅接受原生两分片的显式隔离运行入口、生产环境变量过滤、逐文件/逐断言零跳过校验及选定输入端点摘要。531文件清单完整分为266/265，两片均在四十分钟超时，未生成完整Vitest JSON；2,378个选定输入前后相同，但实际覆盖未完成，全量门禁明确不通过。观察到生成器旧264表断言、拼团夹具漏uid及两个180秒附近的子进程失败，独立复现后仅修正前两类测试夹具：采用现有277表精确合同、补合法归属并增加拒绝/预占幂等断言，拼团原生PG最终58项通过；运行器104项及双类型通过。三个生成器原时限顺序诊断14项中13通过、1失败：Drizzle及缺失约束通过，外键ESM业务断言完成后因临时审计报告路径ENOENT退出1，产物生命周期原因和本机全量容量均仍未解决。超时遗留三个合成库及一个测试角色，已核对归属后无网络单用户清理，原自动清理失败记录保留；本轮四集群均独立核验停机、无PID/口令/监听或可信PG/workerd残留。详见[完整单元门禁与本机边界](workers-ts/docs/local-full-unit-gate.md)及[终态证据](workers-ts/audit/local-full-unit-gate-20260920.json)。本轮未改业务服务，不新增type=3线下退款；当前修正后全量/不可变CI、远端维护/真实客户及完整生产权限/负载/发布仍开放，FE-003L-A3k11d及240／164／404不变。无生产访问、暂存、提交、推送或部署，原九暂存保持；下文定向结果不等于当前全量通过。

普通结算隔离场景增量（2026-09-20，本地未部署）：首单/套餐克隆已补齐计价及配送依赖，移除public回退并重绑全部实际serial列，使用显式预建的独立NOLOGIN所有者安装固定协议。四个旧审计入口鉴权后要求X-Audit-Pricing-Owner，缺失/非法在SQL前拒绝，不自动创建角色或提升业务身份。保留原首单/套餐价格、并发、回滚和商品订单退款断言；原生PG16三类建单/所有者拒绝11项及Node相关133项通过，共144项不同定向用例，完整本地workerd16文件289项及双类型通过。首次来源/地址/模板失败和workerd零断言启动失败均保留；使用同哈希现有运行库副本复验，未改系统安装。4个PG集群独立清理核验及工作区workerd进程零残留；详见[场景与审计入口合同](workers-ts/docs/checkout-pricing-scenario-integration.md)和[证据](workers-ts/audit/checkout-pricing-scenario-integration-20260920.json)。真实远程维护、完整当前单元/不可变CI、真实客户/Hyperdrive及完整权限组合/负载/协调发布仍开放，FE-003L-A3k11d及240／164／404不变。原九暂存保持，无生产访问、暂存提交推送部署。下文场景/维护参数未接入或本机workerd未验的表述由本段覆盖，不改写历史测试结果。

普通结算服务接线增量（2026-09-20，本地未部署）：普通建单末端已改用同schema受审计固定锁能力，先核验实际表解析来源，缺失/漂移失败关闭，不直接LOCK、不增配置DML、不自动安装。完整277表下独立真实LOGIN仅持两张计价表SELECT及精确函数EXECUTE即可建单/幂等重放，直接锁表和更新仍拒绝；控制器、后台写端、并发阻塞/回滚和临时来源遮蔽有证据，客户身份仍为合成夹具。完整普通建单克隆场景已显式供应独立所有者并通过原并发/营销断言。最终17文件555项不同定向测试及双Worker类型通过，首轮业务红测与91项夹具失败均保留；8个自建集群独立核验清理停止，诊断目录保留。详见[服务接线边界](workers-ts/docs/checkout-pricing-service-integration.md)及[终态证据](workers-ts/audit/checkout-pricing-service-integration-20260920.json)。首单/套餐克隆、远程审计维护参数、当前完整回归/workerd/不可变CI、真实客户/Hyperdrive/维护身份、完整商城权限组合和协调发布仍开放，FE-003L-A3k11d及240／164／404不变。原九暂存保持，无生产访问、暂存提交推送部署。以下“普通建单仍直接LOCK”等文字均为历史阶段，当前状态以本段为准。

普通结算编号迁移增量（2026-09-20，本地未部署）：固定锁能力已统一注册到外部0160、内嵌0166及显式安装器，所有者必须预建并由实际维护连接明确配置，不自动供应角色或给业务配置写权。真实PG16九条完整建库/升级路径通过：外部162文件、内嵌167步及七条ORM路径最终277表，五类目录零差异，安装/重复执行保持既有对象身份。本轮25文件781项定向测试与Worker双类型通过，九个自建集群独立确认已停止、无PID/临时口令/监听/自有进程残留；首轮CLI超时及其两个测试角色的离线清理保留记录，未放宽业务断言。详见[注册合同](workers-ts/docs/checkout-pricing-lock-registration.md)与[本轮终态证据](workers-ts/audit/checkout-pricing-lock-registration-20260920.json)。普通建单仍直接LOCK、SELECT-only仍42501；正式维护身份/连接供应、服务切换、HTTP/workerd、完整当前CI、完整商城权限组合和协调发布仍开放，FE-003L-A3k11d及240／164／404不变。本轮无生产访问或部署；以下编号迁移未接入的表述均属历史阶段，最新以本段及终态证据为准。

普通结算权限预检接线增量（2026-09-20，本地未部署）：既有paid-order只读预检仅认可完整目录/实际连接校验通过的固定锁函数OID，新增显式checkout组合预检与CLI --checkout，缺失安装/授权、定义或ACL漂移、配置写权与其它提权函数均拒绝；成员角色保守递归展开。真实PG16权限40项及锁协议36项共76项通过，含独立非超级用户维护LOGIN安装/重装和权限不足原子回滚；运行器59项与现有探针模拟处理器29项、Worker双类型通过，共164项不同用例。七个选定源文件最终PG运行前后哈希相同，两个自建集群清理停止且独立确认无残留。普通建单仍直接LOCK/SELECT-only仍42501；正式角色供应、编号迁移/全部建库、服务切换、HTTP/workerd/全量CI、完整商城角色组合及协调发布仍开放，FE-003L-A3k11d与240／164／404不变。PostgreSQL/Workers技能推动精确协议例外、只读和独立连接验证；原九暂存保持，无生产访问、暂存提交推送部署。见[终态证据](workers-ts/audit/checkout-pricing-runtime-preflight-20260920.json)和[权限范围](workers-ts/docs/paid-order-runtime-permissions.md)。以下为此前阶段记录，最新状态以上述增量为准。

普通结算锁权限协议增量（2026-09-20，本地未部署）：新增显式固定两表锁能力安装器、定义/ACL/所有者/RLS校验、实际连接权限预检及事务取锁组件。独立预建NOLOGIN所有者，不给业务登录配置写权；真实存量连接即使已设NOLOGIN也拒绝。完整277表ORM、真实受限LOGIN/PG16最终34项与运行器58项共92项不同用例、Worker双类型通过；保留首轮夹具参数失败及修复，阶段重跑不累加。四个自建集群已清理停止，独立核验无PID/临时口令/监听/自有进程残留。普通建单仍直接LOCK且SELECT-only仍42501，编号迁移/各建库路径、既有paid-order权限审计、真实HTTP/workerd/全量CI和生产接入尚未完成；FE-003L-A3k11d及240／164／404不变。PostgreSQL/Workers技能约束固定能力、最小权限和真实连接证据；无生产访问、暂存提交推送部署，原九暂存保持。详见[协议增量及未完成门禁](workers-ts/docs/checkout-pricing-config-authority.md)。

线下收银退款范围决定（2026-09-20，所有者明确确认）：保持旧PHP `other_order.type=3` 范围，暂不新增退款；新退款表／接口／按钮均不纳入本次迁移要求。PHP收银控制器与服务没有该退款合同，商品订单线下付款／售后不是此域。此前阶段“关闭退款待完成”的文字由本决定覆盖，不再据此要求新增退款；不确定外部付款仍须保留原单与支付路径并核验真实平台，不能因此当作未付、退款或释放凭据。详见[范围决定与联合验收](workers-ts/docs/admin-offline-order-read.md#退款范围决定与真实浏览器联合验收2026-09-20最新本地未部署)。

后台真实SQL浏览器联合增量（2026-09-20，本地未部署）：新增显式、仅回环的Browser验收宿主，实际Admin→HTTP/JWT/角色→workerd→本地Hyperdrive→独立只读LOGIN/PG16，23条SQL合成订单含已付及历史零元。内置浏览器1280×720／390×844验证20+3分页、空筛选、核验详情、海报／原始码、实际角色撤销、SQL SELECT撤销／恢复及过期会话；两轮均12次只读业务请求、外发0／业务写0／资金全行指纹不变。真实503发现英文提示，已仅在线下页面改为中文并保留未知付款语义，最终构建复验通过；六项红测后实际Vue/Axios132、订单回归44、运行器57共233项通过，另原生PG后台20项复跑通过，合计253项不同用例零跳过；Worker双类型／Admin类型构建通过。保留夹具密码长度与Windows运行库启动失败及修复记录，不把合成登录、无Redis、小程序未配置当真实商户验收。前端测试技能促成真实渲染异常路径复验；真实手机／商户／完整生产权限／当前CI及协调发布继续开放。五个本轮PG集群清理停止，独立确认无PID／临时口令／监听／自有运行进程；浏览器标签关闭并恢复视口，诊断目录保留。FE-003F和240／164／404不变，原九暂存保持，无生产访问、图片／缓存修改、暂存提交推送部署。以下保留此前阶段记录，退款范围以本次明确决定为准。

收银码后台交互增量（2026-09-20，本地未部署）：实际Admin线下消费页接入海报／原始码、重取、预览及Blob保存，最低应付0.01元和非到账凭据提示保持。客户端精确解码版本/模板/嵌入图片与字节上限，不接受任意SVG；关闭、刷新、切格式、筛选、开详情及会话/权限失效清图、撤销URL并拒绝迟到包。实际Vue/Axios前端最终168项、原生PG/workerd接实际解码器20项，共188项不同用例通过，Worker双类型及Admin类型/构建通过；保留TextDecoder类型修正和既有构建警告。内置浏览器实际1081×792／390×844验证正常/不可用/异常/慢请求、撤权和跨标签退出，控制台错误警告0、手机无横向溢出；14次收银码GET/7次列表GET，业务写入0。高层下载事件超时后，同浏览器CDP确认22265字节海报下载completed；手机视口最初作用于控制标签，纠正目标并恢复临时模拟后正常截图复验，未将失效设置算通过。浏览器API/登录及小程序1px图为合成夹具，不冒充真实微信/SQL整链；前端测试技能推动渲染、保存和生命周期验证。临时PG/UI服务与标签已清理，真实手机扫码、微信发布包/商户域名、Redis、关闭退款、完整生产权限/CI及协调发布仍开放；FE-003F与240／164／404不变，原九暂存保持，无生产、图片/缓存修改或暂存提交推送部署。详见[后台交互与验收边界](workers-ts/docs/admin-offline-order-read.md#收银码后台交互增量2026-09-20本地通过未部署)。以下保留此前阶段记录。

收银码接口与海报增量（2026-09-20，本地未部署）：恢复平台 Admin `GET /order/offline_scan` 的原始码／海报两种模式，固定 H5 origin 与小程序发布页面；无订单、金额或付款能力，不写财务表，调用微信与缓存的副作用单独说明。适配器增加8秒单请求含读体期限、16KiB token／1MiB图片上限、拒绝重定向及入缓存前格式校验；外发前后重新核验管理员。首轮真实workerd复现redirect:error在外发前报错，经独立对照改为manual＋拒绝3xx后，最终四文件77项及双Worker类型通过。内置浏览器验证实际海报／原始码往返、刷新及390×844无横向溢出；直接SVG文档一条无来源脚本错误未归因，标准img嵌入无新增错误。后台按钮／严格客户端解码、真实手机扫码、Redis／微信发布包及协调发布仍开放，不以海报渲染代替真实收银验收；FE-003F和240／164／404保持。无生产访问、图片／生产缓存修改、暂存提交推送部署，原九暂存保持。详见[接口、失败修复和产物验收](workers-ts/docs/admin-offline-order-read.md#收银码接口与海报增量2026-09-20本地未部署)。

后台线下消费页面增量（2026-09-20，本地未部署）：新增 /order/offline 和菜单，严格解码版本/金额/收款状态/详情ID/游标，筛选即时清旧数据，列表与详情保持只读；撤权、会话改变、跨标签退出和迟到响应均不能恢复旧记录或采用新账号重试。65项新Vue/Axios生命周期及44项订单页面回归、15项真实PG/workerd→实际前端解码器联验，共124项不同用例通过；Worker主类型、Admin类型与最终构建通过。内置浏览器桌面1081×792/390×844验证精确/空/异常筛选、时间边界、20+3分页、详情重读、零元不可付、刷新及撤权/跨标签退出；无控制台错误警告或手机横向溢出，22次列表GET/8次详情GET、业务写入0。浏览器接口和登录为本机合成，不冒充真实SQL/商户端到端；前端测试技能要求实际渲染交互及截图，临时PG/UI服务和标签已清理，独立确认无端口/进程/PID/口令残留。未做生产/图片/缓存修改或暂存提交推送部署，原九暂存保持。二维码、安全关闭退款、真实商户设备/完整生产权限/CI及协调发布仍开放，FE-003F与240／164／404不变。详见[页面合同和验收边界](workers-ts/docs/admin-offline-order-read.md#后台页面增量2026-09-20本地通过未部署)。以下保留此前阶段记录。

后台线下消费只读增量（2026-09-20，本地未部署）：补实际平台 Admin scan_list／scan_detail，按 order.view 和事务内重验管理员身份/角色读取独立 type=3；默认全部记录，字面用户搜索无匹配不回退全量，20条游标分页。共用客户详情的不可变收款证据，已核验/待核对/异常明确区分，不把原 paid 标记当到账；不返回 ticket 或代付能力。真实非所有者只读LOGIN/PG16及workerd新增15项、客户HTTP回归20项、运行器54项，共89项不同用例与双Worker类型通过；含撤权、账本损坏、查询超时、并发付款快照及外键保护。初轮测试权限码/合成地址与类型修正、外键拒绝记录保留。PostgreSQL/Workers技能约束最小权限、快照和显式投影。无前端或DDL变更，后台页面/API解码/浏览器、二维码、真实商户及生产权限/协调发布仍开放；FE-003F与240／164／404不变，原九暂存保留，无生产访问或提交推送部署。详见[后台消费记录合同与验证](workers-ts/docs/admin-offline-order-read.md)。以下保留此前阶段记录。

线下消费最低应付增量（2026-09-20，本地未部署）：按用户明确决定，折扣后应付必须至少0.01元；报价与锁后建单拒绝不足一分，不向上取整或免费结算。历史零元仅可读取，余额／微信／支付宝均拒绝，不扣款、不赠分、不预留／外发支付。PC／H5提示、旧零元草稿不重发及异常零元付款状态拒绝已接线。原生PG三套142项、实际workerd/HTTP及选择派发82项、客户端76项、Uni运行时20项，共320项不同用例通过；Worker双类型、PC构建内置检查及Uni类型/H5构建通过。浏览器桌面/390×844验证不足一分拒绝、恰好一分报价、历史零元恢复刷新仍不可付款；两端各create/pay均0，无应用错误，H5仅既有路由弃用警告。两个自建PG集群清理停止，独立检查无PID/口令/端口残留。PG/Workers技能约束交易边界，前端技能要求渲染与交互证据。无生产访问/变更、提交推送部署，原九暂存不变。此前“零元政策待定”由本决定覆盖，安全关闭退款/原生设备/真实Provider/完整生产权限及协调发布仍开放；FE-003F与240／164／404不变。详见[最低应付合同与验收](workers-ts/docs/offline-order-http.md#最低应付政策2026-09-20-最新决定本地未部署)。以下保留此前阶段记录。

线下收银实际连接预检增量（2026-09-20，本地未部署）：新增独立只读权限预检及只返回SQL的共享授权合同，核验实际backend身份、27个目录指纹、21表/列与7序列权限，拒绝危险授权、维护身份伪装及目录漂移；混合SET/ADMIN与INHERIT路径原生测试通过。合同已用于HTTP、本地Hyperdrive与签名回调夹具；本轮55项权限/HTTP/workerd、32项回调及67项静态/运行器检查，共154项不同用例及双类型通过。四个自建集群库/角色清理停机，独立确认无PID/口令/监听或可信测试进程残留，保留诊断目录。PG/Workers技能约束只读事务、有效权限及热路径隔离。预检仅覆盖收银与收款恢复，不认证全应用/生产角色或业务历史；真实Provider/远端Hyperdrive/设备、关闭退款及协调发布仍开放。FE-003F和240／164／404不变，原九暂存与新图确认保留，无生产访问、图片/缓存修改、暂存提交推送部署。详见[实际运行连接权限预检](workers-ts/docs/offline-runtime-permissions.md)。以下保留此前阶段记录。

线下收银正式注册增量（2026-09-20，本地未部署）：新增外部0159／内嵌0165，完整ORM及目录门槛增至277表；补齐原SQL的16条CHECK和显式外键名称，保留旧迁移编号与263表历史生产快照。精确裸ORM仅标记orm-pending，普通install拒绝；显式complete-orm在固定NOWAIT锁内核对七台账全空后仅补函数/触发器，拒绝局部结构、既有数据和权限漂移，不补造历史。真实PG16.15九路径277表／3868列／651约束／1068索引／227序列完整一致，外部与内嵌必须原已受保护，七ORM路径补装及重复核验均保留既有对象OID/文件。最终相关回归分批705项及双类型通过；保留默认workerd启动失败、空表DELETE测试语义、静态public限定表名漏识别与旧路由404断言失败及修正记录，未更改付款实现或放宽目录/资金门槛。八个自建集群及测试库/角色均确认清理停机，无PID/口令/监听或可信测试进程残留，诊断目录保留。PG/Workers技能用于权限、短事务、锁序及维护DDL隔离。真实Provider/设备、完整生产角色、旧候选处置和协调迁移发布仍开放；FE-003F及240／164／404不变，原九暂存和新图确认保留，无生产/图片/缓存操作或暂存提交推送部署。详见[正式注册与空ORM完成保护](workers-ts/docs/offline-order-registration.md)。以下保留此前阶段记录。

线下收银受控安装增量（2026-09-20，本地未部署）：新增独立维护入口及PG16目录指纹，覆盖七张凭据表、十函数、十依赖表及公共支付域约束；首次安装与完整候选重复执行受统一维护锁、固定NOWAIT锁序和短事务保护，局部/旧候选、结构或ACL漂移拒绝，不补造历史。仅追加协议表读/追加、派发五列更新及计价锁函数执行权，独立LOGIN/继承权限边界已测，不冒充完整生产角色就绪。全量外部/内嵌建库发现user表物理列序差异，确认字段与完整索引语义一致后按列名比较共享表，新凭据表仍严格核验。最终安装38项、实际鉴权HTTP19项及PG/workerd7项通过；扩大派发回归暴露上一轮quit_url新增后旧断言未更新，补精确原单完成/退出地址后27项通过，相关最终覆盖91项，另运行器51项及双类型通过。七个隔离集群均独立确认停止，无PID/口令/端口及可信测试进程残留。PostgreSQL/Workers技能指导最小权限、锁序与维护DDL隔离。编号迁移/完整ORM/目录清单注册、空ORM完成保护、真实角色/provider/设备及协调发布仍开放；FE-003F和240／164／404不变，原九暂存及新图确认保留，无生产、图片/缓存操作或暂存提交推送部署。详见[受控安装与协议权限验收](workers-ts/docs/offline-order-installation.md)。以下为此前阶段记录。

线下消费回跳增量（2026-09-20，本地未部署）：微信H5与支付宝WAP完成/中途退出入口现由服务端固定PC/H5 origin绑定原单；调用者只选有限return_client，不接受任意URL或Origin/Referer覆盖。两个Pages入口只做无缓存站内303并丢弃成功/金额/签名等参数；PC新增独立只读结果组件，PC/Uni结果必须显式原单号，不回落或覆盖另一笔本地记录，刷新/重登不建单付款。最终Node四文件170项、workerd三文件93项通过；真实PG/本地Hyperdrive/鉴权HTTP、首次派发及恢复三文件53项通过，重放不改变返回客户端或再次外发；PC实际请求/会话257项、Uni页面运行时46项通过。Pages Functions编译和生成类型核验通过；浏览器桌面与390×844验证回跳→登录→原单→刷新，伪成功参数不改服务器未付事实，首轮双方业务POST均0。缺单号H5负面导航被浏览器阻止，HTTP400/no-Location由实际处理器日志及workerd测试验证，不冒充视觉通过。旧无上下文入口不改签，有不合法上下文的候选入口拒绝继续，存量版本协调仍开放。真实商户域名/平台/原生设备、受控DDL/ACL、零元/安全关闭退款和发布门禁未过，FE-003F及240／164／404不变；原九暂存及新图确认保持，无生产或图片/缓存修改、提交推送部署。详见[回跳合同与验证边界](workers-ts/docs/offline-order-http.md)。以下为此前阶段记录。

线下消费找回增量（2026-09-20，本地未部署）：新增本人 `/order/offline/history`，固定20条倒序键集分页／精确单号搜索，有界只读快照、当前账号重验、原单准入核对；游标限本人记录，隐藏单可查，列表不含付款状态／请求键／入口，不把消费线索当作收款凭据。PC／Uni支持无本地记录时明确找回原单，持久化version=2原单指针而非伪造新建单UUID；刷新／重登只读同单，不覆盖另一笔未确认消费，深链付款／SDK前也先保存原指针并校验同源锁。实际workerd/JWT/本地Hyperdrive/独立非所有者LOGIN/PG16.15 HTTP18项、模型与缓存三文件95项（其中模型63）、Uni运行时44项、PC相关255项及三端产物3项通过；最终双Worker/前端类型及PC/H5/微信/App构建通过。首次默认2GB类型检查内存耗尽，改按项目既有4GB上限后修正两处测试类型并复跑通过。浏览器独立PC/H5来源从无待处理记录找回同一单并刷新，20→3分页／他人单无结果；按前端技能修复查询错误提示位置并复验桌面／390×844。共享合成24条记录不变，两端业务POST均0，无应用错误，Uni保留既有路由警告。专属PG与UI服务均独立确认停止。仅是本地接线和分离来源验证，原生真机／真实跨设备会话及provider、平台回跳上下文、安全刷新关闭退款／零元政策、受控DDL/ACL与发布仍开放；240／164／404和FE-003F不变。原九暂存及新图可读确认保持，无生产访问、图片／缓存写、提交推送部署。详见[本人消费找回与验证边界](workers-ts/docs/offline-order-http.md)；下方未接线表述为此前阶段。

线下支付方式发现增量（2026-09-20，本地未部署）：恢复强制顾客鉴权的 `/order/offline/pay/type`，修复旧 PHP 计算后遗漏余额开关的返回缺陷；订单模式绑定原单／原金额／原渠道，以有界只读快照读取当前有效账户、余额和数据库配置，不读写配置缓存，不暴露商户密钥或付款身份。外部方式仅作同源适配器配置／密钥预检，不发起付款；首次外部派发也改用当前数据库配置。PC／Uni 展示不可用原因，付款前再核验，方式读取失败保留原单并清空旧能力；结果页仅读详情。实际 workerd/JWT/本地 Hyperdrive/PG HTTP 15项、独立PG派发27项／路径互斥35项、workerd/PG恢复7项、Node五文件120项及workerd适配器50项通过；后续错误提示保护修复后模型45项、Uni运行时41项复跑通过，重叠批次不累加。最终类型及PC/H5/微信/App构建通过；浏览器桌面和390×844确认开关变化／余额不足阻止付款、读取失败中文提示、伪成功URL不改状态，按前端技能修复禁用文字对比度。合成夹具最终PC2单/2次create/1次pay，Uni1单/1次create/0次pay；无应用错误，Uni保留既有路由弃用警告。四个PG集群与本地浏览器服务均清理并独立确认停止。真实商户／远端Hyperdrive、受控DDL/ACL、跨设备找回、平台回跳上下文、安全刷新关闭退款和零元政策仍开放；240／164／404及FE-003F不变。新图可读确认保持，原九暂存未变，无生产访问、改图、提交推送或部署。详见[支付方式发现与最终验收](workers-ts/docs/offline-order-http.md)；下方尚未接线的表述为历史阶段记录。

线下收银前端增量（2026-09-20，本地未部署）：PC与UniApp加入独立type=3收银入口，分步报价／确认建单／请求支付，先持久化安全UUID及原金额渠道再发送；丢建单响应只允许同键重试，已知单号恢复只读原详情，未知付款不切换路径。结果页不信任URL成功标志／金额或SDK成功回调。修复实际Pinia身份顺序导致的旧身份恢复；共用模型34项、新增Uni运行时11项＋既有结果29项、PC相关255项及Worker四文件68项通过，批次有重叠不累加；最终PC／Uni／Worker类型及PC、H5／微信／App构建通过。已安装浏览器用实际构建／本地合成API完成桌面和390×844交互：PC丢建单／付款响应后1单、同键2次create、1次pay；Uni伪成功结果URL仍显示核对中，两端最终各5次详情GET。无新应用错误，Uni保留依赖层路由弃用警告；不冒充真实鉴权／数据库／provider或原生设备验收。跨设备／存储丢失恢复、支付方式发现、平台回跳上下文、零元与安全刷新关闭退款、受控DDL／ACL及正式验收仍开放。240／164／404与FE-003F不变，新图已确认可读，原九暂存保持，无生产／图片／缓存写或提交推送部署。详见[收银页面与浏览器证据](workers-ts/docs/offline-order-http.md)；下文页面尚未接线为历史阶段。

本批收尾复验（2026-09-20）：下述HTTP增量联合批的两套建库超时，保持代码、断言和30秒期限逐套重跑，外部结算31项／166.18秒、首次派发27项／145.78秒全部通过。五个专属集群均已清理测试库／角色（首轮遗留角色有定点修复记录），独立确认pg_ctl=3、无PID／启动口令、五端口与PostgreSQL／workerd及路径不明对应进程均0；原九暂存路径不变，240／164／404不变。未据复验抹去联合批失败记录，详见[本批执行与清理证据](workers-ts/docs/offline-order-http.md)。

线下收银鉴权 HTTP 增量（2026-09-20，本地接线、未部署）：实际 apiRoutes 新增强制顾客鉴权的建单、原单支付和详情三接口；只接受有界白名单 JSON、持久请求键及服务端金额，修复建单锁后账号 delete_time 复核。详情在 REPEATABLE READ／READ ONLY 快照中复用资金凭据校验，拒绝只有 paid 标志、损坏账单或来源的伪成功，不查询平台、不重发、不切换付款路径。实际 workerd／JWT／本地 Hyperdrive／非所有者 PG16.15 LOGIN 的最终 HTTP 10项通过，包括列级撤权和默认只读后仍可核验已付；建单／余额／HTTP联合84项、226项相关回归及双类型通过。另一个75项联合批73过、2项建库准备超时，失败与逐套复验另载文档，不计作全绿。分步 create→pay→detail 是需配套新页面的合同，不冒充旧PHP组合接口兼容；页面持久化、支付方式发现、零元／刷新关闭退款政策、受控DDL／ACL、真实Redis／provider／远端Hyperdrive及CI发布仍开放。Workers/PostgreSQL技能用于有界请求、短事务及最小权限实证。240／164／404及SUP-001、FE-003F不变，图片已获用户确认可读，本轮无生产、图片或缓存写入，无暂存提交推送部署。详见[收银HTTP合同与执行证据](workers-ts/docs/offline-order-http.md)；下文“公开接口尚未接线”为历史阶段，不是当前代码状态。

线下支付运行时验证增量（2026-09-20，本地候选、未部署）：同一 workerd 二进制经独立目录 Microsoft 签名运行库绕行后，47 项合同测试通过；新增 **7 项实际 workerd→本地 Hyperdrive→原生 PG16.15** 场景通过（66.95秒），覆盖四渠道发起/重放、签名查单、丢失响应禁止重发、未验签不入账及权限故障全回滚/证据恢复。业务使用实际非所有者 LOGIN、真实本地 KV；外发时观察角色无活动事务，最终收款/赠分/节省各一次。170项相关回归和主/运行时类型通过。两轮夹具/启动配置失败有留痕，三集群库/角色清零并独立确认停止、无残留。未修改系统 DLL 或项目依赖，不冒充真实商户、远端 Hyperdrive、正式 HTTP/Queue 或发布验收；240／164／404及SUP-001、FE-003F保持开放。详见[Workers与数据库验证](workers-ts/docs/offline-order-workerd-verification.md)；以下为历史阶段证据，原先“本机workerd尚未执行”的状态已由本条更新。

线下首次支付派发增量（2026-09-20，本地候选、未部署）：支付开关/密钥预检后，将原选择、恢复意图和单次ISSUING记录同事务提交，再在事务外签名/请求；微信H5、JSAPI/小程序及支付宝WAP生成受限支付入口，不当作到账。响应丢失或完成写失败不重发、不换余额；READY只重放原入口，按冻结身份/金额校验并用绑定选择的摘要保护。五分钟保守展示窗口过期只进入核对，未执行上游关闭或宣称未付款；已结算/冲突/人工关闭和在途账号禁用不被晚到入口覆盖。最终三文件100项原生PG16.15（发起27、选择35、恢复38，443.44秒）、十文件222项相关回归及主/运行时双类型通过，测试集群清理且独立核验无残留。新增9项workerd测试两次均因Windows运行时导入前0xc0000005启动失败而未执行，明确不计为通过；未据工具提示安装或升级系统组件。Workers/PostgreSQL技能约束短事务/权限，Firecrawl核对官方合同。安全刷新/新尝试与关闭退款生命周期、旧凭据轮换、公开接口/页面、受控DDL/ACL升级及真实provider/Hyperdrive/workerd/CI发布仍开放；SUP-001、FE-003F及240／164／404不变。已确认新图可读，不重改图片/缓存，九原暂存保持，无生产访问、提交推送或部署。详见[首次派发与恢复验收](workers-ts/docs/offline-order-payment-dispatch.md)；以下保留历史阶段证据。

线下查单证据与恢复增量（2026-09-19，本地候选、未部署）：新增独立不可变查询证据并关联原支付选择及恢复案件，不伪造回调/通知ID；微信签名响应与支付宝直连请求范围明确区分。恢复服务优先重放原证据，无回调时仅按已登记意图及冻结身份查询；证据先提交，随后与回调共用一套财务结算，来源CHECK要求恰有一个。真实SQL复现并修复失败收尾用旧空交易号覆盖新持久证据、导致持续UNKNOWN的问题，补齐跨来源冲突、独立后端竞争、提交后中断及迟到冲突验证。当前代码最终两批五文件153项原生PG16.15测试全过（恢复/预检/会员90项239.84秒；回调/核心63项219.57秒），另195项相关回归、8项退款/配置回归、双类型/运行器语法/空白检查通过。初轮89过/4恢复失败与扩展99过/2错误包装断言失败均有留痕，未放宽业务约束。四个专属集群独立确认停止、库/角色清零，无PID/启动口令/测试端口或可信及路径不明PG进程残留。Workers/PostgreSQL技能约束事务外I/O、证据提交与最小权限。持久派发、原凭据轮换、关闭退款政策、公开接口/页面、受控DDL/ACL及旧候选升级、真实provider/Hyperdrive/workerd/CI与协调发布仍开放；SUP-001、FE-003F及240／164／404不变。九原暂存保持，无生产/图片/缓存写、提交推送或部署。详见[查单证据与恢复验收](workers-ts/docs/offline-order-query-recovery.md)；以下保留此前阶段记录。

线下原身份查单适配增量（2026-09-19，本地候选、未接恢复队列/部署）：新增真实SQL冻结选择/准入核对，提交短事务后再读取配置并调用签名查询；当前应用/商户与原身份不符时网络前拒绝，JSAPI只核对原payer，不采用最新绑定。明确微信签名响应与支付宝直连请求范围两类来源，无伪通知ID、不写回调/恢复/资金状态；查询证据持久化及独立结算接线仍开放。两渠道增加64KiB实际流限额、严格字段校验；微信查单错误也验签，支付宝原始验签节点必须与消费节点一致；含退款含义的关闭状态保留UNKNOWN。独立非所有者LOGIN＋PG16.15最终三文件84项（预检19、回调32、会员33）全过，208.24秒；九文件194项回归、另退款/配置8项、Worker双类型/运行器语法/空白检查通过。首轮83过/1失败为新测试timestamp误用数字，改真实Date并清除多余导入后整批重跑通过，未降低业务断言。两专属集群库/角色清零并独立确认停止，无PID/启动口令/对应监听或可信、路径不明PG进程残留，诊断保留。Workers/PostgreSQL技能约束有界读取及事务外网络，Firecrawl核对官方国内支付宝/微信合同。原身份凭据轮换、查询证据落库、恢复/派发/关闭政策、公开接口/页面、受控DDL/ACL与真实provider/Hyperdrive验收仍开放；SUP-001、FE-003F及240／164／404不变，原九暂存保留，无生产/图片/缓存写、提交推送或部署。详见[查单适配与证据边界](workers-ts/docs/offline-order-payment-query.md)；以下保留此前阶段记录。

线下验签回调与恢复增量（2026-09-19，本地接线候选、未部署）：现有微信/支付宝验签HTTP入口把同一通知的商户/应用/必要付款人交给原子接收事务，事件、outbox、恢复案件、原支付选择绑定一并提交；默认队列使用独立offline_order域，不复用会员结算。新增持久可信回调恢复，无回调/错关联保持UNKNOWN，不用当前商户配置主动查旧交易；一单多交易冲突持久隔离，队列收尾不覆盖既有CONFLICT/CLOSED。真实独立非所有者LOGIN＋PG16.15最终三文件96项（签名链路32、核心31、会员33）全过，218.18秒；六文件97项回归、主/运行时双类型、运行器语法及作用域空白检查通过。覆盖独立Node签名/AES加密→实际Hono分发/验签解密→实际SQL/默认消费者、提交前可见性、原身份不符、缺域DDL/权限失败、Queue发送失败、财务提交后中断和晚到冲突。首轮13项因夹具bigint字符串/数字比较导致Queue观察断言失败，修正后全绿；四处Node/Workers Buffer类型冲突及一处旧静态断言路径同时修正，未降低业务校验。两个专属集群均独立核验pg_ctl=3、无PID/启动口令，测试端口/可信或路径不明PG进程0，诊断保留。Workers/PostgreSQL技能指导原子证据与外部I/O隔离；Queue/KV传输为替身，尚无真实provider/Hyperdrive/workerd验收。原身份支付派发、无回调主动查单/UNKNOWN、公开鉴权接口/页面、受控DDL/ACL、CI及协调发布仍开放；SUP-001、FE-003F和240／164／404不变，九原暂存保持，本轮无生产/图片/缓存写、提交推送或部署。详见[回调链路及验证边界](workers-ts/docs/offline-order-external-payment.md)；以下保留此前阶段记录。

线下外部结算增量（2026-09-19，本地未接线候选、未部署）：新增原支付选择与已持久回调的身份绑定，以及独立type=3原子结算；校验原商户/应用/必要付款人、金额/渠道、事件摘要与已知恢复冲突，重复通知保留首次凭据，不扣余额、不延长会员。积分/节省与余额共用精确策略；迟后写入失败整体回滚，已收到款项不因后来禁用账号或隐藏订单被抹去。候选触发器保护原事件、结算与关联效果，延迟约束禁止无凭据先提交已付标志。首轮外部27项因SQL内CASE语法在安装阶段失败，修正并扩展后外部31项全部通过（162.37秒）；同轮余额47＋选择35项及五文件91项回归、主/运行时双类型/语法/作用域空白检查通过，未将红测计为通过。业务使用真实独立非所有者LOGIN，无余额列写入或现金流水INSERT权限；并发以原生PG阻塞屏障验证。两集群测试库/角色清零并停止，独立核验pg_ctl=3、无PID/启动口令、两测试端口监听及可信或未能识别路径的PG进程均0，诊断目录保留。Workers/PostgreSQL技能指导短事务和最小权限；真实验签、独立回调/主动查单域、持久派发/UNKNOWN恢复、公开接口/页面、受控DDL/ACL与正式环境验收仍开放。SUP-001、FE-003F及240／164／404不变，原九暂存及新图可读确认保持，无生产访问、改图、提交推送或部署。详见[外部结算边界与可重跑证据](workers-ts/docs/offline-order-external-payment.md)；以下保留此前阶段记录。

线下消费支付路径互斥增量（2026-09-19，本地候选、未部署）：余额与外部选择共用订单→顾客锁及READ COMMITTED，新增单订单不可变支付路径、原商户/渠道/付款身份和金额快照；已有外部路径即使没有交易号也拒绝扣余额。余额选择与扣款/凭据同事务提交，延迟约束禁止孤立占位，失败全部回滚；外部未知结果不自动换渠道，外部金额受现有int32分账本上限约束而不缩小余额范围。首轮79项真实PG、最终68项（选择35、会员对账33）、相关90项回归及双类型/运行器语法/空白检查通过，批次不累加；覆盖四类独立连接竞争、身份变更、缺表、迟后失败、最小权限和NaN CHECK。两测试集群清理并独立确认停止。尚无真实provider派发/回调结算/UNKNOWN恢复、公开页面或受控DDL接线，不作为付款成功证明；SUP-001、FE-003F及240／164／404不变。原九暂存路径保留，无生产/图片/缓存写或提交推送部署。详见[支付路径互斥候选](workers-ts/docs/offline-order-payment-selection.md)；以下保留此前阶段记录。

会员支付对账类型增量（2026-09-19，本地修复、未部署）：真实PG16首轮9项失败确认主动查单/人工接受将已付type=2/3及非法类型误认会员；现两入口共用type=0/1且paid=1判断，保留先读同号最多两行再判唯一性及人工行锁。最终33项真实PG、69项相关回归、双类型/运行器语法/作用域空白检查通过，覆盖同号混合类型、独立连接锁后类型变化、迟后写失败回滚及渠道调用前事务已提交；三测试集群清理并独立确认停止。未补造历史、不改余额/会员期，不代表收银provider域或跨表身份统一完成；完整支付/页面/受控发布继续开放，240／164／404及九原暂存路径不变。用户新图可读确认保留，无生产/图片/缓存写或提交推送部署。详见[会员对账类型审计](workers-ts/docs/payment-reconciliation-membership.md)；以下保留此前阶段记录。

线下消费余额结算增量（2026-09-19，本地未接线候选、未部署）：新增基于冻结准入金额的正金额余额支付，按订单→顾客→固定配置锁序原子完成扣款、可见余额流水、积分、节省、推广资格、type3支付状态与不可变凭据；重复请求核验原资金效果，不重新扣款/赠分。修复旧PHP会员分支遗漏线下余额账单及新版类型名称缺口，积分保留旧实际加法而非“双倍”注释，负值/溢出/NaN拒绝。独立非所有者LOGIN真实PG16首批132项、扩展44项、最终余额46项全过；五文件109项和账单四文件48项回归、双类型/运行器语法/作用域空白检查通过，批次不累加为独立用例。覆盖五处迟后权限失败、三类损坏账本全回滚，同单及同余额竞争、锁后会员重验、配置编辑阻塞、已付缺凭据和历史重放；三个隔离集群清理库角色并停机，独立确认无PID/启动口令/监听或可信PG进程残留。Workers/PostgreSQL技能用于短事务、精确金额及固定能力/最小权限。零元结算、支付期限、provider域、公开请求/页面、受控DDL注册及生产验收仍开放；SUP-001、FE-003F与240／164／404不变，原九暂存保持，无生产/图片/缓存操作或提交推送部署。详见[余额结算候选审计](workers-ts/docs/offline-order-balance.md)；以下保留此前阶段记录。

线下消费建单准入增量（2026-09-19，本地未接线候选、未部署）：核对现有会员支付会修改有效期且仅接type0/1，不能直接复用为type3收银。新增真实未支付订单/状态/不可变准入凭据的同事务写入，金额仅接受有界十进制字符串，顾客确认价与锁后权威价不一致则409且无写；按顾客/请求键防重复建单、冲突拒绝，取消/已付重放不释放原键。数据库候选约束和触发器保留金额/身份，缺表不运行时安装；生产模型入口/迁移/公开路由均未接入。首轮14项真实PG失败揭示列级更新权限不足以取得配置表SHARE锁，现用固定两表、无参数的受控锁函数，业务角色不获得配置写权限。最终独立非所有者LOGIN/PG16.15两文件93项（候选26、报价67）及五文件108项回归、Worker双类型/运行器语法/作用域空白检查通过；覆盖迟后权限失败全回滚、同键竞争、用户锁后重验、编辑器真实阻塞和误授权限破坏拒绝。两集群清理测试库角色并停止，独立核验无PID/启动口令、测试端口和可信PG进程残留，诊断目录保留。Workers/PostgreSQL技能指导短事务、固定能力及最小权限。建单成功仍不是付款，余额扣款/资金账单/type3终态/节省及赠分、provider意图/回调/主动恢复、受控DDL目录与ACL注册、公开请求/页面/正式环境仍待完成；SUP-001、FE-003F及240／164／404不变，原九暂存保持，无生产/图片/缓存操作或提交推送部署。详见[建单准入及后续支付合同](workers-ts/docs/offline-order-admission.md)；以下保留此前阶段记录。

线下消费报价增量（2026-09-19，本地候选、未部署）：旧PHP的`other_order.type=3`是独立无商品收银域，不等同商城线下付款或会员卡激活；真实Hono/JWT/PG红测确认报价路由缺失。现恢复`POST /api/order/offline/check/price`，限制1KiB请求/金额上限，以同一SQL快照核验当前用户、会员期限、全局开关与offline权益，整数分截断并保留“数字0表示无会员报价、字符串金额表示有效报价”的旧合同。独立只读LOGIN验证实际鉴权、权限故障、他人隔离、配置优先级、鉴权后状态变更、未提交隔离与提交后重读；建单及支付方式两条路由仍未开放，不以报价代替收款。最终PG16.15两文件83项（报价67、既有节省账本16）、五文件107项相关回归、主/运行时类型和运行器语法通过；初轮3项404、扩展66项及测试类型修正记录保留。四个临时集群清理测试库/角色并停止，独立核验无PID/临时口令/端口或可信PG进程残留，诊断目录保留。按Workers/PostgreSQL技能限制请求和读取并验证最小权限，无新DDL/绑定/依赖。完整type3建单、扣款、回调、赠分/节省账本、页面/真实Hyperdrive/provider和发布仍开放，SUP-001、FE-003F及240／164／404不变；原九暂存、新图可读确认保持，无生产访问、图片/缓存操作或提交推送部署。详见[线下消费合同、缺口及验证](workers-ts/docs/offline-order-quote.md)；以下保留此前阶段记录。

会员节省金额账本增量（2026-09-19，本地候选、未部署）：真实余额支付/outbox红测确认原TS无节省金额写入。新下单现版本化保存准入后的付费会员资格、商品优惠、会员运费与会员券来源；支付后沿用既有事务/锁序只记根单一次，唯一键冲突核对不可变金额，失败整体回滚，顾客供应商/退款子单按自身数量快照展示。普通等级/活动降价及线下或满额包邮不冒充付费会员节省，旧快照不重建、旧账本不覆盖；退款后保留PHP历史累计优惠口径，不重复增加子账。真实会员首页另复现并修复优惠券Date参数未走时间列编码的问题；一分钱运费优惠改随子单原运费减应付运费，避免独立舍入错位。最终原生PG四文件99项、完整迁移/独立业务角色五文件133项及扩展会员券确认30项通过，六文件190项纯计算/运行器/支付任务回归、Worker主/运行时双类型与运行器语法通过（批次重叠，不相加）。保留首次空账本、日期失败、快照错误提示不兼容与默认运行器缺维护权限的红测记录；仅两套明确的ACL/session反例加入全新本机维护集群允许列表，未提升业务或生产权限。八测试集群均清理夹具库/角色并停止，独立核验无PID/启动口令、八端口监听和受信PG进程；诊断目录保留。Workers/PostgreSQL技能指导事务、读取界限及最小权限验证，无新DDL/绑定/依赖。线下other_order节省金额、其他历史报表、真实角色/Hyperdrive/provider及发布仍开放；SUP-001与240／164／404及原九暂存保持不变，无生产访问、图片/缓存操作或提交推送部署。详见[会员节省金额审计与重跑命令](workers-ts/docs/order-member-economize.md)；以下保留此前阶段记录。

会员证据本轮收尾（2026-09-19）：追加活动/确认三文件292项零失败/跳过；加强原单48.00商品＋3.00会员运费－1.00积分＝50.00实付断言后，生命周期3项再次通过，主类型复跑通过。九个尝试集群已独立核验无PID/启动口令、九测试端口监听与受信PG进程均0；完成的运行器均报告测试库/角色清理，诊断目录保留。首次沙箱进程查询被拒绝不作零进程证据，获批只读主机复验成功。原九暂存及240／164／404不变，无提交推送部署。

下单会员优惠证据增量（2026-09-19，本地候选、未部署）：四项真实PG红测确认新订单未保存已计算的会员优惠，以及活动降价误计入会员优惠。现仅从准入后的普通商品会员计算器保留整数分优惠，写入每件商品的vip_truePrice与price_type；活动/不适用权益明确为零，不再以售价差推断会员权益。既有实付单价不再次扣减会员优惠；真实连续两次退款、分批发货失败回滚后重试、跨供应商分单及重放验证单价优惠按子单数量守恒，供应商导出/配货单读取旧权益而不受当前SKU或权益变更影响。新增14项下单证据与3项完整迁移/独立非所有者业务连接生命周期验证；相关7文件147项、扩展3文件74项、独立生命周期3项及3文件142项分配/运行器回归均通过（批次有重叠，不相加为独立用例总数），Worker主/运行时双类型与运行器语法通过。首次夹具跨属运费模板已按正确所有者修正后取得4项红测；两次沙箱PG启动失败及一个建库前拒绝的错误测试文件名未计为通过。Workers/PostgreSQL技能指导沿用既有事务/锁序与独立角色；无新增DDL、绑定或外部调用。旧快照不回填，顾客详情节省金额账本、其他历史报表及真实角色/Hyperdrive/provider与发布仍开放；活动报价指纹变化需要重新确认，不绕过旧回执校验。SUP-001与240／164／404保持不变，原九暂存不变，新图可读确认保留，无生产/图片/缓存操作或暂存提交推送部署。详见[会员优惠证据与可重跑验证](workers-ts/docs/checkout-line-finance.md#membership-evidence-increment--2026-09-19-local-not-deployed)；以下保留此前阶段记录。

供应商导出与旧任务历史增量（2026-09-19，本地候选、未部署）：真实PG红测复现退款剩余子单漏导出、新快照商品/价格丢失、售价误计会员优惠、错UID明细泄露、订单头/商品及队列列表/count跨代次和空表缺filekey。新版Orders显式selection=exact，对齐现代可见子单，缺失/错属/不符发货条件整批拒绝；旧无版本入口保留PHP的pid/store过滤。导出兼容现代product/sku及旧productInfo，金额以整数计算，旧会员优惠按每行截断两位且不回退售价。订单导出、队列列表/明细使用只读REPEATABLE READ和不放宽会话的5秒单语句上限；商品读取保留UID/供应商归属、每单200/整批2000行、64KiB单快照/8MiB聚合字节界限，异常明细或超长单元格拒绝而非静默截断。CSV下载验证字段、行/单元格/4MiB文件界限与公式字符，has_more拒绝下载不完整文件，空表保留固定列；重复查询参数、非法精确分页/类型拒绝。最终新增19项SQL、15项真实CSV字节、1项真实SFC请求、6项分页前置测试；相关PG16.15三文件50项（260.25秒）和11文件150项回归零失败/跳过，Supplier类型/2287模块构建、Worker主类型和运行器语法通过。中间扩展测试仅一项错误把业务权限码400011断言成HTTP403，已核对合同修正，未放宽拒绝或数据断言。按Workers/PostgreSQL技能限制读取一致性与权限；按前端测试技能使用现有Playwright 1.62.1/隔离Chrome（本会话无配套browser技能），127.0.0.1:5182/orders桌面1280×900及手机390×844验证选单CSV、BOM/引号/公式、分页错误无下载、空任务文件和只读明细；每端6次模拟GET、2次下载，控制台/外网/写请求均0，页面无横向溢出。截图/脚本仅在仓库外cinashop-export-qa-02fa0f6276a4453b97e57b51c24d45fd临时目录；加载遮罩/提示动画退场后补拍稳定证据。五个本轮隔离PG集群均清理测试库/角色并停止，独立核验PID/启动口令不存在、六端口监听及受信测试PG进程均0；本地Vite已停止。本轮不关闭现代会员优惠快照留存、旧队列复合身份/完整DTO、财务精确选择、真实角色/Hyperdrive/provider及正式发布验收；SUP-001与240／164／404不变，原九暂存不变，新图可读不重做，无生产访问、提交推送部署。可重跑证据为workers-ts/test/supplier-export-history-reads.test.ts与supplier-csv.test.ts；以下保留此前阶段记录。

供应商Orders页面增量（2026-09-19，本地候选、未部署）：6项真实Vue/Router/Axios红测确认旧详情残留、发货目标随另一详情切换（25表单实际提交32）、准备阶段不可取消、备注重复提交、会话残留与列表代次/选择失效问题。现详情与发货目标独立，读取使用会话/请求代次及取消信号；重载、关闭、切单、离页和账号/权限失效清空对应内容，列表不跨页保留“本页”选择；详情/商品最小展示合同、ID及数量边界校验失败关闭并提供重试。备注、收货、发货与打印固定提交目标/载荷，确认框覆盖在途互斥并在确认后复核当前视图；结果未知在本次页面挂载期间暂停该订单写入，明确拒绝才允许纠正后重试，成功后读取失败单独提示而非误报发货失败；队列/导出补迟到响应与会话隔离，但不作为其完整合同验收。最终39项新增测试连同相关7文件111项、Supplier类型/2287模块生产构建和Worker主类型（4GiB）通过。按前端测试技能以已有Playwright 1.62.1/隔离Chrome回退验证127.0.0.1:5182/orders，1280×900和390×844覆盖详情关闭/失败重试、发货准备取消/再开、真实表单分批提交、成功读回失败、未知结果禁用、A→B→A会话；桌面28次模拟API含2次被夹具接收的PUT，外网及控制台异常0，手机无写入、页面390/390、长目标335/335且底部取消可达。初轮浏览器隐藏radio输入被可见标签遮挡导致测试定位超时，改用可见标签点击并断言选中后完整重跑，不计作应用缺陷或通过。截图/脚本仅在仓库外临时目录cinashop-orders-qa-3c4bd3a8b1e04d968a421eeaf601ab7a；服务已停止且独立确认5182监听0。新图可读不重复处理，原九暂存保持，无生产、提交推送部署。跨刷新写入恢复/幂等权威、完整历史DTO与队列导出、真实角色/PG/Cloudflare/CI及发布仍开放；SUP-001与240／164／404不变。以下保留此前阶段记录。

供应商配货单页面增量（2026-09-19，本地候选、未部署）：5项真实Vue/Router/Axios红测复现重载保留旧收件信息、同组件URL不刷新、会话切换仍可打印、错单响应被接受及卸载后迟到回写。现接入既有会话失效作用域、请求代次/取消和URL监听，切单/重载/离页/账号或权限变化同步清空；增加显式重试，整批校验ID、必需字段、200商品边界及逐分小计，货币不经Number丢分；按钮和原生beforeprint均在失效时禁止旧页输出。隔离Chrome在127.0.0.1:5182（1280×900、390×844）验证7商品按6+1分页、25→26迟到→27、读取失败/错单/重试、非法ID、A→B→A会话和打印媒体隐藏；移动长地址裁切已实测修复，页面390/390、地址行366/366无溢出，控制台/外网/写请求均0。25项新增生命周期/合同测试连同相关6文件72项、Supplier类型/2286模块生产构建及Worker主类型通过；测试全局名错误已改为显式隔离对象，未给Worker添加DOM类型。前端测试技能因本会话缺少配套browser技能回退已有Playwright；HTTP/登录/打印调用均为隔离夹具，不冒充生产登录、原生PG贯通或实体打印。脚本与截图位于仓库外临时目录cinashop-picking-qa-943219d2fbf54b0d91d7ee364bad3126，5182已退出并独立核验无监听；原九暂存保持，无提交推送部署。SUP-001和240／164／404不变，Orders列表/抽屉/发货表单、队列/导出、真实角色/Cloudflare/CI与协调发布继续开放；以下保留此前阶段记录。

供应商配货/待发货商品/状态增量（2026-09-19，本地候选、未部署）：三个读取入口已使用同一只读快照并校验归属、数量与JSON字节边界；配货价格按整数分计算，缺少售价不再以结算成本替代。真实PG进一步复现退款后根单发货误判多个待发货子单，以及读取拒绝错门店子单但直接发货成功；共享写入解析器现排除已退款子单并复核顾客/门店及锁前后归属，保留合法平台分单访问。最终订单/权限联合63项、原生财务/发票/账本与结果合同118项、相关单元72项及双类型全部通过；PGlite夹具修正真实SQL返回形状后扩展181项通过，9项原生专用并发已在PG覆盖。主类型默认堆不足后按既有4GiB配置重跑通过；九临时集群独立确认停止，无PID/口令、测试端口或可信PG进程残留，输出丢失的旧尝试不算通过。Workers/PostgreSQL技能指导有界一致性读取与最小权限验证。SUP-001、240／164／404及原九暂存不变，无生产/图片/缓存或提交推送部署；页面生命周期、完整PHP合同与上线验收仍开放。详见[供应商操作读取与履约审计](workers-ts/docs/supplier-operational-reads.md)；以下保留此前阶段记录。

供应商拆单历史读取增量（2026-09-16，本地候选、未部署）：真实PG复现退款后主单历史误报多个待发货子单、续退并发时旧金额配新商品及归属缺口。split_order现独立读取同一只读快照，限定顾客/供应商/门店、校验商品归属与JSON，SQL出线前限制200子单/全响应200商品/单快照64KiB；保留历史DTO、旧快照与平台分单根的访问边界，未改发货/退款/账务写入。最终真实Hono/JWT/只读LOGIN/PG16联合32项和相关64项回归、双类型及运行器语法均通过；四临时集群独立核实停止且无测试库角色、PID/口令、端口或可信PG进程残留，诊断目录保留。Workers/PostgreSQL技能指导读取一致性和权限验证。新图可读已记录，不重复改图；配货单、待发货商品、状态/报表、完整PHP合同、页面/正式环境与发布仍开放，SUP-001和240／164／404不变，原九暂存保持，无生产操作或提交推送部署。详见[拆单历史审计](workers-ts/docs/supplier-split-order-reads.md)；以下保留此前阶段记录。

供应商订单一致性读取增量（2026-09-16，本地候选、未部署）：原列表已排除支付根单，无重复列单缺陷；三项真实PG红测确认并发续退后列表2行却count=3、旧42.20订单头搭配29.40当前商品，以及错UID商品仍返回。现仅list/info委托独立读取服务，使用同一只读REPEATABLE READ事务及不放宽会话的5秒预算；保留snake_case、count、降序、关键词与顾客删除后员工历史可见性。商品UID/显式供应商归属校验，兼容整组无归属元数据及type0供应商旧快照；200商品与组合64KiB TEXT在SQL出线前限制，非法JSON、错属或过界明确失败，保留合法字符串/null/空串；订单头不再读取未返回的私有TEXT。分页/筛选/重复参数和ID加界，不改通用分页、财务写入、DDL/ACL或绑定。真实Hono/JWT/角色/密码与独立非所有者SELECT-only账号验证权限、首次/续次并发、三次退款55.00守恒及允许边界，最终PG16.15三文件56项（Supplier22、Admin19、员工退款15）240.89秒通过，另六文件81项与Worker双类型/运行器语法通过；保留初轮2项失败及加强并发计数时序后的3项红测证据。四集群/测试库角色已清理停止，独立核验无PID/口令/四端口监听/可信测试进程残留，诊断目录保留。Workers/PostgreSQL技能指导一致性与最小权限；未把上一轮线上图片验收当作订单浏览器验证。配货单/拆单/状态/报表导出、完整PHP合同与页面生命周期、真实Redis/provider/Hyperdrive/CI及发布继续开放，SUP-001不勾选，240／164／404及原九暂存项不变，无生产/图片/缓存或暂存提交推送部署。详见[供应商订单读取审计](workers-ts/docs/supplier-order-generations.md)；以下保留此前阶段记录。

后台订单浏览器—HTTP—PostgreSQL整链增量（2026-09-16，本地验收、未部署）：增加显式运行器与可重复浏览器回归，真实Admin构建产物经HTTP、Hono双别名、JWT/密码摘要/角色到独立只读PG16.15连接；订单/权限/待办响应不再由浏览器伪造，仅登录品牌为壳夹具。两端各九个SQL检查点验证21条真实分页、首次/再次退款生成子单、42.20旧响应不能覆盖同ID的29.40当前单、角色及SELECT撤销/恢复、密码变化退出登录；两次越权核销POST均拒绝，读请求不改业务行。实测并修复地址重复省份、普通积分抵扣错用积分支付字段：主单100积分/1.00元、续拆剩余60积分/0.60元分别展示，财务SQL未改。最终桌面1440×1000与手机390×844两场景36.20秒通过、三文件80项回归及Admin构建/Worker双类型通过；截图核看无溢出/页面异常/控制台错误警告/外部请求，各一条预期旧请求取消。保留初轮误把30.00商品价当实付的两项测试失败，按既有积分财务合同修正为29.40并增加子单/根金额精确断言；环境白名单类型错误已修复。前端测试技能因未暴露browser技能使用现有Playwright，PG技能指导分离维护/业务权限。四集群及测试库/角色、HTTP监听/驱动均独立核实已清理或停止，无PID/临时口令/可信PG进程残留；诊断目录及仓库外截图保留。真实生产角色/Redis/provider/Hyperdrive、写流程、其他读取端/报表、CI和协调发布仍开放，无生产/图片/缓存操作或暂存提交推送部署，原九暂存及240／164／404不变。详见[后台订单真实整链](workers-ts/docs/admin-order-generations.md#joined-admin-browser-regression-2026-09-16)；以下保留此前阶段记录。

后台订单页面跨代次接入增量（2026-09-16，本地候选、未部署）：列表消费服务端精确total，原支付单号可搜索当前子单或单独打开历史详情；主单提示原始金额/商品不可与子单重复累计，补子单导航、退款/核销状态及现代SKU/单价快照，缺失单价不再显示虚构零。响应校验分页/身份/商品和子单归属，路由复用、重读失败、卸载与账号A→B→A均清旧数据并拒绝迟到包；订单相关请求同步冻结凭据，核销确认绑定原码/数量/页面，已有只读权限不暴露写入口。最终五文件117项零失败/跳过（新增实际SFC/API41项）及Admin类型/构建通过。前端测试技能因会话未暴露browser技能使用现有Playwright1.62.1/Chrome153；真实Vue页面、本地拦截API夹具在1440×1000与390×844完成翻页→主子单导航→错误刷新→迟到响应→会话失效，两端各16个GET、零业务写/外部请求/控制台错误警告。首轮浏览器实测暴露清零total触发分页组件退回第一页，已修复加载期计数及显隐，原流程两次复跑通过，截图已核看；临时驱动/截图/结果位于仓库外，Windows ESM路径准备错误及两条既有VueUse构建注释警告留痕。未启动SQL服务器；本轮浏览器不冒充真实认证/HTTP后端/PG整链。订单页真实整链、其他读取端/报表、正式角色/provider/Hyperdrive、持久写回执恢复及协调发布仍开放。无生产/图片/缓存操作或暂存提交推送部署，原九暂存与240／164／404不变。详见[后台页面与浏览器验收](workers-ts/docs/admin-order-generations.md#admin-page-integration-and-browser-qa-2026-09-16-local)；以下保留此前阶段记录。

后台订单跨代次读取增量（2026-09-16，本地候选、未部署）：真实Hono/JWT/角色/PG16复现后台平铺列表同时返回支付根单和当前子单，以及并发退款后的旧订单头配新商品状态。现代Admin双别名改由同一有界只读REPEATABLE READ快照读取，列表排除已退役根单、返回精确total，原支付单号可定位同UID当前子单，子单号保持精确；详情补子单头导航数据并统一隐藏/系统删除过滤，不将源商品快照冒充当前履约。增加分页/选择器/重复参数、对象JSON、商品/子单数量及SQL出线前组合TEXT字节限制，数据库权限错误不转空成功。最终本机PG16.15三文件46项零失败/跳过（新Admin19、原顾客12、原员工退款历史15）及三文件38项合同/运行器回归通过，Worker双类型/脚本语法通过；只读独立LOGIN验证无表所有权/UPDATE/超级用户/建库/建角色/BYPASSRLS。保留初始2项红测、列名泛型类型错误及扩大批次43/44的缺失认证头夹具错误，均已修正且原拒绝断言未放宽。四个获批自建集群独立确认停止、无PID/临时口令/监听，可信PG进程0；首次沙箱启动失败目录由原沙箱单独复验未运行，未改ACL或处理其他服务。现代平铺履约视图不等同PHP旧分组lst/报表；后台页面消费真实总数/子单导航与浏览器、其他读取端/报表导出、Redis/正式角色/Hyperdrive/provider、自身CI及协调发布仍开放。无生产/图片/缓存操作、浏览器重跑或暂存提交推送部署，原九暂存及240／164／404不变。详见[后台订单读取审计](workers-ts/docs/admin-order-generations.md)；以下为此前阶段记录。

顾客订单删除浏览器—HTTP—PostgreSQL整链增量（2026-09-16，本地验收、未部署）：新增可重复的显式运行器入口和双端整链测试，实际构建产物经本地HTTP、完整Hono路由、JWT签名/密码摘要/用户状态检查到独立非超级用户PG16业务连接；订单/退款响应不再由浏览器夹具伪造。每端七个SQL核对点验证未付款保留不写、提交已落库但回包丢失时不重试、库存/积分恢复与单次取消/删除审计、手动刷新和重复删除拒绝；实际退款子单删除不改变资金/不可变证据，终态重放无写，原商品历史仍可在双端查看；新的真实处理中退款即使摘要误为完成仍拒绝删除。缺令牌/伪造/跨用户HTTP拒绝与私有no-store均验证。最终双端2场景42.70秒、原生PG回归32项163.42秒、运行器26项及双类型全过；初次Buffer/ForkOptions/环境类型与运行器表驱动参数错误已修正留痕，未放宽业务断言。前端测试技能因未暴露browser技能使用现有Playwright；PostgreSQL技能指导分离维护/业务权限，驱动环境白名单断言无数据库连接或宿主凭据。桌面1440×1000和手机390×844截图已核看，无页面异常/横向溢出/外部请求；各一条预期丢包错误，移动两条既有SDK警告。四个PG集群独立确认停止，无PID/口令/监听/可信测试PG进程及浏览器驱动残留；诊断目录保留。Redis未接入、支付状态为合成设置，非真实provider/Hyperdrive/workerd/原生设备或生产验收；后台读删/报表导出、CI及协调发布仍开放。240／164／404及原9暂存项不变，无生产/图片/缓存写、暂存提交推送部署。重现入口见[订单删除整链工作流](workers-ts/docs/customer-order-deletion.md#opt-in-joined-browser-workflow)；以下保留此前阶段记录。

顾客订单删除双端入口增量（2026-09-16，本地候选、未部署）：PC与UniApp订单列表接入现有删除接口，共用终态/归属标识准入与严格空成功回执校验；未付款确认明确取消和资源释放，已付款明确不发起退款且保留售后历史。确认前后绑定当前账号、页面代次和原订单，屏蔽重复点击；失败或回执未知不乐观隐藏、不自动重试，显式刷新核对；成功从第一页重读避免偏移分页漏单。新增PC16项、移动18项交互/边界测试；最终PC业务255项及图片9项、移动376项全过，两端类型及PC/H5/小程序/App构建通过。前端测试技能因本会话未列出browser技能使用现有Playwright，真实构建产物在隔离本地HTTP夹具上完成1440×1000与390×844的取消/确认、回执丢失后禁重复与刷新恢复、退款子单隐藏、重载/筛选空态；截图已核看，无溢出/页面异常。最终两端各一条预期丢包错误，移动两次加载各一条SDK路由弃用警告；初轮原生按钮定位与截图动画等待已修正，曾拦截SDK装饰图片请求，最终外部请求为0。八个临时监听及驱动均已确认退出，截图/脚本留在仓库外临时目录。本增量不等于真实数据库/认证/provider/Cloudflare或原生设备验收；后台删除/报表/导出与协调发布仍开放。240／164／404和原9暂存项不变，无生产数据/图片/缓存写入或提交推送部署；以下保留此前阶段记录。

顾客订单删除跨退款代次增量（2026-09-16，本地候选、未部署）：六项真实PG红测复现退款子单/未付款订单不能删除、原始状态2/4/5被误放行及处理中退款被旧摘要漏判。现按PHP语义准入；未付款删除复用取消补偿事务，删除审计失败整笔回滚；已付款终态仅软删除，不重复退款或补偿。支付根单先于子单加锁，等待后复核归属/状态/代次，检查五类处理中退款但不反向锁退款行。连续三次实际退款中隐藏前代订单后，后续退款、终态重放与历史读取仍通过。新增20项删除场景与12项代次读取联合全过，活动回归41项、本地合同45项及双类型通过；旧活动夹具改用真实申请生成预占，未放宽资金/库存/锁等待断言。六个临时PG集群独立确认停止，无PID/口令/监听/可信测试进程残留。Workers/PostgreSQL技能指导复用补偿事务及一致锁序。前端删除确认、其他后台/报表/导出、真实认证/角色/provider/Cloudflare/CI及协调发布仍开放；240／164／404与原9暂存项不变，无生产/图片/缓存修改或暂存提交推送部署。详见[顾客订单删除审计](workers-ts/docs/customer-order-deletion.md)；以下为此前阶段记录。

顾客普通订单跨退款代次读取增量（2026-09-16，本地候选、未部署）：两项真实PG红测复现旧PC列表及顾客统计在退款拆单后漏掉子单，核对PHP搜索器后将字面pid=0恢复为pid>=0；保留旧PC负退款状态的特殊范围。订单/退款计数合并为单条SQL，退款计数绑定原单及顾客身份，保留已隐藏原单的合法历史；核销计数排除审计根。现代列表、旧PC列表/总数及详情使用同一只读REPEATABLE READ快照，明细排除系统删除子单，5秒语句上限不放宽更严格会话值；配置KV不进入事务。真实连续三次退款、六个独立LOGIN交错、归属/删除/权限/超时均验证，订单金额分配55.00守恒（不是退款后净收入）。最终真实PG30项、本地相关回归83项及双类型通过；扩大测试的一项失败为合成订单幂等键重复，已修夹具且未放宽约束/业务断言。四集群独立停止，无PID/口令/监听/可信测试进程残留。Workers/PostgreSQL技能指导短一致性事务及批量查询。删除订单、其他后台/报表/导出、真实角色/provider/Cloudflare/CI及协调发布仍开放；240／164／404与原9暂存项不变，无生产/图片/缓存修改或暂存提交推送部署。详见[顾客订单跨代次读取审计](workers-ts/docs/customer-order-generations.md)；以下为此前阶段记录。

后台/供应商退款历史增量（2026-09-16，本地候选、未部署）：两项真实数据库红测确认详情缺少归档商品，现复用顾客端受保护有界投影，新增refundHistory并保留原申请/支付/审核身份；供应商列表及详情补充store归属一致性。两端以只读REPEATABLE READ读取，超时不放宽更严格会话值；证据异常显式显示错误，权限/缺表失败不伪装旧版回退。新增商品快照界面及共享身份/数量/边界解析，名称规格按纯文本显示，不加载商品图片或新增退款能力。最终真实PG历史33项及原合同回归125项、契约/前端/运行器116项、双类型和两端构建通过；隔离Chrome桌面1440×1000及移动390×844完成打开、切单、错误/旧版、刷新恢复和重载验证，无控制台异常、外网或写请求，截图已核看。三个PG集群独立确认停止，无PID/口令/监听/可信进程残留，浏览器及本地服务关闭。Workers/PostgreSQL技能指导一致性投影，前端测试技能因未列出browser技能而走已有Playwright回退。用户新图可读确认保留，本轮未重做图片/缓存或连接生产；全量供应商旧DTO、通用订单读删统计、公共v2、真实认证/provider/Cloudflare/CI与协调发布仍未完成，240／164／404及原9暂存项不变，无暂存提交推送部署。详见[退款历史读取与两端界面验证](workers-ts/docs/refund-read-generations.md#admin-and-supplier-extension)；以下为此前阶段记录。

顾客退款历史读取增量（2026-09-16，本地候选、未部署）：真实连续退款复现并修复原商品行被余单消耗后详情丢失、历史名称跟随当前行变化两项缺口。完成态v2改读受保护历史回执，在只读REPEATABLE READ事务内核对身份、原申请、父单、数量及各代分区；有界SQL只返回公开商品标量与紧凑身份，不传输完整私有财务快照。新增physicalOrderId不替换原申请/支付身份，现有PC/UniApp解析与列表链接保持兼容。扩大红测另外定位并修复目标行复用、未选行数量、伪整单映射和非法嵌套JSON四项；证据缺失/错误不回退当前商品，缺表/权限失败，旧版/处理中读取不依赖候选表。双独立LOGIN交错验证退款期间详情快照一致、只读及超时不泄漏，最终真实PG历史/权限28项、原合同回归146项、运行器18项及双类型通过。五个隔离集群已独立确认停机，无进程/监听/临时口令残留。Workers/PostgreSQL技能用于限制公开投影和一致性事务；Admin/Supplier历史投影、通用订单读删统计、公共v2、真实Cloudflare/角色/provider/浏览器/CI和协调发布仍开放，240／164／404及原9暂存项不变，无生产写、暂存提交推送部署。详见[顾客退款历史读取审计](workers-ts/docs/refund-read-generations.md)；以下为此前阶段记录。

独立运行账号业务验收增量（2026-09-16，本地候选、未部署）：完整165步/270表结构下，以独立非所有者LOGIN执行下单、供应商入账、申请发票、原子退款、余单拆分发货、收货及再次退款；55.00余额/票额、100积分和31.00供应商退款守恒，重复完成无新增效果。显式核验35表、10序列USAGE及8表UPDATE(id)，四类迟后追加权限撤销整笔回滚、恢复可重试；证据改删/截断、DDL、复制绕过、维护角色及setval拒绝。原表级计价锁确需写权限，列级UPDATE也允许改主键，不把该测试角色宣称为纯只读或完整生产最小权限；未移除锁/约束或改业务DDL。修正夹具建表依赖顺序、默认权益ID冲突及新SKU长度，补实测发票/交易/面单行锁权限。最终独立权限10项、扩展业务199项、运行器17项零失败/跳过，双类型通过，八个隔离集群独立确认停机且无进程/监听/临时口令残留。按Workers/PostgreSQL技能区分原生PG与真实Cloudflare验收，完整应用权限、公共v2、provider/Hyperdrive/浏览器/CI及发布仍开放；240／164／404和原9暂存项不变，无生产写或暂存提交推送部署。用户已确认新图可读，发布条目的旧图片待修描述已同步。详见[运行账号业务与权限审计](workers-ts/docs/refund-runtime-permissions.md)；以下为此前阶段记录。

退款拆单证据安装/注册增量（2026-09-16，本地候选、未部署）：退款实体拆单及后续履约分支两张不可变回执表已接入外部0158、内嵌0164和完整ORM，新增独立维护命令。要求发票v2保护先完成，共享安装锁并依次NOWAIT锁五表；首次/完整重复/显式空ORM完成均受PG16结构指纹、维护所有权和ACL核验保护，旧候选、非空裸表及权限漂移拒绝而不修补证据。真实独立LOGIN仅可读/追加，改删截断、DDL、函数执行和角色/复制绕过均拒绝。最终九路径270表、3770列、608约束、1036索引、227序列零差异；迁移11文件365项、业务6文件300项、静态5文件120项零失败/跳过，双类型通过。五个隔离集群均清理测试库/角色并独立确认停机，受信PG进程0；首轮未使用测试导入已通过新增原始ORM入口验证解决。Workers/PostgreSQL技能指导依赖锁序、事务回滚及权限分离。公共退款v2未启用，完整应用角色、各代次读删统计及未支持业务合同、Hyperdrive/provider/浏览器/CI与协调发布仍开放；历史生产263表快照、240／164／404及原9暂存项不变，无生产、图片/缓存重跑或暂存提交推送部署。详见[退款拆单安装与注册审计](workers-ts/docs/refund-split-installation.md)；以下为此前阶段记录。

发票证据迁移注册增量（2026-09-16，本地候选、未部署）：外部0157、内嵌0163和完整ORM已注册历史/拆票表，共享受控安装状态机；外部/内嵌建库直接具备v2保护，ORM裸表明确为orm-pending，须在三表均空时显式完成保护，已有数据/结构或ACL漂移拒绝而不补造历史。九路径真实PG16.15审计均为268表、3738列、594约束、1030索引、227序列且零差异，重复安装保持对象身份；历史生产263表快照保留。最终迁移10文件320项、业务4文件172项、静态5文件115项零失败/跳过，双类型通过；6个隔离集群均独立确认停机、无监听或受信PG进程残留。Workers/PostgreSQL技能指导事务锁序和结构安装/运行角色验证分离。退款实体拆单等其它候选注册、完整运行角色、Hyperdrive/provider/浏览器及协调发布仍开放，240／164／404不变，原9暂存项保留；无生产、图片/缓存重跑或暂存提交推送部署。详见[发票证据注册审计](workers-ts/docs/invoice-evidence-registration.md)；以下为此前阶段记录。

发票历史受控安装增量（2026-09-16，本地候选、未部署）：新增独立维护命令，支持首次安装、历史v1→分配v2增量及完整v2重复核验；PG16目录指纹、维护所有权、最小权限与继承角色检查均已接入真实执行路径。发票表优先NOWAIT锁和短事务保证安装失败原子回滚，不补造既有票据的“从未开票”历史。完整外部/内嵌建库、实际CLI及独立LOGIN下10.00→3.33＋6.67拆票通过；最终安装/权限2文件53项、业务5文件187项零失败/跳过，双类型通过。首轮类型错误已收窄查询能力类型，扩大测试唯一失败为夹具子单复用空唯一键，已补独立标识而未放宽约束。6个隔离集群均清理测试库/角色并独立确认停机，受信PG进程0。编号迁移/ORM整体注册、完整应用角色、既有未知历史、红冲及协调发布仍开放，240／164／404不变；无生产操作或暂存提交推送部署，原9暂存项保留。Workers/PostgreSQL技能指导维护入口隔离、锁序与权限边界。详见[发票受控安装审计](workers-ts/docs/invoice-evidence-installation.md)；以下为此前阶段记录。

履约/供应商拆单发票归属增量（2026-09-16，本地候选、未部署）：两项真实PG红测复现37.00发货申请与69.00混合供应商申请拆单后仍留原单。现两个实际入口在订单/商品、供应商账本、审计/重放/通知同事务分配未开票申请；首次归档原申请、子单按实际金额建申请，连续拆分保留余单申请ID及逐分尾差。原申请完整快照与分配关系追加留证，曾开票/清号/归档/删行或安装前不明历史不降级，兄弟已开票记录保持隔离；整单发货、单组分配与已提交重放不重复拆票。迟后回执改写、归档复活/金额漂移、重复子票及各类写入失败整笔回滚。43项新拆票测试含实际“发货→原子退款→余单再发货”串联，另4项新非所有者权限测试；最终PG16.15单轮16文件486项零失败/跳过，独立权限14项全过、双类型通过，7个集群独立确认停止且无临时库/角色或受信进程残留。中途夹具漏建供应商入账及无排序断言已修正，业务校验未放宽。按Workers/PostgreSQL技能落实短事务、有界留证与锁序；Kefu/Store隔离器已补结构依赖但未重跑完整托管场景。正常拆单现依赖候选历史/分配DDL，缺表拒绝而不降级，必须受控安装/升级及ACL核验后协调发布；真实票据核验/红冲、旧已拆根单补适配、全旧端/真实角色/Hyperdrive/provider/浏览器及发布门禁继续开放。240／164／404及原9暂存项不变，无生产、图片/缓存/浏览器重跑或暂存提交推送部署。详见[履约与供应商拆票审计](workers-ts/docs/split-invoice-allocation.md)；以下为此前阶段记录。

发票曾开票历史增量（2026-09-16，本地候选、未部署）：三项真实PG红测复现“已开票→拒绝”清空票号后，保留/软删/删除申请三种情况均错误准入新版退款。现候选同事务触发器记录实际创建、未核验旧记录及每个报告票号的首份完整快照；16KiB有界、身份不可挪用，改状态/换号/删除业务行不抹历史，证据改删/截断和申请整表截断拒绝。应用非所有者角色无证据写入或采集函数执行权，正常业务写仍可原子留证；固定search_path和源schema限定通过临时表及独立新连接临时类型遮蔽测试。退款准入与晚期实体完成重验源/根历史，安装前无创建证据的归档申请（包括已新建替代申请）亦由同一SQL快照拒绝；旧行删除同时落未核验证据，不在两次语句间产生空隙。采集失败/零行、晚期出票再清号及回执失败连同财务/订单/证据整笔回滚；原Out手工状态合同保留，但不再等同“从未开票”。新增47项测试；最终本机PG16.15单轮14文件402项零失败/跳过，另独立非所有者权限10项全过，双类型通过；7个隔离集群独立停机，临时库/角色与受信进程无残留。Workers/PostgreSQL技能指导事务证据、最小权限及有界快照。候选DDL未注册总模型/运行迁移或启用公共v2；真实票据核验/红冲、安装前已抹历史裁决、通用履约/供应商拆票与其它旧端合同、受控DDL/真实角色/Hyperdrive/provider/浏览器及发布仍开放。240／164／404及原9暂存项不变，无生产/图片/缓存/浏览器重跑或暂存提交推送部署。详见[发票开票历史审计](workers-ts/docs/invoice-issuance-evidence.md)；以下为此前阶段记录。

Out发票与退款在途互斥增量（2026-09-16，本地候选、未部署）：四项真实PG红测复现退款在途汇总仍为0、票面金额不符时Out元数据/开票状态仍可写。现与顾客申请共用当前代次净额校验，根单行锁先于子单/商品/发票锁，锁后重验归属；五类实际退款在途阻止新写，原请求回执仍可只读重放，原申请归档后返回原ID而不覆写后续记录。实际连续退款剩余票额29.40不重复扣历史；渠道REQUESTING等待及SUCCESS本地恢复期间两类Out写均拒绝，恢复后42.20申请可处理且不重复调用渠道。独立连接验证开票先到拒绝退款、退款先到拒绝开票及根/票锁后变更，迟后回执失败整笔回滚。新增37项SQL用例及既有恢复场景扩展；两次结构断言分别因旧注释、共用模块提取而失败，已检查实际事务绑定/调用关系，业务断言未放宽。最终12文件351项零失败/跳过、双类型通过，4个PG16.15隔离集群独立停止且受信测试进程0。Workers/PostgreSQL技能指导短事务、有界SQL与统一锁序。既有“已开票→拒绝”记录转换不代表真实红冲，曾经开票的不可变历史、Admin/Kefu及通用履约/供应商拆票、DDL/ACL和真实角色/Hyperdrive/provider/浏览器验收仍开放；旧完整Out隔离场景仅补依赖表、本轮未执行，不冒充线上鉴权验收。240／164／404及原9暂存项不变；用户新图可读确认保留，无生产/图片/缓存/浏览器重跑或暂存提交推送部署。详见[Out发票退款联动审计](workers-ts/docs/out-invoice-lifecycle.md)；以下为此前阶段记录。

退款实体拆单的未开票申请分配增量（2026-09-16，本地候选、未部署）：三项真实PG红测复现含待开/驳回申请订单无法原子退款，现已将申请分配接入实际v2财务/订单/供应商同一事务。全退保留申请ID及金额；首次部分退归档原申请并按子单金额创建两份申请；连续部分退保留剩余申请ID并逐次减额，不复制已出票号。不可变退款回执新增16KiB有界原申请快照与分配关系；实际55.00守恒为12.80＋12.80＋29.40，重放不新增记录。根单→源单/商品→发票→用户锁序，等待后重验状态；错属/重复/已开票/旧结构/少退现金在申请及渠道准入前拒绝，原主单发票未分配不能独立下放一个子单。晚期发票插入、更新、更新零行、快照漂移及回执失败全事务回滚，渠道SUCCESS本地恢复不再请求/查询，旧已单独结算流程不能事后补拆发票。首次扩大回归4项仅异常文案断言错误已按具体拒绝原因修正，未放宽状态/资金断言；最终10文件293项零失败/跳过、双类型通过，4个隔离PG16.15集群独立停机、受信测试进程0。Workers/PostgreSQL技能指导有界SQL、短事务和锁序。整体发票迁移不勾选：已开票/红冲、履约/供应商拆票、Out/Admin/Kefu读写及异步退款在途开票互斥、DDL/ACL和公共启用仍开放。240／164／404及原9暂存项不变，无生产/图片/缓存/浏览器重跑或暂存提交推送部署。详见[未开票退款分配审计](workers-ts/docs/refund-invoice-allocation.md)；以下为此前阶段记录。

发票申请与退款/拆单锁协议增量（2026-09-16，本地候选、未部署）：真实PG三项红测复现歧义数字订单标识、实际退款在途但汇总标记为0、拆单审计根三种情况仍错误创建发票。现实际v1/v2共用服务按根单→源单→商品→抬头/发票锁序，订单号与数字ID使用同一结算锁；等待后重验归属与状态，五类真实在途退款阻止申请，取消/拒绝释放资格。新申请只按当前代次有凭据的未退款金额开票，实际55元订单退12.80后为42.20，连续实体退款剩余为29.40且不重复扣旧代次；抬头共享锁防删除/编辑竞态，重复/错属和缺代次证据拒绝。已有主单发票保持可查且不改号/金额，未作归属分摊前阻止子单再次申请；仅已软删除主单记录延续既有再申请合同，新子票仍待开。独立连接与PG阻塞PID验证别名竞争、根先于子锁、锁后金额/归属/抬头变化及实际开票/退款申请双向竞争，晚期失败整笔回滚；真实控制器合成身份HTTP覆盖v1/v2重复与未登录拒绝，不冒充生产鉴权。最终单轮10文件270项零失败/跳过、双类型通过，4个隔离集群独立停机及受信PG进程0。PHP拆票复制/根票删除合同已核对，但不可变票据继承、已开票归属及Out/Admin/Kefu写读适配未完成，新版退款发票门禁不解除；240／164／404和原9暂存项不变。无生产/图片/缓存/浏览器重跑或暂存提交推送部署。详见[发票申请并发审计](workers-ts/docs/order-invoice-admission.md)；以下为此前阶段记录。

退款结算与实体拆单原子集成增量（2026-09-16，本地候选、未启用/部署）：先以两项红测复现“退款财务已完成但实体拆单不随事务回滚”，现服务端显式候选入口将执行版本持久化到严格数量预占凭据；实际余额/渠道返回/回调/恢复共用根订单优先锁序与同事务财务、实体子单、供应商账本及操作回执。已完成申请必须有原指纹回执，不允许事后补拆；原订单转审计根后同申请可重放，改数量及新申请拒绝。渠道SUCCESS但本地失败由定时任务仅恢复本地提交，不再请求或查询退款；UNKNOWN仍按原支付根身份查询。拒绝/取消释放数量预占，新添发票不会强迫必须付款才能拒绝。原拼团测试绕过真实申请插入无凭据记录导致12项失败，已改用真实自动申请流程且全部通过，未放宽财务证据检查。新版拼团实体归属仍显式阻断，默认/HTTP入口保持v1；DDL/ACL/读删报表、发票促销/赠品余单/商家运费/拼团适配及协调发布继续开放。最终真实PG16.15单轮21文件473项、零失败/跳过，含26项新增原子流程测试，两套类型检查通过；4个隔离集群均独立确认停止，受信测试PG进程0。用户新图可读确认保留，无图片/缓存/浏览器重跑、生产写或暂存提交推送部署，原9暂存项及240／164／404不变。详见[退款原子集成审计](workers-ts/docs/refund-atomic-materialization.md)；以下为此前阶段记录。

退款剩余单再次分批发货增量（2026-09-16，本地候选、未部署）：真实PG复现再拆后新发货/保留剩余两分支均无法退款，现履约事务锁后验证原代次，随子单/供应商账本/通知同事务追加各分支不可变关联；保留单延续精确退款指纹与返积分流水ID，新子单独立收货和收入归属，后续退款记录保留分支基线。新增25项覆盖连续分支与七次退款、100积分/107元现金/53元供应商权益守恒、佣金与混合供应商隔离、损坏证据、迟后回滚、重放、缺表及两项真实锁等待。最终17文件395项零失败/跳过、双类型通过，四临时PG16.15集群独立确认关闭，无PID/口令/监听/可信测试进程残留。Workers/PostgreSQL技能指导有界证据、锁序和原子回滚；新候选表/列仅夹具显式安装，主退款原子接入、读写适配、受控DDL/ACL及生产验收仍开放。沿用用户新图可读确认，无生产/图片/缓存写、浏览器重跑或暂存提交推送部署；九原暂存及240／164／404不变。详见[退款后再分批发货审计](workers-ts/docs/refund-fulfillment-branches.md)；以下保留此前阶段记录。

供应商退款跨代账本增量（2026-09-15，本地候选、未部署）：真实SQL复现待收货/已收货订单第一次实体拆单后第二次退款因原结算账本与子单金额不一致而失败。现候选拆单在同一订单/商品/供应商锁事务中校验原收入、退款支出和现金交易，保留原流水金额/编号/时间，仅将旧表示标为-1，并派生已退子单净零账本和剩余单独立收入；原支付/退款交易及业务申请不改，追加证据保存原账本快照和派生ID。另两项红测复现未来子单编号被异常有效流水占用仍拆成功，已增加归属检查；精确派生键冲突和迟后失败均整笔回滚。新增27项覆盖三次连续退款、收货提现穿插、混合供应商隔离、少退现金、零结算、证据异常、锁后重验与重放；最终17文件370项零失败/跳过、双类型及空白检查通过，初次类型收窄问题已修复。五个临时PG16.15集群独立确认停止；Workers/PostgreSQL技能指导锁序、精确守恒和回滚。实体拆单尚未接入退款主流程同事务，拆后再分批发货的代次/权益传播、全读写适配、受控DDL和生产验收仍开放；本轮未改生产/图片/缓存、重跑浏览器或暂存提交推送部署，九原暂存及240／164／404不变。详见[供应商退款跨代账本审计](workers-ts/docs/supplier-refund-generations.md)；以下保留此前阶段记录。

供应商退款/收货/提现并发增量（2026-09-15，本地未部署）：独立PG连接红测复现退款、收货结算及后台拒绝提现未与提现准入共用锁的4种缺失等待关系，现统一沿用原供应商事务锁，后台审核在同事务中锁后复核状态。另复现会话默认Repeatable Read时两笔31元提现均读取旧余额并成功，已仅将提现准入事务显式固定为Read Committed，不改数据库全局配置。退款先完成则按最新25.50元拒绝31元提现；提现先受理仍完成顾客13元退款，保留原提现占用及供应商欠额，不阻止顾客退款或撤销旧申请。新增13项含双向顺序、不同供应商、失败回滚重试、相反审核及已转账占用；最终16文件343项零失败/跳过、双类型和空白检查通过，四个临时PG16.15集群均独立确认停止且无PID/口令/监听/可信测试进程残留。Workers/PostgreSQL技能指导锁序、事务边界及锁后新快照。本轮未改生产/图片/缓存，未重跑浏览器、暂存、提交、推送或部署；九原暂存及240／164／404不变。跨代供应商账本归属、实体退款原子接入和真实生产验收仍开放。详见[供应商财务并发审计](workers-ts/docs/supplier-finance-concurrency.md)；以下保留此前阶段记录。

供应商退款行级结算增量（2026-09-15，本地未部署）：对照PHP按退回商品结算价＋分摊运费的合同，真实SQL复现顾客退款13元时供应商应扣5.50却扣7.20，待收货/已收货均受影响。现共用锁后商品计划按完成数量累计供应商结算额，保留尾差、原支付/退款标识、待结算净额和整件退完但现金少退的净零结算；不按今日SKU价格或顾客现金比例重算。另四项红测确认历史买家/渠道错误和原流水键冲突被静默接受，现代路径已校验归属并使任一冲突整笔回滚，终态重放仍无写。新增22项含真实流水锁等待、现金少退、零结算额及一分钱运费尾差；最终15文件326项零失败/跳过、双类型及空白检查通过。运费夹具归属失败已正确修正，未放宽生产校验；五个PG16.15测试集群独立确认停止且无PID/口令/监听/可信测试进程残留。Workers/PostgreSQL技能指导有界整数计算、锁后重验及事务回滚。跨代供应商收入归属、并发提现隔离及实体主流程/受控DDL/生产验收仍开放，本轮仅修正其结算基数和账本一致性前提；未改生产/图片/缓存、重跑浏览器或暂存提交推送部署，原九暂存和240／164／404不变。详见[供应商退款行级审计](workers-ts/docs/supplier-refund-lines.md)；以下保留此前阶段记录。

退款跨代赠分/佣金归属增量（2026-09-15，本地候选、未部署）：真实SQL复现原单先收货后第二次退款多留100积分，以及拆后收货第三次退款财务校验失败。现冻结首次已收货源单的收入归属与财务基数，后续冲回原入账流水，抵扣积分仍按当前代次返还；原收入身份不改、配置变化不重算，兄弟发货单收入隔离。原佣金接收人并入统一UID锁序，独立连接验证低UID旧接收人先于买家加锁，迟后失败整笔回滚。新增17项SQL场景与17项纯计算，扩大11文件286项零失败/跳过，最终双类型及空白检查通过；三临时PG集群独立确认停止，无PID/口令/监听/可信测试进程残留。Workers/PostgreSQL技能指导有界证据和一致锁序。实体主流程、供应商收入、后续履约/读写、发票促销、赠品/商家运费及受控DDL/生产验收仍开放；无生产、图片、缓存、浏览器重跑或暂存提交推送部署，九原暂存与240／164／404不变。详见[跨代收入归属审计](workers-ts/docs/refund-earned-income.md)；以下保留此前阶段记录。

退款剩余单历史/积分代次增量（2026-09-15，本地候选、未部署）：真实SQL复现续拆保留订单ID后第三次申请报“历史退款商品快照无法定位”（35过/1失败）。现以显式子单快照版本/标记、不可变前代关联和精确返积分流水ID区分财务轮次，申请报价、累计退款、共用行级补偿及渠道准入前校验已接入读取；仅排除原指纹匹配的实体退款，不改原申请/支付/流水，Admin报价仍保留完整原历史。三次实际退款12.80＋12.80＋29.40、抵扣积分20＋20＋60通过；同轮多退、旧业务行删除、九类损坏、四类DDL边界、迟后失败回滚及模拟UNKNOWN原根支付总额恢复均覆盖。新增24项使实体套件59项，最终10文件252项零失败/跳过、双类型及空白检查通过；两项新增独立PG阻塞证明覆盖申请/完成锁后代次变化与用户锁未提前取得。元数据改为独立小列和剩余单索引，不反复读取完整源快照；无标记订单不查候选表，带标记缺表/缺证据拒绝且不运行时修复。四个PG16.15集群独立确认停止，无PID/临时口令/监听/可信测试进程残留。Workers/PostgreSQL技能指导有界索引读取、不可变边界及锁后验证。实体写入仍未由主流程调用；祖先赠分/佣金与供应商收入归属、后续收货/履约/删除读写、发票促销、赠品/商家运费、同事务接线及受控DDL/角色/发布仍开放。无生产/图片/缓存写、浏览器重跑、暂存提交推送部署；九个原暂存及240／164／404不变。详见[退款历史与积分代次审计](workers-ts/docs/refund-order-generations.md)；以下为此前阶段记录。

退款实体订单写入增量（2026-09-15，本地未接线候选、未部署）：新增实际首拆/续拆/整单订单与商品行写入组件，首拆保留支付主单并创建退款/剩余子单，续拆保留剩余单及存续商品行身份；持久记录原退款/支付关联、拆前快照和数量映射，不改原退款或支付流水。按已验证行级财务分摊，保留整数权益尾差、收货状态及核销分区；退款→支付主单→原子单→商品锁序下重验归属，重复调用只读原结果，晚期失败整笔回滚。候选表及防改删/截断DDL仅显式安装于隔离夹具，未注册迁移/模型总入口或接入退款主流程；夹具先完成资金退款再独立拆单，不冒充资金/实体同事务集成。新增35项含三项独立连接锁证明，最终七文件201项零失败/跳过及双类型通过；未使用类型别名导致的初次类型失败已修正留痕。三个PG16.15集群独立确认停止，无PID/临时口令/端口监听/可信测试进程残留。Workers/PostgreSQL技能指导有界事务写入、持久关联和锁后重验。历史补偿/账本归属与相关读写、发票促销、仅剩赠品、独立商家运费及受控DDL/角色/发布仍开放，后两种拆单当前明确拒绝。用户新图可读确认保留，无生产/图片/缓存写、浏览器重跑或暂存提交推送部署；九个原暂存及240／164／404不变。详见[退款实体写入审计](workers-ts/docs/refund-order-materialization.md)；以下为此前阶段记录。

退款行级积分/佣金补偿增量（2026-09-15，本地候选、未部署）：真实下单/收货/退款复现单件应返20积分却返23、应扣100赠送积分却扣46、应退0.30佣金却退0.83；现按已完成商品行预占累计重放并保留尾差，分别计算商品赠分、实付赠分与各来源实入佣金。完成退款与收货后补偿共用用户锁前校验的行级计划；管理员现金少退不转移商品权益，UNKNOWN恢复保持原金额/支付标识，原回执/收入关联及余额不足封顶不变。新增27项覆盖连续顺序、尾差、配置变化、在途申请、异常历史、晚期回滚和两项独立连接锁证明；最终11文件228项零失败/跳过、双类型通过。六个PG16.15集群独立确认停止，无PID/口令文件/端口监听及可信测试进程残留；初始金额红测、冻结字段测试误断言和旧静态签名断言修正均留痕。Workers/PostgreSQL技能指导有界计算、用户锁前证据校验与同事务回滚。退款实体子单、账本归属/不可变关联及发票促销仍开放，不回填旧补偿；用户新图可读已确认，无生产/图片/缓存写、浏览器重跑、暂存提交推送部署。原九暂存及240／164／404不变。详见[行级退款补偿验收](workers-ts/docs/refund-line-compensation.md)；以下为此前阶段记录。

供应商拆单账本连续性增量（2026-09-15，本地候选、未部署）：两项真实SQL红测确认首次/供应商分配后拆单仍把16.50收入挂在原39.00订单，现于发货事务内将待结算权益按子单商品结算价和运费拆为5.50/11.00，原流水金额与支付交易完整保留、派生流水记录来源；退款仅读取当前有效收入，忽略已替代前身。另两项红测复现待收货退款误扣其他已收货子单余额，现待结算退款仅抵扣同单待结算收入，收货/全退时收入与退款同时结清。新增24项SQL覆盖重复拆单、真实收货/余额退款、零现金分配、八类异常、晚期回滚、模拟提现申请及三项独立连接并发；最终八文件138项零失败/跳过、Worker双类型通过。五个PG16.15集群独立确认停止且无PID/口令文件/监听/进程残留。Workers/PostgreSQL技能指导有界计算、保留锁序及同事务账本回滚。退款实体子单、不可变申请/provider关联、积分佣金和发票促销仍开放；不回填历史异常账本。沿用用户新图可读确认，无生产/图片/缓存写、浏览器重跑、暂存提交推送部署；九个原暂存文件及240／164／404不变。详见[供应商拆单账本与验收](workers-ts/docs/supplier-split-ledger.md)；以下为此前阶段记录。

退款行级报价增量（2026-09-15，本地候选、未部署）：真实下单→退款报价SQL复现含运费商品13.00错报11.20、免运费商品30.00错报33.60；报价/申请现复用校验式行级证据与PHP四位分摊，按已完成数量预占依序保留尾分，整单同样校验。管理员少退不把差额转给其他商品，系统必须全额恢复保留明确例外；原回执和已受理UNKNOWN金额/单号不重算。订单锁内补有序商品行锁，缺失/混合/错误现代财务与历史预占拒绝静默降级。新增24项真实SQL覆盖连续退款、余额/库存/积分、实际Admin创建回执、已发货子单、回滚与独立连接；最终八文件259项零失败/跳过，Worker双类型通过。初轮两项原错价及69项重叠回归均留痕，三个PG16.15临时集群独立确认停止、无PID/临时口令/监听/进程残留。Workers/PostgreSQL技能指导有界紧凑历史计算与订单→商品锁序。退款实体子单、账本/不可变关联及真实发布门禁仍开放；旧快照兼容、最多200条完成历史及普通零现金选择等限制明确记录，不据此关闭整体项。无生产/图片/缓存写、浏览器重跑或暂存提交推送部署；原九暂存及240／164／404不变。详见[退款行级报价与验收](workers-ts/docs/refund-line-finance.md)；以下为此前阶段记录。

发货与供应商分单行级金额增量（2026-09-15，本地候选、未部署）：真实SQL复现并修正发货子单运费/实付按商品价误分、复制整单成本及整数积分变小数；新增校验式行级汇总，覆盖两类分单的运费、成本、折扣、五类佣金、改价余分、赠送积分余数与核销分区。版本/金额/数量不一致拒绝写入；赠券等整单字段按子单角色保留，不按金额真假判定。供应商分单后再发货又复现平台审计主单导致所属子单不可操作，现仅允许已核验子单解析已完成分配主单，并保留跨主体/主单直接访问拒绝及锁后归属复核。最终本机非超级用户PG16.15九文件190项零失败/跳过（新增47项真实SQL）、Worker双类型通过；初轮供应商夹具配送归属7失败和随后真实主子单归属1失败均有记录，未放宽断言。六个临时集群独立确认停止，无PID/临时口令/监听/进程残留。Workers/PostgreSQL技能指导保留事务与锁顺序。仍未实现退款实体拆单和账本/不可变回执/发票促销关联，旧快照兼容及freightPrice分摊限制明确保留，不关闭整体迁移项。用户新图可读确认沿用既有记录，无生产/图片/缓存写或浏览器重测、暂存提交推送部署；原九暂存及240／164／404不变。详见[行级分单与验收](workers-ts/docs/order-split-finance.md)；以下为此前阶段记录。

订单行级财务快照增量（2026-09-15，本地候选、未部署）：真实下单事务保存十一类行级金额/积分、原始运费、成本与活动积分、单位核销次数；运费和佣金明细来自实际计费过程，修正低价末行优惠溢出及运费分币丢失/超分配，订单总额不变。次卡核销次数纳入报价回执与库存更新守卫。新增21项纯计算、13项真实SQL验收通过；扩展12文件644项及另4文件68项全过、零跳过（两批重叠，不相加），Worker双类型通过。前两轮次卡夹具配送/门店启用条件与三处测试空值类型错误已修正，未放宽业务或并发断言。四个自建PG16.15集群独立确认停止，测试库/角色、PID/临时口令、监听/进程残留均为零；保留停止的诊断目录。Workers/PostgreSQL技能指导保留现有事务与锁顺序。仍需修正既有发货/退款分区对新增原运费和整数积分的处理，完成子单汇总、不可变回执和账本关联；不据此关闭实体退款拆单。无生产/图片/缓存写、浏览器重测或暂存提交推送部署，原九暂存及240／164／404不变。详见[下单行级证据与验收](workers-ts/docs/checkout-line-finance.md)；以下为此前阶段记录。

退款拆单计算核心（2026-09-15，本地未接入、未部署）：新增十一类行级金额/积分四位除法后截断分摊、余数保留、赠品/整单分支及核销次数分区；缺失快照字段显式报告，不能把计算占位零当财务证据。新纯函数测试86项通过；本机PHP8.4.25直接执行旧源码方法，12组/93对字段及8组改价案例核对通过，并实际复现首子单金额为零时PHP余款少一分的两种边界，新计算按原金额减已分配额保留余数。首轮71/72的测试表参数展开错误已修正，未放宽断言。Workers技能指导有界、无I/O的独立计算；未改数据库事务或运行SQL/浏览器验收。仍需补齐现代下单行级财务证据、子单金额汇总/实体落库、预占迁移、不可变回执及资金补偿关联；本增量不代表退款实体拆单或迁移完成。不改线上图片/缓存，无暂存提交推送部署，原九暂存及240／164／404不变。详见[计算核心与PHP对照](workers-ts/docs/refund-physical-split.md#calculation-increment-2026-09-15-local-and-unwired)；以下为此前阶段记录。

拆单后退款标识衔接（2026-09-15，本地候选、未部署）：推进实体退款拆单时，真实SQL复现发货子单与剩余子单均因十六进制cart_id被退款报价拒绝。新拆出商品改用其实际快照主键序列分配正整数cart_id，同步订单商品清单及JSON id，保留old_cart_id；后续拆分保留剩余行主键/标识，整单发货和已提交重放不耗新ID，失败只留下正常序列间隙，不重置/猜测序列或批量改旧数据。新增20项实际SQL验证报价→申请→撤销→余额退款、重复/多行拆分、回滚/序列归属和并发、微信/支付宝UNKNOWN原支付信息恢复及模拟回调；最终十文件160项零失败/跳过、Worker双类型通过。初轮2项原缺陷、测试漏refundExplain及扩展154项中CRLF静态路由误报均已记录并修正，路由未改；旧客服隔离夹具补OWNED BY，但完整客服场景未重跑。五个自建PG16.15集群已独立确认停止，无PID/临时密码/可信运行时进程/测试监听残留。Workers/PostgreSQL技能指导事务内序列分配及现有锁顺序。仍未实现退款实体拆单：数量再分配、PHP分摊/赠品/核销、发票/促销、不可变回执关联和已退款数量后续履约均有明确开放合同；不以子单可退款代替整体迁移完成。无生产/图片/缓存操作、浏览器重跑或暂存提交推送部署；九暂存与240／164／404不变。详见[实体拆单合同与验证](workers-ts/docs/refund-physical-split.md)；以下为此前阶段记录。

退款申请数量预占增量（2026-09-15，本地候选、未部署）：对照PHP申请预占、拒绝/撤销释放，新增服务端版本化数量快照；申请、数量预占和审计/创建回执同事务，报价不写入；取消/拒绝仅释放当前申请持有量，已预占结算不再重复累加，UNKNOWN/结算失败保留原申请数量供恢复。取消路径补退款锁内状态与订单锁后归属复核；旧无标记申请保留结算计数、不凭空扣减，错误标记/计数漂移拒绝执行。最终本机非超级用户PG16.15十一文件232项零失败/跳过（新增32项，含4组独立连接锁等待）及Worker双类型通过。初轮143项的错误校验顺序和扩展229项的拼团旧计数期望失败均已记录、修正并重跑；拼团用例仍精确验证余额/库存/团状态回滚、重复失败不变及同申请只恢复一次。三个自建临时集群已独立确认停止，无PID/临时密码/运行进程/端口监听残留。Workers/PostgreSQL技能指导原子短事务、统一锁顺序和外部渠道I/O分离。本轮未重跑浏览器，未改生产数据/图片/缓存或暂存提交推送部署；原九暂存及240／164／404不变。PHP实体拆单、全退/子单数量重分配、金额边界与真实角色/Hyperdrive/provider仍开放；所有写入方、执行器与回调必须协调升级，旧执行器不能处理新预占记录。详见[数量预占审计](workers-ts/docs/refund-quantity-reservation.md)；以下为此前阶段记录。

Admin 主动退款后台与浏览器整链增量（2026-09-15，本地候选、未部署）：退款页补订单报价、商品数量选择、金额/原因确认、仅创建/创建并执行及原请求恢复入口；订单详情提供数字ID入口和商品项ID。创建摘要与资金摘要分别校验，原管理员/键/完整内容先落本地再发送，空查询与创建成功均不冒充资金完成；Web Locks、CAS、终态留痕及会话失效防止重复/跨标签/迟到回包误提交。最终六文件150项零失败/跳过（创建客户端31、实际SFC/API60、旧操作客户端25、旧意图15、API合同5、runner14）及一条真实浏览器→HTTP→非超级用户PG16.15多检查点验收通过，Admin类型/构建和Worker双类型通过。首轮浏览器暴露未收货receivedAt=null被误拒，已按服务端合同修复并补3例，不放宽断言；复跑验证丢包后仅查两类回执恢复、创建放弃屏障拦截迟到包、UNKNOWN原键恢复及仅创建不执行资金。桌面1440×1000/手机390×844截图已查看，4条故障注入控制台错误符合预期，额外错误/警告/外部请求0。两个自建PG集群、驱动及监听端口独立确认已停止，截图/结果置于仓库外临时目录。无生产/图片/缓存写或暂存提交推送部署，原九暂存及240／164／404不变；实体拆单、PHP数量预占/金额边界、真实生产角色/Hyperdrive/provider及协调发布仍开放。详见[主动退款后台与整链验收](workers-ts/docs/admin-refund-creation.md#admin-confirmation-and-joined-browser-acceptance-latest-2026-09-15)；以下为此前阶段记录。

Admin 主动退款版本化HTTP增量（2026-09-15，本地候选、未部署）：在两个Admin别名下补齐create/execute/receipt/abandon四类POST；均要求当前Admin、refund.manage、原UUID键与精确身份范围，8KiB UTF-8写入体/512字节查询体、错误no-store。创建仅SQL，执行先提交创建回执再独立处理资金；查询为空不是迟到请求屏障，放弃不取消已有申请或支付。真实应用Request/Response＋PG16.15最终五文件131项零失败/跳过（新增HTTP41项），覆盖跨别名原键恢复、单次余额/库存效果、两供应商网关UNKNOWN与租约、创建/资金两类失败提交边界、读体后的权限撤销及独立后端创建/放弃竞争。初次完整类型检查发现新测试JSON未知类型与异步返回未校验，补真实对象/信封校验和async发送器后双类型通过，未用any/双重断言或放宽测试；最终整批已重跑。三集群均独立确认停止、无PID/临时口令及可信测试进程残留。测试不是浏览器、真实HTTP监听器或线上Hyperdrive/provider验收；后台持久化确认/原意图恢复、实体拆单与生产角色/协调发布仍开放。原九暂存及240／164／404不变，无真实退款、生产/图片/缓存写或提交推送部署。详见[HTTP候选证据](workers-ts/audit/admin-refund-creation-http-20260915.json)及[主动退款创建协议](workers-ts/docs/admin-refund-creation.md)；以下为此前阶段记录。

Admin 主动退款创建回执建表增量（2026-09-15，本地候选、未部署）：补齐外部0156、内嵌0162、ORM导出及已有库独立受保护安装器；仅根维护事务执行，结构/直接ACL漂移拒绝而不修复，忙锁NOWAIT拒绝，重复执行保留已创建/已放弃回执及表索引OID/存储文件。真实受限LOGIN可追加/读取与原键恢复，修改/删除/清空/改表/恢复维护角色均返回42501。最终三批九文件246项零跳过通过（新建表49项），双类型及diff检查通过；保留初轮静态解析未识别无引号表名、测试误用事务参数各1项失败与完整复跑记录，未放宽断言或权限。PG16.15九路径266表/3722列/587约束/1026索引/227序列完全一致，回执在独立重复安装前已完整注册；158外部文件、163内嵌步骤、1096新ORM语句。历史263表生产快照未改，四个自建集群均独立确认停止、无PID/临时口令及测试运行时进程残留。创建写HTTP/后台原意图恢复、实体拆单、生产权限路径/Hyperdrive/provider及浏览器整链仍开放；用户已确认新图可读，不重复改图/清缓存。原九暂存及240／164／404不变，无生产操作或提交推送部署。详见[创建回执注册证据](workers-ts/audit/admin-refund-creation-registration-20260915.json)和[主动退款创建审计](workers-ts/docs/admin-refund-creation.md)；以下为此前阶段记录。

Admin 主动退款报价/期限增量（2026-09-15，本地候选、未部署）：真实应用新增两个只读POST报价入口 `/adminapi/refund/creation/quote`、`/api/admin/refund/creation/quote`，要求当前Admin＋refund.manage＋身份范围声明，8KiB严格UTF-8/JSON、不接收资金操作键或查询参数，鉴权错误同样no-store。报价与创建共用锁内数量、金额、商品可退性及期限核心；报价在任何业务写入/序列分配之前返回，缺创建回执表仍可报价。新创建必须携带报价依据指纹，同金额商品、收货事实或有效期限变化均使旧报价失效；原已提交创建回执的恢复不受今日配置错误抹除。期限在订单锁取得后直接读SQL，遵循全局配置sort/id优先级及缺省/空值为0，不信任KV；已验证到期秒边界和独立连接锁等待期间的配置变更。首轮5项失败源于定价测试辅助函数未把售后期限写SQL，改用显式隔离SQL配置后，八文件136项零跳过通过（报价16项）；未改断言/超时或生产配置。两个自建PG16.15集群均独立确认停止，无PID/口令或运行时进程残留。创建写HTTP、后台交互、受保护建表、实体拆单、正式角色/Hyperdrive/provider与浏览器整链仍开放；旧410不算功能迁移完成，原九暂存及240／164／404不变。无生产/图片/缓存写或提交推送部署。详见[主动退款创建审计](workers-ts/docs/admin-refund-creation.md)；以下为此前阶段记录。

Admin 主动退款创建增量（2026-09-15，本地候选、未部署）：新增严格版本化创建意图、原 Admin＋UUID 键的持久创建回执及创建/查询/放弃服务；退款申请、门店/供应商归属与创建回执同事务提交，再调用既有资金执行器，重放不造新键、不采用无回执业务单。支持显式商品件数与不高于权威报价的手工退款金额，修复少退差额转入剩余商品报价的问题，同时保留系统强制全额退款的剩余支付金额目标。初轮发现退款单号超过账单32字符字段，已改为保留完整身份信息、最长31字符的编码；未扩字段或截断标识。最终扩大回归十文件141项中140通过、1项在beforeEach建库阶段30秒超时；该文件原阈值、原断言独立复跑20项全通过，去重141项均有通过证据（创建30项），双类型检查通过，超时环境根因未确证。五个自建PG16.15集群均确认停止且无PID/临时口令残留，测试无生产/图片/缓存写，原九暂存文件及240／164／404不变。此候选尚无HTTP/报价接口、前端交互或受保护建表注册；PHP配置售后期限、运费/改价金额语义、活动折让、申请数量预占与实体履约拆单仍未完成，不关闭主动/拆单迁移项，不将旧入口410算成功能替代。无新浏览器验收、提交推送部署；详见[主动退款创建审计](workers-ts/docs/admin-refund-creation.md)。以下为此前阶段记录。

Admin 旧退款入口停用增量（2026-09-15，本地候选、未部署）：补齐七个旧 POST 的显式停用保护（四个审核别名＋三个移动管理入口），仍先真实 Admin 鉴权与 refund.manage，再返回 HTTP410/no-store；不读旧包、不造键、不写回执、不触发资金服务。旧控制器导出同样关闭，新版未使用的旧客户端导出已移除；列表/详情、线下收款及 Supplier/Kefu/Out/customer 权限域保持原行为。旧决策/证据测试迁至真实版本化控制器和正式回执DDL，独立后端锁断言保留。最终非超级用户PG16.15七文件111项＋前端去重89项＝十文件200项零跳过，Worker双类型/Admin构建通过；初始空凭据、状态及模拟渠道返回契约问题已修正并留痕，未放宽安全断言。静态路由审计将三个匹配PHP的移动退款路由计为unavailable，不冒充功能完成；主动/拆单退款暂无新版替代，旧在途请求核对与协调发布继续开放。已补完整旧客户端/回退政策，HTTP410及新键放弃均不能证明旧调用未执行。三集群均独立确认停止，无残留PID/临时口令或自有PG进程；无生产/图片/缓存写、新浏览器验收、暂存提交推送部署，原九暂存文件及240／164／404不变。详见workers-ts/docs/admin-refund-legacy-retirement.md；以下为此前阶段记录。

Admin 退款真实HTTP/SQL浏览器整链增量（2026-09-15，本地候选、未部署）：新增独立显式验收入口，不在普通单元中跳过浏览器；真实Vite页面/Axios→loopback HTTP→实际createApp鉴权/权限/控制器/退款服务→全新非超级用户PG16.15，回执表使用正式PK/四CHECK/索引DDL。余额退款真实提交后丢回包，余额+5.00、账单1条、商品/SKU库存各+1；删去测试业务行后凭回执恢复，不再execute。渠道UNKNOWN仅显式原键重试触发一次查询，申请/查询各1次；未受理操作的真实放弃栅栏阻断保留的迟到请求，随后新确认只审批一次。桌面1440×1000/手机390×844，共10次浏览器操作请求及1次保留包内部执行；2条故障注入控制台错误符合预期，额外错误/警告/外部请求0。最终整链1项＋四文件103项回归＝5文件104项零跳过，Worker双类型/脚本语法通过；初始类型和输出证据问题已修正。3个独立集群、浏览器、HTTP/Vite均独立确认停止，无生产数据/图片/缓存写及提交推送部署，原9暂存文件保留，240／164／404不变。此项不是workerd/Redis/Hyperdrive/真实支付或生产最小权限验收；旧客户端退役及协调发布仍开放。详见workers-ts/docs/admin-refund-operation-http.md的Joined Admin / HTTP / native SQL acceptance；以下为此前阶段记录。

Admin v3 原操作回执前端增量（2026-09-15，本地候选、未部署）：真实后台已接入执行/回执查询/显式放弃，先保存原管理员、退款身份、决定和 UUIDv4 键再发请求；重试保留原内容，回执严格绑定身份/键/摘要/动作，空查询不解除未知限制，渠道受理不冒充结算或取消。旧 v1/v2（含旧“已核对”标记）不升级、不清除、不自动重发；新增按 ID 恢复入口，业务详情消失仍可查回执，新决策仍需新详情。三套件89项零跳过（SFC/API49、v3协议25、旧协议15），Worker单元类型及Admin构建通过；浏览器在真实本地页面、合成接口下验证1440×1000及390×844，两条未知结果恢复链共8次操作请求，原键/内容不变，手机取消不增加请求，最终控制台与意外外部请求均为0。现有Playwright/本机Edge验收，不安装依赖；夹具、定位和截图时序的初始失败已修正，临时证据置于仓库外。非真实HTTP/SQL/provider浏览器整链，也非生产验收。旧客户端退役、真实角色/Hyperdrive/Redis/provider、Linux/CI与协调发布继续开放。无生产写、图片/缓存维护或暂存提交推送部署；原9个暂存文件及240／164／404不变。详见workers-ts/docs/admin-refund-operation-http.md的Admin v3 client follow-up；以下为此前阶段记录。

Admin 退款回执HTTP增量（2026-09-15，本地候选、未部署）：新增实际装配的执行/回执查询/显式放弃接口，adminapi与api/admin共用鉴权和refund.manage；强制协议版本、UUIDv4原键及预期管理员作用域，严格4KiB UTF-8与字段校验，认证失败也no-store。查询不调用渠道、不创建放弃栅栏；已受理操作不可被“放弃”覆盖，缺表不自动安装。31项新HTTP测试覆盖双地址重放、跨账号拒绝、十二种鉴权后变化、真实余额/账单/库存回滚、渠道在途重复HTTP与未知结果原键恢复；最终四文件78项在非超级用户PG16.15下全过。兼容六文件127项、补跑真实受限LOGIN权限18项及CLI边界11项通过，按各套件最终结果计12文件234项零跳过（非单轮全量）；初始跳过和类型错误修正留痕。后台版本化意图/旧v2无键请求处理、浏览器、Redis撤销、真实provider/Hyperdrive与发布仍开放，不据此清旧未知结果守卫。无生产写、暂存、提交推送部署，原9个暂存文件保留，240／164／404不变。详见workers-ts/docs/admin-refund-operation-http.md；以下为此前阶段记录。

Admin 退款回执受保护迁移增量（2026-09-15，本地候选、未部署）：补齐外部0155、内嵌0161、ORM导出及265表候选清单，历史263表生产快照不变。仅根维护事务执行，严格检查结构/直接ACL、拒绝漂移和隐式修复，真实受限LOGIN可追加/读取但不能修改、删除或恢复维护权限。各套件最终结果合计16文件393项通过、零跳过（并非单轮全量测试；失败及重跑均单独记录），Worker双类型通过。PG16.15九路径总审计全过：265表/3715字段/582约束/1024索引/227序列完全一致，各路径回执在重复安装前已完整注册，重复安装保持OID/存储文件。总计157外部文件、162内嵌步骤、1094新ORM语句；原序列测试暴露临时对象所有者依赖与search_path快照误差，已修正基线时点，未删除或放宽依赖断言。正式API、版本化Admin意图、生产角色/Hyperdrive/provider及协调发布仍开放；用户确认新图可读，不重复改图或清缓存，不冒充新浏览器验收。8个本机隔离集群均确认停止，测试数据库/角色及运行进程已清理。未暂存、提交、推送、部署或改生产，240／164／404不变。详见workers-ts/docs/sku-membership-price.md的Guarded Admin refund receipt migration candidate；以下为此前阶段记录。

Admin 退款服务端回执执行器增量（2026-09-15，本地候选、未启用）：新增24项真实执行器/SQL测试，并复验15项回执底座测试；审批、拒绝及余额结算的回执与业务写入同事务提交，渠道准入回执先提交再调用支付接口，查询不发起退款，显式放弃原操作持久阻止迟到请求。原操作键绑定管理员、退款身份、金额和决策快照；同键不同内容拒绝，已完成决策可在业务行删除后重放回执。红测发现外层事务会使准入未提交即调用渠道，已在执行入口拒绝非根数据库客户端。独立连接与 pg_blocking_pids 验证重复审批及放弃/执行竞争；余额、账单、未发货库存回退和失败回滚执行真实PG，微信/支付宝仅模拟外部适配器。最终22文件355项零跳过、Worker双类型检查通过；不能据此声称真实provider、HTTP/UI或生产验收完成。受保护建表升级/迁移注册、正式API、新版浏览器意图协议及协调发布仍未实现，旧v2 nonce未发往服务端，不得自动重发或当作受新回执保护。无生产写、暂存、提交推送部署；原9个暂存文件保留，240／164／404不变。详见 workers-ts/docs/sku-membership-price.md 的 Durable Admin refund operation service candidate 一节。

真实 PostgreSQL 并发验收增量（2026-09-15，本地、未部署）：补齐上一轮缺连接而跳过的5项测试，并新增3项授权锁保持到提交、撤权后新请求拒绝的反向并发测试；8项均在 PostgreSQL16.15 独立连接上通过，阻塞证据来自 pg_blocking_pids，不用等待时长代替锁证明。由 PostgreSQL 官方链接取得 EDB Windows 便携包，新增受限的本机运行脚本：随机 loopback 端口、SCRAM、独立非超级用户、全新随机集群、数据库身份核验、仅显式测试文件、错误后检查并停止，不注册服务或写用户环境。18文件251项退款/权限回归与另5文件90项拼团/夹具隔离回归在真实PG配置下通过，另4项启动边界测试通过，合计24文件345项零跳过；双类型检查通过。所有测试库清理完成，5个曾启动集群的 pg_ctl 均返回停止状态，遗留进程0、明文密码文件0；便携运行时与停止的诊断目录留在被忽略的 .cache。该证据关闭本轮 Admin 锁内决策的本机真实PG验证缺口，不代表 Linux/workerd/Hyperdrive/provider 或全部生产验收。无生产写、提交推送部署；原9个暂存文件保留，240／164／404不变。详见 workers-ts/docs/sku-membership-price.md 的 Native PostgreSQL decision verification 一节。

Admin 退款锁内决策增量（2026-09-15，本地候选、未部署）：3项红测复现入口鉴权后账号禁用、退款状态变化和缺失决策快照仍被接受，现均拒绝。新版请求冻结申请类型、原状态及收货确认；退款/订单锁内重新核对账号状态、密码版本、令牌有效期和当前角色/菜单权限，授权行使用短事务 FOR SHARE NOWAIT，余额结算、渠道请求准入及 NOT_FOUND 重领前重复校验；已准入渠道的成功回执仍可完成后续处理，不因操作员后来被撤权而遗留。新增29项真实控制器/隔离SQL与路由测试，最终17文件243项通过；5项专用PostgreSQL独立连接测试因未配置 TEST_FINANCE_POSTGRES_URL 跳过，不计通过。Worker双类型和Admin构建通过；浏览器桌面/390px验证正常审批、状态变化拒绝且只读不重发、禁用后清空详情退回登录；夹具10次读取/3次审批请求仅1次获准，无资金退款。会话退出的Redis撤销原子性、服务端幂等回执、真实PG/Hyperdrive/provider、Linux/CI及协调发布仍开放。无生产写、暂存或提交推送部署；原9个暂存文件保留，240／164／404不变。详见 workers-ts/docs/sku-membership-price.md 的 Admin locked decision admission 一节。

Admin 待核对恢复增量（2026-09-15，本地候选、未部署）：将新版待核对记录扩展为按管理员/退款单隔离的原操作、已审阅身份与金额、原因、验收确认和 nonce；先可靠保存再提交。同源 Web Locks 覆盖保存、提交与回读，读写比较原记录，保留已核对记录以阻断旧标签页覆盖；跨标签变更使旧详情/确认失效。新增只读核对入口，仅服务端身份、金额及操作目标一致才结束本机待核对，不重发；旧版 ID-only 记录、渠道处理中及不一致仍限制提交。新增14项实际 SFC 与15项原操作协议测试，最终12文件194项通过，Worker双类型及Admin构建通过；插件双标签/1280×720/390×844验证一次审批→丢回包→刷新保留→只读恢复、旧页失效，隔离夹具12次读取/1次非资金审批，无退款。该机制不是服务端幂等回执，也不覆盖跨设备/跨域/旧客户端或清空浏览器数据；真实provider/Hyperdrive/角色并发、Linux/CI和协调发布仍开放。未重改生产新图，无生产写、暂存或提交推送部署；240／164／404不变。详见 workers-ts/docs/sku-membership-price.md 的 Admin pending-intent recovery 一节；下方为历史阶段记录。

Admin 售后详情与确认增量（2026-09-15，本地候选、未部署）：补齐有界游标列表、退货快递/运单/电话/备注/凭证和收件信息；核对退款单与原订单归属，校验上传者后签名。移除读取失败回退旧列表行，隔离换单/换号/离页/旧确认；退款专用请求捕获原会话，处理中回执不再误报完成。决策必需携带已审阅的单号、用户、门店/供应商与金额，执行核心在锁内复核，旧 ID-only 调用被拒绝，须协调发布 API 与 Admin。退货审批与资金退款分离，快递 type2/state5 与到店 type3/state4/5 均需人工验收确认；未知结果在本机保留待核对 ID，整页刷新不自动重发。新增20项真实鉴权/SQL/代理与25项实际 SFC/Axios/路由测试，最终11文件165项通过，类型与构建通过。浏览器桌面/390px验证分页、解码、只读角色和提交后丢回执保护，首个夹具仅1次非资金审批；不等于真实provider/R2/Hyperdrive验收。跨标签原子协调、完整待核对恢复、真实角色并发、Linux/CI和发布仍开放；原9个暂存文件未改动，无生产写或提交推送部署，240／164／404不变。详见 workers-ts/docs/sku-membership-price.md 的 Admin return evidence 一节。

供应商退货凭证读取增量（2026-09-15，本地候选、未部署）：补齐 Supplier 售后详情的申请说明、退货快递/运单/电话/备注和最多三张凭证；售后单与原订单须同时属于当前供应商且 UID 一致，附件上传者归属核对后才签发短时链接。数据库只读可重复读快照、8 KiB 凭证/64 KiB cart JSON 限制、损坏/外账号凭证提示、同源只读图片转发均已有回归。页面在换单、关抽屉、换账号/权限和离开路由时清空旧内容；退款确认绑定原单 ID/金额，回执未知时只读核对，不自动重发。新增 12 项真实鉴权/SQL/代理测试与 21 项实际 SFC/Axios/Pinia 测试，最终 11 文件 162 项、Worker 双类型和 Supplier 生产构建通过；浏览器插件 1280×800 / 390×844 验证图片解码、纯文本备注、失败重试、异常单不串图和只读权限，最终仅 1 次隔离备注写入、六类业务快照未变、未挂载资金退款接口。Admin 对应读取/页面、商品明细/申请凭证、收货审批完整验收、跨页面未知意图恢复、真实角色/R2/Hyperdrive 缓存与并发、Linux/workerd/CI、协调发布仍开放；普通事务不作为已验证 Hyperdrive 缓存绕过的依据。无生产写、提交推送部署，240／164／404 不变。详见 workers-ts/docs/sku-membership-price.md 的 Supplier return evidence 一节。

退货物流与凭证增量（2026-09-14，本地候选、未部署）：新图可读已由用户确认，未重复修改生产图片。补齐PC/UniApp客户侧快递公司选择、运单、可选电话、备注和最多三张凭证上传；修复PHP的refund_explain被忽略，新增32 KiB实际字节上限、字段/别名校验及附件账号归属核对后签名。提交体冻结、上传/撤销互斥、迟到回调隔离，未知结果只读核对全部字段和canonical凭证、不自动重发。11项PC、13项UniApp及17项Worker新增回归；最终PC239＋9图片、UniApp358、Worker五文件59、双Worker/前端类型、PC/H5/微信/App构建和3项产物检查通过。插件桌面/390×844完成上传解码、正常提交及SQL已提交但HTTP502→禁重发→回读一致，修正就地核对提示和完成文案后重载复验。最终独立夹具仅2次物流POST/2条状态/1张H5内存凭证，六类非退款快照未变；前一独立夹具含PC/H5两张凭证也通过。H5保留依赖层vue-router弃用警告；非原生/真实R2/Hyperdrive验证。商家收货审批/凭证展示、跨页面恢复、到店退货、申请侧凭证与部分件数、CI及发布仍开放；无生产写、暂存或提交推送部署，240／164／404、FE-003F、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

退款记录与详情增量（2026-09-14，本地候选、未部署）：用户已确认新图可读，本轮未重改生产图片。新增兼容的customer读取版本：账号隔离、删除过滤、显式公共字段、有界游标分页和单号搜索；详情解析本次申请商品/数量，无法确定的旧部分件数保留待核对。PC补退款历史/详情路由及入口，两端补筛选、失败原页重试、换号/离页清空、迟到响应隔离及撤销确认/未知结果只读核对。新增16项PC＋28项UniApp组件测试；最终PC228＋9图片、UniApp345、退款专项27（18项实际控制器/SQL＋9项投影）、各类型检查、PC/H5/微信/App构建及3项产物检查通过。浏览器桌面/390×844验证PC20→26、H5 10→20→26及失败重试；H5“撤销已提交但丢响应”禁重发→回读已撤销通过，修复误用PC图片组件并重载确认解码。临时夹具仅2次撤销/2条状态，六类非退款业务快照未变；无生产数据/缓存写、暂存、提交推送部署。物流提交、商品名搜索、跨页持久恢复、provider/原生/Hyperdrive/CI和发布仍开放，240／164／404、FE-003F、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

退款申请生命周期增量（2026-09-14，本地候选、未部署）：用户已再次确认新图能够读取，本轮未重复改线上图片。两端退款申请绑定当前账号/订单/读取代次，验证当前可退行、类型与说明，冻结提交体并拦截重复点击；UniApp回执类型修正为refundId，成功保留回执、不以申请成功代替到账，跳转失败只重试查看。未知结果不能由订单刷新解除，移动端hide中断也保留待核对状态；跨整页刷新/页面销毁的持久恢复与申请幂等仍未实现。21项PC、27项UniApp实际组件回归、4项真实控制器/隔离SQL测试通过；PC完整212＋9图片及类型/构建、UniApp317及类型/H5/微信/App/3项产物检查通过。浏览器PC 1280×720验证真实申请回执与冻结，H5 390×844验证已提交后丢响应→页内刷新仍禁重发→退款记录→真实详情；修正PC复选框禁用遗漏和H5商品卡片宽度冲突，重载复验。最终独立H5夹具仅1次申请POST/1笔10.00申请/1条状态记录，订单、余额、库存、购物车和账单快照未变；不涉及退款审批/到账。旧退款详情页属性继承警告、PC退款历史入口、部分件数/凭证上传、原生/Hyperdrive/CI和发布仍开放。无生产数据/缓存写、提交推送部署；240／164／404、FE-003F、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

支付结果页权威核验（2026-09-14，本地候选、未部署）：对照PHP支付状态页的getOrderDetail回读，确认新UniApp payResult默认成功且信任URL状态/金额，3项初始红测全部复现。现仅按当前账号/订单/读取代次的服务器paid、金额和售后状态展示，匿名需登录、未知结果只读重试，换号/hide/unload/hash换单同步清空，重复/非法ID拒绝；支付成功跳转不再携带状态和金额。29项新增组件测试与20项详情回归通过，最终完整工具链290项、类型、H5/微信/App构建和3项产物检查通过。首次完整测试的安全测试子进程停滞并终止，独立8项及关闭并行开发服务器后的完整复验通过，不声称根因已修复。插件用实际详情控制器/隔离PGlite验证伪成功URL、登录→注入失败→重试未付10.00、伪失败URL→服务器已付10.00、真实详情跳转及重复ID/外账号拒绝；桌面/390×844无新应用错误，34笔预置订单及六类快照未变、交易写入0。真实provider/回调/原生真机与发布仍开放；无生产数据/缓存写、提交推送部署，240／164／404、FE-003F、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

订单列表完整读取与生命周期（2026-09-14，本地候选、未部署）：对照PHP六状态/分页合同，补PC首20笔后的加载、两端失败原页重试、并发与迟到读取隔离、换号/离页清空和当前行操作约束。浏览器进一步复现UniApp将undefined状态序列化为status=、导致“全部”实际仅查未付；新增红测后在API适配层省略未定义参数，保留状态0。17项PC、18项UniApp新增组件/API测试及9项实际Hono/SQL测试通过；最终PC191＋9图片、UniApp261、双前端/Worker类型、PC/H5/微信/App构建和3项产物检查通过。插件在隔离PGlite的34笔预置订单中读取30笔可见订单，PC20→30、H5 10→20→30且第二页失败均重试原页；待评价/已完成/全部及真实详情往返通过。390×844 H5标签挤压已修复并重载复验，PC本轮仅桌面通过；六类业务快照未变、交易请求0。另于12:19 UTC普通线上首页→70→首页→71核实自托管新图均解码600×600，旧图片失败结论仅为历史；库存0/无SKU仍未改。无生产数据/缓存写、提交推送部署；跨请求OFFSET漂移、原生/provider/Hyperdrive/CI及发布仍开放，240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md和view/pc-ts/docs/test-media.md。下方为历史阶段记录。

订单详情生命周期与支付归属（2026-09-14，本地候选、未部署）：8项初始红测复现两端失败/换号/离页保留旧详情及PC确认期间换单误用新订单号。现绑定当前账号、订单ID和读取代次，清空旧联系人/卡密/收银与弹窗，失败显式重试；支付确认前即加锁，迟到结果不跳转/回填，未知结果先只读核对，收货/评价循环同样隔离。UniApp支持hide/show/unload与H5同页hash切换，支付中返回的旧读取不能提前解锁。新增42项实际组件/共享校验测试；最终PC174＋9图片、UniApp243、两端类型、PC/H5/微信/App构建和3项产物检查通过。浏览器真实页面接实际详情/收银控制器与临时PGlite，完成失败→收银失败→恢复、PC取消确认/退出、H5换单/重复参数拒绝及桌面/390×844验证；3笔预置未付订单、账户/库存未变、交易写入0。修复H5路由属性警告；仅余DCloud依赖弃用提示，夹具缺GET退出接口所致撤销未确认不算真实退出验收。未操作生产数据/缓存、未提交推送部署；原生/provider/Hyperdrive/跨页持久支付恢复与发布仍开放，240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

PC下单跨刷新恢复（2026-09-14，本地候选、未部署）：发现PC原订单key/冻结内容仅在组件内，loadCheckout/重建会清空。现复用共享CheckoutIntentJournal，在按用户隔离的sessionStorage保存并读回后才发送，刷新/换路由先恢复原记录再考虑购物车或报价，未知结果显式重放原key/body；已知订单结果保存ID，跳转失败仅重试查看，成功导航后清除。换号同步清空地址/表单/价格/备注及旧记录显示，需显式重载，不删除旧用户恢复记录；存储损坏/写后抛错/并发点击/迟到响应及错误回执均有门禁。新增17实际组件用例，PC全152＋9图片/类型/构建、相关4文件47项PGlite/协议、UniApp结算23项、Worker双类型通过。浏览器真实结算及Hono/SQL建单在隔离PGlite验证：19.99×2经等级优惠为35.18，首笔提交后主动丢响应，刷新后原key/body重试；一轮报价、两次create请求、最终仅一笔未付订单，商品/SKU库存8→6各一次，已建单后导航重试零新POST。桌面和390×844恢复界面通过，窄屏首次截图缩放异常经同标签重取/刷新纠正；目标详情仍是明确标记的导航验证组件，不冒充真实支付/provider验收。未提交推送部署或修改生产数据/缓存；跨设备/标签协调、浏览器会话清除后结果查询、购物车未知写恢复、实际PG16/Hyperdrive/CI与发布仍开放，240／164／404不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

已确认购买记录的跳转恢复（2026-09-14，本地候选、未部署）：PC/UniApp普通购买及套餐四项新增红测均复现“已加购、跳转失败、重试又POST”。现严格校验单行/完整套餐返回ID和别名、以不可变副本保留当前身份/页面已确认记录，提供继续结算且仅重试导航，跨模式共用忙状态；PC识别路由守卫返回失败，移动端隔离同页旧导航回调并在底栏提供恢复入口。截图发现PC顶部提示可能不在当前视口，已在原购买按钮位置补恢复入口并重测。未确认响应先阻止继续购买，需显式刷新；换号/换页/刷新/hide/unload清除本地恢复，不宣称已撤销旧服务端写。18协议、PC新增10/全135、UniApp新增11/全223、9图片、四端构建/前端类型、Worker双类型和3产物检查通过。插件以仓库外一次性故障配置和本机模拟API验证四条失败→继续链，精确到达810、820/821、830、840/841；PC原位入口重测到达850，合计五次显式初始加购、恢复零POST。H5实际390×844，目标是明确标记的导航验证页而非真实下单。无新应用错误，H5仅现存依赖警告；临时标签/视口已恢复，三服务停止且5333/5334/5338监听为0。未改生产数据/缓存、提交推送部署。跨刷新/未知响应的回执查询、服务端购物车幂等、原生真机/provider/Hyperdrive/运行时/CI及发布仍开放，240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

现代购物车会员价与状态（2026-09-14，本地候选、未部署）：实际cartList原无Env，逐行仍仅原价；现普通商品复用结算SQL资格与整分算法，每次列表共享会员上下文，新增truePrice/trueSumPrice/priceType/levelName，保留原价及旧结算字段，活动不叠加普通会员折扣。两端共享严格金额/库存/身份适配和读写代次，区分加载/错误/空列表，仅选择有效商品、更新后重读、迟到结果隔离；移动端在应用启动同步绑定换号清空，移除新增循环依赖。插件发现并修复PC失效行数量控件min大于max导致表格渲染失败，补实际模板测试；最初H5视口未实际生效也已据截图纠正，DOM确认390×844并重新操作。13项真实PG初测12红/1绿后全绿，旧立即购买夹具补齐必需会员表后最终15文件370项全绿，双类型通过；PC125＋9图片、UniApp212、四端构建和3产物检查通过。实际浏览器使用本机合成接口验证PC全选114.35→改数量87.96、失败只读恢复105.55与换号回原价/取消选择，H5登录返回会员价及实际手机尺寸79.16→105.55；修复后无新应用错误，H5仅现存DCloud路由弃用警告。临时标签/视口已恢复，三服务关闭并确认0监听。活动完整可用性、成功写后导航恢复、原生真机/provider/Hyperdrive/运行时/容量/CI与发布仍开放；未提交推送部署或改线上数据/缓存，240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

商品页身份/路由生命周期（2026-09-14，本地候选、未部署）：PC原12项、UniApp原13项红测复现旧报价/规格弹层/套餐/评价残留及离页后迟到写响应跳转。现按身份纪元、完整路由/商品ID及加载代次同步清空，PC卸载取消订阅、移动端hide/unload清空及show重读，H5处理同页hash复用；迟到读写与导航回调不污染新视图。另红测证实Axios默认异步拦截可令已调用写请求绑定新账号，改同步捕获原身份；不宣称撤销已发出的服务端写。新增14＋1＋15项纳入最终PC113、UniApp204全绿，9图片/41适配/3产物检查、四端构建和前端类型通过。共享测试location搜索/片段泄漏及第二问号截断ID均保留失败记录并修复，未放宽断言。真实插件用本机模拟API验证桌面登录返回29.99→26.39及退出回退29.99，390×844 H5登录返回17.59、规格26.39、旧弹层清空、加载失败重试、慢响应隔离及错误链接恢复；无真实DB/购物车/订单/支付写，H5仅现存路由弃用警告。临时标签、视口和三个服务已恢复/关闭。原生真机、现代购物车/成功写后导航恢复、重复读取优化、provider/Hyperdrive/运行时/容量/CI与发布仍开放；未提交推送部署或改线上数据/缓存，240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

逐规格会员价展示（2026-09-14，本地候选、未部署）：接口原仅返回SKU原价/广告SVIP价，PC/H5切规格丢失当前用户等级价，浏览器复现29.99仍显示原价而结算应26.39。现普通SKU新增member_price/price_type/level_name，复用结算资格与整分算法、每响应共享一次上下文；保留原始价格、不套用活动SKU。两端共用严格金额/分类适配，显示当前银卡/SVIP价，仅将更低正数VIP报价另标专享，旧响应不继承商品汇总折扣，购物车输入仍仅身份/数量。后端初10红转绿并扩至14项，适配41项、UniApp组件5项；最终18文件372项零失败/跳过，前端工具链189项、PC98认证＋9图片、四端构建及3产物检查/类型检查通过。保留两处旧完整对象期望新增字段及活动夹具超8字符的失败/修正记录，未放宽断言、超时或表结构。真实插件以本机合成响应验证桌面与390×844 H5规格价/标签切换、库存数量、零VIP及旧响应；不冒充真实认证/生产端到端。登录切换/页面复用失效、现代购物车、跨查询快照、零折扣选择及Hyperdrive/运行时/容量/发布仍开放。临时浏览器及三个本地服务已关闭；未提交推送部署或改生产数据/缓存。240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/sku-membership-price.md。下方为历史阶段记录。

注册等级自动激活（2026-09-14，本地候选、未部署）：对照PHP仅新用户触发的Register→setUserLevel，确认TS四处用户INSERT漏写激活状态；66项真实PG初测30失败/36通过，修复后66全过。现密码、短信新用户、WeChat/小程序/Apple/PC共享身份核心及Out建号，在身份锁/重复检查后以单条SQL读取两开关，按全局sort/id优先级决定新账户levelStatus，同账户、赠礼和身份/回执原子提交；不依赖会员KV，不激活已有账户，不授予等级/付费会员或手动激活奖励。扩展至71项，含8组独立PG锁等待中的配置变更、4组并发重复建号、赠礼/身份/回执失败整笔回滚；24文件359项零失败/跳过，最终双类型通过。测试KV替身签名问题已修正，未改生产绑定或放宽断言。决策边界为锁后策略SELECT快照，不冻结配置至提交；赠礼KV、提交后令牌/推广关系边界、已有用户模式切换及真实provider/Hyperdrive/角色/运行时/容量/浏览器发布验收仍开放。未改生产数据/缓存、提交、推送或部署；240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/registration-level-activation.md。下方为历史阶段记录。

等级激活计价门禁（2026-09-14，本地候选、未部署）：源PHP购物车/支付getDiscount检查数据库level_status，但TS展示、旧V2购物车和落单未检查；20项真实PG初测16失败，含未激活用户100×2实落160、独立连接在SKU锁等待中停用仍落单。现四类商品展示、旧购物车与核心计价要求功能开启且levelStatus===1；会员快照增加激活布尔并纳入确认指纹，复用已有FOR SHARE NOWAIT在业务写后重验，变化则整笔回滚/重新确认，锁保持到提交，不新增锁类型或外部I/O。未激活等级定义不参与价格、不锁缺失引用；付费会员独立，已落单重放原价。扩展实际激活控制器→报价及重复激活拒绝后21项，最终16文件251项零失败/跳过（含50会员边界、20完整ORM配置），双类型/diff通过；旧夹具仅显式补预期激活状态，未放宽断言。注册无需手动激活的PHP事件链已定位，TS完整生命周期待验证；零折扣用户选择、逐SKU/各端展示、运行时/Hyperdrive/容量/生产验收仍开放。发布后旧会员报价回执需重新确认；未批量激活、提交、推送、部署或改生产数据/缓存。240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/level-pricing-activation.md。下方为历史阶段记录。

会员功能开关统一（2026-09-14，本地候选、未部署）：商品列表/推荐/详情/旧规格原未检查会员开关，旧V2购物车从KV读开关，与结算SQL权威不一致；初始15项真实PG测试全红，修复后全绿。复用结算配置投影的全局sort/id、标量归一化和最低ID权益规则，三开关＋vip_price权益由单条SQL读取，无KV权威；关闭SVIP后先排除该候选再选有效等级价，并补齐旧规格选中报价字段。扩展至26项验证全部开关非法值、单次SQL、无KV依赖、读失败传播及业务/策略行不变；最终12文件196项零失败/跳过、完整ORM结算配置20项通过、双类型及diff检查通过。首轮184过/1项移动SKU五秒超时，降低并发后原时限原断言通过，保留失败记录。等级激活、零折扣用户选择、旧购物车嵌套原始VIP字段/现代购物车与逐SKU展示、全请求快照、实际Hyperdrive/容量/Linux/workerd及发布仍开放；不把单策略快照称为完整报价原子性。用户补充确认新图可读，已记为用户反馈，不重复图片写入、不替代独立浏览器全量验收。未提交、推送、部署或改生产数据/缓存；240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/membership-pricing-policy.md。下方为历史阶段记录。

等级展示价精度修复（2026-09-14，本地候选、未部署）：现有PHP8.4.25/BCMath直接调用源类getMinPrice/setLevelPrice，14组人工样例验证两段截断及零折扣/一分钱下限差异；不启动框架或数据库。旧TS两次toFixed四舍五入导致9项纯函数失败，真实PG六场景5项展示失败而购物车/确认/落单已成功，证实价格及level/member选优不一致。getMinPrice现按整分BigInt截断，保留资格与结算算法、原价/SKU字段；新增金额格式和安全范围拒绝测试。七文件142项通过，补边界后最终纯函数＋六组实际控制器/PG订单35项通过（含前者重叠项），双类型通过，隔离库清理校验通过；未实测浏览器/真实认证/支付。PHP零折扣显示免费却支付原价已明确向用户询问新系统规则，尚未改零值策略；激活/功能开关、每SKU/现代购物车展示、实际缓存/容量/Linux/workerd与发布验收仍开放。未推送、部署、操作生产数据或缓存；240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/member-price-truncation.md。下方为历史阶段记录。

会员等级定义缓存修复（2026-09-14，本地候选、未部署）：真实PG16与实际后台保存/删除处理器复现暖缓存后改名、折扣、停用和删除仍读旧等级，初版17项15失败。getLevel已移除CONFIG_KV六小时读写缓存，按现有DAO读取定义；无效ID免I/O、数据库错误不回退旧价，延迟旧读不回填；保留仅一个合法level键的显式清理，不清其他键。新增18项，六文件116项零失败/跳过、最终双类型通过；列表仍只解析一次等级，不逐商品查等级。测试环境替身初版类型失败，改普通可调用适配器包装spy，仍明确为测试子集，未双重转换或放宽生产类型。夹具为本机隔离库与真实处理器，不含生产鉴权/完整路由、真实KV/边缘缓存或完整迁移ACL验收。零折扣语义、前端SKU等级价与资格策略、容量/跨查询快照、Hyperdrive及Linux/workerd/生产验收仍开放。未提交、推送、部署或操作生产数据/缓存；240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/user-level-cache-authority.md。下方独立等级KV仍开放等表述为此前阶段记录。

商品详情共享缓存修复（2026-09-14，本地候选、未部署）：真实PG16配有状态Redis替身先复现24项失败，确认首访问者等级元数据串用、八类SKU旧价格/库存、下架/删除后仍返回缓存；源码盘点未见商品写路径调用失效方法。移除整份详情跨请求Redis缓存，重读商品/SKU并按当前用户计算，不靠TTL或失效回调保证；修正等级价胜出却返回原SVIP价/零的字段错误，补level_price并保持SKU原价字段。显式旧键清理保留但限普通键＋七类型键，正整数ID门禁；无线上清理。新增27项真实PG测试，八文件88项零失败/跳过、双类型通过；七表读前后不变，随机隔离库清理校验通过。初版测试Promise.withResolvers不符项目lib，改普通Promise屏障，未改目标版本。代价为详情增加SQL读取，跨查询单快照/容量、独立等级KV六小时失效、前端每SKU等级价展示及Linux/workerd/生产验收仍开放，不能称整个价格迁移完成。未推送、部署或改生产；240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/product-detail-cache-isolation.md。下方为历史阶段记录。

HTTP默认缓存策略增量（2026-09-14，本地候选、未部署）：从商品旧图HIT继续核查，实际createApp原本未给未声明响应设置Cache-Control，且公共站点配置异常会保留public缓存头；18项真实装配路由红测后补最外层响应中间件，未声明默认private/no-store、异常和HTTP错误禁止可缓存策略、已有明确no-store/成功公共TTL保持，101升级不触碰且不缓冲正文。新增32项测试，联合五文件61项全过、双类型通过；JWT/路由真实执行但数据服务隔离替身，不冒充真实PG或边缘缓存验收。workerd沙箱内外均在测试执行前0xc0000005崩溃，15项运行时合同仍待Linux/可用主机。Redis商品详情600秒缓存的会员派生字段及失效范围另待专项，不宣称库存/跨用户缓存问题已解决。未改线上配置/数据、未清缓存、未提交推送或发布；240／164／404、A3k13、SUP-004不变。详见workers-ts/docs/http-response-cache-policy.md。下方为历史阶段记录。

测试图片浏览器复验追记（2026-09-14 07:18 UTC，部分通过）：用户反馈能读新图后，以插件新标签从普通首页点击A/B详情，不加验证参数、不拦截请求、不改缓存。首页8张图（4A/4B）及B详情71均真实解码600×600；A详情70仍显示加载失败，不能记全量页面验收通过。桌面1265×744、URL/title/正文与截图已核验，控制台error/warn为空；SKU/库存和禁用购买按钮保持不变，手机/原生未测。新增证据确认生产API开启独立Workers Cache，直连普通详情70为HIT、Age2847且返回旧图；官方文档说明无Cache-Control的可缓存200默认TTL为7200秒，域名级purge不影响该缓存，因此下方“解决缓存权限”建议已被纠正，不再要求域名清缓存权限或登录。未继续清缓存、部署或写线上；回滚记录仍仅本机保存未推送。API缓存策略仍待专项处理，240／164／404、A3k13及SUP-004不变。详见view/pc-ts/docs/test-media.md；以下为历史阶段记录。

测试图片获准上线（2026-09-14，数据库完成、浏览器验收未通过）：PC-only 发布 c224e558，素材来源58ec07b；两张SVG线上200/MIME/hash均正确。06:52 UTC通过固定ID/名称/旧URL/整行摘要和短事务保护，恰好替换68条image，34张A/34张B，其余字段事务内摘要不变、独立读回复核通过，旧值映射已保存；未改另外3件空图商品、库存/SKU、其他前端或生产API。7项真实隔离PG测试、双类型和dry-run通过；临时Worker已删除，控制面和公网404。普通首页/详情URL仍返回旧图片，带验证参数的相同查询返回新图；插件禁用本地缓存后仍复现失败，不能记页面验收通过。精确4个URL缓存清理请求被Cloudflare鉴权拒绝，未清全站或修改缓存规则；后续需解决缓存权限/陈旧响应。58ec07b CI34814667415及37bd78e CI34813283241已成功；本轮新维护工具自身CI尚未运行。清单240／164／404和A3k13、SUP-004不变。详见view/pc-ts/docs/test-media.md；下方“未部署/待授权”为此前阶段记录。

测试图片故障定位与素材准备（2026-09-14，未部署）：浏览器Network确认商品70的接口image为via.placeholder.com/300，图片请求ERR_CONNECTION_CLOSED；首页同源，本站接口/静态资源正常，未发现该链路URL拼接或R2签名错误。公开目录71件中68件A/B测试商品使用此地址，另3件图片为空；不等于完整数据库盘点。按用户“使用测试图片”，新增站点自带、显式TEST标记的两张600×600 SVG，9项图片测试/PC构建及真实浏览器本机合成数据首页→详情解码通过。尚未发布或修改线上68条image，需重新核验并单独授权；不运行会改变用户余额/库存等的_seed。原开发种子外链和重复插入仍待修复。详见view/pc-ts/docs/test-media.md；清单240／164／404不变。

旧商品运费全数组增量（2026-09-14，未部署）：精确get_template已返回所属供应商完整id/name数组，以事务游标每批128行保持单快照，不静默截断。真实PG初版断连挂起（15过/1超时），改为响应专属客户端、断连/取消/总期限关闭后专项18项及七文件131项通过，另三文件17项审计通过；流连接清理独立观察为0且认证PID保持。路由盘点1904/1662/882匹配/864可执行，可行动缺口1005。上一fbbf696 CI34811360502已成功；本增量自身门禁、旧PHP三个调用页面、workerd/Hyperdrive和容量仍待验收。线上浏览器已复现商品图片失败，未修复；未部署或改线上数据。全局仍见1个归属未明的既存测试角色，未删除，不作零残留宣称。A3k13、SUP-004及240／164／404不变，详见workers-ts/docs/admin-shipping-grouped-editor.md。

商品选择运费的权限拆分（2026-09-14，未部署）：对照PHP商品域get_template，补齐商品查看/管理权限可用的分页metadata接口及精确ID回读，仅返回id/name/type；选择器不再请求运费管理详情，无shipping.view时隐藏管理入口。现有运费读写/回执权限未扩，租户仍取实时认证身份。真实PG受限LOGIN新增24项，旧实现16红/8绿，修复后六文件113项及路由/权限审计22项全过，双类型/Supplier构建通过。插件仅商品权限账号完成125条第7页选择、搜索/取消、503保留/重试与外租户空结果；业务写/运费编辑接口请求均0，隔离库/角色清理0残留。上一ac5131d的CI34809884563已成功，本次新增候选自身CI待验证。完整PHP get_template全数组兼容、真实角色/真机、商品保存及发布仍开放，A3k13、SUP-004和240／164／404不变。详见workers-ts/docs/admin-shipping-grouped-editor.md；下方均为历史阶段记录。

商品运费选择器截断修复（2026-09-14，未部署）：插件在125条真实PG模板下复现旧ProductForm仅100个选项，原模板201只回显数字且按名称搜不到。新分页选择器每页20条、服务端搜索/跳页，按ID独立回读当前名称；取消、失败和迟到回读不改原模板ID。运费列表不再阻塞商品基础资料初始化，未扩权限或改变商品保存。浏览器第7页选择202、搜索201、取消保留、503失败/重试及租户隔离通过；首次失败误显示空列表已修复并复测。4文件81项、Supplier完整构建及Worker单元类型通过，五表/序列不变、随机库/角色0残留。商品/分类/规格读取为合成夹具，未执行商品保存，手机/真实角色及完整PHP get_template合同仍开放。上一e72af20的CI34808890823仍运行，新增选择器自身CI未执行；A3k13、SUP-004及240／164／404不变，详见workers-ts/docs/admin-shipping-grouped-editor.md。下方为历史阶段记录。

Supplier列表单快照修复（2026-09-14，未部署）：真实PG独立连接先复现新增/退役/改归属/改名四种“旧行＋新总数”错误，现将有界页面与总数合并为一条SQL，空页仍保留真实总数；不增加锁、事务、DDL或权限。新增7项合同/并发验证，最终5文件607项零失败/跳过、双类型通过。插件经真实注册路由与受限LOGIN复验桌面翻页/空结果通过，随机库和角色独立核验0残留；手机本轮未重验、跨请求OFFSET漂移及容量仍开放。上一候选d985238的CI34807854636已成功，新增单快照修复自身CI待执行。A3k13及240／164／404不变，线上仍9fb7d27、回执表未安装；详见workers-ts/docs/admin-shipping-grouped-editor.md。下方为历史阶段记录。

Supplier分页缺口修复（2026-09-14，未部署）：对照PHP分页组件并用插件真实PG复现共45条却仅能看首20条；现补前后页、跳页、页大小、搜索回首页、失败保留原页/显式重试和迟到响应隔离。桌面浏览器三页覆盖45条、503失败重试、搜索及供应商隔离通过，五表/序列无改动、随机库/角色清理0残留。新增22项分页回归，最终8文件727项零失败/跳过、Supplier构建通过；手机viewport异常，未把缩放截图算响应式通过，末页删除UI/容量/真实身份/发布仍待验。前一候选a064815 CI34804509771终态失败，14项Admin旧夹具缺身份头已本机精确复现，补齐请求合同后通过，未放宽断言或服务端检查，新候选自身CI待执行。A3k13及240／164／404保持。详情见workers-ts/docs/admin-shipping-grouped-editor.md；下方为历史阶段。

真实PG多标签/窄屏增量（2026-09-14，业务仍a064815、未部署）：插件实测Supplier首标签正文挂起时，第二标签被Web Lock拦截且无第二次创建；超时重开后载入原冻结名称/金额。390×844下归属变更后的查询/重试均412、五表和序列不变，错误及底部按钮可见可操作；恢复归属查回10001。另一标签完成旧意图并新建10002后，旧标签确认被拒绝，刷新仍以新原键恢复10002，未被误清除。两标签无控制台error/warn，SQL/ACL独立核验及随机库/角色清理0残留。五端/workerd已通过，但候选CI34804509771仍有单元及目录作业运行，不记全绿、不重复启动。未新增业务源码或重跑702项，保留其前轮证据；登录/Redis替身、单运行连接、非真机/生产等边界详见workers-ts/docs/shipping-create-replay.md。A3k13及240／164／404仍开放，下方为历史阶段。

浏览器身份漂移修复（2026-09-14，未部署候选）：实际插件经注册路由与受限PG LOGIN复现Supplier账号改属后旧意图在新归属重复创建；新增预期身份请求头，与服务端认证身份精确比较，缺失/漂移返回HTTP412，保留原键与冻结表单。修复后查询、原键重试及刷新查询均412且五表/序列不变，恢复原归属查回最初10001；Admin正常创建/恢复通过。损坏摘要拒绝确认和原键409也经插件验证。56项真实PG红测后，最终8文件702项零失败/跳过，双类型及两端构建通过，两次随机库/角色清理0残留。登录/Redis仍为替身、Node宿主，未重做手机或生产端到端。新头是协调发布的必需合同，旧客户端将被拒绝；线上仍9fb7d27、回执表未安装，不能单独发布Worker。A3k13及240／164／404保持，当前候选自身CI及正式门禁仍待完成。详见workers-ts/docs/shipping-create-replay.md；下方均为历史阶段。

真实PG浏览器故障矩阵增量（2026-09-14，业务代码仍9cb173e、未部署）：Admin回执INSERT在事务末端被拒绝且响应丢失后，父模板和三类规则均回滚；刷新查空后原键重试成功10002。Supplier已提交10003后停用数据库角色，查询/重试均400011；恢复角色但撤销回执SELECT时两入口均500而非成功空回执；恢复SELECT后原键查回同一10003，五表快照始终不变。真实PG日志确认末端INSERT与两次SELECT权限错误；独立psql确认最终行数和回执仅SELECT/INSERT，随机库/角色清理0残留。插件桌面截图和实际列表通过；登录/Redis仍是替身，Admin未开放统计接口与Supplier缺favicon/退出撤销未确认均保留说明，不宣称全应用无错误。A3k13及240／164／404不变，完整身份/租户/损坏回执和真实部署门禁仍开放；详细证据见workers-ts/docs/shipping-create-replay.md。下方为此前阶段记录。

真实PG浏览器联测（2026-09-14，代码9cb173e、未部署）：Codex浏览器插件已通过实际Admin/Supplier注册路由、JWT校验、数据库身份权限和完整ORM受限LOGIN PostgreSQL16.15验证。Admin非空配送/包邮/禁配完整创建在断响应后刷新原键查回10001；Supplier半截正文超时后原键重放返回10002/replayed=true，五表快照前后完全相同；确认后真实列表及所属隔离正确。实际角色无高权限，回执仅SELECT/INSERT，不能读订单、改账号或建schema对象；两次随机库/角色已独立确认0残留。登录签发和Redis仍为测试替身，Node宿主，不等于workerd/Hyperdrive/Pages或生产端到端；回滚/权限/身份完整矩阵仍待补。9cb173e自身CI34800821161已11/11成功。未修改业务代码、线上DB/角色或发布；A3k13及240／164／404保持。详细边界、原始场景键/摘要及清理证据见workers-ts/docs/shipping-create-replay.md。下方状态均为阶段记录。

超时恢复增量（2026-09-14，9e20c42之后、未部署）：共享浏览器创建/恢复传输补上覆盖响应头和正文的30秒总期限，独立取消当次请求而不破坏会话；释放Web Lock、保留原键/原表单，不自动重试。旧实现新增回归4项红测，最终8项增量及既有合同合计109项零失败/跳过，双前端完整构建和Worker单元类型通过。另补共享目录push/PR触发路径，2项红测后5项CI门禁测试通过，未改时限。Codex浏览器插件实测Admin/Supplier提交后挂起至超时、同会话原键恢复分别返回704/702，均仅1次创建及1次查询；Admin另完成在途双标签拦截、换号迟到响应隔离及原账号703恢复。接口仍为明确的本机内存夹具，不是生产PG/Redis/Pages验收。9e20c42的既有CI34799544041已11/11成功；超时增量自身CI待执行。线上/main仍9fb7d27，未安装线上回执表或改变角色，A3k13及240／164／404不变。具体证据与剩余范围见workers-ts/docs/shipping-create-replay.md；下方状态为各阶段记录。

当前进展（2026-09-14，9fb7d27之后的未部署候选）：两端运费创建已接入发送前持久意图、稳定身份范围、Web Locks标签协调、精确回执校验、刷新恢复、原键重试与明确完成；新增60项协议测试，和既有会话/API合同合计101项通过，双前端构建/类型与Worker单元类型通过。用户指定的Codex浏览器插件已实际验证两端断响应后刷新恢复、Admin空回执原键重试、旧标签不得清除新意图及390px布局；插件发现并修复Admin冻结状态仍可添加规则的问题。API为仅本机夹具，虽调用真实服务端规范化/摘要，仍不等同于真实数据库/Redis/Pages端到端验收。后端候选9a79573的CI34797832927有1项旧规则测试5秒超时（其分片3137通过），本机原时限完整56项重验通过，不宣称Linux原因已解决。线上与main已为9fb7d27，main CI34797549907已success；此前发布拦截已获得用户精确授权并完成六端发布。实际运行角色、线上回执表安装、完整插件故障矩阵和新候选CI仍待完成；A3k13及240／164／404保持不变。详见workers-ts/docs/shipping-create-replay.md。本条为当前状态，下方旧阶段“尚未接入/仍539a828/待授权”等仅为历史记录。

运费创建/恢复 HTTP 接入（2026-09-14，本地候选、禁止单独部署）：实际 Admin 两兼容路由和 Supplier 创建入口已强制 Idempotency-Key；平台扁平/分组及 Supplier 服务无键插入分支全部移除，编辑仍保留原版本合同。新增 POST 管理权限恢复路由，精确回执、全响应 no-store、稳定账号/租户隔离、缺表/权限/RLS 失败关闭和真实独立连接等待提交/回滚已验证。旧路由18项红测后，最终14文件763项及3文件22项合同全部通过、零跳过；双类型和隔离资源0残留。493项实际路由测试含原250项与新增243项，不能等同于真实Redis/线上角色/浏览器验收。前端尚未持久保存请求键与原始表单，线上回执表未安装，本候选单独发布将拒绝旧页面新建；A3k13、240／164／404不变。详见workers-ts/docs/shipping-create-replay.md及workers-ts/audit/shipping-create-http-20260914.json。上一9fb7d27的CI34795252721已11/11成功，部署被安全审查拦截待精确授权，六端线上仍539a828；main bef1601的CI34794350398已成功。下方“HTTP尚未接入”为历史阶段记录。

运费创建共享规则与旧扁平回执（2026-09-14，本地候选）：已抽出分组共享规则及旧扁平校验，消除回执引擎对Supplier服务的依赖；新扁平引擎保留仅名称/空区域/小数默认值/三种计费合同，与分组共用同一键锁和回执事务，跨格式同键冲突409，原分组摘要不变。先真实PG复现旧平台顺序/并发重复创建的2项红测；新增43项引擎和4项受限LOGIN验证，最终10文件428项零失败/跳过、双类型通过、隔离资源0残留。真实独立peer确认跨格式阻塞、恢复等待提交/回滚及失败整组回滚。HTTP强制键、服务无键路径切换、恢复路由及前端跨刷新持久意图仍未实现，A3k13和240／164／404不变。详见workers-ts/audit/shipping-create-flat-replay-20260914.json；本轮未部署或访问生产业务库。

最新线上测试源为539a828（2026-09-14），API和五端已全量重新部署，两轮各17/17通过；正式迁移代码已发布，但回执表未线上安装。main后续bef1601只修正旧序列测试1090→1091和记录发布证据，未改变线上运行时代码；其CI34794350398记录时仍运行。候选539a828的CI34793000186为一项旧计数失败，原main CI34793782371被后续main推送的并发规则取消，均不算成功。详见docs/releases/20260914-full-test-redeployment-539a828.md。下方“0b9beb9最新/正式迁移未部署”是此前阶段历史。

运费创建回执正式迁移候选（2026-09-14，未部署）：补齐ORM、外部0154/内嵌0160和现有库独立根事务升级；同名表精确目录/所有权/ACL门禁，拒绝漂移、event trigger、可变授权及锁冲突，不替换或修复已有数据。真实PG16九路径264表/3708列/577约束/1022索引/227序列一致，重复安装保持OID/文件；37项新迁移验证、最终13文件337项零失败/跳过、双类型通过、隔离资源0残留。首次旧计数、服务离线与2项清理钩子失败的原始证据保留，最终有限文件并发重验通过；具体证据见workers-ts/audit/shipping-create-replay-migration-20260914.json。HTTP强制键/恢复路由、前端跨刷新持久意图、真实运行角色及获准线上升级尚未完成，240／164／404及A3k13不变。已发布0b9beb9的main CI34764831865现已completed/success，与本候选CI分开记录。

最新全量测试发布（2026-09-13，0b9beb9）：已合并推送 main，API 与五端重新构建部署；两轮各17/17只读冒烟通过，入口资源哈希一致。精确候选CI34763819911已11/11成功；main CI34764831865收尾时仍运行。详见docs/releases/20260913-full-test-redeployment-0b9beb9.md。未执行线上SQL或清库，18项未完成迁移改动保留在codex/shipping-create-replay-migration-20260913；已删除完整合并的旧远端候选分支。发布源虽含回执引擎，但尚未接入HTTP/正式迁移，不能声称线上已实现跨刷新幂等；240／164／404与A3k13保持不变。下方“3904e8b为最新/回执候选未部署”为此前阶段记录。

运费创建持久回执引擎（2026-09-13，本机候选，尚未接入HTTP/正式迁移）：真实PG先复现旧服务同键顺序重试及并发均重复创建的2项红测；新增四表模板与独立回执同事务、必需UUID v4、认证身份作用域、标准化载荷摘要、同键重放/不同内容409、等待中创建的回执查询及RLS隐藏拒绝。新增50项，六文件343项零失败/跳过，双类型通过；真实peer锁等待、回滚和受限LOGIN验证，隔离资源0残留。正式ORM/编号迁移/目录与ACL门禁、两后台强制键入口及跨刷新持久意图/恢复UI尚未完成，不能把本候选当作线上重复提交修复。详见workers-ts/docs/shipping-create-replay.md；240／164／404及A3k13不变。

最新发布追记：3904e8b已全量部署API与五端，17项线上冒烟和入口资源哈希通过；候选Actions34761689488及main Actions34762472925现均completed/success。发布证据见docs/releases/20260913-full-test-redeployment-3904e8b.md。下方“未部署/待自身Linux”为各历史阶段记录，不代表当前3904e8b状态；本轮新回执引擎尚未部署或安装线上。

供应商运费会话与未知回执增量（2026-09-13，本地候选，未部署）：实际 Axios 请求捕获发起时 token；旧会话失效响应不清除新身份，退出立即清本机并保留旧 token 撤销，Pinia 同步身份/权限。运费页绑定挂载会话，账号/权限/跨标签变化或卸载取消待发请求、清空旧表单/列表并关闭本页弹窗；确认后重验，迟到读写不回填。保存只接受精确正整数 ID 回执，删除/退出只接受 null；结果未知保留输入并暂停本页全部写入，查询/重新编辑不解除保护。新增31项真实 Supplier Axios/Pinia测试，12文件392项零失败/跳过；双类型及Supplier构建通过。桌面1280×900/手机390×844共20个真实页面→专用本机PG场景、旧冲突/重读流程复验通过。真实账号/真机、跨刷新持久幂等与未知结果恢复、其他Supplier页面逐一采用会话保护、损坏数据修复、容量、自身Linux及发布仍开放；240／164／404及A3k13不勾选。详见workers-ts/docs/admin-shipping-grouped-editor.md。

上轮发布终态追记：线上仍为7dc52ed，六端全量发布记录见docs/releases/20260913-full-test-redeployment-7dc52ed.md；该源候选Actions34759108922及main Actions34759671112均已completed/success。本轮会话增量尚未发布，不能把上轮CI成功套用于新候选。

供应商运费快照增量（2026-09-13，本地候选，未部署）：供应商详情已改为同一 SQL 的四表有界快照，与 Admin 共用 shipping-v1 指纹；编辑必须携带版本，在有界 READ COMMITTED 事务取得供应商 advisory 锁和父模板锁后复核，拒绝旧表单覆盖和锁等待后的所有权变化，新建不要求版本。同组费率不一致、路径损坏及规则溢出拒绝读取。页面保留冲突输入、暂停保存，确认后重新读取；读取失败不替换输入，迟到详情不覆盖新建表单，窄屏操作区保持可见。最终13文件447项零失败/跳过、双类型及Supplier构建通过，桌面/手机真实本机SQL恢复流程通过；Windows workerd在启动前发生0xc0000005，11个启动错误、无测试执行，不能计为通过，仍待当前候选自身Linux。详情见workers-ts/docs/admin-shipping-grouped-editor.md。清单仍240／164／404，A3k13及供应商身份/回执/未知结果幂等、真实角色/真机/完整容量门禁保持开放。

发布状态纠正（2026-09-13）：c4489c1已快进合并、六端全量测试部署并完成17项只读复测，见docs/releases/20260913-full-test-redeployment-c4489c1.md；该精确SHA的候选Actions34756939498及main Actions34757326726均已成功。下方“默认模板未部署／线上2b6469d／CI待核实”仅为该增量当时记录，不代表当前线上版本。本轮供应商快照候选不自动重部署，不改生产数据库。

默认运费模板合同补齐（2026-09-13，未部署候选）：对照PHP两端ID1禁删规则，修正“解绑后允许删除默认模板”的错误旧测试。共同退役入口在事务前拒绝ID1（无引用/停用/已删/缺失均拒绝），Admin和Supplier列表隐藏删除并标识默认模板，编辑仍可用；不自动修复或重建默认项、不扩大停用限制。11文件411项及4项CI门禁测试通过，双类型、两端构建通过，两端各桌面/手机真实控制器→隔离PG编辑/取消/删除/刷新验证通过，隔离资源零残留。已发布2b6469d的两次CI终态失败：均2752项通过，但Admin会话21项因单元作业缺Axios未能加载；已补两个分片安装Admin锁定依赖，干净临时目录复现失败后原21项执行全过，仍待当前候选自身Linux门禁。详情及原始报告位置见workers-ts/docs/admin-shipping-grouped-editor.md。线上仍2b6469d，240／164／404及A3k13开放状态不变。

最新全量测试发布（2026-09-13 11:29 UTC）：按用户“线上为测试状态，直接全量重新部署”要求，将会话保护提交2b6469d快进合并推送main，并重新构建、部署API及PC/H5/Admin/Supplier/Kefu五端。五个Pages均production/main、精确SHA、success且Functions启用；Worker 47d7d28c承接100%流量，17项只读检查及五端入口哈希全部通过。双类型、五端构建通过，原353项回归报告已核验。候选Actions34753660067及main Actions34754263804仍待终态，不能记为全绿。已删除完整合并的会话保护本地/远端分支及无独有提交的默认模板合同本地空分支；未清库、迁移SQL、改密钥/域名/Hyperdrive。默认模板ID1禁止删除尚未实现；清单240／164／404不变。详见docs/releases/20260913-full-test-redeployment-2b6469d.md；下方“待发布候选”等属于此前阶段历史。

运费会话边界补全（2026-09-13，待发布候选）：真实浏览器复现切换账号后旧删除确认仍发DELETE、损坏成功回执误报保存成功及旧表单残留。现统一会话失效/卸载取消、确认前后复核及正整数ID/空删除回执校验，未知结果禁止直接重试。11文件353项、双类型/Admin构建通过，22项双尺寸真实本机SQL边界及原完整规则正向流程通过；最初一项建库超时及测试器修正保留原记录，详见workers-ts/docs/admin-shipping-grouped-editor.md。此前已发布主线的ce8739d/Actions34752548223已11/11成功，不外推成本轮候选CI。A3k13剩真实角色/真机、持久幂等、损坏修复、完整合同/容量和发布验收，240／164／404不变。

最新全量测试发布（2026-09-13 10:31 UTC）：三个分支已合并至 main，37d3308 的 API 与五个前端已重新构建部署；控制面和17项只读检查通过，五端入口哈希一致，已清理三个本地/远端已合并分支。前置CI最终因新增编辑器未同步页面盘点而失败；已补快照及审计跨平台换行修正，31项专项通过，后续精确修正提交CI仍需核实。详见 docs/releases/20260913-full-test-redeployment-37d3308.md；未清库或执行数据库迁移。240／164／404及A3k13保持开放。下方“本地候选／未部署／线上614bf34”是各增量完成时的历史记录，不代表此刻线上版本。

完整运费编辑增量（2026-09-13，本地候选）：总后台已接入省市分组、件/重/体积、条件包邮和禁配的完整读写；四表快照指纹在父锁后复核，旧无版本表单拒绝，扁平表单不能覆盖结构化规则。真实PG两项红测后最终8文件303项零失败/跳过，双类型/Admin构建、桌面和手机真实SQL新建/编辑/冲突保留输入通过。详见workers-ts/audit/admin-shipping-grouped-20260913.json；A3k13仍待自身Linux、真实角色及剩余完整合同门禁，240／164／404不变。上一ef4fce3/Actions34749398563最终9成功/2失败，唯一单元失败为API审计快照过时，当前刷新并保持精确相等测试，不能把旧CI记绿。当前Admin345调用/365变体均可执行，路由PHP1904/TS1656/匹配881/可执行863，缺口未借新增接口缩小。线上仍614bf34，本轮未部署或写生产。

总后台运费列表增量（2026-09-13，本地候选）：真实PG先复现无界列表及旧父/新子混读两项失败，现以单数据语句快照、有界游标分页、当页完整区域规则和严格参数修复；前端接入搜索/页大小/前后页/失败重试/迟到隔离，手机固定操作列遮挡已修正。两次最终回归5文件275项零失败/跳过，Worker双类型、Admin构建及1280/390隔离界面验证通过。详见workers-ts/audit/admin-shipping-list-20260913.json；完整PHP分组编辑、旧表单冲突、真实角色/生产规模及自身Linux/发布仍开放，A3k13不勾选，仍240／164／404。此增量尚未部署；线上仍为614bf34六应用测试发布。

Supplier Pages 配置验收（2026-09-13）：FE-004K 已以实际 production/main@614bf34、Functions 标记、项目与部署环境清单、固定 API 默认映射、同源构建和线上请求核实关闭。Pages 本身无需直接数据库/存储绑定或 Secret；不外推到上游 Worker 最小权限。新增只读重复检查与 24 项反向/代理测试，联合域名合同 41 项、双类型通过。真实账号/第三方和发布观察仍属 FE-004I/J/L；当前240勾选／164开放／404项，详见 workers-ts/audit/supplier-pages-acceptance-20260913.json。最新六应用测试发布为614bf34，17项冒烟通过；该main Actions34747816178现11/11成功，新验收器bd212ef的Actions34748498956已排队，下方各旧SHA状态为历史记录。

运费受限 LOGIN 路由验收（2026-09-13，本机）：实际注册路由以维护账号和独立受限 LOGIN 双身份执行，230 项通过；模板管理权限仅含四张模板/规则表必要 DML、四序列 USAGE、六引用表及账号/角色/城市只读，不能读取订单或修改账号、商品引用、DDL。补齐自动ID新建、非空三类规则、详情/城市、回滚和同连接权限恢复；不代表整个Worker最小权限、数据库级租户隔离或线上身份验收。A3k12和239／165／404不变，详见workers-ts/audit/shipping-lifecycle-restricted-routes-20260913.json。

发布后CI终态（2026-09-13）：源eaa6773/Actions34744845166已11/11成功；已发布main b6dc8cb/Actions34745307600为8成功3失败，两单元分片各1测试失败及汇总拒绝。规则测试将pg_class统计刷新当作定义改写，真实ANALYZE复现后逻辑快照修正、56项本机通过；砍价新建400原因未明，本机52项复验通过但不视为修复，仅补安全夹具错误诊断。此次发布状态与业务/迁移验收继续分别记录，不以旧SHA或本机通过覆盖main失败。

运费注册路由授权（2026-09-13，本机未上线）：实际 Admin 两兼容入口及 Supplier 路由、JWT 验签、数据库账号/角色、业务控制器和正式生命周期协议在完整隔离 PG16 中联测，98 项通过；覆盖无令牌/失效/撤销/类型、仅查看/失效角色、跨租户/伪造身份、合法写入历史保持及有权限仍禁止删除保留引用。仅令牌桶替身、禁止外部请求；不是线上身份、真实Redis、浏览器/CORS或最小权限验收。首轮9项失败均为夹具合同问题，已如实记录。业务代码未改，A3k12和239／165／404不变；证据workers-ts/audit/shipping-lifecycle-route-auth-20260913.json。

运费重写规则门禁（2026-09-13，未安装线上）：真实 PG16 复现规则改写历史运费、先删保留引用绕过裸触发器协议；七表启用规则已纳入文件/内嵌/独立协议与索引安装前后检查，禁用和无关表规则保留。锁前启用/锁内拒绝及真实 peer 阻塞有证据；55 项规则测试纳入 20 文件 507 项零失败/跳过回归。该门禁不阻止高权限身份在提交后新增规则；A3k12、完整写面/跨表交互/权限/并发/容量/线上验收继续开放，239／165／404不变。详见 workers-ts/audit/shipping-lifecycle-rules-20260913.json。

旧索引提交029cf03的Linux Actions34741866997整体失败，原始日志确认单元分片1为两处未同步合同断言：1080→1090建库语句、原地DROP→受控清理帮助器；该run目录/容量job成功且完整Linux计划已归档，不冒充全CI通过。本增量修正对应测试并新增分片完整输出/JSON失败留存，不放宽门禁或超时。

运费正式索引增量（2026-09-13，未安装线上）：六个全状态引用表达式索引、四个源商品索引已接 ORM／文件0153／内嵌0159及独立升级，精确定义/状态漂移拒绝、已有OID保持、缺少项创建及失败整组回滚通过。最终选定19文件448项零失败/跳过、双类型通过；九路径263表/3701列/571约束/1020索引/227序列全目录一致。前两次审计的首库删除超时保留失败记录；新增仅对已登记invalid且零连接测试库的一次清理恢复，真实恢复成功且最终隔离资源0，不放宽期限或强制断连。

30万引用使用真正索引升级后，父退役112–115ms→3.3–4.1ms、应用247–300ms→43–49ms，真实auto热点预热后的无引用检查过滤0行/12块。六表100行×三样本的108份读写计划记录了部分写入耗时和WAL增加，不以读提速替代容量验收。Linux CI接入同一实验并保留完整计划，本增量自身CI待验；A3k12、多分布规模/完整角色及线上验收仍开放，239／165／404不变。证据workers-ts/audit/shipping-lifecycle-indexes-20260913.json。上一轮应用897136b已全量部署，其main Actions34739769626已11/11成功；下方“未注册/未发布”为相应历史阶段，不代表线上已安装协议或索引。

运费查询容量增量（2026-09-13，未发布）：六类父引用检查与应用共用非空CASE表达式，168组边界等价通过；30万引用实测发现字面量索引计划仍掩盖实际触发器扫描30万行/5749块。新增默认auto热点预热的真实内部计划回归先红后绿；父触发器改固定SQL参数绑定逐次规划，不改全局优化器或超时。十个物理候选索引尚未注册ORM/迁移，最终规模/写成本/线上角色及其余生命周期验收继续开放，A3k12与239／165／404不变。已发布c116838的main Actions34735685981现11/11成功，本增量不借用旧SHA验收。

该增量最终16文件382项零失败/跳过、双类型通过，隔离资源0；30万引用配候选索引后实际父退役2.5–2.7ms、应用43–50ms，预热后的无引用内部检查过滤0行/12块。完整计划、源哈希、红绿测试和未完成边界见workers-ts/audit/shipping-lifecycle-capacity-20260913.json；不把隔离候选索引或一次分布的计时当作正式容量验收。

运费相关触发器交互（2026-09-13，未发布）：三条完整构建路径确认七表无额外触发器；真实PG16裸协议反例复现其他行触发器改写历史运费，以及内部外键级联删除/改绑保留引用时shipping-only目录仍complete。文件/内嵌/独立安装新增共同门禁，锁前/锁内拒绝启用的未审查七表触发器（包含内部RI），保留禁用及无关表对象；维护身份后续DDL、规则、完整权限/并发/容量仍待验收。证据workers-ts/audit/shipping-lifecycle-trigger-interactions-20260913.json，A3k12与239／165／404保持开放计数。

运费正式迁移注册（2026-09-13，本地接入、未安装线上）：文件0152、内嵌0158及已有库独立runShippingLifecycle共用固定安装SQL；根事务/七表NOWAIT锁内重验、环境/目录/ACL、幂等和失败整组回滚也由原始SQL自带验证，0000–0157不改号。最终12文件322项零失败/跳过、双类型通过；九路径目录审计另见workers-ts/audit/shipping-lifecycle-migration-registration-20260913.json。线上角色、容量及其余A3k12验收仍开放，239／165／404不变；下方“未注册”均为阶段历史。

运费安装环境（2026-09-13，未注册/未发布）：真实PG16红测复现DDL事件在协议目录完整时仍改写历史订单，以及replica模式错误准入；候选阻写事务新增锁前/锁内环境复核，拒绝已启用事件和replica，不自动修改维护对象。新增19项、8文件225项零失败/跳过、双类型通过，隔离资源0。正式注册、高权限维护协调及非协议行触发器交互仍开放，239／165／404不变；证据 workers-ts/audit/shipping-lifecycle-install-environment-20260913.json。

最新测试发布（2026-09-13）：main@d66cd19已全量重建发布API及五端，Worker 100%流量、Pages同源main/production、17项只读冒烟及五端资源哈希全部通过。后续已确认main Actions34732805548全部11作业成功。未清库/迁移SQL/换凭据，本次运费正式注册修改未包含在该发布内；迁移验收开放项不因此关闭。证据 docs/releases/20260913-full-test-redeployment-d66cd19.md及workers-ts/audit/shipping-lifecycle-migration-registration-20260913.json。

运费运行权限候选（2026-09-13）：新增实际LOGIN及可达角色只读权限检查，34项新增、七文件206项零失败/跳过、双类型通过。仅覆盖七表协议权限边界，不等于完整服务授权、目录/基线验收或已安装正式迁移；A3k12继续开放，239／165／404不变。证据 workers-ts/audit/shipping-lifecycle-runtime-permissions-20260913.json。

运费候选目录/幂等（2026-09-13，未发布）：5函数/9触发器的固定正文/签名/执行属性/所有权及ACL、触发事件/启用/条件/参数/挂载精确校验已接安装阻写事务；完整一致重复applied=false并保留OID/非空数据，部分/变异拒绝覆盖，创建后复验失败整组回滚。新增38项，最终6文件172项零失败/跳过、双类型及8应用案例通过，隔离资源0。固定的默认EXECUTE ACL不等于正式最小权限，运行角色/非协议触发器交互/新建升级注册/容量仍开放；6bf227e/Actions34728403545已11/11成功，当前增量CI待验。A3k12不勾选，239／165／404不变；证据workers-ts/audit/shipping-lifecycle-protocol-catalog-20260913.json。

运费安装阻写前置（2026-09-13，未发布）：共享结构/引用查询已接入测试候选安装器，根RC读写事务固定7表SHARE ROW EXCLUSIVE NOWAIT后重验，拒绝旧报告替代与坏存量；逐表写锁拒绝/部分锁释放、普通读可行、第二安装器互斥、排队写入在DDL提交后受新触发器保护有真实PG证据。新增22项还覆盖DDL及中途函数冲突整体回滚、SELECT-only/RLS/继承结构/嵌套及严格期限；5文件134项零失败/跳过、双类型及8应用案例通过，隔离资源0。正式精确目录/ACL/幂等/注册与容量仍未完成，不勾选A3k12，239／165／404不变；证据workers-ts/audit/shipping-lifecycle-write-barrier-20260913.json。

运费候选边界补验（2026-09-13，未发布）：真实PG16先复现六表负temp_id写入绕过，再增加最终行显式拒绝；六表INSERT/UPDATE运费模式、两行批量改绑失败全回滚/合法成功、模板批量停用及源归属转移双方向真实等待均有证据。4文件112项零失败/跳过（预检26、CLI2、候选36、应用48）、双类型通过，随机库/schema/角色0。按一致锁顺序及最小权限原则推进，但正式阻写安装/目录ACL漂移/幂等/注册、其余写面和容量仍开放，239／165／404不变。应用4810501已于上一轮全量发布，发布记录442fbbb/Actions34698489744为11/11成功；下方应用“未发布”为阶段历史，不适用于当前线上。证据workers-ts/audit/shipping-lifecycle-candidate-boundaries-20260913.json。

运费存量预检增量（2026-09-12，未发布）：新增7表结构门禁、6类全部保留引用的有界只读一致快照与显式目标CLI；分类统计缺失/停用/跨属/源缺失/负模板ID/默认引用，真实SELECT-only角色验证RLS与权限失败关闭，退出0/1/2不混同迁移安装。另以红测复现并修复测试候选的源商品TRUNCATE CASCADE绕过。4文件97项零失败/跳过（预检26、CLI2、候选21、应用48）、双类型及候选开关各8案例通过，随机库/schema/角色零残留。正式安装仍需阻写边界重验、精确定义/ACL漂移与幂等，未注册迁移或写线上；A3k12和239／165／404计数不变。证据workers-ts/audit/shipping-lifecycle-baseline-20260912.json。

运费生命周期应用集成（2026-09-12，未发布）：共同准入/退役协议已接入总后台、Supplier、Out、MobileAdmin、套餐和活动来源变更；新红测补齐砍价刷新所属方却保留他方模板的问题。专用PG16完整选定18文件306项零失败/跳过，含48项应用协议和20项数据库候选；同一8案例在候选关闭/开启时均通过，双类型通过。第一次无文件并行上限的回归有2个文件因PG超时失败，保留原报告；不改超时阈值、maxWorkers=2复跑全部通过。清理超时遗留的2个invalid隔离库后，随机库/schema/角色均0。未把直接SQL候选注册生产迁移，A3k12继续开放，239／165／404不变；证据workers-ts/audit/shipping-template-lifecycle-application-20260912.json。

A3k12数据库协议候选（2026-09-12）：完整ORM/PG16限定的AFTER触发器覆盖六类最终引用值、源所属方、父删除/软删/停用/身份改变，SHARE NOWAIT和RC快照拒绝并发失效绑定，另拒绝模板TRUNCATE；SECURITY INVOKER且RLS隐藏引用失败关闭。20项本地测试零失败零跳过、双类型通过，真实受限LOGIN及双连接提交/回滚有证据；同一8案例原始基线仍8失败，安装候选后8通过且父行未变。零临时库/schema/角色残留。候选仅在测试帮助器，未注册正式安装/升级、未完成存量/ACL漂移/应用写端和错误映射、保留策略/容量/自身CI，未改线上；A3k12仍开放，239／165／404不变。证据workers-ts/audit/shipping-template-lifecycle-candidate-20260912.json。

A3k12运费生命周期红色验收（2026-09-12）：完整ORM/独立本机PG16中调用真实Admin删除控制器和Supplier服务，六类显式引用、供应商保留砍价引用及默认模板1共8例全部复现“仍被引用却删除”，验收脚本返回1，不是功能通过。已确认KEY SHARE/SHARE及反向锁顺序差异；不照搬PHP自动重绑模板1，不改线上数据。双类型通过、远程入口拒绝、随机库/schema/角色零残留；共同绑定/解绑/删除协议、完整入口/并发/所属方/历史订单及正式验收仍待实现。证据workers-ts/audit/shipping-template-lifecycle-gaps-20260912.json和docs/shipping-template-lifecycle.md；239／165／404不变。

G2命令与业务Linux终态（2026-09-12）：c486f22 / Actions34692558945已11/11成功，385文件4502单元零跳过、精确分片完整互斥，覆盖8受限业务和3命令用例。新Hyperdrive探针bb04311的Actions34693434221已启动，不能把前次CI外推到新探针或运费复现。证据workers-ts/audit/work-parent-cli-business-ci-20260912.json；实际线上六类权限缺口仍未修改。

G2实际Hyperdrive权限实测（2026-09-12）：固定只读临时探针确认两张父表/8引用键齐全、实际身份可见且可读，但ready=false，高权限角色、所有权、建对象、父删除/触发器、引用键更新、复制绕过六类失败。最终访问403/405/404、审计200；两次路由传播404前置失败未执行SQL，三个随机Worker均独立删除复核404，正式89dfbd1e版本/绑定未变。56项本地测试及双类型/打包通过；Windows workerd启动失败0执行，新探针自身Linux CI仍待验。未改变线上权限/凭据/数据，不把当前身份视为最小权限，G2及完整业务验收继续开放；239／165／404不变。证据workers-ts/audit/work-parent-production-permissions-20260912.json。

G2权限命令及Linux复验（2026-09-12）：显式目标只读入口 `npm run audit:work-parent-permissions` 已补齐，退出码0/1/2、非法输入连接前拒绝、真实非空完整ORM身份及错误密码有子进程证据；30项零失败零跳过、双类型通过、隔离资源零残留。27adf07 / Actions34691224392已11/11成功，383文件4491单元零跳过，精确分片完整互斥，覆盖权限预检及前序15目标/百万行容量增量；不覆盖随后8业务用例和本命令，后者CI待验。没有调用线上身份或更改权限/凭据，G2的运行身份、完整业务和wcao策略仍开放；239／165／404不变。证据workers-ts/audit/work-parent-permission-cli-20260912.json和work-parent-permissions-ci-20260912.json；下方相应运行中/待验收为阶段历史。

G2其余15目标真实计划补齐（2026-09-12）：公共采集器保留完整模型和默认auto热身，覆盖三模式/父DELETE及键更新；12个六列引用+3个可空引用共654份内部计划，492次引用拒绝、162次无引用成功回滚，已有窄/部分索引实际块读取2–5、过滤0。完整28项零失败/零跳过、双类型通过；四个日志截断目标在原运行终态后补录4项（另20项筛选排除），来源逐项标注，无损去重计划保留，隔离资源零残留。加上wcao，16个待决索引目标本机RI证据已齐，不等于45个FK全负载或正式验收。0d9fee6/Actions34688447565也已11/11成功、382文件4464单元零跳过；Linux16.14的wcao五个默认auto样本三次全扫描。证据workers-ts/audit/work-fk-all-nested-ri-20260912.json及work-fk-nested-ci-20260912.json。容量脚本新增到独立Linux目录作业末尾，当前增量自身CI待验收；wcao策略/规模/运行权限仍开放，239／165／404不变。

G2统计候选规模反例（2026-09-12）：新增显式本机PG16容量入口，完整ORM下100003/1000003行×默认/1000/10000三档×五样本，251.73秒执行270次真实RI（210次引用拒绝、60次无引用成功回滚），双类型通过、四种越界入口拒绝、隔离资源零残留。1000在十万行采全表，但百万行只采300000行，仍有1/5样本无引用扫描1000003行/40001块；10000在本次百万行样本消除无引用全扫描，却仍有热点位图读取百万项/792块且ANALYZE约3.46–3.50秒。不能把前序十万行改善作为通用修复或直接调生产统计；三表数据、全模型约束/索引及逐轮统计目录保持。证据workers-ts/audit/work-fk-statistics-scale-20260912.json，复现npm run audit:work-fk:statistics-scale。只新增实验脚本/审计，不改变线上；G2及真实规模/保留策略、其余15目标内部计划继续开放，239／165／404不变。

G2首轮候选Linux终态（2026-09-12）：10e7c63 / Actions34687629456于10:23:31UTC完成，11/11作业success；精确分片覆盖382文件、4464单元零跳过（2262+2202），PG目录、workerd、五端构建和密钥扫描均通过。证据workers-ts/audit/work-fk-capacity-ci-20260912.json。该运行只覆盖统计隔离及12项热点多租户增量，不覆盖其后ea1e91f的真实内部计划和本轮默认缓存热身代码；后者仍需自身CI。下方该运行“仍在运行”为历史状态，G2仍开放。

G2默认缓存热身增量（2026-09-12）：5个独立PG16连接/默认ANALYZE样本，每样本18次真实父操作（先DELETE后UPDATE），全程保持plan_cache_mode=auto并用保存点/外层事务回滚。90份内部计划包含70次引用拒绝与20次无引用成功回滚；五样本均观察到第6次DELETE切换参数化计划，两样本无引用扫描100003行/4001块、稀疏引用过滤100000行/4002块；另三样本无引用2块/稀疏4块，但热点位图仍先读取100000项/89块。风险现已在默认RI实际执行中复现，不再只依赖强制generic诊断。4项PG16测试、双类型通过，逐样本统计目录恢复、父子数据/约束/索引保持、隔离资源零残留。详见workers-ts/audit/work-fk-default-cache-20260912.json。下一步为统计候选的实际RI/成本/规模与其余15目标内部计划；G2仍开放，239／165／404不变。

G2真实RI计划增量（2026-09-12）：专用PG16.15保留完整ORM，通过独立连接auto_explain捕获wcao_client_fk的12份真实内部计划，覆盖auto/custom/generic与热点/无引用父DELETE及键更新；4项测试全部通过、双类型通过、隔离库/schema/角色零残留。6次引用拒绝、6次无引用成功均回滚，父子行及外键/索引/统计目录保持。热点generic虽返回1行，位图索引仍先读取100000项/合计89块；不把RI单行上限当性能保证。本次默认统计独立诊断五次中两次全扫描，风险仍开放。证据workers-ts/audit/work-fk-nested-ri-20260912.json；另15目标嵌套计划、长连接auto热身、多次统计样本及生产策略仍待验收。10e7c63候选CI的PG目录、运行时、五端和密钥扫描通过，两单元分片仍运行；本新增代码尚未由该CI覆盖，239／165／404不变。

G2热点多租户补验（2026-09-12）：12个六列复合引用保留完整模型，每项新增双租户各50000条热点引用及稀疏/无引用/错误租户场景；原12项与新12项合计24项PG16通过，零失败/跳过、双类型通过，隔离库/schema/角色零残留。新用例真实父端拒绝96次、无引用操作回滚48次，父子数据及索引/约束/统计目录保持；合法稀疏/无引用custom/generic探针均2–4块。错误租户组合均返回0但过滤50000行；无上限热点探针不等同RI内部只需发现一行的执行，不据此增宽索引。证据workers-ts/audit/work-fk-hot-multitenant-20260912.json及docs/work-fk-capacity-acceptance.md。wcao默认统计不稳定、生产规模/保留策略、真实嵌套计划和自身Linux CI仍开放，G2不勾选，239／165／404不变。

G2统计诊断隔离修复（2026-09-12）：新增真实PG16断言先复现旧测试将STATISTICS 1000诊断提交后继续执行父端验收的缺口；改为同连接回滚事务，并在实际父DELETE/键更新之前比对全部列统计目标及完整pg_statistic行恢复。最终3文件43项零失败/跳过、双类型通过，隔离库/schema/角色零残留。默认统计风险仍复现：五次generic计划三次扫描100003行/4001块，未通过强制索引或提交统计调优掩盖。证据workers-ts/audit/work-fk-statistics-isolation-20260912.json。只修改测试，线上无变化；新候选自身Linux CI、多租户热点覆盖及生产保留/统计策略仍开放，G2不勾选，239完成／165开放／404项不变。

发布后验收推进（2026-09-12）：main@214dfb2 的 Actions34685623400及部署源97b2a13的Actions34685058465均已终态success。main的11个作业全部通过：382文件／4452项单元零跳过，11文件／146项workerd；PG16九路径五类目录零差异，五端构建通过。DB-009G1的33项迁移与2项真实计划测试通过，发布后两次只读复核确认两个0147索引精确定义、valid/ready，目标两表指纹未变；临时入口全部删除并公开404，现关闭G1，239完成／165开放／404项。九表仍65行，但订单与购物车指纹相对部署前改变，两次发布后检查彼此一致，原因未定，不据此声称全库未变或擅自回退。证据workers-ts/audit/foreign-key-child-index-postdeploy-20260912.json；G2、0150、最小权限及正式业务验收仍开放。下方CI运行中等为阶段历史。

线上测试环境全量发布（2026-09-12）：用户明确线上为测试状态，已按授权将97b2a13完整部署至API和Admin/PC/H5/Supplier/Kefu五端。API版本42e39c18占流量100%，五端main/production部署SHA均一致，首页及入口脚本200，同源公开API与匿名拒绝JSON复核通过；商城浏览器首页→商品列表正常，测试商品图片仍不可达。DB-006/007与0146–0149及0151核心增量已完成，核心九表65行指纹不变，幂等复验通过，临时Worker均删除；保留12笔孤儿已付款测试订单，0150仍未安装，不绕过失败关闭。Actions34685058465的运行时、PG16、五端构建及密钥扫描通过，两单元分片及汇总仍运行，不能宣称完整CI成功。详见workers-ts/audit/full-test-deployment-20260912.json及test-release-core-schema-production-20260912.json。238完成／166开放／404项不变，正式生产验收独立开放；下方“未发布”等均为历史快照。

最新生产结果（2026-09-12）：经本次明确授权，DB-006已通过固定维护执行器在生产完成；重放列、唯一索引及wechat 64字符定义均由独立只读事务复核，现有5行的规范化SHA-256前后及幂等复验全部一致，第二次执行applied=false。临时Worker控制面及公开端点均404，正式cinashop-api版本仍为9f1fd655，未发布应用。候选cb5b8ad的Actions34470894844已完整成功。证据workers-ts/audit/withdrawal-replay-production-20260912.json。当前237完成／167开放／404项；DB-007、其余生产结构、最小权限与真实角色门禁仍开放，正式部署成功验证之前保留发布分支。以下2026-09-10的未授权／CI未完成等为历史快照。

时钟CI修复候选（2026-09-10）：已确认旧测试整秒取整可将人为等待延长到约2.2秒，超过业务2秒锁期限。只把测试时钟余量1200改为500ms，新增全1000毫秒相位检查及“实际阻塞开始时尚未到期”断言；不修改业务超时或将SQL失败当重确认通过。未改版本机108项一次通过，不能据此否定CI失败；修正后联合5文件168项零失败／跳过，双类型通过，隔离PG零残留。证据release-catalog-clock-20260910.json。新候选仍须自己的完整Linux CI，不提前发布或勾选；DB-006生产DDL授权仍待答复。

最新生产目录／CI终态（2026-09-10）：`1f8ca6b`的Actions34468277203失败，第一片189文件2202项仅1失败（分佣时钟UTC有效对照在明细INSERT发生55P03锁超时）；第二片188文件2168项零跳过，workerd98项、五端、密钥及PG目录通过。不能称候选全绿。扩展只读探针已在线确认0134四列宽度已到目标，但0146–0148四索引、0149砍价购物车字段／约束缺失；DB-006/007和0150/0151仍缺，0135–0145未据此验收。临时探针已删除并控制面404，未执行生产DDL；4文件59项本机回归与双类型通过。证据release-expanded-production-catalog-20260910.json。236／168／404不变；下方“CI仍运行”为历史快照。

生产DDL执行边界（2026-09-10）：维护候选`1f8ca6b`已推送，Actions34468277203的workerd已通过，完整单元仍执行。尝试执行固定DB-006维护命令时，安全审批因缺少本次具体生产DDL／维护窗口授权而在进程启动前拒绝；未创建维护Worker、未执行SQL、未改变生产结构。须获得明确授权后再执行，不得绕过；DB-006仍开放，236／168／404不变。

完整CI／结构定位（2026-09-10）：`2a6fa02`的Actions34465733296全部成功：374文件／4333单元零跳过、7文件／79 workerd、五端及PG16目录，生产Worker依赖和Kefu完整依赖审计0漏洞。TEST-004G/H据该不可变提交关闭，现236完成／168开放／404项。新只读生产catalog已确认DB-006/007新列／索引及0150/0151函数／触发器缺失，wechat仍15字符，角色可达SUPERUSER。DB-006独立有界执行器和HTTP入口已准备，本机4文件57项通过，尚未执行生产DDL；不将上述旧CI借给新增维护代码。证据release-prerequisite-catalog-20260910.json。

探针自身CI补充：`1fed16a`已提交推送，Actions34465418616的workerd76通过／3失败，失败为新脱敏测试读取console.error.mock元数据异常，响应状态／脱敏断言此前通过。已改为直接捕获调用，增加实际错误事件正向断言并保留无敏感内容断言，本机23项通过；Windows workerd仍启动失败、执行0项，不豁免。旧运行其余作业诊断时仍执行，修复提交需重新验证完整CI，未发布正式应用。

生产权限实测／CI失败定位（2026-09-10）：经正式同一Hyperdrive执行有界只读权限检查，ready=false：必要对象未齐，可达高权限、所有者、CREATE、TRIGGER、TRUNCATE、复制绕过六类限制失败，不据可读写判为可发布。临时探针已删除，控制面及公开端点404，正式Worker未变，无生产DDL／DML／GRANT。Actions34463119442终态failure：第一分片仅容量合同仍断言旧20分钟而失败，其余2176项通过；第二分片2136项、workerd59项通过。已同步合同为unit40／catalog20，其余门禁不变；本机最终3文件45项通过，双类型通过，仍需新CI。详见workers-ts/audit/paid-runtime-production-preflight-20260910.json；234／170／404不变，TEST-004G/H、DB-006/007、A3k11c/d及正式发布继续开放。下方运行状态为历史快照。

首轮CI终态（2026-09-10）：Actions34460778121第一单元分片超过20分钟job期限被取消，汇总失败，其余9作业成功。取消前仍持续新增通过模块（共62条完成记录），未见Vitest断言失败输出，但完整覆盖未产生。已据此将整个unit job预算调整为40分钟，单测期限、断言、两分片与精确零跳过校验不变；需包含3872506品牌修复的新CI，不以超时前局部证据勾选TEST-004G/H。以下“仍运行”是历史快照，仍234／170／404。

候选推送／后台预览进展（2026-09-10）：`8ee335e` 与品牌路由修复 `3872506` 已推送独立 `codex/migration-shop-release-20260910` 分支，未改远端main／正式流量。Admin预览df175ac1已验证只读品牌接口JSON、匿名管理拒绝、空表单及桌面／移动布局，正式后端完整品牌字段和真实登录仍开放。Actions34460778121对应8ee335e，workerd49项、五端、密钥扫描、PG16目录与第二单元分片186文件／2136项／零跳过成功，第一分片及汇总尚待终态；不能替代3872506自身CI。本机新旧workerd池启动失败、执行0项，未豁免；新代理及相关Node31项和双类型通过。详见release-domain-baseline-20260910.json。仍234／170／404，下方未提交／未推送等描述为阶段历史。

最新终态（2026-09-10）：shop.cinaseek.ai 已 active 且 HTTPS 200，正式 PC 未切换；新版 PC 已单独部署到 157b8f1c.cinashop-pc.pages.dev 并验证首页→列表→价格排序→详情及桌面／移动布局。占位图片不可达、测试商品缺少有效 SKU 仍开放，未做真实购买。完整 Worker 单元 90502 已终态成功：371 文件／4287 项零失败／跳过／todo，启动前及终态后选定源码哈希一致；此后补 shop 白名单和生成类型，新增域名测试联合既有登录边界 21 项通过，配置尚未发布到生产 Worker。证据见 release-domain-baseline-20260910.json 与 admin-config-batch-authority-20260910.json。下方“90502 仍运行／无新部署”是历史阶段，不能当当前状态。仍 234／170／404，不提前关闭迁移、登录、权限或正式发布验收。

shop域名最终复核（2026-09-10 08:57 UTC）：shop.cinaseek.ai的Pages域名／证书校验／验证三状态均active，HTTPS首页200，内置浏览器实际显示导航／推荐商品／分类。未替换应用部署，仍为旧PC入口；保留现有根域名／admin／api／www。下方证书pending是创建初期状态，最终证据见release-domain-baseline-20260910.json；域名接通不等于登录与迁移发布验收，完整Worker单元90502仍运行。

发布授权与域名更新（2026-09-10）：用户已将目标扩为提交／推送／部署并直接线上测试；正式后台选定cinashop-admin.pages.dev，商城使用shop.cinaseek.ai，明确保留已占用根域名／admin／api。已按Cloudflare控制面核对旧Worker及四个Pages部署，新增空闲shop域名绑定与CNAME，HTTPS首页200但仍加载旧PC资源；证书控制面尚pending。九个线上无凭据GET发现正式后台/adminapi/config/list返回HTML壳，直接Worker则正确拒绝匿名请求，说明不能把页面200当代理验收。证据release-domain-baseline-20260910.json。发布批准不再是REL-002/003的缺项，但构建／迁移／权限／回滚／线上验收尚未完成；全量90502仍运行，仍234／170／404，不提前勾选。

后台原子保存／锁权限续验（2026-09-10）：后台保存候选新增46项、联合6文件132项和双类型通过，详见admin-config-batch-authority-20260910.json；完整Worker单元90502仍在原句柄运行，src/test/scripts/config冻结。新增隔离权限探针证实固定两表的零参数锁函数可让独立SELECT-only登录取锁而无直接DML／DDL权限，提交／回滚和临时表遮蔽有证据，随机库与角色已清理。但尚未接入应用，直接LOCK的42501缺口仍存在；协议校验、权限预检、迁移和真实HTTP整合待完成，见pricing-lock-capability-20260910.json。仍234完成／170开放／404项，不勾选A3k11c/d，不提交推送或部署。下方原子保存尚待办为历史状态。

全量终态与修复复验（2026-09-10）：首轮370文件含6失败文件，4231项4225通过／6失败，另1个清理钩子超时。修复SQL政策种子／旧断言与行号／清理期限、Out删除配置锁逆序及三时区发现的拼团取消时间判断；最终15文件275项零失败／跳过、双类型通过，1148选定路径哈希前后一致，临时PG零残留。证据pricing-full-regression-20260910.json。尚未重跑完整370文件，后台原子保存和真实权限仍待办，不勾选A3k11c/d，仍234／170／404项，未提交推送／生产写入／部署。下方“全量仍运行”为历史状态。

计价写端／权限反例（2026-09-10）：真实后台批量保存已复现后项失败留下首项包邮提交（运费600分→0），及重复键保存返回200但权威值未变。真实低权限LOGIN两种授权均在SHARE锁处报42501；详见checkout-pricing-open-boundaries-20260910.json。A3k11d须补原子批量写、全局作用域／有效优先级及真实权限，不据20项读端测试勾选。完整Worker单元仍运行且已见失败标记，待最终诊断；234完成／170开放／404总项不变。

授权后复验（2026-09-10）：内置浏览器正式PC首页→列表→价格升序→`/goods/16`导航通过；12张商品图加载失败、详情空标题／画廊仍在，console error/warn采集为空。当前源码已有字段与图片候选修复，未核验正式部署SHA，不重复改源码或勾选FE-002B/C。计价SQL权威新增20项通过，完整Worker单元仍运行，不能称全量验收通过；A3k11d最新候选边界见其条目及checkout-pricing-config-authority.md。仍234完成／170开放／404项，未登录／加购／建单／支付／生产写入／部署／提交推送。

Out条码候选增量（2026-09-10）：已复现原400／1000行查询返回风险；过滤后窗口排名使每码最多两行，初始缺失／歧义在锁前拒绝，保留100条合法同步／幂等和原作用域。第一版LATERAL的100次SKU索引扫描已由真实计划发现并修正。新增13项，最终12文件164项零失败／跳过、双类型通过，43个选定哈希最终测试前后相同；专用PG零残留核验后07:08:47.475 UTC停止。详见out-stock-candidate-boundary-20260910.json和out-stock-candidate-boundary.md。仅关闭此局部返回量缺口，生产规模扫描成本及其它正式门禁仍开放；A3k11c不勾选，234完成／170开放／404总项，未提交推送或写生产。以下同日早期条码待办为历史状态。

Out商品管理增量（2026-09-10）：上下架／编辑／跨商品库存同步3类真实PG死锁已复现修复，SKU／商品NOWAIT、同SQL锁定更新购物车，冲突整笔回滚且保持Out原过滤／幂等政策；另修复退役99件误加到活跃9件得到108的库存汇总。新增完整ORM17项，最终11文件151项零失败／跳过、双类型通过，42个选定哈希核验不变；专用PG零残留核验后06:46:42.992 UTC停止。证据见out-product-concurrency-20260910.json及checkout-out-product-concurrency.md。条码候选有界性、其余交错、正式身份／Hyperdrive／运行时／CI／发布仍开放，A3k11c不勾选，234完成／170开放／404总项；未提交推送或写生产。

移动规格请求体增量（2026-09-10）：控制器128 KiB实际字节上限先红测复现后修复，分块超限取消，保留500行业务能力。新增22项请求合同和1项完整ORM的500-SKU精确上限／超限零变更用例，最终9文件129项零失败／跳过、双类型通过。Windows workerd启动失败执行0项，运行时门禁未完成；35个选定哈希在测试期间及结束后相同。专用PG零残留核验后06:26:12.738 UTC停止，证据见mobile-product-body-20260910.json及mobile-product-request-body.md。A3k11c和正式验收仍开放，234完成／170开放／404总项；未提交推送或写生产。

移动商品管理增量（2026-09-10）：上下架／规格编辑／多商品批量运营与下单3类真实PG死锁已复现修复，商品／SKU NOWAIT，上下架同SQL锁定并更新原购物车集合，冲突完整回滚、无自动重试。新增19项完整ORM及迁移合同9项通过，最终联合7文件101项零失败／跳过、双类型通过、28个选定哈希不变。证据见mobile-product-concurrency-20260910.json，专用PG零残留核验后06:05:56.838 UTC停止。其它入口和引用管理交错、请求体边界、正式身份／Hyperdrive／CI／发布仍开放；234完成／170开放／404总项，A3k11c不勾选，未提交推送或写生产。

SKU退役并发增量（2026-09-10）：实际多商品下单＋后台编辑＋退役的三方死锁已由PG复现并修复；退役／恢复保留建议锁顺序，商品和选定SKU NOWAIT，仅55P03提示刷新并完整回滚。最终5文件73项零失败／跳过，含17项新完整ORM测试，双类型通过、21个选定哈希不变。初次扩大10项测试种子长度失败与修正证据保留，详见sku-lifecycle-concurrency-20260910.json。专用PG零残留核验后05:50:09.927 UTC停止。这里只覆盖指定退役／恢复和多商品交错，不关闭其它Out／移动入口及正式门禁；A3k11c仍开放，234完成／170开放／404总项，未提交推送或写生产。

商品／分类管理增量（2026-09-10）：真实后台编辑与下单的商品／SKU死锁已复现并修复，共享SKU编辑在持商品行时不再等待被占用SKU，仅55P03明确提示刷新重试，整笔回滚且其它错误不掩盖。后台／供应商保存、新建及Out分类树与下单的25项完整ORM专项通过，SKU合同8项通过，双类型通过；其余56文件1419项通过并核验依赖不变，分阶段覆盖58文件1452项，不冒称单次完整CI。首次1449通过／3测试合同／种子序列失败与修正证据保留，见catalog-management-20260910.json及checkout-catalog-management.md。专用PG零残留核验后05:26:53.554 UTC停止。其它SKU管理入口／多商品组合及正式门禁仍开放，清单234完成／170开放／404总项；未提交推送、部署或写生产。

管理／下单候选最终复核：相同47文件1339项全部通过、零失败／跳过，含52项管理专项；双类型通过，实际清单及71个选定文件哈希匹配。首次扩大回归1333通过／6失败及其诊断限制保留在coupon-management-20260910.json：5项清理超时已单独有界处理，1项分销时钟有效对照本次未复现但原原因未确定。最终跨文件并行数2，业务断言／用例内部竞争不变。专用PG零残留核验后04:55:41.200 UTC停止。仍234项完成／170项开放／404项，不据此关闭A3k11c或正式CI／发布；未提交推送、部署或写生产。

管理／下单处理链增量（2026-09-10，未提交）：后台保存、分类保存／删除、品牌显示保存和Out创建／启用／删除现有实际HTTP交错证据，轻量与完整ORM各26场景、专项52项全部通过，双类型通过；保留首轮26通过／4连接帮助器接线失败，修正测试后通过，未改业务或放宽身份检查。最终共享夹具23调用方的47文件回归与哈希见coupon-management-20260910.json，合同见checkout-coupon-management.md。商品编辑／Out分类树其它组合、正式鉴权／权限／生产及其它清单门禁仍开放，234项完成／170项开放／404项；未提交推送、部署或写生产。

优惠券商品／引用候选最终复核：33文件947项全部通过、零失败／跳过（新专项55项包含在内），双类型检查通过，计划文件集合及48个选定源／测试／帮助器哈希匹配。专用PG零测试对象／角色／其它客户端残留核验后04:17:52.312 UTC确认停止，原始报告保留。本机子范围证据见coupon-item-authority-20260910.json；234项完成／170项开放／404项，未提交推送、部署或写生产。

优惠券商品／引用权威增量（2026-09-10，未提交）：13项真实HTTP错误放行已复现后修复，末端在原库存写入／券模板守卫后锁定新商品范围及实际使用的分类／品牌直接引用，比较原报价有效集合；最终缺失引用失败关闭，晚到创建且集合等价时锁定后允许，不改变优惠公式或新增递归政策。94项初始关联、40项并发扩展、52项包含完整ORM真实非所有者的专项均通过，最终多商品／重确认与关联回归见coupon-item-authority-20260910.json，不累加。实际管理写入链、生产权限／维护隔离、其它表单／配置／主体及正式CI／发布仍开放，A3k11c不勾选，仍234／170／404项。

优惠券关系候选最终复核：32文件892项全部通过、零失败／跳过，含新增读端47项与注册7项；双类型检查通过，计划文件集合及最终源／报告哈希匹配，不累加中间阶段。专用PG零夹具残留核验后03:57:32.140 UTC确认停止。前一轮889通过／1失败的真实低权限问题及修复记录保留在coupon-relations-authority-20260910.json；不扩大权限或将失败归为偶发。以下关系读端与注册子范围的本机证据已补齐，商品／分类／品牌权威和其它验收仍开放，234勾选／170开放／404项，未提交推送或部署。

优惠券关系读端与注册增量（2026-09-10，未提交）：真实HTTP首轮14错误放行已复现并修复，商品券现在验证0151保护协议，先取得兼容普通DML的关系锁、再锁模板，以独立新语句重读集合并比较有效范围，保留等价变化和幂等。磁盘0151接入内嵌0157，新库158步，原157个迁移方法内容哈希不变；不在请求内升级，不对旧库重放历史。18项修复、60项读端扩展、41项注册及PGlite22通过／3跳过分别记录，最终关联回归与源／报告哈希见coupon-relations-authority-20260910.json，不相加。商品范围字段／分类品牌引用、实际管理调用链、生产身份和正式门禁仍开放，A3k11c不勾选，仍234／170／404项，无提交推送、部署或生产写入。

优惠券关系写端增量（2026-09-10，未提交）：新增独立0151 SQL／Worker模块／维护执行器，以语句级多重集合净变化锁定相关模板，重复关系保留、孤立引用及安装定义漂移拒绝。最终7文件150项全部通过、零失败／跳过，新文件45项；此前149通过／1失败为多连接池直接BEGIN的测试错误，改为固定单连接后通过，原报告保留。完整ORM及真实非所有者权限、增删改／批量转移／重复行／空集合、实际锁顺序与提交回滚均有局部证据；TRUNCATE须另有关系锁，已保留缺口对照。尚未接入下单父锁后集合重读和失败关闭，也未注册内嵌0157，商品／分类／品牌引用及正式验收仍开放，A3k11c不勾选，仍234／170／404。专用PG零残留核验后03:21:19.574 UTC停止；详情见coupon-product-scope-fence-20260910.json。未提交推送、部署或写生产。

优惠券模板候选最终复核：关联26文件719项全部通过、零失败／跳过，实际文件清单与源码／报告哈希匹配，双类型通过；不与专项25项相加。专用PG零夹具残留核验后03:00:47.739 UTC停止，二进制／数据／原始报告保留。以下模板子范围完成状态不扩展至关系集合、引用或发布验收，仍234勾选／170开放／404项。

优惠券模板增量（2026-09-10，未提交）：9种模板晚到编辑／删除错误放行已通过实际HTTP复现后修复，新增原计算输入的内部依赖和占券后的单行SHARE NOWAIT守卫，规范化比较实际使用的类型／范围，保留等价及无关编辑、无券、幂等正向对照。专项25项零失败／跳过、双类型通过，含真实四后端提交／回滚锁证据；关联回归及两次回滚夹具字段错误修正过程分别见coupon-template-authority-20260910.json。模板锁不覆盖关系集合新增、商品／分类／品牌引用变化，完整ORM／运行权限及正式验收仍待完成；下一步合同见checkout-coupon-authority.md。A3k11c保持开放，234／170／404项，未提交推送、部署或写生产。

真实运行身份权限增量（2026-09-10，未提交）：新增只读 `audit:paid-runtime-permissions` 命令和独立非所有者LOGIN测试，覆盖实际付款／退款／资格读端、用户锁等待、危险DDL拒绝及角色／授权检查。先复现并修复SESSION AUTHORIZATION身份伪装和pgx普通schema被LIKE通配符漏检；最终权限专项22项零失败／跳过，双类型通过。此前核心7文件185项中权限21项由最终22项替代，其余6文件164项依赖不变；共享帮助器其它6文件121项通过，不把多次运行相加冒充单次不可变CI。证据见brokerage-paid-runtime-permissions-20260910.json及paid-order-runtime-permissions.md。专用PG四基线库、零临时库／角色等残留核验后02:40:12.278 UTC停止。覆盖本机低权限子范围，不代表生产Hyperdrive身份、全应用最小授权、管理员维护隔离、PHP政策或正式发布门禁已验收；命令未自动串联deploy。A3k11c保持开放，234／170／404项，未提交推送、部署或写生产。

付款／退款整合增量（2026-09-10，未提交）：完整ORM建库＋累计消费保护新增20项通过，其中10项四后端竞争，覆盖余额／合成渠道入账、部分／全额退款、退货同意／拒绝、晚到失败回滚、重复执行及四种实际拼团建单／付款／团长退款竞争；没有修改生产业务逻辑。最终11文件204项零失败／跳过、unit/runtime类型通过，证据见brokerage-paid-business-transactions-20260910.json。覆盖下方“实际付款／退款锁序待验”的上述子范围；不代表全部活动／结算角色组合、生产目录、非超级用户权限、特权绕过、真实provider或PHP合同验收。完整ORM的user/store_order/store_order_refund自身无外键，不能据轻量夹具的局限断定生产存在特定外键冲突。专用PG零残留核验后01:56:30.892 UTC停止。A3k11c仍不勾选，234／170／404项，未提交推送、部署或写生产。

累计消费迁移注册增量（2026-09-10，未提交）：磁盘0150已注册为内嵌0156，完整新库157步按序调用独立限时READ COMMITTED执行器，旧156个迁移方法不变。新增7项首轮3通过/4缺口失败；修复后最终5文件167项零失败/跳过，unit/runtime类型通过，覆盖三种建库路径、重复安装、数据保留、失败停止/DDL回滚和累计消费读端；ORM生成DDL仍需显式追加独立升级器。详见brokerage-paid-migration-registration-20260910.json，本节覆盖下方“待注册”旧状态，不与1249项或105项重叠报告相加。实际付款/退款锁序、生产权限/特权绕过门禁、PHP语义合同与正式验收仍待完成，A3k11c不勾选，仍234/170/404项；未提交推送、部署或写生产。

累计消费读端增量（2026-09-10，未提交）：已接入实际mode=3资格依赖、原用户SHARE锁后独立SUM复核及保护协议缺失/漂移时拒绝。首轮36项17通过/19错误放行，修复后原36＋写端40项通过，扩展到62项全通过（41 HTTP、19独立并发、2事务约束）；最终49文件1249项全通过、零失败/跳过，双类型通过，专用PG零残留核验后01:12:06.423 UTC确认停止，不累加重叠报告。回归中仅事务参数类型声明调整，TypeScript产出JS哈希不变，不能称源码全程不变的不可变CI；详见brokerage-paid-read-authority-20260910.json。仍待MigrationService注册、生产外键/权限及真实付款/退款锁序、特权绕过门禁、PHP资格/绑定语义合同和正式验收，A3k11c不勾选，仍234/170/404项；无提交推送、部署或生产写入。

已付订单集合写端基础（2026-09-10，未提交）：独立0150迁移及显式执行器新增按用户有序行锁的语句级触发器，最终40项全部通过（含未安装时集合仍可变化的基线缺口证明），unit/runtime双类型通过；源/报告哈希与隔离PG停止记录见brokerage-paid-write-fence-20260910.json。不与前轮1147项累加。下单读端重算、协议缺失时失败关闭、MigrationService注册及真实付款/退款锁序整合未完成，mode=3和A3k11c仍开放，清单234/170/404项。另以已授权内置浏览器只读验证PC首页→全部商品导航，发现12张商品图均为未加载的via.placeholder.com占位图；未登录、下单、改生产数据或部署。

分佣用户/等级增量（2026-09-09，未提交）：首轮63项31通过/32错误放行，现以末端有序SHARE NOWAIT保护推广关系、资格字段、实际等级权益及事业部比例，最终数据库时钟复验自然到期；缺失引用创建失败关闭。原63项修复后通过，扩展144项、角色12项及末端配送等待4项分别通过，不叠加名称过滤或重叠报告。新增108项含56独立PG、48实际HTTP和4错误分类；最终相关回归及清理见brokerage-authority-20260909.json。mode=3已付订单累计额集合、全局配置及完整正式验收仍开放，234/170/404项，不提交推送。

商品/SKU分佣增量（2026-09-09，未提交）：在原商品/SKU UPDATE保护创建时isSub和实际使用的一级/二级指定佣金，不加入客户确认价格。红测36项24通过/12错误建单，修复后36通过，扩展46通过（256项名称过滤）；含14独立PG、积分锁后重算及6种营销订单不使用规则的对照。未改变分佣/收货政策，不代表推广身份、代理等级、到期或全局配置权威已稳定。最终回归和清理证据见brokerage-item-quote-boundary-20260909.json；A3k11c仍开放，清单仍234勾选/170开放/404项。

执行顺序更新（2026-09-09，用户最新目标）：先逐个完成checklist，再提交推送。af86b95之后的改动暂留工作区；本轮不新增提交、推送或部署，已有本地提交保持不动。

会员资格增量（2026-09-09，工作区候选）：先复现18项资格/等级变化或自然到期后错误建单，另2项占用行保护不变量失败。现绑定启用功能下的有效会员布尔值/等级ID/等级权益，末端SHARE NOWAIT保护用户与等级，最终数据库时钟检查到期，保留等价续费/转终身、名称编辑和既有订单幂等重放。缺失等级引用创建失败关闭，须显式修复/重确认；未修生产数据。最终专用PG16.15回归41文件850项零失败/跳过，双类型通过；会员新文件50项含29独立PG、17实际HTTP及4错误分类，验证反向用户锁→库存等待回滚，不与旧轮累加。临时PG零残留核验后12:24:32 UTC停止。会员权益表/全局配置、赠送积分、券/集合及正式门禁仍未完成，A3k11c保持开放，仍234/170/404项，未提交推送或部署。

计价事实增量（2026-09-09，af86b95加未提交自然截止候选）：晚到基础/活动SKU价格、积分消耗与砍价活动price矩阵先复现20项错误放行，会员价/商品开关另6项；现补入现有原子UPDATE和确认投影，不改价格公式/锁序。首版过严拦截免费商品的2项反例已保留并修正，未使用VIP价和砍价活动SKU价格仍可变化。最终专用PG16.15回归35文件769项零失败/跳过，Worker双类型通过；规则文件138项，其中71项独立PG竞争，本轮新增46项，不与旧轮数量累加。证据见monetary-quote-boundary-20260909.json。用户会员/等级/自然到期、赠送积分等其它事实、券/引用集合及正式验收仍未完成，A3k11c不勾选，仍234/170/404项。临时PG零残留核对后12:01:39 UTC停止；未提交推送、写生产或部署。

活动自然截止增量（2026-09-09，基线af86b95，未提交）：新增PG迟到订单INSERT锁探针，在UTC/纽约会话下分别复现套餐、秒杀、拼团过期仍建单，共6项真实缺口；另12项仍有效/无截止对照通过，旧72项在红测命令中被显式名称筛选，不能声称红测零跳过。修复在现有规则行锁保护下，于其它可能阻塞的业务SQL之后按数据库墙钟复验时间；保留套餐截止整秒有效、拼团精确UTC截止及秒杀原排期语义，不放宽超时。最终专用PG16.15回归32文件657项零失败/跳过，含92项确认规则（本文件44项独立PG竞争）及套餐最后一秒正向对照，Worker双类型通过；不与首轮90项累加。临时PG零残留核验后11:39:06 UTC停止。A3k11c仍开放：其余计价事实、券/引用集合、等价表达、正式身份/存储及发布门禁未完成，清单仍234勾选/170开放/404项。

活动标量规则增量（2026-09-09，基线1ca3d3d，本地候选）：真实PG红测68项中52通过/16失败，证明砍价限购/截止、拼团人数/有效时长、积分限购、秒杀配送/退款在初始凭据校验后变化，旧版本仍同价建单。现将已绑定的显式规则投影放入原有活动UPDATE守卫，不绑定库存/销量/元数据、不改砍价公式或锁顺序。最终专用PG16.15扩大回归30文件622项零失败/跳过，Worker双类型通过；含72项确认规则，其中24项为本文件独立PG竞争，以及3项投影单元检查，不与原68项绿测累加。PG核验零残留后11:22:19 UTC停止。A3k11c仍开放，须处理自然截止、其余商品/SKU计价事实、券范围/引用集合、等价表达与自身CI/正式存储/发布；清单仍234勾选/170开放/404项。用户回复“授权”后，精确推送1ca3d3d到origin/main仍被安全审批拒绝，本轮未绕过；所有新候选未推送，未写生产或部署。

套餐/秒杀锁后版本增量（2026-09-09，基线96d7a71，本地候选）：4项实际HTTP晚到变更和4项PG写锁竞争先红测复现同价建单，现比较已锁定套餐完整规则/成员及秒杀父子时段投影。最终专用PG16.15扩大回归21文件518项零失败/跳过、Worker双类型通过，含48项确认规则（8项本文件独立PG竞争）；不与首轮328项重复累加。扩展首轮514通过/4失败来自父活动夹具默认停用，显式启用后通过，未改业务守卫。A3k11c仍开放：套餐墙钟截止/非协作SQL幻读、秒杀缺失引用时段新增/其余规则、其他活动及券范围最后守卫均未完成。96d7a71与本轮新改动均未推送，自身CI待验，保留main推送授权边界；没有生产写入/部署/支付，清单仍234勾选/170开放/404项。

授权推送与占券边界增量（2026-09-09，基线f58f617）：经明确授权，f58f617已推送origin/main；自身[Actions34339592124](https://github.com/cinagroup/cinashop/actions/runs/34339592124)终态失败，两单元分片分别1709通过/1失败、1553通过/1失败，均为旧测试遗漏quoteToken；五端、workerd、密钥扫描和PG目录成功，不能宣称全绿。两个失败已本地复现并对齐凭据合同，另补5项畸形/缺失凭据拒绝测试，未放宽生产接口或CI门禁。新增占券探针复现请求内部已领券门槛/失效变化仍建单；现以原子UPDATE核验已领券事实，并在取得锁后按数据库UTC墙钟复验有效期。最终专用PG16.15关联回归14文件266项零失败/跳过，Worker双类型通过，不与专项98项累加；该新增修复尚未推送、自身CI待验。A3k11c保持开放：券模板/范围关系与活动规则最后提交期重验、正式存储/身份及发布门禁未完成。仍234勾选/170开放/404项，无生产写入、部署或付款；详细证据见确认合同文档。

确认凭据提交前候选复核（2026-09-09）：最新代码在专用Windows PG16.15运行11文件206项，0失败/跳过，涵盖七类规则28项、普通/代客入口、地址/优惠券及2项独立并发；最终Worker双类型通过。隔离实例核对4个基线库和零表/schema/连接残留后停止。下面107/97/83是不同阶段与范围，不能累加。候选待自身CI，A3k11c仍开放；本轮没有执行生产写入、发布或付款。

确认规则扩展（2026-09-09）：新增红测确认优惠券门槛、套餐退款及砍价配送规则变化但同金额仍建单的3个遗漏；补规则事实后相关83项通过，进一步七类结算28项矩阵全通过，涵盖秒杀/拼团/积分/新人。限购/有效期/范围/退款等进入凭据，标题、排序及足额库存不作为报价版本。A3k11c仍开放：请求内部最后提交期的活动/券规则重验、复杂范围/等价表达/独立编辑竞争和自身CI仍待验，未将跨HTTP矩阵外推成提交期规则强一致。前轮全量启动后源与测试清单变化，只是诊断，不是不可变候选验收。仍234勾选/170开放/404项，无生产写入/部署。

确认凭据候选增量（2026-09-09，基线c811c07，未提交）：服务端不可变30分钟凭据与最终事务复核已接入普通/代客，PC/UniApp带凭据提交、明确拒绝后重取但不自动建单，超时后仍保持原幂等意图。定向5文件107项、专用PG16定向5文件97项均零失败/跳过；后者含2项四后端身份核验及精确阻塞关系的并发/等待过期回滚。PC98+图片7、Uni188+三端构建/产物3通过；两端实际Chrome共6流程通过本地SQL建单，配送26→29元首次拒绝不变更数据、二次明确提交一笔未支付订单。全量Worker仍运行，活动/套餐/优惠券完整规则版本、候选自身CI及正式环境门禁待验；A3k11c仍开放，234勾选/170开放/404项。未写生产或部署。修复前8缺口证据保留，详情见checkout-confirmation-contract.md。

地址候选自身CI终态（2026-09-09，450b1bd）：[Actions34332455890](https://github.com/cinagroup/cinashop/actions/runs/34332455890)首轮11/11成功；Linux两片173/172文件、1683/1523项，共345文件3206项零失败/跳过，原生覆盖完整互斥；workerd4文件27项、五端、密钥扫描、PG目录及汇总通过。结构源201/目标263表、共享201、源字段缺口及列漂移0，路由仍1006个可执行缺口，不是整体迁移已完成。A3k11b保留正式登录/外部存储/Hyperdrive与发布门禁；A3k11c转入下面已实测缺口，当前234勾选/170开放/404项。未写生产/执行部署，覆盖下方自身CI运行中的历史表述。

确认绑定缺口实测（2026-09-09，业务候选450b1bd）：新增隔离PGlite可执行审计 `scripts/audit-checkout-confirmation.ts`，实际确认→创建HTTP共10场景，2项正常/幂等对照通过、8项缺口复现、0网络。确认26元后改运费可建成29元、改SKU单价30元、改数量37元；同地址内容变化、换地址/自有cart、未签发或过期key携带cartIds均可新建。该脚本退出1代表未满足合同，不是已修复或通过的回归，不改现有CI门槛。A3k11c继续开放，完成标准和前端未知结果恢复要求见 `workers-ts/docs/checkout-confirmation-contract.md`。未改变A3i价格政策。

地址候选提交后复核（2026-09-09，450b1bd，已推送）：本地两个完整原生分片终态成功，173/172文件、1669/1523项，共345文件3192项零失败/跳过，覆盖完整且不重叠。第一片早于新增14项鉴权测试；最终58项地址/真实注册路由、JWT、非超级管理员数据库角色补验在专用PG16通过，不能累加重叠数量或宣称生产登录/令牌存储通过。自身[Actions34332455890](https://github.com/cinagroup/cinashop/actions/runs/34332455890)于09:08 UTC仍运行，workerd、五端、密钥扫描和PG目录成功，完整单元及汇总待验。临时PG已核对无夹具残留并停止，未写生产或执行部署。A3k11b仍开放，234勾选/170开放/404项；以下“未提交/运行中/真实权限待验”历史表述按本段限定更新。

地址候选续验（2026-09-09，未提交）：旧完整建单脚本已在独占、完整迁移的PG16数据库实际通过认领/幂等/防超卖/活动取消/定价/赠券；修复砍价参与绑定、补自有地址/地区和全部实际serial列隔离，核对public序列不变。代客显式null地址先复现后拒绝。PC桌面/UniApp手机共6条真实页面流程通过本地SQL报价与建单、地址失效/恢复重取报价、自提联系人及虚拟免地址；页面测试发现两端普通虚拟商品仍错误强制地址，已共享修正，PC96项/图片7项、UniApp187项、PC及UniApp三端构建通过。修复后Worker前端关联8文件128项全通过；完整第一分片173文件1669项零失败/跳过且原生分片匹配，第二片运行中，不提前宣称全量通过。真实权限、自身CI及发布门禁仍待验，A3k11b开放，仍234勾选/170开放/404项。未写生产或部署。详见 `workers-ts/docs/order-delivery-address-contract.md` 与同名审计JSON。

上一轮地址候选（2026-09-09，基线64a0e54）：统一地址簿/结构化代客手填解析接入报价与建单，客户端不能覆盖已选地址；实物缺地址拒绝、虚拟免物流与独立自提联系人保留，已有订单先幂等重放。修正拼接地址上限为真实订单列100字符，提交前对地址及完整地区链SHARE NOWAIT重验。新增42项服务/HTTP/代客SQL及9项独立PG并发测试；PG专项59项、相关55文件最终716项零失败/跳过，Worker双类型通过。保留首轮706通过/10失败：8项旧输入/源码断言已对齐，另2项竞争失败串行不再复现但不宣称根因解决。历史未完成事项由上条增量按范围更新，不借给后续提交。

终态补验（2026-09-09，761b787）：Actions34323812650首轮11/11全部成功；Worker两片各171文件、1620/1518项，共342文件3138项，0跳过；覆盖门禁确认分片完整且不重叠。Linux workerd 27项、五端与PG16目录全部通过。关闭TEST-004I；G/H仍保留独立候选复核缺口，不重复升级或削弱门禁。当前234勾选/170开放/404项，覆盖以下运行中记录。接续A3k11b时须兼容预览先于地址选择、无地址优惠券列表、虚拟/自提、代客手填和已建订单重试；普通配送地址不可由客户端覆盖。

自身Linux补验（2026-09-09，761b787 / [Actions34323812650](https://github.com/cinagroup/cinashop/actions/runs/34323812650)）：依赖候选与快照已分开提交推送。五端构建、密钥扫描、隔离PG16目录及workerd八项成功；Linux运行时实际4文件27项、客服17应用+11工具链均通过。两个Worker完整单元分片仍运行、最终汇总未完成，G/H/I仍开放，不宣布全绿；独立候选复核未产生结论。当前233勾选/171开放/404项，无生产SQL/部署。下方“自身Linux待验”历史记录按此范围增补，不覆盖尚未完成门禁。

快照补验（2026-09-09）：已核对Admin activity.ts调用、adminapi.ts带adminAuth的注册和实际adminBargainSkuOptions处理器，以原生成器更新admin-frontend-api-contracts.json。343调用点/363路径变体全部已注册，0未解析/未注册；所有既有调用和处理器合同逐项不变，仅增加SKU选项路径及更新行号/摘要/计数。原严格快照测试2项通过，未修改测试或生成器；新增TEST-004I保持开放至自身Linux CI通过。实际清单233勾选/171开放/404项，覆盖历史计数。

- [x] **TEST-004I 后台API审计快照同步（代码/Linux CI完成）**：cd5ac6c第二分片唯一失败来自新增砍价SKU选项接口后快照未更新。由原生成器严格重新生成，所有既有调用/处理器合同不变；761b787/Actions34323812650首轮11/11成功，原2项严格回归与完整342文件3138项单元零跳过通过。没有改动测试、生成器或注册判定，不等于业务发布。

最新依赖候选（2026-09-09，基线666b21c）：Hono固定4.13.5，Worker/Kefu Vitest及mocker固定4.1.11，保留原Vite/pool/Wrangler与安全断言。最终Worker定向52项、客服17项/工具链11项、构建和双类型通过；生产Worker与全树Kefu审计0漏洞，Worker完整开发树既有8个节点未消除。Windows workerd启动崩溃，执行0项；独立候选复核未产生结论，自身Linux CI待验，TEST-004G/H不勾选。下条历史CI现已终态failure，第二分片发现后台API快照过期（1515通过/1失败），PG目录成功；快照单独修复，不弱化门禁。当前实际计数233勾选/170开放/403项，覆盖下方旧计数；见dependency-patch-20260909.json。

自身CI增量（2026-09-09，cd5ac6c / Actions34321285989，仍在运行）：已确认有实际runner和执行步骤，先前计费阻断不能沿用。PC/Admin/Supplier/UniApp构建与密钥扫描完成成功；runtime任务和Worker第一分片在生产依赖审计失败，日志为Hono三个moderate公告；Kefu完成构建后的全依赖审计因Vitest/mocker新moderate公告失败。第二分片及PG目录仍在运行，不能宣称最终全量结果。新增TEST-004G/H两项开放，当前233勾选/170开放/403项。先核对官方公告及补丁兼容性、修复依赖门禁，再回到A3k11b；不运行audit fix --force、不降低audit门槛、不假定扫描命中等于业务可利用。该记录仅描述cd5ac6c自身CI，不借给后续提交。

最新只读复核（2026-09-09，起点8b65dcf，线上PC与候选分离）：正式`/goods/70`刷新仍为空标题/简介/轮播且显示可购买按钮，入口资源为`index-ClV6Erfd.js`；当前代码已有FE-002B字段映射、FE-002C图片状态及FE-002E规格门禁，本次构建入口为`index-CMfYW0GE.js`，不能把线上旧行为当作当前源码未实现，也不能凭资源名推断正式部署SHA。21项详情/规格/布局回归、PC92会话+7图片、类型/构建通过；隔离GET夹具下的实际构建显示标题/简介/空图提示，缺SKU禁购，390×844宽度375/375且轮播1→2。正式素材与同名商品身份裁决归DATA-006/008，FE-002B/C/D/E和REL-002/003保持开放，无新增重复清单项，仍为233勾选/168开放/401项。未取得部署ID/SHA，未登录/加购/建单/支付/写生产数据/部署。详见`workers-ts/audit/pc-live-candidate-recheck-20260909.json`与审计同名章节；下一项仍为A3k11b地址权威与完整性。

最新增量（2026-09-09，起点bee185a，模板同请求一致性）：9项真实PG红测全部复现旧问题后修复模板/费率/包邮/禁配/城市的单语句快照、所属方验证及实际建单末端SHARE NOWAIT重验，包邮不再绕过禁配。新增24项服务/SQL、12项活动/HTTP和8项独立PG测试；最终相关73文件924项零失败/跳过、离线46项和Worker双类型通过。初轮活动夹具3项失败及修正记录保留。仅勾选A3k11a；A3k11父项不关闭，缺地址入口、跨HTTP确认报价绑定、KV全局费用配置一致性新增明确子项b～d。233勾选/168开放/401项，无前端改动、生产SQL或部署。

最新增量（2026-09-09，起点26aa533，总后台运费模板原子保存）：审计发现总后台原写入口分段提交，真实PG红测6项中5项失败。现以父模板NO KEY UPDATE及同事务区域批量替换保证全成或全退，保留省略字段/特殊规则/归属；兼容供应商保存与删除锁边界，拒绝不存在/已删除模板。新增26项控制器/SQL及7项独立PG竞争，相关30文件415项零失败/跳过、离线26项和Worker双类型通过。仅关闭A3k10本地候选；模板一致报价/建单、引用生命周期、完整管理界面细分为A3k11～13开放项。232勾选/165开放/397项，无前端改动、生产SQL或部署。

最新增量（2026-09-09，起点8dc43e7，砍价自提权威准入）：选择接口、报价与建单按SQL全局开关及精确所属门店判断，绕过陈旧KV；建单保护重复配置覆盖与门店营业状态，并在最终扣库存时原子复核商品归属/类型/上下架。新增24项服务/HTTP及14项PG独立连接、离线65项和Worker双类型通过；最终相关703项702通过/1项历史建库超时，未改超时门槛的原文件单独34项复核通过，不记为单轮全绿；保留红测12失败、嵌套事务3失败的修复记录。配置表SHARE会阻塞全部配置写入至提交，5/2/5秒是语句/锁/空闲上限而非事务总时限，发布前仍须评估全局写入竞争。仅关闭A3k9本地候选；同城/模板并发、A3i与发布仍开放。231勾选/162开放/393项；无生产SQL或部署。

最新增量（2026-09-09，起点5beb266，砍价消费端配送选择）：/order/check_shipping增加精确本人购物车/活动绑定的只读选择合同，按活动规则、全局自提开关与可用所属门店筛选；显式UTC墙钟兼容旧空日期。PC/UniApp接通仅可用方式、门店列表和刷新，原方式失效时保留选择并阻止报价，须用户显式切换；错误不默认放开两种方式。修复非物流报价自动带默认地址与客户端拒绝null地址。专项PG54、离线52、相关45文件665项零失败/跳过，Worker双类型、PC92会话/7图片/构建、Uni182工具链/类型/三端构建/3产物通过；桌面/手机四组实际页面→SQL验证通过。只关闭A3k8本地候选，报价/建单的全局开关及门店归属重验、同城/模板并发、其他活动旧合并选择和发布仍开放。230勾选/162开放/392项；无生产SQL或部署。

最新增量（2026-09-09，起点bbebaae，砍价后台配送配置）：接通配送复选、包邮/固定运费/所属方模板的加载与差异保存；保持PHP枚举与非实物规范化，旧表单原值冲突拒绝，未读/未改规则不回放。模板按商品owner精确筛选且SHARE NOWAIT，沿用活动/源商品锁和有界事务。新增28项合同/HTTP/payload与5项PG独立连接，相关43文件619项、离线28项、Worker双类型/Admin构建通过。1280×900与390×844页面验证错误拒绝、保存重载、SKU重读保留编辑和同活动真实报价，console error/warn均0。只关闭A3k7本地候选；消费端配送选择、门店/同城、模板生命周期/内部并发及旧API创建缺shipping仍开放。229勾选/162开放/391项，无生产SQL或部署。

最新增量（2026-09-09，起点c9cc56c，砍价配送服务端门禁）：真实SQL在旧代码复现禁止配送仍能报价/建单、报价后运费规则改变仍提交（24项中13失败）。新增活动deliveryType校验，以及活动NO KEY UPDATE后对productId/deliveryType/freight/postage/tempId的精确重读比较；改变时要求刷新，不偷偷改价，不影响普通改名、库存变化或既有订单幂等。新增25项服务/HTTP和8项独立PG竞争通过，最终相关41文件586项、离线25项零失败/跳过及Worker双类型通过。只关闭A3k6服务端候选；配送选择接口仍读原商品，两端未按活动方式限制，后台运费/配送、全局开关/门店/同城完整规则仍开放。当前228完成/162开放/390项；无前端改动、生产SQL或部署。

最新增量（2026-09-09，起点58c425e，砍价展示内容闭环）：补齐后台title/简介/单位/8张轮播图/HTML描述，同事务保存type=2描述与活动/SKU；未提交字段保留，公开专用详情返回规范化并签名的媒体，PC和UniApp展示轮播与允许列表描述。新增27项；最终单进程31文件470项零失败/跳过、离线35项、Worker双类型、Admin/PC及UniApp三端构建、工具链176项/产物3项通过。两次并行回归各1项失败（证据行号偏移、历史建库30秒超时）均保留，不借复跑掩盖。桌面/手机共6组真实页面保存/重载/切图/描述通过；UniApp有既有Vue Router弃用warning。仅关闭A3k5本地候选，完整配送/可视化资源编辑/复制/重绑/全局库存、价格政策和发布仍开放。当前227完成/162开放/389项；无生产SQL或部署。

最新增量（2026-09-09，起点df9baff，砍价单规格保存）：按PHP后台单选原商品规格的合同，接通真实活动SKU、属性及快照的事务创建/编辑；新增认证规格读取与管理页面选择器，创建必须显式选择规格。库存/额度修改携带主表与SKU原值，保留SKU身份/销量；只改价格不覆盖不同的SKU库存。重复组合、退役、越库存、替换既有规格及商品重绑均拒绝，多规格旧活动不自动删减。新增28项，相关24文件409项及索引3文件12项零失败/跳过，PGlite21项、双类型、Admin构建及桌面/手机真实页面流程通过。首轮夹具序列冲突、跨项目类型导入及浏览器定位失败均记录并修正。只完成A3k4本地候选；旧客户端主表库存写入、完整配送/复制/重绑、价格快照政策及真实角色/发布仍开放。当前226完成/162开放/388项；无生产SQL或部署。

最新增量（2026-09-09，起点2065ec7，砍价原商品资格）：每次保存检查原商品存在/审核及非SVIP/非预售，来源归属、商品类型、表单/标签/保障/参数由锁定商品继承，忽略请求伪造值；只更新变化的派生字段，不回放商品库存或价格。活动NO KEY UPDATE后获取源商品SHARE，验证双向商品写入等待、兼容共享读取、严格超时及跨截止回滚。新增26项，相关24文件393项、PGlite专项17项、双类型通过；初轮专用端口配置遗漏导致的78项连接失败已如实记录并重验。A3k3仅本地候选，完整SKU/配送/复制/重绑和全局并发仍开放。当前225完成/162开放/387项，无生产SQL或部署。

最新增量（2026-09-09，起点dc28e34，砍价后台时间合同）：创建/编辑增加明确UTC起止时间、严格格式与顺序校验；以数据库墙钟在活动锁后和实际写后复核，并保留旧截止时间，禁止等待跨过旧截止后用延期复活。旧空日期活动必须显式配置，不自动填充。页面按浏览器时区显示、UTC保存，仅提交日期差异；错误自动滚入视野。新增31项，相关19文件355项与索引3文件12项分别零失败/跳过，双类型、Admin构建及桌面新加坡/手机纽约真实页面流程通过。仅勾选A3k2本地候选；A3k完整SKU等与发布门禁继续开放。当前224完成/162开放/386项；无生产SQL、DDL或部署。

最新增量（2026-09-09，起点fef9324，砍价后台编辑）：修复省略字段被创建默认值覆盖、销量/创建时间被重写、人数固定10等问题；保留显式改价与原有报价公式，A3i未作产品选择。实际管理页面改名只提交差异字段，显式库存/额度编辑携带两项原值并在PG活动行锁后校验，避免覆盖交易扣减/回补；停用/编辑拒绝已删除活动。新增67项、相关20文件336项零失败/跳过，Worker双类型与Admin构建通过。1280×900及390×844实际Vue/Axios→内存SQL控制器验证改名、冲突不关闭表单、刷新后成功；修复手机弹窗截断，错误内联可见，最终console error/warning均0。只完成A3k1本地候选；完整PHP规格/物流/日期编辑及商品重绑/全局锁序仍归A3k。当前223完成/162开放/385项，未写生产或部署。

最新增量（2026-09-09，起点2c269f2，砍价后台软删除）：恢复PHP仅标记is_del的删除合同，保留活动/参与/订单补偿历史；后台列表先过滤退役活动再限制100条并禁止缓存。新增20项专项、本机PG16相关17文件269项零失败/跳过，双类型通过。真实下单与删除双向等待、帮砍KEY SHARE兼容及最终回滚、取消/退款精确补偿、严格超时/设置恢复与SQL失败回滚均通过。仅完成本地候选A3j；后台保存默认值重置库存/销量/人数等风险新列A3k，价格策略仍归A3i。当前222完成/162开放/384项；不将旧全量2698项或旧CI算作当前代码验收，未写生产或部署。

最新完整unit复审（2026-09-09，起点cfb443f）：Windows本机PG16.15环境下完整314文件、2698项最终通过，零失败/跳过，原生两分片157/157完整互斥且实际执行一致；双类型通过。首轮11项失败保留并分类：修复帮砍应用时钟准入及写入后截止回滚，新增4项红→绿和2项正向时区用例；修正0155调度测试、秒杀准确拒绝断言及夹具清理预算/引用竞态。另4项初轮时间等待失败的具体根因仍未定，未放宽业务超时或把超时算成功。准确清理1个首轮合成残留库后基线4库、public表0、临时schema0，实例已停止。证据见workers-ts/audit/local-pg16-worker-unit-acceptance.json及审计末节；纯unit/显式离线PGlite不冒充PG16多连接，Windows不替代Linux/workerd/CI或生产发布。路由仍1006项可执行缺口，201表源结构覆盖完整不等于功能完成；价格政策与父项仍开放，清单221/161/382不变。以下增量保留历史范围。

最新增量（2026-09-09，本机独立PG16补验）：从已推送a8170a8建立仅监听127.0.0.1的临时Windows PG16.15，不使用生产/Hyperdrive。砍价14文件最终239通过/0跳过，包含39项独立业务竞争、两项0149锁竞争及完整建库/升级；首轮两项历史建库默认5秒超时保留记录，仅改为30秒有限集成预算后整组复跑通过。双类型通过。九路径原始目录全部263表/3701列/571约束/1010索引/227序列，差异0、六类升级同构与清理均确认；控制库public表0、fixture schema0，服务已停止、原PID/端口消失。逐文件摘要见workers-ts/audit/local-pg16-bargain-acceptance.json，复现见workers-ts/docs/local-pg16-testing.md。Windows16.15不替代Linux16.14/CI，A3f等父项和价格政策仍开放，清单221/161/382不变。

上一轮增量（2026-09-09，砍价跨参与帮砍次数本地候选）：起点`014e38470db98ade522e0cfb55f8d610b793e4ba`及[Actions34291593225](https://github.com/cinagroup/cinashop/actions/runs/34291593225)仍因GitHub计费/额度限制未启动。审计发现好友次数按helper+活动统计，但原锁仅按参与ID；新增先helper后参与的事务建议锁、READ COMMITTED与保留更严格值的5秒语句/2秒锁/5秒空闲限制，不改价格公式。新增12项实际SQL/HTTP测试和6项独立PG16候选，最终相关177项本地通过/36项PG跳过、双类型通过；本机无专用PG配置，不能宣称并发已经验证。A3i已对照PHP快照/底价合同，用户价格政策待确认；未擅自改收费规则。A3f/A3i不勾选，清单221/161/382不变，无生产连接、部署或浏览器操作。

上一轮增量（2026-09-09，UniApp砍价专用入口本地候选）：承接已推送`ff8a6b526909d9d5d697fc566bc48b00b1b9247c`，其[Actions34230301560](https://github.com/cinagroup/cinashop/actions/runs/34230301560)因计费/额度限制11项均未启动。UniApp替换通用购买组件，接入共享严格砍价目录与精确参与绑定，修复mine=1为20条分页本人列表，活动列表补分页/错误/生命周期。发起/本人帮砍重读权威状态，登录返回、换身份/迟到隔离和prepared导航重试通过。新增26项运行时用例，完整工具链176项、类型、三端构建/产物3项、i18n审计9项通过；共享合同4项真实SQL复跑通过。390×844和1280×900 H5真实SQL夹具验证登录表单/隔离认证返回、#80蓝2、失败继续结算不重加购及#90参考价4元，console error为0，保留DCloud既有弃用warning。最终轮cart4/5、累计调试5条，订单0，库存/参与不变。自身Linux/PG16、真机、真实角色和完整结算支付/生产发布仍未验，A3h不勾选。清单221/161/382不变，PHP历史复制N/A。

上一轮增量（2026-09-08，PC砍价专用入口本地候选）：承接已推送`d9abfbab90dc34e4319b4131f9a6184dd34c06e3`，其[Actions34227223180](https://github.com/cinagroup/cinashop/actions/runs/34227223180)因计费/额度限制11项均未启动。本批PC列表修复title/min_price，独立20条分页和同页重试；本人列表正确显示已砍/当前价/进度，按精确参与ID跳转新/bargain/:id详情。专用目录校验、显式活动规格/数量、登录返回、加购绑定和type2/bargainUserId结算参数接通，导航失败保留已建cart。新增25项实际Vue/Axios/Router测试及4项真实SQL→共享合同测试；PC85会话+7图片/类型/构建、Worker相关74项/双类型通过。Playwright本机1280×900和390×844真实SQL夹具验证匿名返回蓝2/#80、导航失败复用cart、切本人#90显示4元；控制台零错误、无横向溢出。尚未验证真实认证、浏览器完整确认建单支付、帮砍/取消/发起完整真实角色流程、自身Linux/PG16或生产发布，A3g不勾选。历史复制N/A，清单221/161/382不变。

本轮最新增量（2026-09-08，砍价持久参与身份业务接线候选）：已核验上一提交`bd759bf1d7f0316ac9e28184c21416a5a3c4c53e`的[Actions34224404289](https://github.com/cinagroup/cinashop/actions/runs/34224404289)因计费/额度限制未启动，11项任务runner_id均0。当前目录、加购、列表、确认、重算、建单及取消已接通精确参与身份：不同参与不合并，旧0值只允许唯一有效参与，正绑定失效不改选；bargainUserId严格表示参与ID，bargainId仅表示活动。真实HTTP/SQL两条参与分别按2元/4元建单并精确取消通过，最终19文件294项本地通过/49项独立PG16跳过，两套Worker类型通过；A3f和生产门禁不勾选。授权后使用已连接内置浏览器只读复核首页和/bargain，分别看到商品/分类及“砍价商品-测试商品A ¥99.90 可砍至 ¥59.90”；没有点击、截图、控制台、登录或购买证据，不算新候选线上验收。本轮没有生产SQL或部署。

上一轮结构增量（2026-09-08，砍价购物车持久参与字段候选）：已复核上一提交`0c368ec59cb03df8c1da84878b4d280f76752c02`及[Actions34222304743](https://github.com/cinagroup/cinashop/actions/runs/34222304743)，仍因账户付款/额度限制未启动，不能借用旧全绿。新增`store_cart.bargain_user_id INTEGER NOT NULL DEFAULT 0`与非负/仅type2可绑定CHECK，外部0149、内嵌0155、真实ORM模型和独立有界升级入口同步；旧行保持0，不自动选择或回填参与。结构测试与回归结果见审计末尾；当时业务接线未完成，现由上条候选接续，A3f仍开放。现存数据库必须先经发布门禁执行独立升级、再部署引用新字段的Worker；不得重跑含历史补写的runAll。结构增量没有生产SQL、部署或浏览器操作。

上一轮已核验状态（2026-09-08，砍价购物车报价认领候选）：上一库存锁修复已推送`1fabfde760371ecf1170a473f18ced0d6d95d9f8`；[Actions34220596985](https://github.com/cinagroup/cinashop/actions/runs/34220596985)终态failure，check 102042539749确认账户付款/消费额度限制导致未启动。没有重跑或修改计费/门禁，最近完整全绿仍为34abedc。本轮复现报价后购物车SKU/数量/活动等改变仍建单的问题，在原子认领UPDATE加入报价字段比较，失败回滚；秒杀沿用相同守卫，不新增锁或数据库字段。新增17项本地真实SQL用例通过，订单身份文件共53项通过，Worker两套类型检查通过；扩展回归见本轮详细审计。新增11项独立PG16候选仍待执行。A3f的专用参与字段、跨目录/加购/报价/建单显式选择、PC/UniApp页面与自身PG/CI均未完成；本轮没有浏览器、生产SQL或发布，不能借用旧SHA的CI/PG16证据。

此前浏览器只读记录（保留历史证据）：通过已连接的Codex内置浏览器只读打开生产首页与`/seckill`，并在加载后重新获取页面树：首页有商品与六类分类，秒杀页仍有三个“－”时段及“当前时段暂无秒杀商品”。该次没有点击、控制台、截图、移动端或购买证据；不把页面可加载或CI成功记为线上流程通过。未修改生产数据或部署。

最新验收增量（2026-09-08复核）：`9cb862dff7d202620a10eff5c2a1217ab8081e7a` / [Actions34200881817](https://github.com/cinagroup/cinashop/actions/runs/34200881817)已终态11/11成功。Worker两分片148/147文件、1221/1139项通过，共295文件2360项零跳过，完整互斥/实际文件匹配均true；客服序列过期断言已修复。真实PG16预算8项、近期/积压容量分布、Linux workerd 4文件26项（含2项扫描失败Queue重试）通过，目录、秘密扫描及五个前端成功。6fe767d的9/11历史失败保留。本轮新增分段遍历与空段continuation是新候选，不能借用9cb862d验证其行为；生产发布门禁继续开放。

当时清单计数（2026-09-08阶段）：233项勾选、168项开放、共401项。A3e～A3k分别记录砍价目录、状态、两端页面、价格策略、后台删除与编辑；A3j/A3k1～A3k10/A3k11a仅勾选本地候选，不等于整个业务或生产完成。A3k11父项仍开放：A3k11b～d明确地址权威入口、跨请求确认报价绑定和全局费用配置一致性；A3k12/13为引用生命周期及完整管理界面。拆分计数不是迁移完成百分比。G1/G2与B1的既有证据和开放发布门禁保留。

本轮浏览器只读复核（2026-09-07）：`https://cinashop-pc.pages.dev/` 的页面树包含商品和六类分类；`/seckill` 的三个时段均只显示“－”，并显示“当前时段暂无秒杀商品”。公开 `/api/seckill/index` 返回成功数组，字段为 `startTime/endTime`；PC 页面读取 `start_time/end_time` 且把后者送入列表参数，没有使用时段 ID，已证实线上字段合同不一致。空列表不能据此认定为无活动。仅取得页面树和公开接口证据，未完成点击交互、控制台、截图、移动端或购买验收，FE-003L-A3 保持开放。上一提交 `9066a4f193b3dd1e2a8afd182613ea90f0dd2d93` / [Actions 34122859491](https://github.com/cinagroup/cinashop/actions/runs/34122859491) 已终态成功、11/11 任务通过；当前未提交目录候选另通过两套 Worker 类型检查，不能借用上一 SHA 的 CI。此次没有生产数据库写入、部署或付款。

迁移进度不能用一个百分比概括：70e01ea已验证九条建库/升级路径的表、列、约束、索引和序列原始目录零差异，并将已选表元数据字段纳入失败门禁。客服游客序列外部0145、内嵌0151、独立事务入口及DB-009F五类稳定检查均完成代码和PG16隔离验收；外键子表索引审查（G）以及完整权限/函数/触发器/策略/视图审计仍未完成。HTTP合同、新系统运营数据、第三方配置和发布状态仍明显滞后；隔离验收不代表生产已经应用。

| 维度 | 当前证据 | 判定 |
|---|---:|---|
| PHP/MySQL 结构参考 | PHP 201/201 表、缺源列 0 | 结构合同完成；历史数据导入不适用 |
| 仓库目标结构 | a8170a8源码本机Windows PG16.15九路径均263表/3,701列/571约束/1,010索引/227序列；原始目录零差异、六类升级与清理确认，含0148/0149 | 新本机证据单列于local-pg16-bargain-acceptance.json，不替代历史Linux CI或生产应用；G和DB-009整体仍开放 |
| 生产目标结构 | 2026-09-04已验证基线：263表、3,696列、896索引、250主键；2026-09-12 DB-006重放列/唯一索引/微信字段扩宽已执行并独立复核，未重新统计全库总量；DB-007流水事件键及通知结构仍未应用 | DB-006已关闭；DB-007及其余结构仍为发布前置 |
| PHP HTTP 合同 | 精确匹配 881/1,904；可执行 863；其中 18 条明确不可用、17 条有证据退役 | 精确注册 46.3%，可执行 45.3%，退役后有效覆盖 45.7% |
| 旧站历史数据复制 | `deploymentMode=fresh_system`；`data_migration_run/checkpoint=0/0` | 不适用；空迁移账本符合部署口径 |
| 新系统运营数据 | 商品/订单/明细/售后为 71/29/28/3；客服账号/会话 0/0，描述/访问/分类关系 0/0/0 | 上线初始化与真实角色验收未完成 |
| Worker 单元测试 | cfb443f起点加本轮时钟修复，本机完整314文件、1414/1284项通过，共2698项零跳过；两片实际文件与原生清单一致 | 新本机证据见local-pg16-worker-unit-acceptance.json；部分显式离线夹具仍为PGlite，不替代Linux/workerd或生产验收 |
| Workers runtime | `cbf1937` Linux workerd 4文件27项通过，恢复Queue专项3项115ms | 空段continuation/发送失败重放和SQL错误重试通过；不等于生产链路验收 |
| CI | `cfb443f`/[34294284594](https://github.com/cinagroup/cinashop/actions/runs/34294284594)整体failure，11项runner_id均0，账户付款/额度未启动注释已核验 | GitHub计费/额度阻断；34abedc仍为最近全绿基线，本轮新候选自身CI待推送后检查，本机结果不替代Linux CI，未部署生产 |
| 主 Worker 发布 | 已记录生产版本为 `9f1fd655-e60f-41c1-8280-738bc85d73ef`，本轮未重查 | 本轮未发布当前代码 |
| Pages 发布 | 已记录Admin/H5来源为 `48297d2`；PC来源为空；无Supplier/Kefu项目，本轮未重查 | 本轮未发布当前代码 |

静态路由统计由 `cd workers-ts && npm run audit:routes` 生成。参数名差异会归一化，ThinkPHP `resource` 会按 `only/except` 展开，PHP 行注释和块注释会先按原长度遮蔽，通配 501 不计为覆盖。`audit/legacy-route-decisions.json` 只允许带源代码证据、原因和替代合同的退役项；退役路由仍保留在原始 PHP 分母和缺失数中，另列可执行缺口与有效覆盖，不能靠删分母粉饰进度。该统计仍只是上限：它不证明权限、响应字段、并发状态机、数据或第三方副作用等价。

### 路由合同分布

| 面 | PHP | Workers | 精确匹配 | 可执行匹配 | 明确不可用 | 原始缺失 | 已退役 | 可执行缺口 | 精确/可执行/退役后有效覆盖 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `/api` | 457 | 866 | 440 | 437 | 3 | 17 | 2 | 15 | 96.3% / 95.6% / 96.0% |
| `/adminapi` | 1,153 | 513 | 220 | 205 | 15 | 933 | 0 | 933 | 19.1% / 17.8% / 17.8% |
| `/supplierapi` | 182 | 160 | 120 | 120 | 0 | 62 | 12 | 50 | 65.9% / 65.9% / 70.6% |
| `/kefuapi` | 63 | 70 | 60 | 60 | 0 | 3 | 3 | 0 | 95.2% / 95.2% / 100% |
| `/outapi` | 41 | 41 | 41 | 41 | 0 | 0 | 0 | 0 | 100% / 100% / 100% |
| `/erpapi` | 8 | 0 | 0 | 0 | 0 | 8 | 0 | 8 | 0% / 0% / 0% |
| 合计 | 1,904 | 1,650 | 881 | 863 | 18 | 1,023 | 17 | 1,006 | 46.3% / 45.3% / 45.7% |

API-004 已将 `/api/v2` 的 16 条真实微信/小程序认证合同全部精确注册；PC/客服登录子批又把 `/api/pc` 22 条全部恢复为可执行合同，并补齐客服 `key/scan/wechat` 三条精确合同。服务端新增的 OAuth state 与 POST key 签发端点是安全扩展，不进入 PHP 匹配分子。客服游客会话、订单、聊天、上传和 WebSocket 安全拆分也已完成；`ticket/[:appid]` 与两条不安全退款合同有源证据退役。当前 `/kefuapi` 为 60/63 可执行、3 条退役、`actionableMissing=0`；逐路由清单以 `audit:routes` JSON 为准。

### 生产数据库事实

- 2026-09-04 一次性令牌Worker先只读确认生产`public`为262表、3,684列、892索引、249主键且唯一缺`admin_user_write_replay`；随后DB-005在2秒锁等待/15秒语句上限的短事务中执行`0119_admin_mobile_user_replay.sql`两遍。终态为263表、3,696列、896索引、250主键，目标表12列、4约束、4索引、序列和空表全部就绪，定义摘要两遍一致，8类相关业务表计数不变。临时Worker无令牌403、错误方法404，删除成功且URL返回404。
- `order_print_job`、`order_print_job_action`、`order_waybill_job`、`order_waybill_job_action` 已创建且行数为 0。
- `out_product_write_replay`、`out_coupon_write_replay` 均为 8 列，`out_user_write_replay` 为 9 列；三表都是 4 约束、3 索引且当前 0 行。新增 `store_order_product_coupon_reward` 为 7 列、4 索引、2 约束、0 行；有效手机号、Out 余额流水、Out 积分流水三个唯一索引均有效；该批当时仓库 223 表清单与生产集合差均为空，后续批次后的当前总览已更新为 242/242。
- 商品 71、订单 29、订单明细 28、售后 3；客服账号 0、会话 0，但客服消息历史 3。
- API-001 只读复核确认 71 个商品均为可售平台普通商品；6 个一级分类可读，精确商品分页总数为 71，销量/评分/收藏三类排行均返回真实 ID。商品描述、五类推荐关系、分类/品牌关系、预售、促销、套餐、商品券关系、DIY 页面、首页组合数据和评价回复均为空；这些接口返回空是新系统运营内容尚未初始化，不再等待旧站数据复制。
- API-002 只读复核确认订单 29（可见 28、未付 8、已付 20）、订单商品 28、售后 3；订单商品孤儿、售后孤儿、售后归属错配、累计件数快照错配均为 0。生产没有退款理由配置、订单发票、核销、配送订单或有效门店，29 单 `delivery_type` 全空，且 1 单发货状态缺物流标识；这些属于真实数据/运营配置缺口。用户订单列表在当前 29 行规模采用 0.038ms 顺序扫描、6 个共享命中且 0 磁盘读，不凭小样本臆造索引。
- API-003 只读复核确认用户 3、已激活用户 0、可见等级 3、活动用户等级 0，会员激活积分/余额/优惠券证据均为 0；9 个激活相关配置中只有 `member_func_status=1` 存在。生产真实用户上的个人中心、活动状态、推广海报、客服会话摘要与商品口令只读烟测全部通过，当前会话摘要 0 条，输出仅保留结构断言，不记录用户或消息 PII。
- API-004-CART 只读复核确认购物车 27 行，其中有效普通购物车 2 行/2 用户；失效商品、缺失 SKU、非正数/超库存和重复有效范围均为 0。71 个有效商品只有 2 个有普通 SKU，普通 SKU 共 2 行，商品属性 0 行，另 69 个商品缺 SKU，属于 DATA-006 的新目录初始化门禁。`store_cart` 只有主键索引但当前仅 24 KiB，完整列表计划 0.026ms、shared hit 1/read 0，本批不在小样本上投机加索引；随机 schema 的列表/属性/增加/换规格/合并/越权拒绝/软删及双连接同规格并发收敛 9 项断言全部通过，`public_state_unchanged=true`、临时 schema `0→0`。
- API-004-DIY 只读复核确认 `system_dise=0`、`city_area=0`，两表孤儿/自指/重复范围均为 0；8 个相关配置仅 `site_url` 存在且有 5 条历史值。真实合同按 PHP 默认返回空 DIY、关闭绑定/自提/换色/视频及商品详情/分类默认值；随机 schema 的 10 项非空合同断言全部通过，`public_state_unchanged=true`、临时 schema `0→0`。当前默认/空响应属于 DIY、城市和权威配置未复制，不能解释为内容迁移完成。
- API-004-COUPON 只读复核确认发行模板 1 条，但启用、手动领取、新人、SVIP 弹窗和当前有效模板均为 0；用户券 4 条，其中当前可用 0、运行时已过期但状态仍未更新 2、owner 孤儿 3，两张领取/商品范围证据表均为 0，`member_card_status=1` 唯一存在。真实新人/今日/可领取合同均返回兼容空结构；随机 schema 的四类范围、商品上下文、品牌页签、旧字段投影、已领有效期、样例商品、新人只读及匿名/普通/SVIP 今日门禁 11/11 通过，`public_state_unchanged=true`、临时 schema `0→0`。当前空列表是可运营模板和范围数据未复制，不能解释为优惠券业务迁移完成。
- API-004-USER 只读复核确认用户 3/活跃 3、微信身份 0，且没有任何候选微信 AppID/Secret 配置；余额账和资金流均为 0。佣金 7、提现 5、充值 6、退款 3 条全部引用不存在的用户，推广用户 1 条但无已下单推广用户，分销协议 `type=2` 为 0。真实活跃用户的余额/佣金/提现/资金/推广列表均为空，佣金轮播也因严格关联活跃用户而为空；随机 schema 的微信资料归属、三类余额账、佣金/提现/资金过滤、推广路径 type 与规则收益共 11/11 通过，`public_state_unchanged=true`、临时 schema `0→0`。这些孤儿按 DATA-007 由业务所有者裁决，微信身份/配置按 DATA-006 初始化，不再等待源库。
- API-004-PROMO 只读复核确认促销主表/辅助表均为 0，父规则、子规则、五类范围、券/赠品/SKU 孤儿也均为 0；71 个可售平台商品存在，但品牌/标签关系为 0。真实三类合同返回兼容空结构；随机 schema 对全场、指定、排除、品牌、标签范围、折扣截断、边框/背景、阶梯规则、积分/券/赠品 SKU、登录态凑单及失效门禁共 12/12 通过，`public_state_unchanged=true`、临时 schema `0→0`。隔离审计同时发现并修复商品 DAO 的 `ids` 过去只排序不筛选、会把显式范围扩散为全目录的问题；生产促销规则及范围改由 DATA-008 运营初始化后重审。
- API-004-HOME 只读复核确认可见根分类 6、可见二级分类 18，孤儿父级和自指均为 0；71 个可售商品的旧 `is_hot/is_benefit/is_best/is_new` 标记全为 0，权威 `type=3/relation_id=1..4` 首页推荐关系也全为 0。六个首页配置只有 `site_name` 存在，微信身份仍为 0；真实匿名/登录首页返回精确六字段空商品结构，匿名首页关注为 true、登录关注为 false。随机 schema 的精确根/子形状、二级分类、四类推荐、品牌/标签、预售四态、v1/v2 匿名差异与公众号身份选择共 12/12 通过，`public_state_unchanged=true`、临时 schema `0→0`；同时恢复旧 UniApp 实际调用的 v1 `subscribe`。
- API-005-PC 只读复核确认 71 个可售商品、6 个可见根分类，但 active 分类关系、PC banner 和城市均为 0；17 个 PC 候选配置只存在 `record_No/site_name/site_url` 3 个。生产还有开放购物车 2、可见订单 28、可见售后 3、商品收藏 1，但余额流水 0。真实公开合同返回商品 `count=71/page=5`，其余内容空集与当前数据一致。随机 schema 对三级 `cid/sid/tid`、分类首页、banner/公司/城市、有效/失效购物车、余额、订单/收藏/售后 UID 作用域、推荐/优品及付费会员二维码共 15/15 通过，`public_state_unchanged=true`、临时 schema `0→0`，审计 Worker 已删除且 URL 返回 404。
- CORE-004-PC-KEFU 最新生产只读复核确认用户 3/活跃 3，`wechat_user=0`、`store_service=0`，旧 `user.uniqid/store_service.uniqid` 扫码键均为 0，`codex_%` 临时 schema 为 0；所有孤儿、多用户 unionid、多客服绑定指标也为 0，但空表不能作为真实登录 E2E 证据。`wechat_open_app_id` 和历史数据库键 `wechat_open_app_secret` 均无配置行；运行时 AppSecret 已改为只接受 `WECHAT_OPEN_APP_SECRET` Worker Secret，不会回退数据库。`system_config_lookup` 及身份索引均存在；专用只读 Worker 已删除，主 Worker版本未变。
- JWT 的 HS256/`APP_KEY`/`jti` 与普通用户 `auth=md5(user.pwd)` 已恢复 PHP 合同，但会话 bucket 尚不跨运行时互通：PHP 使用可配置前缀的 `md5(token)` + PHP serialize，Worker 使用 `tb_<md5(token)>` + JSON/Upstash。正式切换必须选择全量鉴权切流并强制重新登录，或先完成双读/迁移桥；混合鉴权流量当前为 P1 阻断，不能把 JWT 可验证误写成旧会话无感兼容。
- KEFU-TOURIST 只读复核确认公开商品 71/71、客服账号/会话/反馈/游客记录均为 0，客服日志 3 条但游客日志 0；`kf_adv`、`service_feedback`、`tourist_avatar` 和全部 `config_export_*` 均为 0 行。审计只返回计数、布尔存在性、索引和结构指纹，不返回配置值或 PII，不执行 DML/DDL。临时 Worker `e7e190c3-4454-4371-8cd0-d632fdcc23b2` 已删除且 URL 404，主 Worker仍为 `9f1fd655-e60f-41c1-8280-738bc85d73ef`。
- API-006-ACTIVITY 只读复核确认秒杀/时段 `1/3`、拼团/团记录 `1/2`、砍价/参与/帮砍 `1/4/0`，活动订单 7、已支付 3。`routine_lovely`、`combination_banner` 内容和小程序 AppID/Secret、秒杀 banner、砍价订阅配置均缺失。短视频的 `video/video_comment` 两表生产完全不存在，`store_newcomer=0`；因此 marketing 13 条不能只补路由。随机 schema 的配置/banner、H5/小程序码、拼团/砍价海报、归属拒绝、分享原子计数、旧列表字段、`bargainId` 取消及两个部分索引共 14/14 通过，`public_state_unchanged=true`、临时 schema `0→0`。两个生产索引执行两遍幂等，11 组业务指纹不变；临时 Worker 已删除且 URL 返回 404。
- API-006-MARKETING-NEWCOMER 只读复核确认 `store_newcomer=0`，13 个前台新人配置全部缺失；生产真实 service 的列表/info/gift 均返回与当前关闭配置一致的安全空结构。随机 schema 对倒序/可见性、资格过期/已使用、`productValue[suk]`、基础 SKU 实时库存、可选属性、详情元数据/评价/配置、PHP 顶级订单已购数及 info/gift 差异共 10/10 通过；16 张 `public` 表指纹不变、临时 schema `0→0`，临时 Worker 删除且 URL 返回 404。生产已有三个 `store_newcomer` 索引，无需 DDL。
- API-006-MARKETING-SHORT-VIDEO 精确补齐 9 条合同；生产新增 Worker 自有兼容扩展 `video` 18 列、`video_comment` 17 列及 5 个查询索引，两表当前均 0 行。随机 schema 真实 service 对可见性/排序、推荐专属过滤、私有媒体签名、商品过滤、审核推荐、评论投影、跨视频回复拒绝、嵌套回复、删除归属、并发关系切换、评论关系和计数 12/12 通过；五张 `public` 既有业务表逐行摘要不变、临时 schema `0→0`，临时 Worker 删除且 URL 返回 404。PHP v3.1.1 安装 SQL和同版官方数据字典都没有这两张表，故不把它们伪列为 201 张源表或加入共享数据 manifest；生产没有视频行，媒体和运营内容仍未迁移。
- API-006-CHECKOUT 已对齐旧 `uniqueId/secKillId/bargainId/combinationId/storeIntegralId/newcomerId/new` 加购载荷、活动 SKU→基础 SKU 映射、秒杀/拼团单笔及累计限购、活动 SKU 权威价格/成本/运费/快照、四层库存创建/取消/退款和砍价参与记录兼容；同 UID 不同 key 的累计限购由 PostgreSQL advisory lock 串行。营销订单现静默忽略普通优惠券，非 PC 线下支付在建单前及支付入口双重拒绝；积分、分佣、商品赠券和抽奖后置规则已逐事件对齐 PHP。CHECKOUT-DATA 又完成生产只读逐行审计，并为原活动已删除的旧单补齐支付前拒绝、只恢复现存层的取消/退款兼容。生产随机 schema 的创建、支付/取消和退款整套场景通过，增强后的业务表/公共序列全行指纹不变。数据门禁仍很明确：`type=1/2/3` SKU 为 0，秒杀/拼团 `once_num=0` 且拼团 `num=0`；4 个未支付历史单尚待批准实际取消，所以主 Worker仍不能发布。
- USER-CENTER-COMPAT 生产只读复核确认地址 5/有效 5，涉及 4 个 owner、默认地址 2；多默认 owner 为 0，但有地址而无默认的 owner 为 2。地址 owner 孤儿为 4 行/3 个 distinct UID，5 行 `city_id` 均为 0，`city_area/system_city` 均为 0。用户关系/收藏/商品收藏均为 1，四列重复为 0，owner 孤儿 1、商品孤儿 0、当前收藏缺日志 1；签到 1、同一上海自然日重复组 0、owner 孤儿 1。三个域合并后孤儿 owner 共 5 个 distinct UID，域间共同 UID 为 0。商品 71，存储收藏计数 0、真实关系计数 1、漂移商品 1、最大差 1、收藏日志 0。六个目标索引已在生产幂等应用并独立复验，业务行与指纹不变；其中 `us_uid_shanghai_day_uq` 两次迁移均为 `indexCount=6/DML=false/businessRowsUnchanged=true`，精确定义读回成功。临时 Worker 已删除，主 Worker仍为旧版本且未发布。本批没有猜测修复任何孤儿、计数或日志。
- USER-CENTER-COMPAT 生产随机 schema 直接运行真实 service，地址 3/3、收藏 5/5、签到 5/5，共 13/13 通过；新增同一上海自然日不同秒的数据库唯一性断言，Worker 捕获 SQLSTATE `23505` 并稳定转换为“今日已签到”。每个顶层事务显式执行 `SET LOCAL search_path`，13 张 `public` 表的全行指纹前后不变，临时 schema 计数不变且最终删除，临时 Worker 已确认不存在。首轮因 Hyperdrive 未可靠保留 startup `search_path` 而失败，但安全清理且 `public` 无变化；修正 harness 后完整通过。
- API-007-DIY-HOME-WIDGETS 生产只读复核确认 24 张主依赖表与 2 张装饰链支持表全部存在，临时 schema 为 0；`system_dise=0`，因此默认 DIY、悬浮窗和版本均没有真实内容。原 21 个配置键只存在 6 个：`site_url/site_name/member_card_status/sign_give_point/member_func_status/sign_status`，其中 `site_url/sign_give_point/sign_status` 有重复历史；本批再扩展审计 PHP `getImageConfig()` 的 20 个图片键，`image_thumb_status/image_watermark_status`、大/中/小图尺寸及全部水印字段在生产均为 0 行。审计没有返回任何配置原值。用户 3/活跃 3、等级 3；用户券 4、当前可用 0、运行时已过期但状态仍未更新 2、券 owner 孤儿 3；用户关系 1 且 owner 孤儿 1，商品收藏计数漂移 1；签到 1 且 owner 孤儿 1，连续奖励规则 0；视频 0、新人商品 0、促销 0、有效 VIP 价格权益 0，71 个商品仍可返回三类各 3 条排行。真实 service 的八类响应结构全部通过，但空内容是 DATA 缺口，不是迁移完成。
- API-007-DIY-BUSINESS-WIDGETS 公开生产探针确认秒杀/拼团/砍价/积分路由仍返回旧 Worker 的 camelCase/错误秒杀数组合同；`presale/list`、`wechat/live`、`v2/coupons` 在已部署主 Worker 均为 404。候选代码已恢复上海时区秒杀信封、四类旧字段投影、有界分页和八类组件，但主 Worker/Pages 未发布，线上行为没有改变。
- 同一临时 Worker 使用前向外部 `0106`（原 `0105` 保持不变）将 `ur_uid_rel_type_cat_idx` 从旧全局四列唯一安全升级为 `type <> 'play'` 的部分唯一，非播放关系继续幂等、播放保持 append-only；USER-CENTER 六索引迁移在同一事务执行两遍，地址/关系/签到行数为 `5/1/1`，精确定义回读成功、DML=false、三张输入表全部列指纹不变。随后随机 schema 直接运行 8 个真实兼容方法，DIY/用户/视频/新人/排行/签到/悬浮窗共 28 项断言全部通过；24+2 张表和 25 条 public 序列前后指纹一致、25 个 identity/serial 绑定已隔离重建、外部序列依赖 0、临时 schema 删除。一次断言把显式 ID 回退误当无 ID请求、一次 fixture 受数据库会话时区影响，均在 public 指纹不变且 schema 清理后修正 harness；最终把 `pg_temp` 显式置后并固定 `SET LOCAL TIME ZONE 'UTC'` 后通过。临时 Worker及 Secret 已删除，URL 返回 404，主 Worker版本没有改变。
- API-007-PRODUCT-REPLY-DETAIL 生产只读复核确认 7/7 依赖表存在、临时 schema 为 0。评价 2 条且均可见，负点赞/负浏览、商品孤儿和评价点赞计数漂移均为 0；但两条评价的 owner 均不存在。评价回复和评价/回复点赞关系均为 0，没有生产非空交互样本；4 个详情展示配置键只存在 3 个，结合既有配置审计可知缺 `site_logo_square`。随机 schema 直接运行真实 `ReplyService`，详情形状、浏览量、可见性、列表、发布、内容边界、点赞幂等/并发、取消幂等、触发器回滚、search path 与 public 指纹共 12/12 通过，临时 schema `0→0`；临时 Worker/Secret 已删除，主 Worker仍为旧版本。
- 数据迁移控制表存在且运行/检查点均为 0；全新系统不承接旧站历史数据，这个空账本是预期状态，不再以 `SOURCE_MYSQL_URL` 是否存在作为发布门禁。
- `system_config` 有 6 个重复键、20 条额外历史行；其中 `site_url` 曾同时出现示例值和实际 Pages 值，不能自动删除。

### Cloudflare 资源与配置事实

- 已存在并匹配仓库配置：Hyperdrive、`cinashop-api-CONFIG_KV`、`cinashop-assets` R2、`cinashop-order`、`cinashop-order-dlq`、`cinashop-order-dlq-unarchived`。
- 2026-08-31 只读复核确认 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 名为 `cinashop-pg`、origin connection limit 60、缓存开启；主队列 ID `7c5d03145eb541cbbb3695fad3925d70`，实际消费者仍是 `cinashop-api`、batch 10、2 秒、3 次重试并转入 `cinashop-order-dlq`。
- 严重上线缺口：`cinashop-order-dlq`（ID `886e026a32c74359b2b40407f4def8d6`）当前消费者为 0；100% 生产流量仍是 2026-08-09 版本 `9f1fd655-e60f-41c1-8280-738bc85d73ef`，其实际 bindings 没有 `ASSETS_BUCKET`，也没有 `ORDER_DLQ_NAME`。仓库里的 DLQ 归档/R2 代码尚未发布，不能按“生产已启用”计算。
- 主 Worker 当前只有 6 个 Secret 名：`APP_KEY`、`DEBUG`、`INTERNAL_CHAT_TOKEN`、`OPERATIONS_TOKEN`、`UPSTASH_REDIS_URL`、`UPSTASH_REDIS_TOKEN`。
- 支付、短信、Turnstile、电子面单和 `WECHAT_OPEN_APP_SECRET` 当前未配置；即使代码存在也不能投入生产。

## 执行规则

- `[x]` 只表示已获得可重复证据；源码中“有接口/有页面”不等于完成。
- 每个写状态机必须具备权限边界、幂等、并发锁、失败恢复、脱敏审计和 PostgreSQL 隔离场景。
- 每批先完成单元/类型/构建，必要时再使用随机 schema 连接生产 Hyperdrive；禁止把合成数据直接播种到 `public`。
- 生产 DDL 必须短事务、固定 `search_path=public`、设置 `lock_timeout/statement_timeout`，执行前后核对表与业务行指纹，并验证二次执行幂等。
- 主 Worker/Pages 正式发布仍需单独明确批准；本 checklist 不把提交推送解释为发布授权。
- 外部账号、生产凭据或业务取舍缺失时标记 `BLOCKED`，不得用假数据把项目标为完成；已经明确“不承接旧站历史数据”的项目不能继续把源 MySQL列为阻塞。

## P0：生产结构与真实数据

- [x] **AUD-001 可重复路由审计器**：`workers-ts/scripts/route-parity-audit.ts` 与 `npm run audit:routes` 当前验收为 Kefu 60/63 可执行、3 条退役且 `actionableMissing=0`，Out 41/41、ERP 8/0；有证据退役清单同时输出原始缺失、退役和可执行缺口，清单漂移、重复或与 TS 注册冲突时直接失败。PHP 注释遮蔽修复排除了 8 条已经注释的伪合同：API 点赞 2、会员任务 1，Admin 文件读写 3，Supplier 核销 2。
- [x] **AUD-002 生产只读目录审计**：通过一次性认证 Worker 读取表名、目录计数、迁移控制和非敏感业务计数；三次临时 Worker 均已删除，生产无写入。
- [x] **AUD-003 新系统口径一致性门禁**：当前部署明确不承接旧 PHP 历史数据；`data-migration-scope.test.ts` 同时约束本清单和 `workers-ts/README.md` 的开放项，不得重新把源 MySQL、`SOURCE_MYSQL_URL` 或旧站复制列为完成阻塞。历史章节和通用迁移工具继续保留，但必须显式标注为其他部署用途。
- [x] **DB-001 创建小票任务账本表**：已应用外部 `0090_print_job_outbox.sql`（Worker 内嵌 `migration_0097`），创建 `order_print_job` 与 `order_print_job_action`；二次执行只返回 `already exists, skipping`，六张业务表指纹不变。生产引擎随机 schema 场景确认自动/手工幂等、租户隔离、Queue 脱敏、并发单次调用、UNKNOWN 不盲重试与三类人工处置全部通过，临时 schema/Worker 已删除。
- [x] **DB-002 创建电子面单任务账本表**：已应用外部 `0091_electronic_waybill_outbox.sql`（Worker 内嵌 `migration_0098`），创建 `order_waybill_job` 与 `order_waybill_job_action`；二次执行幂等，六张业务表指纹不变，最终四张任务表均为空。生产引擎随机 schema 场景确认请求重放、根单活跃任务、租户隔离、Queue 脱敏、提供商未知/拒绝/本地失败、人工处置与履约精确一次全部通过。
- [ ] **DB-003 清理重复系统配置（只剩1项owner确认）**：生产只读审计已精确导出6组/26行/20条冗余的ID、优先级、状态和值摘要，外键引用为0；5组值完全相同，`site_url`当前运行时按`sort DESC,id DESC`选中启用的ID 389=`https://cinashop-pc.pages.dev`，其余4行均为停用的`https://cinashop.example.com`占位值。机器可读证据见`workers-ts/audit/system-config-duplicate-baseline.json`；临时Worker已删除并复验404。待项目所有者确认保留ID 389后，再以精确ID短事务删除20行并复验非目标配置及序列不变。
- [x] **DB-004 系统配置精确查询索引**：已应用外部 `0103_system_config_lookup.sql`（Worker 内嵌 `migration_0110`），创建 `(is_store, menu_name, sort DESC, id DESC)`；生产短事务固定 `search_path=public`、5 秒锁超时和 30 秒语句超时，连续两次执行均保持 48 行和同一结构指纹，索引定义精确读回，无 DML 或配置值输出。
- [x] **DB-005 应用 Admin 用户写入重放账本**：生产预检确认表/序列均不存在；一次性令牌Worker在2秒锁等待、15秒语句和20秒空闲事务上限内执行`0119_admin_mobile_user_replay.sql`两遍，生产目录`262/3,684/892/249→263/3,696/896/250`（表/列/索引/主键）。目标表12列、4约束（含主键）、4索引（含主键）、序列、空表及两遍定义摘要全部通过，8类相关业务表计数不变；无令牌403、错误方法404，临时Worker删除后URL 404。
- [x] **DB-006 提现重放结构发布前置**：2026-09-12经明确授权，在正式Hyperdrive执行固定外部`0130`合同（内嵌`0134`同义）：新增`user_extract.request_key/request_hash`、`(uid,request_key) WHERE request_key<>''`唯一索引，并把`wechat`从15扩到64字符；无业务DML。短事务首次applied=true，独立只读事务确认精确目录，第二次applied=false；既有5行业务规范化SHA-256各阶段一致。临时维护Worker已删除且控制面/公开端点404，主Worker未变。完整候选CI为cb5b8ad/Actions34470894844成功。证据`workers-ts/audit/withdrawal-replay-production-20260912.json`；不据此关闭DB-007或应用发布。
- [x] **DB-007 提现流水/通知结构发布前置**：2026-09-12按明确授权执行外部0131–0133最终合同。新增capital_flow.event_key唯一围栏、投递withdrawal_id/索引和互斥约束、最终9项通知事件白名单及客服部分索引。四表合计1行业务规范化SHA-256各阶段一致，独立只读目录复核ready=true，第二次applied=false。无历史复制/业务DML/provider调用，临时Worker已删除且控制面/公开端点404。证据`workers-ts/audit/withdrawal-effects-production-20260912.json`。仅关闭结构前置，不等于提现真实角色/模板送达或正式生产验收。
- [x] **DB-008 Drizzle完整SQL生成门禁（生成/兼容/例外范围通过Linux CI）**：四组索引重名按现有迁移修正；补导出客服访客专用序列，起始1,000,000,000/上限2,147,483,647且不循环。保留部署合同中的独立唯一索引，拒绝会破坏已有库升级的唯一索引→约束转换；由限定drizzle-kit0.31.10及三个入口SHA-256的postinstall补丁，将PG新建外键排到引用键创建之后。回归覆盖补丁幂等/源漂移拒绝、真实CLI配置/完整DDL/重复生成/导出、0115/0116原始闭合结构校验、CJS和ESM API已有表加索引或约束再加外键、跨企业拒绝/级联删除及全模型快照无差异；DB-009B后生成模型为263表。子进程禁止TCP/UDP/监听及可选tsx IPC；生产安装省略dev依赖时跳过补丁。用户明确授权后，两个公开源码hash的精确路径/规则/完整行AND例外及范围回归提交为`7ae3544`，[Actions 33967084785](https://github.com/cinagroup/cinashop/actions/runs/33967084785)8/8通过，包含1,520单元/24runtime及231提交全历史扫描无泄露。早期7/8失败保留在审计历史。没有升级依赖或部署生产；生成DDL不等于部署结构已完全一致，差异继续归DB-009。
- [ ] **DB-009 ORM与部署DDL完整一致性**：首次PG16真实目录基线否定了“SQL文本零漂移即可证明等价”。70e01ea/Actions34037834052验证九条隔离路径五类原始目录零差异，E及F均完成代码/CI；F现会拒绝已选8字段的表级差异，而非仅输出报告。G的父端检查索引审查仍开放。完整权限/函数/触发器/RLS策略/视图不属于当前比较器的完整等价范围，这些修复未应用生产，不得以隔离目录一致替代完整迁移和发布验收。
  - [x] **DB-009A 三路径真实目录审计（发现模式已通过PG16 CI）**：`b07d01e`的[Actions 33964618455](https://github.com/cinagroup/cinashop/actions/runs/33964618455)实际执行135个外部SQL、138步MigrationService、1,041条ORM生成语句，三座随机测试数据库均确认删除。完整463条结构记录保存在`workers-ts/audit/orm-ddl-catalog-baseline.json`，逐类别条数与摘要已核对，无截断。只允许专用loopback测试身份，不消费生产URL；比较器8/8及Worker/五端通过，总CI仍因未批准的扫描例外为7/8。发现命令成功不等于定义一致；PG18适配试改已全部撤回。
  - [x] **DB-009B 清除无消费者的phantom拼团声明（代码/PG16 CI完成）**：PHP StorePink模型/安装SQL、当前ActivityJoin/PinkLifecycle/PinkTimeout均只使用`store_pink`；全源搜索中`storePinkFull`只有声明。移除该未使用声明及其两个phantom索引，保留既有storePink全部业务字段，并增加生成回归；不删除任何数据库表、不添加生产表。`731e203`真实ORM表/列/序列数量已与两条迁移路径对齐。
  - [x] **DB-009C 四处列宽对齐（代码/PG16 CI完成，未应用生产）**：新增外部0134/内嵌0138，按现有Worker/ORM合同把user.add_ip、user.last_ip、store_order.user_ip由16扩为45，store_product_category.pic由128扩为512。只接受已知旧/目标varchar且NOT NULL的普通表列；未知、更宽、可空、缺失结构拒绝，事务回滚，不缩短或截断数据。5项回归及PG16三路径验证通过，外部↔内嵌列差异4→0；生产需按真实catalog确认是否需要执行。
  - [x] **DB-009D1 补Worker漏注册的Admin索引（代码/PG16 CI完成，未应用生产）**：外部0127的4个文章索引、0128的5个表单引用索引从未接入MigrationService；现以逐字一致的内嵌常量注册0139/0140，2项回归及真实MigrationService全链通过。内嵌索引902→911，这9项已从差异中消失。
  - [x] **DB-009D2 其余索引差异逐项判定（代码/PG16 CI完成，未应用生产）**：D2a补齐47个基础索引，D2b1补齐22个ORM声明，D2b2修正57个同名定义，D2b3a/b补迁移48项并移除2个重复ORM普通声明，D2b3c/d保留对象身份统一44普通和3约束附属名称，D2b3e严格依赖守卫退役5个外部重复。cc2e5db/34010180395确认四路径1,006索引全量精确一致，180来源合同通过；其余目录差异未豁免。隔离验证不构成生产删除、改名或发布许可。
    - [x] **DB-009D2a 补齐47个基础索引（代码/PG16 CI完成，未应用生产）**：外部0135/内嵌0141按真实catalog严格验证同名索引，仅缺失时创建；未知结构/已有重复充值订单号拒绝并回滚，不删改行。ORM同步恢复PHP/外部要求的ur_order_id_idx，原应用重复回调对象拒绝仍保留。14项专项覆盖精确来源、47定义/旧对象/数据保留、幂等、重复号拒绝、8类同名异构、FK conindid/pg_depend不变及非public/temp隔离。4fe0530的33968639077为8/8；PG16实际内嵌索引911→958，差异52→5且每项均有预期同构别名，ORM充值唯一索引缺口也归零。52项来源/决策见audit/core-index-reconciliation.json；5个别名不补重复索引也不DROP，生产仍须按实际catalog和写入窗口决定增量DDL。
    - [x] **DB-009D2b ORM其余索引与别名收口（代码/PG16 CI完成，未应用生产）**：22缺失声明、57同名定义、50非别名额外对象、47对普通/约束附属别名及5个外部重复均按子项处置。cc2e5db/34010180395的外部↔ORM与外部↔内嵌索引reference-only/candidate-only/changed/possibleRenames全部0，并由四路径全量定义门禁防回归；不是数量相等或同构别名豁免。
      - [x] **DB-009D2b1 补齐22个ORM查询/唯一索引（代码/PG16 CI完成，未应用生产）**：10个模型文件补21个普通索引和独立supplier_admin_id_uq唯一索引，其中8个部分索引；来源/原始SQL/PG16完整目录合同固定在audit/orm-query-index-reconciliation.json。保留既有普通索引和别名，不改部署迁移、不执行DROP。6个降序索引的8处列显式nullsFirst，避免Drizzle默认NULLS LAST偏离原始DESC合同。真实CJS/ESM增量仅22条CREATE INDEX，验证重复admin_id拒绝及事务回滚、旧行/旧索引不变、精确目录和重复生成no-op；新增3项门禁回归，两条候选路径今后任一已收口定义漂移均失败。404baf3的33970859482为8/8、237文件1,537项无跳过；PG16实际ORM索引986→1,008，reference-only74→52、无candidate22→0，两条路径22个精确合同通过且测试库已清理。其他差异保持输出，生产未连接或应用。
      - [x] **DB-009D2b2 57项同名索引定义差异（代码/PG16 CI完成，未应用生产）**：22个模型文件修正38表的24项空值排序、30项缺DESC、3项缺WHERE，共75处降序列明确nullsFirst。57项来源/前后目录/旧真实快照绑定29个外部文件，见audit/orm-index-definition-reconciliation.json；名称、键列及部署迁移未改。58ac3ce/34002410416为8/8、1,540单元无跳过；PG16实际同名变化57→0，79合同全部通过。第四座随机库实际执行旧模型1,061条DDL和精确57组DROP/CREATE，回滚恢复OID、6行夹具及非目标目录/OID/外键conindid/依赖不变，升级后与新建ORM完整已覆盖目录一致；四座测试库确认清理。这不是生产升级许可，不执行线上db:push或DDL。
      - [x] **DB-009D2b3 非别名、别名与外部重复收口（代码/PG16 CI完成，未应用生产）**：a～e均有精确来源、真实引擎、依赖/回滚证据。cc2e5db/34010180395只再移除两个比较面各5条reference-only，共10条原始差异，其余每条完全不变；索引缺5→0，其他索引差异仍0。四路径索引全量对齐、新建与升级后ORM已覆盖目录一致；生产仍需实际catalog和单独发布授权。
        - [x] **DB-009D2b3a 恢复28个有PHP源定义依据的查询索引（代码/PG16 CI完成，未应用生产）**：50个非别名对象全部绑定安装SQL的201表/288条KEY快照，28个具有完整键序、非唯一且无前缀的源声明，另22个保持review-orm-only。外部0136/内嵌0142只补缺失并拒绝同名异构，不改ORM、不删除别名或业务行。2ef0d33/34003919501为8/8，239文件1,544单元全部通过无跳过；PG16实际外部963→991、内嵌958→986、ORM仍1,008，107项精确合同通过。CJS/ESM及PG16均验证28恢复、11类拒绝回滚、重复行及非目标OID/FK依赖保留、幂等和非public/temp隔离；四座随机库确认清理，新建与升级后ORM已覆盖目录一致。父项及生产前置继续开放。
        - [x] **DB-009D2b3b 其余22个普通索引（代码/PG16 CI完成，未应用生产）**：完整查询/旧目录见audit/orm-ordinary-index-reconciliation.json。外部0137/内嵌0143补20项，两个ORM文件只移除so_order_id/supplier_admin_id普通声明，保留原独立唯一索引。86cb73a/34005185002为8/8，240文件1,549单元全部通过无跳过；PG16实际外部991→1,011、内嵌986→1,006、两条ORM1,008→1,006，128正向合同和2退役名禁回归通过，候选额外69→47且其余原始差异完整不变。CJS/ESM及PG16均证明20项恢复/11类拒绝、两项完整底层定义匹配/真实依赖RESTRICT拒绝/故障回滚/4行夹具和非目标OID/FK/唯一性/等值索引访问保留/no-op；四库确认清理。生产未连接或删除。
        - [x] **DB-009D2b3c 44个普通索引物理名称一致化（代码/PG16 CI完成，未应用生产）**：25表/10模型文件只变名称，44对完整目录/旧快照及45处原始建索引行绑定audit/orm-index-name-reconciliation.json。外部0138/内嵌0144只接受已审旧名或新名且定义精确匹配，保留OID的ALTER INDEX RENAME；并存/皆无/异构拒绝。d416f31/34007245605为8/8，241文件1,554单元无跳过；PG16实际8缺/3多，172正向合同+2退役名+44旧名禁回归通过。CJS/ESM和PG16均证明44个依赖视图、索引OID/文件/pg_depend及6行夹具保留，45次破坏性建议拒绝、12类漂移回滚、首条ALTER故障回滚、混合状态/幂等/非public与temp隔离；新建/升级后ORM已覆盖目录一致，四库确认清理。生成器88条仅检查与拒绝测试，不当作已执行升级；生产未连接。
        - [x] **DB-009D2b3d 3个约束附属索引名称（代码/PG16 CI完成，未应用生产）**：data_migration_checkpoint复合主键、kefu_visitor_session的token_hash/visitor_uid唯一约束在两个模型显式采用原部署名称，完整目录/源行/旧快照见orm-constraint-name-reconciliation.json。外部0139/内嵌0145只接受已审旧/新约束与索引整对，严格校验后RENAME CONSTRAINT；不重建、不改唯一性。b0c15fc/34008849823为8/8，242文件1,560项无跳过；175索引+3约束合同及旧名禁回归通过。CJS/ESM和PG16证明3个FK/3个视图、约束与索引OID/文件标识/依赖/触发器及5父行+1子行保留；7次依赖拒绝、12类漂移及首改名故障回滚、唯一/FK/新错误名称、更新/删除级联、混合状态/幂等/非public与temp隔离通过；新建/升级目录一致，四库确认清理。
        - [x] **DB-009D2b3e 5个外部重复物理对象（代码/PG16 CI完成，未应用生产）**：external-duplicate-index-reconciliation.json绑定0003/0004/0005初始CREATE、0108/0082/0085保留CREATE及未改模型/查询消费者。外部0140/逐字内嵌0146仅在完整定义/物理语义一致且旧对象无已检查依赖时RESTRICT移除；旧唯一wu_openid_uq_idx被FK引用则拒绝，保留wu_openid_uq及其FK。cc2e5db/34010180395为8/8、243文件1,565单元无跳过；PG16完整外部历史实际验证34次依赖/漂移拒绝、故障回滚、6行夹具、保留OID/relfilenode/FK/触发器/唯一/级联、混合状态/temp隔离/幂等，外部索引1,011→1,006且其余对象不变。180来源合同及四路径全量索引门禁通过，四库确认清理。无生产连接、删除或部署。
  - [x] **DB-009E ORM约束/默认/序列差异（代码/PG16 CI完成，未应用生产）**：3个主键/唯一约束别名由D2b3d统一，四处默认值、41项缺失约束、12组外键名称及9 CHECK由E1～E4完成；E5的模型、外部0145、独立事务入口和内嵌0151现全部接入并验收。7d14b88/34036234197九路径全部570约束和227序列精确相等，五类原始目录差异0。历史基线及f3cf196/c9de509分阶段证明保持不变，最新登记证明见根JSON的sequenceEmbeddedRegistrationVerification；catalog_equivalent_candidate只表示隔离目录候选，非整体业务/生产完成。F/G及DB-009仍开放，不在生产自动VALIDATE、重置或强压序列类型。
    - [x] **DB-009E1 四处默认值（代码/PG16 CI完成，未应用生产）**：四模型只调整默认表达式，完整来源/旧新快照见orm-column-default-reconciliation.json；外部0141/内嵌0147只接受已审旧/目标默认及精确列形状，未知表达式不执行，不改类型/精度/非空或既有内容。9af7fb6/34011673361为8/8、244文件1,572单元无跳过；PG16完整旧默认路径1,059初始SQL+1守卫DO通过14类拒绝、8行/4视图/4外键/OID/文件/非目标默认保留、首ALTER故障回滚、生成器4条DDL同义、幂等/混合/schema/temp隔离。五路径均实际验证省略/DEFAULT/显式/SQL NULL写入、JSON null区别、金额舍入/边界和长text；全部3,700列及1,006索引精确一致，五库确认清理。仅消除4条默认值原始差异，其余逐条不变。
    - [x] **DB-009E2 41个非别名缺失约束（代码/PG16 CI完成，未应用生产）**：39个CHECK和2个FK；FK为spr_order_cart_info_fk（NOT VALID）及wco_event_id_fk（ON DELETE CASCADE）。其中8项未验证（7 CHECK/1 FK），已保持新增写入约束与既有行验证状态的区别；六路径精确合同及完整旧模型受守卫升级均通过，不通过删外部约束收口。
      - [x] **DB-009E2A NOT VALID生成前置（代码/PG16 CI完成，未应用生产）**：1641e65/34013882048为8/8、245文件1,577单元无跳过。局部CHECK/FK标记经固定版本CLI/CJS/ESM持久化；新建CHECK延后ALTER，FK保持唯一索引之后并显式保留public引用。两座独立PG16.14库各验证12条初始SQL/5条新增、4旧坏行及原对象保留、5类VALIDATE拒绝、8种非法元数据拒绝、精确新写入/NULL/级联/回滚/无变化；均确认清理。不支持的push/pull只读拒绝。该子批五路径及完整88条目录记录、输入和生成SQL摘要与9af7fb6逐条完全一致，原90秒探针边界不变。本项只建立生成前置，业务约束由后续E2B接入，不自动应用DROP/ADD或VALIDATE；上述独立生成器证明在70103a0仍逐字段相同。
      - [x] **DB-009E2B 41项模型及受守卫前向升级（代码/PG16 CI完成，未应用生产）**：70103a0/34017020788最终8/8，第二轮247文件1,584单元无跳过，首轮原生进程故障保留TEST-005。39 CHECK/2 FK含8项NOT VALID的来源/模型/快照固定于orm-missing-constraint-reconciliation.json；0142/内嵌0148只补缺失、拒绝未知定义/验证状态/引用键漂移，无DROP/VALIDATE或业务DML。真实PG16完整旧ORM验证33旧坏行拒绝、8未验证状态、99类元数据拒绝、41组INSERT/UPDATE、16边界及依赖/OID/原行/回滚/幂等/schema隔离。8条跨事务已提交旧行保留，新坏INSERT/变更坏值UPDATE/VALIDATE各8次拒绝、8合法行成功；旧孤立FK值不变时更新其他字段允许。六路径均263表/3,700列/570约束/1,006索引/227序列，全部列/索引及41项完整合同通过，新建与各升级目录一致。原索引/默认升级证明保留，六库及两座独立生成器库确认清理；恰移除41条原始缺失记录，其余46条逐条不变。未连接或改动生产；2秒锁等待不等于无锁上线，校验型新增扫描旧行，生产仍需单独授权与维护计划。
    - [x] **DB-009E3 十二组外键名称（代码/PG16 CI完成，未应用生产）**：六模型显式命名12个FK，列/default/非空/索引和级联规则不变；来源见orm-foreign-key-name-reconciliation.json。外部0143/内嵌0149只RENAME，按序锁17表并核对19字段、父PK/索引、相等运算符及4个不同角色RI触发器；全量预检，未知/并存/缺失/重复别名拒绝。eebe827/Actions34020575416首轮8/8、248文件1,589单元无跳过。真实PG16旧ORM1,068条SQL+1守卫通过141类漂移拒绝、12非法INSERT/UPDATE及父更新、11 RESTRICT/1 CASCADE/2可空合同，保留原约束/触发器OID、行/索引/文件/依赖，验证首改名故障回滚、幂等和schema/temp隔离。9个长名称由引擎正确截到63字节；12组原始DROP/ADD实测替换约束及4触发器身份，只在回滚夹具中执行。七路径全量列/索引及41+12约束合同通过，新建与完整旧模型升级目录相同；七库及两座生成器库确认清理。恰移除12缺失/12额外/12别名记录，其他10条原始差异和既有升级证明逐字段不变。生产未连接或改动；2秒锁等待不等于无锁上线。
      - 首次9fba575/Actions34020071977在类型检查阶段因约2GiB默认V8堆耗尽退出134，七个其他任务通过，单元和PG16未执行。两条检查入口显式设置4,096MiB后，本机与新提交eebe827双配置和完整CI通过；未改检查范围、依赖、并发或时限，不重跑旧SHA，也不把此编译器故障混淆为TEST-005原生崩溃。
    - [x] **DB-009E4 九处同名约束变化（代码/PG16 CI完成，未应用生产）**：8数值CHECK保留写入谓词并对齐NOT VALID，outbox仍已验证且9个允许事件不变，仅调整原始顺序。外部0144/内嵌0150逐字镜像，6表排序锁、16字段/全部9项预检及依赖/注释守卫后同名RESTRICT替换；只接受精确旧或目标状态。194be1c/Actions34024902767首轮10/10、250文件1,596单元无跳过；PG16旧ORM1068 SQL+1守卫通过55漂移拒绝、58非法/70边界/32非空INSERT/UPDATE对、8已提交坏旧行保留、新坏写入/VALIDATE拒绝、全量预检/故障回滚/no-op/mixed/temp/非public隔离。明确9目标CHECK OID及依赖objid改变，注释与非目标对象/行/文件/索引/触发器/函数保留；双连接2,005ms锁拒绝及事务未结束时写入阻塞/回滚通过。8494360首轮PG16后置条件失败保留记录，194仅排除内部conbin字符位置并验证7类语义变动不得隐藏，PG16恰8项location-only；原始SQL目录无规范化。八路径全部570约束/3,700列/1,006索引零差异，八库及两生成器库确认清理，恰消除9条原始CHECK差异。无生产操作；outbox已验证ADD仍扫描旧行并持ACCESS EXCLUSIVE，不承诺在线零锁。
    - [x] **DB-009E5 客服游客序列（代码/PG16 CI完成，未应用生产）**：外部integer/拥有列与旧ORM bigint/无拥有关系的差异已由E5A生成器、E5B模型及受守卫升级收口。7d14b88/34036234197追加内嵌0151并通过真实152步新建库和独立旧ORM升级验证；无DROP/RESTART/setval重置。AS会更换目标relfilenode，OWNED BY使删拥有列带走序列，生产应用仍须按实际目录和维护窗口单独审阅。本轮最长单元任务697秒、余503秒，历史保守余量490秒；原上限、并发和断言不变。
      - [x] **DB-009E5A 类型化序列生成前置（代码/PG16 CI完成，未应用生产）**：ce5805a/Actions34028634339首轮10/10，251文件1,603单元全部通过且无跳过。固定版本/三个入口校验的AS与OWNED BY/NONE持久化生成保留原外键排序和NOT VALID完整窗口；CLI快照/导出/幂等及CJS/ESM真实PG16四序列夹具通过，每格式13条新建/9条升级、22非法状态拒绝及20不安全Number拒绝，编号/OID/注释/旧行/回滚/依赖删除/不循环/倒序默认/耗尽/只读introspection全部通过。整份原八路径两条raw记录、全部旧证明与SQL hash同194be1c逐字段不变；八座业务库、两座NOT VALID库和两座新序列库共12库确认清理。仅完成生成器前置，业务序列差异仍1条；设计见`workers-ts/audit/pg-sequence-generator.md`，其候选状态由根审计的E5A最终验收节取代。本地全量1,599通过/4项PG跳过和双TypeScript、4文件18项回归保留为辅助证据，不替代Linux结果。
      - [x] **DB-009E5B 业务模型与受守卫完整旧ORM升级（代码/PG16 CI完成，未应用生产）**：f3cf196固定实际模型、外部0145及旧/新目录与依赖合同；c9de509提供独立public/READ COMMITTED事务入口，DO前收紧语句/空闲事务时限、保留更严格配置并拒绝嵌套事务。7d14b88/34036234197将逐字SQL守卫登记为0151，在通用事务外等待独立提交；151个历史migration正文不变，新9项编排测试与真实PG16内嵌152步全部通过，失败不跳过/重试/记成功。真实入口11项、500ms取消/回滚、完整旧ORM的30漂移拒绝、16并发唯一编号及14座测试库清理复验通过；现存数据库单项升级仍不重放runAll的历史数据补写。九路径五类目录零差异不等于完整业务、权限/函数/触发器/策略或生产等价，发布仍需独立审阅与授权。
        - [Actions34032529050](https://github.com/cinagroup/cinashop/actions/runs/34032529050)首轮11/11；252文件1,609项单元、0跳过，两片各126文件/810与799项，真实PG16新探针26,611ms，2,007ms锁等待拒绝、16个并发唯一号码、30类漂移拒绝和全部原号码/对象保留通过。九路径各263表/3,700列/570约束/1,006索引/227序列，五类原始差异全部0；9+2+2共13库清理。旧五组升级证明仅初始建库语句各+1，其余字段及两份生成器报告严格不变。最长单元任务668秒/余532秒、目录260秒/余940秒；结合上一轮两片观测仍采用保守529秒，不关闭TEST-005。
  - [x] **DB-009F 收口后的稳定门禁（代码/PG16 CI完成，未应用生产）**：70e01ea/34037834052九路径263表/3,700列/1,006索引/570约束/227序列的已比较原始目录均进入严格失败门禁；原来源合同、退役别名禁止、默认写入和各旧模型升级保护继续通过。表级要求263个唯一身份及name/key、kind、persistence、rowSecurity、forceRowSecurity、partition、partitionKey八字段一致，拒绝增删/重复/共同收缩、两侧同时漏字段、额外字段或隐式转换，不豁免改名或规范化分区表达式。额外空PG16库实际验证9类变化的正反向拒绝及逐例全五类回滚；前6类证明其余四类不变，同名临时表不遮蔽public。255文件1,642单元零跳过、15座专用目录/夹具库清理复验通过；新夹具不计为第十条项目路径。完整权限/函数/触发器/RLS策略/视图仍由DB-009承接，表的RLS开关一致不是策略等价。历史缺口与原f3cf196/c9de509/7d14b88证据保留，最新验收见根JSON的tableCatalogGateAcceptance。
  - [ ] **DB-009G 外键父端检查的子表索引审查**：先前E3仅核对12引用、另加E2评价引用，不是全库外键清单。全模型枚举45个FK；加入0148候选后30项有覆盖全部引用列的非局部索引首部，剩余15项为12个较窄索引/主键候选及3个只有部分索引的引用。没有任何首列候选的模型引用已从1降至0，但这只是代码清单，不证明生产已应用或所有分布性能合格。父端DELETE/键更新、统计、保留策略及自身PG16验收仍见G2；没有生产慢查询结论。
    - [x] **DB-009G1 回调对账与软删评价两索引正式升级验收**：2026-09-12授权测试维护窗口已通过核心增量安装原始0147，业务指纹在迁移事务及幂等复验中不变。main@214dfb2/Actions34685623400完整成功，其中33项升级测试、2项各100003行真实计划测试均通过；保留真实写锁期限、整批回滚、解锁重试、活跃/删除态及generic计划合同。prc_callback_event(callback_event_id)、spr_order_cart_info(order_cart_info_id)的ORM/外部0147/内嵌0153/独立根事务及九路径正向目录合同均通过，九路径当前263/3701/571/1010/227。发布后两次独立只读复核两个索引精确定义、非唯一、valid/ready，目标两表0/2行指纹与迁移时一致；临时Worker控制面及公开404。证据foreign-key-child-index-postdeploy-20260912.json。仅关闭这两索引的升级验收，不代表父项G、G2或全库性能及正式生产验收完成。
    - [ ] **DB-009G2 企业微信引用范围补审**：45个FK清单保留局部谓词和窄首列候选。wcao_client_ref(corp_id,client_id)已同步ORM/外部0148/内嵌0154/根事务升级，并于2026-09-12授权核心增量实际安装；main九路径和完整CI已通过。统计诊断已改为回滚事务并验证完整统计目录恢复；37项迁移/真实锁竞争及索引反事实证据保留。16个待决索引目标的本机真实RI证据现已齐：其余12个六列+3个可空引用新增654次真实检查，原窄/部分索引2–5块/过滤0，完整28项零失败零跳过；四个截断日志目标的补录来源另列。0d9fee6已获11/11 Linux通过，wcao默认缓存全扫描在Linux和Windows均真实复现；百万行下STATISTICS 1000仍有全扫描，10000仍有热点位图启动成本，不能直接作为生产通用修复。详见work-fk-all-nested-ri-20260912.json、work-fk-nested-ci-20260912.json和work-fk-statistics-scale-20260912.json。剩余为当前15目标增量及容量步骤自身Linux CI、wcao适用规模/统计/保留策略及实际运行权限；不以本机计划覆盖替代正式性能验收。
      - 权限与CI恢复增量（2026-09-12）：8fe0993/Actions34689831315整体失败，两个分片各1项失败、4462项通过；目录/容量、运行时、五端和密钥扫描成功。现修复新增容量步骤的精确CI合同及砍价夹具到期/锁超时竞争，应用超时不变。新增两父表8引用键只读权限预检，真实LOGIN覆盖角色继承/切换/ADMIN、PUBLIC、维护身份伪装、复制权限和严格超时恢复。最终本机PG16三文件53项零失败零跳过、双类型通过、临时数据库/schema/角色零残留；空表SQL权限检查不冒充真实企微业务验收，线上身份及权限未改变。证据work-parent-permissions-ci-repair-20260912.json；本增量自身Linux与运行角色/业务路径、wcao策略继续开放。
      - 复合引用增量（历史阶段记录）：原12项单企业不同事件引用合同保留；2026-09-12补齐全部12个引用的双租户热点用例，24项合计431.38秒PG16通过、双类型通过。每表100003行，双租户热点各50000行，合法稀疏/无引用custom/generic均2–4块；错误租户组合过滤50000行但返回0，不冒充真实父键。真实父端拒绝96次、无引用父操作回滚48次，整行指纹及目标约束/索引/统计目录不变；不增六列宽索引。此阶段剩余真实RI嵌套计划已由上文后续增量补齐；当前剩余按G2父项和最新记录为准。证据work-fk-hot-multitenant-20260912.json。
      - 受限角色业务调用增量（2026-09-12）：8项完整ORM/真实PG16 LOGIN用例覆盖客户新增/编辑/删除态与重复、撤权回滚、UID唯一/歧义/冲突/缺失、脱敏分页及双连接SKIP LOCKED；连同权限预检35项零失败零跳过，双类型通过，约束/索引目录前后相同、隔离资源零残留。脱敏联表锁还需outbox单列UPDATE权限，证明父身份预检不等于完整业务授权；禁止父DELETE/引用键UPDATE的同时这些选定服务调用可完成。未改线上角色/凭据，未验收入口/Queue/provider全链路或wcao容量策略；详见work-parent-runtime-flows-20260912.json，新文件自身Linux CI待验证，G2不关闭。
- [x] **DATA-001 取得只读源 MySQL（不适用）**：2026-09-04 项目所有者确认旧 PHP 站无必要真实历史数据；本部署不连接、复制或对账源 MySQL。
- [x] **DATA-002 全量迁移计划（不适用）**：`data:plan`保留为通用工具，但本部署不生成201表历史复制计划。
- [x] **DATA-003 分批复制与可恢复游标（不适用）**：不执行旧账号、商品、用户、订单、资金或消息复制；`data_migration_run/checkpoint=0/0`符合新系统口径。
- [x] **DATA-004 全量源端校验（不适用）**：不以旧 MySQL行数或摘要作为完成条件；生产 PostgreSQL 当前状态是新系统基线。
- [x] **DATA-005 旧站私有媒体迁移（不适用）**：不复制旧`store/comment`对象或旧对象存储密钥；新系统媒体必须从当前R2资产流程创建。
- [ ] **DATA-006 新系统关键业务数据初始化与验收**：创建并验收可售商品完整SKU/分类/描述、主管理员与受限角色、Supplier/门店/客服账号、通知模板、支付与微信配置、打印机和电子面单；每个启用域必须有真实正反角色与非空数据E2E。
- [ ] **DATA-007 当前生产完整性异常处置（禁止猜测修复）**：由业务 owner 判定现有记录应保留、修复或删除并留存变更前快照，再逐项处理用户券 owner 孤儿3、运行时过期但状态仍可用2、用户关系 owner 孤儿1、签到 owner 孤儿1、商品收藏计数漂移1；不得从不存在的旧站权威值推断，且未经单独写入批准不得执行。
- [ ] **DATA-008 DIY/营销运营数据初始化**：人工创建并确认默认/启用`system_dise`、悬浮导航、21个首页候选配置键、20个图片处理键、视频、新人商品、促销、预售、直播与可领取券；先裁决`site_url`5条、`sign_give_point`4条、`sign_status`4条当前重复记录，不能按最新ID自动去重。数据到位后做新H5/小程序/App的golden response和真实登录态验收。

## P0：资金、回调与认证边界

- [ ] **CORE-001 支付/业务回调（已完成详细审计并拆成 A～H）**：父项继续未完成；不能用支付订单行自身的 `paid=0→1` 幂等代替回调事件账本，也不能把 WORK-C 的隔离证据外推到公众号、小程序、寄件或同城配送。
  - [x] **CORE-001-A 支付动态入口与渠道身份（候选代码完成，未发布）**：已精确恢复 `ANY /api/pay/notify/:type`，运行时只接受 `POST`，仅分发 `alipay/wechat/routine/app`；公众号、H5、PC 使用 `wechat_appid`，小程序使用 `routine_appId`，App 使用 `wechat_app_appid`，下单 `notify_url` 与回调 AppID 校验使用同一 profile。微信继续校验时间窗、签名探测、平台公钥 ID/RSA、AES-GCM、AppID、MchID、通知 ID、订单/交易号、CNY 和金额；支付宝强制 RSA2、AppID、非空 SellerID、订单/交易号及金额。微信/支付宝 body 分别限制 64/32 KiB，支付宝拒绝重复 form 字段和非 form content-type；旧 `GET` 不再触发状态写入。路由审计由 API 415/412 提升为 416/413 精确/可执行。该子项只代表入口、profile 和输入防线完成，不代表回调可靠性闭环。
  - [x] **CORE-001-B 支付回调事件账本与快速应答（候选代码与生产结构完成，主 Worker 未发布）**：已新增 `payment_callback_event/payment_callback_outbox`、provider event/replay 唯一键、签名后白名单摘要、接收/处理/终态、租约/尝试/错误码与保留期；HTTP 在微信/支付宝验签解密后仅用短事务原子持久化事件+outbox并快速应答，Queue 只携带 `{action,eventId,replayKey}`，消费者复用商品/充值/会员三类支付状态机。advisory lock、行锁、`SKIP LOCKED`、部分索引、Cron 补偿、8 次 DEAD 与 DLQ 重放覆盖并发/重复/交易冲突 UNKNOWN、持久化/Queue 失败和消费崩溃。按授权直接通过生产 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 在 PostgreSQL 16.14 创建两张空表，最终精确读回 36 列、12 约束、11 索引且 DDL 二次执行幂等；随机 schema 11/11 场景通过并删除，临时审计 Worker 已删除。候选提交 `bd501cf` 与门禁修正 `e654cc5` 已推送，Linux Actions `33470912902` 8/8 成功（含 workerd 与全历史 Gitleaks）。本项不代表真实渠道凭据、主动对账、主 Worker 发布或支付渠道已启用。
  - [x] **CORE-001-C 支付主动对账与告警（候选代码与生产结构完成，主 Worker 未发布）**：微信/支付宝外部支付发起前先登记不可变恢复意图，可信回调与同一案件联动；Cron 根任务、`SKIP LOCKED` 游标分页、opaque Queue、租约、指数退避、12 次 DEAD、老化 `NOT_FOUND/CLOSED`、成功后复用商品/充值/会员支付状态机、证据冲突终止、DLQ 重放和 `RETRY/ACCEPT_LOCAL/CLOSE` 人工处置已完成，provider I/O 位于所有 PostgreSQL 事务之外。新增 `payment_reconciliation_case/action`，按授权直接通过生产 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 创建两张空表；最终精确读回 33 列、13 约束、10 索引，DDL 二次执行幂等，随机 schema 最终 16/16 场景通过并删除，临时审计 Worker 已删除。生产只读基线发现 8 个商城和 5 个充值历史未支付订单均超过 30 分钟，但没有可信外部渠道标记，未做猜测性回填。候选提交 `276e9d0` 已推送，Linux Actions `33474315306` 8/8 成功。本项不代表真实微信/支付宝凭据、告警目的地、主 Worker 发布或发布后观察已完成，这些继续由 CORE-001-H 约束。
  - [x] **CORE-001-D 公众号/小程序消息回调（候选代码与生产结构完成，主 Worker 未发布）**：已恢复 `ANY /api/wechat/serve` 与 `ANY /api/wechat/miniServe`；GET 支持明文/AES URL 验证，POST 只接受安全模式 XML，执行 SHA-1 验签、AES-256-CBC/32-byte PKCS#7 解密和尾部 AppID 常量时间匹配。签名、原始/解密 XML 和用户正文均不落库，正文只存 SHA-256；同步回复先解析最小白名单并固化快照。新增 `wechat_callback_event/outbox/watermark`、事件/replay 唯一键、主体 advisory lock、乱序水位、`SKIP LOCKED`、投递/处理租约、Cron 补偿、opaque Queue、8 次 DEAD 与 DLQ 重放；关注单调、扫码加法且重放安全、卡券删除抵御迟到领卡、消息精确一次，社交支付桥接 CORE-001-B 同一账本，结算/收货复用既有幂等状态机，任何验签/持久化/消费失败均不确认成功。按授权通过生产 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 在 PostgreSQL 16.14 创建三张空表，精确读回 45 列、16 个已验证约束、13 个有效索引（4 个部分索引），DDL 二次执行幂等；随机 schema 14/14 场景通过并删除，临时审计 Worker 已删除。候选提交 `a5e02d0` 已推送，[Linux Actions `33480134867`](https://github.com/cinagroup/cinashop/actions/runs/33480134867) 8/8 成功。生产配置表没有公众号/小程序 AppID/旧 token/AES key，主 Worker 也没有新 4 个 callback Secret；本项不代表真实微信回调、主 Worker 发布或渠道启用，这些继续由 CORE-001-H 约束。
  - [x] **CORE-001-E 企业微信可信回调代码边界**：`ANY /api/work/serve` 的 WORK-C0～C8 已完成签名解密、事件/水位/outbox、Queue、乱序投影和副作用处置代码；真实 Corp、Secret、源数据、全量 reconciliation、预发、发布及观察仍按 WORK-C 父项保持未完成。
  - [x] **CORE-001-F 商家寄件回调（候选代码与生产空结构完成，主 Worker 未发布）**：已按快递100当前直连合同恢复 `ANY /api/order_call_back`，运行时只接受 `POST application/x-www-form-urlencoded`，严格校验唯一 `taskId/sign/param`、独立 `KUAIDI100_CALLBACK_SALT` 与大写 `MD5(param+salt)`，顶层通讯状态必须为 `200`；旧短信 `sms_token` AES-CBC 包络被明确拒绝。新增 `merchant_shipment_callback_event/outbox/watermark`、签名后短事务、opaque Queue、Cron 补偿、租约/8 次 DEAD、task advisory lock、单调状态图和履约回放；原始表单、签名、快递员 PII、费用和图片不落库。官方 `0/1/2/9/10/11/13/14/15/99/101/155/166/200/201/302/400/610` 均显式建模，`302` 围住旧/新 task 水位，取件后迟到取消不回退，取消后只有 `166` 可复活，`15` 在取件或签收后可靠关闭状态。首轮失败夹具曾因绕过事务级 `search_path` 在 `public` 留下 4 条固定审计订单和 1 条审计快递公司；用户明确授权后，清理事务以完整行指纹和 17 类关联均为 0 为前置条件，精确删除 4+1 行。生产 PostgreSQL 16.14 的三张目标表最终均为 0 行，45 列、18 个约束、13 个 valid/ready 索引及含 `SETTLED/REASSIGNED` 的水位约束精确读回，DDL 二次执行幂等；最终原业务订单 28、所有寄件 task/provider order/tracking/stock-up 标记 0、临时 schema 0。扩展随机 schema 15/15 通过并删除，过程中发现并修复取件/签收后 `SETTLED` 被误判为 `SUPERSEDED` 的状态机缺陷；临时 Worker 已删除且 URL 返回 404，主 Worker 未部署。先前实现提交 `e0ca6d6`、runtime 修正 `a5326ab` 与 [Linux Actions `33486809063`](https://github.com/cinagroup/cinashop/actions/runs/33486809063) 已通过；结算修正提交 `5568ff1` 及 [Linux Actions `33491495530`](https://github.com/cinagroup/cinashop/actions/runs/33491495530) 8/8 通过。生产仍没有 callback salt、快递100企业版调试凭据或真实 provider 样本；真实凭据、后台 callback URL、真实重放/乱序、发布及观察继续由 CORE-001-H 约束。
  - [ ] **CORE-001-G 同城配送回调（达达/UU 候选与生产空结构完成；真实渠道验收待完成）**：父项继续未完成；独立 provider 边界和空结构完成不代表任一渠道已启用。
    - [x] **CORE-001-G1 达达**：已恢复 `ANY /api/city_delivery/notify` 的兼容注册，运行时仅接受 `POST application/json` 和精确 `provider=dada&token=...`；官方 callback `signature` 只是 `client_id/order_id/update_time` 排序值的无密钥 MD5，不能证明 provider 身份，因此额外强制高熵 `DADA_CALLBACK_TOKEN` 与预期 `DADA_CLIENT_ID`，错误 token/client/checksum/body 均失败关闭。新增 event/outbox/watermark/reconciliation case 四表、签名后短事务、opaque Queue、Cron 补偿、租约/8 次 DEAD、subject advisory lock、单调状态图、取消/完成冲突、PII 终态清除和固定 HTTPS 主动查单；未知状态隔离，迟到状态/取件后普通取消不回退，官方 `9→10` 妥投异常返还可完成，完成复用共享收货原语。生产 PostgreSQL 16.14 基线同城配送/Dada/UU 均为 0，目标表从 0 创建为 4 张空表；精确读回 64 列、25 个已验证约束、19 个 valid/ready 索引（6 个部分索引、10 个唯一索引）、4 个 `ON DELETE RESTRICT` 外键，DDL 二次执行幂等且业务表指纹不变。迁移后再次执行的随机 schema 16/16 通过并删除，临时 Worker 删除后 URL 返回 404；主 Worker未部署。生产没有 Dada 配置或真实 provider 样本，真实 callback URL/Secret、回放/乱序、预发、发布及观察归 CORE-001-H。
    - [ ] **CORE-001-G2 UU 跑腿（候选代码与生产约束完成；真实联调待完成）**：[UU 当前开放平台](https://open.uupt.com/)已公开 V3 callback 的 `openId/timestamp/biz/sign` 外层、`orderCode/originId/state/stateText/changeTime/driver*` 业务字段、`1/2/3/4/5/6/10/-1/-2/-3` 状态及 `{"code":1,"msg":"success"}` ACK；同一官方“回调与上线”页仍明确说明 callback 验签算法和失败重试策略未公开。候选因此用独立高熵 `UU_CALLBACK_TOKEN` + 精确 `UU_OPEN_ID` 建立认证边界，只做 `sign` 形状检查，不猜测其算法、不复用 Dada token；32 KiB 原始 body、token、sign 和骑手 PII 均不持久化。状态 2 只允许取件前回到待接单并清空旧骑手，状态 6 不提前收货，取件后取消冲突，状态 10 复用共享收货原语；V3 `orderDetail` 主动查单固定 HTTPS/8 秒/32 KiB，但因官网“秒”文字与 13 位示例矛盾，必须在联调后显式配置 `UU_API_TIMESTAMP_UNIT=seconds|milliseconds` 才能发请求。生产四表约束已从 Dada-only 原位升级为 Dada/UU、允许负数取消状态及 UU 水位状态，连续两次执行保持 4 表/64 列/25 约束/19 索引/0 行且业务指纹不变；随机 schema 的 Dada+UU 19/19 场景通过并删除，临时 Worker 删除后 URL 为 404，主 Worker未部署。本项保持未勾选，剩余门禁为：
      - [ ] 取得 UU 测试应用与 `appId/appKey/openId`，由 Secret 注入独立 callback token，禁止写入 `system_config` 或仓库。
      - [ ] 由 UU 官方或联调实测确认 callback `sign` 验证算法、时间窗/时间戳单位、来源约束、ACK 判定及失败重试次数/间隔；确认前不把 V3 出站签名算法外推为入站认证。
      - [ ] 在发单链路提交带独立 token 的精确 callback URL，并核对 `originId`、`orderCode` 与 `store_delivery_order.order_id/delivery_no` 双向一致。
      - [ ] 用真实回调覆盖 `2→3` 重派、`5→6→10`、`-1/-2/-3`、重复、延迟、乱序、持久化失败、Queue 恢复和主动查单；保留脱敏证据。
      - [ ] 完成旧端 E2E、预发、明确发布批准与发布后观察后，才能勾选 G2 与父项 G。
  - [ ] **CORE-001-H 生产启用闭环**：直接绑定 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 做只读结构/配置/历史交易审计和随机 schema 状态机场景；补真实微信/支付宝/公众号/小程序/企微/快递100/达达/UU凭据与沙箱或测试租户，完成 provider 回调、重放、延迟/乱序、故障恢复、旧端 E2E、预发、明确发布批准和发布后观察。生产支付开关/凭据当前仍不可用，候选代码不得解释为渠道已启用。
- [x] **CORE-002 客服资金退款**：已补齐认证 `PUT /kefuapi/refund/refund/:id`，只接受同意动作；提交金额必须等于售后权威金额，历史部分退款失败关闭，完成重放收敛。核心退款 scope 绑定 store/supplier/customer/refund/order/金额/已退金额，并在退款锁前通过授权回调锁定客服会话，转接立即撤权。正式 Hyperdrive 随机 schema 重跑完整退款状态机，金额错配、余额/积分/渠道账本、累计并发、补偿、回调/主动对账既有门禁继续通过；客服专项的金额篡改、单次入账、重复提交、部分历史和转接撤权全部通过，`public_state_unchanged=true`。生产支付 Secret 仍未配置，代码完成不代表渠道已启用。
- [x] **CORE-003 对外 API 资金与用户写入**：退款、分类、实物商品、优惠券和用户三条写入均已在逐路由 ACL、HMAC 脱敏访问审计和幂等状态机下恢复；余额/积分写入与不可变流水同事务，用户新增/资料/等级/标签/推广关系也不再部分提交。每条接口仍必须绑定独立权限 ID，不得因导入目录而自动开放；生产当前没有 Out 账户/接口目录，代码完成不代表真实客户已启用。
- [ ] **CORE-004 认证入口合同（本地安全闭环已完成，生产闭环未完成）**：旧 `GET /api/verify_code` 现必须显式带手机号和用途并创建真实 Turnstile 挑战；AJCaptcha、AJCheck 和图片验证码别名明确返回 410。短信与微信身份按独立 purpose、频率和一次性消费执行，provider 响应限制 8 秒/32 KiB。
  - [x] **扫码/OAuth 本地安全闭环**：PC/客服挑战使用 `pending→scanned→approved→issuing→delivered`，二维码公钥与私有 poll secret 分离，扫码主体/audience/白名单浏览器来源/请求站点/粗粒度设备固定，批准必须同一用户；这些 Origin/UA 字段只供人工核对，不是客户端证明。固定签发时间、租约和 delivered 持久化保证响应丢失后重试交付同一 token。CORS 只反射精确 allowlist，OAuth verifier 使用按 audience+state 隔离的 `__Host-*` HttpOnly/Secure/SameSite=Lax Cookie，AppSecret 只接受 `WECHAT_OPEN_APP_SECRET`。用户 JWT 已恢复 PHP 的 `auth=md5(user.pwd)`，旧 Worker 单层 claim 只在精确 `tb_` bucket 活跃时最多兼容 7 天+60 秒；默认密码不再跳过。生产 bucket 读/写/删异常均真实 503，HTTP、心跳及三类 WebSocket 下行会复核 bucket/期限/数据库身份，注销主动断开；注销删除失败时四端清本地但明确提示服务端撤销未确认。PC/Kefu 使用 per-tab `sessionStorage` 并初始化清除旧 `localStorage`；PC、Kefu 与 UniApp 确认页已接入并通过桌面/390×844 受控浏览器回归。
  - [ ] **生产认证闭环（BLOCKED：需切换策略、凭据、数据与发布批准）**：先明确全量切流强制重新登录，或实现 PHP/Worker bucket 双读/迁移桥；配置开放平台 `wechat_open_app_id`、`WECHAT_OPEN_APP_SECRET`、Kefu 精确生产 Origin和同源 Pages proxy，迁移微信身份与客服账号；补 Turnstile/边缘证明、异常挑战预算及二维码中继风险评估，再完成真实短信/微信、真实账号、真机、Linux runtime、预发、影子流量、明确发布和发布后观察。生产当前没有 Turnstile、阿里云短信、短信模板或微信开放平台/公众号/小程序凭据，也没有微信身份或客服账号样本。
- [ ] **CORE-005 默认管理员与 Token 切换**：确认生产无历史默认密码并轮换管理员密码；核对生产 `APP_KEY`。代码已恢复 PHP JWT auth 摘要，并以精确 `tb_` bucket 为旧 Worker token 提供最长 7 天+60 秒过渡，但 PHP 与 Worker bucket 格式不兼容；必须记录旧签发器停用时间、强制重新登录/迁移桥方案、兼容分支删除时间，并禁止未经验证的鉴权流量回退。

## P1：用户端 `/api` 路由批次

- [x] **API-001 公共首页与商品发现**：新增 17 条 PHP 精确 GET 合同，覆盖导航、首页、个人中心菜单/摘要、品牌、搜索筛选/推荐、三类排行、详情推荐/活动/正文/可选类型、首页推荐、热门、预售和评价回复；`/products` 恢复精确 `count`，详情同时保留 TS 平铺字段与 PHP `storeInfo/productValue`，评价恢复 PHP 六字段统计并保留现 TS 别名。组合配置、品牌/标签和用户态采用批量查询，分页上限 100；生产 `public` 只读事务以 71 个真实商品验证分页和排行，随机 schema 12 项断言验证非空推荐/品牌/标签/导航/描述/预售/评价分档后删除。生产推荐、内容和活动空集归因于 DATA-006/008 的新系统运营初始化，不能把代码完成解释为线上内容或发布已经验收。
- [ ] **API-002 订单与售后 19 条缺口（核心代码与生产隔离验证已收口）**：原 19 项中，`POST /order/comment` 已在 API-001 前完成；本批新增 16 条精确合同，覆盖配送能力、确认单、金额预览、订单统计/奖励/核销记录/评价商品/门店收银、退款原因/商品摘要/兼容申请/退货物流/再次申请/终态删除，以及五分钟不透明键的支付宝兼容入口。`GET /order/nopay` 的 PHP 控制器方法不存在且无一方调用，已带源行证据退役；`ANY /order_call_back` 转入 CORE-001，禁止复制旧 AES 解密后无签名、无重放账本的写状态逻辑。确认与计算接口现在和正式建单共用权威报价路径：普通等级价、有效 SVIP 商品价、优惠券、首单、服务端积分上限、冻结积分排除、固定/模板运费、满额/线下包邮及 SVIP 运费权益不再由展示端另算；`useIntegral` 恢复为 PHP 布尔开关，客户端不能指定扣减点数。支付后商品赠券按模板去重、库存与用户券同事务，并通过订单归属账本阻断重放；`/order/prize` 从持久证据读取，不再依赖两小时缓存。用户售后按 `refund_time_available` 和最后收货状态执行含边界的期限校验，自动退款不受用户申请期限阻断。生产 Hyperdrive 随机 schema 验证报价 `20.00→16.00`、券 `3.00`、积分 `2.00/200`、运费 `5.00→2.50`、实付 `13.50` 与落单完全一致；同模板双商品并发发券结果为 `0/1`、库存只减 1、账本/奖品各 1；过期用户申请拒绝、自动退款放行、未收货申请放行。既有并发/超卖/支付取消/退款补偿场景继续通过，全部 `schema_removed=true/public_state_unchanged=true`。本项仍不勾选：生产商品赠券关系、退款理由、门店、核销和配送样本为空；促销叠加属于 API-006 尚未迁移；旧 PC/UniApp 与新五端浏览器 E2E、预发和正式发布均未完成。
- [ ] **API-003 用户中心 13 条精确路由（核心代码与生产隔离验证已收口）**：审计确认“13”是 PHP `/api/user*` 的精确未匹配路由，不是地址、收藏、账单等 13 个业务域；这些域多数早已有实现。本批补齐会员检测/激活表单、活动状态、客服扫码 GET/POST、客服会话摘要、个人中心、安全自资料、付款随机码、分享记录/口令、小程序推广码和推广海报。会员激活不再接受客户端 `levelId`，配置门禁、资料白名单、积分/余额/券奖励与等级检测均在用户行锁和同一事务内，并写不可变流水；个人中心不复制 PHP 的 GET 生成条码/自动晋升副作用，也不返回密码摘要、账号、登录随机值、IP、缓存清理时间或客服扫码键。分享使用用户行锁和账单证据实现 300 秒冷却；付款码由 Web Crypto 生成并以 Redis `SET NX EX` 原子竞争，在强一致 Redis 中缓存 600 秒，并发请求复用同一个赢家码。旧 `GET/POST /user/code` 已改接 CORE-004 Durable Object 挑战：GET 由已认证用户扫码并绑定主体，POST 只允许同一用户批准或拒绝；不再接受任意缓存键置 `0`。UniApp 扫码确认页现已本地接入并显示请求目标/站点/设备，同时明确这些字段只供人工核对。生产 `public` 在单一 `READ ONLY` 事务中完成活动、自资料、个人中心、推广海报、客服会话摘要和分享口令 6 类结构/敏感字段断言，返回 108 个个人中心键、当前会话摘要 0 条；随机 schema 的会员 7 项奖励/等级断言、重复激活拒绝、分享只写 1 条证据、重复分享拒绝和 6 位码复用均通过。生产仅有 `member_func_status=1`，其余激活表单/奖励配置缺失，因此真实会员激活当前不会开启。一次审计夹具因 Hyperdrive 未保留启动级 `search_path` 误入 `public`，已按固定 ID/标签在事务内精确删除；最终 9 类标记均为 0、临时 schema 前后均为空，后续所有播种强制使用 `SET LOCAL search_path`。本项仍不勾选：付款码尚无 TS 收银消费端，生产扫码正向验收被微信/客服数据和凭据阻断；微信小程序真实凭据/码生成、旧 PC/UniApp 与五端 E2E、预发和正式发布均未完成。
- [ ] **API-003-USER-CENTER-COMPAT 9 条地址/收藏/签到合同（代码与生产隔离验证已收口，数据/外部通知/E2E/发布未完成）**：九条强制登录合同、18 个 `private, no-store` handler、地址 UID 作用域与事务默认切换、product/video 收藏兼容和上海自然日签到均已实现；`us_uid_shanghai_day_uq` 继续阻断 PHP/Worker 同日重复签到。2026-08-31 已进一步恢复旧 `sign_remind_time` 每日上海 10:25 调度、主键游标分页、引用型 Queue、消费前偏好/用户/签到复核、与签到共用 advisory lock、`system_message.event_key` 每用户每日唯一站内信及 DLQ 可重放；生产随机 schema 对候选选择、关闭偏好、扫描后签到、重复消费、失败 60 秒退避/恢复和模板渲染 10/10 通过，Ubuntu Actions `33379089255` 的真实 workerd 1 文件/11 项通过。生产只读事实却是提醒开启用户 0、当天候选 0、`sign_remind_time` 模板 0、旧 timer 0、历史提醒 0，因此没有真实生产正向样本；旧 PHP 同时尝试的短信和小程序订阅消息仍缺通用 provider 投递账本、凭据与模板验收。父项继续等待 `city_area/system_city`、5 个孤儿 owner、默认地址/收藏跨栈门禁、真实 token/端到端流程、预发与明确发布；主 Worker/PC/UniApp 均未发布本批。
- [ ] **API-004 `/api/v2` 54→16 条缺口**：已审计旧 UniApp、`admin-ts`、`pc-ts`、`supplier-ts`、`uniapp-ts` 与 `kefu-ts`；新五端只有 UniApp 抽奖仍直接调用 v2，旧 UniApp 则广泛依赖登录、发票、购物车、DIY、优惠券、分销和评价合同。首批新增 15 条精确路由：搜索列表/清理、客服记录、发票 6、订单发票 3、分销等级 2、评价 1。发票列表/详情/默认值恢复旧端 snake_case 与分页/筛选，新增/编辑补齐手机号、税号和银行字段；单条详情修复 PHP 未按 UID 限定的越权读取，默认项和查重用用户级短事务锁串行化，旧 GET 删除只作为兼容入口且仍按 UID 限定。CART 批次再补重置、列表、SKU 属性和改数量 4 条，恢复 `productValue[suk]`、旧 snake_case/混合字段、批量列表和权威会员价，并用用户级 advisory lock、行锁、归属/状态/库存复核修复 PHP 的跨 UID cart ID 与负数量风险；旧 v1 `type=2 + number` 商品 ID 改数量合同也已恢复。DIY 批次再补 6 条公开配置/城市合同，保持无认证边界、PHP 两种 DIY 合并规则、配置默认值和直辖市重复段地址语义。COUPON 批次再补新人、今日和可领取 3 条只读合同，恢复迁移后 `type/coupon_type` 语义、UID 已领状态、商品/分类/品牌范围、排序、四类计数与 SVIP 弹窗门禁。USER 批次再补资料、微信刷新、三类资金列表和两条分销合同，所有列表按当前 UID 读取并隐藏孤儿历史。PROMO 批次再补活动商品、赠品信息和登录态凑单 3 条只读合同，以 active 平台父活动为权威并恢复五类商品范围、折扣、阶梯和赠品组合。HOME 批次再补紧凑首页和关注状态 2 条，并额外恢复真实旧端调用的 v1 关注合同；生产与随机 schema 证据详见下项。剩余清单：
  - [ ] **API-004-AUTH 16 条微信/小程序登录与绑定（精确路由和安全核心已收口）**：16 条 PHP v2 路径已全部精确注册，并新增 v1/v2 `POST wechat/oauth_state`。`auth_type→auth_login` 使用一次性小程序票据；手机号支持当前微信 `phone_code` 服务端核验和旧 `encryptedData/iv`，旧解密额外验证 watermark AppID；短信路径只消费 `user_social_binding` purpose；公众号 OAuth 必须消费服务端 state。provider code 以渠道摘要全局占用 10 分钟并按来源/渠道 30 次/分钟限流，临时身份、票据、state 和旧 session key 均一次性使用；社交合并在 advisory lock/事务下拒绝 openid、unionid、手机号冲突，响应只投影安全用户字段。生产只读审计为用户 3、活跃且无手机号 0、`wechat_user=0`、`sms_record=0`，所需七项配置全空且生产 Worker 缺微信/短信/Turnstile Secret。本项仍不勾选：没有真实凭据、非空社交样本、旧 UniApp 升级、真机/真实短信与微信、预发和正式发布证据；不得为兼容退回 PHP 的两小时 MD5 弱缓存或无 state OAuth。
  - [ ] **API-004-DIY 6 条公共配置/城市（核心代码和生产隔离验证已收口）**：`get_diy`、绑定开关、门店开关、换色、商品详情 DIY 和地址解析均已按 PHP 无认证边界注册；`diy → system_dise` 继续使用迁移 manifest 的显式改名，商品详情只接受旧默认键、分类 DIY 保留扩展键，城市最多解析 8 段并以精确名称和数字祖先路径查询，缺参数/未录入提示保持旧合同。生产只读确认 `system_dise=0`、`city_area=0`，8 个相关配置仅 `site_url` 存在且有 5 条重复；随机 schema 的默认/命名 DIY、6 个配置、商品/分类合并、四级地址、重复直辖市段和缺失城市 10/10 通过，`public_state_unchanged=true`、临时 schema `0→0`。仍不勾选：需由运营初始化 DIY、城市和权威配置并裁决 `site_url` 冲突，使用当前 R2 资产流程创建媒体后做 UniApp/真机/预发/发布 E2E。
  - [ ] **API-004-CART 4 条购物车/属性（核心代码和生产隔离验证已收口）**：四条 PHP 精确路由均已注册，`get_attr` 正确把第二路径参数作为购物车数量标志并按 `suk` 建 `productValue`；列表批量读取商品/SKU并复用建单会员价，所有写入限定当前用户有效普通购物车，在用户级 advisory lock 与行锁下完成库存复核、换规格合并和软删。生产 2 条有效购物车无完整性异常；随机 schema 的 9 项断言（含双 Hyperdrive 连接同规格并发收敛）、公共表不变和清理均通过。仍不勾选：生产 69/71 有效商品缺普通 SKU、商品属性为 0，需按 DATA-006 完成新目录；旧 UniApp 页面已不在仓库，真实账号/真机/预发/发布 E2E 未完成。目录初始化后再按真实规模复核索引。
  - [ ] **API-004-COUPON 3 条优惠券（核心代码和生产隔离验证已收口）**：`new_coupon/get_today_coupon/coupons` 按原 `force=true/false/false` 注册；新人接口保持只读，不恢复 PHP 已注释的自动发券副作用。列表把目标 `coupon_type/type/day/legacy_*` 精确投影回源 `type/coupon_type/coupon_time/product_id/category_id/brand_id`，恢复领取/使用窗口、UID `used/is_use`、商品上下文、四类计数和三种排序；批量查询领取、范围和样例商品，不引入按券 N+1，并修正 PHP 丢失商品派生品牌而导致旧 UniApp 品牌页签为空的问题。生产只有 1 条不可领取模板和 4 条现存用户券，真实三类列表均为空；随机 schema 11/11、公共指纹不变和清理均通过。仍不勾选：需由运营创建有效模板、范围与领取样本，验收真实可领取、新人和 SVIP 弹窗，再完成 UniApp/真实账号/预发/发布 E2E。
  - [ ] **API-004-USER 5 条用户/分销（核心代码和生产隔离验证已收口）**：`user_update` 只接受旧 `userInfo` 的昵称、头像、性别、语言和地区白名单，在用户与微信身份行锁下同步当前活跃账户；`user/wechat` 从服务端 OAuth code 换取 openid，再读取公众号资料并只更新该 UID 已绑定的 active `wechat` 身份，不接受客户端指定 openid。`money_list/:type` 恢复 0 全部余额、1 支出、2 收入、3 佣金、4 提现、9 资金流和旧 snake_case/退款状态字段；所有分页上限 100、关联批量读取且保持资金只读。`agent_user_list/:type` 修复 PHP 忽略路径 type 导致已下单页签失效及 `%H:%m` 把月份当分钟两处错误，`agent_info` 只统计当前用户收入并以内连接过滤孤儿/停用用户轮播。生产与随机 schema 证据均通过。仍不勾选：生产没有微信身份/AppID/Secret、余额与资金流为空，7 条佣金、5 条提现、6 条充值、3 条退款引用不存在的用户，且分销协议为空；需由业务所有者按 DATA-007 裁决孤儿并按 DATA-006 初始化身份/配置，再完成真实微信 OAuth、UniApp、账号、预发和发布 E2E。
  - [ ] **API-004-PROMO 3 条促销（核心代码和生产隔离验证已收口）**：按 PHP 认证边界注册公开 `productList/:type`、公开 `give_info/:id` 与强制登录 `collect_order/product`；只读取 active、未删除、平台父规则，商品列表支持全场、指定、排除、品牌、标签五类范围及活动边框/背景，折扣按 PHP 两段 bcmath 截断，赠品信息批量读取券、商品与普通 SKU，凑单返回父/子阶梯。服务对分页、活动数和商品数设上限，并对失效活动失败关闭；隔离审计还修复统一商品搜索中 `ids` 只排序不筛选的范围扩散缺陷。生产促销/辅助数据为 0，随机 schema 12/12、公共指纹不变和清理均通过。仍不勾选：旧 UniApp 仅有前两条包装器且页面调用缺失，需由运营创建真实活动、范围、品牌/标签关系和赠品样本；订单促销叠加仍属 API-006，真实账号/旧端/预发/发布 E2E 未完成。
  - [ ] **API-004-HOME 2 条首页/关注状态（核心代码和生产隔离验证已收口）**：`v2/index` 只返回 `info/benefit/likeInfo/subscribe/tengxun_map_key/site_name`，不把 v1 banner 等扩展字段混入；快捷分类修正为 PHP 的可见 `pid>0` 二级分类，数量按 PHP 整数前缀并封顶 100，精品/新品/促销/热门沿用权威商品关系与会员可见性，预售状态恢复 0 非预售、1 未开始、2 进行中、3 已结束。`v2/subscribe` 只读取当前 UID 最新未删除公众号身份，匿名为 false；旧 UniApp 实际使用的 v1 `subscribe` 同步恢复，匿名为 true。三条响应均 `private, no-store`。生产内容区因四个数量配置、推荐关系和微信身份缺失而为空；随机 schema 12/12、公共指纹不变和清理均通过。仍不勾选：需由运营初始化并确认首页配置、推荐关系和微信身份；商品秒杀/砍价/拼团标签依赖 API-006，媒体、真实微信、旧端、预发和发布 E2E 未完成。
- [ ] **API-005 `/api/pc` 22 条合同（核心代码、三端本地接入与生产审计已收口）**：22 条现全部可执行；既有商品、分类、公司/城市、banner、推荐/优品、二维码及六条 UID 作用域用户合同保持不变。`GET key/scan/:key/wechat_auth` 已从 501 重建为来源白名单、按 state 隔离的浏览器 verifier Cookie、可重试一次性交付的扫码/OAuth 合同，另提供 POST key 和 `POST /api/pc/oauth_state` 安全扩展；poll 必须携带二维码外的私有 `X-Scan-Poll-Token`，错误 audience 不能消费批准。新 PC、Kefu 和 UniApp 确认页已经本地接入并通过桌面/移动浏览器回归。本项仍不勾选：Origin/UA 与二维码中继不构成客户端证明，active 分类关系、PC banner、城市和 14/17 既有候选配置未复制，生产开放平台 AppID/Worker Secret 缺失，没有微信身份或客服样本；旧 Nuxt PC 仍需退流，真实微信/账号/真机、预发和正式发布都未完成。
- [ ] **API-006 营销/活动主批次**：静态精确缺口原始清单为 marketing 13、bargain 4、combination 4、seckill 3；活动兼容子批次已将后三组 11 条收口，但整体仍受订单状态机、真实数据/配置、前端 E2E 和发布门禁限制。
  - [ ] **API-006-ACTIVITY 11 条秒杀/拼团/砍价精确缺口合同（新增端点核心代码、生产只读与隔离验证已收口）**：已精确补齐 `seckill/detail/:id/[:time]`、秒杀/拼团 `detail_code` 和 `code`、拼团 banner/海报、砍价 config/start-user/share/poster；二维码仅内存返回 data URL，不写公开附件，个性化码/海报强制登录并校验 UID 归属。砍价分享用短事务原子累加，列表恢复 `title/image/residue_price/pay_status/datatime`，取消同时接受旧客户端 `bargainId`。生产两个部分索引已幂等应用，隔离场景 14/14 通过。本项仍不勾选：早先已算静态匹配的秒杀/拼团/砍价 detail 响应仍比 PHP 简化，生产活动组内容和小程序凭据未迁移，未用真实用户 token/真实微信执行旧 UniApp、未验收预发/影子流量，且主 Worker 未发布。
    - [ ] **API-006-ACTIVITY-PINK 旧拼团状态端点身份与响应合同**：LegacyPinkStatusService按pink记录ID、k_id及有界退款链返回只读状态；本人订单/成员/活动投影不含其他订单与成本，GET不写入或触发provider。faaa441实际PG16的原始Date失败已由c066371列编码修复，31项专项及自身11/11 CI通过。原goods_combination_status路径现直接注册状态页，严格解析成员/团长/活动及用户归属，显示持久化成败和明确待结算；参团转view=skus并保留团长ID，重新开团不带旧团ID。支持本人订单/我的订单、邀请链接、推荐、合成登录返回、隐藏/身份/迟到请求隔离与H5同页hash查询切换，26项新前端回归及CUA通过。本轮取消接口补齐前置订单关联与专用全额退款申请，39项HTTP/SQL申请测试及74e22c3自身CI通过。本轮新增跨截止/原团长退款后同申请恢复，真实余额/库存/团长转移已在隔离SQL验证；并发预留、真实渠道及页面闭环尚未完成。尚需恢复旧取消拼团确认及异步退款结果、分享归因/微信H5和App provider、真实角色/真机与支付/超时维护衔接；订单售后入口不能代替旧取消合同完成。本父项及A3整体保持开放。
      - [ ] **API-006-ACTIVITY-PINK-A 取消原子受理及参团/支付/维护互斥**：fc188d2的原子受理/取消意图准入/余额回滚，及1f4d3a9的库存与全团订单锁顺序均已通过自身PG16 CI。后者12项含8项独立连接，覆盖付款、建单、未付取消与退款双方向及成员退款/超时衔接。9c38154状态与推荐复用同一取消意图条件：状态读将取消中映射为待结算，目录用相关NOT EXISTS在LIMIT前排除，指定取消团明确拒绝；两者使用只读REPEATABLE READ快照，无N+1或新DDL。投影65项SQL回归现已通过自身PG16 CI；全部故障恢复与真实渠道门禁未完成。不发布取消入口，父项不关闭。
      - [ ] **API-006-ACTIVITY-PINK-B 已受理取消的结果恢复（后台32项已通过自身PG16 CI，完整门禁开放）**：同一专用申请跨截止/完成/转移可回读，身份金额件数不一致或撤销/拒绝不隐式重开。独立定时根任务恢复无渠道记录、余额回滚、CREATED、超租期REQUESTING、UNKNOWN和SUCCESS未本地结算，只调用原退款ID执行核心，并在原锁顺序下复查专用申请；终态渠道及不一致记录待核对。固定最大ID/60秒宽限/5项分页，异常不饿死后续ID，队列失败可重放，日志不含私有标识。4a43042的32项（含2项独立PG并发）已全部通过，run整体因下载扫描器失败仅10/11；尚需B1持久索引与容量、完整故障矩阵、真实微信/支付宝及正式后台运行验收，不能以模拟渠道或代码入口存在关闭此项。
        - [ ] **API-006-ACTIVITY-PINK-B1 恢复扫描持久索引与容量验收**：正式0146/0152/ORM九路径1007索引一致与升级保护已取得自身PG16；9cb862d真实容量分布、3秒语句/500ms锁/5秒空闲LOCAL预算及workerd重试均通过。当前分段候选先取最多1000个专用候选ID，再在同一RR只读快照内按年龄取5条，满5条不越过第5条、否则推进段末，空近期段可continuation且固定全局最大ID。PGlite首段近期1万/10万均hit29+4；102页到达10万近期前缀后的5条到期记录，原数据无改写；纯积压窗口会重复前瞻并增加开销，未做总吞吐承诺。当前分段PG16计划/同快照并发与Queue空段发送失败回放仍待自身CI；MVCC膨胀/缺索引/长锁导致连续超时的运维处理、积压清空SLO及维护窗口受控发布继续开放。不能仅凭LIMIT、过滤0、旧SHA或合成数据关闭此项。
      - [ ] **API-006-ACTIVITY-PINK-C 旧状态页取消确认与结果回读闭环**：9c38154取消中投影及c781f45本人只读结果GET已通过各自PG16 CI；8a98b50前端原团长确认→持久原意图→显式同申请POST/只读GET已通过自身11/11 CI。不把POST成功当到账，未知/无申请快照不丢原意图，过期/转移/下架后仍先读原结果。23项新运行时测试覆盖存储失败/损坏、重复回调、跨账号/重登录/离页/路由/迟到响应及可见控件到期，完整工具链150项通过。CUA真实H5连隔离PGlite验证取消弹窗不提交、确认后余额只退一次、刷新和新标签重入不POST。H5浏览器及三端构建未替代原生真机、真实微信/支付宝和正式角色验收，父项继续开放。
  - [ ] **API-006-MARKETING 13 条短视频/新人活动**：新人 4 条与短视频 9 条的精确路径和核心合同均已收口；整体仍受源数据/运营配置、私有媒体、真实旧端 E2E 和发布门禁限制。
    - [ ] **API-006-MARKETING-NEWCOMER 4 条（核心代码、生产只读与隔离验证已收口）**：补齐 `/marketing/newcomer/product_list`、`product_detail/:id`、`info`、`gift`，保留原 `/newcomer/*` 扩展别名；公开/强制登录边界与 PHP 一致，四类响应均 `private, no-store`。列表恢复 `id desc`、审核可见性和基础商品划线价；详情恢复旧 UniApp 实际依赖的 snake_case、`productValue[suk]`、活动价/基础 SKU 实时库存、可选规格、品牌/标签、保障、描述、评价、收藏、配置及 PHP 顶级订单已购数；礼包不再错误返回仅 info 才有的 `last_time/newcomer_agreement`。生产 13 配置和目录均空，真实合同 3/3、随机 schema 10/10、公共指纹和清理均通过。仍不勾选：需由运营初始化配置、目录、`type=7` SKU和赠券样本，使用真实 token 跑 UniApp/真机/预发，主 Worker仍未发布。
    - [ ] **API-006-MARKETING-SHORT-VIDEO 9 条（核心代码、生产 DDL 与隔离验证已收口）**：精确补齐列表/详情/商品、评论发布/回复/删除、评论关系和视频点赞/收藏/分享，认证边界与 PHP 一致，所有响应 `private, no-store`。列表恢复审核可见性、三类排序、推荐模式专属过滤、商品数、站点/直播/用户关系字段和异步播放计数；封面、视频和头像支持 `/api/assets/:id` 私有签名，旧 HTTPS 引用原样兼容。评论限制 500 Unicode 字符、拒绝控制字符，不再为了兼容额外采集 IP/城市；回复必须属于同一视频并扁平到根评论，删除只允许本人。关系切换使用事务级 advisory lock、既有唯一键和非负计数，修复 PHP 检查后写入的并发漂移；推荐详情额外强制 `is_verify=1`，不复制 PHP 的未审核泄漏。PHP v3.1.1 运行时代码虽读写两表，但本地安装 SQL与同版官方数据字典均无 DDL，因此新增 `video/video_comment` 明确标为 Worker 兼容扩展，不加入 201 表共享数据 manifest。生产 DDL 两遍幂等后为 223 表/3,088 列/721 索引，新增两表 `18/17` 列、7 个含主键索引且均 0 行；随机 schema 12/12，五张业务表摘要不变，临时 schema `0→0`。仍不勾选：没有权威源表或源数据可复制，生产视频/评论均空，封面/视频尚无对象进入私有 R2，未以真实 token 跑旧 UniApp/真机/预发，主 Worker未发布。
  - [ ] **API-006-CHECKOUT 活动订单状态机（核心代码与生产隔离验证已收口，数据/发布门禁未过）**：旧加购别名和活动 SKU `unique` 现可映射到同 `suk` 的基础 SKU；秒杀/拼团使用活动 SKU 价格并校验 `once_num`、累计 `num`，事务内以 UID+活动 advisory lock 阻断不同 key 并发绕过；砍价同时接受活动 ID/参与记录 ID、兼容到最低价的 status 1/3，并按购买件数扣减。建单会同时守卫活动主表、活动 SKU、基础 SKU、基础商品四层库存，活动名称/图片/成本/赠送积分/运费/模板进入权威计算和快照；未支付取消及未发货退款对四层库存/销量/配额原子回补。所有营销订单静默忽略普通用户券，确认/计算接口也把券 ID 传给同源报价，确保普通订单预览与建单一致、营销订单券优惠稳定为 0；非 PC `offline` 在订单写入前失败，支付入口和事务内线下支付再做同一门禁。普通商品积分、实付返积分和分佣只结算 `type=0`，商品关联赠券仍覆盖营销订单，抽奖次数只排除线下及 `type=8`，均与 PHP 事件链一致。现有拼团未支付预留、支付激活、成团、超时失败、自动退款、发货门禁和退款重组选主状态机复核无回退。生产隔离场景证明活动 SKU 权威价格/快照、营销券未核销、H5 线下建单零副作用、三类取消恢复、同用户不同 key 累计限购、四层退款、支付取消竞态、回调幂等和补偿均通过；三个随机 schema 均删除且 `public` 全行/序列指纹不变。仍不勾选：生产只有 2 个 `type=0` SKU、没有 `type=1/2/3` SKU；秒杀 ID 7 为 `once_num=0,num=2`，拼团 ID 27 为 `once_num=0,num=0`；4 个未支付及 2 个已支付活动订单商品快照缺 `activitySku`。CHECKOUT-DATA 已证明这些旧单引用的活动主记录均已删除，并补齐严格拒付及只恢复现存层的取消/退款兼容；上线前仍须取得可信活动 SKU/限购配置、批准并实际取消 4 个未支付单，再做真实旧端、支付渠道、预发与发布验收。
  - [ ] **API-006-CHECKOUT-DATA（生产存量恢复性审计与历史兼容已收口，运营数据和实际处置未完成）**：生产只读逐行确认六个现存快照只有 `{product,sku}`，`suk/unique` 均唯一指向基础 SKU，但没有活动 SKU ID/成本；4 个未支付单引用已删除秒杀 3、拼团 19、砍价参与记录 3→已删除活动 19，2 个已支付单引用已删除秒杀 6、砍价参与记录 4→已删除活动 26。现存购物车 `activity_id=0`，限购下界只属于已删除活动，不能推导当前 7/27 的配置。代码现阻止缺快照订单进入任何支付资金路径；取消可恢复基础商品/SKU、购物车与砍价参与状态而不伪造已删除活动；已支付未发货退款同样只恢复现存基础层，正常新订单仍强制四层回补。生产 Hyperdrive 隔离验证拒付零资金副作用、取消状态证据、退款幂等和正常四层退款全部通过，schema 删除且公共指纹不变。仍不勾选：现存三项活动 SKU及限购需运营明确配置，4 单实际取消会改客户订单/库存且尚未获明确批准，2 个已支付单未自动发起退款，真实渠道/E2E/预发/发布均未完成。
- [ ] **API-007 社区/内容/DIY**：article 7、product reply detail 4 精确合同/本地前端及 diy 8 服务端已收口，但数据、媒体、真实端与发布未完成；社区旧端长尾合同已在既有批次完成，媒体统一走私有 R2。
  - [ ] **API-007-PUBLIC-ARTICLE 7 条公开文章合同**：权威范围固定为 `category/list`、`list/:cid`、`like/:id`、`details/:id`、`hot/list`、`new/list`、`banner/list`；七条均为 `GET + StationOpen + optional auth`。分类和四种列表是只读，详情会增加浏览量，点赞是旧 GET 写合同。本批门禁如下：
    - [x] PHP route/controller/service/DAO/model、ThinkORM 分页、旧 UniApp、原 Worker/schema/manifest/Admin/公众号图文和五套 TS 前端均已逐条只读审计；审计起点确认 7/7 为 actionable exact missing，现已全部精确注册。现有 Admin 文章列表和 WeChat news bundle 不能冒充公开合同；旧第一方客户端实际使用 6 条，`new/list` 没有 wrapper，但仍是公开兼容面。
    - [x] 七条精确路径、`image_input` 逗号拆分（空串为 `[""]`）、上海时间、`cid=0`、公众号文章排除、四种列表不同字段集合、分类“热门”首项、正文 fallback、分类名、商品摘要、`is_like` 与 PHP 点赞成功/异常信封均已恢复；`page=0` 用 1,001 行哨兵保留不分页语义并拒绝超过 1,000 行，分页 limit/offset 在查询前限界。
    - [x] 安全偏离已明确并测试：PHP 因 `parent::search($where,false)` 实际泄露停用/隐藏文章，详情和点赞也无可见性检查；Worker 全面固定 `status=1 AND hide=0 AND is_del=0`。匿名仍可读取但匿名点赞失败关闭；详情浏览量使用原子 SQL，点赞以文章行锁、事务、精确核验的部分唯一关系和关系计数重算修复重复请求/取消失败/并发 counter drift；正式连接固定 `public,pg_temp`。
    - [x] 生产 Hyperdrive 审计只返回文章/分类/正文/公众号引用/商品关系/点赞关系、计数漂移、危险 HTML/媒体 token 和索引聚合，不返回标题、正文、URL、UID、业务 ID或指纹；两枚隔离 token、单飞 advisory lock、随机 schema 的 10/10 真实 service 场景和失败后复核均通过，全表/序列快照证明 `public` 不变，临时 schema/Worker/Secret 已清理。
    - [x] 新 UniApp 已恢复类型化七接口 client、新闻列表/详情、首页入口和登录后点赞；正文不用 `v-html`，采用识别引号的保守 tag/attribute allowlist 后交给受限 `rich-text`，并为图片/表格生成跨端固定安全宽度。类型检查、H5/微信小程序构建和 390×844 mock API 验证通过；安全链接的跨端点击策略仍属于下项真实端门禁。
    - [x] **稳定文章媒体引用与服务端发布清洗（候选完成，未发布）**：Admin 文章保存与公众号图文保存统一重建 HTML tag/attribute allowlist，拒绝主动协议、明文 HTTP、协议相对 URL、事件/style/srcdoc 等属性；附件选择器复制出的 `/api/assets/:id?expires=...&signature=...` 在入库前一律还原为稳定 `/api/assets/:id`。公开列表/详情会再次清洗直接导入的旧正文，只在响应时以 `APP_KEY` 为私有 R2 canonical ref 生成新短时签名，并继续 `no-store`；封面、正文和关联商品图均失败关闭危险历史值。当前生产只读复核仍为文章/分类/正文/公众号引用/文章点赞全 0；随机 schema 真实 service 10/10、public 指纹不变且 schema `0→0`，临时 Worker 已删除。该项只关闭代码与空库结构门禁，不代表历史附件已经迁移。
    - [ ] **源附件、双写切流与热点治理**：生产没有文章或媒体样本，尚未建立源附件→`system_attachment`/R2 对象映射与逐对象 hash/mime/size 对账；PHP/Worker 双写或停写切流、历史 UID 0/孤儿关系清理、详情 visit 的边缘限流/异步聚合/兼容成本决策仍未完成。任何迁移批次都必须只持久化 canonical ref，不能把短时签名 URL 写回正文、封面或微信分享数据。
    - [ ] 生产文章/分类/正文/关系样本与媒体迁移、PHP golden response、真实 token、H5/小程序/APP、预发、影子流量、主 Worker/Pages 发布及发布后观察尚需单独门禁；代码或隔离场景通过不能把本父项解释为线上完成。
  - [ ] **API-007-PRODUCT-REPLY-DETAIL 4 条登录态评价详情/回复合同**：权威范围固定为 `POST reply/comment/:id`、`GET reply/info/:id`、`POST reply/praise/:id`、`POST reply/un_praise/:id`；四条均为强制登录。公开可选登录的 `GET reply/comment/:id` 已存在，不计入本批四条，但必须继续保持父评价可见性和回复隐私边界。本批门禁如下：
    - [x] PHP route/controller/service/DAO/model、旧 UniApp 详情页、新 Worker/schema/路由和新 UniApp 逐条审计；确认 3 条 exact missing，另 1 条为更隐蔽的语义假匹配：现有 `POST reply/praise/:id` 错误调用评价级点赞，旧端实际传入 `store_product_reply_comment.id`。评价级 `reply_praise/un_reply_praise` 两条保持不变。
    - [x] 已恢复旧详情 `reply/product/user/star/is_praise`、根回复列表字段、回复发布和回复点赞/取消点赞；详情和所有写操作拒绝已删除或未审核父评价，回复点赞拒绝已删除回复。请求体先限制为 4 KiB，回复按 Unicode code point 限 1,000 并拒绝危险控制字符。
    - [x] 回复点赞关系固定为 `type=like/category=comment`，事务内锁定回复行并按部分唯一关系幂等写入/删除，再从关系表重算非负计数；不复制 PHP 检查后写入、取消不存在关系仍递减及 counter drift。详情浏览量使用原子 SQL，不复制 PHP 读改写丢更新。
    - [x] 新 UniApp 已提供类型化 client 和可到达的评价详情页；商品详情/评价列表统一读取服务端 snake_case，评价级与回复级点赞调用各自兼容路径，只在请求成功后更新或重新加载计数。类型检查及 H5/微信小程序构建通过。
    - [x] Worker 145/145 文件、875/875 项，PRODUCT-REPLY-DETAIL 定向 2 文件 14/14、双 TypeScript 配置、主 Worker dry-run（4,753.98 KiB/gzip 899.10 KiB）通过；生产 Hyperdrive 只读聚合和随机 schema 真实 service 12/12 通过，7 张 `public` 表及关联序列指纹不变，临时 schema/Worker/Secret 已清理。
    - [ ] 生产仅有 2 条评价且 owner 孤儿 2、回复 0、两类点赞关系 0，无法形成 PHP golden response 或真实正向交互；需取得源用户/回复/关系数据并映射 owner，补 `site_logo_square`，再以真实 token 完成旧端/新端、H5/小程序/APP、预发和影子流量。主 Worker/Pages 发布仍需单独批准，因此本父项保持未勾选。
  - [ ] **API-007-DIY-HOME-WIDGETS 8 条首页组件合同（服务端与前端候选已收口，数据/E2E/发布未完成）**：权威范围固定为公开 `get_diy/:id?`、`diy_version/:id?` 与可选登录 `user_info/video_list/newcomer_list/product_rank/sign/get_suspended`，整组必须先经过 `station_open` 门禁；缺失配置默认开放；已存在值按 PHP `json_decode` 真值判断，所有 PHP 假值及损坏 JSON 返回业务码 `410010`。公开分页在数据库查询前拒绝超过 10,000 的 OFFSET。本批发布门禁如下：
    - [x] PHP 控制器、service、旧 UniApp、当前 Worker 与五套 TS 前端逐条只读审计；确认 8/8 精确路由缺失，现有 v2 DIY、签到、短视频、新人和商品推荐只能复用底层能力，不能冒充旧首页包装合同。
    - [x] 发现并修复 Admin “DIY 装修”把 `content/value` 合并、更新时把 `type=1/3` 降为 `0`、不递增版本且可删除默认/启用页的生产数据破坏风险；强类型 DTO、不可变合同字段、独立内容列、事务行锁、版本/时钟及删除保护必须持续通过测试。
    - [x] 发现播放事件与 USER-CENTER 全局四列唯一索引冲突；非 `play` 关系继续部分唯一，`play` 保持 append-only，播放计数与关系事件在同一事务，显式 conflict predicate 与外部/内嵌迁移定义必须一致。
    - [x] 8 条响应的 PHP 字段、匿名/登录分支、上海日/周边界、DIY 组件变换、新人资格、三榜装饰、短视频 `product_info` 与 GET 播放副作用全部通过单元及随机 schema 真实 service 场景；同时修复新人券 raw/tidy 类型、默认开关 presence、UTF-8 2MB 边界、排行 `sort/presale_day` 与 VIP 权益门禁。PHP 删除非末尾 `pageFoot` 后可能把稀疏数组编码成 object，本实现有意返回稳定紧凑数组。
    - [x] 生产 Hyperdrive 只读审计只返回聚合/结构/存在性，没有配置值、PII、业务 ID 或媒体；前向 `0106` 六索引升级两遍幂等、严格定义回读、三张输入表全部列指纹不变，`search_path` 均把 `pg_temp` 显式置后并验证未限定表解析，写场景仅在随机 schema。最终 28 项 service 断言、24+2 表及 25 序列双指纹全部通过，临时 schema/Worker/Secret 均已清理。
    - [x] **新 UniApp 第一前端子批（候选完成，未发布）**：补齐 8 条类型化 client，以 page ID + `diy_version` 做版本缓存和离线/陈旧缓存回退；renderer 只接受 33 个旧组件名的显式 allowlist，不按数据库字符串动态实例化组件。首页在生产无默认 DIY 或请求失败时安全回退现有静态首页，新增正整数 ID 微页面；首批已安全渲染 `headerSerch/homeComb/swipers/swiperBg/titles/blankPage/guide/richText/videos/menus/pictureCube/tabNav/goodList/articleList/ranking/newVip/signIn/userInfor/community/customerService/pageFoot` 21 类，正文复用 HTML sanitizer 且不用 `v-html`，颜色、图片和内部链接均失败关闭。悬浮导航已接入首页和微页面。定向 21 项测试、类型检查、H5/微信小程序生产构建通过；使用隔离 mock API 完成桌面首页、390×844 微页面、无效 ID 失败态及搜索跳转浏览器验收，没有应用级 console error。
    - [x] **纯编辑内容组件子批（候选完成，未发布）**：逐文件对照旧 `news/hotspot/follow` 与 Admin `activeParty` 配置合同，恢复新闻公告两种样式、带安全链接的图片热区、最多四格活动魔方及公众号二维码弹层；所有集合、文本、间距、圆角和热区坐标有上限，图片/颜色/链接复用失败关闭规则，不下载文件、不执行数据库内容。`activeParty` 是 Admin 可保存且 PHP 会返回、但旧 UniApp 首页从未挂载的源端缺陷，本实现明确修复而不伪称逐像素复刻。定向 23 项测试及类型检查通过；桌面页面、公众号弹层、热区→分类页跳转和 390×844 布局浏览器验收通过，无横向溢出或应用级 console error。
    - [x] **八类业务数据组件子批（候选完成，未发布）**：静态挂载 `bargain/combination/coupon/liveBroadcast/promotionList/seckill/presale/pointsMall`；修复生产公开接口暴露 camelCase、秒杀首页缺 PHP 信封及 UTC 时段误判，补齐旧字段投影和 OFFSET≤10,000/limit≤50。促销固定商品/品牌/分类/标签选择为空时失败关闭，直播仅编译到微信小程序，领券要求登录。390×844 隔离 mock 验收确认 H5 可见 7 类各一次、促销切换、商品详情路由及未登录领券跳登录；Worker 183 文件/1,175 项、双 TypeScript、UniApp TypeScript、H5/微信小程序构建与主 Worker dry-run 通过。Windows runtime 仍在 0 条断言前以 `0xc0000005` 失败。
    - [x] **悬浮导航旧页面覆盖子批（候选完成，未发布）**：旧端全局注册但实际由 68 个页面手工写入 `<home>`；新端将其合并为 38 个现存等价页面并逐页静态挂载，加上首页/专题页共 40 页。配置读取使用 5 分钟共享 promise/cache，按钮、图片和链接继续有界且失败关闭；认证/安全设置页和仍未迁移的旧页面不扩大入口。静态映射测试、390×844 商品列表展开两项菜单→品牌资讯跳转→目标页仍有悬浮入口、UniApp 类型检查及 H5/微信小程序构建通过，没有应用级 console error。
    - [x] **私有 R2 中图变体子批（候选完成，未发布）**：精确定位 PHP `product_rank → getRecommendProduct(...,'mid') → get_thumb_water('mid')`；只对稳定 `/api/assets/:id` 生成 `variant=mid` 的 15 分钟 HMAC URL，签名绑定附件 ID、到期时间、变体名和宽高，尺寸限 `1..2048`。只有 `image_thumb_status` 存在且启用、`thumb_mid_width/height` 都合法时调用 Images binding，以 `scale-down` 保持比例；外部历史 URL 原样返回，配置缺失/关闭、非图片或转换失败均回退原图。转换结果只按附件 ID、源 ETag、固定变体、尺寸和格式进入内部 Workers Cache，客户端仍为 `private,no-store`；Cache 写入使用 `waitUntil`，失败只发低基数 `r2_object_*` 事件。生产只读确认全部 20 个图片配置键缺失，所以当前真实合同只走签名原图，水印未启用且仍作为单独兼容门禁。
    - [ ] **真实数据、跨端和发布门禁**：PC 是否消费同一 DIY 仍需产品决策。生产 `system_dise/video/store_newcomer/store_promotions` 均为空、当前可领取券为 0，原 21 个配置键缺 15 个且 3 键重复，20 个图片配置键全缺；已部署 Worker 的三条业务依赖路由仍为 404，无法做真实内容或登录态 golden E2E。仍需处理 DATA-007/008、源附件→`system_attachment`/R2 对象映射与 hash/mime/size 对账、未来开启水印时的 Images 等价实现、Images 计费/缓存/回退指标、未迁移旧页面本身、真实 token、领券、微信直播、旧端与 H5/小程序/APP、预发、影子流量、主 Worker/Pages 发布及发布后观察，因此父项保持未勾选。
- [ ] **API-008 门店/企业微信/内嵌 Admin（73 条起始缺口，已精确拆分）**：静态起点为 store 12、work 10、`/api/admin` 51；其中已有公开门店列表和核销写入不计缺口。外部写操作必须 Queue 化，不能同步调用第三方。
  - [x] **STORE-A 分类与配送员只读 6 条（代码、生产结构与隔离服务场景完成）**：注册公开 `GET store/category`，以及强制登录的 `delivery/info|statistics|data|order|list`；既有公开 `store/list` 也补回 PHP 外层站点营业门禁。配送身份绑定当前活跃用户，指定门店必须存在唯一有效 `type=1/relation_id=store_id` 关系；门店配送员列表绑定当前唯一活跃店员及其营业门店。统计按 Asia/Shanghai 解释 PHP 时间 token，显式日期最多 366 天、分页最多 100 条且 offset 最多 10,000；订单列表批量读取安全商品快照，不做 N+1、不返回原始 JSON。五条个性化响应均 `private, no-store`，服务内无第三方调用。生产已应用并严格回读 `so_delivery_mobile_active` 局部复合索引；六张依赖表齐全、历史 `oid` 索引可用，随机 schema 真实 service 12/12、六表/六序列 public 指纹不变且临时资源清零。生产门店、店员、配送身份和分配订单当前均为 0，故旧页面 golden response、真实配送员 token HTTP E2E、主 Worker 发布与观察仍是父项发布门禁，不能由合成场景代替。
  - [x] **STORE-B 门店订单 6 条（代码、生产结构与隔离服务场景完成）**：六条 PHP 精确合同已全部注册并统一经过营业门禁、强制用户认证和 `private, no-store`。活跃且已审核的唯一店员及营业门店是所有详情/拆单入口的根授权；订单、售后和发货信息均追加 `store_id` 归属，关闭 PHP 按全局自增 ID 读取的 IDOR。核销只接受 `type=1/2`，客服还必须拥有该订单会话，配送员必须匹配订单配送身份；客户端 `auth=0` 平台旁路和重复身份均失败关闭。拆单复用已加订单结算锁的履约状态机，在同一事务复核店员、门店、配送员、开放售后和商品数量，并写状态审计与通知 outbox；手工快递、门店配送、虚拟发货可同步执行，电子面单/商家寄件和第三方同城必须进入可重试任务而不会在请求内调用第三方。生产 18 张依赖表齐全，退款 3 条均无订单孤儿/归属错配；新建并严格回读 `store_order_refund(store_order_id)` B-tree 索引，业务行和序列不变。随机 schema 真实 service 12/12、public 不变、临时 schema/Worker/Cron 全清理。生产店员、门店订单、客服/配送身份及门店配置当前均为 0，故真实店员/配送员 token、旧端 golden response、主 Worker 发布与观察仍归父项门禁。
  - [x] **WORK-A 企业微信 JS-SDK 2 条（代码、生产配置审计与隔离协议场景完成，生产启用/前端/E2E/发布未完成）**：已注册公开 `GET work/config|agentConfig`，只签名规范化后命中专用 HTTPS Origin allowlist 的 URL，统一去除 fragment；企业级和应用级 access token/ticket 使用两枚独立 Worker Secret、分域哈希 cache key 和限时 KV 缓存，provider 5 秒超时、16 KiB 流式限长、512 字节凭据上限，提前失效仅重取一次，响应 `private, no-store`。生产 PostgreSQL 五个旧企业微信配置键全部缺失，生产 Worker也没有两枚新 Secret且未设置专用 allowlist，所以当前两路按设计 503 关闭；随机 schema 真实 service 13/13、`public.system_config` 不变、临时 schema/Worker/Secret 全清理。正式启用仍需录入 CorpID/AgentID、以 Worker Secret 注入两枚凭据、批准 `WORK_WECHAT_ALLOWED_ORIGINS`；该来源的 CORS 已收紧为仅 `/api/work`，不再扩展到其他 API。旧端 URL 编码/SPA 签名、企业微信真机 E2E及主 Worker发布仍需单独完成。
  - [x] **WORK-B 群/客户本地读 7 条（代码、生产结构与隔离服务场景完成，生产数据/页面/E2E/发布未完成）**：精确恢复 `groupInfo`、`groupMember/:id`、`client/info`、`order/list|info/:id`、`product/cart_list|visit_list`，另提供一次性 `context/challenge|exchange` 安全扩展。PHP `ClientMiddleware` 信任 query `userid`、群路由缺少有效中间件及订单详情未按 UID 约束的越权路径均未照搬；现在仅接受企业微信 OAuth 换得的内部员工身份、一次性 state/verifier Cookie和 5 分钟分 audience Bearer，签发时及每次读取均复核本地活跃成员、客户跟进或群主/群成员关系。订单和退款绑定客户 UID，群路径 ID绑定 token，客户标签只读当前员工跟进关系，其他群计数绑定 CorpID；Work-only Origin 的 CORS 仅开放 `/api/work`。生产 Hyperdrive 13 表/索引只读审计完成，work 成员/客户/跟进/群/群成员及两个非秘密配置均为 0；现有索引足够，未执行生产 DDL或业务 DML。随机 schema 真实 service 14/14、public 指纹不变、临时 schema/Worker/Secret全清理。新 UniApp已有类型化 7 读+2上下文 client，但旧五个 Work 页面、回调编排、真实租户/provider、正式数据、H5/小程序/APP E2E、主 Worker/Pages发布仍是独立门禁。
  - [ ] **WORK-C 企业微信回调 1 条（按 C0～C8 拆分；C0～C8 的代码、生产结构与隔离验收已完成，全部未启用/未发布）**：父项保持未勾选；关系 tombstone、provider 读取、真实 Queue 重放、各类 current 投影及 action outbox 证据仍不等于真实源数据已迁移、真实企微租户回调/provider 正向已验证或生产发布闭环完成。
    - [x] **WORK-C0 可信接收、持久投递与乱序水位**：以 `ANY /api/work/serve` 精确注册，运行时仅允许 GET URL 验证和 POST 回调，其他方法 405；签名严格按 `sha1(sort(token,timestamp,nonce,ciphertext))`，AES-256-CBC 使用 43 字符 EncodingAESKey解码、32 字节 PKCS#7手工校验，并绑定解密尾部 receive-id 与明文 CorpID。POST 最多流式读取 64 KiB，只把 22 个协议字段白名单写入 PostgreSQL，不保存外层签名、nonce、密文或完整 XML；`WECHAT_WORK_CALLBACK_TOKEN/AES_KEY` 只允许 Worker Secret。`work_callback_event/outbox/watermark` 同事务接收，Queue 消息严格只有 `action/outboxId/eventId/eventKey`；Queue 投递失败保留 FAILED 行，五分钟 Cron 补偿，至少一次消费按事件唯一键幂等。每个主体以 advisory transaction lock 和 `(event_time, sequence_rank)` 水位处理乱序，同秒删除/解散优先于更新/创建，未知事件留痕为 IGNORED。生产 PostgreSQL 16 已应用 `0109_work_callback_pipeline.sql`（内嵌 `migration_0115`）两遍，三表 39 列/10 索引严格回读且均 0 行，六张既有业务表全行指纹不变；随机 schema 11/11 覆盖明文回显、重复、伪造签名无写、乱序、同秒删除、未知事件、Queue 失败和资源清理。以上仅是 C0 历史证据。
    - [x] **WORK-C1 外部联系人跟进关系 tombstone（代码/生产 Hyperdrive 隔离完成，未启用/未发布）**：首批只处理 `change_external_contact` 的 `del_external_contact` 与 `del_follow_user`；关系水位已经收紧为 `(CorpID, ExternalUserID, UserID)`，在同一事务唯一解析共享 `work_client`、只把匹配 `work_client_follow.is_del_user` 置 1，并以 `APPLIED/APPLIED_NOOP` 记录终态。目标不存在幂等 no-op，重复/乱序/同秒删除不得复活关系；明确禁止复制 PHP `deleteClient()` 对共享 `work_client` 的软删/销毁，也不影响其他员工 follow。
      - [x] 生产 PostgreSQL 16 已将外部 `0110_work_callback_follow_projection.sql`（Worker 内嵌 `migration_0116`）连续应用两遍：当时的 `wce_status_ck` 精确允许 `APPLIED/APPLIED_NOOP`，并建立 `work_client(corp_id,external_userid) WHERE delete_time IS NULL AND external_userid<>''`、`work_client_follow(client_id,userid) WHERE is_del_user=0 AND client_id>0 AND userid<>''` 两个 partial UNIQUE；目录回读确认两者 `indisvalid=true/indisready=true`。C2 随后把业务终态迁入独立 `projection_status`，两个身份 guard 保持不变。应用前活跃 client/follow 自然键重复均为 0，三张 callback 表应用后仍均为 0 行。
      - [x] 同一生产 Hyperdrive 的随机 schema 场景 20/20 通过，覆盖关系级 subject、重复、乱序、同秒删除、目标不存在、只 tombstone 指定 follow、共享 client 不软删、持久 Queue 失败、故障注入事务回滚后服务重试，以及状态约束/两个活跃身份 guard 精确定义；相关 `public` 表全行多重集与 owned sequence 的 `last_value + is_called` 前后不变，随机 schema 已删除。真实 Cloudflare Queue 平台重投不在这 20 项内，后来由 C2 的专用资源场景单独完成。
      - [x] 执行过程如实保留：首轮只读重复预检 SQL 少右括号，在 DDL 前失败；次轮生产 DDL 成功，但随机 schema 故障注入约束范围过宽而隔离断言失败；第三轮修正 harness 后 20/20 全通过。随后一次临时 Worker 请求因 Secret 版本传播延迟在数据库前返回 403；短暂等待后通过。提交前独立复核又发现迁移阶段完整指纹默认遗漏 callback 三表、只以存在性/行数比较管道状态；修正为迁移前后均覆盖业务表、callback 三表及其 owned sequence 后，最终生产重跑再次得到 DDL 两遍、完整 public 指纹不变及 20/20。所有失败轮均完成 schema/临时 Worker 清理且没有 `public` 业务 DML。最终临时 Worker查询为 code 10007、URL 404；主 Worker仍为 `9f1fd655-e60f-41c1-8280-738bc85d73ef`，正式 Secret 名称集合未变。
      - [x] 最终代码门禁为 Worker 双 TypeScript 配置通过、152/152 个测试文件与 923/923 项通过，注释感知路由审计数值不变，主 Worker/审计 Worker dry-run 均通过。Windows Workers runtime 仍在 0 条断言前以 `workerd 0xc0000005` 退出，不能记为 runtime 通过。
      - [ ] **启用阻塞不计入 C1 隔离完成**：新系统不导入旧 Work 数据；初次启用必须先以真实租户 provider 权威全量快照初始化，并预检活跃自然键重复，否则 partial UNIQUE 会显式阻断。C5 已能以 provider 权威快照恢复目标 tombstone，C6 也已收敛群成员 tombstone，C7～C8 的代码与隔离验收也已完成，但 current authorities、真实租户/provider 和发布闭环仍未验收，主 Worker仍不得启用。
    - [x] **WORK-C2 投影状态机、provider 读取基础与真实重放运维（代码、生产 Hyperdrive 与真实 Cloudflare Queue 隔离完成，未启用/未发布）**：C2 只建立安全 provider 读取边界和可审计运行时，不冒充 C3～C7 的成员、部门、客户、群或标签投影。
      - [x] PHP/TS 对照确认旧 PHP 虽配置客户联系、通讯录和自建应用三类 Secret，实际读取链几乎都复用自建应用凭据；token cache key 含明文 Secret，callback 内同步 provider/DML/欢迎语/标签且多数异常被吞后仍 ACK。新 `EnterpriseWechatProviderClient` 明确拆成 `company-jssdk/agent/directory/external-contact` 四个最小权限 scope；后两者必须使用新增 Worker Secret，绝不回退复用 JS-SDK Secret。cache key 只含 `scope+CorpID+AgentID+Secret` 的 SHA-256 指纹，值只存 token/过期时间并提前 300 秒失效，同一 isolate 跨 client 的并发 miss/refresh 合并。
      - [x] provider origin 和 endpoint 封闭为企业微信只读 API，统一 GET/POST、禁止重定向、5 秒 Abort（覆盖 response body）、请求 64 KiB、响应按端点 16～512 KiB且绝对 1 MiB、fatal UTF-8/JSON object/credential/TTL 校验。`errcode` 只接受 JSON number safe integer，缺失、`null` 或字符串码按 provider envelope 不可用且可重试处理。仅 2xx 的 operation-specific `errcode` 可成为 `not_found`：成员 `60111`，部门 `60003/60123`，客户 `40096/84061`，群 `40050/86003`，以及仅在带 tag/group 过滤条件时的标签 `40068`；裸 HTTP 404 和无过滤标签 `40068` 均为 `terminal`。408/425/429/5xx、网络/超时及明确 busy/rate-limit 为 `retryable`，且 HTTP 状态优先于响应体中的不存在码。`40001/40014/42001` 只在 2xx 响应时刷新当前 scope token一次；同 isolate 跨 client 的并发刷新先登记只广播纯结果/错误元数据的 deferred，再由 leader 在自身请求栈做 KV generation 复核，因此定向竞态只删除/获取一次。该 singleflight 只是 same-isolate best-effort，跨 isolate/POP 仍可能重复，不能宣称 exactly-once。错误只含 operation/kind/code/status/有界 Retry-After，不保留 URL、query/body、provider 文本、token、Secret 或 PII；client 不自行循环业务重试，交由持久 Queue 状态机决定。
      - [x] 外部 `0111_work_callback_projection_state.sql`（内嵌 `migration_0117`）把 callback 管道与业务投影物理分列：新运行时代码只写 `RECEIVED/PROCESSING/ORDERED/FAILED/DEAD` 五种管道状态；数据库在 expand 期的 `event.status` CHECK 暂时精确允许这五态加旧 writer 的 `APPLIED/APPLIED_NOOP/SUPERSEDED/IGNORED` 共九态，待新 Worker 100% 发布且旧 writer 全部退出后另做 contract。`projection_status` 精确允许 `PENDING/PROCESSING/REFRESH_REQUIRED/APPLIED/APPLIED_NOOP/SUPERSEDED/IGNORED/FAILED/DEAD`。旧 `ORDERED/APPLIED/...` 安全回填后归一；非 C1 的已识别事件现在明确为 `ORDERED + REFRESH_REQUIRED`，未知事件为 `ORDERED + IGNORED`。CorpID 继续由 callback/JS-SDK/context/provider 共用 1～18 字符验证；callback 列的 64 只是存储容量。
      - [x] 生产 PostgreSQL 16 经 Hyperdrive 连续应用 C0/C1/C2 DDL 两遍：callback event 为 20 列；两个 CHECK 均目录确认 `convalidated=true`、`connoinherit=false`、`conkey` 精确目标列且完整表达式相等，`wce_projection_status_time` 的 btree/列序/无 predicate 精确回读。第二遍两个约束 OID 与索引 OID/relfilenode 均不变；三张 callback 表前后均 0 行，活跃 client/follow 重复组均 0，完整业务行与 owned sequence 指纹不变。随机 schema 27/27 覆盖九种 legacy 状态精确回填、第二遍全部 legacy 行 `ctid/xmin` 不变、C0/C1 回归、`REFRESH_REQUIRED` 分离、约束/索引精确 shape、故障全事务回滚和资源清理；结果为 event/projection 组合 `ORDERED/APPLIED=3`、`ORDERED/APPLIED_NOOP=1`、`ORDERED/REFRESH_REQUIRED=3`、`ORDERED/SUPERSEDED=2`、`ORDERED/IGNORED=1`、`RECEIVED/PENDING=1`，outbox `COMPLETED=10/FAILED=1`。
      - [x] 专用临时 Queue `cinashop-work-c2-audit-source` 以 `max_retries=1` 故意让严格四键 callback outbox 在同一 source message ID、同一 body SHA 下 attempts `1→2` 失败并自动进入专用 DLQ；严格两键 dispatch 直接进入同一 DLQ。两者均归档为 `ALLOW`，outbox 重复归档收敛至 occurrence 2；人工重放作为新投递/attempts 1、同一 body SHA，由真实 `consumeWorkCallbackQueueMessage → EnterpriseWechatCallbackService` 精确处理一次，终态为 event `ORDERED`、projection `REFRESH_REQUIRED`、outbox `COMPLETED`、`attempt_count=1`、watermark 1，并记 `REPLAYED/replay_count=1`；dispatch 记 `RESOLVED`。生产 227 张表中 13 张关键表行指纹与 12 条相关序列完全不变，隔离 schema、临时 Worker/Secret、source/DLQ/unarchived Queue 均删除，两个临时 workers.dev URL 均返回 404；正式 `cinashop-order` 保持 1 producer/1 consumer，正式 DLQ 与 unarchived Queue 保持 0/0，主 Worker仍未发布本批代码。
      - [x] 最终代码门禁为 Worker 双 TypeScript 配置通过、153/153 个测试文件与 939/939 项单元测试通过；注释感知路由审计保持 PHP 1,904、TS 1,446、精确/可执行 746/728、可执行缺口 1,154。主 Worker、callback audit 与 Queue audit 的 minify dry-run 分别为 2,762.25/686.92、695.81/138.76、711.56/144.04 KiB（upload/gzip），三者均精确绑定生产 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 且没有实际部署主 Worker。`git diff --check` 无错误，工作区无一次性 token 文件。Windows runtime 套件仍在 0 个测试/0 条断言前因 `workerd 0xc0000005` 启动失败，明确记为环境失败而非通过；需在 Linux/受支持主机补证。
      - [ ] **启用边界不计入 C2 隔离完成**：生产尚未配置 directory/external-contact Secret、CorpID/AgentID或真实租户数据；C2 的 not-found/429/5xx/网络分类用确定性 mock 验证，没有为测试触发真实租户限流。C3～C7 已按“短事务 claim → 事务外 provider → 短事务复核 lease/watermark并原子应用快照”接入成员、部门、客户、群和标签投影，C8 action outbox 也已隔离验收。由于 provider credential 位于 query string，仓库已显式设置 `[observability.traces] enabled=false`；注入任一 `WECHAT_WORK_*` provider Secret 或启用投影前，必须回读 exact Script Settings 证明 traces 仍关闭。未来若开启 traces，必须先经 Script Settings API 设置并回读 `observability.redact_query_string=true`，缺失或 false 都阻断部署。主 Worker继续未发布，真实源数据、真实租户/provider、预发与发布门禁仍未完成。
    - [x] **WORK-C3 成员 current 投影（代码、生产 Hyperdrive 结构与隔离服务验收完成，未启用/未发布）**：已覆盖 `create_user/update_user/delete_user`，以稳定 member identity 加 UserID alias 收敛 provider 权威快照、扩展资料和部门关系；删除由回调权威 tombstone 且不构造 provider，成员 `60111` 只表示“不可见或不存在”，绝不据此删除。可选字段按 presence 保留或显式清空，部门关系只在完整数组下全量替换；provider I/O 位于数据库事务外，最终短事务复核 lease、latest-seen fence 与身份闭包后才原子应用。
      - [x] 新增 `work_member_current`、`work_member_identity_alias`、`work_member_other_current`、`work_member_relation_current` 四张 Worker-owned current 表；旧 `work_member/work_member_other/work_member_relation` 继续作为不可改写的 legacy/import 证据。生产四张新表迁移前后均为 0 行，没有写入真实成员标识或 PII。
      - [x] UserID rename 使用不可变 direct edge：pending 边反向保存在 target alias，resolved 边正向保存在 source `RENAMED` alias，`canonical_userid` 指向 immediate next 而非压平到 terminal；`link_*` 是不可变边 fence，`last_*` 只表示可前进的 latest-seen。若历史 UserID 后续事件造成 `last > link`，运行时按 contamination/reuse 歧义失败关闭，不把 later event 冒充 rename edge。`0113` 的数据库 trigger 进一步禁止已进入 `RENAMED` 的 alias 改写身份、canonical 或 link fence。
      - [x] 最终审查在提交前发现并修复一个 P1：初版 resolved-forward 顺序曾错误使用可变 `last_*`，可能在旧 UserID 后续事件后错误合并身份。修复后多跳 lineage 先冻结每条 direct edge，再把 pending fence 搬到对应 source alias；严格校验 edge 单调顺序、同一 member、terminal current 与 active alias，并保留全事务回滚。`0113` 对任何已有 `RENAMED` 行拒绝猜测或回填，必须先人工处置。
      - [x] 生产 PostgreSQL 通过 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 执行 `0113_work_member_resolved_rename_fence`：请求 `2efcfb4e-ac21-4d69-827c-c7ebc46534a1`，两遍迁移/10 项检查全部通过；231 张表、209 条序列的全量 digest 前后均为 `50c18bb8e9fa52b50ed89df5d7755541c4c940d5d0f23faed5dd5ad3f878c306`，legacy 3 表/1 序列 digest 前后均为 `1b9e7efced974351d58cff26850103e811d209ddeaa456034a5e7c348c3a2ae8`，四张 current 表前后均 0 行。迁移后只读请求 `4fc11cd7-5667-469a-9d55-6c9534fab9a4` 确认 `0109`～`0113` 全部 ready、guard function/trigger ready、临时 schema 为 0。
      - [x] 最新随机 schema 隔离请求 `47cf8ff5-2447-4105-8b0d-8ab4ba4325a0` 完成迁移两遍、101 个对象、7 张稳定表和 6 条稳定序列复核；`0112` 负向 22/22、`0113` 预检 2/2、`0113` guard 负向 6/6、direct SQL 11/11、真实 Drizzle/callback service 33/33 全通过，`public` 231 表/209 序列 digest 保持不变且临时 schema 已删除。
      - [x] 最终仓库门禁为 Worker 双 TypeScript 配置、155/155 个测试文件与 960/960 项单元测试通过；主 Worker/C3 审计 Worker dry-run 均通过但没有部署主 Worker。一次性审计 Worker 已删除，URL 为 404、Cloudflare API 为 `10007 Worker 不存在`，一次性 token 未写入文件。静态路由覆盖仍只有 39.2%，不因 C3 完成而抬高整体迁移结论。
      - [ ] **C3 启用/运维阻塞不计入本项隔离完成**：生产没有真实企业微信租户数据、CorpID/AgentID、directory Secret 或回调凭据，也没有真实 provider 网络正向/`60111` 验收；完整通讯录全量对账、持久回放容量与延迟指标仍缺。成员 authority 保持关闭，主 Worker未部署；C5～C8 已完成代码与隔离验收，但真实数据/租户、预发和发布仍阻断主回调启用。Linux `workerd` 门禁已由 Actions 关闭，Windows 本机仍在 0 条断言前以 `0xc0000005` 崩溃。
    - [x] **WORK-C4 部门 current-state 投影（代码、生产 Hyperdrive 结构与隔离服务验收完成，未启用/未发布）**：覆盖 `create_party/update_party/delete_party`，以 `(CorpID, DepartmentID)` 为身份；三张 Worker-owned current/fence/leader 表只做 expand-only 前向扩展，旧 `work_department` 保持导入证据且未被改写。
      - [x] 回调采用 latest-seen claim → 事务外 directory provider → exact-fence apply 三相顺序；create/update 只接受完整 provider 快照，`60003/60123` 只进入 refresh，绝不据此删除。delete callback 是目标部门 tombstone 的权威，只清该部门负责人，不级联删除子部门或成员关系。父节点可先以 `UNRESOLVED` placeholder 落地；写入阻断环、跨 Corp 父节点、第二个 ACTIVE root 和超过 256 条父边，目录只展示 current/fence/callback 完全一致且整条 ACTIVE ancestor closure 可达的部门。负责人声明仅是 current 数据，不构成授权。
      - [x] 生产只读基线请求 `43e37f83-fa8c-47a6-938f-cfdc110bfb2a` 确认 legacy 部门和 callback 均为 0 行、三张 C4 表尚不存在，部门/Corp/自然键/父树/负责人 JSON 异常全部为 0；生产 `0114_work_department_current_projection` 请求 `b07498c2-08fe-49fe-8092-4e3e5fd0e8d8` 将 exact migration 连续执行两遍并通过 10/10 断言。legacy 表/序列 digest 前后均为 `f703fe570ea3a667e7996f51f5a2b1ad92c1caa61e211d45f9eb6df3f1255f00`，既有 231 表/209 序列 digest 前后均为 `50c18bb8e9fa52b50ed89df5d7755541c4c940d5d0f23faed5dd5ad3f878c306`，无业务 DML或新序列。
      - [x] 最终随机 schema 请求 `4c00e71c-d0e9-4d64-9cde-bef1d61e378c` 通过 migration 4/4 与 direct-service 20/20，共 24/24：覆盖 child-first placeholder、父节点补齐、uint32 排序、负责人全量替换、provider not-found refresh/no tuple、目标 tombstone、后代/成员边保留、旧响应 supersede、复活、祖先闭包、同 department ID 跨租户隔离、第二 root唯一约束、环和跨 Corp FK；provider 全部为 mock，企业微信网络调用 0。随机 schema 已删除，`public` 摘要保持不变。
      - [x] 首个隔离请求 `8abed223-bfdf-4f77-9540-8b9952ffcb1f` 在首遍迁移失败并清理；诊断请求 `47667834-f50e-414b-8a4a-f920ac25b77c`、`89e7a6ed-2fd7-459a-ace6-a8feebad751f` 证明逐列 `pg_get_indexdef` 不包含 DESC，方向实际位于 `pg_index.indoption`。最终 guard 以列名加 exact `indoption='0 0 3 0'` 校验，避免把正确索引误判为漂移，同时继续验证 predicate/NULL 语义；失败轮不计通过且均未改变 `public`。
      - [x] 终态只读请求 `90ecdeec-8ab8-47df-b54f-5e4824df5c77` 确认 callback/current/fence/leader 均为 0 行，20/16/9/6 列、31 个相关约束、16 个 callback+C4 索引、无 invalid index、临时 schema 为 0；最终全库目录为 234 表、3,235 列、776 索引、221 主键、209 序列。Worker 双 TypeScript、C4 定向 64/64、全量单测 975/975、schema/route audit 与主/临时 Worker dry-run 均通过；临时审计 Worker、Secret 已删除且 URL 返回 404，主 Worker未部署。
      - [ ] **启用/运维阻塞不计入 C4 隔离完成**：生产 legacy 部门为 0 行，只能证明当前无样本，不能宣称真实历史部门数据已迁移；department authority 保持关闭，成员关系 current 尚无 department FK。仍缺真实租户/provider 正向、完整通讯录扫描与 reconciliation 游标、积压/延迟指标、主 Worker发布和观察；C7～C8 虽已完成代码与隔离验收，这些真实运行门禁仍继续阻断 WORK-C 父项完成。
    - [x] **WORK-C5 客户、跟进人与跟进标签权威快照（代码、生产 Hyperdrive 结构与隔离服务验收完成，未启用/未发布）**：覆盖 `add_external_contact/edit_external_contact/del_external_contact/del_follow_user`，从 external-contact provider 穷尽游标并原子收敛客户、全部 follow 和 tags；旧 `work_client/work_client_follow/work_client_follow_tags` 继续作为导入/兼容证据，不会被快照覆盖。
      - [x] 五张 Worker-owned 表把 client profile latest-seen、每员工 direct fence、follow current 和 tag current 物理分离；一条员工删除只 tombstone 目标关系，只有全部已知 ACTIVE follow 终止才把完整客户置为 INACTIVE。非目标 tombstone 不会被另一员工快照复活，快照遗漏也不会无证据删除关系；只有目标员工较新 add/edit 可显式恢复。
      - [x] 回调采用短事务推进 profile/relationship fence → 事务外 provider 全量读取 → 短事务复核 exact fence 并应用。provider 最多 32 页、每页 500 follow、总计 5,000 follow/20,000 tags/4 MiB；缺省 tags 是权威空集，个人标签 `type=2` 可无 `tag_id`，游标循环、跨页资料漂移和跨越更新 profile fence 的迟到响应均可重试。`40096/84061` 只进入 refresh，不推导删除；两个 delete 完全不构造 provider。
      - [x] current-first Work context 在同一成员锁+客户锁事务内校验 member、client、profile fence、callback APPLIED 终态、direct/snapshot follow 来源和 tags；JWT 固化 `client_projection_source`，旧 token 或 legacy/current 来源翻转均失败关闭。Admin client catalog 的 current/legacy fallback 与总数在单条 PostgreSQL 语句快照内计算，越界页仍返回精确 count；authority-off 一旦存在 current footprint 就停止泄漏 stale legacy 目录。
      - [x] 生产 `0115_work_client_current_projection` 首次请求 `cceea909-1d2c-4056-9118-707de99e2bcf` 连续执行两遍：`234→239` 表、`209→210` 序列，五表均 0 行，legacy 三表行/MVCC不变，39 个约束和 57 个目标对象精确；增强封闭表面/索引/predicate 校验后请求 `b53c5f91-e85d-46dd-83c7-13aa4e9bad45` 再执行两遍，仍为 239/210、目标 metadata digest `e74ce06d0f983f1e9c4646534a4af18e70acf7352e62bea6dc4f7fe4875c43d8`。终态只读请求 `f7b5618b-51e0-4ad6-8c95-d33fe9935787` 的目录 digest 为 `373aa9bd034ba49a601fe3c96be50d4104b46e5434011324329c1b62b3e29dea`，8 张 legacy/current 相关表均 0 行。
      - [x] 最终随机 schema 请求 `c44dc50e-a6b5-497d-b3ab-2359f5f2b994` 通过 migration 4/4、direct service 12/12、current context 6/6，共 22/22；包含 relationship delete、跨关系乱序、目标恢复、遗漏不删除、direct provenance、tag replace、迟到响应跨 profile fence 必须重拉、authority-off fail-closed。临时 schema 删除，`public` digest 保持 `373aa9…`；临时 Worker/Secret 全删除且 URL 为 404，主 Worker未部署。
      - [ ] **启用/运维阻塞不计入 C5 隔离完成**：生产 legacy/current 客户、follow、tags 全为 0 行，尚无真实租户、external-contact Secret/full-visibility、CorpID/AgentID或 callback 凭据；所有 provider 场景均为确定性 mock，企业微信网络调用 0。client authority 保持关闭；C6 群、C7 标签目录和 C8 action outbox 均已完成代码与隔离验收，但商城用户真实 UID映射、当前媒体、provider 全量初始化、真实远端副作用与发布闭环仍未完成。
    - [x] **WORK-C6 客户群与群成员投影（代码、生产 Hyperdrive 结构与隔离服务验收完成，未启用/未发布）**：覆盖群 `create/update/dismiss`，以 `(CorpID, ChatID)` 为稳定身份；create/update 只接受 provider 完整群/成员快照，dismiss 为零 provider 的 callback-authoritative 终止态，不再按 `UpdateDetail` 盲加减人数，也不硬删成员历史。
      - [x] 三张 Worker-owned 表保存 group current、group latest-seen fence 和 member current；成员 omission 写 `LEFT` tombstone，重入恢复 `ACTIVE` 但不抹除累计离群证据，dismiss 把仍活跃成员写 `DISMISSED`。终止序列级别压过任何不可能的更晚 create/update 时间戳。
      - [x] expand-only `0116` 在生产通过两遍：`239→242` 表、`210→212` 序列；26 约束、11 索引、40 个封闭对象、2 条 identity sequence 精确回读，第二遍 OID/relfilenode/定义/空 tuple 不变，legacy 群/成员 MVCC 指纹不变。
      - [x] 同一生产 PostgreSQL 引擎随机 schema 最终通过迁移 6/6、投影状态机 13/13、current-first 上下文 5/5，共 18/18；覆盖 omission、幂等 no-op、重入、乱序、跨 fence 在途响应、provider not-found refresh-only、同秒 dismiss、解散后旧 token、authority-off 和非成员失败关闭。
      - [x] Work context 与 Admin catalog 均 current-first；群 token 固化 `group_projection_source`，current/fence/callback 六元组不一致或 current authority 关闭时不回退 legacy。群授权、群锁、详情/成员读取现在位于同一请求事务，Hyperdrive `SET LOCAL search_path` 与读取快照一致。
      - [ ] **启用/运维阻塞不计入 C6 隔离完成**：生产 legacy/current 群和群成员均为 0 行，尚无真实租户、external-contact Secret/full-visibility、CorpID/AgentID或 callback 凭据；provider 网络调用为 0。group authority 保持关闭，主 Worker未部署；C7/C8 的代码与隔离验收已完成，但 provider 全量初始化、真实远端副作用、预发和发布闭环仍未完成。
    - [x] **WORK-C7 企业客户标签与批量同步结果（代码、生产结构与隔离服务验收完成，未启用/未发布）**：以 `(CorpID, StrategyID, remote string ID)` 建立可审计 tag/group current-state，覆盖 `create/update/delete/shuffle`、目录遗漏 tombstone 与 callback-authoritative delete；`batch_job_result` 的 `sync_user/replace_user/invite_user/replace_party` 只保存有界审计元数据并明确保持 `IGNORED`，不冒充目录已经对账。
      - [ ] **C7 启用/运维阻塞**：生产 legacy/current 标签为 0 行，尚无真实租户、最小权限 provider Secret/full-visibility、provider 全量初始化与正向/不存在主体证据；tag authority 保持关闭，主 Worker未部署。
    - [x] **WORK-C8 欢迎语、自动标签与商城用户关联（代码、生产结构、随机 schema 与 Admin 脱敏处置面完成，未启用/未发布）**：三类远端/本地动作已拆为独立事务 action outbox/Queue，具备幂等键、部分成功、429、结果未知、人工重放、回调 payload 有界保留/脱敏和审计终态，不在 callback HTTP 请求内同步执行。
      - [ ] **C8 真实租户发布闭环**：取得 CorpID、最小权限 provider Secret、两枚回调 Secret、当前媒体映射和真实测试租户后，完成真实回调、provider 正向/不存在主体、预发、影子流量、明确发布批准、主 Worker/Pages 发布及发布后观察。
  - [x] **ADMIN-A 内嵌订单/代客下单 32 条（候选代码、静态合同与服务回归 32/32 已收口，未发布）**：覆盖统计/暂存、配送/拆单/面单、改价/备注/线下支付/退款/核销，以及代客购物车、确认、优惠券、创建、支付和状态。全部使用显式受限 Admin session 与动作级 ACL；资金/退款复用现有账本，不因路径位于 `/api/admin` 自动授权。真实旧端 E2E、支付 provider、预发与发布仍由 REL-001/002 管理，不把候选代码完成写成生产完成。
    - [x] **ADMIN-A-STAT 统计只读 5 条（代码、静态合同与服务回归完成，未发布）**：精确恢复 `GET order/statistics|staging|data|time|time/chart`，统一使用受限 Admin session、`order.view` 和 `private, no-store, max-age=0`。统计只读取生产 Hyperdrive 指向的 PostgreSQL，按 Asia/Shanghai、根订单 `pid=0`、双软删除和有效退款状态聚合；每日明细最多 100 条、跨度最多 3,660 天，周期只接受 1/7/30。实现不复制 PHP 把 `uid=0` 错当真实用户过滤以及可能重复计入拆分子单的偏差，也补齐退款/商品日志软删除过滤。本批无 DDL、DML、Queue、第三方调用或部署；候选发布仍需单独批准。
    - [x] **ADMIN-A-FULFILLMENT 订单运营/履约 12 条（12/12 已收口，未发布）**：逐条复用现有履约/核销状态机、任务/outbox 和 Admin 审计；电子面单签发不在请求内执行，扫码兼容合同仅查询可核销订单，实际核销仍由既有完整写状态机负责。
      - [x] **ADMIN-A-FULFILLMENT-READ 履约只读 5 条（代码、静态合同与服务回归完成，未发布）**：精确恢复 `GET delivery/gain/:orderId`、`delivery`、`delivery_info`、`export_all`、`split_cart_info/:id`。全部使用受限 Admin session、`order.view` 与 `private, no-store`；订单号/整数 ID/分页严格有界，只读有效订单、平台配送员、直接数据库配置、启用物流公司和可拆商品。物流公司只返回 `id/name/code`，主动删除 PHP `partner_id/partner_key/net/net_name/key/account` 等凭据或内部字段；全拆主单只接受唯一待发货平台子单，畸形旧商品快照降级为 `null` 而不让整单失败。本批无 DDL、DML、Queue、第三方调用或部署。
      - [x] **ADMIN-A-FULFILLMENT-LOCAL 本地订单操作 3 条（代码、静态合同与服务回归完成，未发布）**：精确恢复 `POST price`、`POST remark` 和 PHP 原拼写 `POST wirteoff/records/:id`。改价只接受绝对金额、最多两位小数，锁根结算域与目标订单，只允许未支付有效唯一订单，并跨重复改价保持原价；备注长度最多 512、去空白、行锁更新，状态审计不记录备注正文。核销记录虽沿用 POST，但只读且映射 `order.view`，分页/商品类型/快照字节数有界，购物车快照同时绑定订单，响应只投影页面必需字段，不返回核销码或操作员身份。本批无 DDL、Queue、第三方调用或部署；未对生产业务数据执行测试写入。
      - [x] **ADMIN-A-FULFILLMENT-WRITE/PROVIDER 4 条（代码、静态合同与服务回归完成，未发布）**：精确恢复 `POST delivery/keep/:id`、`GET export_temp`、`PUT split_delivery/:id`、`POST order_verific`。手工快递、平台配送和虚拟发货复用 `SupplierFulfillmentService` 的结算锁、售后/拼团/预售门禁、面单冲突、通知 outbox 与同事务 Admin 审计；平台配送员姓名/电话从锁定的有效数据库身份覆盖客户端声明。电子面单只创建持久任务并交给 Queue，旧请求没有幂等键时由服务端生成 UUID；模板目录经固定 HTTPS provider、超时/响应限长和 `title/temp_id/pic` 白名单返回，图片仅接受 HTTPS。`order_verific` 按 PHP 语义先查用户条码、再查 12 位订单核销码，只以认证 Admin actor 做最多 20 单的只读查询，忽略 body `auth`；真正写核销仍走既有 `POST /admin/order/writeoff` 的行锁、部分核销、旋码、结算和审计状态机。本批无 DDL、生产 DML、真实 provider 调用或部署。
    - [x] **ADMIN-A-REFUND 退款/资金 4 条（代码、静态合同与服务回归完成，未发布）**：精确恢复 `POST offline`、`POST refund`、`POST refund_agree/:id`、`POST open/refund/:id`。`offline` 是管理员确认已收线下款，复用唯一 paid transition、支付 outbox、活动/拼团门禁和同事务 Admin 审计；重复同渠道收款幂等，其他已支付渠道冲突失败。现有售后同意退款只接受售后权威全额，不复制 PHP 对同一售后行重复增量部分退款；余额/微信/支付宝继续走 CORE-002 的结算锁、渠道 ledger、稳定退款号、查询后重试和回调/主动对账。主动整单/拆单退款按不可变商品快照、历史已退件数和订单实付在事务内计算金额，必须逐分等于提交值，使用确定性内部退款单号收敛重试；`refund_agree` 只把退货售后推进到等待寄回，不移动资金。四条均从认证 Admin 取 actor，`offline` 要求 `order.manage`，其余要求 `refund.manage`，响应 `private, no-store`。本批无 DDL、生产 DML、真实支付 provider 调用或部署；生产 Hyperdrive 配置只读核验通过，生产聚合事务因 Windows workerd 启动崩溃而未执行，不能冒充生产业务 E2E。
    - [x] **ADMIN-A-ASSISTED 代客下单 11 条（代码、静态合同与服务回归完成，未发布）**：精确恢复 `cart/:uid`、`cart/add/:uid`、`cart/del/:uid`、`cart/num/:uid`、`place/list`、`confirm/:uid`、`computed/:key/:uid`、`coupons/:uid`、`create/:key/:uid`、`pay/:uid`、`pay/status`。新增独立 `order.assisted` 权限；Admin、目标 UID、游客 label、购物车精确集合、KV 确认快照、服务端报价、库存、优惠券、订单 `staff_id/is_channel`、支付查询和 immutable status 审计全链路绑定。游客 label 只作 scope、不作凭据；缺 label 不能跨游客猜测，多门店缺 `store_id` 仅在唯一有效门店时回退。购物车变更与订单创建使用 PostgreSQL 锁和原子库存认领，现金/零元复用 paid/outbox 事务入口，微信/支付宝 provider I/O 位于事务外，列表的购物车/退款/优惠券范围均批量读取。本批无 DDL、生产 DML、真实 provider 调用或部署；生产只读聚合在 SQL 前被 Windows workerd `0xc0000005` 阻断，不能冒充生产业务 E2E。
  - [x] **ADMIN-B 内嵌商品 7 条（代码、静态合同与服务回归完成，未发布）**：精确恢复分类树、管理列表、上下架、标签树、规格读取、规格价格/库存更新和分类/标签批处理。全部经过现有 Admin session；GET 要求 `product.view`，POST 要求 `product.manage`，响应均为 `private, no-store`。上下架与分类/标签替换在 2 秒锁/5 秒语句超时的事务和商品行锁中执行，并同步有效未支付购物车与分类关系；回收站商品失败关闭，上架还要求已审核。规格更新只接受当前商品已有 `type=0` SKU、必填唯一标识及有界非负价格/整数库存，锁定商品和 SKU 后重算权威总库存与最高价并写库存差额记录；不能用客户端 `unique` 跨商品改价。批处理只开放旧移动端实际使用的分类/商品标签两类，验证平台有效关系后原子替换主表 CSV 与关系表；PHP 后台隐藏的 3～9 类批处理不冒充本项完成。当前实现未来发布后会直接使用 Hyperdrive `9748c294e21c49a99579c9cef70102e0`，本批没有 DDL、Queue、第三方调用、生产 DML 或部署。
  - [x] **ADMIN-C 内嵌售后 3 条（代码、静态合同与服务回归完成，未发布）**：精确恢复 `GET refund_order/list`、`GET refund_order/detail/:uni` 与 `POST refund_order/remark`；三条均使用受限 Admin session，读要求 `refund.view`、备注要求 `refund.manage`。列表保留 PHP 的分页、售后单/快递/用户/商品/原订单模糊搜索、`refundTypes` 组合状态、`apply_type` 与时间筛选；详情只按有效售后关联的原订单及同一 UID 读取，并兼容退款主键或公开退款单号。备注只接受公开退款单号和 255 字有界文本，在 2 秒锁/5 秒语句超时、退款行锁和单事务下更新，并写不含备注正文的 `admin_refund_remark` 不可变状态日志；重复同值为幂等 no-op。已取消/已删除售后从读写面失败关闭，这是对 PHP 宽松行为的安全收紧。退款同意/拒绝及资金动作仍走既有 CORE-002 状态机；生产只有未来明确发布后才会经 Hyperdrive `9748c294e21c49a99579c9cef70102e0` 使用这些接口，本批没有生产 DML、探针或部署。
  - [x] **ADMIN-D 内嵌用户 8 条（代码、生产 DDL、静态合同与服务回归完成，未发布）**：精确恢复用户标签、可赠/用户券、分组、等级、余额/积分调整、五类用户更新及地址列表/默认地址。全部使用受限 Admin session；GET 要求 `user.view`、POST 要求 `user.manage`，响应均为 `private, no-store`。读取固定平台标签范围与可见等级，地址只按路径 UID 和 `is_del=0` 查询；送券只接受有效 `receive_type=3` 模板和最多 100 个活动用户，用户→券模板固定锁序、库存不足整批回滚，不复制 PHP 静默截断。余额/积分扣减钳制到 0，余额写 `user_money`、积分写 `user_bill`，不复制 PHP 积分可变负；余额/积分、会员天数、送券强制 UUID-v4 `Idempotency-Key`，专用 `admin_user_write_replay` 只保存管理员/操作/请求摘要和有界证据 ID，数据库唯一键阻断并发重复，`system_log` 另存内容脱敏审计。会员时长同时写 `other_order`/状态记录；等级、分组、标签采用行锁下原子替换，标签可显式清空。外部 `0119` 与 Worker 内嵌 `0125` 的同义幂等 DDL 已按 DB-005 在生产执行两遍并精确复核，表为空且仓库/生产均为 263 表；新写路由仍须等待真实最小权限角色 E2E 和明确发布批准。
  - [x] **ADMIN-E ERP 配置 1 条（代码与静态合同完成，未发布）**：精确恢复 `GET /api/admin/erp/config`，强制现有 Admin token 与 `config.view` ACL，响应设置 `private, no-store` 且只返回 `{open_erp:boolean}`。共享能力服务只读取 `erp_open`，缺失/`0`/非法值失败关闭，不读取或返回 ERP token/secret/account；定向测试覆盖开关、空配置、真实响应 envelope/cache header、路由与权限。实际 ERP 认证、同步、真实账号/沙箱和发布仍归 ERP-001/002 与 REL-001/002。
- [x] **API-009 退役动态统计脚本入口**：旧 `GET /api/get_script` 将运营可编辑的 `system_statistics` 原样作为 JavaScript 返回，旧 UniApp H5 再用动态 `<script>` 执行；新五端和 Worker 均无调用。为避免恢复持久化任意脚本与第三方供应链执行面，已按路由、控制器、配置表单和旧调用点四项源证据正式退役；未来分析能力只能以固定 provider、用户同意和 CSP 约束的显式集成重新设计。专项测试同时阻断当前源码重新暴露或消费该合同；API 面可执行缺口 `35→34`。
- [x] **API-010 公共启动配置5条（代码与静态/服务回归完成，未发布）**：精确恢复 `GET wechat/get_logo`、`wechat/teml_ids`、`logistics`、`copy_words`、`get_customer_type`，全部保留 PHP 的站点开启与可选登录边界。登录Logo对稳定R2引用生成短期签名并只接受相对路径或HTTPS；14个小程序订阅事件按PHP short ID从启用的`legacy_type=0`模板确定性投影，缺失模板显式为`null`；客服类型字段按数字/有界文本返回，客服URL只接受站内相对路径或HTTPS。物流列表保持旧端实际使用的`id/name/code`，不再公开PHP曾返回的`partner_id/partner_key/account/key/net*`承运商凭据。专项5/5、全量218文件/1,375项和双TypeScript通过，API面可执行缺口`34→29`；未读取或写入生产数据库、未调用provider、未部署主Worker或前端。
- [x] **API-011 城市与门店发现4条（代码与静态/服务回归完成，未发布）**：精确恢复 `GET city`、`city_list`、`store_list`、`nearby_store`，保留 PHP 的 StationOpen 以及后三条可选登录边界。`city`按`city_area.parent_id`惰性返回子级和可展开标记，`city_list`从`system_city`构造完整`v/n/parent_id/children`树；门店只读取营业且未删除记录，保留坐标校验、6367km Haversine距离、提货过滤、关键词与登录用户常用门店范围。公开投影明确排除`bank_code/bank_address/alipay_account/alipay_qrcode_url/wechat/wechat_qrcode_url`，门店图片对canonical R2引用短期签名。PHP在缺坐标时会把客户端IP发送给外部定位服务，本实现不向未声明provider披露IP，改为确定性`id DESC`；分页默认10、上限100、offset上限10,000，城市全树和单层也有显式上限。专项5/5、全量219文件/1,380项、双TypeScript和路由审计通过，API面可执行缺口`29→25`、退役后有效覆盖`93.0%→93.8%`；提交`35ef232`已推送，[Actions `33938423701`](https://github.com/cinagroup/cinashop/actions/runs/33938423701)八项全部成功。未读写生产数据库、未调用外部定位、未部署主Worker或前端。
- [x] **API-012 积分商城首页与分类2条（代码与静态/服务回归完成，未发布）**：精确恢复 `GET store_integral/index` 与 `category`，并复审同组既有 `list/detail`；四条浏览合同现统一保持 PHP 的 StationOpen 加可选登录边界。首页返回`banner/list/integral`，推荐商品限定`is_host=1`，登录用户只读当前积分、匿名为0；分类从可见`category.group=5`按`sort DESC,id DESC`映射`label/value`，最多1,000项。复审发现旧`list`虽已注册却忽略`store_name/priceOrder/salesOrder/range`且品牌恒空，现已恢复关键词/ID、积分加现金价排序、销量排序、积分区间、基础商品删除门禁和品牌投影；分页最多50、offset约束沿用活动列表门禁。商品与banner只接受站内相对路径或无凭据HTTPS，canonical R2引用响应时短期签名，危险banner跳转清空。专项7/7、全量220文件/1,385项、双TypeScript、结构/路由/可观测性/生产依赖审计和主Worker dry-run通过；API面可执行缺口`25→23`、退役后有效覆盖`93.8%→94.3%`。实现提交`4582e06`已推送，[Actions `33939387023`](https://github.com/cinagroup/cinashop/actions/runs/33939387023)八项全部成功；未读取或写入生产数据库、未调用provider、未部署主Worker或前端。

- [x] **API-013 用户积分与分销只读合同6条（候选代码与SQL回归完成，未发布）**：已补齐 `integral/list`、`extract/bank`、`spread/order`、`spread/count/:type`、`brokerage_rank`、`rank`；统一 StationOpen、强制登录和私有不缓存。16项本地内存PostgreSQL场景通过：用户隔离、分页前过滤、月汇总、五层分佣、冻结时间、提现状态、全局跨页排名、金额聚合超单行精度、快照JSON与签名头像；全量221文件/1,401项、双TypeScript及结构/路由/可观测性/生产依赖门禁通过。实现`99440a5`已推送，[Actions `33940755327`](https://github.com/cinagroup/cinashop/actions/runs/33940755327)八项全部成功。API可执行缺口`23→17`；代码通过不能替代新系统初始化、真实账号前端验收和主Worker发布。
- [ ] **API-014 既有财务接口行为复审（候选主体完成，渠道与发布未完成）**：不以路由注册数替代资金状态机、前端、真实角色和渠道验收。
  - [x] **API-014A 列表与统计合同**：POST推广列表恢复JSON分页、两级关系、关键词/时间/排序及PHP信封；旧`spread/commission/:type`恢复0～4的余额/佣金/提现含义与按月分页，新UniApp的1/2/3分类独立迁到`user/commission/list/:type`且先筛选再分页。财务页和两个个人中心入口共享上海自然日统计，提现拒绝返还不计作新增收益；19项真实内存PostgreSQL读取场景通过。
  - [x] **API-014B 提现事务与重放远端门禁**：已兼容旧/新金额与收款字段，按权威配置校验四种方式、上下限与PHP截断费率；扣除毛额、请求记录净额/手续费、支出账本立即有效。余额提现同时记充值/余额账，拒绝保留原支出并只补偿一次毛额。固定用户→申请锁顺序、事务超时、用户级持久化请求键、冲突409和晚期失败回滚已实现。[Actions `33944067731`](https://github.com/cinagroup/cinashop/actions/runs/33944067731)在真实PostgreSQL16.14上通过18项提现场景（含同键四请求、不同键防超提、并发拒绝的2项多连接测试）、1项非密钥测试值例外范围检查和3项前端金额规则；全量223文件/1,426项无跳过。只关闭事务候选工程门禁，不关闭D/E/F或DB-006。
  - [x] **API-014C UniApp财务页联动与本地浏览器验收**：展示方式/限额/手续费/净额，修复推广弹窗加载和两级搜索；未确认申请保留用户级请求标识、锁定表单，刷新后先查询再重试，不生成新申请。Playwright/Chrome在1280×900及390×844下通过页面身份、非空/无错误遮罩、推广筛选、金额拦截、双击单次提交、同载荷重试、余额零手续费和已登记结果恢复；并修复输入错误提示残留与UniApp数字框旧值残留。全部接口均为本地模拟，不是生产或真实小程序E2E。
  - [ ] **API-014D 自动微信提现渠道**：尚无完整打款适配、持久化未知结果/查单/回调恢复及真实渠道凭据验收。当前人工方式可审核；自动微信请求不能直接标记成功，新前端不展示未接入的自动微信提现。需完成渠道实现和契约测试后再进行受控真实验收，不把失败关闭算作渠道迁移完成。
  - [ ] **API-014E 发布及真实角色验收**：先完成DB-006/007，再用真实客户和最小权限Admin验证申请/审核/拒绝/重试与权限隔离；校验生产提现配置、当前孤儿账本裁决和实际到账证据，获得发布批准后部署并观察。当前主Worker/Pages未更新。
  - [ ] **API-014F 提现成功资金流水与审核通知（候选实现已补，真实验收未完成）**：成功流水使用负净额、`trading_type=6`和对应收款方式；余额自动审核同样覆盖，拒绝通知使用返还毛额。候选工程完成与真实提供商验收分别记录，不把站内消息或模板载荷测试当作实际送达证据。
    - [x] **API-014F1 原子流水、通知outbox与后台恢复门禁**：`WithdrawalEffectsService`在提现事务内写唯一成功流水和不可变事件；复用现有Queue/定时扫描/投递UNKNOWN人工确认机制，独立`withdrawal_id`不冒充订单ID。按PHP映射站内信、短信、成功公众号、成功/拒绝小程序通知，拒绝公众号维持源端未启用行为。[Actions `33945761103`](https://github.com/cinagroup/cinashop/actions/runs/33945761103)对`a0e5e4c`完成8/8工程门禁，新增14项SQL场景全部通过，含真实PostgreSQL16.14四连接并发审核仅记一笔流水/事件；全量224文件/1,440项无跳过。后台本地浏览器已验证桌面/手机、提现筛选、UNKNOWN→SENT模拟处置及历史记录，并修复网格内容溢出。仅关闭候选工程子项，F2真实送达和DB-007仍开放。
    - [ ] **API-014F2 模板与真实送达验收**：完成DB-007后确认生产配置开关、模板字段、短信签名、匹配appid/openid、小程序收款明细落地页与最小权限管理员；受控验证通知送达、提供商拒绝/超时及人工处置，不触发重复扣款。当前没有调用真实短信/微信、没有发布。
  - [ ] **API-014G 提现申请的管理端提醒**：PHP`user.extract→listener/user/Extract`还触发`kefu_send_extract_application`、`adminNewPush`与`WITHDRAW`；F只覆盖成功/拒绝，G的计数和客服收件箱候选实现不代表全部迁移。实时通知、企业微信与真实角色/发布继续开放，不得把提醒重放当成再次提现。
    - [x] **API-014G1 权限内待办计数与Admin入口（候选工程完成，未发布）**：原`AdminAuthService.adminNewPush`的三个固定零计数及错误总数已替换为独立`AdminNewPushService`：同一只读快照统计待发货、库存预警、待回复评价、待审核提现，按四个业务域的查看权限逐项过滤并求和；只有控制台权限不能读财务数量，财务专属角色也无需控制台权限即可读取自己的待办。顶栏显示总数及分类入口，30秒可见页检查、聚焦/手动刷新、审核成功后重取；退出/卸载不接纳旧响应，刷新失败不保留旧业务数。提现入口默认待审核，修正数据库拒绝状态-1与UI旧值2的错配。[Actions `33947577628`](https://github.com/cinagroup/cinashop/actions/runs/33947577628)对`0765f7a`完成8/8门禁，11项SQL/认证/真实服务事务及4项前端生命周期通过；全量226文件/1,455项无跳过，含3项真实PostgreSQL16.14多连接场景。CUA桌面1280×900、手机390×844的展开、跳转、模拟通过/拒绝及1→0/12→11计数联动已验证。不是实时事件推送或生产E2E，G2/G3与发布门禁继续开放。
    - [ ] **API-014G2 持久化申请事件、客服收件箱及实时提醒**：持久事件/隔离收件箱与实时传输分开验收；不能以轮询或本地预览关闭整个G2。
      - [x] **API-014G2a 申请事件与隔离客服收件箱（候选工程完成，未发布）**：申请/余额自动审核事务内写`withdrawal.applied.notice:<id>`，复用outbox租约、Queue及定时补偿；失败整体回滚、按提现ID和客服UID去重。消费时选择有效且启用通知的平台客服、有效绑定用户，重复UID只发一条，离线仍持久化；超过1000收件人明确失败保留恢复，不静默截断。普通用户列表/详情/个人中心未读数共同排除type=2；客服独立列表/游标/详情/显式已读每次校验身份与权限。Admin新增独立站内信开关和三个模板变量；客服新增系统提醒入口及30秒可见列表刷新。[Actions `33949795952`](https://github.com/cinagroup/cinashop/actions/runs/33949795952)对`6af6965`完成8/8门禁；新增专项19/19含PostgreSQL16.14四请求/四消费者，确认一笔申请/扣款/事件和每UID一条提醒；全量227文件/1474项无跳过，客服前端10项通过。CUA客服桌面/手机验证查看不标读、显式已读1→0、未读空态；后台验证配置保存。未连接生产、未发送真实通知，G2b/G3和发布前置继续开放。
      - [ ] **API-014G2b Admin/客服实时传输与恢复**：后端传输和前端接收分开验收。现有ChatRoom只承接会话消息，不向全站聊天用户广播财务事件；G1/G2a的30秒刷新不算实时推送。
        - [x] **API-014G2b1 独立通知DO与持久化刷新子事件（候选工程完成，未发布）**：按admin ID或客服绑定UID分区，连接/发送/心跳重新检查令牌、密码版本和当前权限；管理员需extract.view、客服需有效通知资格。受控Origin、8连接上限、休眠附件、30秒空闲撤权检查与只读协议；仅发送revision刷新提示，不传财务金额/银行卡/申请人。申请收件箱事务内新增唯一`withdrawal.staff.refresh`子事件，事务外5并发RPC，失败按已有outbox恢复，DO重放保持revision。提交后waitUntil及父事件消费后即时唤起Queue，定时扫描补偿崩溃窗口。[Actions `33951790692`](https://github.com/cinagroup/cinashop/actions/runs/33951790692)对`9c102a4`完成8/8：全量228文件/1481项无跳过、4项PG16多连接、双TypeScript、7项通知SQL/认证/重放和6项新增workerd场景通过。首轮3个预期RPC拒绝触发上游测试池缺陷，错误分支改为DO内部入口并补故障关闭/恢复重放断言后运行时21/21无未处理异常；没有放松生产错误处理或忽略异常。授权在runtime中模拟，真实JWT/SQL另由场景测试覆盖，非生产E2E。
        - [x] **API-014G2b2 Admin/客服接收端与浏览器闭环（候选工程完成，未发布）**：两端接入共享只读通知客户端，ready强制取权威HTTP、changed按revision去重、请求中到达的新通知追加一次读取；重连退避、握手/心跳超时、隐藏/离线/bfcache暂停恢复、旧连接/旧账号响应隔离。Admin待办与客服全局未读/收件箱接入，通知不改变聊天在线状态；客服阅读详情时更新列表而不关详情、不自动已读。4001撤权清屏，401清会话跳登录，403/503不误登出。Admin Pages代理保留101/WebSocket及流式正文，Vite补ws代理，common目录纳入CI触发。CUA应用内浏览器以回环HTTP+真实WebSocket夹具验证两端1→2、显式已读2→1、重复revision不增HTTP读取、撤权/恢复、503保留登录、401跳登录和只读管理员无财务socket；桌面1081×792、手机390×844。[Actions `33953586657`](https://github.com/cinagroup/cinashop/actions/runs/33953586657)对`3ba8d42`完成8/8：229文件/1490单元项无跳过、workerd 3文件/24项通过，含新增9项共享客户端/刷新生命周期、7项客服HTTP会话及3项Pages运行时透传测试。不代替正式Pages/Origin、真实角色或生产Redis/Hyperdrive验收。
        - [ ] **API-014G2b3 运行配置与真实角色验收**：DB-007增量还需外部0133/内嵌0137刷新事件CHECK；Workers需新增STAFF_NOTICE及v3 SQLite类迁移，原有DO不变；正式Admin/Kefu Origin须明确配置授权列表。未创建或部署真实DO、未执行生产DDL，生产Redis撤权/Hyperdrive新鲜度/真实角色和延迟验收仍开放。
    - [ ] **API-014G3 企业微信群机器人与真实接收者验收**：源`EnterpriseWechatJob`使用通知配置中的URL发送markdown，是群机器人而非已有企微通讯录/客户联系API。需限制目标、Secret管理、持久投递/UNKNOWN处置、模板转义与数据最小化，再在另行批准的真实目标完成送达/失败测试。当前无真实企业微信请求、无生产配置写入。

## P1：Admin `/adminapi` 路由批次

- [ ] **ADM-001 系统设置与表单**：setting 153、system 31、file 12；区分运行时权威配置、仅历史只读目录和明确退役功能。
- [ ] **ADM-002 营销与微信应用**：marketing 115、app 76、live 17、notify 14；恢复本地目录读写后，再逐类启用微信远端写入与回调。
- [ ] **ADM-003 商品运营**：product 98、freight 9；覆盖规格、属性、分类、品牌、库存、评价、运费模板、虚拟商品与批量操作。
- [ ] **ADM-004 用户与分销**：user 78、agent 35；覆盖标签/分组、等级、资金、佣金、推广关系、事业部、导出和权限。
- [ ] **ADM-005 供应商与财务**：supplier 67、finance 19；覆盖审核、结算、提现、流水、对账及租户隔离。
- [ ] **ADM-006 订单/退款/门店**：order 57、refund 5、store 10；复用订单、履约、核销、打印、面单与退款账本。
- [ ] **ADM-007 内容/DIY/导出**：diy 25、cms 18、export 22；恢复可视化页面链接、素材、文章与异步导出。
- [ ] **ADM-008 客服后台与历史任务**：serve 22、queue 3；管理员不得冒充客服，历史 ThinkPHP Queue/Timer 只读目录需逐项迁移消费者或正式退役。
- [ ] **ADM-009 企业微信 501 占位**：当前 15 条 PHP 精确合同虽已注册但返回 501；为 moment/client/group/template/welcome 建立 Queue、幂等投递、重试/死信、回调验签后才能转为完成。

## P1：Supplier `/supplierapi`

- [ ] **SUP-001 订单 30 条缺口**：列表筛选、导出、详情、备注、发货、拆单、核销、打印/面单、退款协同；验证供应商只能访问自身订单/明细/账本。2026-09-16增量完成现代list/info同代只读快照、商品归属/字节/分页边界和真实鉴权/PG验收（[证据](workers-ts/docs/supplier-order-generations.md)）；后续split_order历史独立快照、顾客/门店/供应商归属与读取界限通过联合32项PG和64项回归（[拆单历史证据](workers-ts/docs/supplier-split-order-reads.md)）。2026-09-19新增配货/待发货商品/状态只读快照、精确配货金额及退款后发货候选与错门店写入修复；最终读写/权限验证状态见[操作读取与履约证据](workers-ts/docs/supplier-operational-reads.md)。配货单页面已补URL/会话/迟到响应隔离、整批合同校验与打印失效保护，25项新增用例、相关72项及桌面/390px隔离Chrome通过（[可重跑测试](workers-ts/test/supplier-picking-frontend.test.ts)）。2026-09-19后续Orders页面分离详情/发货身份、读取代次与会话清空、提交互斥和未知结果保护，39项新增及相关111项、桌面/390px隔离浏览器与类型构建通过（[可重跑测试](workers-ts/test/supplier-order-frontend.test.ts)）。后续订单精确选择导出、现代/旧商品快照与会员折扣语义、订单/队列同快照读取、归属/容量限制及CSV完整性保护通过19项新增SQL与相关50项PG、150项回归和桌面/390px下载验证（[导出历史测试](workers-ts/test/supplier-export-history-reads.test.ts)、[CSV测试](workers-ts/test/supplier-csv.test.ts)）。新建订单现保留准入后的单件会员优惠并排除活动降价，真实连续退款/分批发货/跨供应商分单与导出配货验证守恒，旧快照不回填（[会员证据](workers-ts/docs/checkout-line-finance.md#membership-evidence-increment--2026-09-19-local-not-deployed)）。新下单付费会员节省金额现保存商品/运费/会员券来源，实际支付后事务只记根单一次，顾客拆单详情按自身快照展示；旧账本不回填或覆盖，会员首页日期绑定问题已修复，原生PG/独立角色与回滚重放证据见[会员节省金额账本](workers-ts/docs/order-member-economize.md)。线下other_order节省金额、其他历史报表、旧队列复合身份/完整DTO、财务精确选择、跨刷新写入恢复及幂等权威、真实认证/打印和正式环境验收仍未完成，父项保持开放。
- [ ] **SUP-002 商品 16 条缺口**：分类、规格、批量上下架、库存、虚拟库存与商品类型；解除“仅实物商品”限制前必须完成对应履约链。
- [ ] **SUP-003 文件 11 条缺口（候选路由已收敛，生产只读/R2 验证与发布未完成）**：PHP 文件域 22 条权威合同中，原有 11 条已匹配；本批再实现 R2 视频分片、HTTPS 外链视频登记、固定 R2 上传能力信息和无 path 参数的分类创建表单 4 条，并对不存在的移动表单/分类 read、弱扫码令牌三路、任意 URL 图片抓取及 GET 写上传偏好 7 条建立源证据退役，当前为 15 条可执行 + 7 条退役、`actionableMissing=0`。现有列表/移动/重命名/删除/分类继续强制 `type=4 + relation_id=supplierId + module_type=1`；MP4 使用最多 100 MiB、单片 5 MiB、租户临时前缀、12 小时 Queue 清理、固定长度流式合并、canonical 签名 URL 与单 Range 206。定向 12/12、双 TypeScript、主/审计 Worker dry-run及 Linux workerd 15/15 已通过；候选提交 `1121c52`、门禁修复 `d8afeaf` 已推送，[Actions `33524822780`](https://github.com/cinagroup/cinashop/actions/runs/33524822780) 8/8 成功。生产 PostgreSQL/R2 聚合审计夹具已强制只读且不输出对象键，但因临时 `workers.dev` 会承载脱敏生产聚合而被安全门禁拒绝，须用户对该外部临时端点再次明确授权后执行；之后还需真实 Supplier 前端 MP4/分类/租户越权 E2E、主 Worker/Supplier 发布批准和发布后观察，故父项暂不勾选。
- [ ] **SUP-004 账号/设置/首页（25/25 条代码候选完成）**：详细复核发现原清单还漏列了仍有第一方页面调用的旧 `printing` GET/PUT，因此本域初始相关可执行缺口是 25 条。公共设置/首页 12 条、运费模板 5 条和管理员 8 条现已精确恢复；同时纠正 `home/header|order` 语义偏差、商品 `freight=3` 模板模式、旧页面误调用整模板删除路由，以及 PHP 子账号全权限/跨 Supplier IDOR。Supplier 精确匹配由 83 提升到 108，精确覆盖 45.6%→59.3%，退役后有效覆盖 47.4%→61.7%。父项仍不完成：还需生产角色/子账号只读核验、真实主/子账号旧端与 TS 端 E2E、发布批准和观察。
  - [x] **SUP-004-A 公共设置/首页兼容候选**：12 条精确路由、64 KiB 请求上限、最长 366 天聚合、Supplier 租户派生、打印 Secret 只写不回显、原密码校验/令牌失效及 Supplier TS 前端改密流程已完成；提交 `5ec7775` 已推送，[Actions `33577342683`](https://github.com/cinagroup/cinashop/actions/runs/33577342683) 的 184/1,183 单元、双 TypeScript、Linux workerd 15/15、五端 build、路由/schema/observability 与全历史 Gitleaks 8/8 通过，主 Worker dry-run 通过，未部署。
  - [x] **SUP-004-B Supplier 管理员 8 条 + RBAC 候选**：PHP 权限服务在 `verifiAuth()` 开头直接 `return true`，且 detail/edit/update/delete/status 未逐项绑定租户；现改为 12 组稳定 `view/manage` 权限覆盖全部认证 Supplier 路由，未知路由默认拒绝，主管理员身份只认 `system_supplier.admin_id`。子账号只接受当前 Supplier 的启用 `type=4` 角色，旧菜单数字规则先批量解析成稳定权限；创建/更新角色及给他人分配角色都不能超出操作者权限。八条旧管理员合同与四条租户角色扩展已接通，目标统一限定 `admin_type=4 + relation_id + level=1 + is_del=0`，事务内重验操作者、加 advisory/行锁，禁止操作主管理员或当前账号，密码 bcrypt cost 12、敏感字段不进审计。Supplier TS 增加权限过滤导航、路由守卫、子账号与角色编辑器；提交 `065c522ee0ed6bc3de2471ed744f9edf3efb48b4` 已推送，[Actions `33582012080`](https://github.com/cinagroup/cinashop/actions/runs/33582012080) 8/8，通过 186/1,202 单元、定向 10/10、Linux workerd 15/15、五端 build、路由/schema/observability 与全历史 Gitleaks。未读取生产业务行、未写生产库、未发布。
  - [x] **SUP-004-C 运费模板 5 条候选**：列表、详情、保存、软删除和城市目录均从认证 Supplier 派生租户，所有模板读写统一限定 `type=2 + relation_id=supplierId + is_del=0`，跨租户与不存在返回同一失败面。主模板与计价/指定包邮/不配送子规则在同一事务中按 Supplier advisory lock 和模板行锁原子替换；城市路径、默认全国规则、计费模式、小数精度、重复终点、规则组/路径数量均有界。删除会拒绝仍被本 Supplier 有效商品引用的模板，并与商品保存共享模板锁；商品三种运费模式恢复为 `1=包邮/2=固定/3=模板` 且验证模板归属。新 Supplier TS 页面已恢复模板编辑和商品选择，子规则删除只在本地修改，不再误调整模板删除接口。提交 `9066cb9` 已推送，[Actions `33579118412`](https://github.com/cinagroup/cinashop/actions/runs/33579118412) 的 185/1,192 单元、定向 3 文件/27 项、Linux workerd 15/15、五端 build、路由/schema/observability 与全历史 Gitleaks 8/8 通过；未读写生产数据、未部署。
- [x] **SUP-005 导出/队列/售后 12/12 条候选合同收口（退款 3/3、queue 5/5、export 4/4）**：初始缺口为 export 4、queue 5、refund 3。退款子域恢复两条只读合同并退役危险 GET 写退款；Queue 子域恢复两条按订单归属过滤的历史只读合同，将三条无租户范围的全局 GET 写操作退役并指向 Supplier 专属履约/面单账本；Export 子域保留旧浏览器端表格 manifest 契约，以分页/固定上限、租户联表、最小字段、manage 权限和公式注入防护恢复四条读取。这里的 `[x]` 表示候选合同和可重复工程门禁完成，不代表主 Worker 已发布或真实 Supplier 生产下载 E2E 完成。
  - [x] **SUP-005-A Supplier 售后退款 3 条候选**：`GET /refund/refund/:id` 只返回当前 Supplier 的已支付、可处理售后表单；旧 PHP 同一售后单的“部分退款后立即完成整单”矛盾语义收敛为精确全额合同，PUT 必须提交与售后单权威金额完全一致的两位小数。执行前在短事务中加 2 秒锁/5 秒语句超时和行锁，随后把 Supplier、用户、退款单、原订单、金额、历史已退款值及支付/可见性再次绑定到共享退款状态机；历史部分退款失败关闭，完成重放幂等，状态日志使用正确订单 ID。`GET /refund/reason` 从 `stor_reason` 读取并限制 100 项/每项 255 字，列表增加租户内精确原因过滤，Supplier TS 同步接入筛选并提交确认金额。旧 `GET /refund/agree/:order_id` 因 GET 写状态、忽略 path 改读 query、无租户范围和错误日志 oid 已退役，安全替代为两个 PUT。提交 `edef647d27c5f7d90586bf329a75513ab3baa26d` 已推送，[Actions `33583699196`](https://github.com/cinagroup/cinashop/actions/runs/33583699196) 8/8；187/1,207 单元、定向 5/5、双 TypeScript、Linux workerd 15/15、Supplier build、路由/schema/observability、依赖审计和全历史 Gitleaks 均通过。未读写生产业务数据、未部署。
  - [x] **SUP-005-B Supplier Queue 5 条候选（2 条执行 + 3 条退役）**：PHP `queue_list/queue_auxiliary` 是 Admin/Supplier 共用全局表且没有 `supplier_id`，旧 Supplier controller 的列表、明细、重试、清除和停止都不绑定租户；重试还会把不透明 `queue_in_value` 交给 Admin 履约服务重放。现仅恢复 `GET /queue/index` 和 `GET /queue/delivery/log/:id/:type`，只接受履约类型 7～10/辅助类型 3～6，并强制 `queue_auxiliary.relation_id = store_order.id AND store_order.supplier_id = 当前 Supplier`；列表数量也按当前租户辅助行重算，不返回 `queue_in_value/execute_key/other`，响应明确标记 legacy history/read-only。三条 GET 写操作以 PHP controller/service/旧 Vue 调用四层证据退役：面单重试/关闭改走带 request key、原因和不可变动作记录的 Supplier waybill job API，普通/同城/虚拟发货按归属订单显式重新提交，不提供通用全局队列删除或停止。提交 `bd51824043d6a3a4d217a1107088e9efb632adb9` 已推送，[Actions `33584993388`](https://github.com/cinagroup/cinashop/actions/runs/33584993388) 8/8；188/1,212 单元、Queue 定向 5/5、双 TypeScript、Linux workerd 15/15、Supplier build、路由/schema/observability 和全历史 Gitleaks 均通过。未读取或写入生产业务数据，未查询 Cloudflare Queue 内部消息，未部署。
  - [x] **SUP-005-C Supplier Export 4 条候选**：恢复 `GET /export/storeOrder`、`expressList`、`batchOrderDelivery/:id/:queueType/:cacheType` 与 `financeRecord` 的旧 `{header,filekey,export,filename}` manifest。订单每页最多 250 行并保留旧状态筛选/上海时间与发货单规则；批任务、财务和物流目录分别固定最多 1,000 行，ID 列表严格限 1,000 个，响应硬限 4 MiB。订单/财务/队列都在 SQL 中绑定当前 Supplier，批任务还校验 7～10 与 3～6 的固定类型映射；物流只选 `name/code`，队列不选 `queue_in_value/execute_key/other`。所有字符串统一去 NUL、截断并中和 `= + - @` 公式前缀；订单/批任务与财务下载分别要求 `order.manage`、`finance.manage`，只有非敏感物流对照表允许 `order.view`。提交 `546e6b7857ed05f5a432df5d1e60758561c2a94c` 已推送，[Actions `33586697589`](https://github.com/cinagroup/cinashop/actions/runs/33586697589) 8/8；189/1,218 单元、Export 定向 6/6、双 TypeScript、Linux workerd 15/15、Supplier build、生产依赖 0 漏洞、路由/schema/observability 与全历史 Gitleaks 均通过。生产 Hyperdrive 配置元数据只读复核仍为 `cinashop-pg`/指定 ID/PostgreSQL/连接上限 60/缓存开启；未读取或写入生产业务行，未部署主 Worker。

## P1：Kefu `/kefuapi`

- [ ] **KEFU-001 扫码/微信登录（核心和前端本地接入完成，外部验收未完成）**：`key`、`scan/:key`、`wechat` 已改为浏览器来源白名单、按 state 隔离的 verifier Cookie、可重试一次性挑战、OAuth state/code 重放保护和唯一账号绑定；密码/扫码/OAuth 签发、HTTP 与 WebSocket 均要求客服未删除、业务/账号状态启用，并要求绑定用户存在且启用。Kefu 登录页及 UniApp 确认页已接入并通过桌面/移动受控回归。`ticket/[:appid]` 的 PHP 目标方法不存在且无第一方调用，已带证据退役。仍缺 Kefu 正式 Origin、同源 Pages proxy、开放平台凭据、非空客服/微信身份、反二维码中继风险处置和真实 E2E。
- [ ] **KEFU-002 游客链路 8 条（安全拆分与客户端接入完成，生产 E2E 未完成）**：广告、反馈 GET/POST、公开商品、签名 visitor bootstrap、登录用户订单归属、游客聊天、隔离 R2 上传及游客 WebSocket 均已实现；24 小时 visitor audience、数据库 token 摘要/撤销、权威客服分配、独立 UID、`is_tourist` 未读/转接/实时隔离已通过随机 schema 与本地客户端验证。生产客服/会话/游客内容仍为空，必须迁移真实账号后验证 Hyperdrive、R2、WebSocket hibernation、过期/撤销、限流与转接。
- [ ] **KEFU-003 面单模板与旧退款入口（核心代码完成，生产配置未完成）**：PHP `POST /order/refund` 指向不存在的 `Order::refund()`，已带控制器证据退役并指向表单 GET + 售后资金 PUT；认证 `GET /order/temp` 已恢复固定 HTTPS 一号通只读目录，带 10 秒超时、32 KiB 上限、固定路径和输入约束，与可重试签发账本分离。生产全部 `config_export_*` 和 `CRMEB_ONEPASS_*` Secret 名均缺失，所以当前明确未启用。
- [x] **KEFU-004 退款同意与资金退款**：PHP `GET /refund/agree/:order_id` 同时存在 GET 写副作用、忽略 path 改读 query、状态日志 `oid` 错用退款 ID，已带证据退役；替代为认证幂等 `PUT /refund/agree/:id`，在客服归属锁后原子更新售后/原订单并只写一条正确订单日志。资金退款按 CORE-002 完成生产 PostgreSQL 隔离验收。
- [ ] **KEFU-005 真实账号浏览器 E2E**：迁移至少一个有效客服账号后验证密码登录限流、WS hibernation、断线重连、上传/重签、转接三端通知、日限额、多会话、商品/订单/售后权限。

## P1：Out API 与 ERP

- [x] **OUT-001 分类写入 4 条**：已恢复 `POST /category`、`PUT/DELETE /category/:id`、`PUT /category/set_show/:id/:is_show`。写入固定平台 `type=0/relation_id=0`，逐路由 ACL、双层限流与 HMAC 脱敏审计继续生效；全局事务/表锁串行层级写入，同名创建、更新和显隐重放收敛，删除重放安全。移动拒绝自身/后代和超过三级并同步全部后代 `path/level` 与商品关系父级；删除同时检查所有作用域活动商品的新关系表、旧 `cate_id` CSV、`store_product_cate` 和类目品牌关系。生产 PostgreSQL 16.14 随机 schema 的并发、回滚、跨租户与四类引用门禁全部通过，`public_state_unchanged=true`，临时 schema/Worker 已删除。
- [x] **OUT-002 商品写入 4 条**：已恢复 `POST /product`、`PUT /product/:id`、`PUT /product/set_show/:id/:is_show` 和 `PUT /product/stock/upload`。写入固定平台 `type=0/relation_id=0`，当前只允许实物商品；未迁移的活动、品牌、优惠券、预售、会员、自定义表单、标签、保障与参数能力失败关闭。所有写请求要求 UUID-v4 `Idempotency-Key`，事务账本只保存账户、操作、请求摘要和有界结果；创建并发收敛，修改保持 SKU 拓扑/唯一标识与库存不变，上下架联动购物车/关系，库存按平台条码绝对同步并对缺失、重复和整批失败原子拒绝。商品写与库存解析共用锁，SKU→商品固定行锁顺序避免与下单库存死锁。生产 DDL 二次执行幂等，随机 schema 场景的 ACL、跨租户、非实物商品、并发重放、键冲突、修改回滚、条码歧义、缺失批次回滚和账本脱敏全部通过；`public_state_unchanged=true`，回放表 0 行，临时 schema/Worker 已删除。隔离场景早期曾误写一件商品、一条 SKU 及两条购物车夹具到 `public`，均按固定 ID、全字段和零引用守卫精确删除，购物车序列恢复 27；最终复查审计标记为 0。
- [x] **OUT-003 优惠券写入 3 条**：已恢复新增、状态和软删除，修正 PHP `type/coupon_type` 与目标列交换后的列表/写入合同，并把折扣统一为 PHP 的 0–100 百分比语义。三类写入要求 UUID-v4 幂等键，账本不保存标题、金额、范围、日期或请求/响应体；创建固定平台通用/分类/实物商品范围，启用复核结构和有效期，删除保留模板及 `store_coupon_product` 以保证已领券可继续核销，并阻断商品支付后赠券、抽奖、促销和新人礼包活动引用。生产 DDL 二次执行幂等，随机 schema 的并发、冲突键、跨租户、列表字段、活动门禁、已领/已用保留和故障回滚全部通过，`public_state_unchanged=true`。一次事务外夹具事故新增的用户券、领取证据、抽奖/奖品、促销和配置行已按固定 ID/全字段/序列守卫精确删除，用户券/配置序列恢复4/503，最终事故标记和临时 schema 均为0；清理能力已移除。
- [x] **OUT-004 用户写入 3 条**：已恢复 `POST /user`、`PUT /user/:uid`、`PUT /user/give/:uid`。新增要求唯一合法手机号，密码继续使用旧登录兼容 MD5 但禁止空口令/`123456`，并把用户、新人标记/赠礼、社区资料、标签和等级原子创建；更新改为安全部分字段语义，不再因省略字段清空资料，同时完整校验平台分组/标签/等级、手机号唯一和推广链 100 层环。更新/赠送都强制 UUID-v4 幂等键；余额和积分在用户行锁内按分/整数计算，超额扣减均收敛到 0，资料、推广计数/历史/好友、余额、积分、两类不可变流水和脱敏回放账本同事务。生产 DDL 二次执行成功且业务指纹不变；随机 schema 的 ACL、并发、重放/冲突、重复手机号、部分更新、等级/标签、推广环/换绑、双流水、超额扣减、故障全回滚和账本脱敏全部通过，`public_state_unchanged=true`，临时 schema/Worker 已删除。
- [ ] **OUT-005 用户变更外部回调（BLOCKED：需安全回调合同/真实账户）**：PHP `out.outPush` 在数据库提交后直接请求 `out_account.user_update_push` 任意 URL，只保留余额或积分中最后一项变化，没有可靠重试且形成 SSRF/内网访问面。不能原样迁移；需先确认真实客户签名协议、HTTPS 域名 allowlist、事件版本和去重键，再以事务 outbox + Queue 投递，并对 UNKNOWN 状态人工对账。生产当前有效 Out 账户和接口目录均为 0，无法做真实回调验收。
- [ ] **ERP-001 认证 3 条（BLOCKED：需 ERP 协议/账号）**：授权、回调、access token；凭据只存 Secret，回调必须 state/签名/重放保护。
- [ ] **ERP-002 同步与回调 5 条（BLOCKED：需 ERP 沙箱）**：商品同步、库存、发货、取消、售后收货；必须以事件账本保证乱序和重复安全。

## P1：第三方配置与远端验收

- [ ] **CFG-001 Turnstile**：创建正式 widget，配置 `TURNSTILE_SECRET_KEY`、`TURNSTILE_SITE_KEY`、精确 hostname；PC/H5/App/小程序验证成功、过期、重放和失败恢复。
- [ ] **CFG-002 Aliyun SMS**：配置 access key、secret、签名、模板/区域；验证 Queue 重试、用途错配、频控与不记录验证码。
- [ ] **CFG-003 微信支付/退款**：配置商户私钥、API v3 key、平台公钥/证书 ID、通知 URL；用测试商户覆盖支付、退款、重复/乱序通知和主动查询。
- [ ] **CFG-004 支付宝**：配置 app/seller、私钥、公钥、通知/返回 URL；验证签名、重复通知、未知结果与对账。
- [ ] **CFG-005 CRMEB 一号通**：配置 access/secret 与平台/供应商面单参数；验证签发、明确拒绝、断线未知、已有单号处置与禁止盲重试。
- [ ] **CFG-006 微信公众号/小程序/开放平台/企微**：逐能力配置 app/secret、token/AES、微信侧授权域名和回调；开放平台 AppID 使用精确 `wechat_open_app_id`，AppSecret 只用 `WECHAT_OPEN_APP_SECRET`，PC/Kefu 必须分别配置精确 `PC_AUTH_ALLOWED_ORIGINS` / `KEFU_AUTH_ALLOWED_ORIGINS` 与同源 callback proxy，并验证 audience+state 隔离的 verifier Cookie。每项单独启用，不使用一个总开关掩盖缺失配置。
- [ ] **CFG-007 打印与物流提供商**：迁移打印机凭据、物流 AppCode；验证脱敏、租户隔离、UNKNOWN 人工处置及 Queue/DLQ。

## P2：前端、测试与发布

- [ ] **FE-001 Admin 页面补齐**：机器审计已把“旧378→新56个 Vue文件”的粗略口径收紧为旧端274条启用业务路由/245个独立已路由页面组件，对比新端52条业务路由/51个独立已路由页面组件；文件数和路由数均不等于语义覆盖率，父项继续未完成。
  - [x] **FE-001A 可重复导航盘点**：`audit/admin-frontend-inventory.json` 固化旧端18个实际导入路由文件及 SHA-256、274条业务页面路由、18条辅助页面路由、133个未路由页面文件；新端路由、页面、未路由文件和组件可解析性由测试防漂移。注释路由、未导入模块和错误页不进入业务分母。
  - [x] **FE-001B 商品创建/编辑 501 与字段合同修复**：新 Admin 已从不存在的 `/product/create|update/:id` 改为 Worker 已注册的 `/product/add|edit/:id`；详情由数据库 camelCase 明确投影为页面 snake_case，编辑提交再映射回模型字段，会员价/排序不再在新建时静默丢失。定向测试覆盖路由、详情和更新字段。
  - [x] **FE-001C 商品单位与保障服务首批操作面（候选完成，未发布）**：新增 `/product/metadata`，接回旧 `unitList` 与 `ensure` 活跃能力；单位支持查、新增、编辑、引用保护删除并进入商品编辑下拉，保障支持查、新增、编辑、停启和引用保护删除。服务端继续执行 `product.view/manage`，本地桌面/390px预览交互通过。
  - [x] **FE-001D 274条旧业务路由逐屏语义映射（代码审计/自身Linux CI完成，未发布）**：11份逐屏台账按权威清单对274/274条唯一路由各分类一次；用户分组阶段快照为candidate 40、partial 120、missing 107、retired 7，缺失/重复/越界均为0，当前全集见本文顶部；复核将缺旧素材/选品工作流的社区内容创建及仍显示真实订单状态数的旧图表均列为partial，旧单据设置retired补齐源码行证据。全集断言见`workers-ts/test/admin-all-frontend-parity.test.ts`，各域细节见`workers-ts/audit/admin-legacy-*-route-parity.json`；精确提交`b1dc6ac6f9f54dd2554b4b7b2ebc8c145c10f6c6`的[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35962566501) 11/11成功，599文件10,796项零跳过；该次为38/120/109/7历史快照。资金流水页对应的39/120/108/7快照在精确提交 dcbddca 的[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35972181778) 11/11 成功；新增用户分组后的40/120/107/7为本地候选；f992bf7 的单独运行因订单测试错误断言取消，覆盖该页及断言修复的精确提交48bc8c1之[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35983849804)已失败，故障及后续复验见本文顶部。勾选范围仅是逐屏代码审计；该阶段107屏缺失、120屏部分覆盖的实现继续归FE-001及各功能子项，真实角色/生产数据与权限E2E归FE-001G，发布验收归FE-001H，不能用聚合页标题或API注册推定整屏可用。
    - [x] **FE-001D1 内容域13屏逐项审计与社区设置（候选完成，未发布）**：`audit/admin-legacy-content-route-parity.json` 固化旧内容路由分母、组件、状态、新页面、新 API、权限、已覆盖与剩余缺口；当前为5条候选、5条部分覆盖、3条整屏缺失。社区话题/内容/添加/评论原4屏加本批专用设置页签共5屏进入候选；文章列表/编辑与直播目录仍为部分覆盖，文章分类、直播间新增、直播商品新增明确保持缺失。设置页接回6个旧开关，严格校验、短事务锁、重复配置失败关闭、数据库回读、管理员日志及6个 `CONFIG_KV` 键失效；桌面与390×844实际切换/保存、无页面溢出和控制台0错误通过。本地202文件/1,284项单元、双TypeScript、Admin 2,427模块构建及316/336 Admin请求合同通过；精确实现提交 `9ad50534f10cc1bb21f5fe68e0d7b9a50ec99a96` 的 [Actions `33628399986`](https://github.com/cinagroup/cinashop/actions/runs/33628399986) 8/8成功。未连接生产Hyperdrive、未执行DDL/DML或部署，真实角色与生产数据E2E仍归FE-001G/H。
    - [x] **FE-001D2 CMS文章三屏闭环（候选完成，未发布）**：旧文章管理、平面文章分类、文章新增/编辑三屏已逐字段复核并统一迁到 `/content/article` 的文章/分类页签；内容域统计从5候选/5部分/3缺失推进为8候选/3部分/2缺失。文章列表恢复分类/标题/状态筛选、有界分页、封面、摘要、商品关系、浏览量、排序、热文/轮播和软删除；分类恢复图片/简介/排序/停启、严格20字符标题及文章引用保护；编辑恢复必选分类/封面、作者、摘要、受清洗HTML、分享字段、来源URL、商品、发布/热文/轮播，并可按嵌套素材目录只读选择现有图片。写入采用2秒锁等待/5秒语句上限、分类共享/独占advisory lock、文章锁、候选共享行锁、文章与正文双表原子upsert、精确回读及不含正文的管理员日志；新增4个列表/关系索引。桌面实际完成页签、素材选择、HTML编辑与保存，390×844无页面级横向溢出，控制台0错误。本地203文件/1,289项单元、双TypeScript、Admin 2,428模块构建、325/345 Admin请求合同、schema/route/observability全部通过；精确实现提交 `9fd14e95f337663f910ed6d92a76e1c7b5263186` 的 [Actions `33705817573`](https://github.com/cinagroup/cinashop/actions/runs/33705817573) 8/8成功。未连接生产Hyperdrive、未执行`0127` DDL/DML或部署；生产历史文章/附件、真实角色和写入E2E仍归FE-001G/H。
    - [x] **FE-001D3 小程序直播本地管理与三类只读同步（候选完成，未发布）**：逐路由复核旧直播间、商品、主播controller/service/DAO/页面及微信client后，内容域由8候选/3部分/2缺失推进为9候选/2部分/2缺失；主播屏进入candidate，直播间和商品目录仍为partial，两张远程新增屏仍为missing。新增直播间详情/本地显隐/本地软删除及关系清理、商品详情与仅审核通过商品的本地显隐、主播新增/编辑/显隐/引用保护软删除；主播保存前只读精确核对微信角色，Cron/手动同步扩展为直播间、商品、主播三类Queue任务。所有本地写入使用2秒锁等待/5秒语句上限、固定advisory lock、行锁、精确回读和不含联系人信息的管理员日志；`live_room(id,phone)` 出现歧义时拒绝按单ID写入。桌面实际完成详情、显隐、主播新增/编辑/删除与关联保护，390×844卡片/弹窗无横向溢出，三任务入队提示可见且控制台0错误。本地203文件/1,290项单元、双TypeScript、Admin 2,428模块构建、334/354 Admin请求合同、schema/route/observability全部通过；实现 `8eb81eefb60d818dbdbb7fddef76c58a25cbe53a` 及运行时计数修复 `f688dcd6163d12d58479d6633e13367809b812b4` 的 [Actions `33708390827`](https://github.com/cinagroup/cinashop/actions/runs/33708390827) 8/8成功。微信直播间创建/编辑、房间商品绑定、商品添加/审核/重审/远程删除仍须耐久outbox、幂等策略、对账和当前提供商合同验证；未连接生产Hyperdrive、未调用真实微信、未执行DDL/DML、Queue或部署，真实角色与生产数据E2E仍归FE-001G/H。
    - [x] **FE-001D4 系统设置76屏逐项审计（代码审计/自身Linux CI完成，未发布）**：权威前端盘点中的76条 `/admin/setting*` 业务页已全部逐屏完成本地代码审计，结论为candidate 17、partial 24、missing 30、retired 5、unreviewed 0；剩余61屏的旧组件、API、新Admin页面与Worker合同证据逐条记录在 `audit/admin-legacy-setting-route-parity.json`。四个旧platform占位页有空实现证据才列retired；旧动态配置、group_data、同城配送及DIY缺口不能以聚合页数量推定覆盖。精确提交 ece6ded6b9b05a2b1d1a5228f5fdbb0b89c14b3e 的 [Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35950719144) 11/11 成功，两分片合计10,716/10,716、零跳过，逐屏审计测试8/8；生产历史数据、真实角色、第三方与发布验收及30个missing/24个partial的功能补齐继续归FE-001各域功能项及FE-001G/H，不计入本代码审计项。
      - [x] **FE-001D4A 打印/通知首批5屏（本地候选完成，未发布）**：旧打印机列表和小票配置分别进入candidate，误复制商品规格代码且调用未定义API的“单据设置”明确retired；通知目录和编辑因目前只覆盖四类订单通知、提供商模板及投递台账，仍缺type=1会员/type=2平台全目录、企业微信和安全远端模板同步，保持partial。打印补齐名称/平台筛选、15条分页、小票实时预览、规格/二维码/50字提示依赖校验和旧POST状态别名；服务端请求体流式限制16 KiB、响应`private, no-store`、2秒锁等待/5秒语句上限、固定锁、精确回读和不含凭据的`system_log`。桌面与390×844实际交互、保存、移动弹窗和控制台0错误通过；本地204文件/1,294项单元、双TypeScript、Admin 2,428模块构建通过，Windows workerd仍在断言前`0xc0000005`，提交`2f03669b4e0bf1675da307099ac34a53017dd397`的[Actions `33710753409`](https://github.com/cinagroup/cinashop/actions/runs/33710753409) 8/8成功。未连接生产Hyperdrive、未执行DDL/DML或部署，真实打印机物理小票、生产目录和受限角色E2E仍归FE-001G/H、CFG-007。
      - [x] **FE-001D4B 核心商城设置8屏（候选完成，未发布）**：逐字段复核系统表单、动态系统配置、商城基础/商品/交易/支付/协议/事业部；经B1～B4继续补齐后，原8屏中的系统表单、商城基础、商品、交易、支付、五类协议和事业部7屏为candidate，仅动态系统配置保持partial。`/config/commerce` 只允许42个非敏感配置键，接回站点品牌、登录轮播、favicon、分享、库存警戒及重算、订单时限、售后、支付开关、微信商户号/商户API证书序列号、三profile运行时就绪状态和事业部开关；B3接入次卡临期/到期耐久提醒消费者和售后退货收件信息，B4则明确公众号/H5/PC、小程序、App共享一套APIv3商户凭据并按各自AppID就绪，退休旧独立小程序商户号分支。支付私钥、证书/平台公钥内容和APIv3 Key继续只由Cloudflare Secret管理。写入采用有界正文、动作权限、短事务、固定锁、精确回读、无敏感值审计与白名单KV失效；B2另以Worker固定安全头、双维度登录限速、通用失败响应和bcrypt假哈希替代旧PHP可编辑安全开关。B4后的本地完整门禁为209文件/1,318项单元、双TypeScript、Admin 2,437模块构建及362/362 Admin请求变体；B1～B4精确证据见子项。生产支付公开字段、三种AppID、微信支付Secret和真实支付流水当前均为空，候选运行时保持失败关闭；主Worker及各前端仍未部署。
        - [x] **FE-001D4B1 系统表单Admin操作面（本地候选完成，未发布）**：旧系统表单新增/编辑、目录及提交数据三屏统一迁到 `/config/forms`；补齐名称/状态筛选、15条分页、新增/编辑、10类受控组件、拖拽与按钮排序、字段设置、用户端预览、启停、引用保护删除，以及按用户/来源/关联ID/时间筛选的提交数据抽屉和最多5,000条防公式注入CSV。服务端兼容旧编辑器“时间戳键对象”和新数组两种模板形状，列表不加载完整JSON；写入使用约1.1 MiB流式正文上限、2秒锁等待/5秒语句上限、固定锁、精确回读和不含表单内容的`system_log`，停用/删除前检查商品、秒杀、拼团、砍价与积分商品，并新增五个引用索引。桌面931px preview实际完成新增、字段编辑、排序、保存、数据筛选/重置和导出，文档无横向溢出且console error为0；未取得真实移动端视口证据。Worker全量206文件/1,304项单元、双TypeScript、Admin 2,436模块构建和342/362 Admin请求合同通过；实现提交 `f76e7937ed5184df689441350fd77e65d09b3304` 的 [Actions `33715671291`](https://github.com/cinagroup/cinashop/actions/runs/33715671291) 8/8成功。只读确认生产Hyperdrive `9748c294e21c49a99579c9cef70102e0` 指向 `cinashop-pg` / VPC Service `019fe223-e5a1-7ed1-945a-8993a6f32508`，未查询业务表、未执行`0128` DDL/DML或部署，生产历史模板/引用/提交数据和真实角色E2E仍归FE-001G/H。
        - [x] **FE-001D4B2 商城基础设置尾项（本地候选完成，未发布）**：登录轮播、favicon与微信分享图片统一使用R2素材选择器，Admin预览读取15分钟签名URL但配置只保存稳定 `/api/assets/:id` 引用，避免签名过期污染持久配置；新增受限公共 `/api/site_config` 和兼容 `/api/share`，Admin登录、PC站点品牌/OG元数据及UniApp分享钩子均已消费。旧PHP任意密码/参数过滤开关明确不恢复，改由固定安全响应头、4 KiB登录正文、来源与账号双维度Durable Object限速、HMAC作用域、输入上限、统一认证错误及bcrypt假哈希提供Worker原生保护。桌面完成素材新增/去重/排序、分享字段保存与重载；390×844无横向溢出且登录视觉区正确隐藏，桌面登录轮播可见，console warning/error为0。Worker完整207文件/1,307项单元、双TypeScript、Admin 2,437模块、PC与UniApp构建及362/362 Admin请求变体通过；提交 `9c7429a98690af252966ae702a5b4907f7f9ca9e` 的[Actions `33717730974`](https://github.com/cinagroup/cinashop/actions/runs/33717730974) 8/8成功。只读沿用已确认的生产Hyperdrive控制面信息；本批没有业务表读取、DDL/DML、部署或生产认证尝试。
        - [x] **FE-001D4B3 交易设置消费者（代码与生产结构完成，未发布）**：5分钟根任务按80条keyset分页扫描有效次卡，在逐卡重查、2秒锁等待/5秒语句上限、advisory/行锁下把临期或到期事件、旧`is_advent_sms/is_expire_sms`标志与不可变outbox原子提交；短信/站内信消费者保留PHP模板参数和`reminder_brink_death/expiration_reminder`标记，并以事件/渠道账本保证外部投递幂等。售后详情按门店→供应商→平台层级返回收件人、电话和地址，Admin退款详情及UniApp状态4/5页面已展示；本地Admin preview真实打开售后802确认三字段可见。生产Hyperdrive只读审计确认PostgreSQL 16.14、次卡0、outbox 0、相关模板标记0、提醒时限缺配置而运行时回退1小时、活动售后3条但当前均为平台scope且无状态4/5、平台退货三字段未配置；随后在行数28/关系172 KiB前置范围内以2秒锁/15秒语句/advisory lock幂等执行`0129`，扩展outbox CHECK并创建两个有效/ready的partial索引，二次执行无变化且业务行不变。临时审计Worker已删除并确认404；主Worker/前端未部署，因此真实短信、真实次卡、已配置退货地址和生产角色E2E仍未完成。实现提交`191d6e81d65a12d4f824dba5de01638822aeb013`及测试修复`0ad3bca42c1adc3777ed42770a833649535c8430`的[Actions `33721832795`](https://github.com/cinagroup/cinashop/actions/runs/33721832795) 8/8成功。
        - [x] **FE-001D4B4 支付公开商户配置（本地候选完成，生产只读审计完成，未发布）**：Admin白名单新增格式受限的`pay_weixin_mchid`与`pay_weixin_serial_no`，证书序列号去空白并大写；微信公众号/H5/PC、小程序和App分别使用`wechat_appid`、`routine_appId`、`wechat_app_appid`，共享一套APIv3商户号、私钥、APIv3 Key和平台验签公钥，小程序统一走标准`/v3/pay/transactions/jsapi`，旧`pay_routine_open/pay_routine_mchid`分支退休。三类发起路径在注册对账意图和调用provider前都精确校验目标profile，修复routine-only部署被聚合`wechat_appid`误挡的问题；页面只展示公开字段和三profile就绪状态，私钥、证书/公钥内容、API Key不进入Admin响应、表单、数据库审计或日志。本地真实浏览器完成桌面与390×844保存、序列号`AB12CD34`规范化、零控制台错误和零页面横向溢出；Worker 209文件/1,318项单元、双TypeScript、Admin 2,437模块构建及362/362 Admin请求变体通过。生产Hyperdrive只读审计确认PostgreSQL 16.14、相关公开字段/三AppID/旧小程序商户字段/支付密钥数据库行、微信已支付订单、对账和退款支付行均为0；主Worker Secrets中也没有微信支付部署凭据，故运行时保持失败关闭。`site_url`有5行/2种值并继续归DB-003运营决策；临时审计Worker均已删除并复验404，本批未执行生产DDL/DML、支付调用或部署，真实三profile下单、支付/退款回调、真实角色和发布观察继续归FE-001G/H。实现提交`8cc086acbd555068bad319531e80f143300ea86b`；测试假PEM的精确历史扫描白名单修复为`e406852d03eaeecd5ddcbfaf74028bb0a6a511bf`，最终[Actions `33727980894`](https://github.com/cinagroup/cinashop/actions/runs/33727980894) 8/8成功。
  - [ ] **FE-001E 商品运营剩余操作面**：旧商品域12条启用路由已逐屏审计为6条候选、6条部分覆盖、0条整屏缺失；商品关联资料和安全 SKU 编辑已经候选收口，父项继续被受控 SKU 退役、批量操作和真实数据 E2E 阻塞，不能用目录页数量冒充 ADM-003 完成。
    - [x] **FE-001E1 商品12屏逐项证据与语义纠偏（候选完成）**：`audit/admin-legacy-product-route-parity.json` 为每条旧路由记录组件、状态、新页面、新 API、权限、覆盖与剩余缺口；确认旧 `/ensure/create/:id?` 实际是参数模板编辑页，旧 `product_attr` 才是 SKU 规格模板；搜索热词在 E4 落地前是唯一整屏缺失。定向测试固定12条分母，并随实证把状态从5/6/1推进为6/6/0，禁止按标题推定覆盖。
    - [x] **FE-001E2 SKU规格模板与商品参数模板目录（候选完成，未发布）**：`/product/metadata` 新增两类明确分隔的响应式 CRUD 页签；SKU规格模板支持1～3维、每维最多50个唯一值，参数模板支持最多100项、排序和逐项启停。前端接回 Worker 已有的平台作用域、advisory lock、事务替换和 `product.view/manage` 合同；本地桌面与390×844回归通过，精确实现提交 `ab24479` 的 Actions `33601245292` 8/8成功。
    - [x] **FE-001E3 商品关联资料（候选完成，未发布）**：商品表单已接入品牌、商品标签、保障多选和参数模板；创建/编辑在同一短事务同步保存 `store_product` 兼容列与 `store_product_relation` 的1/2/3/5/6类关系，保存后从 PostgreSQL事务内回读分类、品牌、标签、保障、模板及参数快照，任一不一致即整体回滚。商品行 advisory lock、候选行共享锁与删除行锁串行并发；品牌、商品标签、保障、参数模板删除均检查关系表及旧兼容列。桌面和390×844本地预览完成多选、模板套用与参数值编辑；本地199文件/1,265项单元、双 TypeScript、Admin 2,427模块构建及310/330接口合同通过。精确实现提交 `0e0fe3967623ae491323effa102e481930c3e147` 的 [Actions `33609577709`](https://github.com/cinagroup/cinashop/actions/runs/33609577709) 8/8成功（含 Linux workerd）。尚未连接生产 Hyperdrive，真实历史商品与受限角色验收仍归 E6。
    - [x] **FE-001E4 搜索热词（候选完成，未发布）**：`store_product_words` 新服务固定平台 `(type=0, relation_id=0, is_del=0)`，补齐列表、全部、详情、新增、编辑、显隐和软删除；名称/颜色/图标/排序/开关均有界校验，同作用域写入由短事务与 advisory lock 串行，并在同一事务写 `system_log`。公开热词读取同步修正为仅展示平台有效记录，不再泄漏 Supplier/已删行或错误依赖 `is_hot`。Admin 第五页签含响应式预览与四个可执行请求合同；本地类型、生产构建和定向19/19通过，精确实现提交 `90617dc0` 的 [Actions `33604769652`](https://github.com/cinagroup/cinashop/actions/runs/33604769652) 8/8成功（含 Linux workerd），尚未执行生产 DDL/DML或真实角色 E2E。
    - [ ] **FE-001E5 规格/SKU与批量运营**：安全编辑、全部旧商品批处理、平台与Supplier实物SKU可恢复退役均已候选落地，Admin与Supplier卡密/固定内容、手工虚拟商品及Admin次卡编排也已候选完成；生产`0126` DDL及当前SKU目录只读审计已完成。分店商品两张表已据活动DAO/路由和生产空表证据退休为兼容表，不再虚构“门店SKU”运行时。旧站非实物数据继承已判定不适用；新系统非实物样本、真实角色生产E2E、真实通知及发布观察仍未完成。
      - [x] **FE-001E5A SKU模板套用与安全编辑（候选完成，未发布）**：商品表单可套用1～3维规格模板并生成最多200个完整笛卡尔组合，编辑价格、库存、图片、成本、会员价、条码和编码。保存与商品主表、规格维度、属性快照、库存流水处于同一短事务；事务内回读主表汇总、维度、SKU身份与快照，任一不一致即回滚。历史 SKU 按 `suk` 保留 `unique/sales/sum_stock`，允许改字段和新增组合，但删除、重命名或改唯一标识失败关闭。后台、Supplier、Out、新人专享与优惠套餐五条唯一标识分配路径统一使用共享 advisory lock，修复不同锁域下的极端并发碰撞窗口。本地390px交互、双 TypeScript、Admin生产构建及200文件/1,272项单元通过；精确实现提交 `6bef6de8a67bd8d2c4df3d683684ad36198a471e` 的 [Actions `33613355706`](https://github.com/cinagroup/cinashop/actions/runs/33613355706) 8/8成功（含 Linux workerd、schema/route/observability、五端构建、生产依赖与全历史密钥扫描），未连接生产 Hyperdrive。
      - [x] **FE-001E5B 受控SKU退役与批量运营（候选完成，未发布）**：三个安全子项均已实现并经本地与Linux流水线验证；生产`0126` DDL与当前SKU目录审计已由E5C完成，真实角色/历史商品E2E和发布观察仍由E6及发布门禁承接。
        - [x] **FE-001E5B1 批量上下架、分类与商品标签（候选完成，未发布）**：主管理端已有实际多选入口，单次最多100个商品、每类最多50个关系；请求体上限8 KiB。服务在短事务内按升序锁定商品，校验删除/审核及候选关系，批量同步商品、购物车和关系行，并在同一事务精确回读主表与关系集合；缺行、多行、旧关系残留或状态不一致均整批回滚。每个商品写一条不含业务正文的管理员审计记录，响应必须带 `verified=true` 后前端才提示成功。桌面和390px真实控件验收通过，并修复移动端固定操作列遮挡选择框的问题；本地Worker类型、200文件/1,273项单元、Admin生产构建、schema/route/observability全部通过。精确实现提交 `01e7612fbfa14a175d789e32b20110af5e0be618` 的 [Actions `33615797183`](https://github.com/cinagroup/cinashop/actions/runs/33615797183) 8/8成功，未连接生产Hyperdrive。
        - [x] **FE-001E5B2 受控SKU退役与身份替换（候选完成，未发布）**：平台自营实物SKU新增可恢复退役状态、专用追加式证据表、管理员理由/依赖快照和数据库触发器；退役行不能删除，`product_id/type/suk/unique`不能在退役或转入退役时变更，库存只能由取消/退款等历史补偿增加。服务在商品锁与全局SKU身份锁下阻断未结购物车、未支付订单、秒杀/砍价/拼团/积分/套餐/新人活动、赠品、促销关系、抽奖及历史分店引用；订单、评价、库存流水和虚拟库存只作为保留历史计数。退役/恢复后必须仍是完整笛卡尔组合，并重建活跃维度、属性快照和商品汇总后从数据库回读；取消/退款到退役SKU不会把库存重新混入可售总库存。前台、加购、活动和Out库存路径排除退役行，Admin提供成组退役/恢复入口。物理重命名被明确替换为“新增完整新组合→退役旧组合”，不改写历史身份。本地201文件/1,280项单元、双TypeScript、Admin构建、263表定义和334个Admin路径变体通过；桌面与390px实际退役/恢复、内部横向滚动和零控制台错误通过。精确实现提交 `dd3e05b4abdf08f9a4ca2289663605c65b3ea3f6` 的 [Actions `33621216362`](https://github.com/cinagroup/cinashop/actions/runs/33621216362) 8/8成功。生产重复身份、目录形状及`0126` DDL已由E5C完成，Supplier实物范围由E5D补齐；真实角色、源历史多规格和非实物商品端生命周期仍未完成，因此继续只算候选。
        - [x] **FE-001E5B3 其余旧批处理类型（候选完成，未发布）**：主管理端补齐配送方式、赠送积分/优惠券、用户标签、推荐、系统表单、运费和品牌七类入口；只接受最多100个显式商品、每类最多50个关系和20张赠券，不再扩展筛选结果。操作在2秒锁等待/5秒语句上限的短事务内按序取得共享商品锁和行锁，候选资料以共享行锁验证状态与平台/商品owner范围，随后确定性替换主表、关系和赠券并完整回读，任一商品、候选或集合不一致均整批回滚；同请求安全重试收敛到同一状态。推荐改用独立标记列，避免旧实现把真实商品标签的type=3关系覆盖；直接赠券编辑也接入同一商品锁。桌面真实控件完成双商品赠券替换并收到回读成功，运费条件控件和390×844弹窗无横向溢出、控制台0错误。本地201文件/1,280项单元、双TypeScript、Admin生产构建及schema/route/observability通过；精确实现提交 `34f384f4f5b7446516f947c169e74c9f3ecf8c7e` 的 [Actions `33624903001`](https://github.com/cinagroup/cinashop/actions/runs/33624903001) 8/8成功。未连接生产Hyperdrive、未执行DDL/DML或部署。
      - [x] **FE-001E5C 生产SKU目录审计与`0126`受控DDL（完成，未发布）**：授权后通过Hyperdrive `9748c294e21c49a99579c9cef70102e0`确认PostgreSQL 16.14、SKU 2行/商品2个，均为平台自营未删除实物单规格；重复`unique`、重复商品内`suk`、空身份、负库存、孤儿和规格数量错配均为0。事务先取得2秒超时的独占表锁，再在15秒语句上限及100,000行/64 MiB界限内执行精确内嵌`0126`两遍：4列、日志表/序列、CHECK、3索引、函数、触发器共12个对象全部就绪，定义摘要两遍一致。SKU行数、旧字段摘要、库存137、销量93、累计入库0前后不变，退役/畸形默认/日志行均为0。独立只读Worker再次复核，令牌门禁403、错误方法404，临时Worker删除后URL 404。当前2个开放购物车和8个未付订单会阻止退役现有SKU，但不影响扩展DDL；没有执行商品/SKU/订单/购物车DML，也没有发布主Worker。精确提交`f9060c8736b94ed58882f8aada9127da2c35d13a`的[Actions `33830983585`](https://github.com/cinagroup/cinashop/actions/runs/33830983585)含Linux workerd在内8/8成功。
      - [x] **FE-001E5D Supplier实物SKU稳定编辑与受控退役（候选完成，未发布）**：旧PHP按数组位置覆盖SKU并删除尾部，迁移前Supplier TS更会删除全部普通SKU再插入，存在历史引用错绑/身份重建风险；现已改为复用共享稳定身份编辑器，已有行按`suk`保留`id/unique/sales/sum_stock`，只允许字段更新和新增组合，删除/改名及普通保存恢复退役行均失败关闭。Supplier退役/恢复只接受当前签名上下文派生的`type=2/relation_id=supplierId`，请求体不能覆盖租户；两条写路由归`supplier.product.manage`，详情明确拆分`attrs/retired_attrs`，页面要求理由、数据库回读成功才提示。生产只读复核确认71个商品全为平台owner，Supplier商品/SKU均为0，故未伪造真实业务租户；同一生产PostgreSQL随机schema调用真实服务完成2→3 SKU稳定扩容、删除拒绝、退役、退役后普通保存、伪恢复拒绝、跨租户拒绝、开放购物车阻断和恢复，生命周期日志2、Supplier系统日志2、Supplier归属库存流水5、最终3条活跃SKU且身份稳定。全部26张克隆表及相关公共序列摘要不变，schema、Worker均清理且URL 404。首轮harness的直接seed误落`public`一条精确标记分类，检查其引用为0后按完整`id/type/relation/name`删除；修正为显式事务search path并扩大公共摘要后最终演练通过。Supplier生产构建2,271模块、双TypeScript和专项28项测试通过；桌面实际控件退役/恢复与390×844无横向溢出、控制台0错误。精确实现提交`14880b2436cf5664d858e03ee2aba938468df1c6`的[Actions `33834851835`](https://github.com/cinagroup/cinashop/actions/runs/33834851835)含Linux workerd在内8/8成功。主Worker/Supplier前端均未部署；非实物商品、真实Supplier数据/token/角色及发布观察仍在E5/E6门禁。
      - [ ] **FE-001E5E 非实物商品权威与全生命周期**：卡密自动交付、卡密退款不可回收边界、手工虚拟商品编排与人工交付、次卡创建编辑/付款有效期/核销均已候选收口；旧站历史继承不适用，新系统样本、真实角色/真实通知和发布观察仍未闭环。
        - [x] **FE-001E5E0 商品类型权威与分店表退役（审计完成）**：活动Admin商品页只发布`0普通实物/1卡密或网盘自动发货/3手工虚拟发货/4次卡到店核销`，Supplier只发布0/1/3；`product_type=2`只存在服务/注释残留，活动UI和支付履约分支均没有入口，按未发布版本残留处理。所谓Branch service实际注入并改写主`store_product`与`store_product_attr_value` DAO，Supplier活动路由也未映射Branch属性更新方法；生产`store_branch_product`和`store_branch_product_attr_value`均为0，因此两表只保留无损历史迁移，不建立第二套门店SKU权威。
        - [x] **FE-001E5E1 次卡付款有效期与混单边界（候选完成，未发布）**：次卡模式2在下单时保存不可变SKU配置快照并保持`write_start/write_end=0`，支付outbox完成Supplier分配后以订单持久化`pay_time`原子激活“购买后N天”，固定区间和永久模式保持原语义；行锁、旧值条件更新和已激活窗口保留保证并发及重放不漂移，缺失历史快照不读取可变实时SKU。不同`product_type`现禁止同单，手工虚拟类型3免收货地址和运费。生产只读确认71个有效商品全为平台实物、次卡订单行及虚拟库存均为0；随机隔离schema调用真实服务验证3条次卡、1条实物、支付激活、重放、未支付拒绝及公共表/序列摘要不变，临时Worker `cinashop-second-card-validity-audit-d8c3793a4222`已删除且URL 404。专项12/12、Worker 210文件/1,330项、双TypeScript和schema/route/observability均通过；实现提交`55f29dd73657ce792915bd61543aa661212bb524`。主Worker未发布，生产现有业务行未修改。
        - [ ] **FE-001E5E2 卡密/网盘商品创建编辑与稳定库存**：Admin与Supplier类型1商品创建/编辑、两类SKU库存权威、可恢复退役、不可变订单交付快照及卡密退款不可回收边界已候选完成，并在生产Hyperdrive随机schema验证自动交付、缺货回滚、重放、退款/交付互斥、Supplier租户边界与退役库存隔离；旧站历史继承不适用，真实角色和新系统非空样本仍未完成。
          - [x] **FE-001E5E2A Admin类型1编排与不可变交付（候选完成，未发布）**：Admin新增创建时锁定的`0实物/1卡密或网盘`履约类型，逐SKU显式选择一次性卡密或固定内容。固定内容可维护库存；卡库型新SKU必须从0开始、已有库存不能普通编辑，已有卡记录时禁止改为固定内容。SKU稳定身份、维度/属性快照、库存流水和商品汇总继续同事务回读。下单把含空值在内的`disk_info`写入不可变`cart_info.sku`，支付交付只接受新快照或PHP历史`productInfo.attrInfo`，缺失/畸形快照失败关闭，不再读取可变实时SKU。生产随机schema分别实际验证Admin类型1创建/回读、卡密导入、非法库存和模式切换拒绝、类型不可变、合法更新后身份稳定，以及支付双连接单赢家、无重复卡、缺货全回滚/补货重试、重放幂等和实时SKU被改后仍交付下单快照；所有隔离schema和临时Worker均删除且公共表/序列不变。桌面实际表单验证卡库默认库存锁定、切换固定内容后库存与4KiB内容可编辑；主Worker/Admin未发布。
          - [x] **FE-001E5E2B Supplier类型1编排与退役库存隔离（候选完成，未发布）**：Supplier保存服务只开放`0实物/1卡密或固定内容`，创建后履约类型不可变；类型1强制无物流/零运费，每个SKU显式选择一次性卡密或固定内容，仍要求Supplier结算价并复用稳定身份编辑器。一次性卡密必须从0库存开始且只能经卡密导入增加，商品普通保存与`saveStocks`均不能改写；固定内容允许维护库存。类型1现可受控退役/恢复，退役SKU从库存列表/预警/导入/敏感导出和普通库存调整中排除，卡记录作为历史保留。Supplier页面恢复创建/编辑入口，卡密库存禁用、固定内容库存可编辑，保存后进入现有安全库存页；preview实际交互发现并修复Element Plus库存控件禁用状态复用。生产Hyperdrive随机schema调用真实Supplier保存、库存和退役服务，最终Worker`cinashop-supplier-sku-audit-0a248457b9b2`全部断言为true，26张公共表/序列指纹不变，schema与Worker删除且URL 404。Worker 210文件/1,335项单元、双TypeScript、Supplier 2,271模块构建及专项41项通过；本机Windows runtime仍因`workerd`访问冲突未启动，主Worker/Supplier前端未发布。
          - [ ] **FE-001E5E2C 卡密退款与真实角色闭环**：退款与密钥不可回收子项已候选完成；旧站历史数据不纳入本部署，真实角色、真实通知和发布观察仍须闭环。
            - [x] **FE-001E5E2C1 卡密退款与不可回收密钥边界（候选完成，未发布）**：客户只能对未发放的一次性卡密申请仅退款；已分配或已暴露密钥失败关闭，只有带审计上下文的Admin善意退款可继续，且卡记录永不重置为可售。退款创建、持久化执行和最终入账均复核权威卡分配，退款与支付交付先锁订单再读售后/锁商品行；进行中退款阻止交付，部分完成退款只交付剩余数量，全额退款不生成空交付。固定内容继续按不可变订单快照和`is_support_refund`处理。PC/UniApp隐藏不允许入口和虚拟退货选项，申请页重新读取服务端权威状态。生产PostgreSQL 16.14随机schema真实服务场景全部通过，`public`前后不变，临时schema/Worker清理完成；生产类型1商品、SKU、卡和订单仍全为0，主Worker及前端未发布。
            - [x] **FE-001E5E2C2 旧站历史数据决策（不适用）**：2026-09-04 项目所有者确认不承接旧 PHP 站真实历史数据；类型1商品、SKU、固定内容、卡密库存、订单快照及卡归属不再要求源 MySQL逐行复制或对账。
            - [ ] **FE-001E5E2C3 真实角色、通知与发布闭环**：在新系统创建受控类型1商品、SKU、固定内容和卡密样本；使用主管理员、Supplier管理员、只读/编辑受限角色和真实客户完成下单、付款、交付、通知、越权、外部失败重试及发布观察。凭据不得写入仓库或聊天记录。
        - [x] **FE-001E5E3 手工虚拟商品创建编辑与订单闭环（候选完成，未发布）**：Admin与Supplier恢复创建时可选、编辑时不可变的类型3；强制无物流/零运费、禁止到店自提，沿用普通SKU稳定身份、库存调整和可恢复退役，并显式保存`is_support_refund`。Admin/Supplier人工交付只接受已付款类型3整单、非空有界内容；类型3快递/拆单、实物人工交付、类型1/4人工交付和跨Supplier操作均失败关闭。客户订单详情仅在本人订单已付款且已交付后返回`fictitious_content`，PC/UniApp按“人工交付”展示，通知outbox只带订单标识而不复制交付正文。生产Hyperdrive随机schema调用真实商品、履约和退款服务：Supplier类型3创建、普通库存、稳定编辑、退役/恢复全部通过；最终履约Worker`cinashop-manual-virtual-audit-44ca5c91183c`验证19项边界、退款允许/拒绝与重放，状态证据、`delivery_fictitious`和单一通知outbox正确，公共表/序列指纹不变，schema与Worker均删除且URL 404。Admin/Supplier表单和Supplier交付弹窗已做本地实际控件QA，Worker 211文件/1,342项单元、双TypeScript及四端生产构建通过；精确实现提交`be61e36a1bda2af0e30af17484b4bf3a39587de3`的[Actions `33856242181`](https://github.com/cinagroup/cinashop/actions/runs/33856242181)重跑后8/8成功。主Worker/前端未部署，源类型3历史数据、真实角色与外部通知失败重试仍归E5E5/发布门禁。
        - [x] **FE-001E5E4 次卡商品创建编辑与核销闭环（候选完成，未发布）**：按活动PHP权威恢复仅Admin可创建/编辑的类型4单规格单SKU，核销次数1～99,999,999，支持永久、购买后1～3650天及合法固定区间；类型4强制门店自提/零运费，稳定保存并回读五个核销字段，PC/UniApp在结算和订单详情展示自提及剩余次数/有效期。生产Hyperdrive随机schema调用真实商品、支付激活、核销、退款和提醒服务：创建/更新/稳定SKU、自提策略、支付时激活、部分及完整核销、旧码重复、过期、跨门店、未核销退款允许、已消费退款拒绝、提醒幂等及Queue载荷不含手机号/渲染正文全部通过，2条核销证据不可变；公共表/序列指纹不变，schema和最终Worker`cinashop-second-card-product-audit-bf867156a53b`均清理且URL 404。生产只读同时确认当前类型4商品/订单/提醒outbox/两类通知模板均为0，提醒配置未落库而运行时默认提前1小时，故真实模板、通知、角色和历史数据仍归E5E5。Admin桌面及390×844实际控件、PC/UniApp构建、Worker 212文件/1,349项单元和双TypeScript通过；实现提交`a74d1b2f08294cdd7a745d5920d03b3e6b458bbb`及严格网络重试提交`e6a7cd7964f792a8f038290d05391d281217bc77`的[Actions `33866228404`](https://github.com/cinagroup/cinashop/actions/runs/33866228404)最终8/8成功。主Worker/前端未部署，生产公共业务行未修改。
        - [ ] **FE-001E5E5 非实物新系统数据、角色与发布**：创建并验收类型1/3/4商品、SKU、卡密库存、通知模板和测试订单；使用主管理员、Supplier管理员、只读/编辑受限角色及真实用户完成桌面/移动端E2E，另行批准后发布并观察outbox、库存、核销和通知。
    - [ ] **FE-001E6 真实商品 E2E**：使用主管理员及只读/编辑受限角色，对新系统受控单规格、多规格、虚拟商品执行读、改、取消、重试和移动端流程；生产写测试需独立批准并限定可恢复测试记录。
  - [x] **FE-001F 当前 Admin 请求入口静态 501 清零（候选完成，未做生产 E2E）**：可重复 AST 审计现覆盖 Admin API 模块及页面脚本310个请求调用点，展开联合动作、条件路径和本地 URL 映射后为330个路径变体；与 `/adminapi` 的方法、路径和处理器逐项核对结果为可执行330、未注册0、未解析0、命中受控不可用0。审计修复通知轮询缺路由、商品删除方法、退款路径和用户余额接口；余额调整改接已有事务、幂等、流水和审计实现，旧 `set_other` 别名同步失败关闭到安全实现。会整表读取并任意批量改写生产键值的通用配置页不接危险接口，改为安全停用说明及两个专用配置入口。企业微信远端写路由仍在 Worker 受控不可用，但当前新 Admin 没有请求调用命中它们；真实角色、数据和浏览器写流程归 FE-001G。
  - [ ] **FE-001G 真实权限与生产数据 E2E**：用主管理员和受限角色验证菜单可见性、`*.view/*.manage` 正反权限、真实历史数据、移动端和失败恢复；本地 `preview=1` 只算候选 UI 证据。
  - [ ] **FE-001H Admin Pages 预发、发布与观察**：确认正式 Pages 项目、同源 proxy、Worker Origin和 Secret/资源映射；另行取得发布批准后记录 deployment/Git SHA并观察 501、4xx/5xx、Hyperdrive和关键写入对账。
- [ ] **FE-002 PC 对账**：旧 PC 30、新 PC TS 29 个页面组件；22 条 `/api/pc` 合同和扫码/OAuth 本地 UI闭环已完成。token/UID 已改为 per-tab `sessionStorage`，刷新保留、新标签页不共享且启动清除旧持久值；关闭标签页不等于服务端 logout。仍需支付回跳、手机号安全验证、旧 Nuxt退流和全订单生命周期真实 E2E。
  - [x] **FE-002A 生产匿名浏览器冒烟（仅发现与导航证据完成）**：2026-09-06用户授权后，通过CUA应用内浏览器验证正式PC首页非空、搜索回车及按钮跳转、公开商品详情导航、未登录购物车转到`/login?redirect=/cart`；观察到的页面无框架错误层，采集的console error/warn为空。已做默认窗口及390×844窄屏检查，后者失败见D；截图已在任务中展示。没有提交登录/短信/购物车写入/订单/支付或执行数据库命令，未发布代码。此项不关闭FE-002或真实角色、支付、生产发布门禁。
  - [x] **FE-002B 商品详情字段合同（Linux及当前线上只读复验完成）**：2026-09-06确认线上`/goods/70`标题为空，公开200响应使用`storeName/storeInfo/sliderImage/otPrice/vipPrice`且没有`cart_button`。PC API已接显式运行时映射，兼容camelCase与旧snake_case、保留原始金额字符串；缺失开关只在商品类型/预售/表单均明确为0时推导允许，显式禁用、无库存、下架/删除及限制商品均禁用购买。新增10项回归覆盖实际详情服务（隔离DAO）非缓存/缓存/多规格输出、两种字段形状、价格、限制和坏数据，连同旧PC合同本地16项通过；CUA本地只读夹具验证标题/简介/原价/会员价、数量1→2、预售两按钮禁用及刷新，console error/warn为空。提交`9308448`的[Actions34039794059](https://github.com/cinagroup/cinashop/actions/runs/34039794059)首轮11/11通过，256文件1652项单元零跳过，新增10项实际在第二片执行；PC构建与其余门禁通过。当前PC Production固定部署 b4d86172 源自 7a5508f 且包含本项修复，公开别名与固定部署 HTML/入口脚本一致；2026-09-24 只读 Edge 实页显示商品70标题、¥99.90及无有效规格禁购提示，两个购买按钮均禁用，控制台和网络错误为0，详见[上线复验证据](workers-ts/audit/pc-product-detail-live-recheck-20260924.json)。当前商品主表库存100但无可选SKU，页面可购买库存为0；真实商品SKU、素材、登录及交易仍归 FE-002C/E 等项，不能外推整条 FE-002。
  - [ ] **FE-002C 商品图片失败态与运营素材（失败态本地候选完成，素材/CI/发布仍开放）**：历史线上证据为首页8张非空URL均指向`https://via.placeholder.com/300`且naturalWidth=0，旧`item.image || placeholder`不能处理失败。本轮ProductImage接入15个PC商品页面：空URL显示“暂无商品图片”，实际错误显示“图片加载失败”；不发起替代域名请求、不自动重试、不改商品源数据。新URL重置失败状态，脱离页面/旧SKU图片的延迟错误不能影响当前图片；空详情轮播改读真实主图，主图也缺失时明确提示。7项实际Vue模板/生命周期测试、60项旧PC测试、33项Worker回归及PC类型/构建通过。隔离Chrome在1280/390px验证四类图片、首页点击详情、空/混合轮播、排序/搜索与秒杀选规恢复，无新增横向溢出；只有预设404，无应用异常、外部请求或API写入。真实素材、登录后的收藏/券/订单页面浏览器验收、其他浏览器/真机、精确SHA CI与发布后复验仍待完成。不能用LOCAL TEST夹具冒充素材迁移；同名但不同ID的商品仍不去重/删除。
  - [ ] **FE-002D PC窄屏导航布局（候选通过Linux，待真实角色与发布后复验）**：保留历史失败：线上390px搜索页宽375/608；本地详情宽375/791且导航、搜索隐藏。2026-09-07候选改为响应式网格导航、始终可见的单一搜索表单、900px以下纵向详情、等比例画廊及可收缩商品网格，不隐藏页面横向溢出来掩盖问题。CUA隔离夹具在320/390/768/1024/1280×844下详情页clientWidth/scrollWidth分别同为305/375/753/1009/1265，所有可见头部控件均在水平边界内；窄屏搜索回车/按钮、空结果恢复、超长关键词、分类→列表、登录入口、轮播切换和数量交互通过，console error/warn为空。新增6项结构/夹具回归，连同详情及PC合同共22项通过，类型/构建通过。提交`69067f2`的[Actions34072256820](https://github.com/cinagroup/cinashop/actions/runs/34072256820)首轮11/11通过，257文件1658项单元零跳过，新增6项在第二片实际执行。2026-09-24 对当前 PC Production 固定部署 b4d86172 只读复核：无头 Edge 在320/390/768×844的搜索和商品70共六页均无页面级横向溢出，导航与搜索控件在边界内，仅观察到GET，控制台与非取消网络错误均为0；证据见`audit/pc-responsive-live-recheck-20260924.json`。已登录角色、搭配购弹窗和真机仍未验收，本项继续开放；不外推H5/小程序/App。
  - [ ] **FE-002E 真实规格与立即购买闭环（普通商品候选通过Linux，待完整交易与发布后复验）**：旧PC按已选规格读取库存/unique，提交`new=1`后进入独立订单确认；审计起点：新GoodsDetail原先拼接`sku+商品ID`且buyNow仅提示接入中并普通加购。需贯通详情attr_value→规格选择→所选价格/库存/数量→真实unique提交→正确立即购买标记/返回ID→只确认目标商品；同时验证普通加购不退化、无效/无库存规格拒绝、重复点击/失败恢复、登录后返回、空/非法确认ID失败关闭，以及普通/直接购买购物车的读取合同，不能只加一次页面跳转。先做隔离合同/浏览器正反场景，再做经批准的真实角色/交易验收；本批已接真实规格选择/金额库存/独立new=1行及指定ID确认，修复双向购物车误合并、确认页参数回落和普通勾选丢失；12文件82项本地回归、双类型/PC构建及390/1280px隔离浏览器通过。提交`eb1a39d`的[Actions34074786807](https://github.com/cinagroup/cinashop/actions/runs/34074786807)首轮11/11通过，259文件1672项单元零跳过，新增购物车9项在专用PG16、选择5项在第二片实际执行。真实角色、完整建单/支付/报价、会话过期返回、套餐/拼团规格、服务端并发和发布后复验保持开放，不将按钮锁等同事务并发安全。
    - **完整报价增量（2026-09-07，本地候选通过，待精确Linux）**：确认页改接既有confirm/computed服务端完整报价，金额及商品单价来自同次预览；地址/配送/积分变化立即作废旧报价，乱序/失败不得恢复旧金额或启用提交。computed保留旧平铺金额并补商品/地址/key快照，confirm补积分选项；建单冻结同一key/addressId/配送/表单，重试不自动付款。15文件109项本地回归、Worker双类型和PC构建通过；隔离真实SQL报价5项、PC报价8项为新增测试。CUA在390/1280px验证19.20→积分18.70→另一地址21.70→自提15.70、报价失败恢复、乱序及捕获式两次重试内容相同；修复手机商品表格逐字竖排。没有创建订单或扣减库存/积分，不能等同实际计费一致性。优惠券选择仍固定0，会话过期返回、慢表单加载瞬间的浏览器禁用态、真实建单/支付、活动/并发及发布后复验继续开放；发布须先部署兼容的Worker报价响应，再发布PC。
    - **后续先决条件**：表单必填/格式预校验、上传中禁交与明确拒绝后的安全纠错见下方增量；会话失效仍需完整search/hash返回、旧请求不清除新token及Pinia同步。选券前先修PC优惠券状态路径参数与后端query不一致、camelCase/旧字段适配，再以实际商品范围/门槛和完整报价验收；不能把“我的优惠券”列表直接当结算可用券。未知下单结果不允许盲目换key重试，硬刷新/离页后的意图恢复仍需独立验证。
    - **Linux首轮未通过，合同修正待复核**：f982683/Actions34077838707最终9/11；第一片131文件860项通过，第二片130文件825项中1项失败，准确原因是newcomer旧测试仍断言apiFirstOrderQuote。新增报价13项全部通过（SQL5项实际PG16），不能据此忽略整体失败。已改旧测试验证完整报价接线、首单金额/互斥展示并禁止恢复局部报价，保留全部6项测试；最终本地16文件115项通过，待修正提交完整Linux。下一批优先表单校验/上传/拒绝纠错，其余顺序如上；本轮未发布。
    - **报价修正Linux已验收**：478ba79/Actions34078783081首轮11/11成功，261文件1,685项零跳过；两片实际清单与覆盖核验通过，新增报价13项及newcomer6项均通过。上方首轮失败/待复核文字保留为历史；本条取代报价候选的待精确Linux状态，不代表完整交易或发布通过。
    - **表单增量本地候选通过，待精确Linux**：共用服务端纯校验规则（10类组件/必填/格式/选项/容量），上传等待禁交、按字段/生命周期隔离迟到结果；只有同key、此前无未知尝试、且事务明确拒绝表单才解锁纠错，未知结果继续冻结，不自动付款。新增36项逻辑/接线及5项SQL/控制器回归，相关14文件117项、双类型及PC构建通过；隔离真实SQL证明表单拒绝回滚、修正同key只建1笔18.70元未付单且重试无重复扣减。CUA 390/1280px验证必填/格式拦截、明确拒绝纠错→未知结果→后续拒绝仍锁定；3次捕获请求同key，第二/三次内容完全相同。实际临时图片上传成功/失败后的等待、禁交、恢复/移除通过，不写R2；跨页迟到仍只经状态机测试。Windows workerd访问冲突导致0项运行时执行，未算通过。真实R2、角色、支付、活动/并发和生产发布仍开放。
    - **表单候选Linux已验收**：f1d3ec4/[Actions34081360728](https://github.com/cinagroup/cinashop/actions/runs/34081360728)首轮11/11成功，263文件1,726项单元零跳过，两片实际清单与覆盖证明通过；36项表单逻辑在第一片、5项真实PG16事务在第二片通过，workerd亦通过。本条取代上一表单增量的待Linux状态，保留Windows启动失败记录，不关闭真实R2/完整交易门禁。
    - **会话返回/隔离本地候选通过，待精确Linux**：失效返回保留query/hash，拒绝外部/编码歧义及login循环；请求绑定当时会话版本/token，旧成功或失效响应不能退出新身份或回填旧数据。token/UID原子更新并同步Pinia/购物车，迟到logout及旧购物车finally不覆盖新会话；退款/物流补登录守卫，详情购买补hash；导航栏和个人中心两个退出入口均核对当前会话与撤销状态，个人中心503模拟实际显示未确认警告并退出本机。Worker相关16文件134项、PC真实Axios/Pinia新增10项、双类型/PC构建通过，10项已纳入PC build。CUA 390/1280px验证失效重登完整结算返回、旧会话20秒迟到失效不登出新会话、退出后退款/物流守卫，以及详情登录后恢复campaign/SKU/数量2/#buy且不自动购买。无真实账号/交易或发布；未知建单意图硬刷新/离页恢复、优惠券、活动/并发、真实provider继续开放。
    - **会话候选Linux已验收**：a4974b7/[Actions34083722664](https://github.com/cinagroup/cinashop/actions/runs/34083722664)首轮11/11成功，264文件1,730项Worker单元零跳过，两片清单/覆盖证明通过；导航4项及PC实际Axios/Pinia10项分别通过，workerd亦通过。取代上方会话增量的待Linux状态，不关闭真实角色/provider或完整交易门禁。
    - **优惠券钱包/选择本地候选，待精确Linux**：修复0/1/2路径与query覆盖、camelCase/旧字段映射，区分现金/折扣、未来/过期/失效及未付订单占用3，所有者绑定、私有无缓存、有界游标分页。PC钱包切换/加载失败/重试与结算选择/取消接入权威报价；候选券不等同订单预筛可用券，未知提交后冻结couponId与原payload。新增16项纯逻辑/接线及5项真实SQL（含1笔16.00元未付单及幂等重试）；相关20文件168项曾全通过，收尾复跑一项旧购物车初始化超时造成9项跳过，独立9项及随后完整20文件168项复核通过（12.23秒），保留失败且不宣称根因已修复。双类型、PC实际10项/构建及390/1280px浏览器通过：现金16.00、8.5折18.30、取消21.00；范围/门槛拒绝禁交，两次捕获key/body相同且券41，没有浏览器建单。Windows workerd启动崩溃、0测试不算通过；未改生产/DDL/依赖/工作流或发布。
    - **钱包/选择候选Linux已验收**：7e0b175/[Actions34086118111](https://github.com/cinagroup/cinashop/actions/runs/34086118111) 为11/11；266文件1,751项单元零跳过，两片实际执行清单/覆盖证明通过，新增16项钱包合同与5项隔离PG16回归实际执行，workerd及五端构建通过。本条取代上述钱包候选待Linux状态，保留本地失败历史，不关闭订单预筛/跨端或发布门禁。
    - [ ] **FE-002E-C 独立订单筛券与跨端优惠券合同**：普通订单后端已通过精确Linux，PC新接口/空适用页继续扫描已通过本地候选验收，见下方增量；继续补UniApp折扣/分页/完整报价与冻结幂等（FE-003L）、旧type/issue_type筛选及商品/分类/品牌具体范围和层级展示。旧促销overlay包含5才允许叠券的规则尚未完整迁入新报价/建单，必须以同一商品金额链补齐，不能只在筛券处隐藏；首单/普通营销互斥通过不等于促销叠加已完成。跨端、并发、真实角色/provider及发布后验收仍为开放门禁。
      - **普通订单筛券后端增量（本地候选，待精确Linux）**：新增已鉴权且受station开关控制的旧路径，按本人cartId/new及当前会员商品价筛选，忽略客户端path price；与真实quote/create复用范围、门槛及现金/折扣计算，首单即使优惠为0也互斥，营销单不叠券。自提可先筛券再选门店，普通quote/create仍要求门店。只读private/no-store、每页最多100/旧无分页1000且超限明确失败，游标取最后扫描候选而非最后适用券；关系/分类/品牌批量加载，不对每券重跑报价。23项新增解析/SQL回归；最终类型、相关回归与未接前端/未发布边界以审计末尾为准，不勾选C或父项。
      - **订单筛券后端Linux已验收**：d33bccd/[Actions34088007196](https://github.com/cinagroup/cinashop/actions/runs/34088007196)首轮11/11、267文件1,774项零跳过；新增23项在专用PG16实际通过，分片完整/互斥/实际文件核验与workerd通过。本条取代后端待Linux状态。
      - **PC订单筛券本地候选（待精确Linux）**：已接订单接口而非钱包，独立适配最后扫描游标，空页继续/同游标重试；数量、SKU、目标行、new及配送范围变化清空旧选择并拒绝迟到响应。卡片显示服务端适用金额/预计抵扣，选券仍走完整报价；未选券刷新也更新总价，未知提交暂停在途结果并冻结选择。26项合同/状态机+5项真实控制器/SQL回归通过，相关18文件184项（12.29秒）、双类型和PC10项/构建通过；CUA桌面/手机验证空页失败重试、18元门槛、16.00/18.30/取消21.00、首单刷新19.20、配送迟到、1件/2件及new=0/1隔离、相同key/body的两次捕获。0订单/0账单，无生产调用或发布；跨端C仍开放。
- [ ] **FE-003 UniApp 对账**：已纠正“旧250、新55个页面组件”的失真口径：旧 `pages/` 下虽有250个Vue文件，但 `pages.json` 只有151条逻辑路由（H5实际151，MP-WEIXIN/APP-PLUS各150）；新端当前是93个页面文件/93条各平台路由。151条旧逻辑路由已唯一归档为28条原路径直达、100条兼容规则和23条显式缺口；逐项证据、查询参数兼容与11项 granular checklist 固化在 `workers-ts/audit/uniapp-frontend-parity.json`。扫码登录确认页已完成目标/Origin/设备核对和本地登录返回闭环，真实设备、账号和支付回调仍未验收。
  - [x] **FE-003A 活动清单与旧链接兼容合同（候选完成，未发布）**：源清单SHA-256锁定后复跑确认151条旧路由、新端59条路由；新端建立与 `pages.json` 同步的注册路由白名单，97条旧路由按60条候选覆盖/37条部分替代显式映射，订单详情 `order_id→orderId`、支付结果和搜索参数完成兼容。未知 `/pages/*` 不再被静默放行；DIY、悬浮导航、开屏广告和首页Banner统一经过同一解析器，Tab页使用 `switchTab`。6项专项回归、UniApp类型检查通过。
  - [ ] **FE-003B 用户治理与历史页**：2026-09-21足迹列表/日期分组/分页/确认删除/加入收藏及空态推荐已接入Worker合同，品牌/标签/等级与SVIP参考价、真实秒杀/砍价/拼团ID及有效期导航、异常/过期拦截和推荐SQL本地通过；旧服务端缺少新元数据时才兼容活动专区。2026-09-24 旧隐私正文和三项协议目录已有公开只读候选；`payVip` 已独立读取会员协议，不再误映射用户协议，按旧公开接口显示停用记录正文，会员主页另行隐藏。注销软删除已阻断旧令牌继续鉴权及手机号误占用，但完整 `cancel/user`、资金/订单处置和页面仍缺；会员码两个用途、扫码落点与支付消费端仍缺，详见[会员码恢复合同](workers-ts/docs/member-code-recovery.md)和[注销边界](workers-ts/docs/account-cancellation-boundary.md)。预售详情、促销边框及真实账号/真机验收亦待补齐，不能提前关闭父项。
  - [x] **FE-003C 分销商/代理商自助流程（代码/本地完成，未发布）**：旧5条申请、状态、记录和员工路由已按原路径直达，分销员与代理商分别读取/提交各自合同；两类协议按最新类型2记录判定启用，服务端停用时在短信消费前拒绝且事务内再校验；申请短信用途隔离、一次性消费，代理商和分销员均在消费前做只读业务预检，事务内仍复核。390×844本地H5模拟API已渲染旧代理申请→状态、双域记录、旧分销员申请及员工只读列表；代理与分销时间统一按上海时间展示。UniApp 工具链752/752、Worker专项19/19、原生PG16路由/并发5/5、三端构建证据见 `workers-ts/audit/fe003c-h5-local-smoke.md`；合入前最终精确头 Linux CI 须全绿。旧 `agent/record` 调用的是Supplier记录并跳Supplier页面，属于源端误接；新页展示两类当前有效申请，不承诺已删除历史。代理提交新增专用短信码门禁，旧已安装包若仍发送 `type=agent` 或旧弱缓存码，须原位升级；线上AppID、签名、版本和API域名身份未提供，旧客户端门禁归FE-003K保持开放，不设弱兼容回退。FE-003J/K真实设备、账号、数据和发布仍开放。
  - [ ] **FE-003D 活动长尾**：预售旧入口已有部分替代，新人专享旧 `/pages/activity/new_customer/index` 已直达本地只读礼遇／9件分页商品目录和活动详情；旧活动商品 type=7/1/3/6 查询深链精确进入对应活动详情。直播列表、商品排行两条旧路由也已直达本地只读页面；排行现附当前页商品的活动身份/有效期权威投影，客户端只跳严格匹配且未过期的目标。直播插件未以真实小程序验收。预售 type=6 基础 SKU 全款加购／建单已有本地候选，但累计限购及真实发布仍开放；新人 type=7 的活动规格加购和权威结算尚未接线，不能按普通商品价成交。真实登录、运营内容、真机、Linux CI 与发布验收未齐，不能因路由直达关闭本项。
  - [ ] **FE-003E 移动Admin跨端退流**：16条旧移动Admin管理路由已转向Admin Web的候选能力盘点，仍需移动端覆盖和退役跳转验收；5条核销相关路由已有受角色约束的 `/pages/operator/writeoff` 部分/候选落点。
  - [ ] **FE-003F 支付回调、历史深链与通用WebView**：账户/会员/订单支付已聚合到新流程，但支付宝回跳、收银台、组合支付、线下支付和会员历史必须用真实Provider验证；旧任意URL WebView保持退役，除非设计域名白名单。2026-09-20，type=3已补本地只读报价、持久建单、余额结算、支付路径互斥、独立回调/查单证据和首次持久派发恢复候选；强制顾客鉴权的create/pay/detail/pay-type/history已接实际apiRoutes，本轮18项workerd/JWT/本地Hyperdrive/PG HTTP测试通过。详情核验资金凭据，列表只给本人消费线索；方式发现读取当前DB并作无外发预检，非付款授权。PC／Uni分步收银、原请求持久化、付款前方式重验、只读结果，以及无本地记录时分页／单号查找并保存原单恢复指针已本地接线；模型63项、Uni运行时44项及浏览器独立来源验证刷新恢复、无自动建单付款、账号隔离与原记录防覆盖，桌面／390×844复验。后续本地已补微信H5及支付宝完成/退出原单回跳、PC独立只读结果页和固定Pages303；170项Node／93项workerd、53项PG链路与桌面/H5登录恢复验证见同文最新章节。后续已完成外部0159／内嵌0165／277表ORM正式注册、显式空ORM保护补装及九路径真实PG目录一致性验证，见[注册验收](workers-ts/docs/offline-order-registration.md)。后续后台只读页面／收银码已完成实际浏览器→workerd→本地Hyperdrive→PG16联合验收。2026-09-20所有者确认保持PHP范围，暂不新增type=3退款，此能力按本次迁移不适用；不确定付款仍保留原单与支付路径。真实跨设备会话／原生真机、真实平台回跳与未知结果恢复、完整生产角色／协调迁移发布与真实Provider验收仍未完成，未部署。见[独立收银域审计](workers-ts/docs/offline-order-quote.md)、[首次派发与剩余目标](workers-ts/docs/offline-order-payment-dispatch.md)、[运行时证据边界](workers-ts/docs/offline-order-workerd-verification.md)、[HTTP与前端验收](workers-ts/docs/offline-order-http.md)，不据本地接线关闭本项。
    - 后台记录（2026-09-20，本地通过，未部署）：独立只读 scan_list/scan_detail 复用财务凭据，保留异常记录但不伪称已付；后端15项PG/workerd、客户HTTP20项和运行器54项及双类型通过。后续新增 /order/offline 页面与严格API解码，65项新前端/44项订单回归/15项真实响应解码联验及桌面/手机浏览器筛选、分页、撤权/跨标签退出通过；本地合成UI无业务写入。二维码与真实生产门禁仍开放，见[后台只读合同与页面验收](workers-ts/docs/admin-offline-order-read.md)。不因本地页面完成关闭父项。
    - 最低应付规则（2026-09-20，本地通过，未部署）：用户明确禁止零元结算，应付至少0.01元。报价、锁后准入及三个支付方式落实；历史零元只读，不赠分、不自动加价。320项相关测试、双Worker类型、PC/H5构建及两端浏览器边界/恢复通过，见上述最新审计。此子规则完成不关闭整体收银/发布门禁。
    - 2026-09-20后续增加[实际连接只读权限预检与共享授权合同](workers-ts/docs/offline-runtime-permissions.md)，同一合同下权限/HTTP/本地Hyperdrive 55项、签名回调32项、静态/运行器67项及双类型通过。仅认证声明的收银/收款恢复权限范围，不授予或认证完整生产角色，不据此关闭本项。
    - 2026-09-14本地增量已将现代UniApp订单支付结果页改为服务器详情回读，URL不再证明付款或金额；29项新增/完整290项及隔离SQL浏览器桌面/手机验证通过，首次工具链停滞另留记录。既有付款记录为夹具预置，不是provider支付/回调验收；历史支付深链、支付奖励/抽奖/赠券完整旧页面行为和本父项其余门禁仍开放。
  - [ ] **FE-003G 反馈、企业Work与代客下单**：旧代客记录、选客、选品、确认、收银五条路由均有本地候选，另有只读订单详情；反馈1条与 Work 5条均已注册本地候选 UI；真实企业微信授权及登录反馈提交尚未验收。独立 Admin 会话限定建单管理员读自己的未删除付款根单及最小列表／商品快照，不返回收货、购物车、退款原文；会话到期清屏。确认／重算、结构化收货、积分／候选券、十类商品表单、私有图片上传／预览续签和原请求冻结恢复已接通。收银须管理员显式发起，先查本人付款状态与最新详情；零元代客按旧 PHP 行为显式确认，不套用 type=3 的最低 0.01 元政策，扫码 QR 本机绘制。所有支付带预期金额，现金／零元锁单后按整数分比较、原生 PG flow 39/39 通过。旧 PHP 记录客户端依赖已删除整行字段，公开列表仅接受 `paging=cursor` 并向旧请求返回升级提示；所有者确认线上小程序与 App 需原位升级，但新 manifest 标识为空、版本低于旧源码，真实包身份／签名／分发未核验，此接口门禁不等于整包升级。列表索引／游标有本地原生 PG 候选，生产表规模、索引安装及关键词／状态规模计划仍待验收。同渠道重复发起／锁后金额竞态已有本地声明守卫，callback-first 未提交证据竞态已由订单粒度锁与查单预登记在本地原生 PG 收口；真实签名回调／登录后收银／真机、完整优惠券与大目录分页、图片生命周期、表单 ABA、跨标签协调和 Admin／Redis／Hyperdrive 平台验收仍开放；应用内浏览器已用合成 Admin/API 完成登录、本人记录到收银和桌面／移动布局复验，不能替代真实角色与支付验收。`api/work.ts` 已有五页消费者，仍须真实企业微信与角色验收；不得因路由注册关闭本项。
  - [x] **FE-003H 社区深链（本地候选完成，未发布）**：旧话题、搜索、视频3条查询状态可寻址路由及 App 条件分支已注册；话题/关键词/作者/视频 ID 筛选、排序、翻页重试、话题内发布和图文详情完成本地页面验收。UniApp 工具链702项、定向9项、路由12项、三端构建及构件3项通过；H5 预览桌面与390px复验通过。旧路由151条完整记账；该社区增量验收时目标页76条、旧路由直接覆盖14条、缺口39条，最新台账见文件顶部。预览视频无可播放媒体，真实媒体、真实身份、真机及生产验收仍归 FE-003J/K；旧 App 任意 WebView 外链继续退役。
  - [x] **FE-003I 三平台本地条件编译（候选完成）**：H5、MP-WEIXIN、APP-PLUS生产构建均成功；App保留既有 `request.ts` 同时静态/动态导入的分块提示，不影响本次构建成功。
  - [ ] **FE-003J 真机矩阵**：在真实H5、微信开发者工具/真机及App容器逐项验证路由、登录、分享、支付、扫码、上传和平台条件分支。
  - [ ] **FE-003K 真实账号、数据与发布观察**：使用客户、门店核销员及受限角色完成生产历史链接、Provider回调和越权E2E；另行批准后发布并观察。
  - [ ] **FE-003L UniApp完整结算金额、筛券与冻结幂等**：2026-09-07已将confirm接入完整confirm/computed报价及订单范围筛券，删除本地总价/随机key逻辑；以按uid持久化的原key与冻结payload恢复未知结果。普通与立即购买范围、真实SKU入口、活动参数、表单及上传门禁同步接线；先前11项实际Vue/Pinia及相关14文件167项Worker、本地类型/三端构建和H5隔离报价/冻结重试通过。acac7c3的Linux发现confirm遗漏悬浮导航，本轮恢复并通过3文件32项。钱包新增正确金额/折扣、服务端状态/时间、游标分页/重试和会话隔离，10项真实Vue/Pinia测试及H5真实SQL浏览器验收通过。**尚不勾选：修复候选精确SHA Linux、钱包数量/范围筛选/具体规则及范围导航、三类活动逐入口真实SQL/浏览器、上传完整生命周期与真机、跨标签并发及结果未知人工恢复、J/K真实角色/provider/生产门禁。**以下两条是历史阶段发现，由其后接线增量覆盖，不能当当前页面仍未接线。
    - [x] **FE-003L-S1 券范围商品权威读取（代码/Linux完成，未发布）**：新增GET /coupons/user/:id/products，强制认证与券owner绑定，不信任URL传入的商品/品牌/UID集合。复用结算范围元数据和纯判定，支持父商品、品类path、品牌pid/fid、JSON/CSV及关系表一致性；每次最多扫描100条公开目录候选，空匹配页仍返回最后扫描游标。已失效/过期/未来/占用券、缺模板/非法类型及范围冲突失败关闭，延续目录VIP可见性，只返回scope_only及catalog_price而非可抵扣报价。36f2439/34101253387最终11/11通过，8项新增合同在专用PG16分片672ms通过；不关闭导航父项或生产门禁。
    - [ ] **FE-003L-S2 券范围导航与完整展示（两端接线及Linux通过，余项开放）**：UniApp和PC钱包已按持有券ID进入独立范围商品页，严格响应投影、空扫描页继续、原游标失败重试、刷新/身份/离页隔离及实际商品详情跳转通过。展示券标题、四类范围说明和目录价/结算非承诺；保留UniApp悬浮导航。桌面/移动浏览器发现的新页属性告警和PC离页误请求均已修复，e40fe07/34105686239补齐遗漏导航白名单并获11/11。配置集合名称/层级总览S5与搜索排序后端S6已分别获1fa8a44/b98d137的Linux验收；两端搜索/排序与稀疏交互S7已获698553d的11/11 Linux验收，真机仍缺。PC钱包完整展示S4已通过Linux，不关闭父项。
    - [x] **FE-003L-S3 UniApp SKU会员价展示一致性（代码/Linux完成，未发布）**：已恢复SKU会员价/划线价严格投影；页面及规格弹窗随所选SKU同步售价、划线价、会员价和库存，缺失字段不借商品级价格，零会员价按结算语义隐藏。数量上限取SKU/商品库存及32767的较小值，切低库存规格收敛数量，售罄仍可查看但禁购，无SKU不伪造。新增4项实际SFC脚本/Vue/Pinia运行时测试和1项真实Controller/SQL→适配器测试，桌面/移动浏览器通过；e40fe07/34105686239最终11/11，新SQL项在专用PG16分片371ms通过，原导航失败断言通过。未改变服务端报价或调用真实加购/订单/支付；真机及发布门禁保留。
    - [x] **FE-003L-S4 PC钱包数量、筛选与规则（代码/Linux完成，未发布）**：四类状态展示当前筛选的服务器数量，六种筛选包括24小时内到期和品牌；首刷请求include_counts，续页保留数量/精确游标重试，缺数量显示未知而非0。规则弹窗纯文本保留换行并提示截断，失效券可看规则但不可浏览范围。刷新/切状态/筛选、身份切换和离页清理详情/数据，迟到响应不回填；非响应式登录存储通过状态订阅正确恢复。实际Axios/Vue/Pinia19项、相关Worker6文件66项、PC类型/构建及1280×900、390×844真实SQL夹具浏览器通过；d058f6f/34107810221最终11/11通过，274文件1,845项零跳过。订单专用筛券/报价/建单未改，不关闭真实角色及发布门禁。
    - [x] **FE-003L-S5 券配置范围名称与层级总览（代码/Linux完成，未发布）**：现有owner绑定范围接口新增view=scope，按配置ID独立分页并返回总数与集合摘要，覆盖全部配置而非当前20个目录候选。公共品类/品牌按父级分层批查，商品名遵守目录及VIP可见性；隐藏、私有、删除或缺失节点不输出名称，不丢配置名额，环、断链、陈旧path/fid及超过32级明确提示。PC/UniApp按需查看、完整续页、精确游标重试，纯文本名称、范围/商品数区分、身份/刷新/离页清理及集合变化拒绝拼接均接线；新增4项真实SQL、两端各4项运行时测试，本地Worker6文件70项、PC23项、UniApp48项/类型/三端及产物3项通过。1fa8a44/34113516414最终11/11通过，274文件1,849项零跳过。未改变结算范围判定；搜索/排序和稀疏目录体验仍属S2，真机/生产门禁保留。
    - [x] **FE-003L-S6 券范围搜索与全局排序后端（代码/Linux完成，未发布）**：现有owner绑定接口增加view=search，keyword字面量名称搜索，七种目录全局排序，复合游标携带全部排序键及ID并绑定用户/券/范围/搜索/VIP上下文；每页重查归属和可用性。每请求最多判定500候选、每批100与1条前瞻，填满后从最后实际检查行续查；稀疏空页返回扫描上限与续页，不伪装目录耗尽。结算同源范围判定、金额精确SQL排序及sales+ficti的bigint目录口径；17项新隔离SQL测试、相关7文件87项及双Worker类型通过。b98d137/34115191204最终11/11，275文件1,866项零跳过。旧view缺省和view=scope不变；前端接线另列S7。无生产连接、DDL或发布，非稳定商品目录快照，也不声称数据库引擎只扫描500行。
    - [x] **FE-003L-S7 PC/UniApp券范围搜索交互（代码/Linux完成，未发布）**：两端调用view=search，名称搜索/清空和七种全局排序，PC表单选择框、UniApp原生滚轮；共享严格响应投影、独立已提交条件和输入草稿、字符串游标、扫描计数及请求代次。切条件清空旧商品/游标/配置说明，迟到成功失败不回填，身份变化清空搜索条件；续页按已提交条件原游标重试，重复商品/循环游标拒绝拼接并禁用入口。空扫描与真正无匹配分开提示，不无限自动扫描、不把目录价或展示销量冒充结算价/实销量。PC27项/类型/构建、UniApp52项/类型/三端产物3项与runtime-i18n9项、相关Worker7文件87项/双类型通过。CUA桌面1280×900与H5 390×844使用一次性真实SQL控制器，搜索/排序、20→失败→30、500空匹配→605找到1、迟到响应、无结果/清空均通过；698553d/34117384660最终11/11，275文件1,866项零跳过。未发布、未触生产，真机与父项仍开放。
    - [x] **FE-003L-A1 秒杀规格只读契约与活动身份一致性（代码/Linux完成，未发布）**：旧详情没有活动SKU，新增同路由view=skus的最小只读投影；商品/活动可见性及当前用户VIP复核，type=1与type=0按suk批量匹配，各最多500行，超限/歧义/缺映射明确失败。保留活动unique与基础unique、精确目录价、六类库存/配额最小值和配置限购上限，售罄保留、空配置不造默认项；selection_only和date_window不代表时段/父活动/剩余限购或报价承诺。共享1/2/3解析器新增跨命名空间冲突及unique/suk矛盾拒绝，避免加购/购物车读取换规格。新增31项真实SQL，含实际cart控制器/读取，相关15文件120项及另两文件5项路由/数据口径、双Worker类型通过；两次并行超时记录保留，最终串行未改超时配置。38f3439/34119944273最终11/11、276文件1,897项零跳过。旧详情保持兼容，未接新页面、未改DDL/依赖/生产。
    - [ ] **FE-003L-A2 秒杀时段与父活动统一资格（购买链路本地候选，目录与并发仍开放）**：已核对PHP截止日期包含当天、时段开始含/结束不含及父子字段复制合同。新增统一上海日历策略并接入加购、购物车校验、报价和事务建单；不再仅靠子商品日期授权。完整关闭还需要下列目录和多连接验收，A1的date_window仍不能作为购买资格。
      - [x] **FE-003L-A2a 购买排期守卫（代码/Linux完成，未发布）**：支持HHmm/HH:mm及显式24:00结束，父活动type/status/is_del、日期和时段与子商品取交集；缺失/坏配置拒绝，旧式午夜截止包含当天，非午夜截止保留精确时刻。事务按父→子→时段ID锁定并重读，扣秒杀库存前再次判断，SQL以clock_timestamp而非NOW校验最终窗口。幂等已建订单仍先返回，未改变普通/砍价/拼团日期合同。新增19项纯策略和26项真实SQL场景，9066a4f/34122859491最终11/11、278文件1,942项零跳过；失败记录保留，没有生产连接或部署。独立多连接证据仍归A2c。
- [x] **FE-003L-A2b 目录与购买资格一致性（代码/Linux候选完成，未发布）**：目录SQL在LIMIT/OFFSET前筛选父/子状态、上海包含截止日和时段交集，严格解析有界CSV并拒绝启用的坏时段；保留同日未来/已过场次浏览，不冒充当前可购买。排序补ID稳定键，真实125行分页为10/10/5/0；时段索引统一HHmm/HH:mm数值排序、结束排除、24:00，坏配置显示不可用，1000项上限显式拒绝溢出。旧详情与view=skus复用schedule投影，保留date_window但明确非完整资格；活动SKU仍有界且无N+1。新增48项真实SQL目录/索引差分与边界测试，SKU文件增至35项，父/时段停用、截止日与实际加购拒绝一致；相关7文件136项通过。不是事务快照、库存预留或剩余限购承诺，大目录查询成本和A2c/A3仍开放。 自身提交8784938的Actions34126883602已11/11通过；数量入口后续进展见A2c2。
        - **A2b CI失败记录**：4b170a2/34125230229最终9/11成功，单元分片二的ordinary-query-index-alignment及汇总失败。三份未变查询的源码定位因重构发生偏移，已据实修正165→158、132→125、756→736；不改查询片段、索引定义或严格门禁，六文件34项本地回归通过。A2b仍待新提交CI。
      - [ ] **FE-003L-A2c 多连接与可变规则复核**：独立PG16连接证明父/时段更新等待、发现到加锁间换父拒绝、锁等待跨截止和多请求库存/限购。审查建单与取消/退库、后台保存的完整锁顺序（取消原有先基础SKU再活动库存问题已在A2c4按同活动范围修复，不能据此宣称全局无死锁）；继续核对共享规则、跨活动/多SKU、后台保存与新增SKU身份等边界。PGlite单连接和受控时钟测试不能替代独立连接证据。
- [x] **FE-003L-A2c1 秒杀写入边界计价快照守卫（代码/Linux候选完成，未发布）**：先以六项真实SQL复现证实报价后改限购/价格/退役/数量仍错误建单，再在原购物车认领、子活动及两层SKU/商品库存UPDATE加入参数化字段比较。限购、身份/规格、价格/成本/结算、运费输入、履约快照、所属商家与退款能力变化均拒绝并回滚；已退役SKU与未审核商品拒绝，普通和其他活动类型分支不变。31项新增场景、秒杀文件57项，相关14文件219项通过；正常库存/配额变化不被当作规则漂移，新价重新计算可成交、已建单幂等重放保持。没有改变锁序，不能关闭A2c：共享运费/系统配置、身份资格撤销、新增歧义SKU行、独立连接锁等待及数量接口自身的isPay/排期/限购复核仍待完成。 自身提交8784938的Actions34126883602已11/11通过；数量入口后续进展见A2c2。
- [x] **FE-003L-A2c2 秒杀购物车数量入口（代码/Linux候选完成，未发布）**：事务锁定未认领且属当前用户的线上购物车；复核排期、商品/SKU状态、单笔/累计限购及六层库存，数量须为1～32767整数，最终UPDATE校验数据库时钟。新加40项测试，秒杀文件57→97项；14种既有排期拒绝场景加入setNum断言。相关11文件230项零跳过、双Worker类型通过；合法基础/活动unique只改数量不扣库存，真实建单后拒改、报价后改数量使旧建单回滚并可重新计价。仅购物车行串行化设计，尚无独立连接锁等待证明；规则读取不是库存预留或一致快照，建单仍重查。加购合并、列表数量资格及其他类型setNum竞态继续开放，未发布。 自身提交0286307/Actions34128377900终态11/11成功，独立连接部分另列A2c3。
- [x] **FE-003L-A2c3 独立PG16连接竞争（代码/真实PG16 CI完成，未发布）**：新增12项父/时段更新等待、已持排期锁的编辑阻塞、发现后换父、数量与建单双向认领竞争、不同意图库存/限购竞争、同键幂等及两类真实锁等待跨截止验证。三个独立单连接客户端与观察连接PID互异且稳定，以pg_blocking_pids明确证实等待，数据库时钟证实截止，不以固定sleep/Promise.all当证据。只允许专用回环PG16及既有随机schema；本机无PG，12项未执行，CI零跳过门禁仍完整。测试沿用真实列/默认/主键夹具，非生产完整约束和触发器目录；即使通过也不关闭取消/退库/后台完整锁序、共享规则、新增SKU和真实生产门禁。 自身07d8c1a/Actions34129974237终态11/11，12项真实PG竞争12,017ms通过、完整分片零跳过；后续补偿锁序见A2c4。
        - [x] **FE-003L-A2c4 同活动建单/取消/退款库存锁序（代码/自身PG16 CI完成，未发布）**：取消在任何SKU补偿之前锁定子活动；退款在结算用户和SKU之前锁同一子活动，避免只修取消后新增退款反向依赖。沿用失效/缺失活动的历史补偿策略，不要求排期有效，已发货退款不退库存。新增14项实际SQL补偿回归、2项结构守卫，4项独立PG竞争覆盖建单/取消、重复取消、建单/退款、取消/退款（含用户行NOWAIT验证未先占用户锁）。本地168项通过/16项PG未执行的历史口径保留；自身8633ba8/Actions34131920654已11/11成功，16项实际PG竞争14,981ms通过，全量281文件2,100项零跳过。共同子活动锁只串行化同活动这三条路径，不关闭后台编辑、跨活动/多SKU、完整生产约束/触发器及全局锁序。
    - [ ] **FE-003L-A3 三类活动前端入口、选规与结算逐链路验收**：PC/UniApp秒杀入口已由A3a/A3b候选修复，PC拼团由A3c取得自身CI，UniApp拼团由A3d专用目录/生命周期候选接替基础SKU购买组件并取得c066371自身CI；砍价目录 A3e 已通过自身 Linux CI，状态与两端页面按 A3f～A3i 继续。A1仅覆盖秒杀，不能视为三类活动已接入。完整选择、真实角色登录返回、结算提交、失效恢复和发布后验收仍需完成；不能将本地入口测试当完整页面链路完成。
      - [x] **FE-003L-A3e 砍价专用只读规格/本人参与目录（代码/原生PG16 CI完成，未发布）**：新增GET /api/bargain/detail/:id?view=skus，可选bargain_user_id只接受精确本人参与ID，不兼容活动ID别名；旧无选择器详情不变。按真实主体校验商品可见性，最多500项活动/基础SKU并检测溢出和歧义，库存取各层最小值，不回退基础unique。识别Worker状态1/2/3/4与真实已砍金额；匿名不返回参与信息，多个有效参与可显式指定本人记录，只有隐式选择拒绝歧义。REPEATABLE READ/READ ONLY与最多5秒局部期限、private no-store；无预留、写入或provider调用。30项目录测试在PGlite和独立PostgreSQL 16.15均通过，原生三会话证明REPEATABLE READ/READ ONLY读取旧价10.00/库存6期间，另一事务提交新价12.00/库存1，旧响应保持同一快照而新请求读到4.00/库存1；夹具余留0且集群停机。实际控制器加购与完整报价验证2.00/4.00而非SKU配置777/999；新增持久绑定全链路证据见A3f。目录夹具仅实际列/默认/主键，不代表全生产约束；PC与UniApp已在A3g/A3h本地接入，完整页面链路另验。[Linux CI 首跑](https://github.com/cinagroup/cinashop/actions/runs/35941614593) 的该文件30/30、零跳过且执行原生三会话用例，但其他测试和密钥扫描使整套门禁失败；修正候选 a6922089453f69cef2ca0f2eecab881e6ac2737a 的 [Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35946334384) 11/11 成功，两分片合计10,714/10,714、零跳过，A3e 30/30、原生三会话快照用例通过。
      - [ ] **FE-003L-A3f 砍价状态与重复参与一致性（发起/列表/购物车与订单身份本地修复，完整链路未完成）**：startBargain在有界READ COMMITTED事务中复用1/3，读取最多2条并对歧义拒绝；本人列表按每条记录的真实金额和有效活动判断pay_status，3可购买、2/4不可购买，过期1/3只在展示映射为2。持久绑定候选允许多个有效参与分别购买，移除此前窗口计数一律禁购的策略；隐式加购及旧0购物车仍拒绝歧义，不静默选择最新记录，严格拒绝未到底价或异常金额。发起按Drizzle UTC时间语义显式比较clock_timestamp，并在参与行锁等待后复核，活动SHARE锁保持规则。新订单快照绑定精确本人参与，建单消费校验活动和报价金额，取消只恢复该记录。分阶段历史测试的294通过/49跳过见下方增量；本轮帮砍/取消双顺序竞态在原生PG16同文件11/11通过，精确提交 dcbddca 的[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35972181778) 11/11 成功；后续候选补平台／供应商源商品软删除与发起／帮砍的同商品事务锁、历史参与禁购及结算末端商品资格复核；精确提交 `48bc8c1` 的[完整 Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35983849804) 因容量锁竞争、Worker 分片2五项失败及分片1超时而失败，分片1取消前另观察到一项失败。修正候选 `6b1e8aa` 的完整 Linux CI 已因两处测试夹具缺口及分片2超时失败；`64b7c8e` 的第二分片4708/4708零跳过通过、第一分片因相同四项夹具失败；夹具修复 `72d0bbb` 的完整 Linux CI 11/11 成功，新增7项原生PG行为／竞争用例已在第一分片通过。完整并发/业务规则仍待验，无生产改动，不能勾选整个A3f。
        - 2026-09-24 本地并发补审：仅给活动ID的取消与发起共用同一advisory lock，只选唯一有效参与，多个有效参与或显式ID与活动冲突时拒绝；旧帮砍别名先查唯一有效记录，不再选最近已用记录。后台修改砍价人数、状态及退役与帮砍共享活动规则读写锁；将人数改低于现存参与最大帮砍数，或恰等于已帮次数但参与快照尚不可购买时均拒绝；两个真实独立连接的先后顺序已验证，日期编辑测试亦按新锁序确认帮砍先提交、后台编辑后完成。首批HTTP/SQL 35/35及PG16 9/9，前一候选次批原生PG16六文件119/119；后续修订按资源限制分批复核日期8/8、人数4/4、后台竞争9/9、帮砍竞争12/12，共33/33，每批夹具余留0、隔离集群停机，双Worker类型通过。一次六文件组合因本机磁盘耗尽使PG在断言前停止，已保留日志，不计通过。另一次未设置原生PG连接的扩展PGlite运行192通过/7失败/47跳过，失败涉及PGlite时钟、定价锁目录及多语句触发器，不计原生PG证据；对应原生Linux全套已零失败/零跳过。首批精确提交`648e025b190f75837c1f7f3d0566171e210946a6`的[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35956362112)已11/11成功、两分片590文件10,754项零跳过；次批精确提交`b1dc6ac6f9f54dd2554b4b7b2ebc8c145c10f6c6`的[Linux CI](https://github.com/cinagroup/cinashop/actions/runs/35962566501)已11/11成功、两分片599文件10,796项零跳过，原生PG日期8/8、人数规则4/4均实际执行，完整A3f与生产验收继续开放。
        - 本轮订单身份增量：cart_info.bargainParticipation保存version=1、participantId、activityId、uid；损坏、未知版本、跨身份或混合快照拒绝，只有全部有效JSON完全缺该字段才沿用唯一旧记录回退。实际建单/取消验证多次已用记录不再导致新订单无法定位，重复取消不重复补偿；消费阶段精确活动与三项参与金额变更会回滚。旧无快照歧义不自动补写。
        - 本轮库存锁增量（代码/本地补偿通过，独立PG待验）：建单在购物车之前、取消在SKU之前、退款在结算用户/SKU之前锁定同一活动NO KEY UPDATE，允许help持参与锁后取得KEY SHARE。退款一次解析精确快照或旧唯一别名，锁定结果传给补偿，不晚查改选；已支付退款不恢复参与状态，未发货按数量恢复、已发货不恢复，失效活动不妨碍补偿。13项新增实际SQL回归通过；新增8项独立PG候选覆盖三类库存竞争、三类等待后到期、help兼容和start等待消费，砍价PG共18项均未执行。相关259项本地通过/43项PG跳过；不能据此宣称全局锁序或生产并发完成。
        - 购物车报价认领增量（2026-09-08）：type2认领与type1共用ActivityCartQuoteGuard，在原有UPDATE比较id、cartNum、productId、productAttrUnique、productType、activityId、type、isNew，保留owner/代客/支付/删除/状态守卫。报价后改动拒绝并回滚，重新报价后可用新SKU/数量正常下单及取消；addTime元数据、唯一旧活动/SKU别名与完成key幂等不受影响。新增17项实际SQL测试通过；相关12文件共249项通过、45项独立PG16测试跳过（含11项新增行等待候选），两套Worker类型检查通过。此守卫没有保存参与ID，不等于购物车已经完成参与绑定。
        - 持久字段增量（2026-09-08，结构本地候选，未应用）：新增bargain_user_id整数/NOT NULL/默认0，CHECK要求非负且非type2只能为0；外部0149与内嵌0155字节一致，真实ORM同步，runBargainCartParticipation独立根事务执行。最多2秒锁等待/30秒语句/5秒事务空闲，不放宽调用方更严格期限；ACCESS EXCLUSIVE需要维护窗口，已有列/约束漂移拒绝且回滚，旧数据不查参与、不回填、不改序列。真实历史购物车建表和完整ORM的升级测试、本地回归及PG16未执行项见详细审计。模型本身不代表业务已绑定；上线必须先独立schema升级，再部署Worker，不能重跑历史runAll。
        - 持久身份业务增量（2026-09-08，本地候选）：覆盖上方早期“显式选择也拒绝重复/窗口计数禁购/尚未绑定”的阶段限制。目录允许指定精确本人参与，列表逐条计算资格；隐式发起/加购仍对多个有效参与拒绝。新购物车保存实际参与ID，不同参与、旧0、立即购买及代客/游客/门店范围不混合；同参与普通加购在有界READ COMMITTED/用户建议锁内合并，条件UPDATE拒绝购物车并发变化或占用。确认/计算/建单共用绑定解析，正绑定失效不回退；旧0即使携带参与ID仍要求唯一有效记录，不自动回填。bargainUserId和bargain_user_id严格表示参与，bargainId和bargain_id只校验活动，别名冲突拒绝。type2原子认领额外比较bargain_user_id；实际HTTP/SQL验证两条不同价格参与建单、完成key幂等、精确取消并保留后续参与。最终294项本地通过/49项PG跳过、双类型通过；新增3项独立加购竞争及1项订单等待后绑定修改候选未执行。
        - 跨参与帮砍次数增量（2026-09-09，本地候选）：好友次数作用域为helper+活动，旧单参与锁不能保护跨记录计数；新增731628/helper UID事务建议锁，先helper后731627/参与再参与行，READ COMMITTED刷新等待后的计数快照。语句5秒/锁2秒/事务空闲5秒保留更严格值；仍按活动分额度、自砍不占好友次数、不同helper独立，不改价格公式或活动库存锁级别。12項实际HTTP/SQL回归验证额度、两层持锁、设置恢复、非法ID与失败回滚；6項独立PG16候选未运行。相关177项本地通过/36项PG跳过、双类型通过，不能据此关闭并发或A3f。
        - 本机PG16补验（2026-09-09）：上方历史“PG未执行”由本条按范围补充，不抹除原始记录。Windows16.15实际执行39项业务竞争（帮助6/发起7/订单23/加购3）及两项0149读锁冲突；完整14文件239项零失败零跳过，双类型通过。外部/内嵌/ORM完整建库与重复字段升级通过，九路径五类目录均263/3701/571/1010/227且零差异；独立库/schema清理与停止确认。全部结果为当前a8170a8业务代码及两项测试预算修正，不等于Linux CI、全局锁序或生产验收。
        - 下一实施项：已定义竞争、字段路径和九路径目录取得本机PG16证据，新增帮砍/取消双顺序竞争已取得自身Linux CI；PC/UniApp已取得目录→加购/精确结算入口本地候选，完整真实角色流程和发布验收见A3g/A3h。跨活动/后台编辑/商品退役的全局锁序、剩余帮砍状态竞争与A3i起价变更策略仍需独立审查和真实PG验证。不得以禁止正常复购、自动删除或关闭后续参与消除歧义；不将局部本地候选视为A3f整体完成。生产仍须先独立schema升级、再发布引用字段的Worker，禁止重跑历史runAll。
        - 源商品退役与结算边界增量（2026-09-24，精确提交48bc8c1／自身CI进行中）：平台和供应商的两个Worker软删除入口在商品行写入前持同商品独占事务建议锁，发起/帮砍在活动与参与行锁前持共享锁，READ COMMITTED锁后重查商品删除状态及活动绑定。本人列表保留历史参与，但商品缺失、删除或下架时不再显示可购买；所有砍价配送方式在结算最终商品库存写入重查 `is_del/is_show` 与归属，拒绝报价后退役。供应商回收先处理购物车，再写商品行，避免结算购物车→商品与回收商品→购物车反序。新增原生PG16用例共7项，覆盖双入口双活动拒绝、退役/帮砍双先后、严格超时回滚、虚拟订单报价后退役和购物车先行的回收；本机无原生连接，不能把跳过计通过。接口级历史展示回归28/28、双Worker类型检查通过；完整createOrder与供应商回收的双方真实并发组合、其他全局锁序及生产验收仍开放，不能据此勾选A3f。
        - 时间审查项：砍价建单主活动UPDATE及事务最后SQL均用clock_timestamp() AT TIME ZONE 'UTC'核验状态/日期；隔离触发器在实际订单快照写入后使活动过期，整笔建单回滚已验证。参与、活动SKU和基础SKU实际锁等待跨过截止的3项PG候选未执行，不能将本地单后端结果当作等待证明；其他活动时间比较未修改。
      - [ ] **FE-003L-A3g PC砍价专用页面与结算入口（本地代码/隔离候选）**：列表已修复title/min_price，20条独立分页/同页错误重试；本人列表按真实起价/已砍金额展示当前价、进度和状态，多个有效参与分别跳到/bargain/:id?bargainUserId=精确ID。详情接入A3e，严格校验状态/金额/活动和基础SKU映射/库存/响应身份，显式活动SKU/数量，新加购绑定参与，结算保留type2/bargainUserId。匿名与过期登录返回保留最新意图，切账号/路由/卸载抛弃迟到响应，导航等待后再验期限；已加购或已发起后的导航失败保留结果，重试只导航，不重发写入。保留发起/本人帮砍/确认取消，写入不自动重试；活动改价差异明确提示，未改服务端价格政策。新增25项实际Vue/Axios/Router及4项真实SQL适配器用例，PC85会话+7图片/类型/构建，Worker74项/双类型通过。1280×900与390×844 Playwright真实SQL夹具验证匿名返回蓝2/#80、失败继续结算同cart、本人#90的4元价格，控制台零错误、无横向溢出；最后一轮仅2个cart写入，订单0、业务库存/参与不变。真实认证、页面完整确认建单付款、真实角色发起帮砍取消、自身Linux/PG16及生产发布仍开放，不以隔离入口候选勾选整个A3g。
      - [ ] **FE-003L-A3h UniApp砍价专用页面与结算入口（本地代码/隔离候选）**：已替换通用ActivityPurchase，接入共享严格A3e目录和活动SKU；显式绑定本人参与，支持mine=1的20条分页/错误/空页/精确详情。活动列表修复底价字段并补分页、同页重试和hide/tab/unload隔离。匿名及过期登录返回重读，原owner以外不恢复选择；期限/数量边界、迟到读写、导航代次与成功加购后仅重试同cart通过；发起保存返回ID再读目录，帮砍不乐观改金额，不自动重试写入。新增26项运行时用例，完整工具链176项、类型、三端构建/产物3项、i18n审计9项通过，共享真实SQL合同4项复跑通过。390×844与1280×900实际H5验证列表→登录表单/隔离认证返回→#80蓝2→真实加购→导航失败继续同cart→#90显示4元；最终轮各1条cart、订单0、库存/参与不变，console error为0，DCloud原有弃用warning单列。尚未验证当前提交Linux/PG16、微信/App真机、真实角色发起帮砍、完整浏览器结算建单支付及发布观察，不能据候选关闭整个A3h。
      - [ ] **FE-003L-A3i 砍价参与价格快照策略（源码已对照，待产品决定）**：当前报价max(参与底价,max(参与起价,活动当前起价)-已砍金额)，故起价10→12、已砍8、底价2的同一参与报价由2变4。PHP发起保存起价/底价快照，帮助只累计price，StoreCartServices:358使用参与底价；当前Worker的早期坏行修复会在help覆盖快照，不可直接当作全新系统必要规则。已向用户确认“既有参与锁原快照/改价仅影响新参与”或“既有参与跟随活动现价”，本批未收到选择且未改变公式。A3e仍显式返回activity_price_changed，不承诺永远底价；决定后须统一help/海报/目录/确认/建单并补边界回归，不能只改展示掩盖差异。
      - [x] **FE-003L-A3j 砍价后台删除保留历史（本地代码/PG16候选完成，未发布）**：共用管理控制器恢复PHP仅is_del=1的软删除，严格ID、不存在拒绝、重复删除幂等；列表过滤退役行后真实limit100，private no-store。非键UPDATE不锁参与/SKU，2秒锁/5秒语句/5秒空闲均保留更严格调用者值。20项新增专项与相关17文件269项、本机双类型通过；独立后端实测删除/建单双向等待、help KEY SHARE兼容/最终回滚及55P03后重试，真实取消/余额退款保留活动历史和精确补偿。新增测试夹具认证为替身、不覆盖全生产约束；非砍价删除、后台编辑、真实角色/provider、Linux/workerd/自身CI和生产发布不在本项完成范围。详见local-pg16-bargain-retirement-acceptance.json。
      - [ ] **FE-003L-A3k 砍价后台完整保存/状态与交易并发合同**：通用编辑器默认值覆盖、系统字段重置及状态校验由A3k1修复，起止/过期保存由A3k2、原商品保存资格/继承由A3k3、显式单规格创建/编辑及属性快照由A3k4、描述/轮播图/title/简介/单位的后台到专用消费页由A3k5完成本地候选。仍缺完整PHP attrs/items及旧详情请求兼容、可视化富文本/资源上传选择、完整运费配送和复制流程；A3k6已完成活动deliveryType及报价后运费变更的服务端门禁候选，A3k7补齐后台配置到真实报价；A3k8已接通配送选择与两端刷新，选择接口按全局自提开关/所属门店筛选，A3k9补齐quote/create的SQL全局开关/门店归属重验与局部并发，配置表锁的发布负载与全局写入锁序仍须验证；同城履约、模板引用删除保护/内部并发及旧API创建省略shipping仍须接通验证。关联商品重绑与既有参与/SKU映射、旧多规格整理、旧客户端省略sku时的主表库存写入、全局活动/基础SKU库存同步、商品退役及所有规则变更的全局锁序仍须完整实现并做PG验证；当前保存持源商品SHARE，但不自动停用随后失去资格的活动，也不宣称消费端资格和全局商品/SKU退役合同已完成。A3i价格快照政策仍待选择。不得以基本编辑候选或局部并发通过关闭整个A3k。
        - [ ] **FE-003L-A3k11 运费模板一致报价与建单重验**：A3k11a已接通模板快照/所属方/SHARE NOWAIT，450b1bd补齐b的核心地址权威门禁并通过自身CI。父项不关闭：b仍待正式外部/发布验收；确认key仅存cartIds，未绑定用户此前确认的金额/规则版本；全局配置仍由KV/分次读取得到。必须继续完成b～d和真实端到端验收，不能把同请求内部计价守卫表述成跨HTTP报价承诺。
          - [x] **FE-003L-A3k11a 模板单语句快照与同请求提交守卫（本地代码/PG16候选完成，未发布）**：同语句读取模板/费率/条件包邮/禁配/城市路径；最多200模板、每种子规则10000行，超限拒绝不截断。已绑定模板必须可用且精确属于商品；全国及默认模板禁配生效，包邮只免金额。实际建单保持既有资源锁序，原子认领/库存写入比较购物车、商品归属与有效配送绑定、SKU计量，并在末端持模板/城市SHARE NOWAIT到提交、精确比较快照；初始无模板的固定/包邮也比较原绑定，不能中途改绑。要求READ COMMITTED，兼容嵌套只读报价，不改变调用者隔离，保留更严格5/2/5秒语句/锁/空闲上限而非事务总期限。独立PG验证读取中提交的一致性、实际后台保存/订单双向竞争、城市编辑及设置恢复。证据见workers-ts/audit/local-pg16-shipping-template-quote-acceptance.json；不保护绕过父锁的直接子表SQL，不等于b～d或发布完成。
          - [ ] **FE-003L-A3k11b 配送地址权威入口及完整性（代码/隔离页面/自身CI通过，发布验收待完成）**：450b1bd以统一核心解析自有地址簿或授权结构化代客地址，拒绝缺失/无效实物配送地址与客户端覆盖，完整地区链及100字符订单列校验，提交前SHARE NOWAIT重验；保留无地址预览、虚拟/自提和已有订单幂等。PC/UniApp六条本地真实页面、PG并发及实际JWT/数据库角色补验通过，自身CI345文件3206项与workerd27项通过；仍缺正式登录/外部令牌存储/Hyperdrive及发布验收，不据隔离夹具勾选。详见order-delivery-address-contract.md。
          - [ ] **FE-003L-A3k11c 跨请求确认报价与创建绑定（候选已实现，完整合同未验收）**：450b1bd的8项跨HTTP缺口及2项对照保留为红测基线。服务端不可变凭据绑定身份/key/商品/地址/配送/费用，最终金额改变拒绝回滚；PC/UniApp明确拒绝后重取、未知结果保留原意图。已追加已领券占用、套餐/秒杀锁后投影、砍价/拼团/积分标量规则、秒杀配送/退款、套餐/拼团/秒杀事务末端自然截止及基础/活动SKU价格、适用会员价/商品开关和兑换积分原子守卫；另补用户会员/等级行末端NOWAIT保护与自然到期，缺失等级引用创建失败关闭。赠送积分来源与汇总已绑定凭据，基础来源在原商品UPDATE比较，活动覆盖时未使用的基础值仍可变化，未改收货返积分政策。成本/结算价只在创建请求内以原写入/锁保护，不加入客户报价；新人SKU局部变量遮蔽已按PHP来源修复，库存扣减语义不变。指定分佣isSub及实际使用的SKU一级/二级金额已在原UPDATE保护，保留关闭/不使用规则的正向对照；已补末端有序NOWAIT保护推广关系/资格字段/实际等级权益、事业部比例与最终墙钟到期，缺失引用创建失败关闭；mode=3累计消费和商品券关系集合已补数据库写入协议、末端读端及建库注册，本机完整ORM和实际非所有者子范围有证据，正式权限/维护隔离仍待验；商品范围字段及分类/品牌直接引用已补末端新读、有序锁和缺失引用失败关闭，本机真实HTTP/独立PG及完整ORM低权限子范围有证据，不扩展成递归祖先政策或正式门禁验收；后台券保存、分类/品牌显示管理及Out券创建/启用/删除已有轻量和完整ORM的实际HTTP交错证据，正式鉴权/其它入口不在该证明内；后台/供应商SKU编辑与下单真实死锁已修复，Out分类树移动/显示/删除及商品编辑有完整ORM交错与回滚证据，详见catalog-management-20260910.json；全局配置权威仍开放。分层红绿测试与扩大回归见checkout-confirmation-contract.md，不将重叠测试累加。其它佣金/经营主体事实、表单内容、其它商品/SKU管理入口（Out商品、移动管理、退役等）及多商品交错、完整不可变CI和正式存储/登录/发布仍待验；不擅自决定A3i价格政策，亦不等同于A3k11d全局配置与会员权益表权威一致性完成。
          - [ ] **FE-003L-A3k11d 全局运费优惠配置一致性（SQL权威候选，完整验收未完成）**：11个计价配置键及vip_price/express权益现以同一SQL快照读取，保留系统配置is_store=0、sort DESC/id DESC和权益最低ID优先级；末端READ COMMITTED下通过固定能力SHARE NOWAIT锁表并重验，覆盖缺失键新增、删除、重命名、重复赢家及权益变化，拒绝陈旧KV授权。完整ORM读端20项通过；实际管理员认证／后台原子保存新增46项，联合6文件132项及双类型通过，含保存与下单提交／回滚交错；历史完整单元90502已4287项通过，不代表当前代码全量通过。2026-09-20新增显式固定锁能力组件、定义／ACL／所有者／RLS校验与实际连接权限预检，后续已将精确协议例外接入既有paid-order预检并增加显式checkout组合范围，完整277表ORM下真实LOGIN／PG16锁协议36项和权限40项、运行器59项及现有探针模拟处理器29项与双类型通过，含NOLOGIN存量连接拒绝及独立非超级用户维护安装/重装、权限不足回滚。同日已将统一安装SQL注册到外部0160／内嵌0166，真实PG16外部162文件／内嵌167步及七条ORM完整建库升级路径五类目录零差异、既有对象身份不变，此前25文件781项定向回归、双类型及失败清理证据见checkout-pricing-lock-registration-20260920.json。同日后续已切换普通建单至同schema固定能力并核验实际表解析来源，完整277表下真实SELECT-only计价身份建单/重放通过，直接LOCK/UPDATE仍拒绝，无DML或安装回退；完整普通建单克隆场景已显式供应独立所有者。最终17文件555项不同定向回归、双类型和8集群清理通过，失败与边界见checkout-pricing-service-integration-20260920.json。同日继续完成首单/套餐严格克隆隔离及四审计入口显式维护参数，原生11项/Node133项共144项不同用例和完整本地workerd16文件289项、双类型通过，失败/清理证据见checkout-pricing-scenario-integration-20260920.json。真实远程维护与正式身份供应、真实客户／Hyperdrive、完整商城身份组合、完整当前单元及不可变CI仍未完成，不能全局豁免SECURITY DEFINER。表锁影响两张表所有写入，正式最小权限／其它管理入口／负载和维护竞争仍待验；首单／新人／分佣等其它配置未统一。见checkout-pricing-config-authority.md；本项不勾选。
        - [ ] **FE-003L-A3k12 运费模板引用与生命周期保护**：应用保护已接入总后台/Supplier删除停用、Out/Supplier/MobileAdmin/套餐准入及旧活动来源变更，砍价刷新所属方重验保留模板；不重绑历史引用或修改订单。隔离PG16已补存量预检、阻写锁内重验、精确协议目录/默认ACL与幂等、实际LOGIN权限边界及DDL事件/复制模式门禁。直接SQL协议已接文件0152/内嵌0158新建及独立runShippingLifecycle升级，并补七表启用的非协议行/语句/内部RI触发器交互门禁，未安装线上；启用的七表重写规则也已接入锁前/锁内环境检查并有真实peer证据，但提交后高权限DDL仍需协调；另已接十个正式索引的ORM/文件0153/内嵌0159/独立升级及九路径目录/回滚/读写样本验证，尚未安装线上；最终完整服务授权、高权限维护协调、安装后规则/跨表交互、完整写面/多行竞争、容量和真实角色线上验收未齐，保持开放。详见workers-ts/docs/shipping-template-lifecycle.md。
        - [ ] **FE-003L-A3k13 完整PHP模板管理合同与页面**：2026-09-13分组省市选择、重量/件数/体积、条件包邮和不配送区域的完整读写已成为本地候选；由三类PHP字段替代原扁平表单，版本指纹在父锁后校验，旧表单覆盖明确拒绝，冲突保留输入。总后台有界一致列表及桌面/手机真实SQL编辑、新建、冲突流程通过；8文件303项、双类型/Admin构建见admin-shipping-grouped-20260913.json。仍待自身Linux、真实账号/真机完整流程、身份切换及结果未知浏览器用例、损坏规则受控修复、剩余PHP行为/容量及发布验收，不以局部工程通过关闭父项。
        - [x] **FE-003L-A3k10 总后台运费模板原子保存（本地代码/PG16候选完成，未发布）**：以父模板NO KEY UPDATE到提交包围元数据和区域替换，兼容供应商FOR UPDATE与现有删除UPDATE；失败整体回滚，省略字段/regions保留，显式空数组仍清空，不重写归属/创建时间/特殊规则。增加256KiB请求、1000区域、整数/金额格式边界及保留更严格值的5/2/5秒语句/锁/空闲限制，不是事务总时限。26项实际控制器/SQL合同及7项独立PG竞争通过，离线26项及Worker双类型通过；认证为夹具，无浏览器或生产验收。初始5项真实SQL失败与回归证据见workers-ts/audit/local-pg16-admin-shipping-atomic-acceptance.json；不关闭A3k11～13、父项A3k或A3i。
        - [x] **FE-003L-A3k9 砍价自提SQL权威报价与建单准入（本地代码/PG16候选完成，未发布）**：按PHP缺省/空值、配置sort/id覆盖优先级和所属可用门店验证；选择接口取消KV授权，报价使用单语句一致快照并兼容嵌套事务。活动锁后持配置表SHARE NOWAIT与门店行SHARE NOWAIT到提交，最终库存UPDATE原子比较来源归属/类型/isShow/isDel，不倒置SKU→商品锁序。24项真实服务/HTTP与14项独立PG竞争通过，离线65项、Worker双类型通过；既有幂等/取消恢复、非自提及普通改名保持。表锁阻塞全部配置写入，不是永久资格保留；语句/锁/空闲上限不是事务总时限，高负载/全局写入协议及发布仍待验证。初始失败与回归证据见workers-ts/audit/local-pg16-bargain-pickup-acceptance.json；不关闭父项A3k或价格政策A3i。
        - [x] **FE-003L-A3k8 砍价消费端配送选择（本地代码/PG16/浏览器候选完成，未发布）**：独立有界REPEATABLE READ/READ ONLY快照读取本人有效购物车对应的唯一活动，按活动deliveryType、全局开关和精确所属可用门店返回配送方式；UTC时间比较与旧空窗口兼容。PC/UniApp失败关闭、刷新保留原选择、过期请求隔离，非物流类型1/2/3允许无地址报价且不回退默认地址。专项54项PG与52项离线、45文件665项相关回归、Worker双类型、PC92+7/构建、Uni182/类型/三端构建/3产物通过；1280×900及390×844两端真实页面→内存SQL验证仅自提、后台改规则、刷新阻止旧选择、显式切换重新报价。认证/KV/地址列表为夹具，不创建订单或付款。接口只提供选择建议：全局开关/门店归属的quote/create重验已由A3k9补齐，选择接口本身不能视为授权或保留；同城履约、模板生命周期/内容并发、其他活动旧合并选择、完整PHP编辑及真实角色/设备/生产发布继续开放。证据见workers-ts/audit/local-pg16-bargain-selector-acceptance.json。
        - [x] **FE-003L-A3k7 砍价后台配送与运费配置（本地代码/PG16/浏览器候选完成，未发布）**：新增可读配送/运费配置和所属方可用模板，复选配送、三类运费及金额/模板选择；更新仅发变化的完整规则组与原值，锁后冲突拒绝，未读取/未修改和SKU重读不覆盖旧规则/编辑意图。PHP freight=1包邮/2正数固定/3模板，清除不生效字段；非实物及1/2/3免物流从锁定商品派生。模板owner/status/isDel精确筛选、上限500，源商品SHARE后模板SHARE NOWAIT避免反向等待并保持到提交。新增28项合同/HTTP/payload、5项独立PG后端，43文件619项与离线28项、Worker双类型/Admin构建通过；桌面1280×900和手机390×844真实Vue→SQL保存/重载/报价通过，错误输入不发请求，console error/warn均0。只验证后台配置到报价，不完成消费端选择/门店/同城、模板引用删除保护或内部并发、旧API创建省略shipping、完整PHP编辑及发布；A3k仍开放。证据见workers-ts/audit/local-pg16-bargain-admin-shipping-acceptance.json。
        - [x] **FE-003L-A3k6 砍价配送/运费报价与建单门禁（服务端本地代码/PG16候选完成，未发布）**：按PHP合同检查活动deliveryType而非原商品配置，配送1允许快递1/门店配送3、自提2须允许2；历史空值兼容，非物流1/2/3沿用独立合同。报价后取得活动NO KEY UPDATE再读五项配送/运费/商品绑定字段，变化则在认领购物车前拒绝；不改价格公式或历史幂等，不增加其他资源锁。25项服务/实际HTTP确认重算及8项PG独立连接验证拒绝/正向/取消恢复、真实写入等待、规则回滚、普通改名、订单写入期间活动编辑等待。证据见workers-ts/audit/local-pg16-bargain-shipping-acceptance.json。配送选择接口、后台配置、两端联动、全局开关/门店归属/同城和模板内部并发仍开放；不等于整个A3k、前端/真实角色/支付或发布完成。
        - [x] **FE-003L-A3k5 砍价活动展示内容闭环（本地代码/PG16/浏览器候选完成，未发布）**：后台展示标题/简介/单位、8张轮播图和16000字符HTML描述接通认证一致性读取及活动/SKU/type=2描述原子保存；省略不覆盖、显式清空、旧image-only保留图库尾部。媒体持久路径存储/响应签名，PC和UniApp专用详情使用共享HTML允许列表，未改参与报价。新增24项SQL/HTTP/payload与3项独立PG16竞争验证实际写后下单等待、读快照和跨截止回滚。最终31文件470项零失败/跳过、PGlite35项、双类型与相关构建、UniApp工具链176/产物3项通过。1280×900和390×844共6组实际页面保存重载、切图/规格、第二轮播指示点及描述展示通过；浏览器订单0，认证/KV替身。UniApp弃用warning、首轮请求夹具/索引行号失败、并行建库超时均保留证据。不替代完整可视化编辑、旧raw详情、远端R2下载、真机、角色、Linux/workerd或发布。证据见workers-ts/audit/local-pg16-bargain-admin-content-acceptance.json。
        - [x] **FE-003L-A3k4 砍价单规格事务保存及后台选择器（本地代码/PG16/浏览器候选完成，未发布）**：新建必须传入所选baseUnique，同事务生成独立活动SKU身份、属性和快照；已有身份/销量不重置，显式库存修改同时比较主表及SKU原值，价格单改保留SKU自身库存。认证GET规格接口上限500、只读一致性快照与no-store，不暴露卡密。原商品重复组合/退役/库存不足、既有SKU替换/商品重绑及多规格自动删减均失败关闭。源商品SHARE后使用全局身份try-lock和SKU NOWAIT，7项独立PG后端验证快速失败/可重试、真实下单持源SKU场景无死锁、实际编辑等待及写后跨截止完整回滚。18项服务/HTTP与3项实际前端payload测试；相关27文件421项、PGlite21项、双类型、Admin构建通过。1280×900和390×844实际Vue→Axios→SQL创建/回显/额度编辑/冲突留表单通过，console error/warn均0；认证/KV替身、合成库存变化不冒充生产角色或浏览器支付。新创建请求合同改变，须协调Worker/Admin发布；完整编辑/旧客户端库存/全局并发/A3i及发布门禁仍开放。证据见workers-ts/audit/local-pg16-bargain-admin-sku-acceptance.json。
        - [x] **FE-003L-A3k3 砍价保存原商品资格与派生字段（本地代码/PG16候选完成，未发布）**：每次保存均检查源商品isDel=0/isVerify=1且非SVIP/预售，isShow不作为后台配置门禁；从来源继承八项归属/类型/表单/标签/保障/参数字段，非实物配送及1/2/3免运费规范化按PHP，只写变化的派生值而非库存/价格。活动NO KEY UPDATE→源商品SHARE，保持2/5/5秒及更严格事务局部限制，不锁参与/SKU；17项来源合同、9项PG多连接（双向写入等待、相同源共享读取、超时恢复、截止跨越）通过，相关24文件393项零失败/跳过、PGlite17项和双类型通过。无前端/DDL/生产变更；不得等同于完整SKU/复制/重绑、自动停用、消费端类型履约、全局锁序或自身CI/发布验收。证据见local-pg16-bargain-admin-product-acceptance.json。
        - [x] **FE-003L-A3k2 砍价创建/编辑时间合同（本地代码/PG16/隔离页面候选完成，未发布）**：API仅接受严格规范UTC ISO毫秒格式，前端DatePicker按本地时区显示且提交UTC差异；起止必填且start<stop，旧空日期仅可显式配置。PG clock_timestamp在锁后和实际写后复核新窗口及原始截止，拒绝过期创建、已结束编辑和等待跨截止后延期复活；保持既有有界事务和兼容行锁。新增18项日期合同、8项独立PG16时钟/竞争、5项前端辅助函数测试；相关22文件367项（两次互斥范围执行）零失败/跳过，双类型/Admin构建通过。1280×900新加坡与390×844纽约页面验证创建、刷新时区往返、单端点差异、缺失/倒置客户端拒绝及过去截止服务端拒绝，错误自动可见，控制台0错误/警告。保留完整SKU/复制、真实认证、DST切换边界、Linux/workerd及自身CI/发布验收；无生产访问或DDL。证据见local-pg16-bargain-admin-dates-acceptance.json。
        - [x] **FE-003L-A3k1 通用砍价编辑不丢失业务字段（本地代码/PG16/隔离页面候选完成，未发布）**：基本创建/更新分离，更新只写提交字段；sales/addTime/isDel/quotaShow禁止直接提交，金额/人数/数量/库存/状态有界校验，额度变更才同步quotaShow。READ COMMITTED、NO KEY UPDATE、2秒锁/5秒语句/5秒空闲保持更严格值，库存或额度更新必须携带expected.stock/quota并锁后比较。页面只提交差异且固定本次表单类型；冲突不关闭表单或自动重试，刷新后再提交；砍价人数/额度可编辑，手机弹窗适配和错误内联。新增52项实际HTTP/SQL、6项真实payload辅助函数、9项独立PG16竞争，相关20文件336项、双类型和Admin构建通过；桌面/手机三步页面流程通过。旧版全量表单缺expected会被拒绝，发布须配套更新前后端并验收；不冒充完整PHP编辑器、真实认证、Linux/workerd/自身CI或生产发布。证据见local-pg16-bargain-admin-save-acceptance.json。
      - [x] **FE-003L-A3a PC秒杀列表→活动规格→结算入口（代码/自身Linux候选完成，未发布）**：按index对象选择时段ID，支持服务端分页和失败重试，迟到响应不得覆盖新时段；商品链接改为/seckill/:子活动ID，详情只用view=skus，不回退基础规格或客户端金额。显式选活动unique、数量/排期检查、新购物车与checkout保留type=1/seckillId；登录返回一次性恢复规格数量，超限不静默缩量，刷新重新确认，身份/路由变更丢弃旧结果，写入不自动重试。365755c/34174222066终态11/11、282文件2,106项，原6项SQL在PG执行。复核真实复现原URL红1→改蓝2→410002却返回红1；购买前持锁保存最新选择并在异步导航后复核身份/页面/截止修复，11项新实际SFC/Axios/VueRouter测试及38项总会话测试、PC类型/构建通过，桌面/窄屏过期返回蓝2再次验证通过。原时段迟到、20/1/20分页、限额、重复点击单行、停父拒绝、失败恢复与旧详情隔离证据保留。测试只到结算URL，真实认证、地址/门店、确认报价/提交支付和其他活动仍开放。当前生产旧API不能与本候选前端单独混发；必须先通过发布门禁并验证后端合同。 最新10564a2/34175430169终态11/11，过期返回增量已取得自身证据；真实角色/全链路仍归A3。
      - [x] **FE-003L-A3b UniApp秒杀列表→活动规格→结算入口（代码/自身Linux候选完成，未发布）**：列表使用严格时段对象和ID、20项分页，区分错误/空页，同页重试，hide/tab/unload阻止迟到响应和旧项导航；详情只读view=skus并显式选择活动unique，严格数量/排期，不回退基础SKU、不自动改价或缩量。匿名及410002登录返回重读目录后恢复显式意图；同token身份更新、hide/unload清旧状态；加购new=1/type=1，结算保留子活动身份。新增独立导航锁覆盖匿名、加购成功及prepared重试，旧回调不能解锁新导航，结算导航失败复用同cart不二次加购。20项新实际Vue/Pinia/请求/页面运行时测试，工具链72项、类型检查和三目标构建/3项产物检查通过。H5在127.0.0.1:5190、390×844及1280×900，用真实控制器/PGlite桥接验证切时段迟到、错误重试、显式蓝2、两类登录返回、在途重复点击、停父拒绝、返回刷新及旧详情隔离；真实cart为uid11/type1/activity20/base qablue01/数量2，订单0。认证响应与外设接口仍为隔离替身，未完成真实认证/确认报价/建单支付或真机。结算fragment不再透传路由参数引发Vue警告；DCloud深导入Vue Router的一条弃用警告单列保留。无生产连接或部署，不能作为完整A3收口。 自身10564a2/34175430169终态11/11成功，真机和真实支付不在入口候选勾选范围。
      - [x] **FE-003L-A3c PC拼团列表→活动SKU→指定团结算入口（代码/隔离候选）**：74648e7/Actions34178371997自身11/11成功，284文件2,132项零跳过。新增view=skus只读目录，活动/基础商品/团长ID分离，type3活动unique映射基础SKU，不再硬编码sku00001；实际成员行和待支付订单分别计席位，每单一席与购买件数无关。最多500规格、5建议团及1指定团，批量聚合、不返回成员身份；指定团不存在/过期/满员/已参加/待付款不自动改开团。PC列表改title、20项分页和sort/id稳定排序；详情显式规格数量、独立开团/参团、最新登录返回意图、迟到结果隔离、已创建cart导航重试和显式放弃无效团。新增22项真实SQL、4项纯合同和22项实际SFC测试；相关Worker36项、PC60项/构建、双Worker类型通过。内置浏览器桌面和390×844验证匿名返回蓝2团400、过期返回红3开团、实际cart映射、无效指定团恢复及列表错误重试。该批仅新建1条隔离购物记录，既有1条合成订单及团/库存不变，未建单付款或连接生产。新目录不等于恢复旧pink接口，不关闭API-006-ACTIVITY-PINK与A3全流程。
      - [x] **FE-003L-A3d UniApp拼团列表→活动SKU→指定团结算入口（代码/隔离候选）**：c066371/Actions34183548166自身11/11成功，285文件2,163项零跳过；确认恢复DIY后的UniApp构建与单元门禁通过。专用适配器与composable复用严格活动目录，列表20项分页/失败重试/迟到隔离，显式SKU、数量及开团/参团；原生导航门禁、已加购导航重试、同token续期、隐藏/卸载、匿名及过期登录返回均有回归，旧页面回调不能污染重用实例。27项拼团回归和完整toolchain99项、类型、H5/微信/App构建及3项产物检查通过；路由审计仍151旧页/60新页/51缺口，不增加旧页覆盖分子。CUA桌面和390×844验证蓝2团400→登录→精确结算入口、满员禁参且不自动开团、过期返回红3开团、下架拒绝、无效团显式放弃和列表同页重试。3次隔离cart请求仅1条蓝色2件购物记录，新增订单0，订单/商品/SKU快照不变；团表曾由测试控制器修改人数，其无排序全数组比较为false，不能声称团表指纹不变。819b447自身CI终态9/11，唯一单元失败为遗漏DiySuspendedNavigation；本轮恢复组件，保持38页原断言，重新通过99项/三端构建，并用CUA验证桌面与390×844下展开悬浮按钮→营销活动页。本轮独立导航夹具仅5次GET、cart空/新订单0、订单/团/商品/SKU快照不变，不覆盖或改写上一轮证据。原修复待CI现已由c066371验证；旧状态页新增候选仍不等同于完整取消/退款合同。真实设备、报价建单付款及生产发布仍开放。
    - **钱包最新增量（2026-09-07，本地通过，待自身Linux）**：基础修复10656f1已获11/11 Linux验收。本批新增数量接口及首屏数量头、全部/24小时内到期/通用/品类/商品/品牌筛选和有界纯文本规则，覆盖上段对应未完成状态；列表和数量复用相同UID/状态/范围谓词，但两次读取不是原子快照。13项钱包状态测试及完整工具链32项、相关Worker 10文件116项、类型/三端构建/PC回归通过；H5隔离真实SQL桌面与移动验收通过。具体商品/分类层级/品牌集合及导航、PC对应UI、活动/上传真机/并发恢复和真实角色/provider/发布仍开放，FE-003L不勾选。
    - **钱包页面历史增量（2026-09-07，由上条覆盖后续状态）**：新增apiCouponWallet/useCouponWallet，复用共用规范化和分页状态机，四状态分开，失败不伪装空券，刷新/切页/离页/换账号清理旧详情，旧响应不覆盖当前身份。CUA在127.0.0.1:5176、1280×900/390×844使用隔离PGlite真实控制器验证20→29张、before104失败后同游标重试、8.5折详情、未来券禁入口、已使用/过期/占用状态和12秒旧请求隔离；订单/账单0、库存8/积分100保持不变。29项工具链含10钱包/11结算状态测试，UniApp类型、三平台构建及3项产物检查通过。只提供明确的“浏览商品”，不冒充自动应用或已过滤商品范围；当时数量、快过期/四范围筛选、券规则与指定商品/分类/品牌导航仍开放，当前状态以上条为准。
    - **共用契约前置增量（2026-09-07，本地候选，未接页面）**：PC的纯quote/券页/分页状态机与购买参数解析提取到view/common，保留原PC导出入口；新增UniApp checkoutApi对严格buy/普通已选行、confirm/computed和订单筛券做边界校验，不发送客户端金额/支付方式。uni.request增加独立getResponse保留大小写归一响应头；原get/post等仍返回data，错误保留status/data/httpStatus；登录会话版本防止旧请求成功/过期响应污染新登录。新增20项实际请求模块/适配合同和4项真实控制器/隔离SQL测试通过，相关18文件179项、Worker最终双类型、PC构建、UniApp三端构建通过。**confirm.vue仍未接新API，现有本地计价和随机key问题未修复，不能勾选FE-003L，也不能将未进入页面产物的新模块当三端运行时验收。**
    - **购买入口必须同步修复**：goods/detail的立即购买、seckillDetail、bargainDetail、activity/detail及user/integral加购没有new=1；只有套餐apiDiscountCartAdd显式new=1。普通无scope cart/list确实包含两类行，不能把现状描述为服务端默认只返回new=0。严格buy接线须同步所有入口，保留普通加购new=0，不能先改确认页导致全部旧入口失效。商品页还伪造sku加ID，秒杀/砍价/拼团固定sku00001；需按实际SKU/活动合同修复，不能凭猜测替换。待完成还包括真实Pinia生命周期、H5浏览器全链、离页/刷新意图恢复、表单上传门禁、活动报价及真机/provider。
    - **页面接线增量（2026-09-07，本地通过、待自身Linux）**：商品与三类活动不再伪造SKU；实际商品attr_value归一并校验，普通加购new0，立即购买/活动/积分new1。购物车列表/计数显式scope=cart。配送、积分、券变更后旧金额立即失效，首单/会员/运费/券均展示服务端结果；积分兑换新增pay_integral=活动SKU积分×数量，SQL证明30×2=60且确认/重算不写库存、积分、订单或账单。持久化失败不发新单，首个明确ORDER_FORM_REJECTED可解锁，已有不确定结果则保留原意图；成功仅跳详情，不自动支付。H5两次未知提交捕获key/body完全相同，couponId41/useIntegral=true/shippingType2，样本订单/账单0、库存8、积分100不变。截图检查1280×900及390×844、无业务console error，仅DCloud既有vue-router弃用告警。清单204勾选/145开放/349项不变；未应用生产、未发布。
- [ ] **FE-004 Supplier 对账**：旧端共 41 个 `pages/**/*.vue` 文件，但只有 19 个不同的可导航业务屏幕（20 条 route record，其中账单页重复注册）；其余为 16 个内嵌组件和 7 个未路由/错误脚手架。新端当前为 18 个页面组件、19 条屏幕 route record，不再使用“41→13”失真页数衡量覆盖。19 个旧屏幕中 17 个已有候选覆盖、2 个为部分替代、0 个整屏可执行缺口；逐屏证据和 12 项 granular checklist 固化在 `workers-ts/audit/supplier-frontend-parity.json`。浏览器 API 已同源 `/supplierapi`，Pages Function/Vite proxy 已接入，正式 Supplier Pages 项目、`WORKERS_API` 默认映射、同源代理和配置已按FE-004K验收，614bf34已测试发布；真实账号/第三方与发布后业务观察仍未完成。
  - [x] **FE-004A 页面/能力盘点**：逐项映射 19 个旧业务屏幕、16 个旧内嵌组件、18 个新页面和 19 条新屏幕路由；区分候选覆盖、整合替代、缺失与外部门禁。
  - [x] **FE-004B 订单/财务导出与 Queue 历史入口**：新端已接四个有界 manifest 和两条租户内只读历史合同；订单/发货单、批任务与财务只导出显式勾选行，物流目录保持只读下载，浏览器 CSV 二次中和公式/NUL并清理文件名。旧全局 Queue 重跑、停止、删除入口未恢复。
  - [x] **FE-004C 前端动作级 RBAC**：订单发货/收货/备注/敏感导出要求 `supplier.order.manage`，小票打印要求 `supplier.print.manage`，电子面单入口要求 `supplier.waybill.manage`，提现、收款设置写入和财务导出要求 `supplier.finance.manage`；服务端继续作为最终 authority。
  - [x] **FE-004D 本地桌面/移动浏览器 QA**：预览数据下订单、Queue 历史/明细、财务勾选导出、桌面与 390×844 移动导航均通过；标题/URL/DOM正常、页面级无横向溢出、控制台 0 warning/error。该项只证明本地候选，不是生产 E2E。
  - [x] **FE-004E 商品评价回复（候选完成，已测试发布）**：新增 Supplier 双重归属校验的评价列表与 500 字有界回复写合同、`product.view/manage` 权限、行锁事务、批量回复读取、页面/导航和桌面/移动预览；旧 DELETE 只按全局评价 ID软删且第一方页面没有入口，已证据化退役，不恢复跨租户删除能力。仍需 FE-004I 的真实主管理员/受限账号 E2E。
  - [x] **FE-004F 可复用规格模板（候选完成，已测试发布）**：确认旧 `product_attr` 是活跃菜单且被商品编辑页复用；现有 Worker 五条模板合同已按 `(type=2, relation_id=当前 Supplier)` 修复旧详情/删除裸 ID风险，新端新增规格模板列表/筛选/新增/编辑/删除、`product.view/manage`动作级 RBAC，并在 ProductForm 以二次确认显式套用结构后重新生成 SKU。仍需 FE-004I 的真实账号 CRUD与商品套用 E2E。
  - [x] **FE-004G 配货单预览（候选完成，已测试发布）**：确认旧订单页真实提供单单与最多10单批量打印，但旧 `distribution_info` 只按裸 ID读取收件人、电话、地址、备注和价格。新 Worker以最多10个无重复 ID、`supplier_id=当前 Supplier`和未系统删除条件整批失败关闭，快照解析有256 KiB上限；新 `/orders/picking-sheet` 提供每页6项、A4打印、单单/勾选入口和 `order.view`只读权限。旧二维码每页只编码同一个站点根地址、与订单无关，已明确停用。仍需 FE-004I 的真实账号/真实订单打印预览 E2E。
  - [ ] **FE-004H 附件/富媒体**：等待 SUP-003 生产只读聚合与附件票据专项授权后补商品媒体选择器和真实流程；不得绕过该门禁直连生产对象。
  - [ ] **FE-004I 真实账号 E2E**：用主管理员和只读/仓库/财务受限子账号逐屏验证正反权限、订单、商品、售后、财务、打印、面单和移动布局。
  - [ ] **FE-004J 第三方真实流**：支付退款、打印机、电子面单和附件提供商仍需真实配置、失败/UNKNOWN/重试对账。
  - [x] **FE-004K Supplier Pages 预发**：2026-09-13核实正式cinashop-supplier.pages.dev、production/main@614bf34和Functions。项目与当前部署均无环境变量，代理使用已核对源码的固定WORKERS_API默认值；Pages无直接数据库/存储资源及Secret需求，后端凭据留在Worker。线上入口哈希与本地一致，编译产物baseURL=/supplierapi，初始化200、匿名及无效令牌410000；双读控制面无漂移。重复命令npm run audit:supplier-pages -- <精确发布SHA>，24项配置反向/实际代理测试通过。证据workers-ts/audit/supplier-pages-acceptance-20260913.json；不等同FE-004I/J/L或上游运行角色最小权限。
  - [ ] **FE-004L 发布后观察**：发布授权及614bf34测试部署已完成，deployment/Git SHA和只读冒烟已记录；仍须完成日志、错误率、真实业务对账及观察窗口，不因Pages配置验收关闭本项。
- [ ] **FE-005 Kefu 对账**：旧 Admin 客服目录 31 个组件，新工作台 2 个整合页面；密码、扫码、微信入口和游客会话本地接入已完成，token/identity 使用 per-tab `sessionStorage`；关闭标签页不等于服务端撤销。正式Pages Origin已为cinashop-kefu.pages.dev且完成614bf34测试发布；仍须用真实客服/微信身份和生产兼容数据验证。
- [x] **TEST-001 Linux CI**：最新[Actions `33968639077`](https://github.com/cinagroup/cinashop/actions/runs/33968639077)对`4fe0530`在Ubuntu24.04/Node24.14.1/npm11.11.0以锁安装完成8/8 jobs：Worker双TypeScript、236文件/1,534项全部通过（含4项真实PostgreSQL16.14测试，无跳过）、workerd3文件/24项、Worker生产依赖审计、SQL文本201→263零源列缺口/窄范围漂移0、真实PG16三路径目录审计、PHP权威route审计、五端构建与Kefu17项、通用依赖18项、ECharts10项、UniApp工具链8项/三端产物3项/Intlify9项。Gitleaks233提交全历史未发现泄露；Kefu/PC全依赖审计门禁通过。真实目录外部↔内嵌索引52→5均有同构别名，ORM其余索引/默认/约束/序列仍属DB-009；inventory退出0不代表完全等价，隔离runtime不代表生产E2E。历史失败保留审计，UniApp/Drizzle依赖风险仍归TEST-004，不把Worker生产依赖通过等同全仓库0漏洞。
- [x] **TEST-002 Workers runtime**：Ubuntu 24.04、Node 24.14.1 的 GitHub Actions `33380831249` 已让真实 workerd 进入 13/13 断言，覆盖 Cron 时间窗、Queue ack/retry/DLQ、隔离 KV/R2、DO 持久化/并发、WebSocket 101/hibernation/token 撤销；测试配置不引用生产 Hyperdrive/KV/R2 ID。Windows 原系统运行库组合有启动前 `0xc0000005`；2026-09-20 同一二进制配独立目录 Microsoft 签名运行库后，47项支付合同及7项本地Hyperdrive/PG场景通过，未修改系统组件。详见[本机运行时对照](workers-ts/docs/offline-order-workerd-verification.md)，不以本地测试替代当前代码的Linux CI或生产验收。
- [ ] **TEST-003 性能与可观测性**：父项保持未完成；仓库内指标来源、对象日志和阈值合同已建立并由 Actions `33393069797` 复验，但生产部署、指标基线、通知目标、真实告警与观察窗口尚未完成。
  - [x] **TEST-003A 仓库可观测性合同**：提交 `beb2071b397eb316ee8cb5592656b3dceb7ed1a3` 新增 `audit/observability-policy.json`，定义 Hyperdrive、Queue/DLQ、DO、R2、登录、支付、退款、打印/面单 14 个信号；`npm run audit:observability` 固定资源 ID、100% Workers Logs、27 个关键事件、10 个域和6个已观察发布阻塞。366 个生产 TS 源文件除统一日志器外禁止直接 `console.*`；运行时及 AST 门禁拒绝正文、payload、token、查询、URL、凭据、异常消息、任何 ID/UID、schema 覆盖、对象展开、嵌套对象和非有限数值。HTTP 关键流/5xx及日志约束14/14通过；Actions 同时通过 Worker 165文件/1,031项、workerd13/13、schema201→247零漂移、route1,904→1,448/746/728、五端构建与70提交Gitleaks无泄漏。
  - [ ] **TEST-003B 生产指标与数据库基线（PARTIAL：数据库只读入口已验证，平台指标仍缺私有观测入口）**：读取 Hyperdrive query/error/latency/pool waiter、Queue实时 backlog/oldest/retry/DLQ、DO invocation/error/memory、R2 operation status，以及 PostgreSQL `pg_stat_*` 聚合和四类任务 `UNKNOWN/DEAD`；不得返回 SQL、参数、业务值或 PII。本次授权已通过一次性 SHA-256 Bearer Worker 在 `REPEATABLE READ, READ ONLY` 下完成 DIY 范围生产 PostgreSQL 聚合并删除 Worker，证明 Hyperdrive 只读入口可用且没有 DML/DDL；但尚未采集 Cloudflare 平台指标、完整 `pg_stat_*` 和四类任务基线，因此本项仍未完成，需提供现有私有观测入口或批准一个仅返回上述聚合的受控探针。
  - [ ] **TEST-003C 生产通知与告警（BLOCKED：需通知目标/值班 owner）**：在 Cloudflare Notifications或获批 OTLP/Logs平台物化14条策略，记录 policy ID、接收人、升级链，并用无真实支付/退款/打印/面单副作用的合成信号验证 warning/critical 投递。
  - [ ] **TEST-003D 发布后基线与调优**：候选发布后观察关键域，验证对象字段可检索且无敏感数据；以真实低流量基线校准阈值。fetch traces在企微 query-string 凭据脱敏被证明前继续关闭。
- [ ] **TEST-004 依赖告警归因与修复**：2026-09-05已完成六份锁文件npm官方审计，节点告警数不等于独立漏洞数或生产可利用数；B/C候选及F当前业务范围审查已由a00ac45/e991d92的8/8 CI收口，父项因UniApp/Drizzle等范围仍未收口而保持开放。禁止`audit fix --force`自动降级DCloud/Drizzle或跨主版本强压peer。
  - [x] **TEST-004G Hono新增生产审计阻断**：Hono固定4.13.5，三个公告及真实调用范围、初始红测与保留语义对照见dependency-patch-20260909.json。`2a6fa02`／[Actions34465733296](https://github.com/cinagroup/cinashop/actions/runs/34465733296)全部成功：374文件4333单元零跳过、79 workerd及双类型，生产依赖审计0漏洞。不降低门槛、不宣称业务漏洞实际可利用；仅关闭该依赖修复工程门禁，不代表正式发布或后续改动通过CI。
  - [x] **TEST-004H Vitest/mocker新增全树审计阻断**：Worker和Kefu的Vitest/mocker固定4.1.11，保留原Workers pool/Vite兼容边界、工具链API写／执行及文件读取限制。`2a6fa02`／Actions34465733296完整门禁成功，Kefu17项应用测试、11项工具链测试、构建及完整依赖审计0漏洞；Worker完整单元和workerd通过。不执行audit fix --force；Worker其它既有开发依赖风险不被此项豁免，证据dependency-patch-20260909.json及release-prerequisite-catalog-20260910.json。
  - [x] **TEST-004A 六端依赖基线**：初始Worker 4 moderate；Admin 1 moderate/1 high；Kefu 3 moderate/1 high/1 critical；PC 1 high；Supplier 1 moderate；UniApp 9 low/29 moderate/10 high，共48个受影响节点。已区分生产入口、浏览器产物和本地工具；DCloud从runtime包传递编译依赖，`omit=dev`不等于UniApp真实运行闭包。全部1844个锁节点均为公开npm/公开镜像包，无私有、file/link或凭据URL，审计前已检查元数据披露边界。
  - [x] **TEST-004B 客服旧测试工具链（候选修复通过本地及Linux CI）**：Vitest 2.1.9→精确3.2.6，同时移除测试子树中的Vite5.4.21/esbuild0.21.5，保留顶层Vite6.4.3与Vue插件兼容性，官方全依赖审计5→0。仓库使用`vitest run`且未开启UI/API，不能称线上RCE已复现。新增网络host默认禁write/exec、四组实际RPC旗标/注册文件边界测试（副作用为内存夹具），既有17项客服测试及构建通过。用户确认浏览器授权后，本批又通过CUA应用内浏览器对本地5207→5205隔离夹具复验密码表单登录、实时提醒/详情保留、显式已读/筛选、1013重连、4001清空与随后HTTP401退出；1280×720和390×844无横向溢出，正常流程console error/warn为空。17项业务与6项依赖测试再次通过；该浏览器证据不代替生产角色、真实渠道或生产发布；本批Linux CI `33956473619` 已通过。未复现Windows UI附件链或真实远程执行。
  - [x] **TEST-004C Nano ID公告修复版本与当前调用验证（候选通过本地及Linux CI）**：Admin/PC/UniApp从3.3.17→3.3.18，五端所有锁副本达到公告修复版本；Node/浏览器CJS/ESM、async、non-secure及原生async原函数体共11入口的指定零/负值和常用正长度测试通过。旧3.3.17公开tarball的native默认零长度用同一探针触发有界循环失败，新版通过；真实PostCSS固定`nanoid/non-secure.nanoid(6)`及CSS解析不变。独立复核另确认3.3.18“默认0→正长度覆盖”的Node/native库级残留，本补丁没有修复它；五端业务源码无Nano ID直接调用，唯一锁依赖方PostCSS不走该生成器，尚无业务可达证据。此项只关闭官方版本升级及现有调用验证，不宣称库全部长度组合安全；未来扩大用途必须重审此残留。PC全审计1→0、Admin2→1、UniApp48→47。
  - [ ] **TEST-004D UniApp开发/编译链与运行闭包**：D1已形成本地候选，父项不关闭。保留12个直接/26个锁节点同版DCloud `3.0.0-4020920240930001`，Vite5.2.8→5.4.21符合旧插件`^5.2.8` peer；2026-09-05官方模板50204...003及alpha50205仍精确要求Vite5.2.8，不能直接以整套升级取得Vite6.4.3安全边界。官方全树审计现为50个受影响包条目（9 low/32 moderate/9 high），不是独立CVE或生产可利用数量；剩余项如下。
    - [x] **TEST-004D1 Vite 5兼容补丁与开发默认边界（本地及Linux CI候选完成，未发布）**：Vite直接公告15→3；显式`host:127.0.0.1/cors:false/fs.strict:true`覆盖DCloud宽松默认，保留现有代理合同，验证仅使用loopback夹具。真实插件配置/HTTP/WS及厂商更新隔离共8项、三端产物3项、既有NanoID3项、类型及H5/微信小程序/App资源构建通过，[Actions `33958703299`](https://github.com/cinagroup/cinashop/actions/runs/33958703299)对`71570c7`的8/8 jobs已复验相同门禁。CUA浏览器1280×720与390×844验证登录模式切换、空表单提示、刷新后交互；无横向溢出/错误overlay/console error，存在DCloud深层导入vue-router的弃用warning。未提交登录/短信、未连接生产；复核后在测试与重跑构建中关闭DCloud自动更新检查，此前是否实际外联无法由现有记录确认。npm包条目47→50源于新增三个Vite插件传递告警；锁只改变Vite版本并新增24个嵌套esbuild/平台节点，不伪装为审计归零。
    - [ ] **TEST-004D2 Vite剩余安全升级**：5.4.21仍命中`.map`路径穿越、Windows launch-editor UNC/NTLM及NTFS ADS/8.3路径告警，另有esbuild0.20.2/0.21.5；需受支持的DCloud/Vite6.4.3或更高兼容链，或另经完整验证的补丁方案。当前只收紧默认值，不承诺显式`--host/--cors`、manifest覆盖、端口转发、SSR或所有Windows路径安全，禁止共享网络暴露。
    - [ ] **TEST-004D3 国际化与其余传递链**：MP/App把vue-i18n别名指向DCloud内置9.1.9，顶层升级不足以覆盖；H5裸vue由插件改指uni-h5-vue，MP使用uni-mp-vue，App外置Vue，不按锁中Vue3.5.41推定实际运行版本。Intlify当前业务可达性已按D3A形成有范围no_change证据；继续验证加密uni_modules的adm-zip解压、微信自动化二维码Jimp/jpeg-js与phin、Express/qs、Jest/jsdom/@tootallnate/once。App本轮只证编译资源，空AppID、HBuilderX/基座/签名/真机及真实业务E2E仍未完成；MP/App运行时直连生产域名，不能以H5代理变量推定设备测试已隔离。
      - [x] **TEST-004D3A Intlify当前应用可达性（有范围no_change，本地及Linux CI通过）**：旧message-resolver9.1.9的两类prototype路径及顶层vue-i18n9.14.4的HTML属性风险均库级复现，不能称库已修复；但应用源码未注册/调用Intlify，真实CLI构建的H5、微信、App-vue/App-nvue四轮图均未加载或外部化Intlify。H5/微信使用独立DCloud uni-i18n；App显式外部依赖仍为设备Vue/@vue/shared。新增9项模块标识/真实Rollup负例/独立renderjs及wxs编译阻断/DCloud语言控制/三目标真实CLI制品审计，记录10个App直接复制资源的逐字节来源与SHA256；未来Intlify进入业务图或新增这两类独立脚本即失败并要求重审。候选复核发现的renderjs外层图盲区已确认并补齐真实回归；提交`d456d541159b5aeee42e58bf18c0225c995f3307`的[Actions `33960215805`](https://github.com/cinagroup/cinashop/actions/runs/33960215805) 8/8成功，四轮图及复制资源证据与本机一致。仅覆盖当前源码/CLI制品，不把复制文件hash当任意预打包代码安全证明，不覆盖设备基座、APK/IPA、SSR、开发运行图或未构建的支付宝/百度/抖音目标；依赖版本/50条包告警不变，D3及父项仍开放。
  - [ ] **TEST-004E Worker Drizzle旧esbuild链**：旧链仍为drizzle-kit0.31.10→esm-loader→core-utils→esbuild0.18.20共4 moderate；库级loopback探针确认JS/404/SSE均有通配CORS。独立调查及真实CLI加载跟踪确认当前Drizzle使用tsx：CLI解析到esbuild0.25.12、程序API通过安装的tsx解析到0.28.2，七个Drizzle入口不导入旧loader/core-utils；当前调用路径为有范围no_change，不是库已修复。官方latest仍0.31.10，core-utils3.3.2已弃用；不强压跨兼容范围override或降级。完整模型生成发现的索引/序列/引用键缺口已按DB-008修复候选；ORM↔DDL差异列DB-009，依赖淘汰仍开放，不运行真实`db:push/migrate/studio`消警。
  - [x] **TEST-004F Admin/Supplier ECharts（当前应用路径no_change，回归已通过CI）**：锁定5.6.0的`lines`默认tooltip原始HTML行为已在真实依赖sink复现，不能宣称库已修复。已逐一审查6个import文件、13个setOption构造器/17个图表实例：Supplier只注册LineChart；Admin三处series展开涉及四类API，其真实服务固定生成line/bar、白名单名称与数值数组，其余构造器固定line/bar/pie。新增7项生产者投影测试及Admin/Supplier各5项真实tooltip转义测试本地及Linux CI通过；完整源端到sink矩阵见审计。独立复核及e991d92的8/8 CI已完成，按有范围不可达结论收口，不强行跨主版本升级；包告警1+1仍存在。引入lines、任意图表JSON或改变生产者/注册方式必须重开审查，生产部署版本不由此验证。
- [ ] **TEST-005 Linux Node/PGlite原生测试进程稳定性**：70103a0的Actions34017020788首轮1583/1584通过，既有ESM索引子进程在17,504ms因“corrupted size vs. prev_size”退出，非90秒超时或已报告SQL断言失败；未获得core或原生回溯，根因未确定。对同一提交仅重跑一次失败的完整任务，代码/并发/时限/断言均不变；第二轮1,584项及后续PG16审计全部通过，但不把根因标为已修复。[Node #64500](https://github.com/nodejs/node/issues/64500)和[PGlite #1053](https://github.com/electric-sql/pglite/issues/1053)报告相近并发原生崩溃，但不能据此认定同源。再次出现时先保留脱敏signal/阶段/资源及原生诊断证据，不循环重跑碰绿、不自动升级或关闭WASM检查。
- [x] **TEST-006 迁移CI时长余量（本批拆分与失败汇总已验证）**：原eebe827合并Worker18分54秒/20分钟仅余66秒。本批保留原12项执行内容、4个setup步骤、全部断言、版本、默认并发与子进程边界，将目录/生成器与单元/类型/路由拆为独立20分钟任务；原检查名always汇总且两依赖必须success，新增2项合同测试。8494360首轮单元17分51秒、余129秒，目录失败时汇总确实失败；194be1c/Actions34024902767首轮10/10，单元任务12分30秒/余450秒，目录4分5秒/余955秒，新增初始化成本已包含。采用两次观测中保守129秒余量，不将646.96秒单元的较快运行当固定性能保证；后续增加完整路径须重新审查容量，不靠加并发或放宽时限碰绿。本项不关闭TEST-005原生稳定性风险。
  - E5A最新ce5805a/Actions34028634339首轮未复现，不作为根因修复证据。单元任务1,073秒、20分钟上限余127秒，目录任务238秒余962秒（含完整目录190秒、NOT VALID夹具3秒、序列夹具4秒及初始化/清理），汇总3秒。结合此前1,071/750秒观测，TEST-006后续容量评估采用最新保守127秒；未增加并发、放宽时限或减少执行范围。
  - [x] **TEST-006B E5完整升级前的单元测试分片容量（代码/Linux CI完成）**：94816da/Actions34030550652首轮11/11；同一原生配置的126/125文件两片分别810/794项全通过，合计251文件1,604项、0跳过，包含4项真实PG16测试。每片实际JSON报告逐文件/逐断言通过，清单摘要相同且完整、不重叠；14类拒绝回归通过。两任务671/447秒、最长余529秒，较ce5805a余127秒增加402秒；这是一次完整新配置观测，不是固定性能保证。各片保留默认进程并发/隔离/PG16和原20分钟/子进程上限，非单元检查只在第一片执行；fail-fast=false，原required-check名称与失败汇总体不变，未扩展扫描例外。旧八路径及两份生成器证明整份深度相等，12库确认清理；不使用实际忽略分片参数的`list --filesOnly`冒充覆盖证明。当前验收不关闭TEST-005或E5B。
- [ ] **REL-001 发布候选门禁**：所有 P0 完成，相关 P1 域完成；生成变更清单、DB 前置、Secret/资源检查、回滚版本和 smoke tests。
- [ ] **REL-002 主 Worker 发布（2026-09-12测试部署已完成，正式验收仍开放）**：97b2a13已发布，版本42e39c18-08b8-44c1-8518-5421c6307ea9流量100%，健康／匿名负向／关键只读通过；受控业务写、完整CI终态、最小权限、0150及回滚演练仍需独立验收。旧版本9f1fd655保留，新增DO迁移后不能未经兼容核对直接宣称可回退。
- [ ] **REL-003 Pages 发布（2026-09-12五端测试部署及代理复验完成）**：Admin使用cinashop-admin.pages.dev，PC使用shop.cinaseek.ai；H5/Supplier/Kefu分别使用cinashop-h5/supplier/kefu.pages.dev。五端均已发布97b2a13和Functions，deployment ID及SHA见full-test-deployment-20260912.json。首页及入口脚本200，Admin代理已返回真实JSON匿名拒绝，公开品牌JSON正常。保留已占用域名与admin-ts项目；真实登录、写入和完整五端业务验收仍开放。测试商品图片已在后续独立处理，用户确认新图可读；不据此关闭五端整体验收或重复改图/清缓存。
- [ ] **REL-004 发布后观察与旧 PHP 下线**：至少观察登录/写入/支付/退款/Queue/Hyperdrive/R2/DO；完成流量切换、回滚演练、旧回调撤销和旧数据库只读封存后才能下线 PHP。

## 当前下一步（2026-09-21 更新）

已发布基线为 `main@7a5508f`，受限业务/后台连接及本次授权测试订单维护已完成，不重复安装权限、清理订单或回退高权限。当前 `codex/user-visit-history-20260921` 仅本地候选；推荐分页、品牌/标签/参考会员价，以及真实秒杀/砍价/拼团ID和有效期导航已补。预售已完成PHP全款模式核对及只读时段/规格合同，接下来优先补预售type=6的加购/数量/报价/建单权威校验和履约快照，再连接列表/详情/推荐与结算恢复；随后继续促销边框、隐私/协议与账号治理，并做真实账号/真机及部署后的联合验收。当前路由台账为64目标页、97兼容映射（60候选覆盖/37部分替代）、48未映射或跨端缺口，旧151路由完整计账。主Worker/Pages的新候选发布仍需独立批准；真实支付provider与其余DATA-007异常裁决不在本轮授权内。以下保留2026-09-07当时的待办及计数，不当作当前线上状态或覆盖统计。

### 2026-09-07 下一步历史记录

**最新增量覆盖下段历史待办**：b98d137/34115191204最终11/11通过，275文件1,866项零跳过；S6按代码/Linux勾选。本轮两端搜索/七排序与稀疏续查交互已接线并通过桌面/移动真实SQL浏览器验收，另列S7待自身Linux。S2真机、促销、活动整链和生产门禁保持。HTTP统计不变：TS1,649、匹配881、可执行863、缺失1,023、可执行缺口1,006；清单209勾选/147开放/356项，不以新页面数量当作旧业务等价。

最近完整通过基线为`0a20ec6`/[Actions34092567687](https://github.com/cinagroup/cinashop/actions/runs/34092567687)，11/11、271文件1,829项Worker单元零跳过。后续结算页提交`acac7c3`的34095671406为9成功/2失败，确认为confirm遗漏悬浮导航，本轮已恢复组件并通过相关32项本地测试，修复仍待自身完整Linux。UniApp完整报价、持久化冻结重试及钱包基本状态/分页已接线并通过本地浏览器，不再把这些列为未开始；下一步先核验修复CI，再补钱包数量、快过期/范围筛选、规则和范围导航，促销overlay、活动整链、表单上传真机、跨标签竞争与受控恢复。工具链29项、三端构建、产物3项与三端业务图i18n审计9项通过，不等同真机或生产验收。所有候选未发布，数据库G、DB-009整体及生产应用、TEST-005、1,007条可执行路由缺口、真实业务和发布门禁继续开放；容量仍采用历史保守476秒，根JSON独立证据未改。

候选仓库为263表，201张PHP共享参考表零列缺口；生产最后已验证表集也是263表，但API-014新增提现重放、流水事件键及通知业务列/索引/约束尚未应用，DB-006/007继续开放。项目所有者已确认本部署是全新系统，DATA-001～005的旧站历史复制与行级对账均不适用；后续数据工作是DATA-006新系统初始化、DATA-007现存孤儿/重复状态裁决和DATA-008店面内容配置。当前路由为PHP 1,904、TS 1,647、精确匹配880、可执行862、受控不可用18、原始缺失1,024、退役17、可执行缺口1,007；新增客服收件箱和两个实时socket端点属于目标扩展，不增加PHP匹配分子。提现成功流水/审核通知F1、待办G1、申请事件及隔离收件箱G2a、独立实时后端G2b1与接收端/隔离浏览器G2b2均已通过候选工程门禁；自动微信渠道D、真实模板送达F2、正式Origin/真实角色/部署验收G2b3、企业微信G3及真实角色/发布E尚未完成。Admin 362个前端请求变体全部可执行，UniApp仍有51条旧路由分成7组缺口，可观测性仍有6个发布阻断，依赖告警另列TEST-004。继续按清单处理行为缺口、生产初始化、真实角色/provider、前端E2E和发布门禁；主Worker与Pages均未因本轮审计发布。

### 迁移清单生成时的历史快照（保留审计）

本段保留的是清单生成时的历史数字与判断，已由上方2026-09-04基线取代。旧记录中的“等待源MySQL恢复/复制/对账”不再是活动门禁；Work域改为以真实租户provider权威快照初始化，商城域改为新系统运营数据创建与现存异常裁决。PHP安装SQL继续只作为201张共享表结构及接口语义参考，不作为本部署的历史业务数据源。

# DB-009G2：热点、多租户引用验收边界

最新CI补验：bb04311 / Actions34693434221已11/11成功，385文件4508单元零跳过，
精确分片完整互斥且workerd通过，补齐新Hyperdrive探针自身Linux证据。
线上六类权限缺口未改变，不把测试成功视为权限整改完成；后续运费生命周期不在该提交内。
证据见 `audit/work-parent-production-permissions-20260912.json` 的ciFollowup；下文待CI为阶段历史。

## 当前增量：实际 Hyperdrive 身份实测（2026-09-12）

通过与正式 `cinashop-api` 相同的 Hyperdrive 绑定运行一次固定只读目录审计，结果 `ready=false`。
两张父表、8个引用键及读权限齐全；实际身份可见，但以下六类身份保护仍失败：
可达高权限角色、对象所有权、建对象权限、父表删除/触发器权限、引用键更新和复制绕过。
这不是业务访问失败，也不是安全验收通过；说明不能用当前连接声称父身份不可变。
未更换凭据、GRANT/REVOKE、改表、清理业务数据或发布正式应用。

按 Cloudflare/Workers 技能沿用隔离的临时 Worker、已有生成绑定类型和 Hyperdrive 连接，
不将诊断路由挂入正式 API。执行 `scripts/run-paid-runtime-production-audit.ps1 -WorkParents`，
该开关与 `-CatalogOnly` 互斥；默认仍审计原有付款权限范围。
短期随机令牌、精确路径/方法、no-store、只读事务、有界超时及异常脱敏保持。
请求结束前关闭客户端符合 [Cloudflare 连接生命周期指引](https://developers.cloudflare.com/hyperdrive/concepts/connection-lifecycle/)。

前两次因新 workers.dev 路由短时404停在访问校验，未执行数据库审计；它们先被删除后才重试。
最终执行器对无SQL的匿名GET/鉴权POST至多六轮就绪检查，仅重试404组合，仍要求403/405/404；
第三次在三轮后通过访问检查，数据库审计GET仅一次，HTTP200返回上述真实失败项。
三个随机探针均独立确认控制面及公开端点404，正式版本 `89dfbd1e` 和绑定前后未变。

本机56项（29探针合同+27真实PG16权限）零失败零跳过、双类型检查和Wrangler打包通过，
本机workerd则启动崩溃、0项执行，不能记为通过。新探针增量仍需自身Linux CI；
当前c486f22运行不包含它。证据 `audit/work-parent-production-permissions-20260912.json`。
下一步仍需独立的受限运行身份、完整业务授予/维护隔离及线上实际业务验收；
不能只删除六类权限就宣称整个应用可运行，也不因发现缺口盲目更换 Hyperdrive 凭据。

## 当前增量：可执行只读权限命令（2026-09-12）

在 `workers-ts` 目录执行 `npm run audit:work-parent-permissions`。
必须事先通过安全的环境注入设置 `WORK_PARENT_AUDIT_DATABASE_URL`，使用待审应用的实际登录凭据；
不要把连接串贴入日志、文档或命令行参数。命令不读取通用 `DATABASE_URL`、dotenv 或 Hyperdrive 配置，
并在本CLI进程内清除驱动会继承的 `PG*` 环境变量，不借用 `PGPASSWORD` 或 `PGPORT`。
目标限 PostgreSQL 16；URL 不接受查询选项/fragment，命令不接受额外参数。
非 `127.0.0.1` / `localhost` / `[::1]` 目标还须设置 `WORK_PARENT_AUDIT_ALLOW_REMOTE=1`，
直接远程连接显式 `rejectUnauthorized=true` 强制验证 TLS；本机或已明确建立的环回隧道不启用 PostgreSQL 层 TLS。
若提供商要求 URL 中的 TLS 查询选项，需从专用审计 URL 移除，不能因此绕过证书校验。

连接启动即设只读、固定 search_path 及有界超时，审计内部再使用只读事务。
退出码 `0` 仅表示两张父表的身份保护范围通过；`1` 表示权限过宽或必要检查不满足；
`2` 表示输入、连接、执行或清理失败。输出只有具名检查结果，不输出身份、连接串或原始异常。
本机30项（3命令+27权限）零失败零跳过，双类型通过，临时库/schema/角色零残留。
真实子进程覆盖非空完整ORM下安全身份、提升DELETE权限及错误密码；六类非法输入被连接前拒绝，
监听哨兵收到0次连接，各次有效审计前后业务行与ACL一致。
远程TLS目前只有配置合同证据，未实际调用线上身份，不声称完整业务授权或远程验收。
最小权限技能将本命令限定为检查，不自动 GRANT/REVOKE 或更换凭据。
证据 `audit/work-parent-permission-cli-20260912.json`。

27adf07 / Actions34691224392现已11/11成功，精确覆盖383文件、4491项单元零跳过，
两个分片集合完整且互斥，容量步骤和此前两项CI失败均已复验。
此终态覆盖权限预检及15目标/百万行证据，不覆盖其后的8业务用例和本命令；两者仍需自身CI。
详见 `audit/work-parent-permissions-ci-20260912.json`。下文运行中/待验收描述保留为阶段历史。

## 当前增量：真实受限角色业务调用（2026-09-12）

新增 `work-parent-runtime-flows.test.ts`，每项用完整ORM创建独立PG16随机库及真实临时LOGIN。
不是只在空表执行SQL：通过原应用 `withTx` 调用客户seen/apply、准备阶段解析、动作消费和脱敏服务。
8个业务用例覆盖新增/重复/编辑/标签替换/删除态/另一租户不变、撤销name更新权限后的整笔投影回滚、
UID唯一/歧义/冲突/缺失与重复消息，以及脱敏分页、待处理/UNKNOWN依赖、未来保留期和独立连接SKIP LOCKED。
保留父表DELETE和引用键UPDATE禁止，完整回调/用户/身份等非目标行逐项核对，所有生成约束和索引的
OID/定义/有效状态前后相等。最终两文件35项（8业务+27权限）零失败零跳过，临时数据库/schema/角色零残留。

真实调用还确认：脱敏的联表 `FOR UPDATE` 同时需要callback outbox上的更新权限，虽然不改outbox数据。
缺少该权限时服务42501并回滚；仅补 `UPDATE(update_time)` 后可脱敏，outbox整行仍不变。
同理，父身份预检通过也不证明具备客户name等业务列权限；撤权负向用例显式保留这个边界。
这些测试授权仅覆盖选定业务调用，不是上线GRANT配方，未修改线上身份、凭据或权限。
provider返回为合成快照；UID/脱敏用例的provider/Queue替身遇调用即失败。
完整回调入口、Queue运输、真实provider/租户、线上角色及wcao性能策略仍需独立验收。
证据 `audit/work-parent-runtime-flows-20260912.json`；正在运行的27adf07 CI不包含本增量。

## 当前增量：权限预检及 CI 失败修复（2026-09-12）

8fe0993 / Actions34689831315 已结束为 failure：两个单元分片各1项失败，其余4462项通过；
PG16目录（含百万行统计步骤）、运行时、密钥扫描和五端构建成功。不能记作完整CI通过。
第一处为新增容量步骤后未同步精确CI合同；现将新步骤及命令纳入必须执行列表，原门禁、
40/20分钟作业预算和单项期限不变。第二处为砍价到期夹具2秒期限与应用2秒锁超时竞争；
现待快照/连接/真实行锁就绪才按PG实际时钟设置1秒期限，核验等待开始早于到期，
并在阻塞端单次SQL请求中睡眠至到期后COMMIT。仍要求明确的活动过期拒绝及全部业务快照回滚，
未改应用代码、锁超时、业务时钟或错误容忍条件。

新增 `auditWorkParentIdentityPermissions` 只读预检两张父表的8个引用键，检查实际连接身份、
可达角色、所有权、DDL/删除/键更新及复制绕过权限和需人工审查的 SECURITY DEFINER 例程。
PG16真实临时LOGIN覆盖全部引用列、INHERIT/SET/ADMIN/inert成员关系、PUBLIC授权、
维护身份伪装、严格超时及恢复；27项通过。结合完整砍价并发23项及CI合同3项，最终53项零失败
零跳过，两套类型检查通过，临时数据库/schema/角色均零残留。

权限用例中的父表为空：实际SQL允许/拒绝只证明权限语义，不等于真实企微投影、回调、
脱敏或删除维护流程已用受限角色完整运行。此预检不是完整授权方案，也未调用线上身份或改权限。
本轮按最小权限技能限定为只读预检；策略依据
[PostgreSQL 16 GRANT](https://www.postgresql.org/docs/16/sql-grant.html)。
精确结果及文件SHA见 `audit/work-parent-permissions-ci-repair-20260912.json`。
G2仍开放，须验收本增量Linux CI、实际运行身份/业务路径及wcao规模/统计/保留策略。

保留完整ORM生成的表、CHECK、外键、标识列和现有索引；只在专用本机/CI随机数据库播种。
本项不是生产数据清理许可，也不默认批准新增六列宽索引或修改统计目标。

## 覆盖分母

`work-composite-foreign-key-plan.test.ts` 从模型清单选出所有12个只有窄首列候选的六列外键，
并对固定12个业务夹具名做集合相等校验。原有每子行不同事件的12项保持不变；另增加12项：

- 每表100003行，租户fixture的事件1与租户other的事件2各50000行。
- 事件3/4各一行，分属两个租户；事件5另有一行。
- 两个租户均有真实无引用父事件。支持表同样分别播种两个租户，允许复合键中的数字ID相同。
- 每个外键执行8种完整六列谓词，分别记录custom和强制generic计划，精确验证行数。
- 热点/稀疏父记录各做DELETE及键更新，要求准确报告目标外键；无引用父操作实际执行后回滚。
- 操作前后比对父子整行指纹、目标表全部约束/索引目录及列统计状态，禁止以移除约束通过测试。

## 如何解释代价

| 探针 | 含义 | 不能外推的结论 |
|---|---|---|
| 合法无引用父、稀疏引用父 | 观察真实父键分布下的选择性和generic计划 | 一次ANALYZE不证明普遍稳定 |
| 热点引用，返回50000行 | 无上限完整谓词诊断，验证多子行和真实行锁查询 | 不能当作RI触发器实际需要读取50000行 |
| 已存在的错误租户与另一租户事件ID组合 | 验证完整谓词不会跨租户返回行，并观察过滤代价 | 不等于数据库里存在这种不匹配的父键 |
| 真实父DELETE/键更新 | 验证RESTRICT/NO ACTION实际生效 | 外层EXPLAIN的触发器记录不是嵌套查询计划 |

PostgreSQL 16 `ri_PerformCheck` 对SELECT检查传给SPI的行数上限为1；因此不把无上限诊断
替代真实RI执行，也不自行加SQL LIMIT并宣称规划完全等价。
[PostgreSQL官方源代码](https://github.com/postgres/postgres/blob/REL_16_STABLE/src/backend/utils/adt/ri_triggers.c)。

## 真实RI内部计划增量（2026-09-12）

`work-foreign-key-index-plan.test.ts` 在专用PG16随机库的独立连接中加载auto_explain，
按官方支持的NOTICE/JSON捕获嵌套查询，启用实际行数与块计数，关闭逐节点计时与参数日志。
模块只加载该连接，所有设置为SET LOCAL；12次真实父DELETE/键更新均结束于ROLLBACK。
不改线上日志配置、不重启本机服务、不添加SQL LIMIT替代RI。
[PostgreSQL 16 auto_explain官方文档](https://www.postgresql.org/docs/16/auto-explain.html)。

本机PG16.15四项测试全部通过（51.45秒），双类型通过，隔离数据库/schema/角色零残留。
wcao_client_fk捕获12份完整内部查询计划：auto/custom/generic各含热点和无引用父键的DELETE/UPDATE。
6次引用拒绝精确核验23503及约束名，6次无引用操作成功后回滚；父子行指纹、外键、索引和统计目录保持。

- auto/custom热点实际返回1行、2块；无引用0行、2块。
- generic热点实际返回1行，但Bitmap Index Scan先读取100000项，合计89块；不能由输出一行推断启动工作量小。
- 本次最后默认统计样本下generic无引用为2块；前面五次ANALYZE的独立诊断仍有两次扫描100003行/4001块，不抹除之前三次全扫描的历史证据。
- auto阶段每种RI语句只有两次调用，不证明长连接自动切换generic后的表现；强制模式只用于诊断。

完整证据`audit/work-fk-nested-ri-20260912.json`。本增量只覆盖wcao的真实内部计划，
不替代另外15个目标的嵌套计划、默认缓存热身/多次采样、真实运行身份与规模、生产保留/统计策略。
测试代码本身不作为已部署应用变更；本增量自身Linux CI仍待执行，G2继续开放。

## 默认缓存热身与重复采样增量（2026-09-12）

前条只有短auto序列；现新增5个独立连接/统计样本，每个连接明确断言初始plan_cache_mode=auto，
不设置force模式。完整事务内默认ANALYZE，先DELETE后UPDATE，各按父键1×6、4、2、4执行9次，
每次以保存点回滚，最终回滚采样事务；逐样本验证全部列统计目标和pg_statistic原样恢复。
样本间新建连接以清空RI缓存，样本内保留真实重复调用缓存。

本机PG16.15四项测试通过（52.80秒）、双类型通过、隔离库/schema/角色零残留。
本增量捕获90份实际内部计划（70次精确拒绝、20次无引用成功后回滚），不是手写PREPARE替代RI。
5个样本均在第6次DELETE观察到Plan节点中的参数符号；随后的UPDATE共用同一连接，
已从参数化计划开始，不能称其为独立冷启动序列。参数化计划识别不检查始终带参数的Query Text。
[PostgreSQL官方计划缓存规则](https://www.postgresql.org/docs/16/sql-prepare.html)。

| 默认统计样本 | 热身后的实际RI行为 | 验收含义 |
|---|---|---|
| 2/5样本，corp/client估计均只有一个不同值 | 无引用扫描100003行/4001块；稀疏引用先过滤100000行/4002块 | 默认auto下已真实复现，不能视为只存在于强制generic的假设 |
| 其余3/5样本 | 无引用2块、稀疏4块；热点位图先处理100000索引项/89块 | 索引选择性改善不消除热点启动代价 |

以上比例只是本次五个随机样本的观察，不是生产发生概率。数据是极端热点并把稀疏行放在物理末端，
不据此宣称生产延迟。数据、外键、索引、统计目录均保持，线上没有修改。
完整证据`audit/work-fk-default-cache-20260912.json`。默认缓存是否会触发风险已获得肯定证据；
下一步仍需评估统计候选策略的实际RI效果、采样成本和适用规模，以及其余15个目标的内部计划。
不能仅用提高STATISTICS后的独立SELECT结果关闭G2，也不以该测试替代自身Linux CI。

## 统计候选规模比较：显式执行入口

配置专用本机`TEST_FINANCE_POSTGRES_URL`后，在workers-ts运行：

```powershell
npm run audit:work-fk:statistics-scale
```

`scripts/audit-work-fk-statistics-scale.ts`只接受固定规模100003/1000003和默认、1000、10000三档，
每档5个新连接/ANALYZE样本，初始plan_cache_mode必须为auto。不接受额外作用域/连接参数，
不读DATABASE_URL，禁止PGlite/远程/生产回退；使用既有安全辅助创建并核验专用随机PG16库。
本入口是容量实验，不加入普通单元测试；现接入Linux隔离目录作业末尾单独执行，
必须核验该步骤实际终态，类型检查通过本身不等于运行过此实验。

完整ORM的45个外键与所有CHECK/索引保留；按10000行分批播种，每语句仍有30秒时限。
每个样本的统计目标变更和ANALYZE均在回滚事务内，父DELETE逐次保存点回滚；
默认档不执行ALTER，所有档均不强制custom/generic。六次热点热身后测无引用、物理末端稀疏、再次无引用。
记录ANALYZE VERBOSE的实际采样量、墙钟耗时、表/索引字节及真实RI计划；计时包含诊断开销，
按固定顺序执行并非冷热缓存随机试验，不当作生产基准或三档延迟排名。

逐样本比对列统计目标/选项和pg_statistic恢复，结束比对全模型约束/索引以及三表整行哈希汇总。
百万行汇总使用每行哈希再汇总，避免拼接整表JSON超过单值大小限制；不是逐行导出的历史源端对账。
脚本最终成功输出WORK_FK_STATISTICS_SCALE_AUDIT才表示两个规模及清理均完成，进度行不是验收通过。
1000/10000只是候选诊断，不会自动新增生产迁移、更改保留策略或改造业务约束。

## 规模比较实际结果（2026-09-12，PG16.15）

上述入口完整执行251.73秒：两规模×三档×五样本，270次实际RI检查（210次引用拒绝、60次无引用成功回滚），
保留全部节点指标与120份热身后完整计划。三表数据、全模型约束/索引及每轮统计目录保持，
随机数据库/schema/角色零残留；双类型最终通过，四种入口越界均在执行前拒绝。
脚本执行中仅修正一处被擦除的TS类型断言，修正前后编译JS哈希完全相同，详细哈希记入证据。

| 行数 | 统计目标 | 实际行样本量 | 无引用全扫描的样本数 | 无引用实际块数 |
|---|---|---|---|---|
| 100003 | 默认100 | 30000 | 3/5 | 2或4001 |
| 100003 | 1000 | 100003 | 0/5 | 2 |
| 100003 | 10000 | 100003 | 0/5 | 2 |
| 1000003 | 默认100 | 30000 | 5/5 | 40001 |
| 1000003 | 1000 | 300000 | 1/5 | 3或40001 |
| 1000003 | 10000 | 1000003 | 0/5 | 3 |

百万行1000档仍漏掉罕见键并触发全扫描，否定“十万行有效就可以直接提交1000调优迁移”。
10000档在该规模采全表，仍不能证明更大规模或更复杂分布稳定；热点generic仍先处理百万条索引项/792块。
本次索引随ORM先建、再写入，十万行热点为82块；前序迁移试验写入后建索引的89块不作调优差值比较。
百万行ANALYZE墙钟观测：默认约0.284–1.347秒，1000约2.004–2.022秒，10000约3.457–3.501秒；
含诊断开销且执行顺序固定，不据此作生产延迟承诺或严格性能排名。

`audit/work-fk-statistics-scale-20260912.json`保留原始VERBOSE采样记录、计划、统计值、耗时与执行身份。
该实验是父DELETE与极端热点/物理末端稀疏分布，不代替UPDATE、业务运行身份、真实生产分布或Linux执行。
统计目标没有提交到生产，保留策略也未改变。后续应将统计成本/规模和热点启动成本一起评估，
不通过写死n_distinct、强制planner或删除业务历史来掩盖风险；G2继续开放。

## 候选Linux CI范围

0d9fee6 / Actions34688447565于2026-09-12 10:40:07UTC全部11作业success，
382文件4464单元零跳过（2262+2202）。直接读取已完成作业的新鲜日志确认：Linux PG16.14的wcao
五个默认auto样本有三个无引用全扫描，证明风险不只存在于Windows本机。
详见`audit/work-fk-nested-ci-20260912.json`。该运行不含6615ceb容量实验和后续15目标采集增量。

10e7c63的Actions34687629456于2026-09-12 10:23:31UTC全部11作业success。
两个单元分片精确覆盖382文件、4464项零跳过（2262+2202）；PG目录、运行时、五端与密钥扫描通过。
详见`audit/work-fk-capacity-ci-20260912.json`。这只证明10e7c63中的统计隔离和热点多租户用例，
不覆盖之后ea1e91f的内部计划捕获及本轮默认缓存代码；不得把前序绿灯当作当前HEAD绿灯。

## 其余15目标内部计划补齐（2026-09-12）

公共采集器`test/helpers/foreignKeyNestedPlans.ts`为每种模式新建独立PG16连接，
默认auto不覆盖设置，先六次热点热身，再检查两租户热点/稀疏/无引用父键；DELETE随后UPDATE共用连接，
后者不是独立冷启动测试。真实父动作以保存点及外层事务回滚，引用拒绝精确校验23503和目标约束名。
按目标表与全部子键列筛选内部查询，避免把别的FK查询或外层触发器记录当作目标计划。

完整两文件28项全部通过/零失败/零跳过（24项文件448.70秒，4项文件70.51秒，两个文件并行，时间不可相加为墙钟）。
15个新目标合计654份内部计划，492次引用拒绝、162次无引用成功回滚，父子行、相关外键/索引/列统计保持。
12个六列引用均只用已有窄事件索引，三个可空引用均只用已有部分索引；实际块读取2–5、过滤0，未新增索引。
auto并非每个目标都会选择generic，采集器如实记录模式选择，不为观察到generic而强制设置。

大段工具输出曾截断四个目标的详细日志；在完整28项已终态通过之后，仅补录四个目标，
4项通过、20项按筛选排除。首次筛选未匹配任何项（参数化名称带引号），已修正且不作为验收证据。
原28项零跳过报告独立保留，补录结果不伪装成第二次完整通过。
双类型最终通过；辅助数组只修正被擦除的类型注解，前后编译JS一致。随机库/schema/角色均零残留。

`audit/work-fk-all-nested-ri-20260912.json`逐目标标注原运行或补录来源，654条记录引用114份去重完整计划；
写入前已验证逐记录无损恢复。加上此前wcao，16个待决索引目标均已有本机真实RI证据。
上文“其余15目标计划待补”为阶段历史，由本节按范围更新；不扩大为所有45个企业微信FK的全负载证明。
wcao缓存/统计/容量风险、实际运行身份和业务保留策略仍未解决，新增代码及Linux容量步骤仍需自身CI。

## 前序验收与仍需完成

2026-09-12本机PG16.15实际执行：原12项加新12项全部通过，431.38秒，双类型通过；
新用例合计96次父端拒绝、48次无引用操作回滚，合法选择性探针2–4块，错误租户过滤50000行但返回0。
父子数据、索引/约束/统计目录未变，随机库/schema/角色零残留。
详见`audit/work-fk-hot-multitenant-20260912.json`。这些结果不关闭下列剩余项。

执行结果以本轮JSON审计证据为准，测试代码本身不证明已经通过。
`selectiveProbesUnder50`只是观察值，不是G2自动勾选规则；需要分别判断合法父键与错误租户组合。
现有wcao默认统计五次中三次全扫描问题仍开放。任何统计或保留策略必须独立论证，
不能把测试中的STATISTICS 1000诊断提交到生产。生产规模、运行身份、真实嵌套触发器计划、
保留策略和本轮候选自身Linux CI均不得由局部测试替代。

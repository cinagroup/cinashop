# 跨请求确认报价审计（FE-003L-A3k11c，未完成）

2026-09-09；被审计业务候选 `450b1bdb6d9aac9e6aa4df7098a63f5ad46f173d`。本项不等同于 A3k11b 的地址权威校验，也不等同于单个创建事务内部的快照守卫。

## 原始缺口证据（450b1bd，保留修复前结果）

在 `workers-ts` 目录运行 `npx --no-install tsx scripts/audit-checkout-confirmation.ts`。脚本拒绝存在 `TEST_FINANCE_POSTGRES_URL` 的环境，使用可丢弃的 PGlite；实际挂载确认与创建控制器、实际报价/建单核心和 SQL。身份、KV、订单号为替身，所有外网 fetch 禁止，不挂载或调用付款。每个场景独立夹具，finally 关闭。

原始版本是**缺口探针，不是通过的回归测试**。2026-09-09 修复前实际退出 1，10 个场景中 2 个对照通过、8 个缺口复现、0 个未知拒绝、0 次网络请求。脚本未加入 `test:unit`、未改动现有 CI 门槛；正常路径断言失败或非业务错误不能被当作修复成功。下文单独记录候选修复后的证据，不覆盖此历史结果。

| 两次 HTTP 请求之间的变化 | 确认应付 | 当前实际创建结果 |
| --- | ---: | --- |
| 不变（正向对照） | 26.00 | 26.00，一单 |
| 运费规则 6 → 9 | 26.00 | 29.00，一单 |
| SKU 单价 10 → 12 | 26.00 | 30.00，一单 |
| 同购物车数量 2 → 3 | 26.00 | 37.00，三件 |
| 相同地址 ID 的详细地址改变 | 26.00 | 26.00，但持久化不同地址 |
| 未重新报价，提交另一个自有地址 | 26.00 | 32.00，不同地址 |
| 同一个 key，提交另一条自有购物车 | 26.00 | 16.00，一件 |
| 未签发过的 key，显式提交 cartIds | 无确认 | 26.00，一单 |
| 删除确认缓存后显式提交 cartIds | 26.00 | 26.00，一单 |
| 已建订单后缓存过期、地址删除（幂等对照） | 26.00 | 返回原订单，不新增、不再扣库存 |

这里的变化仅发生在本地合成数据。金额均由现有服务端重新计算，**不是允许客户端任意指定金额**；缺失的是“用户已确认该版本”这一约束。快照表具有实际列类型与主键，但不代表完整生产外键、多连接、Hyperdrive、真实登录或支付验收。

## 原始版本已核实的代码边界

- `LegacyOrderCompatibilityService.rememberCheckout` 仅存 `{cartIds}`；`checkoutPreview` 的 computed 复用原 key，不保存每次展示的报价版本。
- `OrderController.orderCreate` 在请求显式包含 cartIds 时直接解析，不读取确认缓存。服务端仍校验用户和购物车所有权，但不能据此证明该购物车已被当前 key 确认。
- `StoreOrderCreateService` 创建时重新计价；事务内首单资格/积分可再次调整最终金额。只在控制器重新报价一次并比较，仍不能保证实际落库金额与确认一致。
- `view/common/checkoutQuote.ts` 的 fingerprint、PC 的冻结 payload、UniApp 的提交 journal 都是前端状态保护，不是服务端可信报价证明。
- 两端目前只对精确 `ORDER_FORM_REJECTED` 开放明确未建单后的编辑恢复。直接增加通用报价拒绝会使用户被当作“结果未知”锁住；必须配套明确错误合同与结果核验。
- `AdminAssistedOrderService` 已校验操作者/游客/购物车快照，但快照同样不含报价、地址或运费版本。本轮仅源码核实此点，尚未执行其跨请求变化探针，不能把用户端 8 项证据外推为代客已验。

## 完成此项的必要合同

1. 服务端签发可校验的、带有效期和版本的确认凭据，绑定用户/操作者、幂等 key、精确购物车与数量/SKU/活动、地址内容、配送/门店、优惠选项和规则版本。金额按整数分、积分按整数绑定，不能接受客户端自报金额作为权威。
2. 区分稳定的建单幂等 key 与不可变的报价版本；同一结算页改地址、多个页面或乱序 computed 不能覆盖另一响应所指向的确认。若使用 KV，不假设跨地域读取立即一致，不用最后一次可见写入冒充用户收到的版本；存储方案须按实际一致性保证设计，不能只增加客户端比较。
3. 普通、活动、套餐和代客实际入口统一接入；显式 cartIds、无缓存、旧版本或未知 key 不得绕过。允许合法的无地址预览、虚拟免物流和独立自提联系人。
4. 创建事务在真实最终金额确定后、提交前校验确认，变化则整笔回滚；不能只比较创建请求早期金额。保留现有地区/运费/库存守卫，不为跨请求合同长期持有数据库锁。
5. 未建单且明确需要重新确认时返回可识别的 `ORDER_QUOTE_RECONFIRM_REQUIRED` 与精确 orderKey；PC/UniApp 可显式获取新报价，但不能静默改价或自动付款。曾发生超时/未知结果的提交先核验原结果，不能仅凭后续一次拒绝清除旧意图或换 key。
6. 已存在的同所有者订单优先幂等返回；确认过期、地址删除、规则失效不能让重放创建第二单。另补跨账号、过期、篡改、支付入口未触发、失败回滚、多连接竞争与两端浏览器测试。

砍价原参与应锁快照还是跟随活动现价，仍由 A3i 的产品决定约束。本项可以拒绝未确认的新报价，**不能趁机改动价格公式**。A3k11d 的全局配置 SQL 权威/缓存一致性仍是独立缺口，签发凭据本身不解决陈旧配置读。

## 2026-09-09 候选实现与本地验收（基线 c811c07，未完成）

- 新增 `CheckoutConfirmation`：服务端生成随机不透明凭据，KV 每个版本独立键保存身份、订单 key、显式报价事实 SHA-256 和 30 分钟有效期；不保存明文地址或客户端价格。不是签名/HMAC，也不以 KV 消费标记代替数据库幂等。不可见、未知、损坏及过期凭据失败关闭，基础设施异常保留未知结果语义。
- 普通/代客实际 `createOrder` 适配器强制校验凭据，confirm/computed 返回 `quoteToken`；显式 cartIds 不能绕过。已存在的同范围订单仍优先返回。初始校验在取号和事务之前，最终首单/积分金额及事务提交前再次校验。数据库事务内不读取凭据 KV。
- PC/UniApp 保存并提交所展示报价的凭据；首次精确报价拒绝允许同 key 重新报价，必须再次点击才提交。此前发生超时的意图不能因后续拒绝而清除。旧未知结果 journal 仍可重放原 key，不凭空改造为新订单。
- 最终定向 5 文件 **107 项通过、0 失败/跳过**：凭据隔离、损坏/过期、同 key 不可变版本、运费规则同金额变化、地址删除、元数据/充足库存变化容忍、最终首单/积分变化回滚，以及实际路由/角色与代客正向路径。PGlite 证据不等于多连接 PostgreSQL。
- PC `build` 成功，含 98 项会话/结算与 7 项图片测试；UniApp 188 项工具链、类型检查、H5/微信/App 三端构建及 3 项产物检查成功。两端新增的精确拒绝恢复与“超时后仍冻结原意图”测试通过。
- Playwright + 本机 Chrome：PC 1280×900 与 UniApp H5 390×844，在仅 loopback 的真实本地页面各验证配送、自提、虚拟商品，共 **6 条流程通过**。配送确认 26 元后本地运费改为 9 元，首次创建明确拒绝且订单/库存/购物车/用户/流水快照不变，页面显示 29 元；同 key 换新凭据后明确二次提交才生成一笔未支付订单。自提/虚拟商品 20 元、无配送地址。全流程无付款请求。
- 页面身份、非空、无框架报错覆盖层、无横向溢出和截图检查通过。结算页无 console error；UniApp 有既有 vue-router 弃用警告，提交后详情桩页出现 `orderId` 非 props 警告，详情页不在验收范围。Browser 专属 skill 未暴露，按前端测试技能使用已有 Playwright，无插件或依赖安装。
- 隔离 API 5228、PC 5229、H5 5230 验收后已停止。截图在本机临时目录 `cinashop-confirmation-{pc,uni}-{delivery,pickup,virtual}.png`，未放入仓库。身份、KV、订单号及优惠券空列表为替身；没有接入生产或支付。

专用 Windows PG16.15 追加定向 5 文件 **97 项通过、0 失败/跳过**（与前述107项范围重叠，不能相加）：其中新增2项以四个独立且不重连的后端和 `pg_blocking_pids` 证明同key不同有效凭据并发只扣一次库存/创建一单，以及等待准入锁期间过期后全快照回滚。其余含纯凭据单元及真实SQL/路由测试，不是97项并发测试。

最终手动探针 `node --import tsx scripts/audit-checkout-confirmation.ts` 退出0：8项精确拒绝及快照不变、2项正常/幂等对照，0未知拒绝、0外网请求。输出字段改为 `probeInvariantsPassed`，只表示这10项，不冒充完整合同。新增PG文件后的Worker双类型检查也通过。专用PG于09:51:54 UTC正常停止（原PID6860），停止前4个基线数据库、0夹具schema、0public表、0其它客户端，保留二进制/数据/报告。当前全量unit启动时尚未加入新增PG文件，最终统计必须另核对文件覆盖及跳过，不能称为最新完整候选已验。

仍待完成：活动/套餐/优惠券语义规则版本的完整事实绑定与跨请求变化矩阵、候选全量回归和自身 CI、正式登录/Redis/KV/Hyperdrive 的可用性及发布门禁。c811c07 的 Actions34334326168 已成功，但它仅覆盖修复前的审计提交，不能作为本候选 CI。A3k11c 保持开放。

## 语义规则扩展（2026-09-09，本候选追加）

先新增实际confirm→create红测：优惠券门槛0→1、套餐允许退款1→0、砍价配送1,2→1，均仍符合购买资格且金额不变，但旧凭据仍成功建单，3个缺口和3个正常/幂等对照得到确认。候选补入优惠券金额类型/门槛/有效期/规范化适用范围与匹配商品事实、套餐退款/限量开关/有效期/完整成员规则、砍价配送/有效期等字段；不绑定标题、排序或剩余库存。优惠券范围按数值集合排序，避免仅关系顺序不同就失效；此规范化仍需更多等价表达专测。

进一步绑定秒杀父/子/时段排期与限购、拼团人数/有效时长/限购、积分限购、新人资格配置、表单ID及商品退款规则，保持既有定价公式。`checkout-confirmation-rules.test.ts` 最终 **28/28通过**：七类各有正常建单+过期凭据幂等、无关元数据/充足库存容忍、两种同价规则变化拒绝+显式同key重取后成功。前三类相关5文件83项通过；后续扩展首次因夹具SKU unique超出真实char(8)限制发生16个setup失败，修正为8字符后七类28项全部通过。不能把setup错误称为已复现的业务缺口；前三类第二轮的3个失败则是测试误读computed的扁平pay_price，已按真实合同修正，未改控制器返回形状。

**未完成边界仍须保留**：28项证明的是两次HTTP之间的规则绑定，不证明规则在create请求内部的最后提交期全部重新读取。当前最终指纹会反映首单/积分的最终金额，但部分活动与优惠券规则对象仍来自初始读取；套餐既有锁后复核也仅比较部分字段。后续须在现有锁/原子写入边界核验这些事实，补复杂券范围、等价表达、活动排期/成员规则和独立PG编辑竞争，不新增跨请求长事务。新人/全局KV配置权威一致性仍属A3k11d。没有把有限矩阵命名为完整业务验收。

前轮全量unit进程仍是诊断运行：启动后增加了两个测试文件并继续修改了候选源文件，因此无论终态为何，不能当作当前不可变提交的完整验收。待提交后以自身Linux原生分片覆盖校验和终态CI为准；不修改既有零跳过门禁。

提交前最终专用PG16.15回归：`checkout-confirmation*`、`order-delivery-address.test.ts`、`pc-checkout-quote*`、券范围/选择、套餐、代客、砍价配送共 **11文件206项全部通过，0失败/跳过**，含七类28项和独立连接2项。最终Worker双类型通过。PG停止前再次核验角色/版本，4个基线库、0夹具schema、0public表、0其它客户端；已正常停止并保留数据目录。上述范围有重叠，不累加旧轮统计；不包含完整生产登录、真实KV或支付。

## 授权推送与已领券原子占用增量（2026-09-09，f58f617之后的本地候选）

f58f617经明确授权推送main，自身Actions34339592124最终失败。两个完整单元分片各174文件，分别1709通过/1失败、1553通过/1失败；失败是旧UniApp报价替身和砍价首次创建请求缺少新增凭据。二者本地复现为35通过/2失败；现更新正向夹具，并为UniApp补5项缺失/畸形凭据拒绝，砍价重绑定反例也携带有效原凭据，避免被无凭据拒绝掩盖。没有改动生产合同、跳过失败或降低CI门槛。五端、workerd、密钥扫描和PG目录任务通过，不替代失败的单元/汇总门禁。

本轮将独立手动探针扩至14场景，在真实创建核心已经通过凭据检查之后，通过合成Sequence边界修改本地SQL。初轮11项正常/既有守卫通过，以下3项按原确认25元成功建单，证明请求内部未重新核验已领券：

| 已领券的晚到变化 | 初轮结果 | 候选结果 |
| --- | --- | --- |
| 门槛0→1，仍符合且优惠不变 | 创建一单、占券status=3 | 精确重确认拒绝、保留编辑、业务写入回滚 |
| 门槛0→999，已不符合 | 仍按原优惠创建一单 | 同上 |
| isFail 0→1 | 失效券仍被占用并创建一单 | 同上 |

候选占券UPDATE增加isFail=0与最初有效券的issueCouponId/couponPrice/useMinPrice/startTime/endTime守卫；日期使用IS NOT DISTINCT FROM兼容null，数值按实际numeric列比较。原有行锁持续至事务结束，不新增锁顺序，也不把标题/领取来源等无关字段绑入规则。取得占券锁并完成其余可能阻塞的守卫后，再检查数据库clock_timestamp的UTC墙钟与券有效期；这样SELECT FOR UPDATE持锁但不改元组的等待也不能沿用等待前的时间。数据库异常不改装为业务成功或确定未建单。

证据分层：

- 请求内字段矩阵：7项晚到变化（门槛、变为不合格、失效、金额、模板引用、开始/结束时间）红测全失败；带起止日期的晚到标题编辑保持成功。原有七类28项不变，扩为36项。
- 独立PG16：新增3项规则写锁竞争均先红测复现，另有标题写锁正向对照与数据库时钟过期。每次验证4个独立且不重连的后端，精确pg_blocking_pids确认等待。首轮过期用例以及第一轮修复后复测实际触发生产2秒锁超时，不算业务放行或精确拒绝证据；改夹具窗口2.5→1.5秒后通过，不改生产超时。
- 最终专项5文件98项全部通过、零失败/跳过；其中独立PG文件7项（原有2项+本轮5项）。手动14场景退出0：3正常/幂等对照、11精确拒绝，0未知拒绝、0外网请求，晚到编辑本身保留、建单业务写入回滚。拒绝发生在取号之后，**不承诺不消耗订单编号或SQL序列**。
- 最终关联回归在专用PG16.15执行14文件266项零失败/跳过，Worker双类型通过，包含上述98项及两个CI失败文件的修正与追加测试，不重复累加。新增修复尚未推送，自身CI待验。旧PGlite全量诊断346文件3037通过/6失败/191跳过，且运行时源代码变化，不能作为本候选验收。另4项PGlite日期/多语句触发器失败已保留，兼容性原因待进一步核实。

剩余范围：券模板本体与商品关系/分类品牌祖先的编辑和集合幻读、活动/套餐的提交期规则核验、全局KV配置权威、正式身份/存储/Hyperdrive及发布验收。当前只保护所选已领券行，不能因此关闭A3k11c，也不改A3i砍价产品决策。

临时专用PG16.15于10:40:24 UTC正常停止；停止前身份、4个基线数据库、零夹具schema/public表/其它客户端已核验。保留二进制、数据目录和报告，不涉及生产实例。

## 套餐规则与秒杀排期的锁后版本校验（2026-09-09，基线96d7a71）

本轮未获新的main推送批准，96d7a71继续只在本地。继续审计发现，套餐虽然再次锁定父行、成员、商品和SKU，但只比较type/freeShipping/isSupportRefund及已选SKU身份/单价；秒杀虽然锁定并重读父活动、子活动和时段，但只验证新排期当前仍可购买。这些检查不能证明用户确认了相同规则版本。

修复前PGlite实际HTTP测试36通过/4失败：套餐stopTime、成员必选标记以及秒杀stopTime、时段成员集合在初始凭据检查后改变，仍成功创建，金额不变。随后专用PG16对相同4种变化分别加入独立写锁竞争及2项无关元数据对照，46项中38通过/8失败，0跳过；8失败为4项请求内变化和4项精确等待后仍放行，不是setup/SQL异常。每次并发测试核验4个独立且不重连的后端以及pg_blocking_pids的真实阻塞关系。

候选只在原有两处锁定快照读之后增加比较：套餐复用签发时的完整confirmationRules投影，包含未选成员规则；秒杀比较已锁定父/子/时段的完整排期投影。两者均与此次请求最初已通过凭据验证的事实比较，变化则抛出ORDER_QUOTE_RECONFIRM_REQUIRED；不额外请求KV、重算用户价格、不改变锁顺序、库存和幂等流程。首次关联11文件328项零失败/跳过，Worker双类型通过。

随后补充任意搭配套餐新增未选可选成员与秒杀父活动排期修改2项PG情况；并将回滚预期固定为编辑者事务内读出的快照，不能用建单后的整张活动表覆盖预期，避免漏掉意外库存/额度变化。最终专用PG16.15扩大回归21文件518项零失败/跳过，Worker双类型通过；确认规则文件48项中8项为本文件独立PG竞争。扩展首轮514通过/4失败来自父活动默认status=0，在确认阶段就拒绝了新夹具，显式status=1后通过，不能计作新增业务漏洞。套餐新增成员测试模拟遵守父锁的编辑者，不冒充实际后台HTTP/完整FK约束验收。

剩余边界具体化：

- 套餐：AdminDiscountPackageService.save的现有成员增删先锁父行，本轮沿用该边界；不遵守父锁的原始SQL新增成员仍缺少数据库级集合保护。套餐截止时间随真实墙钟推进、等锁前now复用，尚需最终数据库时钟准入测试。
- 秒杀：本轮比较已读取并锁定的排期行，不是所有规则。seckillRuleQuoteGuard已有单次/累计限购等，但deliveryType/isSupportRefund尚未完整纳入最终写入守卫；等价时段字符串/ID集合表示的规范化也仍待测试。loadSlots允许部分引用ID缺行时由其余有效时段继续准入，行锁不能保护不存在的时段被新增，这一集合完整性边界仍待验证/处理。锁住数据不表示冻结真实时间。
- 拼团/积分/砍价：确认事实已绑定，但部分提交期事实仍来自初读；须在各自已有锁或原子UPDATE边界补全，保持砍价A3i价格政策不变。
- 优惠券：96d7a71只保护已领券行；模板类型、适用关系、分类品牌祖先与集合幻读仍开放。全局配置一致性仍归A3k11d。
- 验收：新候选完整不可变CI、正式身份/KV/Redis/Hyperdrive及发布仍未完成，A3k11c保持开放，不将本轮518项外推为整体迁移完成。

专用PG16.15在核验4个基线数据库、零夹具schema/public表/其它客户端后，于11:00:55 UTC正常停止。二进制、数据目录和报告保留。

## 活动标量规则的原子预占校验（2026-09-09，基线1ca3d3d）

在专用PG16.15上新增20项实际SQL测试，旧48项保持不变。修复前68项中52通过/16失败、0跳过；失败均是在已验证凭据后改规则，仍按原价建单：8项由实际HTTP创建核心的合成Sequence边界触发已提交编辑，8项由独立SQL编辑者持有真实行锁、买方实际等待后提交编辑。每类另有1项排序/标题写锁对照通过。独立连接沿用4个不重连后端身份核验和精确pg_blocking_pids屏障，不以sleep或替身断言冒充竞争。

新增通用activityRuleQuoteGuard仅接受显式标量投影，在现有原子UPDATE的WHERE内做IS NOT DISTINCT FROM比较。Date以ISO字符串交给驱动，null不是省略条件；空投影或未映射列失败关闭。沿用原有锁顺序/库存数量守卫，未增加数据库往返、跨请求事务或事务内KV/provider调用。确认凭据存在时，活动预占不匹配返回ORDER_QUOTE_RECONFIRM_REQUIRED；旧纯核心无凭据测试仍保留原ValidateException。未改价格公式、库存算法及幂等重放顺序。

| 活动 | 纳入现有预占UPDATE的规则字段 | 本轮请求内与独立写锁反例 |
| --- | --- | --- |
| 砍价 | deliveryType、startTime、stopTime、num、isSupportRefund、systemFormId、giveIntegral | num 1→9、stopTime延期 |
| 拼团 | people、effectiveTime、onceNum、num、startTime、stopTime、deliveryType、systemFormId、isSupportRefund | people 2→3、effectiveTime 3600→7200 |
| 积分 | onceNum、num、deliveryType、systemFormId | onceNum 3→4、num 8→9 |
| 秒杀 | 在原有守卫上补deliveryType、isSupportRefund | 配送集合缩小但当前配送仍合法、退款1→0 |

这张表列出代码保护的投影，不宣称每个字段均已逐一执行编辑矩阵。反例保持订单价格相同；HTTP拒绝后，购物车/订单明细/库存/用户/账单及活动状态必须等于编辑者已提交的预期快照，随后同key显式重取凭据与二次提交成功。独立PG预期快照在编辑者事务内、买方写入前固定，只保留编辑本身，不用建单后的数据反向构造预期。整笔回滚不承诺订单取号或SQL序列没有消耗。

原68项绿测全通过后，另补4项独立库存/额度/销量编辑正向对照：stock/quota改为9、sales改为1，旧凭据仍成功建一笔未支付订单，最终准确为8/8/2；加3项SQL投影编码/空投影/缺列单元检查。最终扩大回归30文件622项全部通过、0失败/跳过，Worker unit/runtime双类型通过。规则文件72项中24项是本文件独立PG竞争（本轮新增16项），不是全部622项都是SQL竞争。覆盖优惠券、套餐、七类确认、代客、地址、秒杀、砍价下单/绑定/库存补偿、积分及拼团取消/付款准入/退款锁序；不与68项或此前518项累加。报告摘要和可重跑文件清单见../audit/activity-scalar-rule-boundary-20260909.json。

尚未关闭的边界：

- 标量相等不代表时间被冻结。拼团仍用NOW()、套餐仍传递等锁前now，秒杀及其它时限在后续阻塞点的最终自然截止需要独立验证；本轮没有改变这些准入政策。
- 其它初读计价事实：部分非秒杀活动SKU价格/积分及普通基础SKU价格没有同等的最后原子比较；表单ID不等于表单内容版本。已有主商品/规格/配送守卫按各自分支生效，不能泛化为所有事实都稳定。
- 套餐非协作SQL新增成员、秒杀不存在的引用时段新增、券模板/适用关系及分类品牌祖先的集合完整性、等价规则表达仍待验。
- A3i砍价价格政策、A3k11d全局SQL/KV配置、正式登录/KV/Redis/Hyperdrive、候选完整不可变CI与发布门禁不在本轮完成范围。

推送状态：用户回复“授权”后，精确1ca3d3d→origin/main仍被安全审批拒绝；没有绕过或再次推送。origin/main仍为f58f617，新候选仅本地，不能沿用旧CI结果。

专用实例身份为cinashop_finance_test/finance_test、PostgreSQL160015、UTC；停止前确认仅4个基线数据库、0夹具schema、0public表、0其它客户端。11:22:19 UTC正常停止，保留二进制、数据与报告。认证/KV/Sequence为本地替身，未调用外部支付、写生产或执行部署。

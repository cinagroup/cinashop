# 代客购买运行身份验收

## 最新增量：代客支付回调与查单并发栅栏（2026-09-24，本地候选／未部署）

代客微信／支付宝发起先锁 `store_order`，再获取按完整订单号派生的 PostgreSQL **事务级** advisory 锁，最后登记对账意图。验签后的回调持同一订单边界，再取得 Provider 交易／案件锁并写入事件；订单行锁后的改价、本地付款和取消必须越过该边界后再读取意图／回调证据。无订单与普通商城订单的验签回调仍持久登记、重复通知仍去重。事务回滚和超时自动释放事务级锁；锁键带独立命名空间，不复用既有 Provider 键。

对账查单成功先在相同 `订单边界 → Provider 交易 → 案件` 顺序中为**已有案件**登记交易号，回调 T1 与查单 T2 不同则在结算前标记冲突，保留先前事件及交易身份。商城、充值、会员的实际付款事务先锁各自订单行，再持订单边界和案件行锁复核 lease、交易号、回调事件及订单域，直到付款提交；`finish` 重新读案并保留中途提交的回调证据。人工 `ACCEPT_LOCAL` 已先持案件行锁，因此三类订单的后续行锁均用 `NOWAIT`，遇并发更新立即返回可重试错误，避免反向等待。此处对未知结果仍须走对账／人工处理，没有自动重发 Provider 请求。

原生隔离 PG16.15 最终验证：代客完整流程 **49/49**、针对性查单／回调与人工处置 **4/4**、对账会员及三域 `NOWAIT` **35/35**、离线回调 **33/33**；各最终集群夹具余留 0 且已停机。针对性用例实际观测 `pg_blocking_pids` 与 advisory 等待：回调 T1 先提交、T2 先登记后 T1 持锁未提交、人工案件先锁而订单行被占用，均未改变本地 `paid` 或付款流水；无冲突的 T2 则只入账一次并可重放。阶段性 47/49 是测试把 PG `bigint` 回调 ID 误当 `number`，修正仅限小于 2³¹ 夹具投影后 49/49；对账 31/33 暴露初版栅栏额外要求案件 `INSERT` 权限，现改为锁定更新既有案件并复跑 35/35。一次完整流程 `initdb` 在建库同步阶段超时、未进入 Vitest，独立核实无服务器、PID 或监听；另起全新集群完成 49/49，不将其计为业务通过。

现有线下订单保留精确 `^xx[0-9a-f]{30}$` 号段，其回调原有锁序会反向触及 `other_order`，所以该号段不获取本商城边界；本机签名回调专项验证该分域。商城新单为 `wx` 加十进制雪花 ID。未知历史迁移订单号是否越过保留域、真实微信／支付宝回调验签链、Provider 结果未知恢复、旧客户端原位升级与生产渠道证据仍待真实审计，FE-003G 和发布门禁保持开放。本增量没有生产连接、提交、推送或部署。

## 前序增量：代客扫码一次性发起与支付证据守卫（2026-09-23，本地候选／未部署）

微信／支付宝代客支付在配置与签名等本地确定性校验后、真正外呼或交付支付宝签名 URL 前，先在短事务持久登记一次发起意图，再锁订单行核对本人代客归属、未付状态和界面提交的整数分金额。事务提交并释放 SQL 锁后才接触 Provider。重复提交同一订单／金额、金额被并发改动、已存在冲突或另一渠道已提交的支付证据均拒绝外呼；普通会员入口若支付代客订单也进入同一声明，微信保留实际 `wechat/routine/app` profile。支付宝兼容 `pay_key` 缓存第一次已签名的 URL，五分钟跳转读取不重新声明或签发。结果未知不会自动重试；`NO_PAYMENT` 后重发也须另建可审计协议。

订单行锁后的改价、余额／现金／后台线下收款、用户取消及定时取消会检查已发起意图或已验证回调的事件／交易号证据。定时扫描排除待对账订单，单条任务若竞争中发现证据则记为有意跳过，避免把未付超时当作可安全释放库存。最终原生 PG16.15 **43/43**、夹具余留 0、隔离集群停机；相关 Vitest 六文件 **34/34**、另有 Kefu 迁移文件 **10/10**、Worker unit/runtime 类型及空白检查通过；Kefu 初跑 9/10 是内嵌 0103 SQL 的 CRLF 与外部 LF 字节不一致，已仅规范该段换行并复跑通过。上述 Provider 响应是合成的；真实微信／支付宝、签名回调和未知结果恢复未验收。**已知剩余竞态**：历史／旁路 callback-first 事务若尚未提交，跨渠道声明或本地订单变更的 `READ COMMITTED` 查询可能看不到其证据；回调注册与订单变更需要共享订单粒度序列化后，才能宣布该交错安全。FE-003G 与发布门禁继续开放，无生产连接、提交、推送或部署。

## 前序增量：收银浏览器复验与原位升级预检（2026-09-23，本地候选／未部署）

本会话的应用内浏览器控制已恢复。在本机 `127.0.0.1:5179` 隔离服务上运行上轮最终 H5 构建，并用合成 Admin 登录、本人两笔订单及付款响应验证实际页面：记录→收银只读加载；¥12.34 扫码需显式点击，才发起 1 次带 `expected_pay_price=12.34` 的 POST 并绘制二维码，二维码期间发起按钮带禁用属性，状态读取不会自动重新发起；¥0.00 订单的二次确认取消后付款 POST 为 0，确认后为 1 次、携带 `expected_pay_price=0.00`，页面显示已付且不生成二维码。桌面与 390×844 移动视口已查看截图，移动文档宽度未超过可视宽度；控制台 0 error、1 条既有 Vue Router 弃用 warning。测试中一次跳会员登录由隔离服务误将公开悬浮导航接口返回为 410001 引起，修正服务为普通 404 后记录与收银导航稳定通过，并未改产品代码。预览进程和标签已关闭；这只验证合成 API／前端渲染，不证明真实管理员认证、订单 SQL、Provider、回调、外发请求审计或真机行为。

新增显式运行的 `view/uniapp-ts/scripts/release-upgrade-preflight.cjs` 与 **8 项测试**：要求从渠道及候选制品独立记录微信／DCloud 标识、Android 包名、iOS Bundle ID、签名 SHA-256、最高已发布版本、旧新 API Origin、候选制品摘要及原位升级资格证据引用；只比较元数据中可安全静态核对的身份和版本，编译资源仅检查 API 地址字面量存在。缺证据时脚本失败；现有空 AppID、`touristappid` 也会失败。Google Play 的上传证书可能不同于设备安装包的签名证书，[Android 官方说明](https://developer.android.com/studio/publish/app-signing)还允许受控密钥升级；[Apple 官方说明](https://developer.apple.com/documentation/xcode/using-the-latest-code-signature-format)指出 App Store Connect 会重新签名，因此预检不机械要求新旧指纹相等。它不能自行验签、确认实际 API 请求去向、读取线上渠道或证明已安装旧包能收到更新，真实发布门禁仍开放。无生产连接、提交、推送或部署。

## 前序增量：代客收银页面与旧客户端门禁（2026-09-23，本地候选／未部署）

所有者已确认旧版微信小程序和 App 均有线上用户，要求原位升级。新 manifest 的空标识和较低版本不能按新应用发布来替代；真实线上 AppID、包身份、签名与最高版本仍须渠道核对，本地仅能把这项保留为发布硬门禁。

旧 `/pages/behalf/cashier/index` 已注册为 UniApp 页面；本人记录和只读详情可进入。页面使用独立 Admin 会话，先读付款状态和最小详情，现金、零元完成及扫码均须管理员显式点击；零元代客沿用 PHP `CreateOrder.php` 的结算行为，但另加确认，不套用仅针对 type=3 线下消费的 0.01 元规则。所有付款 POST 携带界面核对的 `expected_pay_price`。二维码只在本机绘制，使用安静区及整数像素格，避免高密度二维码的白格被相邻黑格覆盖；隐藏或切换身份会清除内容。本地定点 15/15、UniApp 工具链 693/693、类型、H5／微信小程序／App 构建与产物 3/3 通过。路由账本现为目标 72、旧路由直接注册 11、兼容映射 98、未映射／跨界面 42，旧 151 条仍完整记账。

服务端所有代客付款要求格式正确的 `expected_pay_price`；现金／零元在真实订单行锁后的授权钩子再次按整数分比较，不匹配则整笔拒绝，付款和 outbox 均不写。原生 PG16 完整 flow 首轮 37/39：两例新增并发测试在 HTTP 入锁前轮询超时；修正测试同步钩子后，独立集群复跑 **39/39**、夹具余留 0 并确认停机，未把首轮红测计绿。Worker 双类型及金额 parser 单测 5/5 通过。Provider 路径仍会重新读单：对预期金额的入口检查并非发起时原子保证，已有对账意图即使处于 CONFLICT 也未阻止外呼，同金额重试仍可能重复发起；外部真实支付、回调及结果未知重试属于发布阻断，不能把现金 PG 通过推广至扫码安全。真实管理员登录与真机渲染也未验收。根会话浏览器与电脑操作入口重置后仍在内核初始化报 `failed to write kernel assets … os error 3`；独立子会话 IAB 在本地最终 H5 构建完成桌面与 390px 移动匿名收银守卫→独立管理员登录导航，控制台无 error、仅一条既有 Vue Router 弃用警告，移动无水平溢出；浏览器安全检查拒绝网络请求审计，故未证明无异常外发。未用外部 Playwright，也未登录或触发支付；预览端口已停止。FE-003G 仍因反馈／Work、平台与支付边界开放，清单维持 240／164／404；无生产连接、提交推送部署。

旧 PHP UniApp 的记录页仍期待 `page/limit` 数组和已删除的敏感整行字段；当前公开接口只在请求实际到达新 Worker 时给升级提示。旧源码 App 版本 `3.1.1(311)`、DCloud 与微信 AppID 有值；新 manifest 为 `0.1.0(100)`、两个标识为空。旧小程序有更新管理器，旧 App／H5 未见统一拦截，新包无更新流程。已发行包的实际 API 目的地、包身份／签名及渠道状态未核实，不能把接口门禁等同整包强制升级。发布前须核验旧包实物和渠道、配置可原位升级的标识与递增版本、发布新版并用旧／新包实测；这些属于待决发布门禁。

## 前序增量：代客订单游标列表与有界索引安装（2026-09-23，本地候选／未部署）

发布协议修正：公开 `GET /api/admin/order/place/list` 在鉴权后、读取前只接受唯一的 `paging=cursor`；旧 `page/limit`、缺失或重复参数返回 HTTP 400、升级提示和 `private, no-store`，不会调用列表服务。下段所述旧数组合同仅残留于内部服务，不再由公开路由提供；此门禁不能代替 H5／小程序／App 分发层的整包强制升级验收。

`GET /api/admin/order/place/list` 新增显式 `paging=cursor`，完整位置为 `add_time:id`，与 `add_time DESC, id DESC` 排序一致；每页仍按创建管理员、代客渠道、系统／业务删除及所选状态过滤，读取 `limit+1` 判断后续页，只返回最小公开列表字段。无效游标、冲突页码和过深旧 OFFSET 明确拒绝；无 `paging=cursor` 的旧调用仍返回数组，最多跳过 10,000 行。UniApp 记录页消费严格解码的服务器游标和 `has_more`，过滤草稿变化不会继续读取旧条件，失败保留原游标供显式重试。

新增普通复合索引 `so_assisted_actor_list(staff_id,is_channel,is_system_del,is_del,add_time DESC,id DESC)`，登记为外部 0165、内嵌 0171 及 ORM 目录；仅独立维护入口可安装，普通事务内建索引在目标表超过 100,000 行时拒绝，不自动在 Worker 请求中执行，也不允许向已升级目录重放全量初始化。原生 PG16 的同管理员 3 万行／外部 1 万行计划揭示原 OR 游标虽用索引却过滤 10,001 条前缀；现已改为 `(add_time,id)<(游标时间,游标ID)` 行值比较，generic/custom 均将完整二元条件下推 `Index Cond`，该页前缀过滤为 0，局部执行由 6.537／5.524 ms 降至 0.184／0.058 ms。两文件 29/29 通过、0 跳过，临时集群夹具余留 0、停机并只清理本轮目录；20 万无关管理员行的首屏也使用索引。关键词 `ILIKE` 与部分状态过滤在合成计划中仍有扫描／排序，不能据本索引称已解决；上述毫秒数不是生产延时预测。真实生产规模、超过阈值时的在线维护方案、索引生产安装和 Admin／浏览器／Provider 验收均未完成，不据本地候选宣布上线。

所有者选择旧 PHP 版 UniApp 客户端强制升级，不恢复已删除的 `refund/cartInfo` 敏感整行；发布前仍须核验版本拦截、应用分发和旧包实际无法继续使用不兼容列表。FE-003G 与 240 完成／164 开放／404 总项不变；本增量未连接生产、提交推送部署。

## 前序增量：只读订单详情、精确订单号与 Admin 到期清屏（2026-09-23，本地候选／未部署）

新增 `GET /api/admin/order/place/detail/:orderId`，只使用 `order.assisted` 和独立 Admin 身份。原生 SQL 以创建管理员、代客渠道、未删除付款根单及最多 200 条商品行为边界，在短 `REPEATABLE READ READ ONLY` 事务内取最小字段；单条商品 JSON 超过 64 KiB 时不读取该快照，再只输出名称、SKU、数量和价格，不复用广域后台详情或返回收货人、电话、地址、原始表单／购物车／退款对象。列表同步改为明确字段投影，不再将整行及购物车、退款原文交给客户端。已拆分付款根单仅显示原始购买快照，并提示不能代表各履约子单状态。UniApp 新增只读详情页，从本人记录进入；独立 Admin 会话、隐藏／卸载及身份版本变化清除数据，迟到响应不能覆盖新身份，不加收款、取消或退款写操作。Admin 会话自然到期也会清屏；计时器按版本／令牌守卫，新会话不受旧计时器清理，页面恢复可见时同步复核过期。

订单号歧义按旧 PHP 源码区分：`CreateOrder.php` 付款先用原始 `uni` 精确查单，只有生成外部支付单后才加三位前缀，并在状态轮询剥前缀。新版付款写入口、订单表单读取和详情因此只接受最多 32 字符的精确订单号；轮询为兼容旧前缀仍可接收 `NNN_` 加最长 32 字符原号（总长 36），但在同一 SQL 快照中先检查全库精确 ID，即使它属于另一管理员或已软删也不能回落到别名。轮询响应同时返回实际命中 `order_id`，新 UniApp 严格比对请求号，避免展示另一订单的已付状态。旧 PHP 收银只检查 `status/time`，所以服务端碰撞拒绝是必要防线。

会话计时器修改前，本地 IAB 浏览器已实际渲染 H5 匿名详情路由 `/#/pages/behalf/order_detail/index?orderId=ORDER_1` 的独立 Admin 登录守卫，并点击返回记录／登录页；最终计时器源码复验时 IAB 报 `Browser is not available: iab`、浏览器清单为空，未能重复渲染，因此不把前一轮截图和交互冒充最终浏览器验收。最终 UniApp 工具链 **674/674** 通过、无失败／跳过，类型、H5／微信小程序／App 构建及产物 **3/3** 通过；首次受影响测试 184/185 中的旧购物交互断言失败，已在动作入口同步检查到期并将定点 56/56 与全量重跑至绿。微信空 `utils/articleRichText` chunk 为既有非致命提示；本轮预览端口 5175 已停止，未操作另一个占用 5174 的进程。

Worker 路由／权限／前端账本三个静态文件 **21/21** 通过，unit/runtime 类型检查通过。最终代客详情源码在独立 PG16.15 上专项 **33/33** 通过、0 失败／跳过（291.11 秒），含别名碰撞、最长旧轮询号与超长拒绝；既有购买流程在另一全新实例上 **37/37** 通过、0 失败／跳过（321.20 秒）。两实例 `finance-postgres-3k0h2F:65245` 与 `finance-postgres-9oMC0z:63698` 的夹具余留均为 0，独立确认 `pg_ctl` exit 3、无 PID／引导口令／监听。此前严格原号过渡版曾 32/33，唯一“表单变更前上传”用例在 8 秒内报订单过期；同一源码诊断重跑 33/33 通过（301.46 秒），未复现，临时诊断已移除，首轮红测不计绿测。两次最终 PG 文件的 33＋37 条互不重复；历史重跑和早期版本通过数不再相加。

没有真实管理员凭据，不能据此称已完成登录后详情、支付 Provider 或真机验收。旧 PHP 小程序／App 的代客记录页直接依赖整行 `refund/cartInfo`；新最小列表与旧包不兼容。所有者后续选择强制升级，不能恢复敏感整行；实际拦截／分发仍待验收。`store_order` 的 `staff_id` 专用列表索引与游标边界已在上方本地候选补入，尚未安装生产；深旧 OFFSET 被限界，包含式姓名／电话搜索及真实规模计划仍需验收。收银、未使用／结果未知图片生命周期、表单同内容恢复的 ABA 版本语义、真实 Admin／Redis／Hyperdrive／设备及生产发布门禁保持开放，FE-003G 与 240／164／404 不变；本轮未连接生产，也未提交、推送或部署。

## 前序增量：报价绑定表单内容及商品改绑并发守卫（2026-09-23，本地验证／未部署）

代客报价现在将服务端读取的有界启用表单 `id/name/value` 作为确认指纹的事实；不是仅绑定 `system_form_id`、响应 schema 的 `version:1` 或秒级 `update_time`。每张 KV 报价凭证仍只保存最终 SHA-256，不保存表单内容。建单保留既有订单重放优先顺序：首次创建在事务前快检，在原事务以 `FOR SHARE NOWAIT` 锁定模板后、解释答案前再核对；合法的名称／字段标题变动也明确返回 `ORDER_QUOTE_RECONFIRM_REQUIRED`，旧答案不能静默提交。普通非代客报价的投影未变。普通 type=0 代客商品在库存条件更新时额外核对 `system_form_id`，借该行锁封住锁定模板之后、扣库存之前的商品改绑窗口；预售 type=6 原有产品快照守卫已覆盖表单 ID。锁／SQL 故障仍是非确定结果，不伪装成可清除原请求的业务拒绝。

全新本机 PG16.15 临时集群 `finance-postgres-ub82PS`（端口 62707）完成 30/30，修复商品改绑缺口后在另一全新集群 `finance-postgres-9XWkU1`（端口 60756）完成 31/31；最终添加“事务前快检与锁定之间编辑”的用例后，于 `finance-postgres-btf2sV`（端口 58525）完成 **32/32**、耗时 253.38 秒。均是真实独立应用／后台 LOGIN、JWT、SQL 权限、事务回滚及合成 KV/R2/Queue。四项新定点用例分别验证两种原答案仍合法的纯文字编辑、快检后编辑、商品跨连接改绑：旧凭证无业务写，新凭证同 key 成功，库存／积分／订单／收集均回滚。三座成功集群夹具余留 0；停机后独立核验 `pg_ctl` 返回 3、无 PID／引导口令文件及监听。

原 77 项代客历史套件在 `finance-postgres-yuIvpx`／端口 49178 的单文件运行触及既有十分钟上限 `ETIMEDOUT`，没有最终断言数；中断使一个合成夹具数据库残留在已停机诊断目录。未改上限、未删除诊断，独立复核无 PID／引导口令或监听。随后只重组测试注册，不改 77 项断言：同目录共享 `assisted-purchase-runtime-cases.ts`，两个发现入口分别注册表单／图片 40 项和结算／支付／取消／权限 37 项；不是 `testNamePattern` 或 skip。精确 runner 白名单及本地协议测试同步更新，静态发现 40＋37＝77；类型检查通过，运行器单测 111/111。两座全新独立集群 `finance-postgres-Eert63`／端口 58295 和 `finance-postgres-03Mqdc`／端口 53775 分别 **40/40**（297.49 秒）与 **37/37**（282.10 秒）通过，均夹具余留 0，停机后独立确认无 PID／口令／监听。连同专项 32、普通原生 289、相关单元 22，去重为本轮业务／单元 **420 项通过**；另 111 项 runner 边界测试通过，不把 PGlite 重复的 21 项再计入。Worker unit/runtime 类型及空白检查通过。

扩展普通报价回归时，四文件初跑报告 146 失败、54 通过、111 跳过；独立复现的普通结算创建返回 400，底层错误为本地 PGlite 夹具查询缺失的 `public.store_order_purchase_origin`。进一步核对发现正式购买来源和计价锁协议都只在独立 PG16 安装，不能补一张无约束假表冒充建单验收。已将两文件的真实建单测试组限定在 PG16：轻量凭证测试 21/21 通过，268 项明确跳过；在全新 `finance-postgres-pTmgD2`／端口 55520 的正式 PG16 复验 **289/289 通过**、耗时 328.38 秒、夹具余留 0，停机后独立确认无 PID、口令或监听。该普通路径没有 `assisted`，本轮指纹及商品守卫不执行；不把初跑红色或轻量跳过称为通过，也不据此推断生产数据异常。其余模板语义的 ABA（编辑后恢复完全相同内容）不由净内容指纹识别，若需“每次编辑都作废”须引入真正单调修订协议。未使用／结果未知代客图片仍不能调用现有 key-only R2 删除队列：消费端无订单引用复核；后续需持久上传／清理意图、过期宽限、原子归属核查和可重试的 guarded 消费。代客详情也须创建管理员专属最小只读投影，不能复用广域后台详情；收银和真实 provider 尚未接通。FE-003G、240 完成／164 开放／404 总项不变；无生产连接、DDL／权限／绑定、提交推送部署或分支清理。

本增量最终源码／测试 SHA-256（历史段落中的旧摘要保持当时快照，不冒充当前版本）：

```text
workers-ts/src/services/order/StoreOrderCreateService.ts 112b3606511ba21fdc5de2e4fc9ad7e4eb98e0155acb74954436979cd407fb2c
workers-ts/test/assisted-form-consumption-postgres.test.ts dee6ce1e53c21b8b7e32b99ad09e8bea99ce605cf45bcac13c4781314850d45e
workers-ts/test/assisted-purchase-runtime-cases.ts 9734e7a44bff0925996be5dc548c4f0efef8764663e640e81af29f199608e243
workers-ts/test/assisted-purchase-runtime-postgres.test.ts e685eed096d9cb23318567a2a6b88821c645438584f7be1f59e11d5e4981fe4e
workers-ts/test/assisted-purchase-flow-postgres.test.ts 98020f82416b2765f704c8bce80bcf35bf1ff8dcb0a5aac665d3c7d79421b1b7
workers-ts/test/checkout-confirmation.test.ts a133722fe38948a808d33e715cabaae04f15d84be40923b634204c71e07078d1
workers-ts/test/checkout-confirmation-rules.test.ts 08f2257390c701fb255ba415a7e2c0b47d58b6f031dc9e036da20fb949d62a6f
```

## 前序增量：代客表单页面、答案恢复与草稿预览（2026-09-23，本地通过／浏览器未验收／未部署）

`/pages/behalf/order_confirm/index` 已接入服务端权威表单，不再一律因 formId>0 禁止建单。报价必须携带匹配 ID／版本的完整合法模板，旧后端、损坏模板和不支持组件失败关闭。十类共享控件用同一校验器；图片默认值清空，不能从模板导入会员或另一结算的附件。相同结算／相同模板重算保留答案；模板改变清空答案、预览和同意状态。修改答案、联系人或备注也要求重新同意。

图片上传仅向原管理员的 `form_image/:key/:uid` 发送 Admin Authorization，不发送商城会员 token，不调用普通会员图库。只接受匹配安全整数 ID 的 canonical 引用及同路径、未到期的签名链接；显示链接仅在内存，提交和恢复只保留 `/api/assets/<id>`。新 `POST /api/admin/order/form_preview/:key/:uid` 使用既有 `order.assisted` 与应用 LOGIN，在有界只读可重复读事务内重新检查未过期／未建单结算、精确买家购物车、启用表单及独立附件命名空间；最多 900 个唯一 int32 ID，正文上限 16 KB，不扩大数据库权限。

原生选图前显式标记 chooser 生命周期：仅该 chooser 导致的隐藏保留表单，上传结束及返回可见页面后重新报价，不复用旧同意状态。普通隐藏、卸载、管理员或买家变化清除界面数据并拒绝迟到上传／预览结果；旧登录失效响应不能清除新 Admin 或商城身份。多图中部分成功先保留已知引用，其余失败可纠正；上传中禁止移除和建单，原生选择返回数量也受配置上限约束。共享时间范围控件修复第一次选择被清空；共享日期范围校验拒绝只填一端，包含普通结算回归。

恢复记录新增有界最小答案 `{id?,name,value}`，拒绝模板、签名、客户端图片归属和其它额外字段，保留无表单的旧 v1 记录兼容。确认提示明确披露姓名、电话、地址、表单答案和图片引用会在设备未加密暂存；核实成功后清除。写入并核验成功后才发送请求，未知结果按原管理员／买家／key／报价 token／答案重试，不上传新图或重读模板改写原请求。仅首次新提交且服务端明确 `ORDER_FORM_REJECTED` 或 `ORDER_QUOTE_RECONFIRM_REQUIRED`、key 匹配时释放冻结；经历不确定结果后的相同拒绝仍不能丢弃恢复记录。本页不执行收款。

### 本轮最终验证

共 **1,056 个不同有效用例通过，新增 47，0 失败／跳过**，不把重复执行或无用例的辅助文件当作新增测试：

| 范围 | 通过 | 证明与边界 |
| --- | ---: | --- |
| UniApp 完整工具链 | 649 | 包含代客确认 81（原 44＋新增 37）；实际 Vue／Pinia／请求层及 SFC setup，原生 I/O 为合成，不是浏览器渲染 |
| PC 回归 | 294 | 正式 build 的会话／组件 285、图片 9；单独传入只导出注册函数的 recovery 辅助文件产生的空容器计数 1 已排除 |
| Workers 四文件 | 68 | 代客权限、图片作用域、订单表单读取、共享表单；新增日期范围 2 |
| 原生 PostgreSQL 两文件 | 33 | 代客 28（原 20＋新增续签 8）、普通表单 5；独立受限 LOGIN、真实 JWT／SQL 权限与交易，平台传输为合成 |
| UniApp 产物／依赖审计 | 12 | 产物 3、运行时 i18n 9；实际 H5／MP／App 构建，不等于真机 APK／IPA 验收 |

命令：UniApp `npm run test:toolchain`、`npm run typecheck`、三个 `build:*`、`test:build-artifacts`、`test:runtime-i18n`；PC `npm run build`（包括测试、`vue-tsc -b`、Vite）；Workers `vitest run test/admin-assisted-order-migration.test.ts test/assisted-form-attachment-scope.test.ts test/assisted-form-read.test.ts test/system-form-migration.test.ts --maxWorkers=2`、`npm run typecheck`；原生 `node scripts/run-local-finance-postgres.mjs --schema-maintenance <PG16 bin> test/assisted-form-consumption-postgres.test.ts test/pc-checkout-form-postgres.test.ts`。最终 unit／runtime 和两端类型、PC／Uni 三端构建及空白检查通过。PC 初次误调用不存在的 `typecheck` script，随后使用项目正式 build 完成，不隐藏该命令失败。保留微信既有空 chunk、PC VueUse PURE 注释警告，没有放宽断言或升级依赖。

最终原生运行于 15:47:20 开始，242.89 秒、33 项通过。前次 15:40:53 开始的 28 项／218.67 秒只作共享日期修正前的中间证据，不重复计数。两个私有 PG16.15 集群分别为 `.cache/finance-postgres-ExXiVx`／`127.0.0.1:61228` 和 `.cache/finance-postgres-otKMTP`／`127.0.0.1:60904`，均报告测试数据库／角色夹具余留 0，并在停机后独立核验 `pg_ctl` 退出 3、无 PID／引导口令文件、监听数 0；保留已停止的诊断目录。

前端测试技能要求真实页面证据，本轮统一浏览器 `await cua.getState()` 仍在初始化时报 `failed to write kernel assets: The system cannot find the path specified. (os error 3)`。没有取得页面 URL／视口／DOM／控制台／截图，也没有进行浏览器交互或静默切换 Playwright。拟验收路径为确认页填写表单→独立选图→原请求恢复；运行时模拟覆盖该状态链，但**页面身份、非空、错误覆盖层、控制台、截图及真实交互六项均未验收**，不能以绿色构建替代。Workers／PostgreSQL 技能约束了独立身份和短只读续签事务。

仍开放：跨请求模板内容版本绑定（当前创建校验并锁定当时模板，不保证所有合法文字变动都会使旧报价失效）、未使用／结果未知图片的受控清理、完整券信息、代客订单详情／收银及真实 R2／Hyperdrive／Queue／provider／浏览器／真机验收。恢复记录不是加密或跨标签事务；手动清空设备存储后的未知订单需人工核对。页面注册数核验为 70，本轮不增加路由页面；修正 FE-003G 的“确认页未注册”陈旧描述，仍为 **240 完成／164 开放／404 总项**。没有 DDL／数据库权限计划／绑定／依赖变更，没有生产连接、暂存、提交、推送、部署或分支清理。

### 本轮最终来源 SHA-256

```text
view/common/assistedForm.ts 4ea8d544f5c74988ae5d0e3b47ecb84ac94785b9f7923ed22ea0d49ef32318cb
view/common/assistedCheckout.ts 79d6135d9f08b12e29a02dd83553cb104612a7d750f06334943c4bc44fd3b92e
view/common/order-system-form.ts 7d2d9ffd138920f7106c22500953d2b4208089b5ab8f07de43532681549400b2
view/uniapp-ts/src/api/assistedCheckout.ts ba431d2bfa04fe48d214c0ff0a9aae7c7d8496ea7525bd0434a6c5a2a13ef1cc
view/uniapp-ts/src/composables/useAssistedCheckout.ts 2b8c81484df82bf5cfa81a3f7553425ccf080f6997b4c75cda40e5101edc0832
view/uniapp-ts/src/components/SystemFormFields.vue ead08cb2621f1956542cfd57ae978b1c7e505ddecfe97231fb35b30618e13937
view/uniapp-ts/src/pages/behalf/order_confirm/index.vue 69a25c19887337b749b92e9276f11b725688314c60a96158d1ff8d9d9a777218
view/uniapp-ts/scripts/assisted-checkout-runtime.test.cjs e75b717ea451cbefd2df07b5d2ad99a6712b11c60c2748b3613b6a5caa90328d
workers-ts/src/services/admin/AdminAssistedOrderService.ts b812f6629654c5bdb8c7ff743428d1b784416323c4036e3c9e8652c4d7d6c4c8
workers-ts/src/controllers/api/v1/AdminController.ts 828515b579f87fb6fbec394eb3953d019b5adcffdace0bb9b72418420774a3ca
workers-ts/src/services/admin/AdminPermissionService.ts b52fdd21ed6c729fc49aa18081c9b3375935e66116cd30bdcd4e5ab81e1bb69d
workers-ts/src/routes/v1/index.ts 60960348ab1ce451ee12d119b732a4cb7bfc89375b57945e8f0e5904f7102823
workers-ts/test/assisted-form-consumption-postgres.test.ts bc5e95dbbfa2187ef7475b84b4c09c6a68be9aa04184f0d0f26afbc192b60941
workers-ts/test/admin-assisted-order-migration.test.ts a83173b3bfda74ac53cfab2e9229c31837771c5d72ad7dc98015f3a0a67e2289
workers-ts/test/system-form-migration.test.ts 3a590c82a7d11d85246bdc116848208e7c826bbcb94a2473d70d4d8df381d169
```

## 前序增量：图片建单归属与订单读取（2026-09-23，本地验证通过／页面未接通）

代客建单现只消费当前管理员、买家／游客、结算 key、表单 ID 共同绑定的 module=5 图片；不再借用会员普通图库，也不接受 HTTPS／带签名 URL 绕过归属。服务端重新生成图片归属摘要，覆盖客户端和可编辑模板中的同名元数据，在原创建事务中以 `FOR SHARE NOWAIT` 固定精确附件记录；订单、表单收集、库存和积分仍原子提交。持久快照不另存原游客标识，添加归属后再次检查 1 MB 上限。SQL／锁失败不伪装成可修改原请求的明确业务拒绝。

新增 `GET /api/admin/order/form/:orderId/:uid`，要求 `order.assisted`，使用独立应用 LOGIN 和只读可重复读快照；仅原代客管理员可读对应可见订单。会员商城详情也接入相同读取器。图片签名必须匹配持久归属和当前附件命名空间；原结算 KV 过期或购物车改变不影响已提交订单。真实拆单的随机子单 key 不能代替原结算 key，读取追溯精确付款根单，并要求管理员／买家／来源及原表单快照一致；缺失或损坏证据时图片不返回，正常文本仍可读。上述表单读取／商城详情及嵌套拆单详情会移除内部归属元数据，普通订单 HTTPS 兼容保持不变；未将所有原有列表／后台原始 DTO 宣称为已完成的表单展示入口。

原生测试首次发现游客拆单被发货通知的正数 UID 校验整体回滚。修复仅允许来源明确的普通代客游客发货事件，入队和消费均检查真实订单；仍按现有开关暂存该订单手机号的短信任务，但绝不读取 UID=0 的会员／微信身份或写入共享站内消息。不放宽拒绝退款／次卡提醒的正数 UID 合同。测试中手机号、身份、R2 字节、KV、Sequence、Queue 和 Hyperdrive 传输均为合成；没有实际短信或外部 I/O。

最终 **308 个不同用例通过，新增 39、0 失败／跳过**：原生链路 20、普通表单 5、既有代客 77、商城退款／拆单代次 12，共 114；单元／权限／运行器八文件 194。新增为 20 项原生链路、18 项有界读取、1 项通知 UID 合同用例；不是 308 项全部为新功能专项。原生 25 项于 15:01:25 起、160.84 秒通过；完整代客 77 项于 15:04:48 起、582.11 秒通过，没有放宽运行器十分钟上限；商城代次参数化展开 12 项于 15:15:08 起、100.60 秒通过。单元八文件于 15:04:52 起、10.90 秒通过，最终 Worker unit/runtime 类型及空白检查通过。

首轮原生 22 通过／1 失败准确暴露游客通知问题，修复后完整重跑；单元回归还发现既有迁移文本比较仅因 CRLF/LF 不同而失败，现只统一换行后完整比较，不修改 SQL。四座本机私有集群均报告夹具余留 0，分别独立确认 pg_ctl 退出 3、PID／引导口令文件不存在、监听数 0；保留已停止的诊断目录。最终 19 个来源文件摘要复核一致，命令／结果／失败与限制见[机器证据](../audit/assisted-form-consumption-20260923.json)。先前上传与表单阶段的历史记录保留，不能将其“尚未接通”当作本轮状态。

Workers／PostgreSQL 技能推动服务端身份、短事务固定归属和真实受限 LOGIN 验证。未改 DDL／权限计划／绑定／依赖或页面，未连接生产、提交推送部署。**页面仍对 formId>0 禁止建单**：十种控件、答案持久恢复／精确重放、草稿图片续签、未使用／未知对象生命周期、跨请求模板内容版本以及真实浏览器／平台验收仍开放；泛后台／供应商表单展示也未作完整验收。FE-003G 和 240 完成／164 开放／404 总项不变。

客户端接线必须分离持久值与展示链接：答案只保存 `/api/assets/<att_id>`，上传返回的临时签名 URL 仅用于预览，不得当作建单值。签名到期后的草稿续签需要重新核对本次结算归属，不能调用普通会员素材接口；提交结果未知时冻结的仍是原管理员／买家／key／报价凭据和原答案，不得替换为新图片或新模板后重发旧 key。新增表单内容还需纳入已有本机明文留存提示与清除流程；本轮未提前解除前端保护。

## 前序增量：代客表单图片上传隔离（2026-09-23，本地验证通过／端到端未完成）

新增 `POST /api/admin/order/form_image/:key/:uid`，继续使用 `order.assisted` 和应用 LOGIN，后台连接不承担附件业务写入。管理员来自已验证身份，买家／游客／购物车来自既有结算快照，正文不能改写这些归属或任意选择表单。先检查未过期、未建单、精确购物车归属与商品绑定的启用图片表单，再读取有界 multipart；拒绝素材分类和重复文件部分。

附件使用 `type=1/relation_id=管理员/module_type=5/file_type=1/image_type=8/pid=0` 独立命名空间。完整 SHA-256 摘要绑定版本、管理员、会员 UID 或游客标识、本次 key 和表单 ID；持久保存在 `attachments/ao/<digest>/<随机值>.<扩展名>` 对象键及数据库 name 列，最长 96 个 ASCII 字节，不扩大原 VARCHAR(100)，也不暴露原游客标识。现有后台公共素材、会员／供应商／客服命名空间不包含这类附件。

R2 写入在 SQL 事务外完成；随后元数据事务再次检查结算与商品表单，并以 `FOR SHARE NOWAIT` 固定购物车、商品与表单，使用有界语句和空闲事务时限。明确提交前／SQL 拒绝才清理新对象，直接清理失败交由既有清理队列处理。元数据提交结果未知则保留对象，不能因响应丢失而删除已被提交记录引用的图片；特别排除了 PostgreSQL `40003`（[官方错误码](https://www.postgresql.org/docs/16/errcodes-appendix.html)）。这不是 SQL/R2 分布式原子性，未确认对象和双重清理失败仍需后续受控核查。

最终六文件单元／权限回归 70 项、原生 PostgreSQL 整文件 77 项及两套 Worker 类型检查通过，共 **147 个不同用例通过，新增 52、0 失败／跳过**。新增为 35 项作用域／错误分类／multipart 用例、17 项真实 SQL／HTTP 上传用例；其余为既有代客流程及附件／表单／权限回归。首轮原生 77 项通过，但期间有最终保护增量，仅保留为中间证据；最终整文件运行于 14:26:51 开始、590.72 秒完成，未将重复运行累计。两座私有 PG16.15 集群都报告夹具余留 0，并独立确认 pg_ctl 退出 3、无 PID／引导口令文件且无端口监听，仅保留已停止的诊断目录。

真实本地独立应用／管理 LOGIN 和 JWT／SQL 权限验证了会员／游客上传、跨管理员／买家／角色拒绝、过期／失效／已建单拒绝、R2 写入期间表单与购物车变化、INSERT 权限失败、清理失败后队列消费，以及实际元数据提交后注入回执丢失／40003／08006 时保留对象。R2 上传回调通过真实 pg_stat_activity 验证无 API／Admin SQL 事务；对象字节、R2／Queue／KV／Hyperdrive 传输仍是替身，签名读取只证明对合成对象的服务行为，不是实际 R2／workerd／浏览器验收。精确源码摘要、运行结果和服务范围见[本轮机器证据](../audit/assisted-form-uploads-20260923.json)。Workers／PostgreSQL 技能促使外部 I/O 与短事务分离、真实角色验证及未知结果保守处理；最新类型下载受沙箱网络限制，采用已安装 5.20260828.1 类型与当前官方 R2 文档。

**仍未接通代客新图片的建单消费和订单详情读取**，不能把上传入口等同于完整表单功能。后续须把服务端归属贯穿不可变订单快照、拆单和游客后台读取，并完成草稿图片链接续签、未使用图片生命周期、十类页面控件／答案持久恢复／未知结果重放、模板版本和真实浏览器验收。本轮未改页面，`formId>0` 继续禁止建单；未改 DDL／权限计划／绑定／依赖，未连接生产或提交推送部署。清单仍为 240 完成／164 开放／404 总项，FE-003G 保持开放。

## 前序增量：代客商品表单服务合同（2026-09-23，本地候选／未部署）

本轮补齐的是确认／建单服务端前置合同，不是代客表单的端到端完成。没有修改页面、上传路由、数据库结构、权限计划或平台绑定，没有连接生产、提交、推送或部署。

- 既有 `order.assisted` 路由只读取本次商品决定的启用表单，不新增任意表单目录读取权限。确认／重算在原应用 LOGIN 的只读可重复读事务中返回 `systemForm: {version:1,id,name,value}` 和兼容 `custom_form`；仅投影 ID／名称／有界模板，不读取历史提交记录。无表单时分别为 `null`／空数组。
- 建单接入 `customForm`／旧 `custom_form`，同时给出两种参数明确拒绝。流式正文上限为 1,100,000 字节，与既有表单 1,000,000 字节及 100 组件／10,000 字符字段上限配合。十种组件的值按数据库模板校验，客户端标题、选项、必填标记不能覆盖模板。数组与旧时间戳键对象统一解析，重复组件或损坏模板失败关闭。
- 订单快照与 `system_form_data` 收集仍在原创建事务中提交。模板读取使用 `FOR SHARE NOWAIT`，复用既有 `system_form(id)` 行锁权限，不授予配置字段写权；模板被并发编辑时快速失败，校验后阻止模板修改直至订单提交。报价只读路径不加行锁。**这尚未把模板全部内容绑定进原报价指纹**：创建会校验并锁住当时模板，但合法的文字等改动不必然要求重新确认，跨请求模板版本合同仍开放。
- 私有 canonical 图片只接受该会员自己的 `type=3/module_type=3/file_type=1/image_type=8` 记录；拒绝其它会员、游客命名空间、后台素材和非 R2 记录。UID=0 不充当附件主体，既不借用零归属图片，也不在订单读取时签署它们。已有 HTTPS 文本引用兼容不变，不代表已证明其私有文件归属；本轮没有真实 R2 上传或图片对象验证。
- 仅核心建单的已知提交前表单／报价拒绝，才返回带原 key 的 `ORDER_FORM_REJECTED`／`ORDER_QUOTE_RECONFIRM_REQUIRED`。SQL、权限、锁冲突和提交后回读失败不返回“可修改原请求”的标记。已提交订单先按原管理员／买家／key 回放，不因模板停用或答案变化而重复收集。

### 本轮验证与边界

最终目标回归 **206 个不同用例通过，新增 26、0 跳过**：真实 PostgreSQL 代客 60 项／普通订单表单 5 项，表单／附件／管理模板本地 61 项、代客路由合同 4 项、UniApp 普通及代客结算运行时 76 项。Worker unit/runtime、PC 和 UniApp 类型及空白检查通过。代客由 37 增为 60（新增 23），共享表单由 6 增为 9（新增 3）；未把重跑或初始单独 9 项重复累计。首批原生 130 项通过，但执行期间仍有后续源码增量，只保留为中间证据；其中额外 60 项预售、7 项跨连接报价回归不计入上述最终 206 项。

第二批最终业务代码下 64 通过／1 项测试夹具错误：并发用例误用 `withFinancePeers`，其注册数据库身份保护拒绝了另一套合法隔离夹具。已改为该夹具已有 `withPeer`，没有放松任何身份校验、权限或业务断言。随后完整代客文件于 13:46:01 开始，448.63 秒完成，60 项通过；普通订单表单 5 项在上一批已通过且此后业务源码未改。并发测试实际证明持有模板编辑锁时建单快速失败、校验后其它连接的修改等待超时、创建提交后同一连接才可修改；MVCC 用例证明报价模板与商品来自同一快照，创建重新验证新增必填字段。另一个初始附件 SQL 文本断言因 Windows CRLF 失败，现仅统一换行后比较，61 项整批复验通过；没有修改迁移 SQL。

测试使用真实 PostgreSQL16、完整注册 schema、独立应用／管理 LOGIN 与实际 JWT 授权；KV、Sequence、Queue 和 Hyperdrive 传输为本地替身。测试图片仅为合成元数据行，不把它们当作真实 R2 对象。三座私有测试集群均报告夹具余留 0，并独立核验已停止、PID／引导口令文件不存在、监听数 0；仅保留诊断目录。没有浏览器／真机、真实登录基础设施、provider、当前候选 Linux/CI 或生产验收。PostgreSQL／Workers 技能推动有界模板读取、事务内共享锁与不确定结果分离。精确运行、失败记录和最终源码摘要见[机器证据](../audit/assisted-system-forms-20260923.json)。

剩余顺序：先建立绑定管理员／买家／游客／本次结算的代客上传与读取归属，再接入页面十种控件、答案最小化持久恢复、首次表单拒绝纠正和未知结果原内容重放；随后补跨请求模板版本、图片对象与浏览器验收。当前页面继续在 `formId>0` 时禁止建单，不伪造空答案，也不冒用会员令牌上传。收银及其它既有门禁仍开放，FE-003G、240 完成／164 开放／404 总项保持不变。

## 前序增量：代客确认页与冻结建单恢复（2026-09-23，本地候选／浏览器阻塞）

恢复 `/pages/behalf/order_confirm/index`，从实际选品购物车进入；目标流程为“选客→选品→选择配送／优惠→取得完整报价→原生确认建单→核验或恢复原订单”。这不是收银完成，也不是完整旧页面对等验收。本轮仅改前端及台账，无 Worker 业务、数据库、权限或绑定变更；未连接生产、提交、推送或部署。

- 独立 Admin 会话调用既有确认／重算／创建路由，验证版本化管理员、买家／游客、精确购物车集合、选择和凭据合同；不发送商城 token 或请求自选 staff/admin 身份。读取旧后端不完整响应失败关闭。草稿买家或管理会话变化立即移除旧报价，URL UID 本身不授予权限。
- 保存地址按当前会员读取；手填地址使用 `/city` 的 `city_area` 级联目录，不用旧 `/city_list`，收货人／电话／详情显式填写。自提只选返回的实际门店并填写联系人；非物流类型不带配送地址，次卡只走自提。会员候选券按需读取，积分只提交开关。显示同次服务端商品单价、原价、会员／券／首单／积分优惠、原始运费／运费优惠／应付运费与应付金额；选择变化立即作废报价及确认。目录错误可以显式重读，但会清空当前填写。选择现金／微信／支付宝仅用于报价和建单，本页不调用付款接口。
- 原生确认前说明库存、券、积分预占和本机收货信息留存。冻结原管理员／买家／key／cart IDs／quoteToken／配送／券／积分／备注等，写入按管理员隔离的本机恢复记录，准确回读并持久记录发送次数后才发请求。存储不可写或回读不一致则不发新单；没有保存管理令牌或密码。数据未加密，页面明确提示不在不可信／共用设备操作。
- 超时、服务端错误或无法核验的成功结果保留原内容，只允许原请求重试；仅首次明确报价拒绝且无其他已记录尝试时可清除并重新报价。服务器成功后也不自动付款；恢复本机成功记录仍先重放原 key 向服务器核验，核实后才允许清除本机收货信息。损坏、冲突或无法确定的记录不能静默丢弃。选客和购物车写入在操作边界重新检查记录，阻止另开买家或继续修改；刷新后需重新登录原管理员，恢复不依赖旧内存草稿。隐藏／换身份不发布迟到结果；已保存的原结果仍供原管理员恢复。
- 商品要求系统表单时明确禁止建单，没有伪造空答案；收银、代客表单服务合同、券完整规则／分页及大型地址／门店／购物车目录、跨标签原子协调、损坏／永久无法核验记录的人工处置入口仍待完成。这里不是跨标签事务，也不改变先前购物车累加写入非幂等的事实；未开始建单前的游客购物车仍只在内存保存上下文。

### 本轮验证与未验证边界

最终前端工具链 **612**、三端实际构建／运行依赖审计 **9**、产物 **3**、路由合同 **17**，共 **641 个不同用例通过，新增 47、0 失败／跳过**；新增确认运行时 44 项、选客／购物车恢复接线 3 项。最终 UniApp 类型检查通过。先执行的 88／99 项局部测试和首轮全量不重复累计。最初类型检查发现半成品适配器缺一个右括号（TS1005），修复后通过；没有通过放宽合同消除错误。最终轮额外补入原始运费和运费优惠展示，并重新执行完整工具链及三端构建审计、产物检查。MP 单独首轮构建保留既有 `utils/articleRichText` 空 chunk 警告。

测试使用实际 Vue/Pinia、SFC setup、API 适配器和请求层，原生生命周期、HTTP、存储／确认窗口使用合成替身；证明报价失效、乱序、存储故障、首次拒绝与不确定结果区分、管理员隔离、重复点击、原内容恢复、目录和表单失败关闭等客户端合同。**本轮没有运行真实 SQL 或真实建单／扣款**，先前服务端原生证据单独保留，不把两者拼接成端到端证明。运行命令、最终源码摘要见[前端机器证据](../audit/uniapp-frontend-parity.json)的 `assistedCheckoutIncrement`。

前端测试技能要求真实渲染证据；统一浏览器本轮调用 `cua.getState()` 在初始化即报 `failed to write kernel assets: The system cannot find the path specified. (os error 3)`。未取得 URL／标题、DOM、控制台、截图或控件交互，0 页面验收；没有未经允许使用 Playwright 回退，也没有启动本地预览服务。真实登录／Redis／Hyperdrive／provider、H5 浏览器和小程序／App 真机及当前候选 Linux/CI 仍开放。

路由审计实际核对旧 151 条：目标 69→70、直达 9→10、兼容映射 98（60 候选／38 部分）、未映射 44→43；FE-003G 缺路由 8→7（代客只剩收银未注册），但其功能父项保持开放。240 完成／164 开放／404 总项不变；不因新增路由关闭购买验收或其他迁移门禁。

## 前序增量：代客确认／重算的同快照合同（2026-09-23，本地候选）

接入确认页面前发现：原确认的购物车展示与核心报价分开查询，重算只返回金额；手填地址虽已参与校验和指纹，却没有返回展示，积分开关固定为关闭。本轮修复这些前置缺口，**不是确认／收银页面已经完成**。

- 确认和重算的商品投影、买家、保存／手填地址、SQL 计价配置与金额统一使用应用 LOGIN 的短只读可重复读事务。设置本地 5 秒 statement/idle 超时上限，保留更严格的调用方配置；不改变正式角色或权限计划。
- 支付可用性与首单配置仍沿用既有 KV 权威，在事务外读取。核心只读报价新增内部预载首单配置参数，不从 HTTP 参数复制，建单始终重新读取；这并不宣称首单 KV 与 SQL 具备全局同快照或已完成首单配置权威治理。确认快照和不可变报价凭证只在 SQL 事务结束后写 KV，KV 失败不返回可提交报价。
- 核心报价返回已校验地址字段、积分策略与商品决定的表单 ID。代客响应补充 `checkout.version=1`、实际管理员／目标买家／游客／购物车集合、配送／门店／地址来源、积分与券选择等元数据；手填地址仅返回该次报价数据，不保存地址簿。自提／虚拟商品不再借用默认配送地址展示。
- `computed.data.result` 保留原平铺金额和 `quoteToken`，同时包含完整确认投影、`priceGroup` 和原 `orderKey`。前端应完整替换当前报价，不能仅合并价格；新增字段不等于客户端请求值获得授权。原 key 与冻结请求的建单恢复协议仍须前端实现。
- 报价与建单均只接受代客已有的微信／支付宝／现金方式，不能用普通订单的 `offline` 规则报价后以另一方式建单。明确选择的自提门店也在报价时校验启用状态，创建时继续重验。

验证：最终代客 37、共同报价凭证 27、地址 58 项原生 PG16 通过；未改变业务源码的前一批预售 60 项通过，加上代客迁移合同 4 项，共 **186 个不同用例通过，新增 9 项、无跳过**。新用例覆盖六种会员／游客交付组合的完整确认／重算响应、真实独立连接在展示读取后修改数量／价格／地址／积分配置仍保持原快照、旧凭证建单拒绝且无业务写入、重算后可创建，以及 KV 缓存未命中／报价凭证写入失败时不持有业务事务。两个独立实际 LOGIN 的鉴权和权限负向验收继续保留。

首轮原生 95 通过／2 失败：并发样本“数量减半且单价翻倍”导致总价恰好相等，修正为不抵消的变动；KV 断言把并行预取配置的单条 SQL 自动事务误认作被持有的报价事务，改为核心期间禁止 KV 加上数据库 `idle in transaction` 检查，仍执行真实核心且不伪造报价。最终整文件复跑通过，失败与重跑不重复计数。类型首轮有测试 `this` 和 KV 重载声明错误，已用显式服务类型及窄配置接口修正，最终 unit/runtime 双套类型通过。没有为消除测试失败放宽业务检查或数据库权限。

两个私有集群报告夹具余留 0，独立确认 `pg_ctl=3`、无 PID／引导密码文件／对应监听，最终 PostgreSQL 进程数 0；停止后的合成诊断目录保留。验证结果与最终源码摘要见[机器记录](../audit/assisted-checkout-preview-20260923.json)。本轮未连接生产、未提交／推送／部署，无 DDL、正式权限计划、绑定、依赖或前端页面修改。Workers／PostgreSQL 技能促使 KV 移出事务、实际独立 LOGIN 并发验证与连接释放检查。当前 Workers 类型包下载被网络权限拒绝，使用项目已安装版本，不声称已获取最新版。

继续开放：代客确认／收银 UI、`custom_form` 的真实展示及提交、优惠券接口完整规则／分页和同快照整合、必填地址／自提联系人的交互、未知建单结果的冻结重试、浏览器／真机／真实登录与支付提供商。`checkout.systemFormId` 只是发现标志，不代表表单已接通。此前浏览器插件初始化故障仍未解决，本轮不把服务端测试称为浏览器验收。FE-003G 与 240 完成／164 开放／404 总项不变。

## 前序增量：选客、选品与代客购物车（2026-09-23，本地候选）

恢复旧 `/pages/behalf/user_list/index` 与 `/pages/behalf/goods_list/index`，并从个人中心及代客记录页提供入口。管理登录仍是独立、仅内存的 Admin 会话；没有复用会员 token 或把链接的 `uid/staff_id` 当作授权。前端复用既有用户目录、商品目录、SKU 和代客购物车路由，本轮不改 Worker 业务、数据库、权限计划或平台绑定。

- 会员目录需要 `user.view`，选品需要 `product.view`，购物车需要 `order.assisted`。可搜索昵称／手机号／完整 UID，分页失败重试原页，重复偏移拒绝；只允许选择当前显示且正常的会员。没有会员查看权限的操作员可使用具备其他必要权限的游客入口。
- 游客通过已有安全随机数工具生成独立标识；缺失安全随机能力时拒绝，不使用时间戳或 `Math.random`。当前跨页草稿只保留管理员／买家／游客标识和写入恢复状态，不持久化会员资料。隐藏页面清空会员结果及搜索条件，管理员代次变化清空草稿，迟到响应不覆盖新身份。
- 目录仅请求上架商品，显式选择服务器真实 SKU；失效、售罄和预售不进入普通代客购物车。不发明默认规格。目录与规格价格标作参考价，购物车显示服务端当前买家参考价；不假设它等于最终结算报价。
- 购物车读取校验管理员、买家、普通商品类型、活动／门店／购买模式及商品 SKU 归属。新增累加数量、绝对数量更新和原生确认后删除均调用既有接口，不乐观修改页面，也不扣库存或创建订单。
- 加购不是幂等操作，不自动重试。请求在途的锁跨页面隐藏保留；失败、无效成功体或成功后的读取失败均要求重新读取并人工核对。人工解锁只表示操作员已核对，不代表服务器取消了超时请求；迟到处理、多标签和持久恢复协议仍是限制，不能宣称 exactly-once。

验证：新增 **52 项**实际 Vue／Pinia、API 适配器、请求层及两个真实 SFC setup 的运行时测试，原生生命周期、网络、导航、确认窗口和随机数由本机合成替身驱动，不冒充 DOM 或后端端到端。最终工具链 **565**、三端依赖审计 **9**、默认构建产物 **3**、路由／导航 **17**，共 **594 个不同用例全通过，新增 53、无跳过**；此前 50 项定向运行、563 项中间运行及前序原生数据库测试不重复累加。UniApp 类型与最终 H5／MP／App 两套临时／默认构建均通过。

产物检查过程保留两次失败：依赖审计的构建输出在临时目录，初次默认产物检查发现旧 67 页 MP／App 产物；重新构建后通过。随后加强 H5 检查，初次断言把首页误当普通路径，读取生成代码确认其为 `/` 加首页路径别名后改为精确校验该别名，其他页面继续要求实际路径；最终三项通过，没有只检查文件存在便放行。MP 保留既有 `utils/articleRichText` 空 chunk 警告。新增页 CSS 与页面资源存在，但不是渲染效果证明。

目标注册页 **67→69**，直达旧路由 **7→9**，未映射／跨界面路由 **46→44**；旧151条路径仍精确且不重复地记账，FE-003G 的未注册路径10→8，其中代客确认／收银两页仍未完成。分类层级、旧高级筛选／会员徽标、记录详情和支付导航也未补齐；超200购物车行／500规格行明确拒绝，未改后端大列表协议。刷新应用或更换管理员会丢失内存游客标识及恢复锁，不能从本页重新找回游客购物车；当前 App 游客安全随机路径未开放。直达链接需重新选客，不声称完整旧页面等价。

前端测试技能要求真实页面身份、非空、错误覆盖层、控制台、截图及交互证据。前序统一浏览器初始化两次失败（`kernel assets ... os error 3`，重置无效），替代 Playwright 授权仍未答复；本轮没有重新启动浏览器或静默切换工具，**0 页面／截图／交互验收**，视口和真机行为未知。计划的验收链为个人中心→管理员登录→会员／游客→商品 SKU→购物车加改删及异常核对。构建通过不关闭该门禁。

预售允许积分抵扣的既定规则不变，当前完整前端工具链包含预售积分开关／报价竞态／原请求恢复回归；上轮60项原生报价／实际扣分／取消返还证明仅作为前序记录，本轮没有重跑或计入594项。源码摘要及具体执行记录见[选客选品增量证据](../audit/uniapp-frontend-parity.json)。本轮没有生产连接、数据库集群／浏览器服务、提交推送／合并／部署／分支清理；清单仍为240完成／164开放／404总项。

## 前序增量：选客前置用户目录读取（2026-09-23，本地候选）

接入选客页前核对发现，两个现有用户列表路由共用的 `adminUserList` 调用了 `UserDao.selectList`，后者读取整行并原样返回，包含 `pwd`、证件、登录 IP、识别码及任意扩展文本。`page=0` 或 `limit=0` 还会跳过 DAO 分页；没有固定排序，旧选客页传入的 `nickname` 搜索也被忽略。仅本机合成夹具复现，未查询生产，不能据此断言发生过实际泄露。

本次改为独立 `AdminUserListService`，在 SQL 中显式选择 12 个列表字段，并提供 4 个过渡 snake_case 别名。保留当前管理页依赖的余额、积分、状态、等级、注册时间和推广人；不把密码摘要、证件、登录信息、条码或扩展 JSON 读入列表服务。两条路径仍为 `/api/admin/user/list` 与 `/adminapi/user/list`，成功信封 `{list,page,limit}` 不变，添加 `private, no-store, max-age=0`／`Pragma: no-cache`。

- 仅接受 `page/limit/uid/phone/nickname/group_id/status`，重复或未知过滤器拒绝，不静默当作全目录。默认第 1 页、10 条，最多 100 条／10,000 页；整数和文本类型、长度、控制字符在业务 SQL 前验证。
- 按 UID 降序稳定分页，同时排除 `is_del` 和 `delete_time` 删除标记；管理员仍可查询禁用用户，`status=0` 与 `group_id=0` 有明确含义。读取不是下单资格凭据。
- `nickname` 支持昵称／手机号的字面子串以及精确 UID，`phone` 和 `uid` 筛选保持精确；组合条件取交集。`%/_/反斜线` 转义且始终绑定参数，不作为调用方 SQL 通配符。PHP 的 UID 模糊匹配改为精确 ID 是明确差异；本次不迁移旧后台的全部筛选、会员徽标和扩展资料。
- 单次查询在只读短事务内执行，语句超时不超过 5 秒并保留更严格调用方限制，事务结束恢复设置。无 DDL、索引、正式权限计划、绑定、依赖及任何业务写入变化。
- 现有 `user.view` 权限不变；`order.assisted` 不自动授予读用户目录，`user.view` 也不自动授予代客建单。独立 LOGIN 验收另将一次性合成读账号收紧为精确列权限，以验证 SQL 投影本身不需要读取密码列；这不是生产权限方案的变更。

最终验证：用户目录 22 项（10 项真实 SQL、12 项输入类型），既有代客原生 28 项（新增两条用户目录鉴权路径）、预售报价／扣返原生 60 项，以及用户管理／运行鉴权／本地运行器回归 132 项，共 **242 个不同用例通过，新增 25 项，无跳过**。4 文件本地运行 154 项通过；3 文件原生 110 项通过，其中用户目录 22 项与前者相同，不重复累计。最终版目录又以独立非超级用户 PG16 复验相同 22 项；只读事务中的实际 UPDATE 由 SQLSTATE `25006` 拒绝，0／250ms 调用方配置分别得到 5000／250ms 上限，退出事务恢复原设置。双套 Worker 类型、运行器语法和 diff 空白检查通过。预售继续遵守所有者已选的积分报价／扣减／取消返还一致规则，不因本次选客前置工作改变。

失败过程保留：首轮定向 2 项失败，旧接口缺少管理页需要的 snake_case 别名且仍返回整行；修复后首批 8 项通过。扩展至事务观测时 153 项中 151 通过、2 失败，原因是测试 spy 重复包装 PGlite 原生结果适配器；改为容器边界代理后 153 项通过，再加入运行器精确准入用例后最终 154 项通过。没有修改共有数据库夹具、跳过拒写断言或放宽业务判断来消除失败。

两个新私有集群 `finance-postgres-3PU0cd`（64734）与 `finance-postgres-svHBd9`（65106）均报告夹具库／角色余留 0。普通沙箱的独立检查因私有 ACL 被拒，未把不可读取当作“无残留”；提权只读复核后，两个 `pg_ctl status` 均退出 3，PID／密码文件、所属进程及监听均为 0，全部 PostgreSQL 进程为 0。保留停止后的私有诊断目录。源码摘要与精确运行时间记录在[前端迁移审计的选客前置增量](../audit/uniapp-frontend-parity.json)，历史快照不改写。

PostgreSQL 技能推动了 SQL 列白名单、原生列权限负向证明及事务拒写测试；按 [Workers 官方最佳实践](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/)保留请求级数据库会话并验证释放，不改 Hyperdrive 连接／绑定。最新类型获取先前失败，本轮沿用已安装的 `@cloudflare/workers-types 5.20260828.1` 作类型核验，不称最新运行时验收。选客页、选品／购物车、确认／收银、记录详情及真实登录／provider／浏览器整链仍未完成；本轮没有前端页面变更，不能关闭 FE-003G 或把后端读取合同当作完整旧版界面等价。前序浏览器初始化故障及替代测试授权仍单独保留。未连接生产、未提交推送／合并／部署／清理分支，清单 240／164／404 不变。

## 前序增量：全路由与游客付款后置任务（2026-09-23，本地候选）

本轮将同一套逐路由真实 Admin JWT／实时 SQL 权限、独立应用／管理 LOGIN 验收扩展到全部 11 条代客接口。新增购物车列表／数量／精确集合删除、优惠券、订单列表、现金付款与状态查询；仍仅为普通商品代客入口，不新增预售代客能力。完整 schema 279 表、运行权限、平台绑定及依赖未改。

真实 PG16 首轮 17 项中 16 通过、1 失败，复现 **游客付款已提交，但 `order.paid` 后置处理因不存在 UID=0 用户而失败**。旧 PHP `app/jobs/order/OrderJob.php::setUserPayCountAndPromoter` 只有查到用户时才增加购买次数。现在仅对 `uid=0/type=0/is_channel=2/staff_id>0/coupon_id=0` 的明确代客游客订单跳过账户赠券、抽奖权益和购买次数；仍执行供应商账务、履约处理和最终任务完成。同类会员路径不跳过，正 UID 缺用户仍保留错误，来源标记缺失的 UID=0 订单拒绝并回滚后置业务效果。没有创建共享 UID=0 用户或放宽数据库权限。

新增 11 个原生用例，文件总数由 15 扩为 **26 项全部通过**：

- 会员／游客现金付款、跨管理员／买家拒绝、订单列表与状态归属、重复付款和重复任务处理；直接运行真实 outbox 消费服务至 `COMPLETED`，不是仅验证消息入队。配置了合成商品赠券和抽奖活动，会员权益各一次，游客为零，供应商入账一次。
- 会员／游客购物车只返回本人代客范围；跨主体、混入非授权购物车和超库存数量拒绝；合法改量后旧报价不能继续建单，删除不扣库存。
- 优惠券查询排除游客及他人券；重算→实际占用与 100 积分扣减一致；取消释放、付款核销，以及重复处理不重复变化。
- 撤销 outbox／支付审计 INSERT 时现金支付整笔回滚且没有 Queue 调用；测试恢复的仅是此前明确授予的基线权限，不是业务运行时自动修复。
- Queue 发送失败不撤销已提交付款，保留 FAILED 事件及错误日志；重复付款不再发消息，后续对保留事件调用消费服务可完成一次账务。未验证实际 Cloudflare 调度投递。

外部微信／支付宝仅验证未配置 provider 时拒绝、无付款状态变化及无外部 fetch，不能称外部支付验收。Queue 发送本身为合成替身；保留前序 JWT 本地签发、KV／序列／Hyperdrive 传输替身及 Redis 撤销关闭边界。所有请求按技能要求关闭连接，场景末请求会话余留为 0；未接生产、未提交推送或部署。当前候选 Linux/workerd/CI、前端 FE-003G、真实登录／外部支付、其他活动与更广泛并发场景仍开放。

最终代客原生 26 项、会员权益／预售付款原生回归 29 项、付款单元回归 24 项，共 **79 个不同用例通过，新增 11 项、无跳过**。单元首轮发现既有充值索引合同的 CRLF/LF 比较失败，测试现只归一化换行，SQL 不改。Worker unit/runtime 类型通过，三个私有集群独立确认为停止，无所属 PG 进程／监听及 PID／密码文件。技能推动了不扩大权限、支付事务回滚和请求连接释放的验收。精确执行记录见本轮[机器证据](../audit/assisted-lifecycle-runtime-20260923.json)。前序机器证据保留为当时源码快照，不改写历史。

## 前序阶段：建单与取消四路由

日期：2026-09-23。状态：本地测试通过，未提交／部署，未连接生产。基线 `7a5508f2b0935d7be13f40b4ea0fdb7fd37ed38b`。本轮不修改业务代码、DDL、权限计划、绑定或依赖；新增真实运行身份测试及本地运行器的精确文件准入项。没有把代客普通商品入口扩展为预售或其他活动。

## 验证范围

使用完整 279 表的隔离 PostgreSQL 16.15 数据库，通过已审查的整店初始化程序建立两个独立真实 LOGIN，先做应用／后台精确权限审计。HTTP 请求使用本地签发 JWT，执行真实验签、密码声明、实时管理员状态、SQL 角色权限服务及 `adminRuntimeAuthMiddleware`；测试管理员为 level=1，仅有 `order.assisted`，不借超级管理员逻辑跳过路由权限。

四条实际控制器链路为 `cart/add/:uid` → `confirm/:uid` → `computed/:key/:uid` → `create/:key/:uid`，权限中间件按正式代码逐路由挂载。请求内核对两个数据库连接身份，代客业务使用应用连接，管理连接不可读取原购凭据，应用连接不可改系统配置。每个请求的应用／管理连接均关闭，场景结束查询 `pg_stat_activity`，相关会话余留为 0。

| 目标 | 交付方式 | 积分与取消验收 |
| --- | --- | --- |
| 会员 UID=11 | 保存地址 | 报价／原购记录 100 积分，定时取消返还 100 |
| 会员 UID=11 | 手填结构化地址 | 同上 |
| 会员 UID=11 | 自提门店 | 同上，不要求配送地址 |
| 会员 UID=11 | 虚拟商品 | 同上，失效地址簿不阻止无地址建单 |
| 游客 UID=0 | 手填结构化地址 | 即使请求使用积分，报价／扣分／返还均为 0 |
| 游客 UID=0 | 虚拟商品 | 同上，不要求地址 |

每条正向路径经过加购、只读报价、重算、实际下单和幂等重试，再由真实定时关单服务取消。原购／取消凭据由业务事务和真实触发器产生，不由测试补写；商品、SKU、购物车和用户状态恢复到报价前状态，第二次定时关闭不重复补偿。

负向验证包括跨管理员／目标用户／游客购物车、跨管理员或用户复用报价、缺 token、没有代客权限、禁用账号和旧密码声明。已保存的报价也不能绕过随后发生的账号禁用、密码变更或角色撤权；拒绝在管理连接观测点及序列调用之前发生，业务状态不变。撤销应用原购 INSERT 会使会员／游客建单整体回滚；撤销取消 INSERT 会使游客定时补偿整体回滚，恢复明确的原购／取消维护权限后重试成功，不回退后台账号或自动授予缺失权限。

## 测试与失败记录

- 最终原生 73 项／2 文件：新增 `assisted-purchase-runtime-postgres` 15 项，既有 `order-delivery-address` 58 项。
- 单元／边界 128 项／3 文件：代客迁移合同、本地 PG 运行器、后台运行鉴权回归。运行器新增 1 项精确准入边界。
- 共 **201 个不同用例通过，新增 16 项，无跳过**；不累加首轮失败、8 项中间验证或此前阶段。Worker unit/runtime 双类型、JSON／源码摘要及 diff 空白检查通过。
- 首轮 8 项中 1 通过、7 失败：测试把鉴权挂在通配 `use()`，改变 `c.req.routePath`，导致仅有 `order.assisted` 的账号按不同路由权限被拒绝。对照正式路由修正测试挂载后 8 项通过，再扩展为最终 15 项。没有授予 `order.manage`、使用 level=0 或修改业务代码来消除失败。
- 三个私有集群均报告夹具库／角色余留 0，独立核验 pg_ctl=3，无 PID／`bootstrap-password`／所属数据库进程／监听；最终匹配的测试和类型进程 0。停止后的诊断目录保留。

机器证据：[assisted-purchase-runtime-20260923.json](../audit/assisted-purchase-runtime-20260923.json)。

## 仍未证明的范围

这里是真实 Node HTTP 控制器、JWT 验签、数据库连接与权限验收，但 JWT 由测试签发，KV、序列与 Hyperdrive 传输是替身，Redis 令牌存储／撤销关闭；不能称为真实登录或生产 Hyperdrive/浏览器端到端。没有本轮 workerd 运行，前序 Windows 启动访问冲突不因这些测试通过而关闭。

其余代客列表、优惠券、购物车编辑／删除、现金／外部支付及查询并未在本文件做完整运行身份验收，不能称 11 条路由全部完成。其他活动类型、前端代客页面 `FE-003G`、provider、当前候选 Linux/workerd/CI、生产启用仍开放。累计额度政策与积分返还是独立决策，累计限购仍关闭。Checklist 保持 240 完成／164 开放／404 总项。

PostgreSQL 技能推动仅声明权限和独立 LOGIN 验证；Workers 技能推动请求级连接关闭、实际鉴权路径及平台测试边界。读取了当前[Workers 最佳实践](https://developers.cloudflare.com/workers/best-practices/workers-best-practices/)与[Hyperdrive Postgres.js 文档](https://developers.cloudflare.com/hyperdrive/examples/connect-to-postgres/postgres-drivers-and-libraries/postgres-js/)，本机 Firecrawl 不可用时采用官方文档工具；类型验证使用已安装的 `@cloudflare/workers-types 5.20260828.1`，不声称已获取最新类型。

# 优惠券管理与下单并发合同

本文件记录 FE-003L-A3k11c 的本机管理处理链证据，不是整个清单或生产发布批准。

最终关联回归47文件1339项通过、零失败／跳过，包含本组52项；双类型检查通过，71个选定文件哈希和实际回归清单核验一致。跨文件并行数为2，各用例真实多连接竞争保持原样。专用PG清理核验后于2026-09-10 04:55:41.200 UTC停止。此前失败见下文，不与最终通过数累加。

## 执行边界

同一组场景分别使用原始列级轻量夹具和完整 ORM 生成 DDL 的隔离数据库。完整模式通过显式测试数据库工厂复用原确认／创建夹具的种子和HTTP环境，不更改默认模式，不从生产环境变量回退，也不把生成DDL改写成轻量表。

请求实际经过 `adminCouponSave`、`adminCategorySave`／`adminCategoryDel`、`adminBrandSave`，以及 Out 控制器→OutApiService→OutCouponService；买方实际经过订单创建控制器和服务。四个独立、稳定 PostgreSQL 后端以及 `pg_blocking_pids` 确认等待关系，不把请求钩子或计时暂停当成并发证明。管理员／Out／买方身份、KV、取号是合成输入；没有运行正式鉴权中间件、真实provider或生产Hyperdrive。

完整ORM数据库使用已有专用随机数据库帮助器，所有外键、唯一／检查约束和索引均由实际模型DDL生成，0151另行显式安装。低权限运行身份不是本组的证明范围；本组使用隔离维护角色，前轮的局部非所有者证据也不扩展成整条应用权限验收。

## 实际验证场景

| 场景 | 必须观察到的结果 |
| --- | --- |
| 后台保存发生在初次计算之后：范围扩展、转通用、重复编码 | 前两者即使同价也拒绝并整笔回滚；等价编码成功；明确重新确认后可创建 |
| 后台持模板锁、正在等待替换关系 | 买方拒绝；管理请求释放后模板和关系完整提交，无半套数据 |
| 买方通过末端校验后，后台开始替换关系 | 管理请求等待买方实际提交或下游错误回滚 |
| 后台新建／编辑在关系INSERT失败 | 模板及关系一起回滚，保留原业务数据；不要求序列号回退 |
| Out新建先后与买方库存锁竞争 | 新建等待买方结束，或买方等待Out幂等记录提交；新模板的关系触发器不与同事务父锁冲突 |
| Out最后的幂等记录INSERT失败 | 模板、关系和记录全回滚；恢复测试故障后同key重试只创建一次 |
| Out启用持模板锁且反向等待买方库存 | 买方在末端NOWAIT拒绝并回滚，Out随后正常完成；不是无限等待或吞掉死锁异常 |
| Out删除等待买方提交／回滚 | 保留已领券商品范围，reserved数量反映实际结果；重放不新增记录 |
| 后台修改／删除分类，或修改分类／品牌显示信息 | 有效范围变化／缺失时拒绝；显示编辑不使已提交报价事实失效 |
| 后来的分类／品牌保存遇到买方引用锁 | 等待买方提交／回滚后保存；整笔买方回滚不留下库存或优惠券占用 |
| Out新建商品券／分类券，再提供合成已领券 | 实际确认与创建能使用新模板和范围；不冒充真实领取／发券流程 |
| 并发同key Out创建 | 一笔模板、一套关系、一条幂等记录；另一个请求返回同一结果 |

每种结构26个场景，结果及哈希见 [审计证据](../audit/coupon-management-20260910.json)。多阶段和重复运行的测试数不可相加。初始轻量15项通过；完整模式首轮26通过／4失败是两个调用点误用只允许轻量数据库的连接帮助器，修正为已有专用数据库连接接口，未放宽原身份检查。保留失败报告，不将其归为业务缺陷或隐藏。

## 未完成的边界

扩大回归的第一次47文件运行得到1333通过／6失败，原始报告保留：5项发生在完整数据库的afterEach清理阶段，已根据实际Vitest超时栈和PG checkpoint同步记录，为清理单独设置120秒上限；SQL超时和业务测试时限不变。另1项为分销时钟有效对照返回拒绝，旧断言未保留底层异常，不能认定为过期或资源争用；现补充原异常传播，保留原截止时间和业务预期。后续以`--maxWorkers=2`重跑相同47文件，只限制跨文件并行，不改变各用例的独立PG连接、锁屏障或提交／回滚验证。最终结果以审计证据的独立阶段为准，不抹掉此前失败。

- 正式管理／Out鉴权、完整应用最小授权、生产目录、Hyperdrive连接及特权维护隔离。
- 商品编辑、Out分类树编辑及其它管理入口的完整业务组合。本组只证明表中明确列出的入口，不能概括为所有商品／分类／品牌管理均已验收。
- 全局SQL/KV配置、会员权益、经营主体／表单、PHP政策裁定与其它仍开放的清单项。
- 不可变候选的完整CI、发布与回滚。按所有者顺序，清单未全部完成前不提交推送。

Workers／PostgreSQL技能促使本组使用实际事务、稳定独立连接和精确锁屏障，保留提交／回滚双向对照。没有为了测试变绿更改业务公式、锁协议或授权。[PostgreSQL锁冲突与事务释放](https://www.postgresql.org/docs/16/explicit-locking.html)是锁序判断依据。
